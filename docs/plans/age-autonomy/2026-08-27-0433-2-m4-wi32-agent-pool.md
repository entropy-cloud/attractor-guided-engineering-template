---
status: active
mission: age-autonomy-implementation
work-item: M4-WI32
group: "2026-08-27-0433"
verify: [test, verify-age]
---

# 2026-08-27-0433-2 M4 agent 池 + 角色互斥（age-autonomy M4-WI32）

> Source: `docs/backlog/age-autonomy-implementation-roadmap.md` WI32；`docs/design/age-autonomy/04-efficiency.md` §2
> Related: 2026-08-26-1411-2（dispatch-resolve/exec-arm 派发链）、2026-08-26-1954-2（WI29 恢复 resume-or-redispatch）

## Current Baseline

- policy schema 面已在库（M2-WI13 终审 P1-3 落地）：`law-policy.mjs` `AGENT_DEF_FIELDS` 含 `mode/poolKey/idleTtlMinutes/rotateEvery/fixedPrefix/model/requireDistinctModel/downgrade`，pooled 模式强制 poolKey、TTL/rotate 正整数校验；`missions/autonomy.policy.yml` 已声明 `drafter: mode pooled`（poolKey `drafter:{projectRoot}`、idleTtlMinutes 30、rotateEvery 8）与 `executor: mode pooled`（poolKey `executor:{projectRoot}`，:178-181），`reviewer`/`auditor` 为 fresh。
- **executor pooled 声明 vs 运行时裁定**：本 plan 是 `mode` 字段的第一个运行时消费者（今日 `binding.mode` 在 dispatch-resolve 计算 but 零消费）；executor 池化保持**休眠**（WI28 裁定沿袭——plan 执行仍是引擎 run 领地，executor 池消费归 M4-WI33/M5-WI37 后继评估），即本 plan 交付的池层对 execute 派发类型不启用（声明在库、消费留后继，注记成文非静默忽略）。
- 运行时零池化：`plugin/dsh/src/supervisor/exec-arm.ts` `createDispatchAgent`（:230）每次派发新建 continuable 子代理会话——无复用、无空闲 TTL dispose、无代际令牌；`plugin/dsh/src/` 无 `efficiency/` 目录。
- 派发类型出口（exec-arm）：plan-review / closure-audit / deep-audit / draft-plans / execute（reclaim 重派沿 execute 链）；WI28 裁定 plan 执行仍是引擎 run 领地（守夜人 initial-execute 接线否决，executor prompt 组装归 M4-WI33）。
- 恢复面（WI29）：`recovery.ts` resume-or-redispatch + `sessionLivenessOf` live/dead/undecidable 三态；04 §2.3 attemptId 代际令牌 = 「followup 续用 vs 崩溃 resume」判据——当前无实现。
- group 标识面（01 §2 frontmatter `group` 字段）已在账本格式在库；同批 plan 文件名时间戳前缀为回退。
- WI36 门指定 `plugin/dsh/test/pool-lifecycle.test.mjs` ≥10 例（drafter/reviewer 池生命周期 / 空闲 TTL dispose / 代际令牌 / audit 禁入池）。
- 底线：零 engine.js diff、零新增 npm 依赖；plugin `test` script = check-manifest + `node --test test/*.test.mjs` + tsc --noEmit + build-bundle --check + smoke-import 全链绿（plugin src 新模块无 ALLOWED_MODULES 登记面——该通道仅管 engine→assets 模块复制，M3 supervisor 全部 .ts 新件先例零登记）。

## Goals

- 交付 agent 池运行时（04 §2）：`drafter:{projectRoot}` 长期复用、`reviewer:{groupId}` 同批复用；create-on-first-use / followup 续用 / 空闲 TTL dispose / rotateEvery 计数轮换 / attemptId 代际令牌（陈旧 attempt 显式 revoke）。
- 交付独立性红线（P7 + 终审 P2-5）：closure-audit / deep-audit / multi-audit 派发类型结构性禁入池；同一 continuable 子代理不得同时为 drafter 与 reviewer/auditor；同 run 内 auditor session ≠ 任何执行者 session。
- 接线 supervisor 派发链与 WI29 恢复面；`pool-lifecycle.test.mjs` ≥10 例全绿；roadmap WI32 勾选。

## Non-Goals

- 不做 PromptAssembler / fixedPrefix 消费 / hash 台账（M4-WI33；上游文档 hash 变化轮换触发随之）。
- 不做上下文画像（M4-WI34）、独立形态降级（M4-WI35）。
- 不改账本/law/完成语义（效率层 = 优化不是契约，04 §1）；零 law 规则改动、零 engine.js diff、零新依赖。
- 不接线守夜人 initial-execute 派发（WI28 裁定维持）。

## Task Route

- Type: `implementation-only change`（设计基线已批准，落地 04 §2）
- Owner Docs: `docs/design/age-autonomy/04-efficiency.md` §2/§5（agents 语义）、`docs/design/age-autonomy/02-rule-law.md` §3（schema）
- Skill Selection Basis: 无匹配 skills——Skill: none

## Infrastructure And Config Prereqs

- No infra prereqs beyond existing baseline.（池为内存态性能缓存非状态权威，P2 原则：丢失只损失缓存命中，正确性从 git 文件完整重建。）

## Phase 1 — 池模块与生命周期

Targets: `plugin/dsh/src/efficiency/agent-pool.ts`（新目录新模块）、`missions/autonomy.policy.yml`
Skill: none

- Item Types: `Add | Decision`
- Prereqs: 无（与本组 plan 1 无依赖，但执行序在其后——M3 门先收口）

- [x] Add: 池注册表——poolKey 占位替换（`{projectRoot}`/`{groupId}`，group 缺省回退文件名时间戳前缀，01 §2）；成员 = continuable 子代理句柄（经 agents face）；create-on-first-use + followup 续用 + 空闲 TTL dispose（Node timer + `ctx.effect` 生命周期，watchdog 心跳先例；dispose 幂等）；rotateEvery 计数轮换（同池派发达阈值强制换新成员）；内存态 = 性能缓存非状态权威（P2，模块头注成文）。
- [x] Add: attemptId 代际令牌——每次派发持代际令牌；接管/恢复前先验代际，陈旧 attempt 显式 revoke（04 §2.3：同代续用、跨代重派）；池成员崩溃凭持久 session 恢复（`sessionLivenessOf` 三态对接）。
- [x] Add: 组作用域——同组顺序 review 复用同一 reviewer、跨组必新启（组是复用最大粒度，杜绝跨批次判断污染，04 §2.2）。
- [x] Decision | Add: live policy `reviewer` 翻 `mode: pooled`（poolKey `reviewer:{groupId}`、idleTtlMinutes 30、rotateEvery 8——镜像 drafter 先例，04 §2.2 组内轮换 K 有值）。默认 = 翻（04 §2.1/§2.2 字面设计意图；anchoring 偏置由 rotateEvery + 跨组必新启兜底）。备选 = 保持 fresh（部署保守选择，池能力交付但本仓不启用）——否决理由：设计基线已批准且轮换护栏在案。残险 = 同组连续评审的风格收敛偏置 → 轮换阈值兜底 + 观测。policy yml 为 P8 保护路径，本 plan active 即合法例外（自指先例）。

Exit Criteria:

- [x] 池生命周期行为可测：创建/续用/TTL 摘除/轮换/代际 revoke 各有正向用例
- [x] `pnpm --prefix plugin/dsh test` 全绿（manifest + tsc + bundle + smoke 全链）
- [x] No owner-doc update required（04 §2 为已批准设计；CONTEXT.md/packaging 增量随 Phase 3 回写项落地）

## Phase 2 — 独立性红线与派发链接线

Targets: `plugin/dsh/src/supervisor/exec-arm.ts`、`plugin/dsh/src/supervisor/recovery.ts`、`plugin/dsh/src/efficiency/agent-pool.ts`
Skill: none

- Item Types: `Add`
- Prereqs: Phase 1

- [x] Add: P7 审计禁入池——closure-audit / deep-audit 派发类型结构性绕过池（无视 agent `mode` 配置，池层硬拒 + 注记；multi-audit 非 runtime 派发类型 = deep-audit 的 prompt 文件面，随 deep-audit 覆盖）；结构独立审计每次独立新派发。
- [x] Add: 角色互斥（终审 P2-5）——同一 continuable 子代理不得同时为 drafter 与 reviewer/auditor（池角色标签登记，冲突派发拒绝）；同 run 内 auditor session ≠ 任何执行者 session（执行者集 = run 内 plans 的 claim holders（账本 frontmatter 派生）∪ 池角色登记 executor 标签；审计派发时校验候选 session）。
- [x] Add: exec-arm 接线——池挂点在 `createDispatchAgent` 内部（recovery redispatch 直调该函数自动经池，无绕过孔）：plan-review 派发经 `reviewer:{groupId}` 池、draft-plans 经 `drafter:{projectRoot}` 池；execute / closure-audit / deep-audit 保持现 fresh 建会话路径（executor pooled 声明休眠裁定见 baseline，零行为变化）；`dispatch-resolve` 零改动（池在解析链下游）。
- [x] Add: 恢复互操作——WI29 resume-or-redispatch 的 followup-vs-redispatch 判据接 attemptId 代际（同代续用 / 跨代重派 + 池成员摘除）；headless 无 agents face 显式降级路径保持。

Exit Criteria:

- [x] 审计派发类型在任何 policy 配置下都不经池（反向用例钉住）
- [x] supervisor 既有测试（core/trigger/dispatch/terminal/failures/continuous/recovery）零回归
- [x] `git diff --stat tools/mission-driver/src/engine.js` 为空 + 双 package.json 零依赖新增

## Phase 3 — Proof 与回写

Targets: `plugin/dsh/test/pool-lifecycle.test.mjs`、`docs/backlog/age-autonomy-implementation-roadmap.md`、`tools/mission-driver/CONTEXT.md`、`docs/architecture/dsh-plugin-packaging.md`、`docs/logs/2026/08-27.md`
Skill: none

- Item Types: `Proof | Add`
- Prereqs: Phase 2

- [x] Proof: `plugin/dsh/test/pool-lifecycle.test.mjs` ≥10 例（WI36 门下限）：drafter/reviewer 池生命周期 / 空闲 TTL dispose / 代际令牌（同代续用、跨代 revoke）/ rotateEvery 轮换 / audit 禁入池 / 角色互斥（drafter ∩ reviewer/auditor 空 + auditor ≠ executor）/ 组作用域（同组复用、跨组新启）/ 池复用下评审租约行为（同 sessionId dispatch 行与 writer-identity 最新行租约相容）全绿。
- [x] Proof: `./verify-age.sh` L1+L2+L2.5 GREEN（插件/引擎/真值表计数只增不减）。
- [x] Add: roadmap WI32 勾选 + 行内证据指针；`> Last Updated` 头同步。
- [x] Add: CONTEXT.md「效率层」增量段 + packaging doc（src 树 efficiency/ 条目、test 树 pool-lifecycle、ALLOWED_MODULES 计数、changelog）；`docs/logs/2026/08-27.md` 条目。

Exit Criteria:

- [x] pool-lifecycle ≥10 例 0 失败；verify-age 全绿
- [x] roadmap WI32 `[x]` + 证据注记
- [x] `docs/logs/` updated

## Draft Review Record

- dispatch review #review-2026-08-26-130203-mission-driver-2026-08-27-0433-2-m4-wi32-agent-pool-1-b81d4f60 to ses_fc0355c47ffeDjU8Qj76H3rUDH
- 2026-08-27：iteration 1，共识 acceptable-with-changes #review-2026-08-26-130203-mission-driver-2026-08-27-0433-2-m4-wi32-agent-pool-1-b81d4f60（两阻塞项：B1 executor pooled 声明 vs 运行时未裁定、B2 ALLOWED_MODULES 登记对 plugin src 模块不可执行；另五项非阻塞建议）
- 2026-08-27：iteration 2，共识 acceptable-as-is #review-2026-08-26-130203-mission-driver-2026-08-27-0433-2-m4-wi32-agent-pool-1-b81d4f60（B1/B2 确认解决——baseline :16-17 executor 休眠裁定 + :23 通道口径修正；multi-audit 括注与 .ts 计数两 cosmetic 残项随后落地；status 翻 active）

## Verification

- pass test 2026-08-26-130203-mission-driver basisHash=c6d5636fbb3229a43d180de5927f7ba0ab636a61d7e00ed46c8985fb9ee22780 exit=0
- pass verify-age 2026-08-26-130203-mission-driver basisHash=c6d5636fbb3229a43d180de5927f7ba0ab636a61d7e00ed46c8985fb9ee22780 exit=0

## Closure

- dispatch audit #audit-2026-08-26-130203-mission-driver-2026-08-27-0433-2-m4-wi32-agent-pool-1-9b014341 to ses_auditor_2026-08-27-0433-2 models={exec:zhipuai/glm-5.2,aud:zhipuai/glm-5.2}
- accepted #audit-2026-08-26-130203-mission-driver-2026-08-27-0433-2-m4-wi32-agent-pool-1-9b014341：独立收口审计（ses_auditor_2026-08-27-0433-2，单模型部署 lineage 声明 = policy 02 §4.9 downgrade: single-model）通过——21 项计数域全勾与 live 工作区逐项对账：① Phase 1 池模块：`plugin/dsh/src/efficiency/agent-pool.ts` 在库且非空壳（角色池 `drafter:{projectRoot}`/`reviewer:{groupId}` poolKey 占位替换 + `groupScopeOf` 组作用域三腿 / create-on-first-use + followup 续用 + 空闲 TTL dispose（可注入 timers、dispose 幂等）/ rotateEvery 计数轮换 / attemptId 代际令牌 `attemptStale` 单一判据面）；live policy `reviewer` 已翻 `mode: pooled`（`missions/autonomy.policy.yml` :171-175，M4-WI32 Phase 1 Decision 注记在案）。② Phase 2 红线与接线 live：`exec-arm.ts` `createDispatchAgent`（:277）池挂点——`pool.acquire`（:301）+ `registerRole` 角色互斥登记（:332）+ executorSessions 审计候选校验（:338）；三派发出口（:444/:677/:739）全经该函数，recovery redispatch 直调自动经池无绕过孔；`recovery.ts` :301 消费 `attemptStale`（同代续用/跨代重派）；watchdog 持 mount 级池（`watchdog.ts` :316 `createAgentPool` + stop() 幂等 dispose）；audit 禁入池反向用例（auditor 声明 pooled 也恒 fresh）钉在 `pool-lifecycle.test.mjs`。③ Phase 3 Proof 与回写：`plugin/dsh/test/pool-lifecycle.test.mjs` 13 例 0 失败（WI36 门 ≥10 下限）；roadmap WI32 `[x]` + 行内证据指针 + Last Updated「M4 首片」句；CONTEXT.md「效率层 agent 池」段 + packaging doc 四面（状态头/src 树 efficiency/ 条目/test 树 pool-lifecycle/AGENT POOL 面）+ `docs/logs/2026/08-27.md` 收口条目均在库。④ 审计者独立复跑机械验证：`pnpm --prefix tools/mission-driver test` **923/923 pass + prompt-check OK** exit 0；`./verify-age.sh` **L1+L2+L2.5 GREEN** exit 0（插件 378→391 / 真值表 116 只增不减）。⑤ 不变量与 Deferred 诚实性：`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）、双 package.json 零依赖新增；唯一 Deferred「上游文档 hash 变化触发的组内轮换」为真实域外残项且后继在册（Successor M4-WI33），无 in-scope live 缺陷或契约漂移藏匿于 Deferred。结论：21/21 计数域全勾 + 双 pass 行 basisHash=c6d5636f…e22780 与当次 basis 绑定 + 本回执对满足 01 §5.2 完成派生公式。

## Deferred But Adjudicated

### 上游文档 hash 变化触发的组内轮换

- Classification: `out-of-scope improvement`（本片交付计数轮换腿）
- Why Not Blocking Closure: hash 面依赖 fixedPrefix 文件 hash 台账 = M4-WI33 PromptAssembler 交付面；设计 04 §2.2 双触发之一，计数腿已可独立执法
- Successor Required: yes（M4-WI33 落地 hash 台账后接线）
