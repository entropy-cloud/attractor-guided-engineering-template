---
audit-rounds: 2
---

# AGE Autonomous Run Implementation Roadmap

> Last Updated: 2026-08-27（**M5 最终收口：WI40 最终 Verification Gate 全绿 + Follow-up P2 清偿——roadmap 全 done（`grep -c "^- \[ \]"` → 0 实测，M1–M5 五里程碑 44 WI 全勾）**——plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md` 三 Phase 收口：Phase 1 monitor extends 合并修复（三配置读面经 `loadMission` 单一合并，`monitor.test.js` +7 用例，引擎 954→961/0）清偿 Follow-up 末条未勾项；Phase 2 六门实测全绿（verify-age L1+L2+L2.5 GREEN 961·420·116 / test 961/0 / mission-check exit 0 / **verify:e2e 真实宿主 PASS exit 0** / gate-check 自指 + WI36 语料 exit 0 三硬门 allow / 勾选前 grep=1）；Phase 3 独立 subagent 收口审计 **findings=none**（M1–M4 全部 Verification Gate 复跑绿 + 三硬门全 allow face；回执 = plan `## Closure` 同 id 配对，`docs/audits/age-autonomy/<final-closure>.md` 旧指针退役裁定在案）；此前：**M5 第三片：WI39 design+architecture owner-doc 一致性收口**——plan `docs/plans/age-autonomy/2026-08-27-1023-3-m5-wi39-ownerdoc-consistency-closure.md` 三 Phase 收口：清点矩阵 42 行（rg 全量命中 33 行零遗漏对账 + Current Baseline 已知项/人工清点补充 8 行 + Non-Goals 归属注记 1 行；三态 = closed-by-evidence 11 / still-deferred 6（触发条件复核均不变）/ superseded 1 / 已核无需动作 24，矩阵本体落 `docs/logs/2026/08-27.md`）+ 四 Fix 项（02 §4.7 引擎侧 law 内核放置 + P8 保护姿态 as-built 注记——收口 0950-1 WI21 Deferred「law 内核 P8 覆盖缺口」/ 05 §2.2 `mdcontrol.unlock`「目标能力」→ M3-WI28 落地指针 + continuous 行复核（`mdcontrol.stop` 仍目标形态）/ 00 §7 P0–P4 落地状态注记（P4 → 06 清单 + WI37 总判定）/ 06 清单 #9+G3 WI38 已收口刷新 + 04 §6/05 §4 D1 裁定指针 + baseline §Public Exports EXIT_MAP M5-WI38 增量补登记；packaging doc 全条目复核准确零编辑）；零代码 diff、零新增依赖、零契约语义变更（全为事实性增补）；验证 test **953/953 / 0 失败** + verify-age **L1+L2+L2.5 GREEN** + gate-check 自指 exit 0）；此前：**M5 第二片：WI38 EXIT_MAP 显式增补 partial/blocked → exit 3**——plan `docs/plans/age-autonomy/2026-08-27-1023-2-m5-wi38-exit-map-partial-blocked.md` 两 Phase 收口：`src/exit-map.js` EXIT_MAP 11→13 键（supervisor 终态词入表 = 前瞻防护独立形态暴露面，exit 3 = 新类「终态非完成、需人工处置」，03 §8 终态映射纪律的独立立项变更）+ `EXECUTION-PRINCIPLE.md` §11 表两行 + 表尾语义句同步 + `exit-map.test.js` 13→19 例钉住（partial→3 / blocked→3 / 既有 11 键逐行回归 / fall-through + 头注一致性）；plugin assets 副本经 build-bundle 同步；零引擎行为变更（engine.js 零 diff、引擎 `_result` 不发射两词、零新增依赖、monitor web 零触碰）；验证 `pnpm --prefix tools/mission-driver test` 953/953 / 0 失败 + `./verify-age.sh` L1+L2+L2.5 GREEN）；此前：**M5 首片：WI37 引擎退役判定清单收口**——plan `docs/plans/age-autonomy/2026-08-27-1023-1-m5-wi37-engine-retirement-decision-checklist.md` 两 Phase 收口：清单工件 `docs/design/age-autonomy/06-engine-retirement-checklist.md` 落库（覆盖矩阵 12 行 + D1–D7 裁定三要素齐备 + 总判定「**引擎留任主后端（条件退役）**」+ 缺口前置清单 G1–G6；00 §7/§8 + 03 §10 + CONTEXT.md 登记；WI38 增补中如实登记不阻塞；零代码变更，test 947/0 + verify-age L1+L2+L2.5 GREEN）；此前：**M4 收口：WI36 Verification Gate 三门全绿——M4 区块 5/5**——plan `docs/plans/age-autonomy/2026-08-27-0558-3-m4-wi36-verification-gate.md` 两 Phase 收口（prereq 0558-1/0558-2 `gate-check --law` derived completed 核验通过；三门实测 pool-lifecycle **13/0** · prompt-assembly **16/0** · context-profile **13/0** 全 exit 0 超门限；观测项缺宿主 env fail-fast exit 1 括号语义分支如实留档 +「机制落地即收口，收益待观测」原句；效率层四件套（池化/组装/画像/独立形态降级）完成态，CONTEXT.md 收口注记在案）；此前：**M4 第四片：WI35 独立形态降级统一裁定：池化 → `--session` 续用 + 前缀纪律**——plan `docs/plans/age-autonomy/2026-08-27-0558-2-m4-wi35-independent-form-degradation.md` 两 Phase 收口（04 §6 as-built 承载裁定）——① 承载裁定：独立形态效率面 = 步内 `--session` 续用（correction/parse 共会话，`lastSessionId` 唯一消费面）+ 前缀纪律；**跨步续用不在库且不交付**（`engine.js` 三 `_executeAgentStep` 调用点传 null）——CLI 会话池（`PoolAgentsFace` DSH 独有 + attemptId 代际缺失陈旧上下文风险 + 违反零引擎 diff）与 engine threading（零引擎 diff 底线 + 会话跨步累积改变 run 语义）双备选否决 → 后继 M5-WI37 二选一裁定；② 步内续用覆盖核对引用一致零缺面（runner-routing/prompt-cmdline-limit/step-executor/transitions/parse-fallback 五处行号逐项核对）零重复造测；③ 前缀纪律审计 11/11 模板零重排（唯一大易变负载 `{{runSkeleton}}` 居 run-postmortem 固定指令前导之后非前置）+ 钉住测试 `test/prompt-prefix-discipline.test.js` 15 例（live 渲染路径 `resolveTemplateVars` 双渲染共享固定前缀字节断言 + 三类注册表 drift gate），引擎 929→944；④ 如实成文：04 §6 as-built 注记 + 05 §4 ◐ 行同步 + CONTEXT.md 降级段 + `independentChannelOf` 残项文字核对零 seam 编辑；`./verify-age.sh` L1+L2+L2.5 GREEN / web typecheck/build 绿 dist 零漂移 / lint:prompts OK / 零引擎 diff 零新依赖；此前：**M4 第三片：WI34 上下文画像 context-profile 执行化**——新 `plugin/dsh/src/efficiency/context-profile.ts`（04 §4：工件 `docs/references/context-profile.json` schema v1 `{version, seededFrom, updatedAt, entries:[{path,reads,lastSeenAt}]}`——**不进 `missions/`**（mission scanner `*.json` 全量扫描污染面）项目所有进 git、非完成证据面（效率缓存 P2）不经 law 门禁，写盘纪律 = 防抖 + tmp+rename 原子写；首启由 AGENTS.md「Read This First」清单种子化（仅直接路径行入表、「listed in …」间接引用行跳过——间接面经该文件自身嵌入覆盖）；未知 version = 显式注记 + 保守重建（重新种子化）不静默沿用，序列化确定性同输入字节恒等；**挖掘** = watchdog `emitTerminalEvent` 链尾 run-terminal 事件触发一次（单飞守卫复用、fail-soft——采集/归并/写盘异常只产回执不影响终态回执与停派、`profileMining` 挂载选项注入 io/sessionEvents 供测试），数据源三分层各有消费面——① 池成员 `sessionEvents` 句柄透出 + `memberSessionEvents()` 池面（零池语义改动）归集 `tool/call` 事件 × read/grep/glob 类文件参数（事件 schema 以 live 宿主 SessionEventMap 钉住 `{turn,step,callId,name,arguments:string}`）② `_tmp/<runDir>/run-state.json` steps[].promptFile 文本挖路径（存在性过滤）③ Reflexion `memory/runs.md` 同法，缺面 fail-soft 注记非静默；headless（无 agents face）= 显式降级注记保留种子表不挖掘（独立形态完整面归 0558-2/WI35）；**防抖裁定落地** = 空 tally 不写盘 ∨ top-N 集（按生效 topN 截断的 path 集，排序后集等价判定）不变不写盘——集内 rank 波动不触发（消费面只按集取材）、进出振荡天然抑制；**消费** = `law-policy.mjs`（tools+assets 双副本）fixedPrefix 增第四 kind `profile`（`{kind, ref, topN?}`，topN 正整数且仅 profile kind 合法、缺省 5 DSL 逐 agent 覆盖）+ live policy drafter 追加 profile 块（P8 例外 = 本 plan active 先例沿袭）+ 仓库首启种子工件入库；`resolveAssemblyBlocks` 钉 `profileRoot`、`filesOfBlock` 读工件按 reads 降序/path 升序取 top-N 逐文件展开——复用 embedStamp/maxFileBytes/hash 台账/CONTINUE dedup 全部 file 语义，工件缺失/未知 version/无 projectRoot = 显式 note 不崩溃；角色显式 DSL 声明优先、未声明部署薄指针 prompt 逐字节不变（向后兼容钉）；测试 `context-profile.test.mjs` **13 例 0 失败**（WI36 门 ≥8 下限），引擎 925→**929**（profile kind schema +1 与前批 +3）/ 插件 407→**420** / 真值表 116 只增不减，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖）；此前：**M4 第二片：WI33 PromptAssembler 双模式组装执行化**——新 `plugin/dsh/src/efficiency/prompt-assembler.ts`（04 §3：`assemble(mode, spec, dynamicCtx, ledger, io)` 纯函数——FRESH = fixedPrefix 块（text/file/dir 三 kind）聚合 + 动态块；CONTINUE = 经 per-agent hash 台账比对的 deltaEmbed（未变文件跳过、变更文件全量重发）+ 动态块；embedStamp `<file path="{path}" hash="{hash8}">{content}</file>` schema 可覆写模板串，`{hash8}` = sha256 前 8 hex（算法源与 `computeBasisHash` 同源）；目录全文嵌入（kind dir + maxFileBytes 逐文件上限，超限显式注记不静默截断）；前缀纪律执法（固定字节最前、易变字节后置、marker 指令属动态后缀——两次 FRESH 字节恒等钉住）；hash 三用（dedup / dispatch 时 hash 不符重发 / grep 可审计）；compaction 对抗 = 台账 `__sends` 计数器 + 每 8 次 CONTINUE 周期性全文重发）；`law-policy.mjs` 增 `assembly:` 段 schema（embedStamp 三渲染槽校验 + continueDelta 布尔）+ live policy `assembly` 块 + drafter `fixedPrefix` charter（AGENTS.md persona text + project-context.md 嵌入）；消费接线：池成员双模式（create ⇒ FRESH / followup ⇒ CONTINUE，per-member 台账随成员生灭、crash 后首发重发 FRESH 保守向）+ exec-arm `dispatchPromptFor` 四 followup 位统一（dispatchAgentExit / draft-plans / reclaim / recovery redispatch；未声明 fixedPrefix 部署薄指针 prompt 逐字节不变向后兼容钉住）+ WI32 Deferred 收编 = charter hash 变化强制换池成员（04 §2.2 第二触发腿，`charterHashesDiffer` 单一实现）+ `native-executor.ts` `assemblyPrefix` config 面（run 子代理首步 FRESH / 后续步 CONTINUE 同源组装，engine prompt（promptsDir 链已解析）为动态后缀——policy 叠加不取代 04 §7，镜像 nativeModelSelection config-face 先例）；测试 `prompt-assembly.test.mjs` **16 例 0 失败**（WI36 门 ≥12 下限），引擎 923→**925**（assembly schema +2）/ 插件 391→**407** / 真值表 116 只增不减，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖）；此前：**M4 首片：WI32 效率层 agent 池 + 角色互斥执行化**——新 `plugin/dsh/src/efficiency/agent-pool.ts`（04 §2：角色池 `drafter:{projectRoot}` 长期复用 + `reviewer:{groupId}` 同批复用（组作用域 `groupScopeOf` = frontmatter `group:` → 文件名时间戳前缀回退 → 裸 stem；跨组必新启）；成员 = continuable 子代理句柄：create-on-first-use + followup 续用 + 空闲 TTL dispose（可注入 timers + watchdog stop 幂等 teardown）+ rotateEvery 计数轮换 + 崩溃凭持久 session 换新；attemptId 代际令牌 `attemptStale` 单一判据面接 WI29 恢复扫描——同代续用 / 跨代重派 + 显式 revoke + 成员摘除；独立性红线在池层执法：P7 审计禁入（closure-audit/deep-audit/multi-audit 结构性永不入池，无视 agent mode 配置硬拒 + 诚实注记）+ 角色互斥登记表（drafter ∩ reviewer/auditor = ∅ 冲突派发拒绝）+ 同 run auditor ≠ 任何执行者（执行者集 = plans claim holders frontmatter 派生 ∪ 池 executor 标签，审计派发候选校验）；池挂点在 `createDispatchAgent` 内部（recovery redispatch 直调自动经池无绕过孔）；execute 池化声明休眠（WI28 裁定沿袭，注记指向 M4-WI33/M5-WI37，executor 标签仍登记）；live policy reviewer 翻 `mode: pooled`（TTL 30/rotate 8 镜像 drafter 先例）；内存态 = 性能缓存非状态权威（P2，重启清空成文）；测试 `pool-lifecycle.test.mjs` **13 例 0 失败**（WI36 门 ≥10 下限），插件 378→**391** / 引擎 923 / 真值表 116 只增不减，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖）；此前：**M3 收口：WI31 Verification Gate 四命令实测 + gate-check `--law` 执法面交付**——supervisor-trigger 43/0 · supervisor-recovery 18/0 · verify:e2e:continuous 无宿主 env fail-fast exit≠0（括号语义 + WI28 真实宿主 PASS standing evidence）· `gate-check <WI31-plan> --law` exit 0（9 enforce gate 全 allow；`--law` = 第四 CLI 模式：owning mission 真实 policy 全量 gates + 完成派生 + 队列谓词视图，plan `2026-08-27-0433-1`，引擎测试 gate-check-law.test.js +10 → 923/0，零引擎 diff 零新依赖）；此前：M3 第六片：WI30 卡死检测 + 往返检测 + 停滞指纹（账本 hash + 活动信号）执行化**——`plugin/dsh/src/supervisor/stagnation.ts` 纯函数检测器（`computeLedgerFingerprint` = 排序后 (planPath → basisHash) 行 + roadmap 全文 hash 聚合，computeBasisHash 完成公式同源；**指纹域 = basisHash 域**（frontmatter + Phase + Closure Findings）——DRR/Verification/Closure 追加在域外、dispatch/pass 行不重置指纹；`observeStagnation` 纯转移：一轮停滞 = 指纹不变 ∧ 活动窗口（默认 = 一个心跳周期）内 noteActivity 零命中——**活动信号必须参与**（有活动轮清零重积，长任务未落盘不误判，03 §7 字面正例钉住）/ **往返检测腿** = per-plan 状态连续交替翻转计数，两态间 ≥ K 完整往返（2K 翻转）无终态进展 → 饱和注入 `{rounds: threshold, threshold}` → **同一 R4 出口**（03 §7「停滞检测收口」单出口纪律；第三态破对重计/终态 disposition 重置/离场 plan 清行））/ 承载 = watchdog 持有内存 scratch 态（03 §6 归零成文接受——重启重积保守向））+ **双入口同注入（1411-3「两入口同一实现」契约保持）**：检测器输出为 watchdog 持有单点 `stagnationFact`——周期末端求值（既有入口）与 exec-arm `forwardTerminalDecision` 声明面入口（新 `ExecArmOptions.stagnationFact` 回调透传）同读同一状态，零单周期分歧（交叉用例钉住）；**N/K 阈值策略配置**：policy limits 单键 `stagnationRounds`（`law-policy.mjs` `resolveStagnationRounds` 双源：policy 权威 / `flows/mission-driver.json` 顶层回退键 / 双缺默认 10 = 30s 心跳 × 10 ≈ 5 分钟墙钟换算成文；0 = 检测器 off）+ K = `max(2, floor(N/5))` 同键派生（无第二配置键，02 §3 单键单义）+ `missions/autonomy.policy.yml` limits 行与头注换算句 + host-adapter `MissionLawContext.stagnationRounds` 透传；检测器命中首跳 `stagnation-detected` 观察回执（fingerprint/ping-pong 双措辞），终态沿 1411-3 既有 setTerminal 回执链零终态面改动；测试 `supervisor-recovery.test.mjs` **9→18 例**（WI31 门 ≥8 超额：指纹域钉住 / N-1·N 边界 e2e / 活动参与 / 往返 unit+e2e（hash 盲腿）/ 重启清零 / 双入口交叉 / partial/blocked 区分恢复语境变体 / 双源矩阵 + schema），插件 378 / 引擎 910 / 真值表 116 只增不减，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖；此前：M3 第五片：WI29 崩溃恢复扫描执行化：resume-or-redispatch + 幂等面/评审租约最新行作答 + deep-audit 同 occurrence 重派零自增**——`plugin/dsh/src/supervisor/recovery.ts`（恢复处置表逐字收录：①claim TTL 未到期不动/已过期走既有 reclaim trigger ②dispatch 无结论 = 本面 resume（原会话 followup 零新行）∨ redispatch（新 nonce8 行 + 旧行 append-only 保留）③awaitingClosure = 1411-2 既有 trigger 面；引擎侧孤儿归 `reap-orphans.mjs` 零引擎 diff；per-mount handled 集 = 连续恢复周期零重复动作 + headless 无 agents face 显式降级（不可判 → 观察回执不动作））/ `watchdog.ts` recovery 周期前置分支（trigger='recovery' ∧ 未终态；不受 continuous 门——崩溃收尾非新无人值守推进）/ `dispatch-resolve.ts` `dispatchAlreadyRegistered` + `law-rules.mjs` writer-identity 评审租约（tools+assets 双副本）**最新行作答**单一语义面（stale 行不占位——redispatch 后 plan 写面不永久锁死，1411-2 显式移交的第二消费面收口）/ `audit-rounds-overflow` 同轮次重派豁免（重派复用已付轮次零自增——耗尽预算后死会话在飞仍可重派，「永不重派」死锁解除，01 §3.1）/ 02 §4.6 不计清单第四行（恢复 redispatch 不计 failures——「不把单次崩溃计为计划失败」）；exec-arm 残项收口（WI29 移交注记 + 降级回执措辞 + createDispatchAgent 导出）；02 §4.6/03 §6/CONTEXT.md/packaging doc 同步；残险成文（stale 判定仅恢复语境——宿主存活期会话死亡等重启或 WI30 R4 兜底）+ Deferred「TTL 未到期死会话 claim 提前回收」归 WI30；测试 `supervisor-recovery.test.mjs` 9 例 + 真值表 113→116，插件 367 / 引擎 910，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖；此前：M3 第四片：WI28 连续模式 opt-in：roadmap 即队列 + `mdcontrol.continuous`/`mdcontrol.unlock` 路由 + 终态回执收口**——`plugin/dsh/src/` 增连续模式面（`decision-core.ts` `applyContinuousGate` opt-in 门——off 时 dispatch 型 execute-posture 决策降级观察回执，meter-write/receipt 决策不受门；**行为收紧**：triggers 在场即 execute 的 1411-2 现状收紧为显式开启（03 §4 opt-in 纪律），mount log BEHAVIOR TIGHTENING 句 + CONTEXT.md 增量段钉住可发现性，headless 经 bundle config `supervisor.continuous` 预启用 = 等价显式声明 / `watchdog.ts` per-root 内存 continuous 标志（重启即清，ActiveRunGuard 先例）+ `setReceiptTarget` 终态回执投递目标 / writer `unlockPlan`（held→active 同写 failures=0+hold/claim 清除）+ `disposePlan`（cancelled|superseded|deferred 终态 disposition）/ `emitTerminalEvent` 链式沿 03 §3 边 2 接线——continuous on 时一次立即再评估周期，mission 终态停派优先）+ 第六路由 `mdcontrol.continuous`（toggle/查询 → enabled/posture/mounted）+ 第七路由 `mdcontrol.unlock`（经守夜人 writer role=supervisor 执行，deny 结构化透出；law-rules.mjs :397/:407 过渡注记消解——规则行为零改动）；终态回执投递目标 = continuous 开启会话（A8 尽力投递既有）；e2e `verify:e2e:continuous` npm script + `scripts/e2e-continuous.mjs` + fixture `e2e-continuous.cordis.yml`（缺 env fail-fast exit ≠ 0 CI opt-in 不阻塞——WI31 门铺垫；`DSH_E2E_CONTINUOUS=1` 真实宿主三连跑**实跑 PASS**：off 观察 → on 账本派发链落地 → fixture 驱动 R3 blocked 终态回执 + late-draft 停派）；执行腿裁定 = plan 执行仍是引擎 run 领地（0815-3 沿袭，Deferred 后继 M4-WI33/M5-WI37 在册）；测试 `supervisor-continuous.test.mjs` 15 例，插件 357 / 引擎 910 / 真值表 113，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖；此前：M3 第三片：WI27 终态规则 R1–R4 + failures 归因桶 + maxFailures 双源收口**——`plugin/dsh/src/supervisor/` 增两件（terminal-rules.ts R1–R4 序贯求值核心：partial/blocked 显式区分 + 复合声明值单点归一 + stagnation 注入接口（WI30 供给）+ 两入口同一实现（watchdog 周期求值 × policy terminal 声明面）/ failures.ts 三桶归因 + 熔断 applyCircuitBreaker：exec-arm 三失败点接线 + writer holdPlan held 同写清 claim + 全 held 经同一求值核心 R3 终态化）；终态落点 = run-terminal 回执 + mdcontrol.status 透出 + 循环停派（mount 粘滞 + 跨重启幂等重评零新 store，零引擎面）；maxFailures 双源 resolveMaxFailures（policy 权威 / flows/mission-driver.json 顶层回退键 / 双缺默认 3——终审 P2-3 收口 + policy 头注 schema-reserved 句兑现）；0815-3 WI17 残项「R1/R3 终态执行」WI27 侧收口 + Follow-up round-1 P2「mission config 前向引用」自愈注记 absorbed-by 本片；测试 supervisor-terminal 22 + supervisor-failures 9 + supervisor-trigger e2e 三例升 M3-WI27 执行面，插件 342 / 引擎 907 / `./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖；此前：M3 第二片：WI26 trigger 执行 + 派发面收口**——`plugin/dsh/src/supervisor/` 增三文件（trigger-eval 求值器 14 谓词双域 fail-纯函数 / dispatch-resolve 派发解析链：三字段模型绑定补 native-executor ModelSelection gap + `agent:` 覆盖路由 + requireDistinctModel 运行时三态（sameModelPair 与静态面同源）/ exec-arm 七出口执行臂：verify-runner 直跑写 pass 行 + 派发登记 + deep-audit 预算·计量同写 + reclaim 换发 + P2-1 续期写账本 bounded + terminal 转发 1411-3），`decide()` triggers 段在场切 execute posture（无段保 WI25 观察姿态，supervisor-core 23 例零改动）；`prompts/draft-from-roadmap.md` step 4 重写（Follow-up P2 收口——评审独立性回到流程结构）；0925-1/0950-3/WI17 残项三 Deferred 显式收编 + 0815-2 WI14 残项（requireDistinctModel 运行时）收口；测试 supervisor-trigger 43 / supervisor-dispatch 22 / 插件 311 / 引擎 907 / 真值表 113 全绿，`./verify-age.sh` L1+L2+L2.5 GREEN，零引擎 diff 零新依赖；此前：M3 首片 WI25 守夜人五职责 seam——`plugin/dsh/src/supervisor/` 五文件（decision-core 纯函数契约/watchdog 心跳+事件沿+单飞+恢复扫描/唯一机器字段写者 Q4 ③=守夜人 CAS+原子落盘+写前 law 自检/receipt 最小面 A8/第二 cordis service publication `mdsupervisor` 零宿主面），dispatch 恒 no-op 观察直至 WI26，测试 supervisor-core 23 例、插件 246/真值表 113/引擎 907 全绿；M2 收口：WI23 CI 门禁接线——`.githooks/pre-commit` 暂存区触发面 + `verify-age.sh` L2.5 law gates 三面 + `age-ci.yml` 触发路径扩集（账本/执法数据/hooks 路径）；WI24 Verification Gate 四命令真实绿——policy 18 gates / 真值表 113 用例 0 失败 / 三硬门全 allow / mission-check exit 0；同批清偿 round-1 P2 退役格式段（Status Values 表删除、ready 散文退位）；同日早前：WI22 证据面重建——plan-status-gate 退役 + `legacy-plan-freeze` 收编 + 引擎 legacy 审计通道退役 + duplicate-anchor 结构 error，测试 904/223；WI21 路径与结构护栏 + P8 执法层自护——work-item 注册对账/路径护栏/回写护栏/自护规则 + plan-structure 切 enforce，真值表 107 例；WI43 架构 owner-doc 契约同步——ledger 三模块登记进 baseline §Public Exports + packaging 四漂移修复，计数按执行时 live 26 核定；WI42 校验器生产读面接线 + WI44 verify:[] 空真封堵；2026-08-25：WI41 回执感知路由修复 + Follow-up「引擎读面 defaultVerifyKeys」P2 清偿；新建于 2026-08-24：基于 `docs/design/age-autonomy/` 设计基线（已 human 批准 2026-08-24 转 supported baseline）+ 审计记录 `docs/audits/dsh-plugin/2026-08-24-age-autonomy-design-audit.md` + 终审 `docs/audits/dsh-plugin/2026-08-24-age-autonomy-design-final-review.md`；同日 human 提议加严「自动验证」硬约束，已在每个 milestone 末位插入 Verification Gate WI）
> Source: `docs/design/age-autonomy/{00-overview,01-file-ledger,02-rule-law,03-supervisor,04-efficiency,05-usage}.md`、`docs/analysis/2026-08-24-{0003,0004}`、`docs/discussions/2026-08-24-roadmap-plan-frontmatter-reform.md`、`docs/plans/00-plan-authoring-and-execution-guide.md`、`docs/backlog/00-roadmap-authoring-guide.md`
> Owner: `docs/design/age-autonomy/`
> 关联 mission: `missions/age-autonomy-implementation.json`

## Purpose

驱动把 `docs/design/age-autonomy/` 设计基线（supported baseline，human 批准 2026-08-24）落进 mission-driver 引擎 + dsh 插件，实现 Ledger · Law · Supervisor · Efficiency 的目标形态。roadmap 决定顺序与验证门，design 决定契约，audit 决定验收，mission 决定执行上下文。

## 核心纪律

1. **零引擎 diff 为底线**（`docs/design/age-autonomy/00-overview.md` §3）：门禁与 Supervisor 全部沉淀在 `plugin/dsh/` 侧，引擎核心不被改写。受支持行为仍以 `docs/architecture/mission-driver-baseline.md` 为准。
2. **自动验证先行（Verification Gate 硬约束）**：每个 milestone 末位设 `Verification Gate` 一项（[ ]，落地后 [x]），必须为可执行的命令 + 真实断言。**Verification Gate 未勾 = milestone 未收口**——不允许 chat/口头式 close。每条 claim 必须有 grep / 命令 / 退出码与之配对。
3. **CI merge-blocking 三层**（L1 单测 → L2 双后端矩阵 → L3 真宿主 e2e 门禁），由 `.github/workflows/age-ci.yml` 与根级 `verify-age.sh` 协同保活，禁止「本地过、CI 飘」。
4. **审计回执绑定硬门**（02 §4.1）：任何状态位写回（`status` / `dispatch` / `claim` / `audit-rounds` / `failures`）必须有独立派发 + 同 id accepted 行匹配；不存在手动 close 通道。
5. **人类可读性 ≠ 验收标准**。验证命令 + 退出码 + 真实断言是唯一收口判据；plan frontmatter 字段、`> Plan Status:` 等散文格式不可作为收口条件。

## Work Item Status

> **此为唯一动态状态块。仅在此处更新状态。状态只挂在 work item，不挂在 milestone。**
> 状态 = 列 0 勾选框（`- [ ]` 未完成 / `- [x]` 完成——完成判据 = 独立 closure audit 通过 + Verification Gate 真实绿，勾选时行内追加证据指针）。roadmap 侧无生命周期值散文：评审/排队态由交付 plan 的 frontmatter `status` 承载（00-roadmap-authoring-guide 2026-08-25 现行格式：done=勾选、状态语义归 plan 侧）。

### M1 — P0 Ledger 账本改造（frontmatter 化 + 完成派生 + 审计内联）

- [x] WI1 frontmatter 解析器：30 行内置解析；扁平标量 + 单层流式数组；块标量/嵌套对象禁用（依据 01 §2）（证据：`tools/mission-driver/src/ledger-frontmatter.mjs` + `tools/mission-driver/test/ledger-frontmatter.test.js`，plan `docs/plans/age-autonomy/2026-08-25-0635-1-m1-frontmatter-ledger-core.md`）
- [x] WI2 plan frontmatter 最小集实现 + guide 同步（status/mission/work-item/group/failures/verify/hold/claim/claim-expires）（证据：同上 plan；guide 增补 `docs/plans/00-plan-authoring-and-execution-guide.md` § Plan Frontmatter Field Table）
- [x] WI3 状态格 + 完成派生公式 + 计数域 grep 共享实现（Phase + Closure Findings）（证据：`tools/mission-driver/src/ledger-sections.mjs` + `tools/mission-driver/test/ledger-derivation.test.js`，plan `docs/plans/age-autonomy/2026-08-25-0635-2-m1-ledger-sections-derivation.md`）
- [x] WI4 Closure Gates 消解（codemod；可执行项并入最后 Phase；独立性/验证/一致性由公式派生）
- [x] WI5 评审/审计内联区格式 + Draft Review / Closure Findings / Verification / Closure 示例与结构校验（证据：`tools/mission-driver/src/ledger-sections.mjs` `scanPlanLedger` + `tools/mission-driver/test/ledger-sections.test.js` + 00-guide § Plan Body Sections 示例，plan 同 WI3）
- [x] WI6 Deep Audit Record 格式 + accepted findings=none|items 机器可读（证据：`scanRoadmapLedger` Deep Audit Record 解析 + 00-roadmap-guide § Roadmap Frontmatter And Audit Record 示例，plan 同 WI3）
- [x] WI7 存量 plan / roadmap codemod + 双读过渡（plan-check.mjs 同时识别旧 `> Plan Status:` / `> Review Hold:` 与新 frontmatter，env 切换）
- [x] WI8 `> Last Reviewed` / `> Source Audits` / 外部 `docs/audits/` 跨文件生命周期消解（迁移并归一）
- [x] WI9 plan-guide/roadmap-guide 同步新格式（rules 11/12/13 退役；count 域 grep；frontmatter 字段表）
- [x] WI10 CI 前置：跑通 plan-check frontmatter 版 + mission-check + 双读断点切换开关
- [x] **WI11 Verification Gate — M1**（自动验证硬门，下列命令真实绿方可勾选；任何一条红 = M1 未收口）
  - `node tools/mission-driver/src/plan-check.mjs docs/plans/00-plan-authoring-and-execution-guide.md --strict` → exit 0（frontmatter 解析器对既有 plan guide 仍兼容）
  - `pnpm --prefix tools/mission-driver test` → 0 失败
  - `node plugin/dsh/test/ledger-frontmatter.test.mjs`（或 `node tools/mission-driver/src/frontmatter.test.mjs`，择一）→ 至少 12 用例（解析器、字段集、状态格、完成派生、双读切换、append-only）全绿
  - `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → 与 roadmap M1 WI 总数一致（无散落示例污染计数域）

### M2 — P1 法律：门禁族 + 三硬门 + 机械验证机械化

- [x] WI12 gate 纯函数签名 + actor + proposedContent（02 §2）+ mission 注入适配（plugin/dsh pre-execute + plan-check CLI）（证据：内核 `tools/mission-driver/src/law-core.mjs` + `test/law-core.test.js`（23 例）+ CLI `tools/mission-driver/src/gate-check.mjs`（--policy 校验 / 单文件结构面）+ 宿主适配层 `plugin/dsh/src/law/host-adapter.ts`（observe-only + `_tmp/law-observations.jsonl` 观察日志）+ 真值表奠基 `plugin/dsh/test/law-truth-table.test.mjs`（含真实 cordis waterfall 宿主面证据），plan `docs/plans/age-autonomy/2026-08-25-0815-1-m2-law-seam-policy-schema.md`；roadmap「核心纪律」散文与引擎侧放置裁定的张力在该 plan Phase 1 成文裁定）
- [x] WI13 `missions/autonomy.policy.yml` schema + missionCheck + 结构测试钉住（version/limits/gates/triggers + `agents`/`dispatch` 具名派发映射与 agent 名引用校验；见 02 §3/§4.9，A7 裁定；+ mission.json 增 `autonomyPolicy` 字段与 mission-check 校验（终审 P1-5）+ fixedPrefix 块 schema `{kind: text|file|dir, ref, maxFileBytes?}`（终审 P1-3）+ trigger 谓词语法=受限 and/or/not + 谓词集，R1–R4 归属同步（终审 P2-2））（证据：`tools/mission-driver/src/law-policy.mjs`（受限 YAML 子集解析 + schema 校验 + trigger 语法）+ 真实实例 `missions/autonomy.policy.yml`（plan-structure observe + 7 triggers + 4 agents + 6 dispatch）+ `test/law-policy.test.js`（18 例 schema 矩阵）+ mission-check autonomyPolicy 存在性校验 + config.js fail-fast 加载（`autonomyPolicyPath`/`_agentNames`），plan 同 WI12）
- [x] WI14 三硬门 1：审计回执绑定（dispatch/accepted 同 id + 写者 actor 匹配；plan Closure + roadmap Deep Audit 同构；+ dispatch 行 model lineage；audit agent `requireDistinctModel` 派发时强制）（证据：规则 `tools/mission-driver/src/law-rules.mjs` `closure-audit-binding`/`roadmap-audit-binding`（经 law-policy 注册三消费面可达）+ `models=` 后缀解析 `src/ledger-sections.mjs` + 静态可满足性 `src/law-policy.mjs` `checkDistinctModelSatisfiability`（`downgrade: single-model` 显式降级通道，真实 policy 已声明）+ 真值表 `plugin/dsh/test/law-truth-table.test.mjs`（双面配对/写者错位/伪造回执/中间态/models= 畸形 16 例）+ gate-check corpus 输出（0635-3 awaitingClosure / 0635-1/2 legacy 域外跳过均 exit 0），plan `docs/plans/age-autonomy/2026-08-25-0815-2-m2-three-hard-gates.md`；**残项注记**：`requireDistinctModel` 派发时运行时强制（实际模型对校验）归 M3/WI26 守夜人派发点——WI26 文字未点名该面，接口 = dispatch 行 `models=` 数据 + 本检查函数复用）
- [x] WI15 三硬门 2：状态转移写者身份（draft→active reviewerSessionId；held→active 仅守夜人 unlock/reviewer；人工 disposition 经 `mdcontrol.unlock`/`mdcontrol.disposition`）（证据：规则 `src/law-rules.mjs` `writer-identity`（01 §5.1 逐边表 `LEGAL_TRANSITIONS` + draft→active 配对回执 + held→active failures 重置/hold 移除同写强制 + 执行者永禁写 status（role + claim holder 双面）+ 评审租约；身份依赖边结构面退化 unverified-writer 注记不冒充——`mdcontrol.unlock` 路由归 M3/WI28）+ 真值表转移边 × actor × 证据三维矩阵 11 例，plan 同 WI14）
- [x] WI16 三硬门 3：完成派生校验（status=active ∧ 全勾 ∧ 机械验证 ∧ 审计回执 ∧ 派发登记；整文件 proposed content；整 plan 粒度）（证据：规则 `src/law-rules.mjs` `plan-completed`（全勾三岔：回执齐 → `deriveCompleted` 公式校验（stale basisHash deny 指向重验证）；无回执 → 有效 claim 持有者放行入 awaitingClosure 且同写清除 claim（无/过期/错主/残留 deny）；审计拒绝 = Closure Findings 未勾项自然脱离全勾；+ 终态冻结 = basis 域（frontmatter + Phase + Closure Findings）不可变，防旧回执复用——WI21 后继 plan 只消费不重实现）+ 真值表 11 例 + corpus 按文件类钉住（0635-3/0815-1 = awaitingClosure 合法态、legacy 双读跳过、真实 enforce policy 全勾越权写入 end-to-end deny），plan 同 WI14）
- [x] WI17 nothing claim 兜底门禁：仅当 `draftPlans==0 ∧ activePlans==0` 允许 Deep Audit 触发；`audit-rounds ≥ max` 或 `findings=none ∧ roadmap 有未勾` 走终态 R1/R3（证据：规则 `tools/mission-driver/src/law-rules.mjs` `nothing-claim-guard`（`action:terminal-claim` 动作面：`kind: nothing-to-draft` × 注入谓词 `ctx.plans`——`draftPlans()>0 ∨ activePlans()>0` → deny 指向未完成 plan，否则 allow 携带 Deep Audit 触发信号数据形状（M3/WI26 消费））+ `audit-rounds-overflow`（roadmap `## Deep Audit Record` 新 dispatch 行 × `audit-rounds ≥ maxAuditRounds` → deny；max 双源解析 `src/law-policy.mjs` `resolveMaxAuditRounds` = policy limits 权威 / mission flow 回退 / 双缺 0——0815-1 裁定的消费面切换兑现）+ 真值表 12 例（terminal-claim 矩阵 + 真实 plansDir fixture + 触发信号形状 + 预算边界 = max / < max / 未配置 0≥0 × 新增/既有行 + `resolveMaxAuditRounds` 优先级矩阵），plan `docs/plans/age-autonomy/2026-08-25-0815-3-m2-supporting-gates.md`；**残项注记**：R1/R3 终态执行与 trigger 派发语义归 M3/WI26/WI27——本 WI 只交付判定面与触发信号数据）
- [x] WI18 claim 合法性门禁：plan frontmatter `claim` 内含 holderSessionId；执行者勾选需 actor.id 匹配；claim 在 active 外/awaitingClosure 前必清（证据：规则 `src/law-rules.mjs` `claim-validity` 五面——写者角色白名单 engine/supervisor（executor/drafter/reviewer/auditor deny；DSH id-only = unverified-writer 注记，role-bearing deny 面随 M3 守夜人）+ 写入 TTL 合法形状 / 勾选 holder 尾部锚定匹配 ∧ 未过期（ctx.now 注入时钟，结构面退化 claim 存在 ∧ 未过期）/ claim action 单活（转移面；parse 面 = 重复键拒绝，两层边界注明）/ 全勾无回执必清 claim（0815-2 plan-completed ② 同约束另侧声明）/ status ≠ active 禁携 claim + 清除 = holder ∨ dispatcher，真值表 7 例（角色矩阵 / 时钟 < = > / claim action 五列 / ④⑤双 deny 面 / 清除四例），plan 同 WI17；**残项注记**：过渡期 claim 写者供给 = 引擎流程派发面（EXECUTE prompt 指令，承 0635-3 模式）——M2 内 claim 字段无生产写入者（与现状持平），M3/WI25 守夜人接管唯一机器字段写者后消除 prompt 可绕残险）
- [x] WI19 机械验证门禁：守夜人在 awaitingClosure 直跑 `commands.test/build/lint/typecheck` 写 `## Verification` pass 行（basisHash sha256）；`verify` 只能是 commands.* key（证据：规则 `src/law-rules.mjs` `verify-keys`（plan `verify` ⊆ mission `commands.*` 非空 key 枚举 deny 面 + ctx.commands 缺失 fail-open 注记；`verify: []` 空真通道归 WI44 不在本 WI 裁定）+ commands runner `src/verify-runner.mjs`（零引擎 diff 新模块：`resolveVerifyPlan`（verify 缺省 → mission 默认 test/build/lint/typecheck 交集）+ `runVerifyCommands`（仅 spawn commands.*、cwd=projectRoot、10min/命令超时、输出截尾）产出每 key `{exitCode, passLine}`，passLine 01 §4.2 语法、basisHash 经 `computeBasisHash` 与完成公式同源绑定）+ 执行面 `src/gate-check.mjs <plan.md> --verify`（owning mission 祖先走查 + verify-keys gate + runner 端到端，stdout JSON 不写 plan 文件），plan 同 WI17；端到端：0635-3 `--verify` 真实执行 `commands.test` 863/0 exit 0 产出合法 passLine；**残项注记**：BUILD_VERIFY prompt 步不动（过渡期写者裁定不变）、守夜人接管执行与自动写盘归 M3/WI26——runner 的 M3 消费面经 build-bundle ALLOWED_MODULES 预登记（unreachable-allowed 直至接线））
- [x] WI20 append-only 门禁：`## Draft Review Record` / `## Verification` / `## Closure` / roadmap `## Deep Audit Record` 只追加（证据：规则 `src/law-rules.mjs` `record-append-only`（直接 enforce——02 §6 例外授权同 0815-2 Phase 3 引用；current/proposed 逐区前缀保持比对：既有行（含 prose——容忍策略是「未知行不参与语法匹配」非「可删」）原行原序保留、仅容尾部追加与行尾空白/尾部空行清理；删行/改行/换序/整区删除 deny + reason 指出首个违例行；proposed 非 frontmatter 账本 = 双读域外、无 currentFileState = 不可观测注记）+ 双 policy gate 条目（plans + roadmap DAR 域）+ 真值表 7 例（追加合法 / 删 / 改 / 换序 / 整区删除 / prose 删除 / 尾空白容忍 / DAR 面），plan 同 WI17；corpus 回归：0635-3/0815-1/0815-2 全 gate 无误杀、legacy 0635-1/2 域外跳过）
- [x] WI21 路径与结构护栏（含 one-mission-one-roadmap 边界；`work-item` 命中 roadmap 已登记；终态冻结；**执法层自护 P8**：`plugin/dsh/src/law/**`、`missions/autonomy.policy.yml`、`tools/mission-driver/src/{plan-check,gate-check}.mjs` 对 AI 写 deny，人工/CI/已批准立项为合法例外）（证据：注册谓词 `tools/mission-driver/src/law-core.mjs` `expandWorkItemLabel`/`workItemRegistered`（复合标签 `M<n>-WI<a>(+WI<b>)*` 语法钉住——裸 token 继承首 token 里程碑、显式前缀等价展开接受 + (milestone,id) 二元组对 `scanRoadmapLedger` 注册表逐 token 对账、空注册表 fail；接进 plan-structure 增项：语法面从 label 单独可判定即 deny、注册面经 ctx.roadmapText 注入、缺省不可观测注记）+ 三规则 `src/law-rules.mjs` `path-guardrail`（plan 形 .md = frontmatter 三键齐备 status+mission+work-item；合法域 = ctx.plansRoots 被动扫描并集（各祖先默认 docs/plans + missions plansDir，malformed 零根，承袭 plan-status-gate 先例）；域外 deny 列注册根集，新建/改写同拦）/ `roadmap-write-guard`（允许 = 已注册 WI 行勾选仅 [ ]→[x] + 行内尾部证据注记追加；WI 行增删/换序/改 id 与 milestone 标题增删改 deny，例外 = actor role ∈ {engine,supervisor}（id-only 面退化 unverified-writer 注记）∨ 已批准立项（`activePlanReferencing` active plan 正文含路径，reason 记命中文件与行））/ `law-self-protection`（P8 唯一无条件对抗级直接 enforce：保护路径四族按 ctx.projectRoot 解析（`isLawProtectedPath`）；例外集 = 02 §4.7 字面三支 human role / CI（部署面承载）/ 已批准立项——`engine` 不入例外集（反向真值表用例钉住）；plan corpus 未注入 fail-closed；规则第一个合法消费者 = 它自己的宿主 plan（active 引用 = 自指一致性用例））+ one-mission-one-roadmap `src/mission-check.mjs` `checkRoadmapUniqueness`（roadmapPath 跨 mission 唯一反向对应，冲突 = 结构化加载错误；CLI + DSH `loadLawContextAt` 双接线，冲突祖先贡献零 law 上下文）+ policy schema `{{projectRoot}}` 占位符（match/resolve 双面）+ `missions/autonomy.policy.yml` 17 gates（plan-structure 切 enforce——0815-1 预留开关兑现 + 7 新条目全 enforce）+ gate-check CLI `workItem` 对账明细输出（owning mission roadmap 注入，10 份 frontmatter 语料 live 全过）+ DSH 适配层 ctx 增注 projectRoot/plansRoots/roadmapText/plans（保护路径时）+ 真值表 `plugin/dsh/test/law-truth-table.test.mjs` **107 例**（语法矩阵/注册表反例/10 份语料 live 对账/路径域三列/回写护栏结构面 × 例外面/真实 roadmap WI21 翻转+追加 smoke/P8 四路径 × 三角色 deny + engine 反向 + human/active-plan 例外 + corpus 缺省 fail-closed + 自指一致性 + 真实 policy 11 文件全 ctx 无误杀）；**残项注记**：「已批准立项」例外判据为结构近似（active plan 正文路径字符串包含），精确化（Phase Targets 逐条解析 + 守夜人统一落盘路径）归 M3；02 §4.7 保护清单字面只列 `plugin/dsh/src/law/**`（宿主适配层）而规则实现本体在引擎侧 `tools/mission-driver/src/`（0815-1 放置裁定的既有字面错位，先于本 plan）——归 M5 WI39 docs 一致性收口（plan Deferred「law 内核 P8 覆盖缺口」防丢线索）；path-guardrail 并集域跨 mission 弱化 watch-only（CI 结构面兜底）；plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md`，`pnpm --prefix tools/mission-driver test` 899/0 + `npm --prefix plugin/dsh test` 240/0 + `./verify-age.sh` L1+L2 GREEN + 零 engine.js diff）
- [x] WI22 WI13 证据面重建（run-state 子流程不再权威；证据谓词改读 plan frontmatter/closures；plugin/dsh plan-status-gate 迁移或退役）（证据：裁定 = **退役 + 保护收编**——① 保护语义收编 law 规则 `tools/mission-driver/src/law-rules.mjs` `legacy-plan-freeze`（plan 域 .md 写入携带/改写/删除 legacy 终态行 `> Plan Status: completed|cancelled|superseded|deferred` → deny；matcher 复用共享 `PLAN_STATUS_RE`+`normalizeLegacyStatus` 零第二实现、代码围栏跳过；例外 = P8 字面三支 human/CI/已批准立项（`activePlanReferencing`）；roots 缺省 fail-open、语料缺省 fail-closed 同 P8 姿态；policy 第 18 条 gate enforce；适配层 `host-adapter.ts` 终态面预检 + freeze corpus 按需注入，54 份 legacy 终态语料冻结保护有承接）② run-state 证据面（F1/F2/F3）废弃：`plugin/dsh/src/plan-status-gate.ts` + `test/plan-status-gate.test.mjs`（23 例）+ `scripts/e2e-plan-gate.mjs` + package.json `verify:e2e:gate` 删除，service.ts 收敛单 law 监听器，`grep scanSubflowMatches|registerPlanStatusGate plugin/dsh/src/` 零命中、run-state 残留消费面 = mdcontrol 状态透传/监控显示（非证据面，Non-Goals 类）——plan frontmatter/closures 是唯一完成证据面（01 §5.2）③ 引擎 legacy 审计通道退役：flow-loader 删 `AUDIT_STATUS_RE`/`_scanOpenAuditsList`/`_isMissionLevelAudit`/注册表 `openAudits` 键，`flows/deep-audit-loop.json` 删 CHECK_OPEN_AUDITS/SCAN_NEW_RESULTS（entry 直连 MULTI_AUDIT，OPEN_AUDIT otherwise/transitions/onError 三处终态直连，无悬空引用守护断言钉住），`prompts/draft-from-audit.md` 删除，engine.js 零 diff（optional-changed 消费面退化 []）；退役前置清点 2026-08-26 live 复核 6 命中/1 open/5 planned 与基线对账零漂移 + 唯一 open 记录（mission-driver-step-audit 2026-07-21-0919 multi-audit）机械关闭 + Disposition 注记 ④ duplicate-append-only-anchor 结构 error（deep-audit round-1 P2 清偿）：`scanPlanLedger`/`scanRoadmapLedger` 重复 `## Closure`/`## Verification`/`## Draft Review Record`/`## Deep Audit Record` 锚点报 `duplicate-anchor` error 进 scan.errors（首锚定派生面保留），deny 面（plan-structure）/读面（inspectPlan）可见，存量 corpus 零误伤断言；⑤ 文档同步：CONTEXT.md WI22 段 + `docs/architecture/dsh-plugin-packaging.md`（scripts/test/src 树条目、dsh-tools type-only pin 持有者、§Service Surface Reinforcement gate RETIRED、changelog）+ 设计文档退役注记（mission-driver-flow-design §6 重写 + 审计门 openAudits 腿注记、step-execution-and-audit-count 顶部 banner、flow-engine-design forEach 例/prompt 树/子流程步列表、mission-design 表达式注册表行/mermaid）；测试 `pnpm --prefix tools/mission-driver test` 904/0（audit-convergence 重写守护 6 例含无悬空引用 walk + draft-plans-audit-gate 删 Phase 5 块保留 A–G + ledger-sections +5 + context-map 11 钉扎）+ `npm --prefix plugin/dsh test` 223/223（+6 freeze 例；freshness 42 文件 content-equal）+ 真值表 18-gate corpus；Follow-up Backlog duplicate-anchor 条目清偿注记；plan `docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md`）
- [x] WI23 CI 门禁接线：`plan-check.mjs` frontmatter 版 + pre-commit hook + CI job（结构子集 + audit track）；与现有 `verify-age.sh`/`age-ci.yml` 协同（证据：plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md`——① `.githooks/pre-commit`（三段暂存区触发面：`docs/plans/**/*.md` gate-check 结构子集 / `docs/backlog/*.md` roadmap-check 字段集 / `missions/*.json` mission-check；执行期裁定：legacy 形 plan 与无 roadmapPath 的 base 配置注记跳过——WI22 freeze 无 actor 面 fail-closed 设计 + monitor 过滤先例；启用 = `git config core.hooksPath .githooks` 文档化进 CONTEXT.md）② `verify-age.sh` 第三段 L2.5 law gates 三面（`gate-check --policy` / `docs/plans/age-autonomy/` 全量 corpus 逐文件 / 真值表独立显式调用）③ `age-ci.yml` 触发路径对称扩集 `docs/plans|backlog/**`+`missions/**`+`.githooks/**`（单 job 本地=CI 同构保持）；Proof：hook 五场景（空/违规 plan exit 1 reason 指向 plan-structure/违规 roadmap exit 1 fieldErrors/合法/无 mission 语境 fail-open）+ `./verify-age.sh` 三段全绿（904/223/113）+ L2.5 拦截双探针（L1 语料钉扎层与 L2.5 段各证一次 exit 1）；「结构子集 + audit track」映射 = gate-check 单文件面（02 §6 独立形态行）+ 回执绑定结构面（closure/roadmap-audit-binding dispatch/accepted 配对），全量 corpus 跑同时覆盖两者）
- [x] WI41 [P0·deep-audit R1] 修复 D2 账本收口死锁：`flows/plan-execution.json` 将 `CLOSURE_SCRIPT_CHECK.pass → BUILD_VERIFY` 且 `closureScriptCheck`（`flow-loader.js`）对账本格式回执盲视——全勾 plan 永远到不了 CLOSURE_AUDIT（`## Closure` 回执唯一写者），`deriveCompleted` 的 auditReceipt 合取永假 → plan 永驻 `activePlans()` 无限重喂（live 受害者 0635-3；`docs/bugs/2026-08-25-ledger-plan-closure-deadlock.md` D2 open 且无 roadmap 项）。修法 = closureScriptCheck 对 frontmatter plan 增加「全勾 ∧ 缺 verify pass 行 / 缺配对回执 → fail（路由 CLOSURE_AUDIT）」+ 回归测试；落点（引擎 diff vs 插件层）立项时按「零引擎 diff 底线」裁定（证据：`tools/mission-driver/src/flow-loader.js` closureScriptCheck 回执感知 fail 条件（derived.reasons 逐条进 fail text/SCRIPT_CHECK_DETAILS）+ `src/plan-check.mjs` `missionDefaultVerifyKeys` 单一实现与 `inspectPlan` 派生视图增量字段（derivedCompleted/completionReasons/verifyKeys/verifyKeysSource）+ 引擎读面三面注入 `defaultVerifyKeys=["test"]`（flow-loader 谓词族/closureScriptCheck/plan-check CLI——`discoverOwningMission` 上移 mission-check.mjs 共享）——同时清偿 Follow-up「引擎读面未注入 defaultVerifyKeys」P2；回归测试 `tools/mission-driver/test/closure-routing.test.js` 13 例（三态 fixture + legacy 逐字节钉住 + 0635-3 live 双向断言）；`engine.js`/`flows/plan-execution.json` 零 diff；bug doc D2 → fixed；plan `docs/plans/age-autonomy/2026-08-25-0925-1-m2-wi41-closure-routing-deadlock.md`，commit `00aeb9c`，测试 876/0 + verify-age L1+L2 GREEN）
- [x] WI42 [P1·deep-audit R1] 接线 `validatePlanFrontmatter`/`validateRoadmapFrontmatter` 到生产读面（M1-WI2 校验器当前零生产消费者——plan-check/flow-loader/monitor/roadmap-check/corpus 测试均不调用）：live 证实手写 `status: completed`（禁写派生态）+ 伪 basisHash 可过 `plan-check --strict` exit 0，normalized="completed" 而 completed:false/terminal:false 自相矛盾，且使 plan 静默退出 active/draft 双队列（第二真相通道，正是 01 §2 纪律所禁）；未知字段/键名 typo 同样静默通过（证据：读 seam `tools/mission-driver/src/ledger-dualread.mjs` `fmReadResult`——`readPlanStatus` frontmatter 分支调 `validatePlanFrontmatter`，fieldErrors/fieldsValid 随 `planLedgerState` 透传全引擎读面；消费面 ①plan-check `analyzeFrontmatter` fieldErrors 并入 `field:` details → exit 1 ②flow-loader `_scanPlansByStatus` console.warn 一次/文件/扫描（kill silence 非 kill queue，成员资格仍按可解析 status）③monitor plans 列表条目透传 fieldErrors（roadmap API 经 `parseRoadmapMarkdown` spread 同覆盖）④roadmap-check `parseRoadmapMarkdown` hasFm 点跑 `validateRoadmapFrontmatter` + 新 CLI 面 exit 0/1/2；测试 `test/field-wiring.test.js`（三 live 探针反向钉住 + legal corpus CLI exit 0 + flow-loader warn console-spy 与成员资格不变断言）+ `test/ledger-corpus.test.js` fieldsValid===true / roadmap fieldErrors [] 断言 + `test/monitor.test.js` plans API 端到端；plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md`，测试 899/0）
- [x] WI43 [P1·deep-audit R1] 架构 owner-doc 契约同步：M1 三个公共契约模块（`ledger-frontmatter.mjs`/`ledger-sections.mjs`/`ledger-dualread.mjs`——四引擎消费面 + 插件 assets 通道 + 两 guide 声明的 machine face）未登记进 `docs/architecture/mission-driver-baseline.md` §Public Exports（该文件最后更新停在 M1 之前，违反其自身 Update Rule）；伴生漂移：`docs/architecture/dsh-plugin-packaging.md` "the 19 allowed modules"（build-bundle 实为 22）+ import-graph 枚举缺 ledger 链 + plan-status-gate 的 `PLAN_STATUS_RE` import 源已改 `assets/src/ledger-dualread.mjs`（doc 仍写 plan-check.mjs）（证据：baseline §Public Exports 登记 ledger 三模块各一条（契约 01 分节指针 + 四读面/assets 通道/两 guide machine face 消费面 + 零 import/零 npm 约束；rg -c 逐模块 =2 ≥1）+ M1 交付进度句（0635 批次指针，Update Rule 字面形态=同 change 登记）；packaging 四漂移修复——计数 19→**26**（执行时 live 核定：plan 撰写时 ALLOWED_MODULES=22，0815 law 批次 4 模块落地后 live 26=assets/src 实测=ALLOWED_MODULES，按「与实测清单一致」退出判据取 26，组成 19 引擎+3 ledger+4 law 逐名可对，closure 25/26 钉于 build-bundle --check）、PLAN_STATUS_RE 源 :212/:279 → `assets/src/ledger-dualread.mjs`（修前恰两处，修后 `rg "assets/src/plan-check.mjs"` 零命中）、import-graph 补 ledger 链精确边（plan-check/flow-loader/roadmap-check → ledger 三件 + 库内链，含 WI42 后增的 roadmap-check→ledger-frontmatter 边；law 边显式标注归 0815 批次登记债）、`rg "19 (allowed|files)"` 零命中；两文档 ledger 相关键全文核对零残留漂移；`pnpm --prefix tools/mission-driver test` 899/0 + mission-check exit 0；plan `docs/plans/age-autonomy/2026-08-25-0925-3-m2-wi43-arch-ownerdoc-contract-sync.md`）
- [x] WI44 [P1·deep-audit R2] 堵住 `verify: []` 空真（vacuous-pass）通道：`validatePlanFrontmatter` 接受空数组、`deriveCompleted`（ledger-sections.mjs `mechanicalVerification` 合取）对空 keys 集合 `missingKeys=[]` 直接判 true——零条 pass 行即机械验证通过，live 探针证实 `verify: []` + 回执齐全的 plan 派生 `completed:true`，agent 可静默豁免全部机械验证；设计 01 §4.1 只定义「缺失→mission 默认」未定义空数组，与 roadmap 纪律 2（Verification Gate 硬约束）意图相悖；修法 = 校验器拒绝空 `verify`（或按缺失处理走 defaultVerifyKeys/no-verify-keys）+ 空集不空真的 deriveCompleted 回归测试（现测试仅钉 `verify: []` 可解析，未钉派生语义）（证据：双层封堵——`tools/mission-driver/src/ledger-frontmatter.mjs` 校验器对 `verify: []` 报 error 指路「非空数组或省略回落 mission 默认」（`law-core.mjs` plan-structure 消费同一校验器，写时 observe 面自动生效）+ `src/ledger-sections.mjs` `deriveCompleted` 显式空数组按 no-verify-keys fail-closed 且优先于注入的 `opts.defaultVerifyKeys`（显式空=拒绝语义）；解析面 `verify: []` 可解析既有用例零修改（解析/校验分层）；测试 `test/ledger-frontmatter.test.js` 拒绝用例 + `test/ledger-derivation.test.js` WI44 describe 四例（双层面/旁路防御/precedence fail-closed/绿路径 `verify: [test]` 无伤）+ 语料断言 `rg "^verify: \[\]\s*$"` 零命中；01 §4.1 verify 行空数组语义裁定句成文；plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md`，测试 899/0）
- [x] **WI24 Verification Gate — M2**（自动验证硬门，下列命令真实绿方可勾选）（证据：plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md` Phase 3 四命令实测全绿——① `node tools/mission-driver/src/gate-check.mjs --policy missions/autonomy.policy.yml` exit 0（valid=true，18 gates / 7 triggers / 4 agents；roadmap 字面 check-policy 的落地 CLI 前门形态，0815-1 裁定对齐注记在案）② `node --test plugin/dsh/test/law-truth-table.test.mjs` **113 用例 / 0 失败**（≥30 超额；分类覆盖：三硬门 gate1+gate1-roadmap 15（closure-audit-binding/roadmap-audit-binding 配对/写者错位/伪造回执/models= lineage）+ gate2 10（writer-identity 逐边×actor 矩阵 + 评审租约）+ gate3 9（plan-completed 全勾三岔/终态冻结）= 34，配套门 gate-nothing 4 + gate-claim 7 + gate-meter 4 + gate-append-only 2 + gate-verify-keys 2 = 19，护栏/结构面 freeze 8 + p8 5 + path-guardrail 3 + rwg 5 + wi-× 4 + seam 5（actor 缺省结构子集）+ corpus 5 + adapter 8 + runner 3 + policy 3 + boundary/truth-table 12；node 直跑测试文件的 `--test` runner 形态对齐注记在案）③ `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md` exit 0——语料 = 当期最新收口 M2 plan（WI22，回执对 + pass 行齐全）；**三硬门全 allow**（closure-audit-binding / writer-identity / plan-completed）+ 其余 6 结构规则 allow + workItem 对账 ok（M2-WI22 注册命中）④ `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0（autonomyPolicy 字段经 mission 校验））
  - `node plugin/dsh/src/law/check-policy.mjs missions/autonomy.policy.yml`（或 plan-check 的 `--policy` 模式）→ exit 0 且 schema 校验通过
  - `node plugin/dsh/test/law-truth-table.test.mjs` → 真值表测试至少 30 用例（覆盖三硬门全部正向/反向/边界 + actor 缺省结构子集 + 评审租约 + **law 域 deny 面** + `requireDistinctModel` 正向/反向/单模型部署显式降级 + agent 名引用校验）+ 0 失败
  - `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/<a-plan>.md` → 实际 plan 文件三硬门全部 pass（grep 至少一个 plan 文件跑通）
  - `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` → exit 0（policy 字段被 mission 校验通过）

### M3 — P2 守夜人：Supervisor seam + claim/乐观锁 + 连续队列

- [x] WI25 Supervisor 拆 cordis service（plugin/dsh）或可独立运行（CLI）+ 五职责（sustain/trigger/meter/restart/receipt）（证据：**seam 切片收口**（dispatch 执行归 WI26 / 终态归 WI27 / 恢复完整语义归 WI29）——服务模块 `plugin/dsh/src/supervisor/` 五文件（`decision-core.ts` 纯函数契约 `decide(snapshot, policy, clock) → decisions[]` + 谓词族经 assets 通道复用 + 1411-2 trigger/1411-3 终态接入点与 SustainDuty/TriggerDuty/TerminalDuty 声明接口成文于头注；`watchdog.ts` 心跳沿默认 30s + 事件沿 fs.watch 防抖降级 + 单飞守卫 + start() 恢复扫描 restart seam + onTerminal 回执链 seam；`writer.ts` 唯一机器字段写者四写函数 claim/claim-expires/failures/audit-rounds——写前 law 自检同一 `evaluateGates`（actor role=supervisor）+ baseHash CAS（computeBasisHash 同源，受限重试）+ tmp+rename 原子落盘 + deny 不落盘且回执；`receipt.ts` `_tmp/supervisor-receipts.jsonl` append-only JSONL + 尽力投递 A8 死会话容忍 + `mdcontrol.status` 透出既有 route 扩展；`service.ts` `SupervisorService` 第二 cordis service publication（名 `mdsupervisor`，同 bundle/同 isolate realm，零宿主面新增，无 root 配置 = idle 降级））+ 测试 `plugin/dsh/test/supervisor-core.test.mjs` **23 例 0 失败**（判定矩阵/writer CAS·law-deny·原子性/receipt/watchdog 起搏·单飞·防抖·恢复扫描/服务挂载 dispose 幂等）+ 三裁定指针 plan `docs/plans/age-autonomy/2026-08-26-1411-1-m3-wi25-supervisor-service-seam-five-duties.md` Phase 1（① 服务形态 = DSH 插件内第二 service publication，独立 CLI 仅文档 seam；② Q4 写回路由 02 §4.5 三选一 = ③ 守夜人唯一机器字段写者串行落盘；③ 默认姿态 dispatch 决策恒 no-op 观察——1411-2 接线前存量宿主零无人值守推进）+ **0815-3 claim 残项收口**：role=supervisor 写者经 claim-validity/writer-identity 白名单面零规则改动通过（executor role 同写 deny 用例钉住 role-bearing deny 面激活），「过渡期 EXECUTE prompt 指令供给可绕」残险随唯一机器字段写者通道在库而消除（claim 生产发放面归 1411-2 派发接线，plan Deferred 已登记）；验证基线只增不减：引擎 907 / 插件 246 / 真值表 113，`./verify-age.sh` L1+L2+L2.5 GREEN，`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 运行时依赖）
- [x] WI26 trigger 规则：从 `missions/autonomy.policy.yml` `triggers:` 段读取并执行（plan-review/closure-audit/deep-audit/mechanical-verification/reclaim/draft-plans/nothing→deep-audit；+ 派发时按 `dispatch` 映射解析具名 agent 并应用模型/组合：DSH 形态补 native-executor 的 ModelSelection documented gap（agentProvider/agentModel/reasoningEffort），独立形态复用 config.js model/variant/agentFile 通道；plan frontmatter `agent:` 覆盖经守夜人路由；+ claim TTL 续期信号 = 活动信号（events/session 工具活动），续期是否落账本于立项时定（终审 P2-1））（证据：plan `docs/plans/age-autonomy/2026-08-26-1411-2-m3-wi26-trigger-execution-dispatch-wiring.md` 四 Phase 收口——① 求值器 `plugin/dsh/src/supervisor/trigger-eval.ts` 纯函数（14 谓词全实现：per-plan 域 plan.full-tick/plan.status/mechanical-verification-missing·pass（basisHash stale 视 missing）/closure-receipt-missing/review-dispatch-missing/claim-expired × mission 域 draftPlans()/activePlans()/heldPlans()/roadmap.unchecked/all-done/deep-audit.accepted-findings/terminal-claim（读 `_tmp/<runDir>/terminal-claim.json` 动作记录面）；fail-soft（非数比较/畸形值 = errors[] 不崩溃）；命中携带 occurrenceKey 材料（03 §5 账本派生））② 派发解析链 `plugin/dsh/src/supervisor/dispatch-resolve.ts`（dispatch 类型 → policy `dispatch:` 映射 → 具名 agent → 绑定；plan frontmatter `agent:` 覆盖三态路由；DSH 三字段 `agentProvider/agentModel/reasoningEffort`（**native-executor ModelSelection documented gap 收口**：config `nativeModelSelection` → agentOptions + `installModelSelection`，`src/native-executor.ts`）；独立形态 `independentChannelOf` 纯解析 seam（config.js model 通道，CLI runner 不交付）；**requireDistinctModel 运行时三态强制（0815-2 WI14 残项收口）**——比对共用 `src/law-policy.mjs` 抽出的 `sameModelPair` 纯函数（静态面同源零第二实现），不满足无降级 = 拒该次派发、`downgrade: single-model` 在场 = 诚实 `models=` lineage；幂等面 `dispatchAlreadyRegistered` 重扫账本作答（review/audit=dispatch 行、deep-audit=DAR unpaired 在飞、draft-plans=回执 JSONL 登记）③ 执行臂 `plugin/dsh/src/supervisor/exec-arm.ts` 七出口（**0925-1「BUILD_VERIFY pass 行写入机械化」+ 0950-3「gate 命令守夜人自动化执行」+ 0815-3 WI17 残项「Deep Audit 触发信号消费」三 Deferred 显式收编**：mechanical-verification = verify-runner 直跑 → 全绿 writer 写 `## Verification` pass 行（basisHash 同源）→ 链式 closure-audit，双驱动幂等=谓词面读账本；nothing→deep-audit = 预算预检 + DAR dispatch 行与 audit-rounds 自增同写 + claim 记录消费；reclaim = writer 清/换发 + execute 重派；**P2-1 裁定 = 续期写账本**（`renewClaim` bounded ≤ now+60min 单窗口 + 活动信号面 `noteActivity`，残险 = 伪造活动无限续期 → bounded + WI30 停滞指纹双兜底成文接受）；terminal 出口仅产决策对象转发 1411-3）④ prompt 对齐 `prompts/draft-from-roadmap.md` step 4 重写（**Follow-up P2 收口**：起草者保持 draft、不自派评审不自行置 active——评审独立性回到流程结构，writer-identity 门禁承接执法）；build-bundle verify-runner 注记转已消费登记（插件侧 supervisor 为 live 消费方，引擎闭包不可达属设计）；验证 `plugin/dsh/test/supervisor-trigger.test.mjs` **43 例**（七 trigger 正反 + e2e 全链 echo fixture：pass 行落盘 deriveCompleted 合取真 → closure-audit dispatch 行 + models= lineage）+ `supervisor-dispatch.test.mjs` **22 例**（映射矩阵/三字段/三态/幂等/law 自检 deny 面）+ supervisor-core 23 例零改动通过（无 triggers 段 WI25 观察姿态保真）；`./verify-age.sh` L1+L2+L2.5 GREEN（引擎 907 / 插件 311 / 真值表 113）；`git diff --stat tools/mission-driver/src/engine.js` 为空 + 零新增 npm 依赖）
- [x] WI27 终态规则 R1–R4（clean exit / silent-completed 修复 / hold 死锁 / 停滞熔断；`partial/blocked` 显式区分；+ failures 归因桶枚举成文（executor 错误 / 测试红 / claim 到期无产出，各桶计/不计规则）与 maxFailures 默认值进 mission config（终审 P2-3））（证据：plan `docs/plans/age-autonomy/2026-08-26-1411-3-m3-wi27-terminal-rules-failures-buckets.md` 三 Phase 收口——① **R1–R4 求值核心** `plugin/dsh/src/supervisor/terminal-rules.ts` `evaluateTermination(snapshot, {maxAuditRounds, maxFailures, stagnation?}) → {decision: completed|partial|blocked|continue, rule, reasons[]}` 纯函数（03 §8 字面顺序 R1→R4 序贯首条命中即决——顺序是成文契约；R1 三岔含「active 带未过期 claim → continue 不提前杀活」（活 claim 探测 = plans frontmatter × expiredClaims 差集）+ 未勾 → partial 不得静默记 completed；**partial/blocked 显式区分** = blocked↔R3∧held>0 ∨ R4 停滞 / partial↔R1 未全 done ∨ R3∧held==0（R1∧held>0 刻意取 partial——预算耗尽主导因，备选 B 否决理由成文）；复合声明值 `partial/blocked` 经 `normalizeDeclaredTerminal` 单点归一（policy 声明面不动、core continue 恒压声明）；R4 stagnation {rounds, threshold} 注入接口钉住（检测本体 WI30，Deferred 登记在案）；**两入口同一实现**：watchdog 周期末端求值 + policy 末条 trigger terminal 声明面（exec-arm `forwardTerminalDecision` 重扫快照经同一核心执行，双入口漂移残险由真值表交叉用例钉住）；三裁定（承载形态/终态落点=回执+mdcontrol.status 透出+循环停派非账本写入零引擎面/显式区分规则）成文于模块头注与 plan Phase 1）② **failures 归因桶 + 熔断**（02 §4.6 增量成文：三桶 `executor-error`/`verification-red`/`claim-expired-no-output` 计/不计规则表 + 不计清单（CAS 冲突重试/恢复扫描观察记录/双驱动幂等跳过）+ maxFailures 双源 + 熔断执行语义，changelog 行 M3-WI27，与 01 §6 failures 行互指（01 不另改）；`plugin/dsh/src/supervisor/failures.ts` `recordPlanFailure`/`FAILURE_BUCKETS`/`applyCircuitBreaker`；exec-arm 三失败点接线（verify 红跑 → verification-red / reclaim 清除成功（noop 不计）→ claim-expired-no-output / 派发失败 → executor-error）；writer.ts 增 `holdPlan`（status→held+hold+failures re-pin+claim 同写清除一原子写——claim-validity ⑤/writer-identity T5 零规则改动对接）；熔断四态 = held / 全 held 经 Phase 1 求值核心终态化 R3 blocked / 单 held 不阻塞（03 §4）/ held→active 同写重置 failures=0（writer-identity T6 既有执法面消费注记）③ **maxFailures 双源（终审 P2-3 收口）** `tools/mission-driver/src/law-policy.mjs` `resolveMaxFailures`（policy limits 权威 / mission `flow.maxFailures` 回退 / 双缺默认 3——镜像 resolveMaxAuditRounds 单权威+单回退）+ 回退键实落 `flows/mission-driver.json` 顶层 `maxFailures: 3`（镜像 :7 maxAuditRounds 先例、引擎惰性键、双源 3/3 一致无行为变化）+ `missions/autonomy.policy.yml` 头注 schema-reserved 句兑现 + host-adapter `MissionLawContext.maxFailures`/`SupervisorPolicyFace.maxFailures` 透传；**终态出口执行**：watchdog 周期后置面 = 熔断 pass → 新扫描 → R1–R4 求值 → 终态 = run-terminal 回执（含理由，A8 尽力投递 + onTerminal 链）+ `statusFace().terminal`（mdcontrol.status 透出）+ 循环停派（execute-posture 命中抑制，mount 内粘滞 + 跨重启重扫描幂等重评零新 store——restart e2e 钉住）；R1 与 audit-rounds-overflow deny 面互补注记（门禁拒新审计派发 + 守夜人收口双面，一个预算，e2e budget-gate 用例双断言钉住）；**0815-3 WI17 残项「R1/R3 终态执行」WI27 侧收口**（R3 声明面 e2e：partial 归一 + status + 停派 + 跨重启同词）；0815-1「maxFailures schema-reserved」头注残句兑现。测试 `plugin/dsh/test/supervisor-terminal.test.mjs` **22 例**（≥16 超额：四规则正反 + 顺序优先双例 + R1 三岔/静默边界 + 区分矩阵五例 + 复合值归一 + R4 N-1/N + maxFailures 双源六态 + 幂等重评 + 真扫描 fixture）+ `supervisor-failures.test.mjs` **9 例**（三桶各计一次/不计负例×3（CAS 域内 rig/观察与 no-keys skip/dedup 幂等跳过）/held 同写形态含 claim 清除/全 held 真扫描 R3 blocked/单 held 不阻塞/低于阈值幂等/重置边 allow+deny 双面）+ supervisor-trigger e2e 三例更新为 M3-WI27 执行面（budget-gate = deny 回执 + R1 run-terminal + status；terminal 出口 = 归一 + 回执 + 停派 + 跨重启同词；reason 边界注记同步）；验证 `npm --prefix plugin/dsh test` **342/342**（311 基线 + 31 新；tsc/freshness/smoke ok）、`pnpm --prefix tools/mission-driver test` **907/907**、web typecheck/build 绿（dist 零漂移）+ lint:prompts OK、`./verify-age.sh` L1+L2+L2.5 GREEN、`mission-check missions/age-autonomy-implementation.json .` exit 0；零引擎 diff（engine.js 空 diff）+ 零新增 npm 依赖；Deferred 两项登记（R4 停滞指纹供给 → M3-WI30 / 独立形态终态退出码 → M5-WI38 EXIT_MAP 冻结契约独立立项）；Follow-up round-1 P2「mission config 前向引用缺失文件」自愈事实注记 absorbed-by 本 plan 回写相位）
- [x] WI28 连续模式 opt-in：roadmap 即队列 + `mdcontrol.continuous` route + `mdcontrol.unlock` 路由 + 终态 receipt 回执（证据：plan `docs/plans/age-autonomy/2026-08-26-1954-1-m3-wi28-continuous-mode-opt-in-routes.md` 三 Phase 收口——① **opt-in 门（行为收紧）**：`decision-core.ts` `applyContinuousGate`（off 时 dispatch 型 execute-posture 决策降级观察回执，note 携门注记；meter-write/receipt 决策不受门）+ `watchdog.ts` per-root 内存标志（构造选项 = bundle config `supervisor.continuous` headless 预启用 / `setContinuous`/`isContinuous`/`setReceiptTarget`/`statusFace().continuous`；重启即清 ActiveRunGuard 先例）+ mount log BEHAVIOR TIGHTENING 句——1411-2「triggers 在场即 execute」收紧为显式开启（03 §4 opt-in 纪律执法，CONTEXT.md 增量段钉住可发现性）② **执行腿裁定**（Phase 1 Decision）：plan 执行仍是引擎 run 领地（0815-3 过渡裁定沿袭 + 1411-2 评审注记；守夜人 initial-execute 接线否决——executor prompt 组装是 M4-WI33 交付面；无引擎 run 宿主停滞由 R4 捕获 blocked + 回执等人；Deferred 后继 M4-WI33/M5-WI37 在册）③ **队列链式沿**（03 §3 边 2 接线）：`emitTerminalEvent` 末端 continuous on 时一次立即再评估周期（单飞守卫复用；mission terminal word 停派优先；心跳兜底；exec-arm 幂等承载双触发）④ **第六路由 `mdcontrol.continuous`**（toggle/查询 → `{enabled, posture, mounted}`；未挂载 toggle bad-request / 查询诚实闲置态 / root 不匹配双拒；`followup {sessionId}` 登记回执目标）⑤ **第七路由 `mdcontrol.unlock`**：held plan 人工处置——writer `unlockPlan`（held→active 同写 failures=0 + hold/claim 清除，writer-identity T6 形状零规则改动）+ `disposePlan`（cancelled|superseded|deferred 终态 disposition，01 §5.1 supervisor 合法边）；经守夜人 writer（写前 law 自检 role=supervisor + CAS + 原子落盘）执行，deny 结构化透出；law-rules.mjs :397/:407 过渡注记消解（「routing is M3」→「arrives via mdcontrol.unlock (M3-WI28)」，规则行为零改动）⑥ **终态回执接线**：continuous 开启会话登记 run-terminal 投递目标（A8 尽力投递既有，死会话容忍）⑦ **e2e**：`verify:e2e:continuous` npm script + `scripts/e2e-continuous.mjs` + fixture `e2e-continuous.cordis.yml`（缺 env fail-fast exit ≠ 0 / `DSH_E2E_CONTINUOUS=1` 真实宿主三连跑：off 观察 → on 账本派发链落地 → fixture 驱动 R3 blocked 终态回执 + late-draft 停派——**实跑 PASS**）；测试 `plugin/dsh/test/supervisor-continuous.test.mjs` **15 例 0 失败**（opt-in 三态/路由面/链式沿 on-off/Queue ≠ approval（held/draft 不拾取）/终态停派优先/重启清零/门作用域 + unlock·dispose 正例、参数域 deny、law deny 透出、回执目标接线）；验证基线只增不减：引擎 910 / 插件 357 / 真值表 113，`./verify-age.sh` L1+L2+L2.5 GREEN，`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 依赖）
- [x] WI29 崩溃恢复扫描：回收过期 claim、终态化残留 running、按 trigger 派发下一个（dispatch 行无结论时 resume or 重派）（证据：plan `docs/plans/age-autonomy/2026-08-26-1954-2-m3-wi29-crash-recovery-scan.md` 两 Phase 收口——① **恢复扫描执行化**：新 `plugin/dsh/src/supervisor/recovery.ts`（`scanStaleDispatches` 最新 valid dispatch 行 unpaired = 在飞 occurrence / `sessionLivenessOf` live·dead·undecidable 三态 / `runRecoveryScan` resume（原会话 followup 注入零新行）∨ redispatch（新 nonce8 dispatch 行经同一 law 自检 writer + `createDispatchAgent` 链，旧行 append-only 保留）∨ headless 降级（无 agents face = 不可判 → 观察回执不动作）/ per-mount handled 集 = 连续恢复周期零重复动作）；`watchdog.ts` cycle() recovery 分支（trigger='recovery' ∧ 未终态前置运行，fail-soft，不受 continuous 门——崩溃收尾非新无人值守推进）；过期 claim 回收沿既有 reclaim trigger（每周期求值含恢复周期，对齐注记非重实现）② **「终态化残留 running」处置表成文**（recovery.ts 头注 + 03 changelog 逐字收录）：claim TTL 未到期不动/已过期走 reclaim trigger；dispatch 无结论 = resume-or-redispatch；awaitingClosure = 1411-2 既有 trigger 面；引擎侧孤儿归 `reap-orphans.mjs` 零引擎 diff；LiveRunRecord 内存态随进程消亡（ActiveRunGuard 先例）③ **最新行作答（幂等面 × 评审租约单一语义面）**：`dispatch-resolve.ts` `dispatchAlreadyRegistered` 同 occurrence 多行取末位（deep-audit：最末行 paired → 开放下一轮，stale 行不占位）；`law-rules.mjs`（tools+assets 双副本）writer-identity 租约持有者 = 最末 unpaired dispatch review 行 sessionId（superseded 行不持约、最末行配对即关——redispatch 后 plan 写面不永久锁死，1411-2 显式移交的第二消费面收口）④ **deep-audit 同 occurrence 重派零自增**（01 §3.1）：恢复重派复用已付轮次号（counter=paidRound）跳过 audit-rounds 自增与预算预检重入；`audit-rounds-overflow` 门禁同轮次重派豁免（新行 iter ∈ 现 unpaired 轮次集 → 不耗预算不 deny——耗尽预算后死会话在飞仍可重派，「永不重派」死锁解除）⑤ **不计 failures**：02 §4.6 不计清单第四行（恢复 redispatch 不计三桶——「不把单次崩溃计为计划失败」）；recovery.ts 全模块零 recordPlanFailure ⑥ exec-arm 残项收口（头注移交注记 + reclaim 无 agents face 降级回执措辞 + `createDispatchAgent` 导出）；02 §4.6/03 §6/CONTEXT.md/packaging doc 同步；Deferred 在册「TTL 未到期死会话 claim 提前回收」（WI30 后继）+ 残险成文（stale 判定仅恢复语境——宿主存活期会话死亡等重启或 WI30 R4；存活误报 → TTL 回收 + WI30 双兜底）；测试 `plugin/dsh/test/supervisor-recovery.test.mjs` **9 例 0 失败**（本 plan 份额 7：过期 claim 恢复周期回收重派 / resume 零新行 / redispatch 旧行保留+幂等面最新行 / 不计 failures / 双周期幂等 / redispatch 后写面（drafter in-flight deny + 死会话 deny + 新 reviewer allow + 结案放行）/ deep-audit 零自增+预算耗尽不死锁；边界 2：预算宽裕变体 + 结案最新行重开 + headless 降级）+ 真值表 **113→116**（租约最新行 +2 / 门禁同轮次豁免 +1）；验证基线只增不减：引擎 910 / 插件 367 / 真值表 116，`./verify-age.sh` L1+L2+L2.5 GREEN，`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0，`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 依赖）
- [x] WI30 卡死检测 + 往返检测 + 停滞指纹（账本 hash + 活动信号）（证据：plan `docs/plans/age-autonomy/2026-08-26-1954-3-m3-wi30-stagnation-pingpong-detection.md` 两 Phase 收口——① **停滞指纹 + 活动信号检测器**：新 `plugin/dsh/src/supervisor/stagnation.ts` 纯函数（可注入 clock 与 activity map，确定性可测）——`computeLedgerFingerprint`（排序后 (planPath → basisHash) 行 + roadmap 全文 hash 聚合 sha256，computeBasisHash 同源；**指纹域 = basisHash 域**（frontmatter + Phase + Closure Findings）——DRR/Verification/Closure 追加在域外、dispatch/pass 行不重置指纹（评审派发 ≠ 进展），测试语义钉住：移动指纹的只有 status 翻转/勾选/Closure Findings 追加/roadmap 变更）+ `observeStagnation(state, input)` 纯转移（停滞腿：指纹不变 ∧ 活动窗口（默认 = 一个心跳周期）内 noteActivity 零命中 → 轮数 +1；**活动信号必须参与**（03 §7 字面）：有活动轮清零重积，长任务未落盘不误判；往返腿：per-plan 状态采样连续交替翻转计数，两态间 ≥ K 完整往返（2K 翻转）无终态进展（终态 = completed 派生 / cancelled 等写终态）→ **饱和注入** `{rounds: threshold, threshold}` 等效满足 R4 → 同一出口 blocked + 回执（03 §7「停滞检测收口」单出口纪律，03 §8 终态词表封闭；第三态破对重计/终态 disposition 重置/离场 plan 清行；threshold < 1 = 检测器 off 保守向））；承载 = watchdog 持有内存 scratch 态（03 §6 归零成文接受——重启重积，最多多等 N 轮不误杀）② **双入口同注入（1411-3 契约保持 + 评审阻塞项修复）**：检测器输出为 watchdog 持有单点——周期末端求值（既有入口）与 exec-arm `forwardTerminalDecision` 声明面入口（新 `ExecArmOptions.stagnationFact` 回调 seam）同读同一状态非仅周期入口；跨入口时序差 ≤ 一周期由幂等重评收敛，交叉用例钉零单周期分歧；检测器命中首跳 `stagnation-detected` 观察回执，终态沿 1411-3 既有 setTerminal 回执链零终态面改动（1411-3 面 zero-change）③ **N/K 阈值策略配置**：`law-policy.mjs` `LIMITS_FIELDS` 增 `stagnationRounds`（非负整数 schema 校验免费获得）+ `resolveStagnationRounds(policy, missionConfig)` 双源（policy 权威 / mission flow 回退 / 双缺默认 10——30s 心跳 × 10 ≈ 5 分钟墙钟换算成文；0 = 合法显式值 = 检测器 off）；K = `max(2, floor(N/5))` 完整往返同键派生（独立 `pingPongTimes` 键否决——02 §3 单键单义）；`missions/autonomy.policy.yml` limits 行 + 头注换算句；`flows/mission-driver.json` 顶层回退键（镜像 maxFailures 先例引擎惰性数据键）；host-adapter `MissionLawContext.stagnationRounds` 透传；tools+assets 双副本经 build-bundle 同步 ④ Proof：`plugin/dsh/test/supervisor-recovery.test.mjs` **18 例 0 失败**（1954-2 份额 9 + 本 plan 份额 9：指纹聚合确定性 + 域钉住（DRR/Verification/Closure 追加不动 / status 翻转·勾选·Closure Findings·roadmap 变更动）/ 停滞 N-1/N 边界 e2e（R4 blocked + stagnation-detected + run-terminal 回执）/ 活动信号参与正例（推进时钟 + noteActivity 清零重积）/ 往返检测 unit 边界（K 派生矩阵 + <K/=K/第三态/终态重置/threshold 0 inert）+ e2e oscillation（auditRounds=0 隔离 R1–R3——纯 hash 盲腿）/ 重启清零（fresh mount 不误熔断、满 N 重积）/ 双入口交叉（声明面与周期末端同词 blocked + 无 fact → 声明 deferred）/ partial/blocked 区分恢复语境变体（R3∧停滞 → blocked 叠加 / held==0 R3 → partial order-shadow，沿 1411-3 区分矩阵经声明面）/ `resolveStagnationRounds` 六态矩阵 + schema 面（负数/非整数 deny、未知键仍 deny）——WI31 门 ≥8 超额）；验证基线只增不减：引擎 **910**（law-policy 真实例 limits 断言随新键更新）/ 插件 **378** / 真值表 **116**，`./verify-age.sh` L1+L2+L2.5 GREEN（policy face 含新键校验），`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0，`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 依赖；03-supervisor changelog / CONTEXT.md / packaging doc 同步；Deferred 承 1954-2「TTL 未到期死会话 claim 提前回收」保持 watch-only residual（停滞指纹落地 = 兜底已就位，Successor M5/WI37 评估））
- [x] **WI31 Verification Gate — M3**（自动验证硬门，下列命令真实绿方可勾选）（证据：plan `docs/plans/age-autonomy/2026-08-27-0433-1-m3-wi31-verification-gate-law-cli.md` 两 Phase 收口——**Phase 1 交付 `gate-check.mjs --law` 执法面**（roadmap 门命令 4 此前面上不存在，0815-3 DRR iteration 2 裁定补模式不迁就修文）：第四 CLI 模式 `<plan.md> --law` = `discoverOwningMission` 祖先走查 → mission `autonomyPolicy`（回退 `missions/autonomy.policy.yml` 发现）`loadPolicyFile` 真实 policy → `evaluateGates` 全量 gates（ctx = plansRoots/roadmapPath/roadmapText/projectRoot/commands/agentNames/maxAuditRounds + 默认 verify 键复用 `plan-check.mjs missionDefaultVerifyKeys` 单一实现；P8 保护面语料仅当目标命中保护路径时注入，镜像单文件面条件读）→ stdout JSON：per-gate verdict + `deriveCompleted`/completionReasons + workItem 对账 + plan 队列谓词（draftPlans/activePlans/heldPlans/closedPlans/openPlans/awaitingClosure，mission plansDir 语料、排序确定性输出）；exit 0 iff 全部 enforce gate allow；trigger→closure 链路覆盖拆分裁定成文（trigger 派发腿归门命令 1 e2e，gate-check 不复刻 plugin 侧求值器——TS 引擎侧不可达 + assets 单向）；新引擎测试 `tools/mission-driver/test/gate-check-law.test.js` 10 例 0 失败（usage 面/allow 面+对账+派生视图/队列谓词钉住/伪造回执 enforce deny/P8 条件语料注入分支/无 owning mission/policy 回退发现/双缺 deny/legacy 域外放行），引擎 923/0 只增不减（基线 913+10），web typecheck/build 绿，`git diff --stat tools/mission-driver/src/engine.js` 为空；**Phase 2 四门实测**：① `node plugin/dsh/test/supervisor-trigger.test.mjs` **43 用例 / 0 失败** exit 0（门限 ≥20 超额；七 trigger 正反 + e2e 全链 echo fixture：pass 行落盘 → deriveCompleted → closure-audit dispatch）② `node plugin/dsh/test/supervisor-recovery.test.mjs` **18 用例 / 0 失败** exit 0（门限 ≥8 超额）③ `pnpm --prefix plugin/dsh run verify:e2e:continuous` 本机无宿主 env → **fail-fast exit 1**（`DSH_E2E_CONTINUOUS` 缺失即拒跑，括号语义分支如实记录；真实宿主三连跑 PASS standing evidence = WI28 plan `2026-08-26-1954-1` 实跑回执）④ `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-27-0433-1-m3-wi31-verification-gate-law-cli.md --law` **exit 0**——9 enforce gate 全 allow（plan-structure/closure-audit-binding/writer-identity/plan-completed/claim-validity/verify-keys/record-append-only/path-guardrail/legacy-plan-freeze）+ workItem M3-WI31 对账 ok + 派生视图（active/未全勾/missing-pass 逐项可解释）+ 队列谓词（activePlans 3 / closedPlans 16 / awaitingClosure 0）——自指先例：本 plan 自身 = active plan；**M3 里程碑收口（WI25–WI31 全 done）**）
  - `node plugin/dsh/test/supervisor-trigger.test.mjs` → trigger DSL 真值表至少 20 用例（含 `terminal-claim=nothing-to-draft ∧ draftPlans==0 ∧ activePlans==0` 派 deep-audit、`full-tick ∧ mechanical-verification-pass ∧ closure-receipt-missing` 派 closure-audit 等 7 条 trigger 全部覆盖）全绿
  - `node plugin/dsh/test/supervisor-recovery.test.mjs` → 崩溃恢复模拟至少 8 用例（过期 claim 回收 / dispatch 无结论 resume-or-redispatch / 停滞指纹 / 往返检测 / `partial/blocked` 显式区分）全绿
  - `pnpm --prefix plugin/dsh run verify:e2e:continuous`（如本地无 env 则 fail-fast exit ≠ 0，CI 视为 opt-in 不阻塞）→ 真实宿主下连续模式 e2e 三连跑全绿
  - `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/<active-plan>.md --law` → 活态 plan 走通 trigger→closure 链路

### M4 — P3 效率层：池化 + prompt 组装 + 上下文画像

- [x] WI32 agent 池（drafter:{projectRoot} / reviewer:{groupId}）+ CLOSURE_AUDIT/DEEP_AUDIT/multi-audit 禁入池（P7 原则；+ 角色互斥：同一 continuable subagent 不得同时为 drafter 与 reviewer/auditor，同 run 内 auditor session ≠ 任何执行者 session（终审 P2-5））（证据：plan `docs/plans/age-autonomy/2026-08-27-0433-2-m4-wi32-agent-pool.md` 三 Phase 收口——① **池模块与生命周期**：新目录 `plugin/dsh/src/efficiency/agent-pool.ts`（效率层首件；plugin src .ts 无 ALLOWED_MODULES 登记面，assets 计数不动 26）——角色池 `drafter:{projectRoot}` 长期复用 + `reviewer:{groupId}` 同批复用（poolKey 占位替换 {projectRoot}/{groupId}；组作用域 `groupScopeOf` = frontmatter `group:` → 文件名时间戳前缀回退（01 §2）→ 裸 stem 兜底；组 = 复用最大粒度、跨组必新启）；成员 = continuable 子代理句柄（经 agents face）：create-on-first-use + followup 续用 + 空闲 TTL dispose（可注入 timers + watchdog stop() 幂等 teardown → ctx.effect 生命周期，心跳先例；dispose 幂等）+ rotateEvery 计数轮换（同池派发达阈值强制换新）+ 池成员崩溃凭持久 session 恢复（liveness 三态：dead → 摘除换新）；内存态 = 性能缓存非状态权威（P2 模块头注成文，重启清空）；live policy `reviewer` 翻 `mode: pooled`（poolKey `reviewer:{groupId}`/idleTtlMinutes 30/rotateEvery 8——镜像 drafter 先例；anchoring 偏置残险由轮换 + 跨组新启兜底）② **独立性红线 + 派发链接线**：P7 审计禁入池 = closure-audit/deep-audit（multi-audit = deep-audit prompt 文件面随覆盖）**结构性绕过池**——无视 agent `mode` 配置池层硬拒 + 诚实注记（反向用例：auditor 声明 pooled 也恒 fresh、池恒空）；角色互斥（终审 P2-5）= session-role 登记表（drafter ∩ reviewer/auditor = ∅，冲突派发拒绝、host 复用 session id 同拒）+ 同 run auditor session ≠ 任何执行者 session（执行者集 = run 内 plans claim holders 账本 frontmatter 派生（runId 锚定剥离 attempt-<runId>-<ses>-<nonce8>）∪ 池 executor 标签，审计派发时校验候选 session，watchdog 每周期刷新透传）；池挂点在 `exec-arm.ts` `createDispatchAgent` 内部（**recovery redispatch 直调该函数自动经池无绕过孔**）：plan-review 经 reviewer 池、draft-plans 经 drafter 池，execute/closure-audit/deep-audit 保持 fresh 路径（executor pooled 声明**休眠**——WI28 裁定沿袭成文注记指向 M4-WI33/M5-WI37，executor 标签仍登记供红线消费；fresh 派发同样登记角色标签 = 登记表面非池面）；`dispatch-resolve` 零改动（池在解析链下游）；恢复互操作 = WI29 resume-or-redispatch 判据接 attemptId 代际（`attemptStale` 单一判据面：同代续用 / 跨代重派 + 显式 revoke + 成员摘除——活会话陈旧 attempt 也不 resume；headless 无 agents face 显式降级路径保持）③ **Proof**：`plugin/dsh/test/pool-lifecycle.test.mjs` **13 例 0 失败**（WI36 门 ≥10 下限：drafter/reviewer 生命周期 / 组作用域（同组复用·跨组新启·groupScopeOf 三腿）/ 空闲 TTL dispose / rotateEvery 轮换 + 代际（旧 stale·新 current·崩溃换新）/ P7 禁入（池层 + 派发链反向钉）/ executor 休眠注记 + 标签 / 角色互斥（登记表 + 派发链）/ auditor ≠ executor（候选校验 + executorSessionsOf 推导）/ 恢复同代 resume e2e + 跨代重派 e2e + 死成员换新 / 池复用 × 评审租约（同周期双派同 session + writer-identity per-plan 最新行租约）/ headless 降级）；验证 `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 923 / 插件 378→**391** / 真值表 116 只增不减），web typecheck/build 绿 + lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零依赖新增；Deferred 在册「上游文档 hash 变化触发的组内轮换」（M4-WI33 hash 台账落地后接线））
- [x] WI33 PromptAssembler（FRESH/CONTINUE 双模式 + `<file path hash sha256>` 嵌入 + 目录全文 + compaction 哈希台账）（证据：plan `docs/plans/age-autonomy/2026-08-27-0433-3-m4-wi33-prompt-assembler.md` 三 Phase 收口——① **assembler 纯函数 + assembly schema**：新 `plugin/dsh/src/efficiency/prompt-assembler.ts`（04 §3 全量：`assemble` FRESH=固定块聚合+动态块 / CONTINUE=hash 台账比对 deltaEmbed（未变跳过·变更全量重发）；`hash8Of` = sha256 前 8 hex（`computeBasisHash` 同算法源）；`renderEmbedStamp` 默认 `<file path="{path}" hash="{hash8}">{content}</file>` 可覆写模板；`resolveAssemblyBlocks` 经 `resolvePolicyPlaceholders` 单一占位解析面（{{projectRoot}}/{{plansDir}}/{{roadmapPath}}，单括号 poolKey 纪律不动）；目录全文嵌入 = 顶层文件排序确定性展开 + maxFileBytes 逐文件上限超限显式 `[NOT EMBEDDED: …]` 注记不静默截断；前缀纪律 = 固定块 → 动态块（薄指针 prompt 即动态后缀）→ volatile 尾缀，两次 FRESH 字节恒等；hash 三用 = dedup（CONTINUE 跳过）/ 陈旧检测（不符重发 + 池轮换判据）/ grep 可审计（path+hash 渲染进输出）；compaction 对抗 = 台账 `__sends` 派发计数 + 每 `COMPACTION_RESEND_EVERY=8` 次 CONTINUE 全 charter 重发（周期性重发不对抗压缩）；fail-soft = 缺文件/缺目录显式 note 永不崩溃；`commitToLedger` 单一写面（读/写分离，sentHashes 无 sentinel 污染）；`law-policy.mjs`（tools+assets 双副本 build-bundle 同步）`assembly:` 段 schema（`ASSEMBLY_FIELDS=[embedStamp, continueDelta]` + 三渲染槽存在性校验 + 布尔校验 + `DEFAULT_EMBED_STAMP` 同源导出）+ 引擎测试 +2 → 925/0；live policy `assembly` 块（默认 stamp + continueDelta: true）+ drafter `fixedPrefix`（`{{projectRoot}}/AGENTS.md` text persona + `{{projectRoot}}/docs/context/project-context.md` file 嵌入 maxFileBytes 60000）——P8 例外 = 本 plan active 自指先例；`gate-check --policy` exit 0 ② **消费接线**：`agent-pool.ts` 池成员增 `sentHashes` per-member hash 台账（随成员生灭，crash/轮换 → 替补空台账首发 FRESH——P2 保守向裁定成文）+ acquire 增 `currentFileHashes` charter-hash 轮换腿（04 §2.2 第二触发腿，**M4-WI32 Deferred 收编**：`charterHashesDiffer` 判增/改/删、sentinel 免疫，池层无 fs IO——hash 面由调用方注入）+ acquire 出口携台账；`exec-arm.ts` `dispatchPromptFor`（fixedPrefix ∧ assembly 材料在场 → 组装（薄指针 prompt 为动态块，04 §3.2/§7 叠加不取代）；`continueDelta: false` 钉 FRESH；否则薄指针逐字节透传）+ 四 followup 位统一接线（dispatchAgentExit / dispatchDraftPlans / reclaimClaim / recovery.ts redispatch——后两者经 lawCtx 占位 + io 注入）；`native-executor.ts` `assemblyPrefix` config 面（镜像 `nativeModelSelection` config-consumed 先例）：run 子代理首步 FRESH、后续步 CONTINUE（per-run 内存台账，resume 后保守 FRESH），engine prompt（promptsDir 覆盖链已解析）原样为动态后缀——policy 叠加不取代既有解析优先级（04 §7）③ **Proof**：`plugin/dsh/test/prompt-assembly.test.mjs` **16 例 0 失败**（WI36 门 ≥12 下限：FRESH 字节序+确定性 / 前缀纪律 volatile 后置 / CONTINUE dedup / 文件变全量重发新 hash / 目录全文排序+子目录跳过 / maxFileBytes 超限显式注记 / hash8=sha256-8hex+commit 协议 / text 逐字节 vs file 盖章+模板覆写 / compaction 第 8 次 CONTINUE 全重发 / charter 轮换判据（改/增/删/sentinel 免疫）/ 缺文件 fail-soft / 占位解析+schema 同源 / 池双模式 e2e（create FRESH·复用 CONTINUE·charter 变轮换 FRESH）/ 未声明部署逐字节向后兼容钉 / continueDelta:false 钉 FRESH / native-executor 首步 FRESH·次步 CONTINUE·无声明 legacy 逐字节）；执行期修正 = 测试 rig（charter 变更后需经 dispatchPromptFor commit 才入台账——补全真实派发流；假 agents `get` 需报活否则池误判崩溃换新；池 idle timer 须假定时器防吊住 node --test 事件循环）。验证：`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 923→**925**/0 · 插件 391→**407**/0 · 真值表 116/0 只增不减 · policy face ok）；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零依赖新增。Deferred 在册：画像 top-N 取材（WI34 后继）、独立形态 CONTINUE CLI 交付（WI35）、FRESH vs CONTINUE token 收益观测（WI36 观测项不阻断））
- [x] WI34 上下文画像 `docs/references/context-profile.json`（种子化 / run 终态挖掘 / 防抖 / schema 版本化）（证据：plan `docs/plans/age-autonomy/2026-08-27-0558-1-m4-wi34-context-profile.md` 三 Phase 收口——① **画像模块纯函数 + schema 版本化 + 种子化**：新 `plugin/dsh/src/efficiency/context-profile.ts`（效率层第三件；可注入 io/clock 零运行时 import）；工件 schema v1 `{version, seededFrom, updatedAt, entries:[{path,reads,lastSeenAt}]}`——`loadProfile`（缺失 = 未初始化→种子化路径；未知 version = 显式注记 + 保守重建重新种子化，不静默沿用）+ `saveProfile` 确定性序列化（entries 按 path 排序、字段序固定、单尾换行，同输入字节恒等）+ tmp+rename 原子写（writer.ts 先例 + 首启父目录 mkdir）；种子化 `seedFromReadFirst` 解析 AGENTS.md「Read This First」清单——仅直接路径行入表（整行恰一路径 token），「listed in …」间接引用行跳过；`mergeReads` 读频归并（reads 累加、lastSeenAt 刷新、projectRoot 前缀剥离归一）；**工件不进 `missions/`**（mission scanner 全量 `*.json` 扫描污染面，落 `docs/references/`）；`law-policy.mjs`（tools+assets build-bundle 同步）fixedPrefix 增第四 kind `profile`（`{kind, ref, topN?}`，topN 正整数且仅 profile kind 合法）+ 引擎测试 +1 → 929/0 + live policy drafter 追加 profile 块（P8 例外 = 本 plan active 先例沿袭）+ `gate-check --policy` exit 0 + 仓库首启种子工件入库 ② **挖掘接线 + 消费接线**：采集面三分层各有消费面——池成员句柄 `sessionEvents` 透出 + `memberSessionEvents()` 池面（零池语义改动）归集 `tool/call` 事件 × read/grep/glob 类文件参数计数（事件 schema 以 live 宿主 SessionEventMap 钉住）∨ `_tmp/<runDir>/run-state.json` steps[].promptFile 文本挖路径（存在性过滤）∨ Reflexion `memory/runs.md` 同法——缺面 fail-soft 注记非静默；headless（无 agents face）= 显式降级注记保留种子表不挖掘（独立形态完整面归 WI35）；watchdog `emitTerminalEvent` 链尾 run-terminal 事件触发一次挖掘（单飞守卫复用；fail-soft——采集异常只产回执不影响终态回执与停派；`profileMining` 挂载选项注入 io/sources）；防抖裁定落地 = 空 tally 不写盘 ∨ top-N 集（排序后**集**等价判定）不变不写盘（集内 rank 波动不触发、进出振荡天然抑制）；消费 = `resolveAssemblyBlocks` 钉 `profileRoot` + `filesOfBlock` 读工件按 reads 降序/path 升序取 top-N（缺省 5，DSL 逐 agent 覆盖）逐文件展开——复用 embedStamp/maxFileBytes/hash 台账/CONTINUE dedup 全部 file 语义，工件缺失/未知 version/无 projectRoot = 显式 note 不崩溃；角色显式 DSL 声明优先，未声明部署薄指针 prompt 逐字节不变（向后兼容钉，0433-3 先例沿袭）③ **Proof**：`plugin/dsh/test/context-profile.test.mjs` **13 例 0 失败**（WI36 门 ≥8 下限：种子化（直接/间接行区分 + 首启自动）/ run 终态挖掘（tool/call tally + 三层源归并 + watchdog 终态沿 e2e + fail-soft）/ 防抖（空 tally + 集 unchanged 双腿 + rank 漂移不写）/ schema 版本（v1 往返 + 字节确定性 + 未知 version 保守重建）/ 不进 missions/（路径域断言 + listMissionsString 零污染反向例）/ top-N 展开与角色覆盖向后兼容 / headless 降级注记）。执行期修正 = fsProfileIo 首启写盘前父目录 mkdir（e2e 探针发现）+ 防抖集比较从有序串改排序集等价（Phase 2 Decision 语义对齐——rank 波动不写盘）。验证：`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 925→**929**/0 · 插件 407→**420**/0 · 真值表 116/0 只增不减 · policy face ok）；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零依赖新增。Deferred 在册：独立形态挖掘采集面（headless，归 0558-2/WI35）、native-executor run 子代理默认启用画像取材（重开条件：观测显示重复读取成为成本主因时经 DSL 显式启用））
- [x] WI35 独立形态降级：池化 → `--session` 续用 + 前缀纪律（证据：plan `docs/plans/age-autonomy/2026-08-27-0558-2-m4-wi35-independent-form-degradation.md` 两 Phase 收口——① **承载裁定（Decision）**：独立形态效率面 as-built = 步内 `--session` 续用（correction/parse 共会话）+ 引擎 prompt 前缀纪律；**跨步续用不在库且不交付**（`engine.js` 三 `_executeAgentStep` 调用点 :1119/:1356/:1790 传 null、`lastSessionId` 唯一消费面 = correction :949）——CLI 会话池（continuable face `PoolAgentsFace` DSH 独有 + 跨 run 缺 attemptId 代际判据 = 陈旧上下文风险 + 引擎 dispatch 重构违反零引擎 diff）与 engine threading（零引擎 diff 底线 + 会话跨步累积改变 run 语义超出效率层「不改契约」边界）双备选否决 → Deferred 后继 M5-WI37 二选一裁定；残险 = 跨步 KV 缓存损失（04 §6「部分存在」收窄为「步内存在、跨步未交付」）② 步内续用覆盖核对**引用一致零缺面**（runner-routing :34-49/:69/:169-181 / prompt-cmdline-limit :60/:90-91 / step-executor :165 / transitions :431 / parse-fallback :164）零重复造测 ③ 前缀纪律审计 **11/11 模板零重排**（唯一大易变负载 `{{runSkeleton}}` 于 run-postmortem.md :28 居 27 行固定指令前导之后非前置，余 `{{var}}` 均小引用）+ 钉住测试 `tools/mission-driver/test/prompt-prefix-discipline.test.js` **15 例 0 失败**（live 渲染路径 `resolveTemplateVars` 双渲染共享固定前缀字节断言 + PAYLOAD/VOLATILE/STABLE 注册表 drift gate + 除 run-postmortem 外零 payload + payload 后置双断言），引擎 929→**944** 只增不减 ④ 如实成文：04 §6 as-built 注记（四点）+ 05 §4 对照行 ◐ 注记同步 + CONTEXT.md 独立形态降级段 + `independentChannelOf` seam 残项文字核对（reasoningEffort 无 config.js 载体维持 M5 independent-form gate = M5-WI37，零 seam 编辑零行为改动）；验证：`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 944/0 · 插件 420/0 · 真值表 116/0）、web typecheck/build 绿（dist 零漂移）+ lint:prompts OK、`git diff --stat` engine/runner/executor 为空（零引擎 diff）+ 零新依赖）
- [x] **WI36 Verification Gate — M4**（自动验证硬门，下列命令真实绿方可勾选）（证据：plan `docs/plans/age-autonomy/2026-08-27-0558-3-m4-wi36-verification-gate.md` 两 Phase 收口——prereq 核验 0558-1/0558-2 `gate-check --law` derived completed（五合取全真 + verification basisHash 双 fresh）后三门真实复跑全绿：① `node plugin/dsh/test/pool-lifecycle.test.mjs` **13 tests / 0 fail / exit 0**（门限 ≥10 超额）② `node plugin/dsh/test/prompt-assembly.test.mjs` **16 tests / 0 fail / exit 0**（≥12 超额）③ `node plugin/dsh/test/context-profile.test.mjs` **13 tests / 0 fail / exit 0**（≥8 超额）；观测项（终审 P2-4，不阻断）缺宿主 env 分支如实留档——`pnpm --prefix plugin/dsh run verify:e2e:continuous` **fail-fast exit 1**（`DSH_E2E_CONTINUOUS` 缺失 opt-in gate 拒跑，WI31 括号语义先例同型；且 `e2e-continuous.mjs` 为 fixture 驱动 scratch 面，mock 模型 usage 固定，在库无 FRESH vs CONTINUE token 差采集面）——「机制落地即收口，收益待观测」原句记录，重开条件 = 真实宿主连续模式长跑随运营观测自然采集（0558-3 Deferred watch-only residual，Successor Required: no）；M4 区块 **5/5 全勾**（WI32–WI36），收口复核 `roadmap-check` exit 0 + `./verify-age.sh` L1+L2+L2.5 GREEN（引擎 944/0 · 插件 420/0 · 真值表 116/0，0558 组执行后计数只增不减）+ `pnpm --prefix tools/mission-driver test` 0 失败）
  - `node plugin/dsh/test/pool-lifecycle.test.mjs` → 至少 10 用例（drafter/reviewer 池生命周期 / 空闲 TTL dispose / 代际令牌 / audit 禁入池）全绿
  - `node plugin/dsh/test/prompt-assembly.test.mjs` → 至少 12 用例（FRESH vs CONTINUE 字节序 / 缓存命中 / 目录全文 / hash 台账 / 文件变则重发）全绿
  - `node plugin/dsh/test/context-profile.test.mjs` → 至少 8 用例（种子化 / run 终态挖掘 / 防抖 / schema 版本 / 不进 `missions/`）全绿
  - （观测项，不阻断；终审 P2-4）真实宿主连续模式跑 3 个 plan，对比 FRESH vs CONTINUE 的 token 差 >20% 视为达到设计目标（机制落地即收口，收益待观测）

### M5 — P4 引擎退役判定门（可选收口）

- [x] WI37 评估门禁 + 守夜人覆盖引擎全部职责后，列出 engine 退役判定清单（transient 分类退避、pingPong、reconcile、L2 parity 等迁移证据）（证据：plan `docs/plans/age-autonomy/2026-08-27-1023-1-m5-wi37-engine-retirement-decision-checklist.md` 两 Phase 收口——清单工件 = `docs/design/age-autonomy/06-engine-retirement-checklist.md`（living decision-gate artifact，00-overview §7 P4 行 + §8 文档地图双登记 rg ≥2 实测）：① **覆盖矩阵 12 行**（transient 分类退避/循环防护 ping-pong·max*/审计预算轮门/失败预算与熔断/reconcile·孤儿回收/子流程编排四步链/draft 管线/主流程编排与 execute 腿/终态与退出码面（WI38 增补中如实登记）/monitor·run-state/marker 协议/L2 parity 证据——每行引擎职责→门禁+守夜人覆盖→证据指针（模块+测试+plan）→缺口→判定三态，rg 断言「判定」≥13 实测 22）；② **D1–D7 累积 Deferred 逐项裁定**（verdict/理由/重开触发三要素齐备，rg「重开触发」≥7 实测 11：D1 跨步续用维持 as-built 不交付/D2 reasoningEffort 维持无载体注记/D3 BUILD_VERIFY 保留至退役执行期/D4 execute 腿本轮不接线（前置 = 独立立项+真宿主长跑证据）/D5 死会话 claim 不立项（停滞指纹兜底已就位）/D6 cron 声明面文档 seam 完备/D7 marker 物理删除 = 退役执行期动作）；③ **总判定 = 引擎留任主后端（条件退役）**——账本域职责已覆盖（预算/熔断/恢复/队列/终态/派发），run 内执行语义面/execute 腿/run-state·monitor/EXIT_MAP 冻结契约/marker 存续期 = 引擎留任面；缺口前置清单 G1–G6（execute 接线/独立形态后端替代/EXIT_MAP 增补（归 WI38）/跨步续用/marker 删除/monitor extends P2）；03-supervisor §10 指针 + changelog、CONTEXT.md 增量段同步；零代码变更（零引擎 diff 零插件行为变更零新依赖）；验证 `pnpm --prefix tools/mission-driver test` **947/947 / 0 失败**（944 基线 + 新 plan corpus 面 3）+ `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 947 · 插件 420 · 真值表 116，计数只增不减））
- [x] WI38 `partial/blocked` → `EXIT_MAP` 显式增补（保护契约变更走独立立项 + 测 `exit-map.test.js`）（证据：plan `docs/plans/age-autonomy/2026-08-27-1023-2-m5-wi38-exit-map-partial-blocked.md` 两 Phase 收口——① **契约变更（03 §8 终态映射纪律的独立立项兑现）**：`src/exit-map.js` EXIT_MAP 11→13 键，`partial: 3, blocked: 3` = 新退出码类「终态非完成、需人工处置」（补救 = unlock/dispose 人工裁定），与 2 码族（预算/上限保护——重跑）与 1 码（不可恢复失败）分离；备选否决四项（复用 2 / 分离 3·4 / 映射 1 / 维持 fall-through exit 0）与重开触发（出现按退出码分支的真实消费者）成文于 plan Phase 1 Decision；头注更新（supervisor 终态词入表理由 = 前瞻防护独立形态暴露面；`skipped`/动态 done 词 fall-through 语义不动）；`EXECUTION-PRINCIPLE.md` §11 表增两行 + 表尾语义句（词源守夜人 03 §8 R1–R4、引擎存续期 `_result` 不发射）逐行同步；plugin assets 副本经 build-bundle 同步（bundle freshness 门绿）。② **测试钉住**：`test/exit-map.test.js` 13→19 例——DOCUMENTED 表翻 13 键 + 「exactly 13」drift guard + M5-WI38 专项 4 例（`partial`→3 / `blocked`→3 / 既有 11 键逐行回归含 `single_step_done`→0 与四键 2 码族 / `skipped`·未映射动态词 fall-through exit 0 + 头注一致性断言）。③ **零行为变更**：`git diff --stat tools/mission-driver/src/engine.js` 为空、引擎不发射两词（DSH 回执面不变）、零新增 npm 依赖、monitor web 零触碰。④ **回写**：03-supervisor §8 changelog 独立立项兑现注记 + CONTEXT.md 关键约束增量句（EXIT_MAP 13 键）+ `> Last Updated` 头「M5 第二片」句 + `docs/logs/2026/08-27.md` 条目。验证：`pnpm --prefix tools/mission-driver test` **953/953 / 0 失败**（947 基线 + 6 新例只增不减）+ `./verify-age.sh` **L1+L2+L2.5 GREEN**）
- [x] WI39 docs/design + architecture owner-doc 一致性收口（关闭 §Deferred But Adjudicated 立案条目）（证据：plan `docs/plans/age-autonomy/2026-08-27-1023-3-m5-wi39-ownerdoc-consistency-closure.md` 三 Phase 收口——清点矩阵 **42 行**落 `docs/logs/2026/08-27.md`（rg 全量命中 **33 行**零遗漏对账：packaging 9 / 05-usage 2 / 01-file-ledger 7 / 02-rule-law 1 / 03-supervisor 3 / 06 清单 10 / 00-overview 1、baseline 零命中 + Current Baseline 已知项与人工清点补充 8 行 + Non-Goals 归属注记 1 行）；三态分布 = closed-by-evidence **11** / still-deferred **6**（触发条件复核均不变）/ superseded **1** / 已核无需动作 **24**；四 Fix 项——① 02 §4.7 as-built 放置与保护姿态注记（引擎侧 law 内核三模块 0815-1 放置 + 不入 P8 字面集理由 + 替代保护面 import 闭包/build-bundle freshness/CI+立项纪律，**收口 0950-1 WI21 Deferred「law 内核 P8 覆盖缺口」**+ 02 changelog 行）② 05 §2.2 unlock 行「目标能力」→ M3-WI28 已落地指针 + continuous 行复核落 route 名 + 头部句精确化（`mdcontrol.stop` 仍目标形态——wire 七路由无 stop 实证）+ 05 §4 池化对照行 D1 裁定指针 ③ 00 §7 P0–P4 落地状态注记（P0–P3 落地 plan 指针 + P4 = WI37 总判定 + G1–G6，事实性增补不改契约语义）④ 其余矩阵项：06 清单行 #9 + G3「增补中/进行中」→ WI38 已收口刷新（13 键/19 例）、04 §6 + 05 §4「归 M5-WI37」→ D1 维持不交付裁定指针、baseline.md §Public Exports `src/exit-map.js` 补 M5-WI38 增量登记（11→13 键 supported behavior 变更的 same-change 登记补偿 + M3/M4 supervisor/efficiency 面判定 = plugin 侧归 packaging doc 无需 baseline 登记）、packaging doc native-panel T1–T3/run-guard/draft-split/preset-residual 全条目复核准确零编辑；零代码 diff、零新增 npm 依赖、diff 自查零契约语义变更（全部事实性增补/状态注记/三态处置）；monitor extends P2 附挂 deferral 按 Non-Goals 仅注记归属（后继代码修复 slice）；验证 `pnpm --prefix tools/mission-driver test` **953/953 / 0 失败** + `./verify-age.sh` **L1+L2+L2.5 GREEN** + `gate-check <本 plan>` 自指 exit 0（decision allow、workItem 对账 ok））
- [x] **WI40 Verification Gate — M5（最终关门）**（自动验证硬门，下列命令真实绿方可勾选；任何一条红 = 整 roadmap 不收口）（证据：plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md` 三 Phase 收口——Phase 1 清偿 Follow-up P2「monitor 读 mission 配置未走 extends 合并」：`monitor.js` 三配置读面（`readMissionConfig`/`handleListConfigs`/`handleGetRoadmap`）统一经 `mission-check.mjs loadMission` 单一合并实现（base → base.local → mission；display 面不传 projectRoot、throw → null/skip 优雅退化、`!roadmapPath` 过滤防线保留），继承 mission 在 configs/roadmap/plans/run 详情四 API 面显示有效配置，`monitor.test.js` +7 用例钉住（引擎 HEAD 基线 954 → **961/0** 只增不减）；Phase 2 六门实测全绿——`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 961/0 · 插件 420/0 · 真值表 116/0）+ `pnpm --prefix tools/mission-driver test` **961/961 / 0 失败** + `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` **exit 0** + `pnpm --prefix plugin/dsh run verify:e2e` **真实宿主 e2e PASS exit 0**（env 在场实跑绿：demo 双后端 shape identity + correction-retry 恢复 + onboarding 双形式 parity + monitor render oc-/native- + WI12 三技能——非 fail-fast 括号分支）+ `gate-check <WI40 plan>` 自指 exit 0 + M4 已收口语料 `gate-check <WI36 plan>` exit 0（closure-audit-binding / writer-identity / plan-completed 三硬门全 allow face）+ 勾选前 `grep -c "^- \[ \]"` = 1 确认；Phase 3 独立 subagent 收口审计 **findings=none**（M1–M4 全部 Verification Gate 复跑绿：plan-check --strict / 961·0 / ledger-frontmatter 23·0 / gate-check --policy / 真值表 116·0 / WI22 语料 gate-check / mission-check / supervisor-trigger 43·0 / supervisor-recovery 18·0 / verify:e2e:continuous 缺 env fail-fast = 文档化括号语义 / WI31 --law exit 0 / pool 13·0 · assembly 16·0 · profile 13·0 + 三硬门 allow + 引擎零 diff 零新依赖 + Follow-up 行清偿核验；回执见 plan `## Closure` 同 id 配对）；收口审计回执落点 as-built 裁定（plan Phase 3 Decision）：末条门字面 `docs/audits/age-autonomy/<final-closure>.md` 为 M2-WI22 证据面重建（2026-08-25）前旧指针——外部审计生命周期通道已退役，plan 级收口审计唯一面 = plan `## Closure` 回执、mission 级 = roadmap `## Deep Audit Record` inline，不新建旧形文件；勾选后 `grep -c "^- \[ \]"` → **0** 实测（roadmap 全 done，M1–M5 五里程碑 44 WI 全勾））
  - `./verify-age.sh` → L1 + L2 全绿
  - `pnpm --prefix tools/mission-driver test` → 0 失败
  - `pnpm --prefix plugin/dsh run verify:e2e` → 真宿主 e2e（缺 env → fail-fast exit ≠ 0；CI 视为 opt-in 不阻塞）
  - `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` → exit 0
  - `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → 0（roadmap 全 `done` 后无未勾项；与 M1 WI11 末尾 grep 协同，但语义不同：此处检查**所有** WI 完成态 + Verification Gate 完成态）
  - `docs/audits/age-autonomy/<final-closure>.md` 收口审计通过（独立 subagent 跑通 M1–M4 全部 Verification Gate + 三硬门全 allow face 验证；`pass` 才允许整 roadmap 全 `done`）

## Dependencies & Notes

### 形态聚焦：DSH 插件优先

- 本 roadmap 在落地上**优先 DSH 插件形态**（`plugin/dsh/`）——`docs/design/age-autonomy/00-overview.md` §5 双形态产品中 DSH 形态有宿主原生循环、pre-execute 拦截面、native agent dispatch 三个**独有**优势，可让门禁/守夜人/池化以最低成本落地。
- **零引擎 diff 为底线**（`tools/mission-driver/src/engine.js` 状态机核心 + 零 npm 依赖约束不破）：所有新增逻辑沉淀在 `plugin/dsh/src/{ledger,law,supervisor,verify,efficiency}/` 五个子目录。
- 独立形态（CLI + `mission-driver.sh run`）的门禁通过 `tools/mission-driver/src/gate-check.mjs` CLI 复用同一纯函数（结构子集，no actor）；pre-commit hook 与 CI job 同源。

### 阶段依赖

- M1 是后续阶段的地基；M1 WI11 Verification Gate 未绿前禁止 M2 切 enforce。
- M2 的硬门（WI14–16）+ WI22 须以**零引擎 diff** 路径落地；插件层承担。
- M3 触发 trigger DSL（WI26）需要 M2 schema（WI13）钉稳；可与 M2 并行观察但 enforce 需 M2 后。
- M4 全部为插件层增强；不影响账本/法律/完成语义，可与 M3 并行观察。
- M5 是 P4 判定门；当前不必预先启动。

### 自动验证纪律

- 每个 Verification Gate WI（WI11 / WI24 / WI31 / WI36 / WI40）由**真实命令 + 真实断言**组成；不允许「视觉上完成」「chat 上完成」式收口。
- 任何 Verification Gate 红 = 整 milestone 不收口 = 整 roadmap 不收口。
- 独立 subagent 跑最终收口审计（WI40）方可标记 `done`。

### 计划与回写

- 每个 WI 触发 planning rules 时按 `docs/plans/00-plan-authoring-and-execution-guide.md` 建 plan；plan 落地后由实施者回写本文件 `Work Item Status`。
- 计划模板改动：plan guide（rule 9/12）将与 M1 WI9 同步更新；M1 落地前旧 plan 继续按现行 guide 写。

### marker 迁移纪律

- `<AI_STEP_RESULT>` / `<FLOW_VARS>` 是第二真相通道（与 `> Plan Status:` 行同病）——M1 落地后这两类 marker 在 plugin 路径**仅作诊断/日志**（correction-retry 反馈面、Reflexion postmortem 输入、monior 人类可读面），不参与 status 转移与跨步传参的裁决。
- 引擎后端保留 marker 解析作为双读过渡协议；M5（引擎退役判定门）评估物理删除时机。详见 `docs/design/age-autonomy/00-overview.md` §4「marker 溶解」。

### 与其他 mission/roadmap 的关系

- 本 mission（`age-autonomy-implementation`）是 age-autonomy 设计在仓库内的**唯一执行 mission**；不存在姊妹 mission 复刻同一份 design。
- 旧 `docs/backlog/age-autonomy-plugin-roadmap.md`（如未来再有类似产物）应视为本 roadmap 的「插件形态视角」，不应另立 mission；本路线选择后该产物应被本 roadmap 收编或弃用。

## Deep Audit Record

- dispatch audit #audit-2026-08-25-063133-mission-driver-age-autonomy-implementation-roadmap-1-38473bf4 to ses-2026-08-25-063133-deep-audit-r1
- accepted #audit-2026-08-25-063133-mission-driver-age-autonomy-implementation-roadmap-1-38473bf4 findings=items：1×P0（D2 账本收口死锁无 roadmap 项 → WI41）+ 2×P1（frontmatter 校验器零生产接线 → WI42；架构 owner-doc 契约漂移 → WI43）+ 4×P2（Follow-up Backlog）；基线 810 tests green、corpus 0 error、mission-check exit 0
- dispatch audit #audit-2026-08-25-063133-mission-driver-age-autonomy-implementation-roadmap-2-faae192a to ses-2026-08-25-063133-deep-audit-r2
- accepted #audit-2026-08-25-063133-mission-driver-age-autonomy-implementation-roadmap-2-faae192a findings=items：1×P1（`verify: []` 空数组使 mechanicalVerification 合取空真、零 pass 行即派生 completed → WI44）+ 3×P2（Follow-up Backlog）；WI41/WI42/WI43 复核仍 open 未重复立项；基线 810 tests green + prompt-check OK + mission-check exit 0 + web typecheck green

## Follow-up Backlog

  - [x] ~~[P2] mission config 前向引用缺失文件：`autonomyPolicy: missions/autonomy.policy.yml` 与 `commands.gates` → `tools/mission-driver/src/gate-check.mjs` 今日均 ENOENT（WI12/WI13 落地后自愈；mission-check 不校验这两字段，期间无人报错）。source: deep-audit round 1~~ **已清偿（自愈事实注记 absorbed-by M3-WI27 回写相位，plan `docs/plans/age-autonomy/2026-08-26-1411-3-m3-wi27-terminal-rules-failures-buckets.md` Phase 3；0950-3 absorbed-by 先例）**：两文件 live 在位核实（2026-08-26：`missions/autonomy.policy.yml` 自 M2-WI12/WI13 落地即在库且 18 gates 全 enforce；`tools/mission-driver/src/gate-check.mjs` 同批落地）——「缺失」状态自愈成立，且 mission 字段 `autonomyPolicy` 设置即经 mission-check 校验存在（M2 起），无悬空引用残面。
  - [x] ~~[P2] roadmap 残留已退役格式段：`## Status Values` 表与 `Work Item Status` 导语中的 `ready` 生命周期散文，按 00-roadmap-authoring-guide 2026-08-25 changelog 已退役（done=勾选、ready 语义归 plan 侧）。source: deep-audit round 1~~ **已清偿（absorbed-by M2 收口，plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md` Phase 3）**：`## Status Values` 表删除、`Work Item Status` 导语改列 0 勾选框语义（评审态归 plan frontmatter `status`），按 00-roadmap-authoring-guide 2026-08-25 现行格式归一。
  - [x] ~~[P2] `scanPlanLedger`/`scanRoadmapLedger` 对重复 append-only 锚点静默容忍（只扫首个 `## Closure`/`## Verification`/`## Draft Review Record`/`## Deep Audit Record`，重复区内的回执从派生面消失且无结构 error）——建议补 duplicate-anchor 结构 error。source: deep-audit round 1~~ **已清偿（M2-WI22，plan `2026-08-25-0950-2` Phase 2）**：重复锚点现报 `duplicate-anchor` 结构 error 进 scan.errors（首锚定派生面保留），正反例 + 存量 corpus 零误伤断言钉住（`test/ledger-sections.test.js`）。
  - [x] [P2] 引擎读面（flow-loader/monitor 的 `planLedgerState` 调用）未注入 `defaultVerifyKeys`，01 §4.1「verify 缺失时用 mission 默认」在引擎路径未实现——省略 `verify` 的 plan 即使回执齐全也派生不出 completed（与 WI41 同族，裁定归 WI19/WI41 立项）。source: deep-audit round 1（已清偿 2026-08-25 WI41：flow-loader 谓词族 + closureScriptCheck + plan-check CLI 注入 `["test"]`，单一实现 `plan-check.mjs missionDefaultVerifyKeys`；monitor 显示面按 WI41 plan「Deferred But Adjudicated」不注入——display-only + extends 合并 P2 前有误读风险，归 monitor extends P2 修复时顺带）
  - [x] [P2] roadmap 头部 `> Last Updated: 2026-08-24` 过期：2026-08-25 已落地 round-1 回执、WI41–WI43 与 Follow-up Backlog 各轮编辑，头部日期未同步更新。source: deep-audit round 2（已清偿 2026-08-25：WI41 回写同步头部日期；0925 批次其余 plan 回写时维持同步；M2 收口 plan `2026-08-25-0950-3` Phase 3（round-2 本条对应的最近一次刷新）再同步——absorbed-by 持续清偿）
  - [x] ~~[P2] monitor 读 mission 配置未走 `extends` 合并（`readMissionConfig`/`handleGetRoadmap`/`handleListConfigs` 裸 JSON.parse，引擎侧 `loadMission` 走 base→local→mission 合并）——从 base.json 继承 roadmapPath/plansDir/commands 字段的 mission 在 dashboard 显示为空/缺字段且被 `/api/configs` 过滤；本仓库无实害，模板消费者面 latent。source: deep-audit round 2~~ **已清偿（M5-WI40，plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md` Phase 1）**：monitor 三配置读面统一改经 `mission-check.mjs` 导出面 `loadMission`（单一 extends 合并实现，base → base.local → mission；display 面不传 projectRoot 保持 dangling-path 容忍、throw → null/skip 优雅退化、`!roadmapPath` 过滤防线保留），继承 mission 在 `/api/configs`·`/api/configs/:name/roadmap`·`/api/configs/:name/plans`·run 详情 config 面显示有效合并配置；`monitor.test.js` +7 用例钉住（合并链可见/继承 roadmap·plans 解析/base.local 优先级/dangling graceful skip/base 过滤回归钉/无 extends 形状钉）。
  - [x] [P2] `prompts/draft-from-roadmap.md` step 4 指示 drafting agent 自派 sub-agent 评审后自行将 plan 置 `active`，引擎 REVIEW_PLANS 步骤对这些 plan 变成空转（`draftPlans()` 为空 → forEach all_complete 直通 EXEC）——评审独立性从流程结构保证退化为 prompt 纪律，直至 M2-WI15 写者身份门禁落地（0815 批次回执实践正常，暂无实害）。source: deep-audit round 2（已清偿 2026-08-26：absorbed-by M3-WI26 plan `docs/plans/age-autonomy/2026-08-26-1411-2` Phase 3 Fix——step 4 重写为「起草后保持 draft；评审由守夜人/引擎 REVIEW_PLANS 派发独立 reviewer；起草者不得自派评审、不得自行置 active」，守夜人 plan-review trigger（`plan.status=draft ∧ review-dispatch-missing` → dispatch reviewer + dispatch 行进 Draft Review Record）与 writer-identity 门禁承接执法——评审独立性回到流程结构，lint:prompts 绿 + assets 副本同步）
