# DSH Plugin Packaging — Technical Architecture

> **Status: P1 DELIVERED (2026-08-23) — StepExecutor seam, ProcessExecutor, programmatic orchestration entry + EXIT_MAP hoist, driver validation, and embed-mode gating have landed (M1-WI1..WI5). P2 DELIVERED (2026-08-23, M2-WI6 + M2-WI7 + M2-WI8 + M2-WI9 + M2-WI10): the plugin shell (`plugin/dsh/` bundle manifest + isolate-realm patch + build bundling with an import-closure gate), the NativeExecutor dispatch backend + engine-bridge backend-selection factory, the L2 dual-backend parity matrix (merge-blocking via `verify-age.sh` + `age-ci.yml`), the L3 host-integration harness (`scripts/host-harness.mjs` + env-gated `verify:native` local gate; recorded keyless green run in `docs/testing/2026/08-23.md`), and the `mdcontrol.*` route layer with the async job contract + plugin-level active-run guard + the L4 dual-leg e2e (`mdcontrol.run` immediate start → `mdcontrol.status` polling on a real in-process cordis runtime; `npm run verify:e2e` gate; identical normalized run-state shape, markers parsed, one artificial correction-retry recovered — evidence `docs/testing/2026/08-23.md`) have landed. P3 first slice delivered (2026-08-23, M3-WI11: onboarding dual-form parity + descriptor registration + the monitor step-log dual-label fix — same `verify:e2e` gate, evidence `docs/testing/2026/08-23.md`). P3 second slice delivered (2026-08-23, M3-WI12, plan `2026-08-23-1852-2`): the `mdcontrol.draft`/`mdcontrol.analyze` routes (1621-2 deferred adjudication collected; draft through the pre-authorized `cmdDraftMission` executor seam — the plan's ONLY engine diff, CLI byte-identical — analyze through a plugin-owned thin runner adapter over `runPostmortem`, zero engine diff) + the three Mission Control skills registered on `ctx.skills` and wired to the routes (same `verify:e2e` gate extended with skills/analyze/draft legs, evidence `docs/testing/2026/08-23.md`). P3 third slice delivered (2026-08-23, M3-WI13, plan `2026-08-23-1852-3`): the `tools/pre-execute` plan-status reinforcement gate (matcher anchored on the bundled `PLAN_STATUS_RE`; adjudicated run-state evidence rule F1 in-flight subflow / F2 closed CLOSURE_AUDIT / F3 fast-path, deny iff evidence surface exists and no face holds; `verify:e2e:gate` real-host tool-pipeline gate + 23-case unit truth table with engine-generated fixtures, evidence `docs/testing/2026/08-23.md`) has landed — RETIRED 2026-08-25 by age-autonomy M2-WI22 (plan `docs/plans/age-autonomy/2026-08-25-0950-2`): protection semantics collected into the law kernel rule `legacy-plan-freeze`, run-state evidence faces abolished — see §Service Surface. **P3 complete**. P4 first slice delivered (2026-08-23, M4-WI14, plan `2026-08-23-2202-1`, zero engine diff): the AGE agent preset (`plugin/dsh/preset/age/` — mode-prompt section + standard-subset tool catalog + zero service rows), the NativeExecutor agent-preset composition setup (every create mounts `meta.agentPreset` through the host roster when composed; R1 §7 host-loader verification closed), the `bootstrapNativeConfig` base.json `agent` defaulting for native runs, and the `verify:e2e:preset` composition gate (roster + interactive mount + mission child + realm-collision face, evidence `docs/testing/2026/08-23.md`). **P4 complete** (WI15 delivered 2026-08-23, plan `2026-08-23-2202-2`: the status-panel decision adjudicated as **deferral** — mechanism reachable, rc/undocumented API + no demand; owner-doc write-back + deferred record with reopen triggers T1/T2/T3; pure-docs change-set, zero engine/plugin diff, existing gates green). Phase gates below define when each remaining claim becomes supported behavior. Increment (age-autonomy M3-WI25, 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1411-1`): the supervisor seam landed as the bundle's SECOND cordis service publication (`mdsupervisor` under `src/supervisor/` — decision core + watchdog + sole machine-field writer (Q4 ③) + receipt face, observe-only until M3-WI26; unit chain 23 cases, zero engine diff, zero new dependencies). Increment (age-autonomy M3-WI26, 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1411-2`): trigger execution + dispatch wiring — the pure 14-predicate trigger evaluator (dual domain, fail-soft), the dispatch resolution chain (three-field model binding filling the native-executor ModelSelection documented gap, plan `agent:` override routing, requireDistinctModel runtime three-state via the shared `sameModelPair`), the seven-exit execution arm (verify-runner pass-line write, dispatch registration, deep-audit budget+meter same-write, reclaim/re-issue, P2-1 bounded ledger claim renewal, terminal forwarding to 1411-3), the draft-from-roadmap step-4 independence fix, and `verify-runner.mjs` now a LIVE assets consumer (still engine-closure-unreachable by design); unit chain 23+43+22 cases, zero engine diff (`engine.js` untouched), zero new dependencies. Increment (age-autonomy M3-WI27, 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1411-3`): the R1–R4 termination evaluation core + failure attribution buckets + circuit breaker landed under `src/supervisor/` (`terminal-rules.ts` pure sequential evaluator with partial/blocked explicit distinction + `failures.ts` three-bucket metering + breaker; `law-policy.mjs` gains `resolveMaxFailures` — policy-authoritative / mission-flow fallback / default 3, with the `flows/mission-driver.json` top-level `maxFailures` key mirroring `maxAuditRounds`; terminal execution = run-terminal receipt + `mdcontrol.status` exposure + dispatch suppression, dual entry one core, restart-idempotent); unit chain +22 (terminal truth table) +9 (failures/breaker) cases, zero engine diff, zero new dependencies. Increment (age-autonomy M3-WI28, 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1954-1`): continuous-mode opt-in — the `mdcontrol.continuous` route (sixth: per-root opt-in toggle/query; default OFF, in-memory per-root flag mirroring the ActiveRunGuard precedent, `supervisor.continuous` bundle-config pre-enable for headless) + the `mdcontrol.unlock` route (seventh: held-plan human disposition — `unlock` held→active same-write failures=0/hold removed + `dispose` cancelled|superseded|deferred, through the supervisor writer role=supervisor) + the `applyContinuousGate` posture gate (OFF downgrades dispatch-type execute-posture decisions to observation receipts — behavior TIGHTENING over the 1411-2 triggers-present-is-execute posture; meter/receipt decisions ungated) + the onTerminal chain edge (03 §3 edge 2: one immediate re-evaluation cycle when continuous is on) + the run-terminal receipt target wiring (continuous-enabling session via route `followup`, A8 best-effort unchanged) + `scripts/e2e-continuous.mjs` with the `verify:e2e:continuous` gate (env-gated fail-fast exit ≠ 0 without `DSH_E2E_CONTINUOUS=1`, never CI-blocking; live three-leg run over the real in-process cordis host + scripted model endpoint); unit chain +15 (`supervisor-continuous.test.mjs`), zero engine diff, zero new dependencies. Increment (age-autonomy M3-WI29, 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1954-2`): the crash-recovery scan landed (`src/supervisor/recovery.ts` — the restart-duty action face the watchdog's recovery-labeled first cycle runs: un-concluded dispatch occurrences → resume (live session followup) ∨ redispatch (new nonce8 line, old line append-only, deep-audit paid-round reuse with no increment + the matching audit-rounds-overflow gate exemption); latest-line semantics in the dedup face and the writer-identity review lease; law-rules.mjs edits flow to assets via the build; unit chain +9 (`supervisor-recovery.test.mjs`) + truth table 113→116, zero engine diff (`engine.js` untouched), zero new dependencies). Increment (age-autonomy M3-WI30, 2026-08-27, plan `docs/plans/age-autonomy/2026-08-26-1954-3`): the §7 stagnation fingerprint + ping-pong detector landed (`src/supervisor/stagnation.ts` — pure `computeLedgerFingerprint` (per-plan basisHash set + roadmap full-text hash, basisHash-domain pin: DRR/Verification/Closure appends outside) + `observeStagnation` (stagnant round = fingerprint unchanged ∧ zero noteActivity in the window — activity MUST participate, clears the count; ping-pong leg = consecutive alternating flips ≥ K round trips without terminal progress → saturated `{rounds: threshold, threshold}` injection into the SAME single R4 exit; in-memory scratch, restart clears); the fact is a watchdog-held single point read by BOTH R4 entries (cycle end + the declared face through `ExecArmOptions.stagnationFact` — dual entry, one detector, zero single-cycle divergence); policy key `limits.stagnationRounds` via `law-policy.mjs` `resolveStagnationRounds` (policy-authoritative / mission-flow fallback / default 10 ≈ 5 wall-clock minutes at the 30s heartbeat; `flows/mission-driver.json` top-level fallback key; K = max(2, floor(N/5)) derived from the same key; 0 = detector off); `supervisor-recovery.test.mjs` grows 9→18 (the WI31 ≥8 gate), zero engine diff, zero new dependencies). Increment (age-autonomy M4-WI32, 2026-08-27, plan `docs/plans/age-autonomy/2026-08-27-0433-2`): the efficiency-layer agent pool landed (`src/efficiency/agent-pool.ts` — 04 §2 role pools `drafter:{projectRoot}` / `reviewer:{groupId}` with groupScopeOf (frontmatter `group:` → filename timestamp prefix fallback → bare stem), create-on-first-use + followup reuse + idle-TTL dispose (injectable timers, watchdog-stop teardown) + rotateEvery rotation + attemptId generation tokens (`attemptStale` consumed by the WI29 recovery scan: same-generation resume / cross-generation redispatch + explicit revoke); independence red lines enforced IN the pool layer — the P7 audit ban (closure-audit / deep-audit / multi-audit structurally never pool regardless of agent `mode` config, hard-refuse + honest note), the session-role mutex registry (one continuable subagent never both drafter and reviewer/auditor; same-run auditor ≠ any executor, executor set = plan claim holders from ledger frontmatter ∪ pool executor tags, checked at audit dispatch); executor pooling declared-but-dormant (the WI28-continuation ruling — execute bypasses the pool with the explicit successor note); the pool hook lives INSIDE `createDispatchAgent` so the recovery redispatch rides it with no bypass hole; `missions/autonomy.policy.yml` reviewer flips to `mode: pooled` (poolKey `reviewer:{groupId}`, idleTtlMinutes 30, rotateEvery 8 — the drafter precedent mirrored); memory state = performance cache, restart-empty by design; unit chain +13 (`test/pool-lifecycle.test.mjs`, the WI36 ≥10 gate lower bound), zero engine diff, zero new dependencies). PROMPT ASSEMBLY DELIVERED (2026-08-27, age-autonomy M4-WI33, plan `docs/plans/age-autonomy/2026-08-27-0433-3`): the efficiency layer's second module `src/efficiency/prompt-assembler.ts` (the pure dual-mode PromptAssembler of 04-efficiency §3 — FRESH = fixedPrefix blocks ++ dynamic block, CONTINUE = per-agent hash-ledger delta with unchanged files skipped and changed files resent in full; embedStamp template `<file path="{path}" hash="{hash8}">{content}</file>` schema-overridable; directory full-text embed with per-file maxFileBytes caps and explicit over-cap notes; hash8 = first 8 hex of sha256, algorithm source shared with `computeBasisHash`; prefix discipline fixed→dynamic→volatile with byte-stable FRESH output; compaction counter re-sends the full charter every 8th CONTINUE dispatch) + the `law-policy.mjs` `assembly:` schema section (embedStamp template with the three render slots + continueDelta boolean; `DEFAULT_EMBED_STAMP` exported same-source; tools↔assets synced through the bundle channel) + consumption wiring: pool members carry the per-member hash ledger (create ⇒ FRESH / followup ⇒ CONTINUE; a crash replacement starts empty — the conservative P2 posture), `exec-arm.ts` `dispatchPromptFor` composes the thin-pointer dispatch prompt as the dynamic suffix at all four followup sites (undeclared deployments byte-identical — the backward-compat pin), the charter-hash rotation leg (04 §2.2 leg 2, the M4-WI32 deferred collection) forces a new pool member when a fixedPrefix file's hash changes, and `native-executor.ts` consumes `config.assemblyPrefix` (the nativeModelSelection config-face precedent — the run child's first step FRESH / later steps CONTINUE, the engine prompt already resolved through the promptsDir chain stays the verbatim dynamic suffix: the policy overlays, never replaces, 04 §7); unit chain +16 (`test/prompt-assembly.test.mjs`, the WI36 ≥12 gate lower bound), zero engine diff, zero new dependencies. CONTEXT PROFILE DELIVERED (2026-08-27, age-autonomy M4-WI34, plan `docs/plans/age-autonomy/2026-08-27-0558-1`): the efficiency layer's third module `src/efficiency/context-profile.ts` (the 04 §4 context profile — schema-v1 artifact `docs/references/context-profile.json`, project-owned and git-committed, NEVER under `missions/` where the mission scanner would pollute; first start seeds from the AGENTS.md Read This First list, direct path lines only; run-terminal mining aggregates the three data-source layers — pool-member `tool/call` session events × read-class file args + run-state prompt products + reflexion memory/runs.md — with debounce: an empty tally or an unchanged top-N SET never writes; headless = explicit degrade note with the seed table preserved, the complete independent-form face = WI35) + the `law-policy.mjs` fixedPrefix fourth kind `profile` with `topN` (default 5, per-agent DSL override; tools↔assets synced through the bundle channel) + the live policy's drafter grows the profile block (P8 exception = the plan active) + consumption wiring: `prompt-assembler.ts` expands kind:profile blocks into the top-N stable files (reads desc, path asc) riding every FILE semantic unchanged (stamp / cap / hash ledger / CONTINUE dedup; fail-soft notes on a missing or unknown-version artifact; undeclared agents byte-identical — the backward-compat pin) and the watchdog's run-terminal chain tail triggers one fail-soft mining pass (single-flight; mining failures never touch the terminal receipt or stop-dispatch; pool members retain their host session-events accessor — zero pool-semantic change); unit chain +13 (`test/context-profile.test.mjs`, the WI36 ≥8 gate lower bound), zero engine diff, zero new dependencies.

## Multi-Plugin Forward Reference (nop-* family)

The multi-plugin refactor (mission `multi-plugin-dsh`, design owner `docs/design/multi-plugin-dsh-architecture.md`) will repackage this single bundle as the `nop-*` plugin family. Inheritance: this doc remains the **as-built architecture doc of the `nop-age` plugin** after the migration — mapping `plugin/dsh/` → `plugin/nop-age/`, package `dsh-mission-control` → `nop-age`, isolate realm `missionControl` → `nopAge` (Concept Mapping table there); the cordis service name `mdcontrol`, the `mission-control-*` skill IDs, and the `/mdcontrol/api` HTTP prefix stay verbatim (token-map carve-out, Migration Surface there). The second bundle `nop-route` is designed in that doc §nop-route Plugin; a §nop-route architecture section lands with M4/M5 per its Concept Mapping row. Until the refactor lands, every `plugin/dsh` statement in this file is live truth (as-built faces are rewritten at M2 close, not before).

## Purpose

Define the technical implementation plan for packaging mission-driver as a DSH plugin with a native agent-dispatch execution backend, per the feature design in `docs/design/dsh-plugin-integration.md`.

This document owns: the packaging layout, the execution-backend seam refactor inside the engine, the native dispatch API chain, contract-preservation rules, and the phased delivery plan. It cites verified external API usage rather than restating third-party internals.

## Scope and Boundary Impact

One existing architecture rule was amended by this plan (amendment landed with P1, 2026-08-23):

- `docs/architecture/module-boundaries.md` previously stated the engine core is consumed "in-process: not exported (engine runs as a process, not a library)". Since P1, the engine is importable as a library behind the injected step-executor interface; CLI operation remains the default. That boundary doc was updated in the same change that landed P1.

All other boundary rules are preserved:

- Engine stays zero-npm-dependency at its core; no `@deepseek-ai/*` import ever enters `tools/mission-driver/src/`.
- Engine single-sourcing holds: the plugin installs from this repository into a DSH profile; nothing is copied into consumer projects.
- `install-age.sh` → engine dependency remains forbidden.

## Packaging Layout

new top-level directory in this repository — **landed 2026-08-23 (M2-WI6, extended M2-WI7 + M2-WI8 + M2-WI9 + M2-WI10 + M3-WI11 + M3-WI12 + M3-WI13 + M4-WI14)**, tree below is the as-built state (files marked with their owning work item):

```
plugin/dsh/
├── package.json          # LANDED: `dsh.bundle.patch` manifest + exact-pinned @deepseek-ai/* deps
│                         #   + WI9: 16 exact-pinned L3-composition devDeps + verify:native[:keyless] scripts
│                         #   + WI10: 17th devDep @deepseek-ai/dsh-app-boot (in-process e2e boot) + verify:e2e
├── cordis.patch.yml      # LANDED: `- insert:` op → cordis:group with isolate realm → service row
├── tsconfig.json         # tsc --noEmit over src/*.ts (allowJs: engine bundle JS enters as inferred types)
├── scripts/
│   ├── check-manifest.mjs     # structural manifest/patch validation (plain-YAML, dev-dep `yaml`)
│   ├── build-bundle.mjs       # copy-style engine bundling + import-closure gate (+ `--check` freshness)
│   ├── smoke-import.mjs       # no-host bundle import smoke (all 5 entry modules, zero npm resolution)
│   ├── host-harness.mjs       # LANDED (WI9): L3 harness — HarnessLineRpcTransport + session driver
│   │                          #   + 4-scenario runner (--dry / --keyless / --scenario); NOT bundle content
│   ├── verify-native.mjs      # LANDED (WI9): env-gated L3 local gate; never CI-blocking (R3 §5)
│   ├── e2e-policy.mjs         # LANDED (WI10): shared deterministic model policy (STEP-TOKEN pure
│   │                          #   function; artificial marker break + correction signature)
│   └── e2e-demo.mjs           # LANDED (WI10): L4 dual-leg e2e — CLI leg (real engine child + PATH-first
│                              #   `opencode` stub) + native leg (in-process boot() + route direct calls)
│                              #   + normalized diff + markers-parsed + correction-retry assertions;
│                              #   `verify:e2e` gate (explicit local, never CI-blocking);
│                              #   WI11: onboarding dual-form + descriptor/monitor assertions;
│                              #   WI12: skills-list + analyze (explicit/latest) + draft two-stage legs
│   └── e2e-preset.mjs         # LANDED (WI14): preset-composition leg — one in-process runtime carrying
│                              #   the service row + the host preset roster (roots → in-repo preset/);
│                              #   asserts roster health, interactive mount model-visible, mission-child
│                              #   composition via the executor setup, marker legality, mdcontrol
 │   │                              #   collision-free; `verify:e2e:preset` (explicit local, never CI-blocking)
 │   └── e2e-continuous.mjs  # LANDED (age-autonomy M3-WI28): continuous-mode three-leg e2e — real
 │                              #   in-process cordis host (fixture e2e-continuous.cordis.yml, supervisor
 │                              #   row on) over a scripted local model endpoint; legs: continuous OFF
 │                              #   observation (zero ledger dispatch) → ON queue advancement (ledger
 │                              #   dispatch registration) → fixture-driven R3 terminal receipt +
 │                              #   stop-dispatch; `verify:e2e:continuous` (DSH_E2E_CONTINUOUS=1
 │                              #   explicit local, missing env = fail-fast exit ≠ 0, never CI-blocking
 │                              #   — WI31 gate posture: a missing env must never read as a green pass)
├── test/
│   ├── bundle-scaffold.test.mjs        # plugin local test entry (WI6) — reused by WI7/WI8/WI10
│   ├── native-executor.test.mjs        # NativeExecutor unit branches (WI7; fake agents service)
│   ├── engine-bridge.test.mjs          # selection factory + native config + orchestrateRun smoke (WI7)
│   ├── backend-parity-matrix.test.mjs  # L2 dual-backend parity matrix, six assertion groups (WI8)
│   ├── host-harness-transport.test.mjs # LANDED (WI9): 12 pure-logic transport cases (fake streams)
│   ├── mdcontrol-routes.test.mjs       # LANDED (WI10): routes + async contract + guard + receipt +
│   │                                   #   HTTP dispatcher (17 cases); WI12: + draft/analyze (28 total)
│   ├── mdcontrol-skills.test.mjs # LANDED (WI12): registration shape + route wiring + dispose (6 cases)
│   ├── law-truth-table.test.mjs # law kernel truth table (age-autonomy 0815/WI21/WI22 batches; the
│   │                               #   WI22 `freeze:` sections pin the legacy-plan-freeze rule that
│   │                               #   collected the retired WI13 plan-status gate's protection face)
  │   ├── supervisor-core.test.mjs # LANDED (age-autonomy M3-WI25): supervisor seam suite (23 cases) —
  │   │                               #   decision-core matrix, machine-field writer (CAS/law-deny/
  │   │                               #   atomicity), receipt face (A8 + status passthrough), watchdog
  │   │                               #   (pacing/single-flight/debounce/recovery), service mount+dispose
  │   ├── supervisor-terminal.test.mjs # LANDED (age-autonomy M3-WI27): R1–R4 termination truth
  │   │                               #   table (22 cases) — four rules ±, order priority, R1
  │   │                               #   three-way, partial/blocked distinction matrix, compound
  │   │                               #   normalization, R4 N-1/N boundary, maxFailures dual-source,
  │   │                               #   idempotent re-evaluation, real-scan wiring
  │   ├── supervisor-failures.test.mjs # LANDED (age-autonomy M3-WI27): failure attribution buckets +
  │   │                               #   circuit breaker suite (9 cases) — three buckets each count
  │   │                               #   once at their wired points, not-counted negatives (CAS
  │   │                               #   conflict/observation/idempotent skip), held same-write
  │   │                               #   shape + claim clear, all-held R3 terminalization, single
  │   │                               #   held never blocks, reset edge (writer-identity T6)
  │   ├── supervisor-continuous.test.mjs # LANDED (age-autonomy M3-WI28): continuous-mode opt-in
  │   │                               #   suite (15 cases) — opt-in three states + route face
  │   │                               #   (unmounted bad-request/honest idle, root mismatch),
  │   │                               #   chain edge on/off, queue ≠ approval (held/draft never
  │   │                               #   dispatched), terminal stop-dispatch outranks the chain,
  │   │                               #   restart clears the flag, gate scope (meter/receipt
  │   │                               #   ungated), unlock/dispose writer shape + parameter-domain
  │   │                               #   denies + law-deny passthrough + receipt-target wiring
  │   ├── supervisor-recovery.test.mjs # LANDED (age-autonomy M3-WI29): crash-recovery scan suite
  │   │                               #   (9 cases; M3-WI30 grows the file to 18 — stagnation
  │   │                               #   fingerprint / ping-pong / dual-entry cross appended,
  │   │                               #   WI31 ≥8 gate exceeded) — expired-claim reclaim through the
  │   │                               #   recovery
  │   │                               #   cycle, resume (live session, no new line), redispatch
  │   │                               #   (dead session, old line preserved append-only, latest-
  │   │                               #   line dedup), failures non-count, two-cycle idempotency,
  │   │                               #   write face after redispatch (lease latest-line via
  │   │                               #   evaluateGates), deep-audit zero-increment redispatch
  │   │                               #   (exhausted budget no deadlock + budget-available variant
  │   │                               #   + concluded latest line re-opens), headless degradation;
  │   │                               #   M3-WI30 (plan 2026-08-26-1954-3) +9: fingerprint domain
  │   │                               #   pin + determinism, stagnation N-1/N e2e (R4 blocked +
  │   │                               #   receipts), activity participation (clear + re-accumulate),
  │   │                               #   ping-pong unit boundaries + e2e oscillation (hash-blind
  │   │                               #   leg), restart re-accumulation, dual-entry cross (declared
   │   │                               #   face ≡ cycle end), recovery-context partial/blocked
   │   │                               #   distinction, resolveStagnationRounds matrix + schema
   │   ├── pool-lifecycle.test.mjs # LANDED (age-autonomy M4-WI32): agent-pool truth table (13
   │   │                               #   cases, WI36 ≥10 gate lower bound) — drafter/reviewer
   │   │                               #   pool lifecycle (create/reuse/attemptId per dispatch),
   │   │                               #   group scope (same-group reuse, cross-group fresh,
   │   │                               #   groupScopeOf three legs), idle TTL dispose, rotateEvery
   │   │                               #   rotation + generation judgment (old stale/new current,
   │   │                               #   crash-detected member replaced), P7 audit ban (pool
   │   │                               #   layer + dispatch chain with a pooled-DECLARED auditor),
   │   │                               #   executor dormant note + executor tag, role mutex
   │   │                               #   (registry + dispatch-chain refusals), auditor ≠ executor
   │   │                               #   (candidate check + executorSessionsOf derivation),
   │   │                               #   recovery interop (same-generation resume e2e +
   │   │                               #   cross-generation redispatch e2e + dead member), pool
   │   │                               #   reuse × review lease (same-cycle double dispatch,
   │   │                               #   writer-identity per-plan latest-line lease), headless
   │   │                               #   degradation preserved
   │   ├── prompt-assembly.test.mjs # LANDED (age-autonomy M4-WI33): PromptAssembler truth table (16
   │   │                               #   cases, WI36 ≥12 gate lower bound) — FRESH byte order +
   │   │                               #   determinism, prefix discipline (volatile last, fixed
   │   │                               #   region byte-stable), CONTINUE dedup (unchanged files
   │   │                               #   skipped), changed-file full resend under the NEW hash,
   │   │                               #   dir full-text embed (sorted, no recursion), maxFileBytes
   │   │                               #   over-cap explicit note, hash-ledger semantics (sha256-8hex
   │   │                               #   same-source + commit protocol), text-verbatim vs
   │   │                               #   file-stamped + embedStamp template override, compaction
   │   │                               #   counter (8th CONTINUE full re-send), charter-hash
   │   │                               #   rotation judgment (change/add/remove/sentinel-immunity),
   │   │                               #   fail-soft missing notes, placeholder resolution, pool
   │   │                               #   dual-mode e2e (create FRESH / followup CONTINUE /
   │   │                               #   charter-change rotation), undeclared-deployment
   │   │                               #   byte-identical backward-compat pin, continueDelta:false
   │   │                               #   FRESH pin, native-executor assemblyPrefix dual-step
   │   ├── context-profile.test.mjs # LANDED (age-autonomy M4-WI34): context-profile truth table (13
   │   │                               #   cases, WI36 ≥8 gate lower bound) — Read This First seeding
   │   │                               #   (direct paths only, indirect lines skipped) + first-start
   │   │                               #   auto-seed, schema v1 round-trip + byte-determinism +
   │   │                               #   unknown-version conservative rebuild, tool/call session
   │   │                               #   tally, missions/ scanner zero-pollution reverse case,
   │   │                               #   mergeReads/topN ordering, debounce (empty tally / unchanged
   │   │                               #   top-N SET never write), three-source-layer mining merge,
   │   │                               #   headless degrade, watchdog run-terminal mining e2e +
   │   │                               #   fail-soft, kind:profile top-N expansion + role-override
   │   │                               #   backward-compat pin
   │   ├── fixtures/
│   │   ├── harness.cordis.yml          # LANDED (WI9): L3 composition fixture — 16 rows, non-PTY base
│   │   ├── preset.cordis.yml           # LANDED (WI14): preset-composition fixture — e2e base + the
│   │   │                               #   agent-presets roster row (roots → in-repo preset/, hermetic)
 │   │   └── e2e.cordis.yml              # LANDED (WI10): L4 in-process composition — L3 base minus the sdk
 │   │                                   #   server plus the REAL mission-control service row (relative
 │   │                                   #   specifier, non-isolated for root-realm route access);
 │   │                                   #   WI12: agent-spine skills enabled (hermetic DSH_HOME)
 │   │   └── e2e-continuous.cordis.yml   # LANDED (age-autonomy M3-WI28): continuous-mode e2e composition
 │   │                                   #   — e2e base PLUS the supervisor config row (projectRoot via
 │   │                                   #   DSH_E2E_CONTINUOUS_ROOT, heartbeatMs fast tick); consumed
 │   │                                   #   only by scripts/e2e-continuous.mjs (never CI)
│   ├── age-preset.test.mjs             # LANDED (WI14): structural gate for the AGE preset — shape,
│   │                                   #   preset.yml display-only, loader dialect (!!js inert), zero
│   │                                   #   service rows (D3), relative rows on disk, bare-name
│   │                                   #   allowlist, catalog sufficiency, no-marker posture
│   └── helpers/
│       ├── fake-agents.mjs             # reusable fake in-process agents service (WI7; matrix-extended WI8)
│       └── matrix-harness.mjs          # shared L2 matrix harness: both legs + comparators (WI8; the
│                                        #   normalizeRunState vocabulary is reused by the WI10 e2e diff)
├── preset/                # LANDED (WI14): the AGE agent preset (discoverable roster directory)
│   └── age/               #   agent.cordis.yml + preset.yml + age-mode.mjs — see §AGE Preset
├── src/
│   ├── service.ts        # LANDED (WI10): route wiring — cordis Service publication (`mdcontrol`) +
│   │                     #   optional /mdcontrol/api HTTP dispatcher + mount log; skeleton was WI6;
│   │                     #   WI12: skills registration wiring (reactive ctx.inject);
│   │                     #   law gate mount (registerLawGate, age-autonomy M2-WI12 0815-1) — the SOLO
│   │                     #   tools/pre-execute listener since WI22 retired the WI13 plan-status gate
│   ├── native-executor.ts# LANDED (WI7): DshNativeExecutor — full StepExecutor over the agents service;
│   │                     #   WI10: agentOptions {provider,model} + resolveAgentsService (ctx.get first)
│   ├── mdcontrol-routes.ts # LANDED (WI10): mdcontrol.run/status/list wire-method record + ActiveRunGuard
│   │                     #   + opt-in terminal receipt (agents.get → followup) + HTTP dispatcher;
│   │                     #   WI12: + mdcontrol.draft (async, shared guard slot) + mdcontrol.analyze (sync);
│   │                     #   age-autonomy M3-WI28: + mdcontrol.continuous (per-root opt-in toggle/query,
│   │                     #   watchdog posture gate + receipt-target registration) + mdcontrol.unlock
│   │                     #   (held-plan disposition — unlock/dispose through the supervisor writer) —
│   │                     #   seven routes total
│   ├── mdcontrol-skills.ts # LANDED (WI12): three mission-control runtime skills (instruction bodies →
│   │                     #   mdcontrol.* route calls over the HTTP dispatcher) + registerMissionControlSkills
  │   ├── law/              # age-autonomy M2-WI12+: the law-gate host adapter (host-adapter.ts — policy
  │   │                     #   discovery + kernel evaluate + observation log; type-only dsh-tools import
  │   │                     #   lives here since the WI22 retirement of plan-status-gate.ts, whose
  │   │                     #   protection semantics moved into the kernel rule `legacy-plan-freeze`)
  │   ├── supervisor/       # LANDED (age-autonomy M3-WI25, plan docs/plans/age-autonomy/
  │   │                     #   2026-08-26-1411-1): the supervisor seam — SECOND cordis service
  │   │                     #   publication in this same bundle (`mdsupervisor`, Service-subclass
  │   │                     #   precedent MdControlService; same isolate realm, zero new host face):
  │   │                     #   decision-core.ts (pure decide(snapshot, policy, clock) → decisions[],
  │   │                     #   predicate family reused through assets; with a policy triggers:
  │   │                     #   section decide() runs the M3-WI26 trigger duty, execute posture)
  │   │                     #   + watchdog.ts (heartbeat + fs-watch event edges, debounce,
  │   │                     #   single-flight, recovery scan, onTerminal seam; noteActivity
  │   │                     #   P2-1 claim-renewal signal face; trigger hits route to the exec
  │   │                     #   arm, policies without triggers keep the WI25 observe-only posture)
  │   │                     #   + writer.ts (SOLE machine-field writer, Q4 ③: claim/claim-expires/
  │   │                     #   failures/audit-rounds with baseHash CAS + tmp+rename + pre-write
  │   │                     #   evaluateGates self-check, actor role=supervisor; M3-WI26 adds
  │   │                     #   appendSectionLines — dispatch/pass-line registration into the
  │   │                     #   append-only ledger sections through the same law-self-check
  │   │                     #   pipeline, full-text CAS) + receipt.ts
  │   │                     #   (_tmp/supervisor-receipts.jsonl + best-effort delivery, A8) +
  │   │                     #   service.ts (mountSupervisor; config row supervisor.projectRoot absent
  │   │                     #   → idle posture, the absent-webServer degrade precedent). M3-WI26
  │   │                     #   (plan 2026-08-26-1411-2) adds the execution trio: trigger-eval.ts
  │   │                     #   (pure 14-predicate evaluator, per-plan × mission dual domain,
  │   │                     #   fail-soft, occurrenceKey material), dispatch-resolve.ts (dispatch
  │   │                     #   type → policy agents/dispatch → three-field model binding + plan
  │   │                     #   `agent:` override routing + requireDistinctModel runtime three-state
  │   │                     #   via the shared law-policy sameModelPair + ledger-rescan occurrence
  │   │                     #   idempotency), exec-arm.ts (the seven trigger exits: verify-runner
  │   │                     #   direct run + pass-line write + closure-audit chain, reviewer/
  │   │                     #   auditor/drafter dispatch registration + prompts, deep-audit budget
  │   │                     #   gate + DAR meter-increment same-write + terminal-claim consumption,
  │   │                     #   reclaim/re-issue, P2-1 bounded ledger claim renewal, terminal
  │   │                     #   declared face — since M3-WI27 executed through the SAME R1–R4 core,
  │   │                     #   never blind-forwarded). M3-WI27 (plan 2026-08-26-1411-3) adds
  │   │                     #   terminal-rules.ts (the R1–R4 sequential termination evaluation
  │   │                     #   core: pure evaluateTermination(snapshot, {maxAuditRounds,
  │   │                     #   maxFailures, stagnation?}) → {decision, rule, reasons} with
  │   │                     #   partial/blocked explicit distinction + compound-value
  │   │                     #   normalization; dual entry — the watchdog cycle evaluation AND the
  │   │                     #   policy terminal trigger exit — one implementation) + failures.ts
  │   │                     #   (failure attribution buckets executor-error/verification-red/
  │   │                     #   claim-expired-no-output + recordPlanFailure meter wiring + the
  │   │                     #   02 §4.6 circuit breaker applyCircuitBreaker: failures ≥ maxFailures
  │   │                     #   → held same-write claim-cleared via writer holdPlan; single held
  │   │                     #   never blocks others). The watchdog cycle post-execution face runs
  │   │                     #   breaker → fresh re-scan → R1–R4 evaluation: a terminal word lands
  │   │                     #   the run-terminal receipt + statusFace().terminal (mdcontrol.status
  │   │                     #   passthrough) + dispatch suppression for the mission run (sticky
   │   │                     #   per mount, re-derived idempotently across restarts). mdcontrol
   │   │                     #   status gains the supervisor read face (existing-route extension).
   │   │                     #   M3-WI28 (plan 2026-08-26-1954-1) adds the continuous-mode faces:
   │   │                     #   watchdog per-root in-memory continuous flag (constructor option =
   │   │                     #   the supervisor.continuous bundle-config pre-enable; setContinuous/
   │   │                     #   isContinuous/setReceiptTarget; restart clears — ActiveRunGuard
   │   │                     #   precedent) + applyContinuousGate in decision-core (OFF downgrades
   │   │                     #   dispatch-type execute-posture decisions to observation receipts —
   │   │                     #   behavior tightening; meter/receipt decisions ungated) + the
   │   │                     #   emitTerminalEvent chain edge (continuous ON → one immediate
   │   │                     #   re-evaluation cycle, single-flight guarded; terminal-word
   │   │                     #   stop-dispatch outranks the chain) + writer unlockPlan/disposePlan
   │   │                     #   (held→active same-write failures=0 + hold/claim cleared; held→
   │   │                     #   cancelled|superseded|deferred disposition — role=supervisor law
    │   │                     #   self-check, served by mdcontrol.unlock). M3-WI29
    │   │                     #   (plan 2026-08-26-1954-2) adds recovery.ts (the crash-recovery
    │   │                     #   scan executor: watchdog start() first cycle trigger='recovery'
    │   │                     #   runs it BEFORE the trigger evaluation; disposition table —
    │   │                     #   claim TTL face / un-concluded dispatch resume-or-redispatch /
    │   │                     #   awaitingClosure stays with the 1411-2 triggers; latest-line
    │   │                     #   semantics shared with the dedup + lease faces; deep-audit
    │   │                     #   same-occurrence redispatch reuses the paid round with NO
    │   │                     #   audit-rounds increment and the matching audit-rounds-overflow
     │   │                     #   gate exemption; per-mount handled set = zero duplicate
     │   │                     #   actions across recovery cycles; headless no-agents-face =
     │   │                     #   explicit degraded observation). M3-WI30
     │   │                     #   (plan 2026-08-26-1954-3) adds stagnation.ts (the WI30
     │   │                     #   stagnation-fingerprint + ping-pong detector feeding R4:
     │   │                     #   computeLedgerFingerprint = sorted (planPath → basisHash)
     │   │                     #   rows + roadmap full-text hash, the basisHash domain pin
     │   │                     #   (DRR/Verification/Closure appends outside); observeStagnation
     │   │                     #   pure transition — stagnant round = fingerprint unchanged ∧ zero
     │   │                     #   noteActivity in the window (default one heartbeat; activity
     │   │                     #   clears the count — long tasks never misjudged); ping-pong leg =
     │   │                     #   per-plan consecutive alternating flips ≥ K round trips without
     │   │                     #   terminal progress → saturated {rounds: threshold, threshold}
     │   │                     #   injection, same single R4 exit; in-memory scratch state, restart
     │   │                     #   clears; watchdog-held single point read by BOTH R4 entries —
     │   │                     #   cycle end + the declared face via ExecArmOptions.stagnationFact;
     │   │                     #   policy key limits.stagnationRounds via resolveStagnationRounds,
     │   │                     #   K = max(2, floor(N/5)) derived, threshold < 1 = detector off)
     │   ├── efficiency/       # LANDED (age-autonomy M4-WI32, plan docs/plans/age-autonomy/
      │   │                     #   2026-08-27-0433-2): the efficiency layer — agent-pool.ts (the
      │   │                     #   04 §2 role pools: drafter:{projectRoot} long-lived reuse +
      │   │                     #   reviewer:{groupId} same-batch reuse with groupScopeOf frontmatter
      │   │                     #   group → filename timestamp prefix fallback; create-on-first-use,
      │   │                     #   followup reuse, idle-TTL dispose via the injectable timers face,
      │   │                     #   rotateEvery count rotation; attemptId generation tokens with
      │   │                     #   attemptStale the recovery scan consumes — same generation resume
      │   │                     #   / cross generation redispatch + explicit revoke; P7 audit ban
      │   │                     #   (closure-audit/deep-audit/multi-audit structurally never pool,
      │   │                     #   any mode config); session-role mutex registry (drafter ∩
      │   │                     #   reviewer/auditor = ∅, same-run auditor ≠ executor — executor set
      │   │                     #   = plan claim holders derived from frontmatter ∪ pool executor
      │   │                     #   tags); executor pooling declared-but-dormant ruling; memory state
      │   │                     #   = performance cache, restart-empty by design) — M4-WI33 (plan
      │   │                     #   2026-08-27-0433-3) adds prompt-assembler.ts (the 04 §3 dual-mode
      │   │                     #   PromptAssembler: assemble FRESH/CONTINUE over fixedPrefix blocks
      │   │                     #   with the per-agent hash ledger — embedStamp template, dir
      │   │                     #   full-text embed with maxFileBytes caps + explicit over-cap
      │   │                     #   notes, hash8 = sha256-8hex (computeBasisHash algorithm source),
      │   │                     #   prefix discipline fixed→dynamic→volatile, compaction re-send
      │   │                     #   every 8th CONTINUE; pool members carry the ledger, the charter-
      │   │                     #   hash face feeds the 04 §2.2 rotation leg) — M4-WI34 (plan
      │   │                     #   2026-08-27-0558-1) adds context-profile.ts (the 04 §4 context
      │   │                     #   profile: schema-v1 artifact docs/references/context-profile.json
      │   │                     #   — project-owned, NEVER under missions/ (mission-scanner
      │   │                     #   pollution); AGENTS.md Read This First seeding; read-tally
      │   │                     #   mining over pool-member tool/call session events + run-state
      │   │                     #   prompt products + reflexion memory/runs.md; debounce = empty
      │   │                     #   tally / unchanged top-N SET never writes; headless = explicit
      │   │                     #   degrade note, seed preserved (WI35); prompt-assembler kind:
      │   │                     #   profile consumption expands the top-N stable files through the
      │   │                     #   FILE semantics unchanged; the watchdog's run-terminal chain
      │   │                     #   tail triggers one fail-soft mining pass). Plugin-src .ts
      │   │                     #   modules — NO ALLOWED_MODULES registration face (assets channel
      │   │                     #   governs engine→assets copies only; count stays 26)
     │   └── engine-bridge.ts  # LANDED (WI7): resolveExecutor factory + bootstrapNativeConfig + runNativeMission;
│                         #   WI10: beginNativeMission detached-start primitive (async job contract);
│                         #   WI12: beginNativeDraft (executor seam) + runNativeAnalyze (thin adapter)
└── assets/               # build output — COMMITTED (web/dist precedent); freshness gated
    ├── src/              # the engine allowed-module copy (26 files = 19 engine closure
    │                      #   + 3 shared ledger + 4 law kernel; closure 25/26,
    │                      #   verify-runner unreachable-allowed — mirrors ALLOWED_MODULES;
    │                      #   consumed since M3-WI26 by the plugin-side supervisor exec arm)
    │                      #   — relative imports preserved
    ├── flows/            # copied from tools/mission-driver/flows/
    ├── prompts/          # copied from tools/mission-driver/prompts/
    └── agents/           # copied from tools/mission-driver/agents/
```

### Build bundling (as landed)

`scripts/build-bundle.mjs` (pure Node, copy-style — rejected esbuild/rollup single-file bundling: the engine is zero-npm-dependency and small, copying keeps source auditable and path resolution identical to engine semantics; rejected symlinks: not portable in the DSH profile install form):

1. **Import-closure gate** (machine enforcement of the NOT-bundled rule): from the five entry modules (`orchestrator.js` / `config.js` / `engine.js` / `runner.js` / `step-executor.js`) it computes the transitive static-import closure and requires closure ⊆ allowed set. Importing `monitor.js` / `draft-job.mjs` / `spawner.mjs` / `main.js`, any npm package name, or anything escaping the engine `src/` root fails the build. Comment/prose strings cannot fake imports (the scanner is string- and comment-aware). Negative self-test verified all four failure classes go red.
2. **Copy**: the 26 allowed modules land in `assets/src/` with relative imports verbatim, so `import.meta.url`-relative `TOOL_ROOT` resolution (config.js pi persona `agentFile`, flow-loader `flows/`/`prompts/`) keeps exact engine semantics with `TOOL_ROOT = assets/`; `flows/`, `prompts/`, `agents/` are copied to `assets/`. Count source: `ALLOWED_MODULES` in `plugin/dsh/scripts/build-bundle.mjs` (its section comments carry the breakdown) — 19 engine modules + 3 shared ledger library (`ledger-frontmatter.mjs` / `ledger-sections.mjs` / `ledger-dualread.mjs`, age-autonomy M1-WI1/WI3/WI7, closure-reachable since the `2026-08-25-0635-3` consumer wiring) + 4 law kernel (`law-core.mjs` / `law-policy.mjs` / `law-rules.mjs` / `verify-runner.mjs`, age-autonomy M2 `2026-08-25-0815` batch; `verify-runner.mjs` is CONSUMED since M3/WI26 — the plugin-side supervisor execution arm imports this assets copy — while staying unreachable from the ENGINE entry closure by design: its engine consumer is the gate-check CLI, deliberately not bundled).
3. **Freshness**: artifacts are committed (clone-and-run like `web/dist`); `build-bundle.mjs --check` recomputes the copy plan and content-diffs it against the committed tree — stale or extra files fail. The `assets/` name deliberately does not fall under `.gitignore`'s `dist/` rule.

The verified import graph the allowed list encodes (packaging doc baseline, now machine-checked on every build; law-kernel dependency edges are guarded mechanically by `ALLOWED_MODULES` and the build-bundle closure gate — see `scripts/build-bundle.mjs`; no per-batch doc enumeration required):

- Flow engine path: `engine.js` → `expression.mjs`, `platform.mjs`, `sys-snapshot.mjs`, `reap-orphans.mjs` (→ `run-reconcile.mjs`), `active-run-registry.mjs`, `roadmap-check.mjs`
- Config path: `config.js` (imports only node builtins + `mission-check.mjs`) — needed by the programmatic entry (`engine-bridge`)
- Process backend path: `runner.js` (→ `executor.js`, `platform.mjs`) — needed by ProcessExecutor
- Orchestration entry path: `orchestrator.js` and its dependencies: flow loading (`flow-loader.js` → `plan-check.mjs` + `ledger-dualread.mjs`), CLI-parity bootstrap (`env-loader.js` `loadDotenv` before `resolveConfig` — the designed "env-loader → secret-resolver" chain is dormant today: `secret-resolver.js` has zero imports under `src/`, so the live chain is dotenv only), the draft pipeline (`cmdDraftMission`, `extractBriefGate`, `parseDraftArtifact`), Reflexion analysis (`postmortem.mjs` → config/expression, backing `mdcontrol.analyze`), and the hoisted `EXIT_MAP` (`exit-map.js`, pinned row-by-row by `test/exit-map.test.js`).
- Shared ledger library (age-autonomy M1-WI1/WI3/WI7; consumers wired by plan `2026-08-25-0635-3`): `plan-check.mjs` → `ledger-dualread.mjs` + `ledger-sections.mjs`; `roadmap-check.mjs` → `ledger-dualread.mjs` + `ledger-sections.mjs` + `ledger-frontmatter.mjs` (`validateRoadmapFrontmatter`, M2-WI42); `flow-loader.js` → `ledger-dualread.mjs` (direct, beside its `plan-check.mjs` import); library-internal chain `ledger-dualread.mjs` → `ledger-frontmatter.mjs` + `ledger-sections.mjs`, `ledger-sections.mjs` → `ledger-frontmatter.mjs`. The law kernel's `legacy-plan-freeze` rule (M2-WI22, the retired plan-status-gate's successor face) consumes `PLAN_STATUS_RE` from the bundled `ledger-dualread.mjs` copy.

NOT bundled: monitor server (`monitor.js`), draft-job detached-process management (`draft-job.mjs` + its `spawner.mjs` seam — both remain CLI/monitor-only), CLI commander wiring (`main.js`).

### Version pins (as landed)

`plugin/dsh/package.json` pins, exact (no ranges), per the P2-start survey re-run (2026-08-23, `docs/analysis/2026-08-23-0001-p2-version-survey.md` — dist-tags identical to R2, no cohort drift): `@deepseek-ai/cordis@4.0.1` + `dsh-agent`/`dsh-goal`/`dsh-tools`/`dsh-subagent` all at `0.1.1-rc.2`. Since M3-WI13 the `dsh-tools` pin is CONSUMED — type-only import (`PreToolDecision`/`ToolExecution` + the package's `declare module '@deepseek-ai/cordis'` Events augmentation typing the `tools/pre-execute` subscription; erased at runtime, so the runtime dependency graph is unchanged — plan `2026-08-23-1852-3` D4); since the age-autonomy M2-WI22 retirement of `plan-status-gate.ts` the importing module is `src/law/host-adapter.ts` (successor listener, same type-only posture). `dsh-goal` remains pinned-but-unconsumed (reserved single-cohort consistency; no current consumer). Bumping any pin is an explicit changelog event.

L3 harness composition devDeps (added 2026-08-23, M2-WI9 — same survey doc, Addendum section): sixteen packages at exact `0.1.1-rc.2` (bin `dsh-sdk-jsonrpc-demo`, server `dsh-sdk-jsonrpc-server`, and the 14 spine/backend rows of `test/fixtures/harness.cordis.yml`) — devDependencies only, so the shipped bundle dependency surface is unchanged. The `latest` dist-tag of these packages lags at `0.0.1-rc.x`; pins are therefore literal versions, never tags. The L4 e2e boot added a 17th exact devDep, `@deepseek-ai/dsh-app-boot@0.1.1-rc.2` (M2-WI10 — same cohort; `boot()` for the in-process e2e composition), same devDependencies-only posture. M4-WI14 added nine more exact `0.1.1-rc.2` composition devDeps (the roster `@deepseek-ai/dsh-agent-presets` plus the preset's bare-row packages: `dsh-persona`, `dsh-agent-instructions`, `dsh-tool-bash`, `dsh-tool-pwsh`, `dsh-tool-fs-search`, `dsh-skill-filesystem`, `dsh-tool-skill`, `dsh-compaction-tool-result-pruner`) so the AGE preset's rows resolve inside the in-process composition leg — same cohort discipline, devDependencies only. The L3/L4 verification surface itself (composition, transport, gate posture) is owned by R3 (`docs/analysis/2026-08-22-0003-verification-harness-design.md` §4-§6).

## Execution Backend Seam (Engine Refactor)

The engine historically routed AI execution through injected delegates (`engine.js`: `delegates.runAgent` for agent steps, `delegates.runParseAgent` for no-marker parse fallback and marker-correction retries, `delegates.runTool` for tool steps; wired from `main.js`). The refactor formalized this existing seam into one named interface so backends become pluggable. **Landed form (M1-WI1): the three-capability method set** — a single `delegates.executor` object exposing `executeAgent` / `executeParseAgent` / `executeTool` (1:1 with the legacy delegate trio; `src/step-executor.js` `ProcessExecutor` is the process backend). The single-method sketch below is the conceptual shape, not the landed signature:

```
StepExecutor.execute(stepCtx) → { code: 0|1, text: string, errorTail: string, sessionId?: string|null }
```

- **ProcessExecutor** wraps the existing runner+executor pair unchanged; selected for every current driver value (`opencode` | `pi` | `cline`). Behavior byte-for-byte identical to today.
- **NativeExecutor** (implemented in the plugin layer — `plugin/dsh/src/native-executor.ts`, landed M2-WI7 — not in the engine core) fulfills the same interface over the DSH agents service.
- Backend selection is a plugin-layer factory (landed M2-WI7, `plugin/dsh/src/engine-bridge.ts` `resolveExecutor({ driver, ctx, config })`): `"native"` → a PER-RUN `NativeExecutor` (handle lifetime = one run); all other values → `ProcessExecutor` over the bundle-internal runner. The engine core stays backend-blind — zero engine diff, zero `@deepseek-ai/*` import under `tools/mission-driver/src/`. A missing agents service is an explicit wire error; there is no silent ProcessExecutor fallback (the degradation ladder is a separate explicit decision, see §Dependency and Version Risk).
- **Draft-side executor seam (M3-WI12, plan `2026-08-23-1852-2` — that plan's ONLY engine diff)**: `cmdDraftMission(desc, opts)` accepts `opts.executor` (the same StepExecutor interface; a module-local `runnerFromExecutor` adapter maps it onto the `{runAgent, close}` runner shape the pipeline consumes, with `close()` → `dispose?.()`). Without an executor the `__runnerFactory` path is byte-identical (CLI unchanged; `executor` is not a resolveConfig key). The plugin's `mdcontrol.draft` uses this seam for native two-stage drafting; the analyze side needed NO seam (the plugin calls `runPostmortem` directly with its own thin runner adapter — see §Service Surface).

Two P1 hardening items the refactor included (both landed 2026-08-23, M1-WI3/WI4):

1. **Driver validation.** Resolve-time validation with supported values `opencode | pi | cline | native` (`SUPPORTED_DRIVERS` in `config.js`, enforced at every `resolveConfig` return point — an unknown value previously reached spawn time and failed mid-mission as a SPAWN ENOENT); `native` is legal only inside the plugin host (internal `allowNativeDriver: true` option) and is rejected with a clear error by the standalone CLI.
2. **Embedded-mode gating of startup diagnostics.** `FlowEngine.run()` used to perform process-level startup work unconditionally for non-subflow engines: active-run registration under `~/.mission-driver/active/`, system snapshots via execSync, and orphan reaping that kills OS processes whose command line matches `opencode run` + `[MISSION_DRIVER]` tags. These are now gated behind an embed flag (`cfg.embed === true`, off when NativeExecutor is selected); active-run registration moves to the plugin's own guard (see §Service Surface).

Contract preservation rules (each machine-pinned by the L2 backend-parity matrix since M2-WI8 — `plugin/dsh/test/backend-parity-matrix.test.mjs`, merge-blocking via `verify-age.sh` + `.github/workflows/age-ci.yml`; divergence ledger in the spec header, every entry owner-doc-backed):

1. **Marker contracts unchanged.** `<AI_STEP_RESULT>pass|fail</AI_STEP_RESULT>`, `<BRIEF_GATE>…</BRIEF_GATE>`, and all step-transition markers keep their definitions (`docs/architecture/mission-driver-baseline.md` §Marker Contracts). NativeExecutor returns the child agent's final assistant message text as `text`; the engine's existing extraction, correction-retry (max 2 re-prompts), and transient-fault backoff operate on it unmodified. *(Matrix: groups 1–2 — classification, correction budget, transient classification identical across both backends.)*
2. **Run-state shape unchanged.** `_writeWorkflow` atomic writes, `steps[]` fields including `sessionId`, remain the durable surface shared with the monitor. *(Matrix: groups 3+6 — normalized run-state shape (field sets/types/status sequences; sessionId presence/type per R3 §3, timing presence/type only) and artifact file-set existence identical; the monitor's consumption surface IS the run-state file, so shape identity is monitor identity.)*
3. **Exit semantics synthesized.** Where ProcessExecutor yields real exit codes and stderr tails, NativeExecutor synthesizes compatible values (`code: 0` on completed turn with parseable outcome; `code: 1` + `errorTail` on abort/error) so the hoisted `EXIT_MAP` terminal-status mapping and retry budgets behave identically. *(Matrix: group 4 — synthesized exit → engine terminal status → `EXIT_MAP` lookup identical on both legs, 8/10 keys end-to-end; group 5 — `maxTotalSteps`/`maxCycleVisits`/`maxRetries` budgets fire identically.)*

## Native Dispatch API Chain

The implementation follows the verified in-process pattern used by DSH-better-sidebar's sidechat feature (`src/sidechat-routes.ts`; upstream: <https://github.com/omdsh-dev/DSH-better-sidebar> — an offline clone is kept at `~/ai/dsh-plugins/DSH-better-sidebar` for local reference only):

| Step | Host API | Notes |
| --- | --- | --- |
| Resolve service | `ctx.get('agents')` | degrade to wire error if absent. (M2-WI10 e2e finding: on a real cordis context a service property read without a declared `inject` THROWS — the plugin resolves the agents face via `ctx.get(name)`, `resolveAgentsService` in `native-executor.ts`.) |
| Create child agent | `agents.create(options)` → `{ agent, dispose() }` | `options.sessionId` = generated child id; `options.meta` carries `{ cwd: projectRoot, origin: 'subagent', delegationDepth, agentPreset, seedLength }`; `options.seed` = one durable `subagent/descriptor` event (WI11: `snapshotSubagentDescriptor` + `seedDescriptorTurn` from `@deepseek-ai/dsh-subagent`, staged like the sidebar precedent — cold children without a descriptor render as 'corrupt' diagnostics in host enumeration); `options.signal` = timeout; `options.agentOptions = { provider, model }` (M2-WI10 e2e finding: a real-host turn without them fails "has no provider/model" — sdk-jsonrpc-server `createSession` + dsh-headless precedents; provider defaults `deepseek-official`, model prefers engine `config.model` then `DSH_MODEL`) |
| Dispatch prompt | `agent.followup(createUserMessage({ content: [{ type: 'text', text }], source: { kind: 'user' } }))` | text = assembled prompt template output, prefixed by a `[MISSION_DRIVER:<runId>]` boundary line; in native mode the prefix serves log/run-dir identifiability only (the orphan reaper identifies OS processes by command line, which native child agents do not have) |
| Await completion | `await agent.whenIdle()` — whole-agent quiescence promise (host SDK `runtime-types.ts`) | replaces log-mtime heartbeat; `agent.status` (`idle`/`running`) kept for diagnostics only |
| Read result | final assistant message from `agent.session.events` | fed to marker extraction as `text` |
| Continue session | `agents.resume({ resumeSessionId })` when the handle went cold between steps | native continuity; supersedes regex session scraping |
| Release | `handle.dispose()` | deterministic cleanup after each run or abort. ⚠️ `dispose()` stops the loop AND removes the session from the store — the handle must live for the whole mission run; between steps reuse the live handle, and only `resume()` can recover a persisted session after disposal |

Each run child registers ONE durable descriptor at create (WI11, landed): mode `continuable`, provider `mdcontrol`, label `Mission: <mission>` (runId fallback), `agentProvider`/`agentModel` mirroring the create `agentOptions`. As-built shape: the handle is one child per RUN (reused across steps, R1-A2) — so the descriptor row is run-level, and step-level progress surfaces through run-state/monitor (channel 1), not through per-step descriptor rows. Enumeration health only: the host list plane is parent-scoped (`listChildren(parentSessionId)`), and an mdcontrol child carries no DSH parent session, so the row's guaranteed reach is the persisted child session log (`subagent/descriptor` event survives compaction) — exactly the e2e assertion plane; `agents.resume` needs no re-seeding (the seed is durable session state).

Terminology glosses for reviewers unfamiliar with the DSH host: **isolate realm** — a cordis service scope that keeps a plugin's mounted instances private instead of process-global; **wire error** — a structured RPC error surfaced to the client UI; **`dsh.bundle.patch`** — the package.json manifest field declaring this package as a mountable DSH bundle plugin.

### Implementation state and boundaries (M2-WI7, landed 2026-08-23)

The dispatch chain above is implemented in `plugin/dsh/src/native-executor.ts` (`DshNativeExecutor`) and wired by `plugin/dsh/src/engine-bridge.ts` (`resolveExecutor` + `bootstrapNativeConfig` + `runNativeMission`). Verified in the fake-agents unit domain only (no real host — L3 belongs to WI9; native end-to-end belongs to WI10). Implementation boundaries, all deliberate:

- **Model selection: the parse distinction is ignored, a single model now flows**: `mission.model` passes through to `agentOptions.model` on create since M2-WI10 (one model for every dispatch — agent, parse, and correction turns alike); `parseModel` (the cheap-parse distinction) remains ignored in native mode — `executeParseAgent` and `executeAgent` collapse to the same dispatch on the same child session. See §Behavioral differences below.
- **Log artifacts are written but content shape is not byte-equivalent**: `logFile`/`promptFile` land in the engine run-dir with the same naming convention (`native-<step>-<ts>-<rand>.log` + `.prompt`); the log body is the harvested assistant text plus a header/round summary, NOT a subprocess stdout transcript (file existence/readability is the compatibility contract; byte-level content shape is not). **M2-WI10 monitor-check correction, CLOSED by M3-WI11**: run-state-based rendering was never affected; the monitor's step-log endpoints (`listStepLogs` / `handleGetLog` / `handleGetNodeDetail`) matched the `oc-` prefix convention only — since WI11 they accept the shared dual-label shape (`oc-` / `native-`), machine-pinned by engine monitor unit cases + the e2e `assertMonitorRender` pass over all four runs (fix details and the third-site (:929) enumeration gap in `docs/bugs/2026-08-23-monitor-native-log-naming.md`, closed).
- **`executeTool` is the plugin layer's own minimal spawn**: `child_process` spawn + timeout + exit code + output tail, ZERO diagnostics — no `sysSnapshot`, no `~/.mission-driver/active/` touch. Reusing the engine `executor.js` tool path was rejected because its heartbeat pair is intentionally not embed-gated ("a native-mode embed host never selects this backend") — sharing it would run execSync snapshots and active-run registry touches inside the DSH host, the exact host-invasive behavior M1-WI4's embed gating prevents. Known residual drift (the process-path `runTool` currently drops the engine's `timeout` opt; the native path consumes it as milliseconds) is pinned by the WI8 L2 matrix's tool-step assertions (landed: scenario `tool-timeout-drift` in `plugin/dsh/test/backend-parity-matrix.test.mjs`, divergence ledger D1).
- **No silent fallback**: missing `ctx.agents` (or a failing native create) surfaces as an explicit wire error to the caller — never an implicit ProcessExecutor downgrade.
- **Callback contract mirrors runner.js exactly**: `onStepUpdate` is resolved at call time (`opts.onStepUpdate ?? config.onStepUpdate`, both with `typeof === "function"` guards) because `orchestrateRun` assigns `config.onStepUpdate` only after executor construction; two-point callbacks fire files-first (`{stepName, logFile, promptFile}`) then session (`{stepName, sessionId}`), so subflow wrapping and the monitor live channel behave identically in native mode.
- **`stderrTail` is always null natively** (no subprocess stderr surface; `errorTail` carries the error text); exit codes are synthesized per contract-preservation rule 3.

### Behavioral differences vs ProcessExecutor (documented, accepted)

| Concern | Process behavior | Native behavior |
| --- | --- | --- |
| Permissions | `--dangerously-skip-permissions` bypass | child inherits host sandbox/approval stack; stricter by default. AGE worker preset must carry a tool catalog sufficient for execute/closure steps |
| Model selection | `mission.model` opencode-style ids passed through; `parseModel` routes no-marker parse fallback + marker-correction retries to the cheaper model | since M2-WI10 `mission.model` flows to `agentOptions.model` (single model, one child session); `parseModel` remains ignored (documented gap, not silently pretended). **M3-WI26 fills the selection side of the gap**: config `nativeModelSelection` `{ provider, model, reasoningEffort? }` (the supervisor dispatch-resolution face over policy `agents:`/`dispatch:`, 02-rule-law §4.9) composes the create through `agentOptions {provider, model}` PLUS the dsh-agent `installModelSelection` face (reasoningEffort included — agentProvider/agentModel/reasoningEffort three-field channel; the durable descriptor seed still mirrors provider/model only — the host descriptor face carries no effort field) |
| Watchdog | 60-min log-idle SIGTERM | hard per-step timeout: `agent.cancel(cause)` first, `dispose()` as last resort; no partial-output grace |
| Native loop-driver precedent | — (n/a) | host's own `goal-round-driver` drives bounded same-session rounds via a queued prompt gated by an `agent/pre-step` listener; Flow DSL still owns sequencing because it adds branching transitions, script checks, marker contracts, and per-branch budgets beyond round counting |
| Crash isolation | child crash contained by OS | runaway turn bounded by abort + dispose + model-side budget; correction-retry still applies |

Official in-tree precedent for the whole dispatch shape: `@deepseek-ai/dsh-headless` creates one persisted Agent via `ctx.agents`, submits the task, waits for quiescence, and harvests the last non-empty assistant text (`packages/bundle/headless/README.md`) — validating NativeExecutor (in-process) and a one-shot CLI driver against host-own usage. A corresponding `dsh` ProcessExecutor driver value is a post-M2 candidate and is intentionally absent from the WI3 whitelist until then.

## Service Surface (Mission Control)

The cordis service mounts once in an isolate realm (preset-style mounting precedent: `dsh-anchored-standard/preset/agent.cordis.yml`) and registers:

- Routes (namespace `mdcontrol.*`) — **as-built (M2-WI10 + M3-WI12 + M3-WI28)**: `run`, `status`, `list`, `draft`, `analyze`, `continuous`, `unlock` (seven routes — the M3-WI28 pair landed 2026-08-26, plan `docs/plans/age-autonomy/2026-08-26-1954-1`: `mdcontrol.continuous` = per-root continuous-mode opt-in toggle/query over the watchdog posture gate; `mdcontrol.unlock` = held-plan human disposition through the supervisor writer) are live in `plugin/dsh/src/mdcontrol-routes.ts`. Thin wrappers over engine orchestration, never reimplementing engine logic. Exposure surface (better-sidebar precedent, host-source verified): the wire-method FULL-NAME record is the primary surface — published as the cordis service `mdcontrol` (`ctx.get('mdcontrol').routes`, Service-subclass form like the host's own AgentRegistry) — plus the plugin's own HTTP dispatcher at `POST /mdcontrol/api/<method>` (`{ok:true,value}` / `{ok:false,error:{code,message}}` envelope, wire codes `bad-request|not-found|run-in-progress|internal`) registered through `ctx.get('webServer')` when the host provides one; headless compositions degrade to a mount-log line, never a mount failure.
- **Async job contract** — **as-built (M2-WI10; extended M3-WI12)**: `mdcontrol.run` NEVER blocks on mission completion — a mission takes tens of minutes to hours, and a synchronous wait would hang the calling agent's turn until timeout. The route validates config (bootstrap + executor selection before the task exists — failures are fail-fast wire errors with the guard never occupied), starts the engine loop as a detached in-host task (`engine-bridge.ts` `beginNativeMission`: `orchestrateRun` simply not awaited — zero engine diff; the hanging promise never rejects; the single run-terminal executor dispose lives in its finally arm), and returns immediately with `{ runId, status: 'started', runDir, startedAt }`. runId = basename of the engine runDir — the same value the engine stamps into `run-state.json.runId`, so route, run-state, and monitor share one vocabulary. Progress flows through `mdcontrol.status` (thin `run-state.json` passthrough — no second state machine) and the monitor dashboard; `mdcontrol.list` enumerates runs (disk scan of `_tmp/*/run-state.json`, monitor listRuns precedent, merged with this instance's live records). On terminal state the engine optionally posts a one-line summary back to the requesting agent (`followup: { sessionId }` opt-in; `agents.get` → `agent.followup` — see §Dependency and Version Risk for the six-call ledger). Session lifetime is decoupled: closing the chat session that started a run does not stop it (a dead receipt target is a warn-only skip). Precedent in-tree: `draft-job.mjs` already uses exactly this pattern (detached job + state file + monitor polling). **`mdcontrol.draft` (M3-WI12, plan `2026-08-23-1852-2`)** takes the same contract through the pre-authorized `cmdDraftMission` executor seam (the ONLY engine diff of that plan — `orchestrator.js` accepts `opts.executor`, CLI path byte-identical): fail-fast desc validation with the engine's own `validateDraftDesc` before the guard, jobDir + initial `draft-state.json` in the engine `startDraftJob` vocabulary (jobId `draft-<ts>-mission-draft`, `status:"running"`, `phase: brief|draft` — the monitor/CLI consumption face is reused, never a second state machine), then the two-stage brief→draft pipeline as a detached in-host task over a per-job NativeExecutor; progress = `draft-state.json` (phase `brief → brief_done → draft → completed`, terminal `completed|failed|blocked`); opt-in terminal receipt reports the draft-state status + mission name. **`mdcontrol.analyze` is SYNCHRONOUS by adjudication**: `runPostmortem` is exactly ONE agent dispatch (no engine loop), so the result carries the postmortem outcome verbatim (`{ targetRunId, targetRunDir, jobDir, postmortemFile, memoryUpdated, text }`); target resolution reuses the list disk scan (explicit runId / most-recent by mtime / `not-found`); the dispatch seam is a plugin-owned thin runner adapter over a per-call NativeExecutor (engine `orchestrateAnalyze`'s 4-line runner plumbing replaced — the postmortem pipeline itself stays 100% in `postmortem.mjs`; zero engine diff; adapter owns the single executor dispose in a finally arm).
- Skills — **as-built (M3-WI12)**: `mission-control-run` / `mission-control-draft` / `mission-control-analyze` are registered as runtime rows on `ctx.skills` (pure markdown instruction bodies — the host `SkillRegistration` has no executable handler) by `plugin/dsh/src/mdcontrol-skills.ts`, mounted through a reactive `ctx.inject(['skills'], …)` fiber (order-independent, auto re-register on service change, headless compositions without skills degrade to a mount-log line — the absent-webServer posture). Chain: `tool-skill` renders the pre-step catalog → the model loads a skill via the `skill` tool → the instruction body directs the call to the corresponding `mdcontrol.*` route over the plugin's HTTP dispatcher using an HTTP-capable session tool (`web_fetch` / bash+curl — conditional tool-face cooperation item pinned in the skill content). True-model natural-language invocation is an env/manual leg outside the deterministic gate (e2e asserts the registration plane: `ctx.skills.list()` carries all three).
- **Supervisor service — LANDED as a seam (age-autonomy M3-WI25, plan `docs/plans/age-autonomy/2026-08-26-1411-1`) + EXECUTION WIRED (age-autonomy M3-WI26, plan `docs/plans/age-autonomy/2026-08-26-1411-2`) + TERMINAL RULES + FAILURES (age-autonomy M3-WI27, plan `docs/plans/age-autonomy/2026-08-26-1411-3`) + CONTINUOUS MODE (age-autonomy M3-WI28, plan `docs/plans/age-autonomy/2026-08-26-1954-1`) + CRASH RECOVERY (age-autonomy M3-WI29, plan `docs/plans/age-autonomy/2026-08-26-1954-2`) + STAGNATION/PING-PONG DETECTION (age-autonomy M3-WI30, plan `docs/plans/age-autonomy/2026-08-26-1954-3`) + AGENT POOL (age-autonomy M4-WI32, plan `docs/plans/age-autonomy/2026-08-27-0433-2`) + PROMPT ASSEMBLY (age-autonomy M4-WI33, plan `docs/plans/age-autonomy/2026-08-27-0433-3`) + CONTEXT PROFILE (age-autonomy M4-WI34, plan `docs/plans/age-autonomy/2026-08-27-0558-1`)**: the SECOND cordis service publication inside this same bundle (`mdsupervisor`, `plugin/dsh/src/supervisor/service.ts` — Service-subclass precedent MdControlService; same isolate realm, no new host entry, no new HTTP route). Five-duty seam: pure `decide(snapshot, policy, clock)` decision core (predicate family reused through the assets channel; with a policy `triggers:` section decide() runs the M3-WI26 trigger duty — execute-posture hits), watchdog loop (heartbeat 30s default + fs-watch event edge debounced + single-flight + start() recovery scan — restart seam, full semantics M3-WI29; `noteActivity` claim-renewal signal face), the SOLE machine-field writer channel (Q4 ③ adjudication: claim/claim-expires/failures/audit-rounds with baseHash CAS + tmp+rename + pre-write `evaluateGates` self-check, actor role=supervisor — claim-validity/writer-identity enforce with ZERO rule changes; M3-WI26 adds `appendSectionLines` dispatch/pass-line registration into the append-only sections through the same pipeline; M3-WI27 adds `holdPlan` — the circuit-breaker held write, status+hold+failures+claim-clear one atomic write), and the receipt minimal face (`_tmp/supervisor-receipts.jsonl` + best-effort delivery, A8 dead-session tolerance) surfaced through the `mdcontrol.status` supervisor read hook (existing-route extension). M3-WI26 adds the execution trio under `src/supervisor/`: `trigger-eval.ts` (pure 14-predicate trigger evaluator, per-plan × mission dual domain, fail-soft, occurrenceKey material), `dispatch-resolve.ts` (dispatch type → policy agents/dispatch resolution + plan `agent:` override routing + the three-field model binding agentProvider/agentModel/reasoningEffort + requireDistinctModel runtime three-state through the shared `law-policy.mjs` `sameModelPair` + ledger-rescan occurrence idempotency), and `exec-arm.ts` (the seven trigger exits: verify-runner direct run + `## Verification` pass-line write + closure-audit chain, reviewer/auditor/drafter dispatch registration with pre-registered ids and engine-prompt-referencing dispatch prompts, deep-audit budget gate + DAR meter-increment same-write + terminal-claim consumption, reclaim clear/re-issue, P2-1 bounded ledger claim renewal; the terminal exit — since M3-WI27 — executes through the SAME R1–R4 core). M3-WI27 adds `terminal-rules.ts` (the pure R1–R4 sequential termination evaluator `evaluateTermination` — order priority R1→R4, R1 three-way with in-flight-claim continue, partial/blocked explicit distinction, compound declared-value normalization, stagnation injection seam for WI30) and `failures.ts` (the 02 §4.6 failure attribution buckets + `recordPlanFailure` meter wiring at the three exec-arm failure points + `applyCircuitBreaker`); the watchdog cycle post-execution face runs breaker → fresh re-scan → R1–R4 evaluation, and a terminal word lands the run-terminal receipt + `statusFace().terminal` + dispatch suppression (sticky per mount, restart-idempotent — no new store). Posture: policies WITHOUT a `triggers:` section keep the WI25 observe-only behavior byte-compatible; a bundle config row without `supervisor.projectRoot` mounts idle (degrade note, never a mount failure); the DSH agents service composed on the host is the dispatch face (absent ⇒ ledger registration + receipts only). M3-WI28 adds the continuous-mode opt-in faces: a per-root in-memory `continuous` flag on the watchdog (default OFF — behavior TIGHTENING over the 1411-2 triggers-present-is-execute posture: dispatch-type execute-posture decisions are downgraded to observation receipts by `decision-core.ts` `applyContinuousGate` until explicitly enabled; meter-write/receipt decisions ungated; restart clears the flag — ActiveRunGuard precedent; the `supervisor.continuous` bundle-config key pre-enables for headless = equivalent explicit declaration; mount log carries the BEHAVIOR TIGHTENING line), the 03 §3 edge-2 chain wiring (`emitTerminalEvent` tail: continuous ON → one immediate re-evaluation cycle, single-flight guarded; the mission terminal word's stop-dispatch outranks the chain), the writer `unlockPlan`/`disposePlan` human-disposition writes (held→active same-write failures=0 + hold/claim removal, held→cancelled|superseded|deferred — role=supervisor law self-check, served by `mdcontrol.unlock`; the 1411-1 writer-identity T6 shape, zero rule changes), and the run-terminal receipt-target wiring (the continuous-enabling session registered via `mdcontrol.continuous` `followup {sessionId}` → `watchdog.setReceiptTarget`, A8 best-effort delivery unchanged, dead-session tolerance). M3-WI29 adds the crash-recovery face: `src/supervisor/recovery.ts` — the restart-duty action executor the watchdog's first 'recovery'-labeled cycle runs before the trigger evaluation (fail-soft; suppressed once the mission terminal word landed; NOT continuous-gated — crash completion of already-dispatched work, not new unattended progression): un-concluded dispatch occurrences are judged by agents-face session liveness → resume (original-session followup injection, no new line) ∨ redispatch (a NEW dispatch line with a fresh nonce8 id through the same law-self-check writer + createDispatchAgent chain; the old line stays append-only; deep-audit reuses the PAID round with no audit-rounds increment — the `audit-rounds-overflow` gate's same-round redispatch exemption prevents the exhausted-budget never-redispatch deadlock, 01 §3.1); a per-mount handled set keeps repeated recovery cycles at zero duplicate actions; no agents face ⇒ explicit degraded observation (headless posture); recovery redispatch never counts `failures` (02 §4.6). The same latest-line semantics lands in two消费 faces: `dispatch-resolve.ts` `dispatchAlreadyRegistered` (deep-audit: a paired latest line re-opens the occurrence — stale unpaired lines do not occupy) and `law-rules.mjs` writer-identity review lease (holder = the LAST unpaired dispatch review line's session; superseded lines hold no lease; a paired last line closes the lease — redispatch cannot lock the plan write face forever). M3-WI30 adds the §7 dead-lock detection faces: `src/supervisor/stagnation.ts` — the pure stagnation-fingerprint + ping-pong detector feeding R4 (`computeLedgerFingerprint` = sorted (planPath → basisHash) rows + roadmap full-text hash — the completion-formula same source, with the basisHash-domain pin: DRR/Verification/Closure appends stay outside, dispatch/pass lines never reset the fingerprint; `observeStagnation(state, input)` pure transition — a stagnant round = fingerprint unchanged ∧ zero noteActivity hits in the window (default one heartbeat period; activity clears the count, the long-task-not-yet-landed positive case), and the ping-pong leg = per-plan consecutive alternating flips between two states ≥ K round trips without terminal progress → the saturated `{rounds: threshold, threshold}` injection into the SAME single R4 exit — a third state restarts the pair, a terminal disposition resets, departed plans drop; threshold < 1 = detector off); the detector state is in-memory scratch held by the watchdog (03 §6 restart-clears, conservative re-accumulation) and its output is the watchdog-held SINGLE POINT both R4 entries read — the cycle-end evaluation and the declared face (exec-arm `forwardTerminalDecision` through `ExecArmOptions.stagnationFact`, ≤ one cycle skew converged by idempotent re-evaluation, the 1411-3 cross-case contract); N lives in policy `limits.stagnationRounds` (`law-policy.mjs` `resolveStagnationRounds` dual-source — policy authoritative / mission-flow + `flows/mission-driver.json` top-level fallback / default 10 ≈ 5 wall-clock minutes at the 30s heartbeat; K = max(2, floor(N/5)) derived from the same key); a first trip lands the `stagnation-detected` observation receipt (fingerprint/ping-pong wording), the terminal word rides the 1411-3 setTerminal receipt chain unchanged. M4-WI32 adds the efficiency-layer pool face: `src/efficiency/agent-pool.ts` — the watchdog holds one mount-scoped pool (created alongside the stagnation detector; disposed idempotently on stop() → the ctx.effect lifecycle) and passes it into every ExecArmOptions + the recovery scan; the pool hook lives INSIDE `createDispatchAgent` (the recovery redispatch calls it directly — no bypass hole): plan-review rides `reviewer:{groupId}` and draft-plans rides `drafter:{projectRoot}` when the resolved agent declares `mode: pooled` (the live policy's reviewer flipped pooled — drafter precedent mirrored, rotateEvery 8 + cross-group fresh as the anchoring backstops), while execute / closure-audit / deep-audit keep the fresh path (the dormant-executor ruling note; the P7 audit ban + the same-run auditor ≠ executor candidate check against executorSessions refreshed per cycle from the scanned snapshot's plan claim holders ∪ pool executor tags); every fresh dispatch still registers its session role tag (the mutex registry leg); the generation face (`attemptStale`) feeds the recovery judgment — live-but-stale pooled members are redispatched, never resumed (04 §2.3); absent pool ⇒ byte-compatible pre-pool fresh path (headless posture unchanged). Proof: `test/supervisor-core.test.mjs` (23 cases) + `test/supervisor-trigger.test.mjs` (43) + `test/supervisor-dispatch.test.mjs` (22) + `test/supervisor-terminal.test.mjs` (22) + `test/supervisor-failures.test.mjs` (9) + `test/supervisor-continuous.test.mjs` (15) + `test/supervisor-recovery.test.mjs` (18 — 9 recovery + 9 M3-WI30 stagnation/ping-pong) + `test/pool-lifecycle.test.mjs` (13 — the M4-WI32 agent-pool truth table) + `test/prompt-assembly.test.mjs` (16 — the M4-WI33 PromptAssembler truth table: FRESH/CONTINUE byte order, prefix discipline, dedup/resend, dir embed, hash ledger, compaction re-send, charter rotation, pool dual-mode e2e, backward-compat pin, native-executor dual-step) in the CI plugin chain. M4-WI33 adds the prompt-assembly face: `src/efficiency/prompt-assembler.ts` (the pure dual-mode assembler — FRESH = fixedPrefix blocks ++ dynamic, CONTINUE = hash-ledger delta; embedStamp template, dir full-text embed with explicit over-cap notes, hash8 = first 8 hex of sha256 same-source as `computeBasisHash`, prefix discipline fixed→dynamic→volatile, compaction re-send every 8th CONTINUE) + the `law-policy.mjs` `assembly:` schema section (embedStamp/continueDelta, synced tools↔assets through the bundle channel) + the consumption wiring: pool members carry the per-member hash ledger (create ⇒ FRESH, followup ⇒ CONTINUE, crash replacement starts empty — conservative), `exec-arm.ts` `dispatchPromptFor` composes the thin-pointer dispatch prompt as the DYNAMIC suffix at all four followup sites (undeclared agents pass through byte-identical), the charter-hash rotation leg (04 §2.2 leg 2, the M4-WI32 deferred collection) forces a new pool member when a fixedPrefix file's hash changes, and `native-executor.ts` consumes `config.assemblyPrefix` (the nativeModelSelection config-face precedent: the run child's first step FRESH / later steps CONTINUE, the engine prompt — already resolved through the promptsDir chain — stays the verbatim dynamic suffix, policy overlays never replacing it, 04 §7). M4-WI34 adds the context-profile faces: `src/efficiency/context-profile.ts` (the 04 §4 profile module — schema-v1 artifact `docs/references/context-profile.json` outside `missions/`, Read This First seeding, three-layer read-tally mining with debounce, headless degrade to WI35) with the watchdog's `emitTerminalEvent` tail triggering one fail-soft mining pass per run-terminal event (single-flight, mining failures receipted as exceptions, terminal receipt + stop-dispatch untouched; `profileMining` mount option injects io/sources for tests; pool members retain `sessionEvents` accessors exposed through `memberSessionEvents()` — the collector's DSH leg, zero pool-semantic change), the `law-policy.mjs` fixedPrefix fourth kind `profile` + `topN` (positive-int validation; `topN` outside kind profile is an error), and the `prompt-assembler.ts` consumption: `resolveAssemblyBlocks` pins `profileRoot`, `filesOfBlock` expands the artifact's top-N entries (reads desc, path asc) into per-file records riding the FILE semantics unchanged, fail-soft on missing/unknown-version artifacts; Proof: `test/context-profile.test.mjs` (13 — the M4-WI34 truth table: seeding, schema round-trip + determinism, unknown-version rebuild, session-event tally, missions/ zero-pollution reverse case, debounce both legs, three-layer merge, headless, watchdog e2e + fail-soft, top-N expansion, role-override backward-compat pin) joins the CI plugin chain.
- Reinforcement gate — **RETIRED (age-autonomy M2-WI22, plan `docs/plans/age-autonomy/2026-08-25-0950-2`; history as-built in M3-WI13 plan `2026-08-23-1852-3-pre-execute-plan-status-gate.md` Phase 1 Decision Record D1–D4)**: `plugin/dsh/src/plan-status-gate.ts` and its truth table `test/plan-status-gate.test.mjs` are deleted; the `verify:e2e:gate` script (`scripts/e2e-plan-gate.mjs`) is removed with them. The retirement split the gate's three faces: ① the "deny legacy `> Plan Status: completed` writes" protection semantics were collected into the law kernel rule `legacy-plan-freeze` (engine-side `law-rules.mjs`, glyph tolerance inherited from the shared `PLAN_STATUS_RE`, exceptions = the P8 literal three legs, registered `mode: enforce` in `missions/autonomy.policy.yml` — the run-state corpus of 54 legacy terminal plans stays frozen); ② the run-state evidence faces (F1 in-flight / F2 closed CLOSURE_AUDIT / F3 fast-path) are abolished — plan frontmatter/closures are the only completion evidence face (`docs/design/age-autonomy/01-file-ledger.md` §5.2; in-flight legitimacy is the claim face's, 0815-3); ③ the pre-execute mount collapsed into the law adapter (`src/law/host-adapter.ts` — the SOLO `tools/pre-execute` listener since WI22; 0815-1's coexistence posture converged to one listener, fail-open unchanged).
- **One engine activity at a time per project root — landed (M2-WI10; slot shared by draft since M3-WI12)**: a plugin-level `ActiveRunGuard` owned by the `mdcontrol.*` routes (in-service registry keyed by resolved projectRoot; cleared on every terminal path — success, engine failure, task crash, start failure; concurrent same-root start = explicit `run-in-progress` wire error). Since M3-WI12 `mdcontrol.draft` occupies the SAME root slot (draft↔run mutual exclusion: a draft writes missions/docs while a run reads them — the conservative posture may relax later; concurrent same-root draft = the same wire error). This is deliberately stricter than the engine (the CLI startup reaper explicitly spares concurrent active runs and supports N parallel runs, including same-root); the plugin starts conservative and may relax later. Concurrent activities across different roots remain independent engine instances. `mdcontrol.analyze` does NOT occupy the guard (read-mostly single turn over an already-terminal run — 1852-2 Phase 1 Decision 3). (Run-guard adjudicated in plan `2026-08-23-1447-1` §Deferred But Adjudicated, collected by `2026-08-23-1621-2`; draft-slot sharing adjudicated in `2026-08-23-1852-2` Phase 1 Decision 2.)

Draft jobs — as-built split (M3-WI12 adjudication, plan `2026-08-23-1852-2` Phase 1 Decision 2): the CLI/monitor path keeps `startDraftJob` detached-node concurrency exactly as-is (engine-level background work; moving it in-process remains deferred). The PLUGIN route `mdcontrol.draft` deliberately does NOT spawn a detached node — `main.js`/`draft-job.mjs`/`spawner.mjs` are outside the bundle (NOT-bundled list), so the in-host form runs the same `cmdDraftMission` pipeline as a detached in-host task through the pre-authorized executor seam (native AI dispatch). One vocabulary on disk either way: `_tmp/<jobId>/draft-state.json`.

Monitor: unchanged. It reads run-state files from disk; whether the engine runs as CLI or in-host is invisible to it. As-adjudicated (2026-08-23, M4-WI15, plan `2026-08-23-2202-2`): a native client panel is **deferred** — the decision record verified the host's static client-plugin surface is reachable from this bundle form (package.json `dsh.client` + `exports['./client']`, scanned per loader entry — the bundle patch's own service row makes `dsh-mission-control` a loader entry; better-sidebar dual-form precedent) with zero new shipped dependencies, but found it developer-preview and undocumented for external authors, poll-only over this plugin's data plane (`/mdcontrol/api/status|list` — no SSE-equivalent push exists there; the monitor's SSE lives in the separate monitor server, which the bundle does not ship), and without recorded demand. Reopen triggers live in the plan's `Deferred But Adjudicated` (T1 stable documented client-plugin API / T2 recorded in-DSH visibility demand with the monitor unreachable / T3 monitor embedding ruled out while demand holds).

## AGE Preset (M4-WI14, landed 2026-08-23 — plan `2026-08-23-2202-1`)

The AGE mode ships as a **host-discoverable agent preset** at `plugin/dsh/preset/age/` — a directory holding `agent.cordis.yml` (the composition), `preset.yml` (display-only name/description/order), and `age-mode.mjs` (the one local row). Host-loader facts this shape rests on are the R1 §7 verification record (closed by the plan's Phase 1 D1, host source `packages/preset/agent-presets/`): a preset = its directory (id = directory name), mounted ONCE per process under a standing scope, sessions join by scope parentage; bare package names resolve from the HOST composition base while relative names travel with the directory; a preset row publishing into the root realm makes the whole mount REJECTED — the core of the realm posture below.

- **Realm posture (the P4 gate's collision freedom, by construction)**: the AGE preset carries ZERO service rows. Every row either publishes nothing (tool/persona/instructions/skill rows register into host registries and layered prompt/skill registries) or sits inside the preset's own entry-local isolate realm (the compaction group — same labels as the host standard preset are safe: entry-local realms are private per standing mount). `dsh-mission-control` stays mounted exactly ONCE, by `cordis.patch.yml`; AGE sessions consume it through the globally registered skills and the `/mdcontrol/api` HTTP dispatcher — never through a preset-side second instance (which would break the single active-run guard). Machine-pinned by `test/age-preset.test.mjs` (zero-service-row + bundle-patch-single-mount cross-check) and the composition leg.
- **Contents**: `age-mode.mjs` registers ONE system-prompt section (`age:mode`, order 10 — additive, not `complete`) carrying the AGE session posture (repo-as-source-of-truth, owner-docs-first routing, file-in/file-out) + the Mission Control entry points (three skills + the HTTP face) + the async-job contract essentials. It carries NO marker examples by design (interactive posture, not a step executor — pinned by the structural gate; the preset stays outside the engine `prompt-check.mjs` jurisdiction by construction). The composition rows: the persona row (static text, shadows the deployment persona per the `dsh-persona` scope-only contract), `dsh-agent-instructions` (the host AGENTS.md digest — complement, not duplicate), a standard-preset-subset tool catalog (bash/pwsh platform pair, `dsh-tool-fs`, `dsh-tool-fs-search`, `dsh-tool-todo`, skills rows `dsh-skill-filesystem` + `dsh-tool-skill`, compaction group). Deliberately absent: goals, plan mode, delegation/workflows, web, jobs — the AGE loop drives through mdcontrol routes; smaller row surface = smaller collision surface. ALSO absent vs the host standard preset: `dsh-command-compact` (`/compact`) — it injects the host-plane `commands` registry, and a preset row waiting for a service the deployment never composes makes the WHOLE preset unmountable (observed live: the loader's inactive-row guard rejected the first composition).
- **Route injection, two legs**:
  1. Interactive sessions — the `age:mode` section documents the surface; the three `mission-control-*` skills are ALREADY registered globally by the service mount (§Service Surface) and reach AGE sessions through the merged skill catalog. Reuse, no preset-side re-registration (the skills registry is layered per scope; a preset-local duplicate is at best double-render).
  2. Mission children — `NativeExecutor`'s create calls pass a `setup` that mounts `meta.agentPreset` (the engine config `agent` field) through the host roster (`agentCtx.get('agentPresets')`): roster absent / id not on it / no agent field → no-op (roster-less compositions and leftover non-DSH agent values never brick native runs; the roster's own unjoined advisory covers observability); preset on the roster → `mount()` — a broken composition rejects and rolls the child creation back (fail-loud for real AGE deployments). This wiring exists because `meta.agentPreset` is passive metadata — no host code mounts it for direct `agents.create` calls (only a caller-provided `setup` composes; the subagent-tool path does it via `composeFrom`, host source `packages/subagent/subagent/src/child-agent.ts`). `agents.resume` needs no setup (durable scope parentage).
- **Agent-field plumbing**: the engine's run path resolves `agent` as `args.agent || env.OPENCODE_AGENT || "build"` and never consults `missions/base.json` (only the draft/analyze return points do). `bootstrapNativeConfig` therefore defaults the native run config's `agent` from base.json when no explicit arg/env names one — explicit args/env keep precedence; engine untouched. Consumers select AGE mode per project via `missions/base.json` `"agent": "age"` (runs, drafts, and analyze then share one knob).
- **Distribution**: copy the `plugin/dsh/preset/age/` directory into the host's user preset root (`${DSH_HOME:-$HOME/.dsh}/.agent-presets/age/`) — or register an extra configured root pointing at a copy — then restart the host. Step-by-step in `docs/process/dsh-plugin-development-guide.md` §AGE Mode.
- **Verification**: structural gate `test/age-preset.test.mjs` (11 cases) in the CI plugin chain; unit wiring tests in `test/native-executor.test.mjs` (5 cases) + `test/engine-bridge.test.mjs` (base.json defaulting); the composition gate `npm run verify:e2e:preset` (explicit local, never CI-blocking — R3 §5 posture) proves the P4 claim same-tree: roster health, interactive mount model-visible (section + AGENTS.md digest coexistence + preset-only grep/glob tools), mission-child composition with valid markers, `mdcontrol` collision-free before/after, skills intact (four consecutive green runs, `docs/testing/2026/08-23.md`). Real-host roster registration is the documented manual env leg; true-model AGE-session natural-language quality is the plan's deferred watch-only residual.

### Execution Model (where the long-running loop lives)

In native mode the FlowEngine control loop runs **inside the DSH host process** as a cordis service. The coupling is inherent: native dispatch requires `ctx.agents`, which is reachable only in-process; an out-of-process engine would have to drive agents via SDK/headless — i.e., degenerate to ProcessExecutor-style spawning.

What executes where:

| Component | Location | Form |
| --- | --- | --- |
| FlowEngine state-machine loop | DSH host process | pure async JS coroutines — mostly `await agent.whenIdle()`; negligible CPU, does not block the event loop |
| Agent steps | child agents (cordis scopes, same process) | not OS processes; creation cost ≈ a function call |
| Script/tool steps (`CLOSURE_SCRIPT_CHECK`, `BUILD_VERIFY`) | short-lived OS child processes | spawned from the host (`pnpm test` etc.), exit immediately |

Accepted risks and mitigations:

| Risk | Mitigation |
| --- | --- |
| Host crash/restart mid-mission | run-state atomic per-step writes enable disk-based step resume; nothing depends on memory alone |
| Engine defect destabilizing the host | embed-mode gating removes engine startup diagnostics (incl. the orphan reaper that kills `opencode run` processes) from the host process (M1-WI4) |
| Resource pile-up | single-run-per-root guard at route level |
| Coupling proves unstable in practice | degradation ladder: detached engine + headless CLI driver behind the SAME `mdcontrol.*` surface — skills/routes unchanged, backend swaps |

Progress monitoring channels (all reading the same run-state files):

1. Monitor dashboard (existing Vue3+SSE, port 9300) — zero-change compatibility, step timeline/markers/timing.
2. DSH native subagents topology UI — one healthy run-level child descriptor per run (`Mission: <mission>`, continuable); step-level progress flows through channel 1.
3. In-chat query — `mdcontrol.status` reads run-state without dispatching any AI.
4. Trajectory view — child-agent turns are ordinary session logs, filterable by source.
5. Completion summary returned through the skill.

Realtime leader is channel 1 (SSE pushes step-level events). The M4 RPC-direct panel question was adjudicated 2026-08-23 (M4-WI15, plan `2026-08-23-2202-2`): **deferred** — had it been built, it would swap its data source (polling `mdcontrol.status`/`mdcontrol.list`, same run-state schema, strictly weaker realtime than channel 1), and that finding stands as the reopen-time implementation constraint.

## Dependency and Version Risk

`@deepseek-ai/*` packages used by the plugin layer (`dsh-agent`, `dsh-subagent`, cordis core; `dsh-tools` type-only since the M3-WI13 pre-execute reinforcement gate — `dsh-goal` still reserved/unconsumed) are developer-preview and versioned pre-release (reference pinning practice: better-sidebar pins `@deepseek-ai/dsh-subagent@0.0.1-rc.1`). Mitigations:

- The plugin touches exactly **six** host calls (`create`, `resume`, `followup`, `status`, `dispose`, plus `get` since M2-WI10 — the terminal-receipt lookup `agents.get(sessionId) → agent.followup(...)`, host-source verified at `AgentRegistry.get` in `packages/core/agent/src/index.ts` ["Look up a live agent … undefined when no live agent has that id"], live-agents-only semantics, returns the bare Agent without a handle) plus descriptor registration; breakage repair is localized to `native-executor.ts` / `mdcontrol-routes.ts`. (The original five-call pin was amended by plan `2026-08-23-1621-2` Phase 1 Decision 3, which performed the verification obligation.) Since M4-WI14 the executor ALSO consumes the preset roster face (`agentPresets.list` + `.mount` through the child's scoped `agentCtx.get`) — consumption-only, structurally typed (`PresetRosterFace` in `native-executor.ts`), degrading to a no-op wherever no roster is composed; the roster PACKAGE itself is a devDep (composition leg only), never a shipped dependency.
- Host package versions are pinned per release; bumping is an explicit changelog event.
- Engine core remains DSH-agnostic; worst case is disabling the plugin while the CLI form continues unaffected.

## Phased Delivery

Day-to-day development procedure (host setup, Creator-mode online loop, flow-customization checklist) lives in `docs/process/dsh-plugin-development-guide.md`.

| Phase | Deliverable | Verification gate |
| --- | --- | --- |
| P1 ✅ delivered 2026-08-23 | StepExecutor seam over the delegates injection points; ProcessExecutor wrapper; programmatic orchestration entry + `EXIT_MAP` hoist; driver validation; embed-mode gating of startup diagnostics; module-boundaries.md update | full engine test suite green (incl. exit-map pinning); CLI behavior unchanged (`run demo` smoke test) |
| P2 ✅ delivered 2026-08-23 (M2-WI6 + M2-WI7 + M2-WI8 + M2-WI9 + M2-WI10; WI10 evidence complete — prereq WI9 `done` verified at delivery time) | Plugin shell + NativeExecutor; `mdcontrol.run` executing `demo` mission end-to-end natively. **Delivered (WI6)**: `plugin/dsh/` scaffold, bundle manifest, isolate-realm patch, build bundling + import-closure gate, plugin test entry. **Delivered (WI7)**: `DshNativeExecutor` (full dispatch chain, handle lifecycle, watchdog, exit synthesis, plugin-layer minimal tool spawn) + engine-bridge selection factory/native config wiring, unit-tested over a fake in-process agents service incl. an `orchestrateRun` full-chain callback smoke. **Delivered (WI8)**: L2 dual-backend parity matrix (six R3 §3 assertion groups over ProcessExecutor-with-fake-runner vs NativeExecutor-with-fake-agents; 3 divergence-ledger entries all owner-doc-backed) wired merge-blocking through root `verify-age.sh` + `.github/workflows/age-ci.yml`. **Delivered (WI9)**: L3 host-integration harness — `scripts/host-harness.mjs` boots a real spawned DSH runtime serving `dsh-sdk-jsonrpc-server` (16-row non-PTY composition fixture + exact-pinned devDeps, R3 §6 resolved) and drives it over stdio NDJSON; four-scenario green run recorded keylessly (official e2e precedent) in `docs/testing/2026/08-23.md`; env-gated `verify:native` local gate (skip without flag / fail-fast without key / never CI-blocking, R3 §5); 12 transport unit cases inside the plugin CI chain. **Delivered (WI10, plan `2026-08-23-1621-2`)**: `mdcontrol.run/status/list` route layer + async job contract (`beginNativeMission` detached in-host task, immediate `{runId, status:'started'}`) + plugin-level active-run guard (single run per projectRoot, 1447-1 adjudication collected) + opt-in terminal receipt (`agents.get` sixth host call) + L4 dual-leg e2e (`npm run verify:e2e`: real standalone-CLI leg + real in-process cordis-runtime leg over a deterministic scripted model endpoint; three consecutive green runs recorded in `docs/testing/2026/08-23.md`) | demo mission completes with identical run-state shape (normalized dual-leg diff EMPTY, `e2e-report.json`); markers parsed (every AI step's marker valid for its transitions, both legs); correction-retry exercised once artificially (REVIEW `banana` break → exactly one correction re-prompt → recovered to `completed`). Known boundary recorded, non-blocking: monitor step-log endpoints are `oc-`-prefix-only for native-named artifacts (`docs/bugs/2026-08-23-monitor-native-log-naming.md`) |
| P3 delivered 2026-08-23 (M3-WI11 + M3-WI12 + M3-WI13) | `onboarding` parity + descriptor registration + skills wired + pre-execute gate | WI11 landed (plan `2026-08-23-1852-1`): onboarding dual-form parity proven in the deterministic stub domain (extended `verify:e2e`: both forms complete with EMPTY normalized run-state diff, markers valid for the real mission-driver flow, bounded one-loop script — mechanism-plane assertions only, doc-quality semantics out of the deterministic gate) + descriptor registration at create (durable `subagent/descriptor` seed, machine-asserted in the e2e native legs' persisted session logs) + the WI10 monitor step-log finding fixed engine-side (dual-label `oc-`/`native-` endpoints, bug closed). WI12 landed (plan `2026-08-23-1852-2`): `mdcontrol.draft` + `mdcontrol.analyze` routes (draft = async job contract through the pre-authorized `cmdDraftMission` executor seam — the plan's only engine diff, `orchestrator.js` + its unit tests, CLI byte-identical; analyze = synchronous single-turn postmortem through a thin plugin runner adapter, zero engine diff) + the three Mission Control skills on `ctx.skills` (runtime rows via reactive `ctx.inject`; e2e asserts `ctx.skills.list()` membership + full route behavior: analyze tag-parse explicit/latest, draft two-stage terminal state). WI13 landed then RETIRED (plan `2026-08-23-1852-3`, zero engine diff; retired by age-autonomy M2-WI22 plan `docs/plans/age-autonomy/2026-08-25-0950-2`): the `tools/pre-execute` plan-status completed gate — deleted together with its truth table and `verify:e2e:gate` e2e; protection semantics collected into the law kernel rule `legacy-plan-freeze` (enforce, PLAN_STATUS_RE glyph tolerance, P8 exception channels), run-state evidence faces F1/F2/F3 abolished (ledger receipts + claims are the only completion evidence face), pre-execute mount collapsed into the law adapter as the solo listener. Historical landing verification (1852-3): Unit suites: engine 660/660 (zero engine diff), plugin 116/116 (+23 gate cases with engine-generated run-state fixtures); `verify:e2e:gate` three consecutive green runs on the real host tool pipeline (deny write+edit / allow F1+D3, evidence `docs/testing/2026/08-23.md`); true-model natural-language skill invocation stays an env/manual leg by design |
| P4 delivered 2026-08-23 for WI14 (M4-WI14, plan `2026-08-23-2202-1`, zero engine diff) | AGE preset integration (AGE mode) — WI14 landed: `plugin/dsh/preset/age/` (age:mode prompt section + standard-subset tool catalog, ZERO service rows) + NativeExecutor create-setup preset composition (mounts `meta.agentPreset` when a roster is composed and the id is on it; no-op otherwise) + `bootstrapNativeConfig` base.json `agent` defaulting for native runs + structural gate (11 cases) + unit wiring tests (5) + the `verify:e2e:preset` composition gate (roster health · interactive mount model-visible incl. AGENTS.md digest coexistence + preset-only grep/glob tools · mission child composed with valid markers · mdcontrol collision-free before/after · skills intact — four consecutive green runs, evidence `docs/testing/2026/08-23.md`). R1 §7 closed (host preset loader verified, plan Phase 1 D1). **Gate "preset + plugin compose without realm collision": MET** (zero service rows by construction + same-tree composition leg). WI15 delivered 2026-08-23 (plan `2026-08-23-2202-2`): status-panel decision adjudicated as **explicit deferral** — host static client-plugin surface verified reachable from the bundle form (zero new shipped deps) but developer-preview/undocumented; unique value (DSH-embedded step-level status, poll-only) unrequested; owner docs written back as-adjudicated + `Deferred But Adjudicated` record with reopen triggers; pure-docs change-set (plugin chain 133/133, verify-age.sh GREEN, zero engine/plugin diff) — **P4 complete** | preset + plugin compose without realm collision |

Each phase lands as its own plan under `docs/plans/` with draft review and closure audit per the standard workflow.

## Update Rule

When the StepExecutor interface, native dispatch chain, or packaging layout changes supported behavior, update this doc and the affected owner docs (`docs/architecture/module-boundaries.md`, `docs/architecture/mission-driver-baseline.md` §Driver selection and §Public Exports — the latter when P1 hoists `EXIT_MAP` out of `main.js`) in the same change.
