# Testing Note — 2026-08-23: L3 host-integration harness keyless green run (dsh-plugin M2-WI9)

> Plan: `docs/plans/dsh-plugin/2026-08-23-1621-1-l3-host-harness-sdk-server.md` Phase 3 Proof
> Gate: `npm --prefix plugin/dsh run verify:native:keyless` (env-gated local script per R3 §5; NOT CI-blocking — `./verify-age.sh` / `age-ci.yml` never invoke it)

## Date / Environment

- 2026-08-23, macOS (darwin), Node `v25.3.0` (homebrew), plugin deps installed via `npm install` in `plugin/dsh/`.
- Credential-free mode: the harness spawns a local OpenAI-compatible SSE stub endpoint on `127.0.0.1:<ephemeral>` (official `examples/jsonrpc-agent/tests/keyless-smoke.e2e.ts` precedent) — real spawn of the real runtime, zero external network, zero model credentials.
- Composition under test: `node <plugin/dsh/node_modules/@deepseek-ai/dsh-sdk-jsonrpc-demo/lib/bin.js> plugin/dsh/test/fixtures/harness.cordis.yml` — all 16 composition packages at exact `0.1.1-rc.2` (devDeps, Phase 1).

## Command

```
cd plugin/dsh
npm run verify:native:keyless          # = node scripts/verify-native.mjs --keyless
```

Gate semantics also verified the same day:

- `npm run verify:native` (no env) → `verify:native: skipped (DSH_VERIFY_NATIVE is not "1").` → **exit 0**
- `DSH_VERIFY_NATIVE=1 npm run verify:native` (no key) → fail-fast `DEEPSEEK_API_KEY is missing` → **exit 1**
- `./verify-age.sh` (no env) → **exit 0**, L1+L2 GREEN (L3 not wired into CI)

## Scenarios under test (R3 §4 post-erratum harvest surface)

1. `marker-roundtrip` — step-style prompt with marker contract example; gate = root-session LAST committed assistant text (via `session.event` `assistant/message` + `turn/end` reason) contains `<AI_STEP_RESULT>pass</AI_STEP_RESULT>`.
2. `silent-idle-arrival` — `session.status` idle arrives for the prompted session (running→idle transition observed).
3. `session-continuity` — two turns on one `sessionId`; gates = stream identity (turns 1→2, assistant messages on both, same session) + nonce-echo (turn-2 root assistant text contains the turn-1 `NONCE-<hex>`).
4. `shutdown-hygiene` — `shutdown` → `{}` response, child exit code 0, `ps` orphan scan (bin path + scratch root needles) empty, stdout purity (0 malformed NDJSON lines).

## Observed behavior (two consecutive runs, identical; run 2 = flake check)

```
[harness] initialized: {"name":"deepseek-harness-sdk-runtime","version":"0.0.1"} (maxTokens=2048)
[harness] scenario marker-roundtrip: PASS — turn 1 reason={"kind":"completed"}; root last assistant text contains marker
[harness] scenario silent-idle-arrival: PASS — idle arrived (turn 1 reason={"kind":"completed"}; running transition seen: true)
[harness] scenario session-continuity: PASS — stream-identity ok (turns 1→2 on sessionId=harness-continuity, assistant messages 1+1); nonce-echo ok (turn-2 reason={"kind":"completed"})
[harness] scenario shutdown-hygiene: PASS — shutdown={} ok; exit code=0 signal=null; orphans=0; malformed stdout lines=0
[harness] observation: subagent.finished count=0 (no model delegation occurred — expected for direct root prompts)
[harness] observation: stdout purity — 0 malformed lines, 0 inbound (dead-capability) requests
[harness] SUMMARY mode=keyless (local stub endpoint) scenarios=4 pass=4 fail=0
[harness] keyless stub served 4 model request(s)
```

Exit code 0 on both runs. `subagent.finished count=0` matches the R3 §4 erratum: the notification only fires for model-delegated subagents, never for direct root-session prompts — recorded as a conditional, non-gating observation exactly as designed.

## Expected behavior

All four scenarios green against a REAL spawned DSH runtime serving `dsh-sdk-jsonrpc-server` over stdio NDJSON; clean shutdown with no orphan processes; stdout stays pure NDJSON. — **Met.**

## Deferred-ledger disposition (1447-3 "真 spawn 腿矩阵覆盖" reopen clause)

The L2 mock domain was compared against live behavior in this run: notification frame shapes, `turn/end` reason vocabulary, `{messageId}` receipt shape, `session.status` transitions, and shutdown/exit semantics all matched the mock/official-e2e domain — **no divergence found; the deferred item stays closed** (no reopen). The comparison domain is the keyless stub endpoint; a real-model run can only add provider-side variance, which the L2/L3 assertion surfaces do not gate on.

## Follow-up required

- **Credential-backed live run** (real DeepSeek endpoint): requires `DEEPSEEK_API_KEY` (+ optional `DEEPSEEK_BASE_URL`), which this machine does not have in any env/store. The exact command is wired and fail-fast-verified:
  `DSH_VERIFY_NATIVE=1 DEEPSEEK_API_KEY=… npm --prefix plugin/dsh run verify:native`
  Human execution + recording the output here closes the credential leg. Not a defect — an environment availability note.
- No `docs/bugs/` entry: no defect discovered.
