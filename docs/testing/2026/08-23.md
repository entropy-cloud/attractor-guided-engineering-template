# Testing Note — 2026-08-23: L3 host-integration harness keyless green run (dsh-plugin M2-WI9)

> Plan: `docs/plans/dsh-plugin/2026-08-23-1621-1-l3-host-harness-sdk-server.md` Phase 3 Proof
> Gate: `npm --prefix plugin/dsh run verify:native:keyless` (env-gated local script per R3 §5; NOT CI-blocking — `./verify-age.sh` / `age-ci.yml` never invoke it)

## Date / Environment

- 2026-08-23, macOS (darwin), Node `v25.3.0` (homebrew), plugin deps installed via `npm install` in `plugin/dsh/`.
- Credential-free mode: the harness spawns a local OpenAI-compatible SSE stub endpoint on `127.0.0.1:<ephemeral>` (official `examples/jsonrpc-agent/tests/keyless-smoke.e2e.ts` precedent) — real spawn of the real runtime, zero external network, zero model credentials.
- Composition under test: `node <plugin/dsh/node_modules/@deepseek-ai/dsh-sdk-jsonrpc-demo/lib/bin.js> plugin/dsh/test/fixtures/harness.cordis.yml` — all 16 composition packages at exact `0.1.1-rc.2` (devDeps, Phase 1).

## Command

```
cd plugin/dsh
npm run verify:native:keyless          # = node scripts/verify-native.mjs --keyless
```

Gate semantics also verified the same day:

- `npm run verify:native` (no env) → `verify:native: skipped (DSH_VERIFY_NATIVE is not "1").` → **exit 0**
- `DSH_VERIFY_NATIVE=1 npm run verify:native` (no key) → fail-fast `DEEPSEEK_API_KEY is missing` → **exit 1**
- `./verify-age.sh` (no env) → **exit 0**, L1+L2 GREEN (L3 not wired into CI)

## Scenarios under test (R3 §4 post-erratum harvest surface)

1. `marker-roundtrip` — step-style prompt with marker contract example; gate = root-session LAST committed assistant text (via `session.event` `assistant/message` + `turn/end` reason) contains `<AI_STEP_RESULT>pass</AI_STEP_RESULT>`.
2. `silent-idle-arrival` — `session.status` idle arrives for the prompted session (running→idle transition observed).
3. `session-continuity` — two turns on one `sessionId`; gates = stream identity (turns 1→2, assistant messages on both, same session) + nonce-echo (turn-2 root assistant text contains the turn-1 `NONCE-<hex>`).
4. `shutdown-hygiene` — `shutdown` → `{}` response, child exit code 0, `ps` orphan scan (bin path + scratch root needles) empty, stdout purity (0 malformed NDJSON lines).

## Observed behavior (two consecutive runs, identical; run 2 = flake check)

```
[harness] initialized: {"name":"deepseek-harness-sdk-runtime","version":"0.0.1"} (maxTokens=2048)
[harness] scenario marker-roundtrip: PASS — turn 1 reason={"kind":"completed"}; root last assistant text contains marker
[harness] scenario silent-idle-arrival: PASS — idle arrived (turn 1 reason={"kind":"completed"}; running transition seen: true)
[harness] scenario session-continuity: PASS — stream-identity ok (turns 1→2 on sessionId=harness-continuity, assistant messages 1+1); nonce-echo ok (turn-2 reason={"kind":"completed"})
[harness] scenario shutdown-hygiene: PASS — shutdown={} ok; exit code=0 signal=null; orphans=0; malformed stdout lines=0
[harness] observation: subagent.finished count=0 (no model delegation occurred — expected for direct root prompts)
[harness] observation: stdout purity — 0 malformed lines, 0 inbound (dead-capability) requests
[harness] SUMMARY mode=keyless (local stub endpoint) scenarios=4 pass=4 fail=0
[harness] keyless stub served 4 model request(s)
```

Exit code 0 on both runs. `subagent.finished count=0` matches the R3 §4 erratum: the notification only fires for model-delegated subagents, never for direct root-session prompts — recorded as a conditional, non-gating observation exactly as designed.

## Expected behavior

All four scenarios green against a REAL spawned DSH runtime serving `dsh-sdk-jsonrpc-server` over stdio NDJSON; clean shutdown with no orphan processes; stdout stays pure NDJSON. — **Met.**

## Deferred-ledger disposition (1447-3 "真 spawn 腿矩阵覆盖" reopen clause)

The L2 mock domain was compared against live behavior in this run: notification frame shapes, `turn/end` reason vocabulary, `{messageId}` receipt shape, `session.status` transitions, and shutdown/exit semantics all matched the mock/official-e2e domain — **no divergence found; the deferred item stays closed** (no reopen). The comparison domain is the keyless stub endpoint; a real-model run can only add provider-side variance, which the L2/L3 assertion surfaces do not gate on.

## Follow-up required

- **Credential-backed live run** (real DeepSeek endpoint): requires `DEEPSEEK_API_KEY` (+ optional `DEEPSEEK_BASE_URL`), which this machine does not have in any env/store. The exact command is wired and fail-fast-verified:
  `DSH_VERIFY_NATIVE=1 DEEPSEEK_API_KEY=… npm --prefix plugin/dsh run verify:native`
  Human execution + recording the output here closes the credential leg. Not a defect — an environment availability note.
- No `docs/bugs/` entry: no defect discovered.

---

# Testing Note — 2026-08-23: L4 dual-leg e2e demo mission green run (dsh-plugin M2-WI10)

> Plan: `docs/plans/dsh-plugin/2026-08-23-1621-2-mdcontrol-run-async-contract-e2e.md` Phase 2 Proof
> Gate: `npm --prefix plugin/dsh run verify:e2e` (explicit local invocation, R3 §5 posture — never wired into `verify-age.sh` / `age-ci.yml`; zero credentials, zero external network)

## Date / Environment

- 2026-08-23, macOS (darwin), Node `v25.3.0` (homebrew), plugin deps installed via `npm install` in `plugin/dsh/`.
- Deterministic scripted model domain (e2e gate is keyless/stub-only BY DESIGN — exact marker sequencing cannot be guaranteed with a live LLM):
  - CLI leg (ProcessExecutor): real standalone engine child process (`tools/mission-driver/src/main.js demo`), hermetic driver = executable `opencode` stub FIRST on PATH (WI3 driver whitelist pins the driver NAME; prompt arrives as last argv, opencode promptMode "arg"; `opencode session list` → `[]`). Shared policy module `plugin/dsh/scripts/e2e-policy.mjs`.
  - native leg (NativeExecutor): real cordis runtime booted IN-PROCESS (`@deepseek-ai/dsh-app-boot` `boot()` + `plugin/dsh/test/fixtures/e2e.cordis.yml` — the WI9 L3 16-row composition minus `sdk-jsonrpc-server` plus the real `mdcontrol` service row, non-isolated so `ctx.get('mdcontrol')` resolves), real agents/spine/llm stack, model served by a local scripted OpenAI-compatible SSE endpoint (127.0.0.1).
- Scratch demo mission: `missions/demo.json` (model `deepseek-v4-flash`) + custom flow `demo` (CHECK → REVIEW → EXEC(tool) → DONE; runDirs `cli-e2e-mission-driver` / `native-e2e-mission-driver`).

## Command

```
cd plugin/dsh
npm run verify:e2e            # = node scripts/e2e-demo.mjs   (--keep preserves scratch + report)
```

## Observed behavior (three consecutive green runs post-fix; identical shape)

```
[e2e] CLI leg: spawning the real standalone engine (ProcessExecutor backend)…
[e2e] native leg: booting the real cordis runtime (fixture e2e.cordis.yml, stub model on 127.0.0.1:<port>)…
[e2e] mdcontrol.run resolved in 4ms → {"runId":"native-e2e-mission-driver",…,"status":"started",…}
[18:35:46] [step 1] CHECK (visit #1) … marker: pass
[18:35:46] [step 2] REVIEW (visit #1)
[18:35:46]   marker "banana" not in transitions, correction retry 1/2 (session=native-…)
[18:35:47]   corrected marker: pass → pass
[18:35:47] [step 3] EXEC (visit #1) … marker: pass
[18:35:47] [step 4] DONE (visit #1) … marker: pass → done: completed
[e2e] mdcontrol.status terminal: {"exitCode":0,"status":"completed","error":null}
[e2e] stub model served 4 request(s): CHECK → REVIEW → correction → DONE
[e2e] normalized run-state diff: EMPTY (dual-leg shape identity)
[e2e] SUMMARY: PASS — dual-leg demo mission green, shape identity, markers parsed, correction-retry observed once and recovered
```

Key assertions (all machine-checked in `scripts/e2e-demo.mjs`, report at `<scratch>/e2e-report.json`):

- CLI leg exit 0; engine log shows `correction retry 1/2` + `"banana" not in transitions` (artificial break observed on the process backend too).
- `mdcontrol.run` async contract on the REAL host surface: resolved in ~4ms with `{runId, status:'started'}` while the run was still live; `mdcontrol.status` polled to terminal `completed`/exit 0; `mdcontrol.list` returned BOTH legs' runs.
- Stub model served EXACTLY 4 requests (CHECK, REVIEW-break, correction, DONE); artificial break fired exactly once; correction re-prompt observed exactly once and recovered to `pass`.
- Markers parsed explicitly: every AI step in BOTH legs has a `marker` field valid for its transitions (CHECK/REVIEW/EXEC/DONE all `pass`); REVIEW recovered to `pass` after the break.
- `normalizeRunState` (matrix-harness vocabulary) dual-leg diff EMPTY — divergence allow-list (D1/D2/D3 + R3 §3 sessionId value/timing/error-text/basename exemptions) not even needed beyond the built-in type-only fields.

## Monitor dashboard render check (manual, file-format identity)

Procedure: `node tools/mission-driver/src/main.js monitor --dir <scratch> --monitor-port 9378` then REST verification of the render data + dashboard HTML serving (CLI-based render-data check; no browser interaction performed — recorded honestly as such):

- `GET /api/runs` lists BOTH runs: `native-e2e-mission-driver` / `cli-e2e-mission-driver`, status `completed`, mission `demo`, flow `demo`.
- `GET /api/runs/<id>` renders 4 steps each with status `completed` + marker `pass` + timing fields; step logFiles carry each backend's naming (`oc-CHECK-…` vs `native-CHECK-…`).
- Dashboard HTML served (`<!DOCTYPE html>` Vue app shell).

**Finding (recorded, not fixed — engine-side, out of this plan's zero-engine-diff red line)**: the monitor's step-LOG endpoints are `oc-`-prefix-convention-only — `listStepLogs` matches `^oc-(.+)-(\d+)-([a-z0-9]+)\.log[.prompt]` and `handleGetLog` searches `oc-<step>-` prefixes (monitor.js:461/646), so native-mode log artifacts (`native-<step>-…`) are NOT listed in the run-detail log panel and `GET /api/runs/<id>/logs/<step>` answers `{"error":"log not found"}` for native runs. Run list/detail/steps/markers/timing/config rendering is unaffected (run-state file-format identity holds — R3 §3 group 6's claim). This contradicts the WI7 packaging-doc sentence "preserving monitor log viewing" for the log-endpoint subset; doc corrected by this plan's Phase 3, engine-side regex widening recorded as follow-up (`docs/bugs/2026-08-23-monitor-native-log-naming.md`).

## Expected behavior

Dual-leg demo mission green with identical normalized run-state shape, markers parsed on both legs, and the artificial correction-retry observed once and recovered — **Met** (P2 gate evidence).

## Follow-up required

- Monitor `oc-` prefix convention vs native log names — engine-side widening follow-up, see `docs/bugs/2026-08-23-monitor-native-log-naming.md` (out of WI10 red line; not a P2-gate blocker).
- No credential leg exists for this gate by design (deterministic scripted model); real-model native e2e belongs to future manual verification when the M3 skills surface lands.

# Testing Note — 2026-08-23: onboarding dual-form L4 parity + descriptor health + monitor render fix (dsh-plugin M3-WI11)

> Plan: `docs/plans/dsh-plugin/2026-08-23-1852-1-onboarding-parity-subagent-descriptors.md` Phase 3 Proof (also machine-pins the Phase 1 monitor fix and the Phase 2 descriptor registration)
> Gate: `npm --prefix plugin/dsh run verify:e2e` (same single-command entry as WI10 — extended in place per Phase 3 Decision 4; still explicit-local, never wired into `verify-age.sh` / `age-ci.yml`; zero credentials, zero external network)

## Date / Environment

- 2026-08-23, macOS (darwin), Node `v25.3.0` (homebrew), plugin deps installed via `npm install` in `plugin/dsh/`.
- Same two-form harness as the WI10 note, now driving TWO missions per run: `demo` (unchanged WI10 contract) + `onboarding` (WI11 dual-form parity).
- Onboarding scratch basis (Phase 3 Decision 1 — committed-fixture replica of the minimal install SHAPE): `missions/base.json` + `missions/onboarding.json` copied VERBATIM at runtime from `template/install/missions/` (fixture tracks the real install artifact), plus empty `docs/plans/onboarding/`, `docs/audits/`, `docs/context/` skeleton and a one-done-item `docs/backlog/onboarding-roadmap.md`. `install-age.sh` itself not exercised (its copy correctness is the installer's own verification domain, not the dual-FORM parity claim).
- Deterministic script (Decision 2): the REAL built-in `mission-driver` flow; empty plans/audits keep REVIEW_PLANS (`forEach draftPlans()`), EXEC_PLANS (`forEach activePlans()`), DEEP_AUDIT (all `when` false) at ZERO agent turns; stub answers CHECK→`pass`, DRAFT_PLANS→`nothing` ×2; the second `nothing` hits the audit-quota clean short-circuit (auditRound 1/3, no active plans, no open audits). Bounded: exactly 3 stub turns, one loop round.
- Assertion surface (Decision 3 — mechanism plane): step sequence, per-step markers valid against the REAL `flows/mission-driver.json` transitions, artifact existence, `normalizeRunState` dual-leg shape diff. The stub driver does not write docs — "fills copied docs" semantic quality is OUT of the deterministic gate (**verification scope limited**); a real-model leg, if ever taken, is an env-gated manual item (verify:native posture).

## Command

```
cd plugin/dsh
npm run verify:e2e            # = node scripts/e2e-demo.mjs   (--keep preserves scratch + report)
```

## Observed behavior (three consecutive green runs, identical shape)

```
[e2e] mdcontrol.status (onboarding) terminal: {"exitCode":0,"status":"completed","error":null}
[e2e] stub model served 3 onboarding request(s): ONBOARDING-CHECK → ONBOARDING-DRAFT_PLANS → ONBOARDING-DRAFT_PLANS
[e2e] [demo] normalized run-state diff: EMPTY (dual-leg shape identity)
[e2e] [onboarding] normalized run-state diff: EMPTY (dual-leg shape identity)
[e2e] descriptor rows: 2 (labels: Mission: demo, Mission: onboarding)
[e2e] monitor cli-e2e-mission-driver: stepLogs=8 (oc-: 8), /logs/CHECK=200, node-detail logTail ok=true
[e2e] monitor native-e2e-mission-driver: stepLogs=8 (native-: 8), /logs/CHECK=200, node-detail logTail ok=true
[e2e] monitor cli-onboarding-mission-driver: stepLogs=6 (oc-: 6), /logs/CHECK=200, node-detail logTail ok=true
[e2e] monitor native-onboarding-mission-driver: stepLogs=6 (native-: 6), /logs/CHECK=200, node-detail logTail ok=true
[e2e] SUMMARY: PASS — demo dual-leg green … + onboarding dual-form parity … + descriptor rows healthy … + monitor render green …
```

Per-item assertions (all machine-checked in `scripts/e2e-demo.mjs`, report at `<scratch>/e2e-report.json`):

1. **Onboarding dual-form parity (P3 gate first sentence)** — both legs exit 0 / `completed`; CLI leg spawns the real standalone engine on the onboarding mission (no correction retry — happy bounded path); native leg `mdcontrol.run` → started → terminal `completed`/exit 0 through the real cordis host; stub served exactly the 3-turn script; normalized run-state diff EMPTY; every AI step's marker valid for the real flow's transitions (CHECK `pass`, REVIEW_PLANS/EXEC_PLANS auto `all_complete`, DRAFT_PLANS `nothing` ×2); audit-gate short-circuit observed in the engine log (`auditRound=1/3 … clean short-circuit`).
2. **Descriptor health (Phase 2, child-session-events plane)** — the persisted native-leg session logs (`<scratch>/dsh-sessions/<cwd-ns>/<sessionId>/session.jsonl`) each contain ONE `subagent/descriptor` event: `{"version":2,"mode":"continuable","provider":"mdcontrol","label":"Mission: demo"|"Mission: onboarding","agentProvider":"deepseek-official","agentModel":<mission model>}` at seq 0 with header `seedLength:1` / `origin:"subagent"`. Host list plane not asserted (parent-scoped `listChildren` is unreachable for parentless mdcontrol children — plan Decision Record, not a defect).
3. **Monitor render (Phase 1 fix machine-pinned + Phase 3 render check)** — an in-process engine monitor (`startMonitor`) serves all FOUR runs: `GET /api/runs/:id` stepLogs non-empty with the right label per leg (8 demo / 6 onboarding entries; `oc-` CLI vs `native-` native), `/logs/CHECK` 200, node-detail `/api/runs/:id/nodes/CHECK` `logFile` + non-empty `logTail` — the exact three endpoints the WI10 note recorded as broken for native runs (`docs/bugs/2026-08-23-monitor-native-log-naming.md`, now fixed).
4. **Demo contract zero regression** — WI10 assertions unchanged and green (async contract, 4-request correction script, marker parse, shape identity).

## Expected behavior

Onboarding runs identically through both dispatch forms (normalized shape identity + valid markers), each native run registers a healthy durable descriptor, and the monitor step-log surface serves both naming labels — **Met** (P3 gate first-half evidence: onboarding dual-form consistency + healthy descriptor rows during runs).

## Follow-up required

- None in scope. Real-model onboarding doc-quality verification remains a human/env-gated item outside the deterministic gate by design (see verification scope above).

---

# 2026-08-23 — M3-WI12: mdcontrol.draft/analyze routes + Mission Control skills e2e (plan `2026-08-23-1852-2` Phase 4)

## Scope

Extended `verify:e2e` (same single command, same scratch/stub architecture) with three WI12 legs inside the native boot, after the WI10/WI11 legs:

1. **Skills registration face** — `test/fixtures/e2e.cordis.yml` agent-spine row now runs `skills: enabled: true` (agent-spine mounts SkillRegistry + SkillFileSystem + toolSkill from its own dependency tree — base-bundle form; NO new fixture rows, NO new direct devDeps: the skill packages are transitive dependencies of the already-pinned `@deepseek-ai/dsh-agent-spine-demo`, so the exact-pin discipline trigger does not fire). The mdcontrol service registers the three mission-control skills through reactive `ctx.inject(['skills'], …)`. Gate: poll `ctx.skills.list()` until all three names are present (registration lands via the inject fiber, possibly shortly after boot settles). `DSH_HOME` points at the scratch root — hermetic skill discovery, no host-user skills leak into the gate.
2. **`mdcontrol.analyze` (synchronous route, Decision 3(a) seam)** — explicit-runId leg (target = the native demo run) + latest-run leg (target = native onboarding run, deterministically the most-recent `run-state.json` by mtime). Stub answers the postmortem turn (routed on the real `run-postmortem.md` opener "Reliability Engineer") with the `<POSTMORTEM_FILE>`/`<MEMORY_UPDATED>` return tags. Gate: tags parsed verbatim into the route result + target resolution + exactly one dispatch per call (`WI12-ANALYZE ×2` in the stub log).
3. **`mdcontrol.draft` (async route, pre-authorized `cmdDraftMission` executor seam)** — route returns immediately (`{jobId: 'draft-…-mission-draft', status:'started'}`); stub answers the brief turn (gate `pass` + `<BRIEF_FILE>` tag) then the draft turn (`<MISSION_FILE>` tag pointing at a pre-created `missions/e2e-generated-mission.json` — the stub cannot write files; the tag-parse + missionsDir-scan mechanism is what is gated). Gate: `draft-state.json` reaches terminal `completed`/`completed` with `briefGate:'pass'`, `briefPath`, and `missionName:'e2e-generated-mission'` (tag target read via the newest-with-roadmapPath fallback scan); stub sequence `WI12-BRIEF → WI12-DRAFT`.

## Findings during hardening (both fixed in this change)

- The skills-enabled composition appends a durable user-role `<system-reminder>` skill-catalog message AFTER the step prompt, so the stub's last-user-message selection matched the catalog, not the step (stub 500 "no policy matched"). Fix: `lastNonReminderUserTextOfChatBody` (walk user messages backwards, skip `<system-reminder>` bodies).
- The draft/analyze executor configs initially carried no model → every child turn failed "has no provider/model" (the known M2-WI10 real-host finding, now hit again on the new seams). Fix: `baseAgentConfigOf` light `missions/base.json` read (model/agent passthrough into the NativeExecutor config; no env side effects — the draft pipeline's resolveConfig remains the authoritative resolution inside the task).

## Command

```
cd plugin/dsh
npm run verify:e2e            # same single entry; --keep preserves scratch + report
```

## Observed behavior (three consecutive green runs)

```
[e2e] ctx.skills.list() carries all three mission-control skills (mission-control-analyze, mission-control-draft, mission-control-run)
[e2e] mdcontrol.analyze (explicit native-e2e-mission-driver) → postmortemFile=docs/postmortems/e2e-postmortem.md memoryUpdated=no
[e2e] mdcontrol.analyze (latest) → targetRunId=native-onboarding-mission-driver
[e2e] mdcontrol.draft resolved in <500ms → { jobId: 'draft-…-mission-draft', status: 'started', … }
[e2e] draft-state terminal: {"status":"completed","phase":"completed","briefGate":"pass","briefPath":"docs/backlog/e2e-generated-brief.md","missionName":"e2e-generated-mission", …}
[e2e] stub model served 4 WI12 request(s): WI12-ANALYZE → WI12-ANALYZE → WI12-BRIEF → WI12-DRAFT
[e2e] descriptor rows: 5 (labels: Mission: demo, Mission: onboarding, Mission: analyze-run-…, Mission: analyze-run-…, Mission: draft-…-mission-draft)
[e2e] SUMMARY: PASS — … + WI12 legs green (3 skills in ctx.skills.list(), analyze explicit+latest tag-parse verbatim, draft async two-stage terminal completed with briefGate/missionName parsed)
```

WI10/WI11 assertions unchanged and green (dual-leg shape identity ×2, markers parsed, correction-retry once, bounded onboarding script, monitor render over all four runs).

## Expected behavior

The three mission-control skills are present on the session registration face, `mdcontrol.analyze` resolves the requested/latest run and returns the postmortem tags verbatim, and `mdcontrol.draft` runs the two-stage pipeline asynchronously through the native executor seam to a terminal `completed` draft-state — **Met** (mechanism plane).

## Follow-up required

- None in scope. True-model natural-language skill invocation ("run the onboarding mission" through a live session) is an env/manual leg outside the deterministic gate by design (plan Non-Goal; `Deferred But Adjudicated` watch-only residual if never exercised). The real-host isolate-realm visibility of `ctx.skills` follows the existing agents/webServer posture (root-realm composition is the asserted plane).
