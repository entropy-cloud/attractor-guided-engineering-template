# Development Log - 2026-07-20

### 2026-07-20

- WI4 (mission-driver-step-audit) closed: DRAFT_PLANS 决策引擎化 (删 done 出口 + audit-gate + prompt 改造 + Phase 5 audit-type filter).
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1559-1-draft-plans-audit-gate.md` → `Plan Status: completed`.
  - `tools/mission-driver/flows/mission-driver.json` — `DRAFT_PLANS.transitions` now contains only `created` / `nothing`; `onMaxRetries` changed from `{ goto: "DEEP_AUDIT" }` to `{ done: "failed" }` (retry exhaustion = failure, not escape into audit); top-level `markerAliases` no longer contains the `"done": "complete"` alias (semantic-clarity cleanup per design §5.4; not load-bearing on onUnknown).
  - `tools/mission-driver/src/engine.js` — new method `_shouldCompleteOnAuditQuota(currentStep, marker, transition)` placed right after `_reconcileTerminal`. Guards: only engages when `flow.auditEntry` exists AND `transition.goto === auditEntry` AND `marker === "nothing"` (zero-intrusion on flows without audit concept). Body reuses the `_reconcileTerminal` calling pattern: `max > 0 && round >= max && activePlans().length === 0 && openAudits().length === 0`. Inserted the gate short-circuit at the top of the `transition.goto` branch in `run()` — emits `transition { via: "audit_gate" }` then returns `completed` without entering another DEEP_AUDIT round. Truth-table row 4 (openAudits non-empty) is preserved by the `oa.length === 0` clause — open issues can never be silently dropped.
  - `tools/mission-driver/prompts/draft-from-roadmap.md` — rewrote the result-marker section: removed the `done` branch entirely; only `nothing` / `created` remain. Added a new "Mission Completion Decision" section with the three required key phrases: "Do not decide whether the mission is complete", "`docs/audits/` may contain plan-level closure audit artifacts" / "plan-level closure audit", "the engine decides based on the audit round count". Final-marker contract pinned to `nothing` / `created` only.
  - `tools/mission-driver/src/flow-loader.js` (Phase 5 Decision: Option A) — new `AUDIT_TYPE_RE` regex + new helper `_isMissionLevelAudit(filePath, content)`. `_scanOpenAuditsList` now filters plan-level closure audits out of `openAudits()` so they cannot inflate the audit-gate's input. Classification rules: (1) `> Audit Type:` header wins if present — types containing `plan`/`closure` are plan-level, anything else is mission-level (forward-compatible with future mission-level types like `security`); (2) no header → filename fallback (`*closure-audit*` / `*plan-audit*` → plan-level; `*multi-audit*` / `*open-audit*` → mission-level); (3) no signal → mission-level by default (preserves backward compat for legacy audits; never silently drops an open audit). Both `_scanOpenAuditsList` and `_isMissionLevelAudit` are now exported for testability.
  - `tools/mission-driver/test/draft-plans-audit-gate.test.js` (new, 10 cases in 2 describe blocks) — WI4 truth-table rows 1-4 (Cases D / A / B / C) + zero-intrusion (Case E, flow without `auditEntry`) + legacy `done` marker (Case F, uses real `createMissionDriverFlow({ flowName: 'mission-driver' })` for structural smoke + simplified flow for behavioral check). Phase 5 block: header-based classification, filename fallback, end-to-end `_scanOpenAuditsList` with mixed audit types, end-to-end gate-fires-when-only-plan-level-remains.
  - `tools/mission-driver/test/prompt-markers.test.js` — updated the regression test from `["created", "nothing", "done"]` to `["created", "nothing"]` (and explicitly asserts `done` is NOT present), reflecting the new WI4 contract.
  - `tools/mission-driver/design/mission-driver-flow-design.md` (Phase 6 owner-doc sync) — added a 2026-07-20 WI4 entry to the Changes section; updated the Section 3 mermaid (added `DP -.->|nothing + audit-gate hits| DONE` edge with "engine audit-gate, not AI" label); rewrote the DRAFT_PLANS step description's exit list ("created → REVIEW_PLANS; nothing → DEEP_AUDIT OR audit-gate short-circuit to completed"); updated the Exit Mechanism paragraph.
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 467 pass / 0 fail (457 baseline + 10 new, all WI1/WI2/WI3 tests still green); `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` built; `pnpm --prefix tools/mission-driver run lint:prompts` OK.
  - Roadmap: `docs/backlog/mission-driver-step-audit-roadmap.md` WI4 → `done` (M3 milestone now fully done; top dynamic-status block + M3 milestone table both updated).
  - Phase 5 Decision pinned: **Option A** (implement the audit-type filter now) over Option B (defer). Rationale: `> Audit Type:` header is already required by both `prompts/multi-audit.md:9` and `prompts/open-audit.md:9`, providing a stable signal; `plan-execution`'s `closure-audit.md` edits plan files directly (does not normally land in `docs/audits/`), so the §5.4 "openAudits() pollution" concern is theoretical for the default convention but real for users who save non-trivial closure audits as separate files per `00-audit-execution-guide.md`. Helper-encapsulated logic keeps future audit types a one-line change.
  - Known flake (unrelated to WI4): `test/from-step.test.js` Case 3 (`--from-step NOPE`) occasionally times out at 15s when run in the full suite on a busy Windows box; passes deterministically when run alone. Already present at the WI3 closure baseline; not a WI4 regression.
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback).
- WI3 (mission-driver-step-audit) closed: `--from-step <STEP>` entry override.
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1147-3-from-step-entry.md` → `Plan Status: completed`.
  - `tools/mission-driver/src/main.js` — `--from-step` option registered on both `run` subcommand and the implicit main command (mirrors `--step` placement). Mutex check (`opts.step && opts.fromStep → exit 1`) lives at `cmdRunMission` entry before any side effect. Step existence + available-step list extended to the `--from-step` branch (reuses `getTopSteps()`). Post-resolve wiring: `config.entryStep = config.fromStep; config.singleStep = false` (explicit false forms the dual of WI2's `--step → singleStep: true`).
  - `tools/mission-driver/src/config.js` — `resolveConfig` now passes `entryStep` and `fromStep` through verbatim (previously dropped, which had silently broken `--step` at the CLI level too — the WI2 unit tests only exercised the engine path so the latent bug went unnoticed). This plumbing fix is what makes both WI2's `--step` and WI3's `--from-step` actually fire from the CLI.
  - `tools/mission-driver/src/main.js` help aftertext — `--from-step` example line + 选项说明 paragraph (Chinese) describing the dual semantics and mutex rule.
  - `tools/mission-driver/README.md` — Common flags line now lists `--from-step` with the mutex note.
  - Tests: `tools/mission-driver/test/from-step.test.js` (new, 4 describe / 5 it). Case 1: engine.run(fromStep) starts at fromStep, stepCount > 1, flow byte-identical (deep-equal JSON snapshot). Case 2: `--step` + `--from-step` → exit 1, no `[step 1]` log (engine never started). Case 3: `--from-step NOPE` → exit 1, error lists CHECK/EXEC_PLANS/DRAFT_PLANS/REVIEW_PLANS/DEEP_AUDIT. Case 4: singleStep flag duality — `--from-step` path yields `singleStep === false`, `--step` path yields `singleStep === true`.
  - Test invocation note: CLI-spawn cases use the MAIN command path (`node main.js <mission> --from-step ...`), not the `run` subcommand path. Reason: commander 15.0.0 has a quirk where options shared between a subcommand and the implicit main command get stripped from the subcommand. This is a pre-existing latent issue affecting `--dry-run`, `--no-monitor`, `--step` etc. at the `run` subcommand level — out of scope for WI3 to fix; documented in the test file header.
  - Live CLI spot-check (main command path): `--from-step DEEP_AUDIT --dry-run --max-total-steps 5` starts at DEEP_AUDIT, loops into DRAFT_PLANS → REVIEW_PLANS → ..., hits stepCount=5 → `max_total_steps` (loop continues, NOT capped at 1 → confirms `singleStep=false`). `--step CHECK --from-step DEEP_AUDIT` → exit 1 mutex error. `--from-step NOPE` → exit 1 with available-step list.
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 457 pass / 0 fail (452 baseline + 5 new); `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` built; `pnpm --prefix tools/mission-driver run lint:prompts` OK.
  - Roadmap: `docs/backlog/mission-driver-step-audit-roadmap.md` WI3 → `done` (M2 milestone now fully done).
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback).
- WI2 (mission-driver-step-audit) Phase 3: new `single-step.test.js` covering all four exit classes.
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1147-2-single-step-fix.md`.
  - `tools/mission-driver/test/single-step.test.js` (new) — 5 cases total: 4 single-step exits (transitions / onError / onUnknown / onMaxRetries) each asserting `stepCount === 1`, `status === "single_step_done"`, goto/retry target NOT reached, and flow byte-identical before/after (deep-equal on JSON snapshot); plus one regression case proving the same flow without `singleStep` still runs multi-step to normal completion.
  - Case 4 (onMaxRetries) note: the retry target re-execution is blocked by the cap because `totalSteps` is not rolled back (only `visitCounts` is, on transient retry). The onMaxRetries branch is therefore structurally unreachable in single-step mode — documented in the test comment per plan instruction "以引擎实际行为为准".
  - Phase 3 verification (full green): `pnpm --prefix tools/mission-driver test` → 451 pass / 0 fail (446 baseline + 5 new, `skip-steps`/`core`/`transitions` all still green); web typecheck/build clean; lint:prompts OK.
  - Roadmap: `docs/backlog/mission-driver-step-audit-roadmap.md` WI2 → `done`.
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback).
- WI2 (mission-driver-step-audit) Phase 2: engine `maxSteps` hard cap + `single_step_done` status.
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1147-2-single-step-fix.md`.
  - `tools/mission-driver/src/engine.js` `run()` (~:1302) — added `const maxSteps = cfg.singleStep ? 1 : Infinity;` next to `maxTotalSteps`. `Infinity` when not in single-step mode → zero behavioral change.
  - `tools/mission-driver/src/engine.js` `run()` loop (~:1346) — condition changed from `while (totalSteps < maxTotalSteps)` to `while (totalSteps < maxTotalSteps && totalSteps < maxSteps)`.
  - `tools/mission-driver/src/engine.js` `run()` loop-exit (~:1831) — new branch: `if (cfg.singleStep && totalSteps >= maxSteps) → single_step_done`, placed before the existing `max_total_steps` limit_hit emit/return.
  - Decision pinned: new dedicated status `single_step_done` (not reusing `completed`) so monitor/logs can distinguish single-step debug runs from real mission completion (design §4.3.1). Residual: `_finalizeWorkflow` will still mark the step record as `status:"failed"` because `finalStatus !== "completed"` — this is a known run-state precision quirk deferred to WI5.
  - Phase 2 verification: `pnpm --prefix tools/mission-driver test` → 446 pass / 0 fail (no regression in `--fast`/`--skip-steps`/core/transitions); web typecheck/build clean; lint:prompts OK.
- WI2 (mission-driver-step-audit) Phase 1: removed transition mutation, added `singleStep` flag + exitMap entry.
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1147-2-single-step-fix.md`.
  - `tools/mission-driver/src/main.js` (~:583-595) — deleted the `for (const [, t] of Object.entries(step.transitions || {}))` in-place mutation block; the `if (config.entryStep)` branch now keeps only the existence check + console.log, and sets `config.singleStep = true` for the engine to consume via `delegates.config`.
  - `tools/mission-driver/src/main.js` (~:615) — `exitMap` now includes `single_step_done: 0` (single-step completion maps to exit code 0, same as success).
  - Phase 1 verification: `pnpm --prefix tools/mission-driver test` → 446 pass / 0 fail; web typecheck/build clean; lint:prompts OK.
- WI1 (mission-driver-step-audit) closed: `auditRound` / `maxAuditRounds` persisted in run-state.json.
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1147-1-audit-count-persist.md` → `Plan Status: completed`.
  - Engine changes:
    - `tools/mission-driver/src/engine.js` `_initWorkflow` (~:313) — added `auditRound: 0` + `maxAuditRounds: this.flow.maxAuditRounds ?? 0` to the workflow object.
    - `tools/mission-driver/src/engine.js` `_wfOpen` (~:329) — increment guarded by `(this.delegates.config || {}).isSubflow !== true && name === (this.flow.auditEntry || this.flow.entry)`, applied before `_writeWorkflow` (crash-safe "round N in progress").
    - `tools/mission-driver/src/engine.js` `run()` maxAuditRounds gate (~:1344) — rewritten to read pre-increment `(this.workflow && this.workflow.auditRound) || 0` with `>=` per design §5.2 写法 2. Gate position unchanged (still before `totalSteps++` / `_wfOpen`), so the gate-stopped iteration leaves NO phantom step record, NO extra stepCount, NO extra step_started event.
  - Tests: `tools/mission-driver/test/audit-count.test.js` (new) — cases A/B/C/D.
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 446 pass / 0 fail; `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` built; `pnpm --prefix tools/mission-driver run lint:prompts` OK.
  - Decision pinned: 设计文档 §5.2 写法 2 (gate reads pre-increment value with `>=`, position before `_wfOpen`). Trace for maxAuditRounds=3: entries 1/2/3 run (auditRound bumps to 1/2/3 after `_wfOpen`); 4th entry gate sees 3 → completed, no bump. Final `auditRound === 3 === maxAuditRounds`.
  - Backward compat: legacy run-state without `auditRound` reads as 0 via `|| 0`; `_wfOpen` re-establishes the field on first audit entry via `(undefined||0)+1`.
  - Roadmap: `docs/backlog/mission-driver-step-audit-roadmap.md` WI1 → `done`.
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback).
- Next: WI4 (DRAFT_PLANS 引擎化决策) and WI5 (可观测性) now unblocked; WI2/WI3 independent ready.
