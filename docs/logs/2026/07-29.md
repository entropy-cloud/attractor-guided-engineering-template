# Development Log - 2026-07-29

### 2026-07-29 (mdr-fix-4: draft description supports directory/multi-file references)

- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-29-1620-1-draft-description-directory-support.md` Phase 1（单 Phase，prompt 措辞 + CLI help + 文档对齐），全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`，roadmap work item 4 → `done`。mission `2026-07-29-162002-mission-driver` 全部 4 work item 完成。
- Fix/Add（纯措辞对齐，零受保护区、零新依赖、零 delegate var → context-map drift gate 不触发）:
  - `prompts/mission-brief.md:9` — "Target file (optional)" → "Target file or directory (optional)"，显式说明 description 可引用任意路径（单文件/目录/多文件/抽象目标），`--target-file` 是可选输入辅助非必填约束。保留 `{{targetFile}}` 替换 + read-when-non-empty 行为 + `:27,:40-41` 的 `<BRIEF_GATE>`/`<BRIEF_GATE_REASON>`/`<BRIEF_FILE>` marker；无 literal `docs/backlog/`（`draft-path-consistency.test.js:324-331` 守卫保留）。
  - `prompts/mission-draft.md` — brief-gate 段后新增 note：user request 可引用目录/多文件/抽象目标，`--target-file` 仅可选输入辅助。`<AI_STEP_RESULT>created</AI_STEP_RESULT>`/`<MISSION_FILE>` 输出契约未改；无 literal `docs/backlog/`。
  - `src/main.js:867` — `--target-file` help text 改为 "目标文件/目录路径（可选输入辅助，非必填约束；description 可引用任意路径）"。option name `--target-file <path>` 与 `args.targetFile` plumbing 不变，`<description>` 仍为唯一必填 arg。
  - Decision A（同 slice 对齐用户手册）: `user-manual.en.md`（`:104`,`:259`）+ `user-manual.zh.md`（`:309`,`:464`）`--target-file` 框架改为 "target file or directory / optional input aid"，各加一个 directory-based example（`draft "Read all requirement docs under docs/input/..."` / `draft "读取 docs/input/ 下所有需求文档，生成 roadmap"`）。
- Safety scope: 仅 plan 4 scope。未触碰 sibling plan（work item 1/2/3 已落地）。未改 flow JSON、未改 engine core、未加 delegate var、零新依赖、`web/dist` 无变更、`memory/_index.md`/`install-age.sh` 未触。`prompt-check.mjs` 确认不受影响（`mission-brief.md` 无 `AI_STEP_RESULT` tag；`mission-draft.md` 的 `created` marker 不绑定 flow step，marker-membership check 被跳过）。
- Owner-doc: user-manual en+zh 对齐 IS the owner-doc update（Decision A）。`CONTEXT.md` 不文档化 `draft` 的 `--target-file` surface，无需 edit。
- Closure: solo cold-replay（Reviewer-Availability Fallback，非受保护/非高风险 slice，无第二 reviewer 可用）。
- Verification（full green）: `pnpm --prefix tools/mission-driver test` → **597 pass / 0 fail** + `prompt-check: OK`；`node tools/mission-driver/src/main.js draft --help` 显示 reworded text + `<description>` 唯一必填；`web typecheck` + `web build` clean（`web/dist/` git status 空 = 字节一致，零 web 影响）；`lint:prompts` OK。



### 2026-07-29 (mdr-fix-3: CHECK configurable deterministic-state gate)

- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-29-1221-3-check-configurable-gate.md` Phase 1（Decision，已完成）+ Phase 2/3/4，全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`，roadmap work item 3 → `done`。
- Phase 1 Decision (B)（已记录）：保留 `fail → {done:"failed"}` 终态（OPT-4 未配置路径契约不破），新增独立 `needs_fix → {retry:"CHECK",maxRetries:2}` transition key 承载配置态自动修复重试——`needs_fix` 是已知 transition，由其自身 `maxRetries:2` + step 级 `onMaxRetries:{done:"failed"}` 共同 bound（同 `plan-execution.json` EXECUTE 的 retry 契约），无死循环漏洞。拒绝 (A) roadmap 字面 `fail→retry`（重开 OPT-4 repair death-loop）与 (C) engine 运行时 config 驱动 transition 选择（受保护区更大改动）。
- Phase 2（配置管线 + flow 契约，受保护区 ask-first 由独立 subagent draft review 满足）:
  - `missions/base.json:10` — `commands` 新增可选 `"check": ""`（空 = 未配置 → git-status 回退）。`mission-check.mjs:14` `REQUIRED_COMMANDS=["test"]` 未加 `check`（可选字段）。
  - `src/main.js:700` — `delegates.vars` 新增 `checkCmd: g.commands.check || ""`。drift gate 满足：`context-map.mjs:59`（VAR_PROVENANCE）+ `:90`（EXPECTED_VARS）均登记 `checkCmd`。
  - `flows/mission-driver.json:38-44` — CHECK 保持 `pass→REVIEW_PLANS`、`fail→{done:"failed"}`、`onMaxRetries/onError→{done:"failed"}`，新增 `needs_fix→{retry:"CHECK",maxRetries:2}`。未削弱任何 OPT-4 终态保证。
- Phase 3（prompt 重写）: `prompts/health-check.md` 由 "lightweight/never build/repair" 改为 "deterministic-state gate"——`{{checkCmd}}` 配置时运行它，可自动修复 emit `needs_fix`/不可修复 emit `fail`；未配置回退 git 冲突标记检测（clean/dirty→`pass`，未解决冲突→`fail`）；显式 "Do NOT run commands.test"。所有 `<AI_STEP_RESULT>` example marker（`pass`/`needs_fix`/`fail`）均为 CHECK transition key，`lint:prompts` OK（无需新 markerAlias）。
- Phase 4（Proof）: 新增 `tools/mission-driver/test/check-configurable.test.js`（4 用例）— flow shape（`needs_fix` retry 契约 + `fail`/`onMaxRetries` 仍终态，OPT-4 共存）；engine 行为（`needs_fix` 一次后 `pass` 在 maxRetries 内到达 REVIEW_PLANS，CHECK 调用 2 次；`needs_fix` 耗尽 maxRetries 经 `onMaxRetries` 终止为 failed，CHECK 调用恰 3 次 = 1+maxRetries:2，无第 4 次无死循环）。用真实 `createMissionDriverFlow` + `makeMockDelegates`。
- OPT-4 reconciliation 显式: 未配置 `fail` 路径仍终态（`check-lightweight.test.js` 全保留、零修改，`needs_fix` 纯增量）——无 silent deletion。
- Safety scope: 仅 plan 3 scope。未触碰 sibling plan（marker alias work item 1、promptsDir work item 2 已落地）。未加 npm 依赖。flow JSON 改动严格限定在 plan Decision (B)。
- Owner-doc: `CONTEXT.md` "Mission 配置系统" 新增 "CHECK 为可配置确定性状态门" 段（commands.check + needs_fix retry 语义 + OPT-4 兼容 + context-map drift gate 登记）。
- Verification（full green）: `pnpm --prefix tools/mission-driver test` → **597 pass / 0 fail**（+4 新用例）+ `prompt-check: OK`；`web typecheck` + `web build` OK（dist 无变更 = clean）；`lint:prompts` OK；`node src/mission-check.mjs missions/mission-driver-actionable-fixes.json .` exit=0 valid。



### 2026-07-29 (mdr-fix-2: Per-mission promptsDir config support)

- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-29-1221-2-per-mission-promptsdir.md` Phase 1（单 Phase，config 管线 4 文件），全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`，roadmap work item 2 → `done`。
- Fix: 新增可选 mission 字段 `promptsDir`，使每个 mission 可独立覆盖整套 prompt。解析优先级链（高→低）：`mission.promptsDir` → `missions/prompts/`（共享覆盖）→ 内置 `TOOL_ROOT/prompts/`。
- 改动（config 管线，零依赖不变式保持）:
  - `src/config.js:585-587` — 解析 `missionPromptsDir = mission.promptsDir ? resolve(projectRoot, mission.promptsDir) : ""`，加入返回 config（line 665）。镜像既有 `missionsDir`/`auditsDir` 平铺 config-field 模式（Decision A）。
  - `src/main.js:674-677` — `projectPromptDirs` 由原单一硬编码 dir 改为 `[config.missionPromptsDir, resolve(config.missionsDir, "prompts")].filter(Boolean)`，传 `createMissionDriverFlow`。
  - `src/flow-loader.js:311-321` — `loadSubFlow` 读 `this?.config?.missionPromptsDir` 构建同链 `[missionPromptsDir, missionsDir ? resolve(missionsDir,"prompts") : ""].filter(Boolean)`，保留 falsy-missionsDir 守卫。
  - `src/mission-check.mjs:79` — `promptsDir` 加入 existence-checked 列表（typo fail-fast，同 `moduleDir`/`contextDir`）；未加入 `REQUIRED_FIELDS`（可选）。
- Proof: 新增 `tools/mission-driver/test/prompts-dir.test.js`（11 用例）— config.js missionPromptsDir 解析（设置→绝对路径/未设置→空串）；createMissionDriverFlow 三级链（mission 级 shadow 共享级、per-file fallback、built-in 兜底）；loadSubFlow 同链 + falsy-missionsDir 守卫；mission-check 拒绝坏路径/接受存在路径/可选字段向后兼容。
- Safety scope: 仅 plan scope — 未触碰 sibling plan（CHECK needs_fix/configurable check、draft description、approved alias 已由 work item 1 完成）。未改 flow JSON（受保护区）、未加 delegate var（context-map drift gate 不触发）、零新依赖。
- Owner-doc: `CONTEXT.md` "Mission 配置系统" 新增 `promptsDir` 字段 + 三级解析链说明。
- Verification（full green）: `pnpm --prefix tools/mission-driver test` → **593 pass / 0 fail** + `prompt-check: OK`；`web typecheck` + `web build` OK；`lint:prompts` OK；`node src/mission-check.mjs missions/mission-driver-actionable-fixes.json .` exit=0 valid。



- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-29-1221-1-review-approved-marker-alias.md` Phase 1（单 Phase），全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`，roadmap work item 1 → `done`。
- Fix: `flows/mission-driver.json:30` markerAliases 新增 `"approved": "all_complete"`。消除 REVIEW_PLANS 每个草稿 plan 因 per-item `approved` marker 命中不到 aggregate transition 而触发的最多 2N 次无效 parse-model correction 调用。
- Proof: 新增 `tools/mission-driver/test/forEach-marker-alias.test.js`（3 用例）— 断言别名映射；直接 `_executeForEach` 断言单 `approved` 项聚合成 `all_complete` 且 `runParseAgent` spy 零调用；真实 engine run 断言 REVIEW_PLANS → EXEC_PLANS（loadSubFlow('plan-execution') 观测到）且全 run 零 parse 调用。
- Safety scope: 别名经 `_tryAliasMarker` 仅在 `transitions[all_complete]` 存在时返回，CHECK/DRAFT_PLANS/DEEP_AUDIT 无此 transition → 对其它 step 惰性短路为 null；`plan-execution.json` 有独立 markerAliases + CLOSURE_AUDIT 直接 `approved` transition（direct-match-before-alias 优先）→ 无回归。
- Verification（full green）: `pnpm --prefix tools/mission-driver test` → **593 pass / 0 fail** + `prompt-check: OK`；`lint:prompts` OK；`web typecheck` + `web build` OK。
- 关键决策: 仅 plan scope — 未触碰 sibling plan 改动（CHECK `needs_fix` transition、config.js/main.js/flow-loader.js 等 promptsDir/checkCmd 工作）。CONTEXT.md "关键约束" 不枚举 markerAliases，故无需 owner-doc 更新。

### 2026-07-29 (实现 + closure audit)

- 实现 reaper 并行误杀修复 plan 三 Phase（`docs/plans/2026-07-29-0900-…-plan.md`），全部 Exit Criteria 达成，Phase 1/2/3 Status → `completed`。
- Phase 1: 新增 `src/active-run-registry.mjs`（零依赖，per-user 全局登记 `<homedir>/.mission-driver/active/<runId>-<driverPid>.json`）；`runner.js:166` 标记 → `[MISSION_DRIVER:<runId>]`（无 runDir 回退旧标记）；`engine.js` run() 入口注册（`runId && missionName` 守卫，非 subflow）；`main.js:777` finally 唯一注销点（best-effort/ENOENT 幂等）；`executor.js:351` heartbeat `touchActiveRun`；`EXECUTION-PRINCIPLE.md:181,182,312` 标记契约同步。
- Phase 2: `reap-orphans.mjs` `reapStartupOrphans` 签名加 `opts={ownRunId,registryDir}`；Phase 1 判定改为 run 维度——登记查 `isAliveAndOurs`（任一 entry alive 即 spare）+ `_parentIsAliveDriver` 保守回退（ppid 链找 main.js，无法证明死亡则 spare，R2）；`engine.js _warnOrphans` 传 `ownRunId`。
- Phase 3: 新增 `test/active-run-registry.test.js`（18 用例）+ `test/reap-orphans.test.js`（analysis §5 全 6 场景 + CLI 回归，driver cmdline mock 写实 missionName 出现/runId 不出现）；更新 `test/prompt-cmdline-limit.test.js` 断言为新标记格式。
- 关键决策: 登记文件名含 driverPid 防同秒 runId 碰撞；missionName=null（draft/analyze）不登记落到父进程回退，消除误杀；注销不散布到 engine 25 个 `_result` 返回点。
- Verification（full green）: `pnpm --prefix tools/mission-driver test` → **593 pass / 0 fail** + `prompt-check: OK`（基线 07-27 的 5 个 PRE-EXISTING 环境相关 fail 在当前环境已为 0，无新增回归）；`node src/reap-orphans.mjs --startup _tmp <PID>` exit=0。手动双 mission 并行实跑 = `verification scope limited`，以 reap-orphans.test.js 场景 2 注入快照为代理证据。
- 独立 closure audit（`ses_05316f5f…`）初判 `do-not-close`：唯一阻塞 = 本日志缺实现条目（rule 11 log 一致性）。已补本条目修复；audit 确认行为/退出标准/受保护区/零依赖不变式/grep 全部 PASS → 闭环。

### 2026-07-29

- 基于 analysis `docs/analysis/2026-07-29-0002-mission-driver-reaper-concurrent-runs.md` 创建并评审通过 reaper 并行误杀修复 plan：`docs/plans/2026-07-29-0900-mission-driver-reaper-concurrent-runs-plan.md`（Plan Status: `active`）。
- 独立 subagent draft review 经 2 轮达成共识（`accept`）：iter 1（`ses_0532d756…`）发现 3 blocker + 1 矛盾，iter 2（`ses_05324ce2…`）确认全部 resolved、无新矛盾。
  - B1: runId 仅秒级时间戳会碰撞（`config.js:649-657`）→ 登记文件名改为 `<runId>-<driverPid>.json` + 多 entry lookup。
  - B2: driver cmdline 不含 runId，`isAliveAndOurs` 身份匹配依赖 missionName；draft/analyze `missionName:null`（`config.js:500,534`）若登记会误杀 → 注册加 `runId && missionName` 守卫。
  - B3: `EXECUTION-PRINCIPLE.md:181,182,312` 记录 spawn 标记契约 → 纳入 Phase 1 doc target。
  - C1: 注销唯一落点 = `main.js:777` finally（不散布到 engine 25 个 `_result` 返回点）。
- Next step: 按 plan 三 Phase 实现（runner 标记 + registry 模块 + reaper 改写 + 测试）；触及受保护区 `engine.js`（ask-first），实现须走 plan + closure audit。
