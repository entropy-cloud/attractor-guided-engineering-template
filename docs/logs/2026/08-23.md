# Development Log - 2026-08-23

### 2026-08-23

- **BUILD_VERIFY（mission dsh-plugin commit-time step，plan 1852-1 closure-then-commit，full-green verification）**：四命令单轮全绿——`web run typecheck` exit 0 / `web run build` 绿 / `lint:prompts` OK / 全量 `pnpm --prefix tools/mission-driver test` **656/656**（含 prompt-check OK）；另加跑插件链 `npm --prefix plugin/dsh test` **76/76**（含 manifest + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import 五入口）+ `npm --prefix plugin/dsh run verify:e2e` **PASS**（demo 双腿 + onboarding 双腿归一化 diff 空 ×2 + descriptor rows 2〔`Mission: demo`/`Mission: onboarding`〕+ monitor 四 run stepLogs/`/logs/CHECK`/node-detail logTail 双 label 全绿）。红线复核：引擎 diff 限定 `monitor.js` + `test/monitor.test.js`、`plugin/dsh/package.json` 零 diff、引擎目录零 `@deepseek-ai`、`verify-age.sh`/`age-ci.yml` 零 diff。`web/src/` 零改动 → build 重生成的 dist 还原 HEAD（0 diff 不提交，同 1621-2 先例——committed dist 的多代陈旧产物为 mission-driver 项目既有状态，CI dist 新鲜度门禁 path-scoped 至 `web/**` 不受本 change-set 影响）。
- **dsh-plugin M3-WI11 executed（plan 聚合条目，Phase 1-4 全过）**：onboarding 双形式对齐 + subagent 描述符注册 + monitor step-log 引擎侧修复收编——**P3 首片交付**。Plan: `docs/plans/dsh-plugin/2026-08-23-1852-1-onboarding-parity-subagent-descriptors.md`（Plan Status → `completed`）；roadmap M3-WI11 → `done`（M3 剩 WI12-WI13）。Phase 1/2/3 实现细节见下方三条 provisional 条目。
  - Phase 4（owner docs + 台账回写）：packaging doc——状态标头 P3 first slice delivered（WI11）、§Native Dispatch API Chain create 行增 `seed`/`meta.seedLength` + 描述符段落 as-built 重写（**步级措辞 → run 级 child**：mode continuable / provider `mdcontrol` / label `Mission: <mission>`；断言可达面 = 持久化 child session log，host 列表面 parent-scoped 对无父 child 不可达——enumeration-health 保障非缺陷；resume 免补写）、§Implementation state and boundaries log-viewing 行 **CLOSED by M3-WI11**、§Progress monitoring 通道 2 措辞对齐（run 级单描述符 + 步级进度走通道 1）、§Phased Delivery P3 行 partially delivered 注记（WI11 证据 + WI12/13 剩余）；design doc §User-visible capabilities 3 措辞对齐（run 级 + 步级进度经 run-state/monitor）；R3 §2 L4 onboarding 扩展注记；roadmap WI11 done + Last Updated；bug `docs/bugs/2026-08-23-monitor-native-log-naming.md` **closed**（三站点修复落点 + 验证证据 + **:929 第三站点超出原枚举的补注记**——draft review it2 发现）；1447-3 §Deferred "monitor 前端渲染深化" 台账 **reopen trigger 命中 → 收编闭环**。全量验证（收口复跑）：`./verify-age.sh` exit 0（L1 引擎 **656/656** + prompt-check OK、L2 插件 **76/76** + manifest + tsc + bundle 新鲜度 + smoke-import）；`verify:e2e` **四连跑全绿**（demo 双腿 + onboarding 双腿形状 diff 空 ×2 + descriptor rows 2 + monitor 四 run 三端点全绿）；`web typecheck` exit 0 / `lint:prompts` OK（web/src 零改动 → dist 还原 HEAD 0 diff，先例）。**verification scope limited: e2e 为 stub 域显式本地门禁（机制面断言——onboarding 文档质量语义不在确定性门禁，真模型腿 = env 门禁人工项）；monitor 渲染为 REST 渲染数据断言（无浏览器交互）**。
- [provisional, dsh-plugin plan 1852-1 Phase 3 / WI11] onboarding L4 双形式冒烟 diff + Phase 1 机器断言固化 + 描述符健康断言：`plugin/dsh/scripts/e2e-demo.mjs` 扩展（单命令入口 `verify:e2e` 保持，Phase 3 Decision 4 默认倾向采纳）——CLI 腿泛化（demo + onboarding 两 mission spawn）；native 腿单 boot 双 mission（demo → 50ms guard grace → onboarding，stub 请求按相位切片）；`e2e-policy.mjs` 增 onboarding 路由（真实 prompt 文本短语：`deterministic-state gate check` → pass、`Draft 1-3 plans from the remaining roadmap items` → nothing）。**Decision 四项**（录于 plan + `docs/testing/2026/08-23.md`）：① fixture 复刻最小安装形状（mission 文件运行时逐字复制自 `template/install/missions/`，install-age.sh 全量安装否决——拷贝正确性属安装器域）；② 有界剧本 = 空 plans/audits 骨架使 REVIEW_PLANS/EXEC_PLANS/DEEP_AUDIT 零 agent 步 + CHECK→pass + DRAFT_PLANS→nothing×2（第二次命中 audit-quota 干净短路，恰 3 stub 回合一轮循环）；③ 断言面 = 机制面（marker 对真实 flow transitions 合法 + 归一化形状 diff），CLI stub 不增强可写文件，**verification scope limited 显式标注**；④ 扩展现有脚本。**新增机器断言**：`assertMonitorRender`（进程内 `startMonitor`——引擎 monitor.js 跨目录 import，matrix-harness 先例；四 run × stepLogs 非空且 label 正确〔8/8/6/6 条〕+ `/logs/CHECK` 200 + node-detail `logFile`/`logTail` 非空——Phase 1 三站点修复的实证固化）；`assertDescriptorHealth`（扫 `dsh-sessions/<cwd-ns>/<sessionId>/session.jsonl`——首版漏 cwd-namespace 层级已修——断言每 native run 一条 `subagent/descriptor`：provider mdcontrol / mode continuable / version 2 / label `Mission: demo`+`Mission: onboarding`，agentModel 镜像 mission model）。验证：`verify:e2e` **三连跑全绿**（onboarding 双腿 completed/exit 0 + 归一化 diff 空 ×2 + stub 序列恰 `ONBOARDING-CHECK → ONBOARDING-DRAFT_PLANS ×2` + descriptor rows 2 + monitor 四 run 全绿）；插件链 76/76、引擎 656/656、web typecheck/build/lint:prompts 绿（dist 还原 HEAD 0 diff）。**verification scope limited: e2e 为 stub 域显式本地门禁（非 CI merge-blocking）；onboarding 文档质量语义面不在确定性门禁内（真模型腿 = env 门禁人工项）**。
- [provisional, dsh-plugin plan 1852-1 Phase 2 / WI11] subagent 描述符注册（Explore → Decision → Add）：**Explore**（host 源码只读）`packages/subagent/subagent/src/descriptor.ts`（`snapshotSubagentDescriptor`/`foldSubagentDescriptor`/`SUBAGENT_DESCRIPTOR_VERSION=2`；mode `'one-shot'|'continuable'`，continuable 必带 label；事件 model-hidden/log-only/survives compaction）+ `descriptor-seed.ts`（`seedDescriptorTurn` 真 Session staging 赋 seq + lossless-JSON）+ `list-children.ts`（列表面 parent-scoped + `origin==='subagent'`，身份权威 = projection fold，无 descriptor → `corrupt` 诊断；label 过滤是消费端行为）。**Decision**（录于 plan Phase 2 Decision Record）：注入点 = `agents.create` `seed` 数组 + `meta.seedLength`（sidebar :161-174 先例）；provider = `'mdcontrol'`；label = `Mission: <missionName>`（回退 runId basename）；mode = `'continuable'`；agentProvider/agentModel 镜像实际 create agentOptions（model 未解析同步省略）；resume 无需补写（durable seed 随 session log）；断言面首选 child session events（host 列表面 parent-scoped 对无 parentSession 的 mdcontrol child 不可达，非缺陷）。**Add**：`plugin/dsh/src/native-executor.ts` `_ensureHandle` 双 create 路径注入 seed（`snapshotSubagentDescriptor` + `seedDescriptorTurn` import 自已钉 `@deepseek-ai/dsh-subagent`，`dependencies` 零变化）。**Proof**：`fake-agents.mjs` create trace 增 `seedLength`；`native-executor.test.mjs` 首用例改 pinned options 形状断言（seedLength 一致 + `foldSubagentDescriptor` 回读 mode/provider/label/agentModel/agentProvider）+ 新增用例（label runId 回退 / model-less create 无 agentOptions 且 descriptor 同步省略 / watchdog dispose 后 fresh create 新 child 新 seed / calls trace seedLength `[1,1]`）+ 冷 handle 用例补 resume-不-re-seed 断言。验证：`node --test native-executor.test.mjs` **15/15**（13 存量 + 1 新增 + 断言扩展）；插件链 `npm --prefix plugin/dsh test` **76/76**（含 manifest 14 项 + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import）；`web typecheck`/`build`/`lint:prompts` 绿（web/src 零改动 → dist 还原 HEAD 0 diff，先例）。**verification scope limited: fake-agents 单元域；真实宿主域描述符断言归 Phase 3 e2e native 腿**。
- [provisional, dsh-plugin plan 1852-1 Phase 1 / WI11] monitor step-log 端点 `oc-` 前缀边界引擎侧修复（收编 Fix，bug `docs/bugs/2026-08-23-monitor-native-log-naming.md` + 1447-3 deferred reopen trigger 命中）：`tools/mission-driver/src/monitor.js` 三处站点加宽为共享 `<label>-<step>-<ts>-<rand>.log[.prompt]` 双 label（`oc-`/`native-`）形状——① `listStepLogs` 两 regex 合并为单 `STEP_LOG_NAME_RE`（`^(oc|native)-(.+)-(\d+)-([a-z0-9]+)\.log(\.prompt)?$`，`STEP_LOG_LABELS` + `stepLogPrefixes()` 共享常量）；② `handleGetLog` `?file=` 快路径门 `startsWith("oc-")` → `STEP_LOG_NAME_RE.test` + 前缀搜索 `oc-${step}-` → 双前缀（mtime newest-first 排序与 basename/charset 安全校验语义不变）；③ `handleGetNodeDetail`（bug 文档枚举遗漏的第三处，node-detail `logTail` 对 native run 恒 null 的根因）同型双前缀。测试：`test/monitor.test.js` 新增双命名形用例（mixed 目录 native- log/prompt 列出 + `/logs/:step` 200 + `?type=prompt` + `?file=` 绝对路径快路径 + node-detail `logFile/logTail` 非空 + oc- node-detail 零回归专例），存量 oc- 用例零改动零回归。验证：引擎全套 `pnpm --prefix tools/mission-driver test` **656/656**（+2 新用例）+ prompt-check OK；`./verify-age.sh` exit 0（L1 656/656 + L2 插件链 GREEN）；`npm --prefix plugin/dsh run verify:e2e` 复跑 PASS 后 `--keep` + monitor 子命令（`main.js monitor --dir <scratch> --monitor-port 9417`）REST 人工观测：native run `GET /api/runs/native-e2e-mission-driver` stepLogs **8 条**（4 log + 4 prompt 全 `native-` 前缀）、`/logs/CHECK` **200**（file `native-CHECK-…-ccl0im.log`）、`/api/runs/.../nodes/CHECK` **logTail 非空**（含收割文本）；CLI run 回归 stepLogs 8 条 + `/logs/CHECK` 200 不变。**verification scope limited: REST 渲染数据观测（无浏览器交互）；机器断言固化进 e2e 脚本归 Phase 3**。
- **BUILD_VERIFY（mission dsh-plugin commit-time step，plan 1621-2 closure-then-commit，full-green verification）**：四命令单轮全绿——`web run typecheck` / `web run build` / `lint:prompts` / 全量 `pnpm --prefix tools/mission-driver test` **654/654**；另加跑插件链 `npm --prefix plugin/dsh test` **75/75**（58 存量 + 17 mdcontrol-routes 新增，含 manifest + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import 五入口）。`web/src/` 零改动 → build 重生成的 dist 还原 HEAD（0 diff 不提交，同 1621-1 先例——committed dist 的 echarts 残留为 mission-driver 项目既有陈旧产物，不属本 mission scope）；引擎目录 `git status` 全净（零引擎 diff 红线保持）。

- **dsh-plugin M2-WI10 executed（plan 聚合条目，Phase 1-3 全过 + 文档收口）**：mdcontrol.run 异步作业契约 + 插件层 active-run 守卫 + L4 native 双腿 e2e 全量落地——**P2 里程碑收口**。Plan: `docs/plans/dsh-plugin/2026-08-23-1621-2-mdcontrol-run-async-contract-e2e.md`；roadmap M2-WI10 → `done`（M2 全 done，M3 剩 WI11-WI13）。Phase 1/2 实现细节见下方两条 provisional 条目。
  - Phase 3（文档收口 + roadmap 回写）：packaging doc——状态标头 **P2 DELIVERED**（前置显式核对：WI9 done + Phase 2 证据齐）、§Service Surface 全段 as-built（路由范围/暴露面双面〔cordis Service 发布 + `/mdcontrol/api` dispatcher〕/异步契约/runId 词汇表/守卫/回执/draft-analyze → M3）、§Dependency and Version Risk 五调用 → **六调用**（+`get`，`AgentRegistry.get` 锚点引用——宿主源码核实义务兑现）、§Native Dispatch API Chain 两行 e2e 发现注记（inject 语义 + `agentOptions {provider,model}` 双官方先例）、§Implementation state and boundaries model 行更新 + **log-viewing 措辞修正**（monitor step-log 端点 `oc-` 前缀专用，bug 立案）、§Packaging Layout as-built 树 + 五新文件、§Version pins +17th devDep、§Phased Delivery P2 行 ✅ + 证据指针；design doc §Running as-built 注记；R3 §2 L4 行 + §5 L4 门禁双注记；roadmap WI10 done + M3 WI12 完成前置注记（draft review it2 条件项②兑现）+ Last Updated（P2 收口）；1447-1 §Deferred guard 台账回写"已收编"（实现落点 + 原文引用位置 + 单测钉住，闭环）；survey Addendum 2（`dsh-app-boot` devDep）。全量验证（双腿结果数字 + 门禁）：引擎 **654/654**（一次 653/654 为 doc-line-refs 抓 packaging doc `index.ts:583` 行号引用——即时改锚点引用复跑全绿，非引擎回归）；插件链 **75/75**（58 存量 + 17 新增）+ manifest + tsc + bundle 新鲜度 + smoke-import；`./verify-age.sh` exit 0（L1+L2 GREEN）；e2e `verify:e2e` **四连跑全绿**（含收口复跑——归一化 diff 空/markers parsed 双腿全 AI 步/破坏-恢复恰一次/stub 恰 4 请求）；`web typecheck`/`build`/`lint:prompts` 绿（web/src 零改动 → dist 还原 HEAD 0 diff，先例）；引擎目录 + web/dist `git status` 全净（零引擎 diff 红线保持）。**verification scope limited: e2e 为 env 门禁本地跑（scripted stub 模型域，非 CI merge-blocking）；monitor 渲染为 REST 渲染数据 + HTML 服务核对（无浏览器交互）；真实模型凭据腿不适用于本门禁（确定性 marker 序列要求，by-design stub-only）**。
- [provisional, dsh-plugin plan 1621-2 Phase 2 / WI10] L4 双腿 e2e demo mission 跑通 + P2 gate 证据：**Decision (c) 进程内真宿主**——`@deepseek-ai/dsh-app-boot`（新增 exact `0.1.1-rc.2` devDep）`boot()` + 自有 fixture `test/fixtures/e2e.cordis.yml`（16 行 = L3 组合 − sdk-server + 真实 mdcontrol service 行〔相对名 `../../src/service.ts`，非 isolate，`ctx.get('mdcontrol')` 根 realm 可达〕）；(a) 交互宿主不可复现、(b) sdk 协议面 sessions-only 无法调路由，均否决。`scripts/e2e-demo.mjs`（+ 共享策略 `scripts/e2e-policy.mjs`）：CLI 腿 = 真实引擎子进程 + PATH 前置 `opencode` stub（WI3 白名单钉名，prompt 末位 argv）；native 腿 = 路由直调——`mdcontrol.run` ~4ms 返回 started → `mdcontrol.status` 轮询至 completed/exit 0 → `mdcontrol.list` 双 run 在列；stub 模型恰好 4 请求（CHECK → REVIEW 破坏 → correction → DONE）；**归一化 run-state 双腿 diff 空**；**markers parsed 显式断言**（双腿每 AI 步 marker ∈ transitions）；**correction-retry 一次人为触发**（REVIEW 首答 `banana`，correction re-prompt 恰 1 次，恢复 completed）——双腿各自观察面（stub 请求日志 / 引擎日志行）。门禁固化 `npm --prefix plugin/dsh run verify:e2e`（显式本地、keyless/stub-only by design、不接 CI），三连跑全绿，记录 `docs/testing/2026/08-23.md`。Phase 2 途中两项真宿主发现（插件层修复）：① cordis 未声明 inject 的 service 属性读抛错 → `resolveAgentsService()`（`ctx.get` 优先）三消费点统一；② agents turn 无 provider/model 报错 → NativeExecutor create 增补 `agentOptions {provider, model}`（sdk-server + dsh-headless 双官方先例；parseModel 区分仍忽略）。**monitor 渲染人工核对发现**（记录未修，引擎侧超出零引擎 diff 红线）：monitor step-log 端点 `oc-` 前缀专用（monitor.js `listStepLogs`/`handleGetLog`），native 工件不进 log 面板且 `/logs/:step` 404 → `docs/bugs/2026-08-23-monitor-native-log-naming.md` 立案 + WI7 "preserving monitor log viewing" 措辞由 Phase 3 修正；run-state 文件格式同一性（R3 §3 组 6）不受影响。验证：插件链 **75/75** + `./verify-age.sh` exit 0（L1+L2 GREEN）+ e2e ×3 exit 0；引擎目录零 diff。**verification scope limited: e2e 为 env 门禁本地跑（scripted stub 模型域），非 CI merge-blocking；monitor 渲染为 REST 渲染数据 + HTML 服务核对（无浏览器交互）**。

- [provisional, dsh-plugin plan 1621-2 Phase 1 / WI10] mdcontrol 路由层 + 异步 run 契约 + 守卫落地（单测域）：`plugin/dsh/src/mdcontrol-routes.ts`（新）——wire-method record 工厂 `createMdControlRoutes({ctx,logger})`（`mdcontrol.run`/`mdcontrol.status`/`mdcontrol.list`，better-sidebar `buildApi` + `/sidebar/api` dispatcher 先例形态：自有 HTTP dispatcher `registerMdControlHttpDispatcher()` 挂 `/mdcontrol/api/<method>` prefix，`ctx.get('webServer')` 解析、缺失 = mount-log 降级；`MdControlError` wire code 信封 bad-request/not-found/run-in-progress/internal）+ `ActiveRunGuard`（1447-1 裁定收编：单 run per resolved projectRoot、宿主侧 in-service 注册表、占用期并发 = 显式 wire error、成功/引擎失败/任务崩溃/启动异常四路径清册、跨 root 独立，裁定原文引用于头注）。`engine-bridge.ts` 新增 `beginNativeMission`（detached start 原语：bootstrap + executor 选择先于任务 promise（校验失败/agents 缺失 = plain throw → wire error 且守卫未占用）；`orchestrateRun` 不 await 悬挂为 in-host 任务；promise 永不 reject（错误捕获 `{exitCode:1,error}`）；finally 单点 dispose 与 `runNativeMission` 同型零新增位点）。`service.ts` 骨架转实装（cordis Service 发布 `super(ctx,'mdcontrol')`——`ctx.get('mdcontrol')` 可达，AgentRegistry 同型；头注台账收窄 "run/status/list — M2-WI10；draft/analyze → M3〔WI12 前置〕"）。回执 Decision：采纳 `AgentRegistry.get` 为第六宿主调用（宿主源码只读核实 `packages/core/agent/src/index.ts:583`，bare Agent、live-only、undefined 语义；packaging doc 五调用清单增补归 Phase 3 条件项）；实现 = opt-in `followup:{sessionId}` → 终态一行纯文本经 `get`→`followup`；session 不 live = warn-only 跳过。runId Decision：basename(runDir) = run-state runId = monitor 词汇表；status = run-state 透传无第二状态机。新增 `test/mdcontrol-routes.test.mjs` 17 用例全绿（非阻塞机器断言 <150ms 返回 / 守卫双跑+三异常路径清册+跨 root / session 解耦 / 回执 opt-in 双侧 / status 三态 / list 五形态 / dispose 恰一次 / HTTP dispatcher 六分支 / payload 校验）。验证：`npm --prefix plugin/dsh test` **75/75**（58 存量 + 17 新增，含 manifest/tsc/bundle 新鲜度/smoke-import）；`./verify-age.sh` exit 0（L1+L2 GREEN）；runtime 冒烟 = apply() 于真实 `new Context()` 发布成功；引擎目录零 diff。

- **BUILD_VERIFY（mission dsh-plugin commit-time step，plan 1621-1 closure-then-commit，full-green verification）**：四命令单轮全绿——`web run typecheck` / `web run build` / `lint:prompts` / 全量 `pnpm --prefix tools/mission-driver test` **654/654**；另加跑插件链 `npm --prefix plugin/dsh test` **58/58**（46 存量 + 12 传输单测）+ 聚合门禁 `./verify-age.sh` 整链 exit 0（L1 654/654 + prompt-check OK、L2 58/58 + manifest + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import 五入口）+ `verify:native` 无开关 skip exit 0 实测。`web/src/` 零改动 → build 重生成的 dist hash 漂移还原 HEAD（0 diff 不提交，先例）；引擎目录 `git status` 全净。
- **dsh-plugin M2-WI9 executed（plan 聚合条目，Phase 1-3 全过）**：L3 宿主集成骨架 host-harness.mjs——R3 §6 收口 + 四场景 harness + env 门禁落地。Plan: `docs/plans/dsh-plugin/2026-08-23-1621-1-l3-host-harness-sdk-server.md`（Plan Status → `completed`）；roadmap M2-WI9 → `done`（M2 里程碑仅剩 WI10）。Phase 1/2/3 实现细节见下方三条 provisional 条目。
  - Phase 3（verify:native 门禁 + 活体绿跑 + 文档收口）：`plugin/dsh/scripts/verify-native.mjs`——`DSH_VERIFY_NATIVE` 缺开关 = skip 提示 exit 0 / 开关开 + 缺 `DEEPSEEK_API_KEY` = fail-fast exit 1（三态实测）；`verify:native:keyless` 伴随门禁（显式调用免开关，严格 env 门禁属凭据路径）；**不接** `verify-age.sh`/`age-ci.yml`（无 env 实测 exit 0，L1+L2 GREEN）。活体绿跑 = keyless 双连跑 **4/4 全绿**（真实 spawn demo bin + fixture），记录 `docs/testing/2026/08-23.md`（命令/环境/摘要/门禁语义/1447-3 deferred 处置 = 零分歧维持闭合）；真实凭据腿命令已接线、本机无凭据留待人工 env（testing note Follow-up，非降级）。文档收口：R3 §2 实现落点注记 + §5 门禁落地引用 + **§4 双注记**（run-state 断言归属 L4/WI10；收割面勘误——`subagent.finished` 仅模型委派触发，root prompt 门禁面 = 最后提交 assistant 文本 + `turn/end`，先例 1447-3 seam 勘误）+ §6 resolved（Phase 1）；packaging doc 状态标头/as-built 树/Version pins L3 devDeps 引用/P2 行 WI9 标注；roadmap WI9 `ready → done` + Last Updated。全量验证：`./verify-age.sh` exit 0（L1 **654/654** + prompt-check、L2 **58/58**〔46 存量 + 12 传输单测〕+ manifest + tsc + bundle 新鲜度 + smoke-import）；keyless 门禁 ×2 exit 0；`web run typecheck`/`build`/`lint:prompts` 绿（web/src 零改动 → dist 还原 HEAD 0 diff，先例）。**verification scope limited: L3 活体域为 env 门禁本地跑（keyless stub endpoint 域 + 凭据腿 env 已接线待人工），非 CI merge-blocking**。
- [provisional, dsh-plugin plan 1621-1 Phase 2 / WI9] L3 harness 实现 + 纯逻辑单测：`plugin/dsh/scripts/host-harness.mjs`——`HarnessLineRpcTransport`（NDJSON 行缓冲半包重组 / id 顺序配对 / 通知历史+实时双路 / RpcError wire code 保留 / EOF 统一拒绝 pending / malformed 行忽略计数 / dead-capability 入站 request 记录不误配）+ 会话驱动（initialize 断言 serverInfo → session/prompt 断言 `{messageId}` 回执 → `runTurn`：等该 session 下一个 `turn/end` 切片收割 root-session `assistant/message` 最后提交文本——勘误后收割面 → shutdown）+ 四场景 runner（marker-roundtrip / silent-idle-arrival / session-continuity〔流身份 + nonce-echo 双层门禁〕/ shutdown-hygiene〔exit 0 + ps 双 needle 孤儿扫描 + stdout 纯度〕，每场景独立 assert + 可读 diff）+ `--dry`（组合参数打印不 spawn）+ `--keyless`（本地 stub OpenAI 兼容 SSE endpoint，官方 `keyless-smoke.e2e.ts` 先例——stub 从请求体回显 marker/nonce，零凭据零外网）；`initialize.maxTokens = 2048` 钉值。新增 `plugin/dsh/test/host-harness-transport.test.mjs` 12 用例（fake PassThrough 流域）。**keyless 活体四场景 4/4 PASS**（真实 spawn demo bin + fixture 组合，turn/end `{kind:'completed'}`，`subagent.finished` 条件性观察 count=0 符合勘误预期）；验证：插件 **58/58**、引擎 **654/654** 零回归、`./verify-age.sh` exit 0、web typecheck/build/lint:prompts 绿（dist 还原 HEAD 0 diff）。**verification scope limited: keyless stub endpoint 域**——凭据腿归 Phase 3（见聚合条目）。

- [provisional, dsh-plugin plan 1621-1 Phase 1 / WI9] L3 宿主启动组合收口（R3 §6 unresolved → resolved）：**Decision 1a**（plugin/dsh 自持）——启动命令 = demo bin `dsh-jsonrpc-agent`（`@deepseek-ai/dsh-sdk-jsonrpc-demo@0.1.1-rc.2` 发布版纯 ESM `lib/bin.js`，config 经 argv[2]）；config 供给 = 自有 fixture `plugin/dsh/test/fixtures/harness.cordis.yml`（16 行组合，基座 = 官方 repo 根 `examples/jsonrpc-agent/cordis.yml` 非交互非 PTY 组合；**PTY 裁定** = minimal 样例的 `dsh-terminal`/persistent-bash/danger-full-access 行显式缺席——piped-stdio spawn 无 POSIX 终端；`maxTokensAsSuccess: false` 钉值；persona/model catalog/sessions compression:none 三处有意 delta 均注记于 fixture 头）；bare 名解析 = fixture 位于 `plugin/dsh/test/fixtures/` → Node 走访 `plugin/dsh/node_modules`；凭据 env-only（`DEEPSEEK_API_KEY`/`DEEPSEEK_BASE_URL`，不落盘）；备选 (b) 本地 clone / (c) npx / (d) headless 一次性 stdout 收割（保留为降级 fallback，无通知流与 §4 承诺冲突故未用）均记录裁定。**Decision 2**：传输层手写薄 NDJSON（`HarnessLineRpcTransport` 命名区别官方 `JsonRpcLineTransport`，协议面 3 方法 + 4 通知），`dsh-sdk-protocol`/`dsh-sdk-client` 不引入。devDeps 16 包 exact `0.1.1-rc.2`（cohort 一致；`latest` tag 停在 0.0.1-rc.x 属 tag 滞后 → 钉 literal version 不钉 tag）——survey addendum 落 `docs/analysis/2026-08-23-0001-p2-version-survey.md`；shipped `dependencies` 零变化。**活体 boot 证据（keyless，本地 stub SSE endpoint，官方 `keyless-smoke.e2e.ts` 先例）**：完整帧流——initialize → `deepseek-harness-sdk-runtime`、session/prompt → `{messageId}`、`session.event`（`assistant/message` + `turn/end {kind:'completed'}`）、`session.status` running→idle、shutdown `{}` → exit 0。R3 §6 resolved 注记回写。验证：`npm --prefix plugin/dsh test` 46/46 exit 0（fixture/devDeps 零回归）；`pnpm --prefix tools/mission-driver test` 654/654 零引擎 diff。

- **BUILD_VERIFY（mission dsh-plugin commit-time step，plan 1447-3 closure-then-commit，full-green verification）**：四命令单轮全绿——`web run typecheck` / `web run build` / `lint:prompts` / 全量 `pnpm --prefix tools/mission-driver test` **654/654**（无 flaky 复发）；另加跑 `./verify-age.sh` 聚合门禁整链 exit 0（L1 654/654 + prompt-check OK、L2 插件 **46/46**〔矩阵 22 + WI6/WI7 24〕+ manifest 14 项 + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import 五入口）。`web/src/` 零改动 → build 重生成的 dist hash 漂移还原 HEAD（0 diff 不提交，同 WI1-WI3 先例）；引擎目录 `git status` 全净、`plugin/dsh/package.json` 零 diff。
- **dsh-plugin M2-WI8 executed（plan 聚合条目，Phase 1-3 全过 + closure）**：L2 契约测试——双后端行为矩阵落地并接 CI merge-blocking 门禁。Plan: `docs/plans/dsh-plugin/2026-08-23-1447-3-l2-backend-parity-matrix.md`（Plan Status → `completed`，Closure Gates 9/9）；roadmap M2-WI8 → `done`（M2 里程碑 WI9-WI10 未动）。Phase 1/2 实现细节见下方 provisional 条目。
  - Phase 3（CI 接线 + 文档收口）：CI merge-blocking = **备选 A 聚合形态**——根级 `verify-age.sh`（exec bit；L1 引擎链 + L2 插件链〔含矩阵〕，devDeps 按需 `npm ci`）+ `.github/workflows/age-ci.yml`（push/PR 路径限定 `tools/mission-driver/**`+`plugin/dsh/**`+门禁自身，Node 24，跑 `./verify-age.sh`）——L1+L2 全绿才过门（实测 exit 0：654/654 + 46/46）；R3 §2 补两条注记（实现落点引用 + **seam 勘误**：`__setRunnerFactoryForTest` 为 draft 管线 seam〔orchestrator.js:302〕，mission run 真注入点 = `orchestrateRun({config, executor})`，矩阵 ProcessExecutor 腿 = `new ProcessExecutor(fake runner)`）；packaging doc——状态标头/§Packaging Layout as-built 树/§Phased Delivery P2 行 +WI8、§Execution Backend Seam 三条契约保全规则逐条标注"由 L2 矩阵钉住（test 文件 + 门禁 + 台账指针）"并附各组矩阵映射、§Implementation state and boundaries tool 漂移行补 `tool-timeout-drift` 场景落点；baseline 显式核对 = **No owner-doc update required**（引擎目录零 diff、零 standalone 行为变化；矩阵以 live import 消费 baseline 所载引擎事实，漂移即红）。全量验证（双后端双腿结果数字）：`./verify-age.sh` 单轮 exit 0——L1 引擎 **654/654** + prompt-check OK、L2 插件 **46/46**（矩阵 22 + WI6/WI7 24）+ manifest 14 项 + `tsc --noEmit` + bundle 新鲜度 36 文件 + smoke-import 五入口；矩阵单文件 ×5 连跑零 flake + 审计会话加跑 ×2 绿；`web run typecheck`/`build`/`lint:prompts` 绿（web/src 零改动 → dist 还原 HEAD 0 diff）；引擎目录 `git status` 全净、`plugin/dsh/package.json` 零 diff（钉版无新增）。**verification scope limited: L2 mock/scripted 域——真 spawn 仅 tool 步短命令（D1 钉住域），driver 真 spawn 与真宿主归 L3（WI9）、native 端到端归 L4（WI10）**。
  - Closure audit（independent，fresh-session subagent 冷回放，session `ses_fd250f2f1ffetLa0TfabYTFMve`）：**PASS**——交付物与契约保真逐项核验（双腿机制 live 核验、六组覆盖、台账 D1/D2/D3 引用、`tool-timeout-drift` 断言方向 = owner doc 漂移方向）；审计会话独立冷跑全绿（verify-age.sh exit 0 / typecheck / lint:prompts / 矩阵 ×2）；红线三项复核干净；非阻塞观察 5 条（未提交属 commit 时序、waiter 表示为行为等价实现细节等）不影响收口。证据录于 plan §Closure Audit Evidence。
- [provisional, dsh-plugin plan 1447-3 Phase 1+2 / WI8] L2 双后端行为矩阵落地：共享参数化 spec 对 ProcessExecutor（scripted fake runner 注入，tool 步转真 `createRunner().runTool`）与 NativeExecutor（fake in-process agents service）各跑一遍，R3 §3 六组断言全绿。交付物：`plugin/dsh/test/helpers/matrix-harness.mjs`（共享 harness——FlowEngine 直驱 + mkdtemp runDir + `config.onStepUpdate` 逐字镜像 `orchestrator.js:644` 生产接线点（executor 构造后赋值）+ 双腿统一 `embed:true`（M1-WI4 startup 诊断为 executor 无关引擎行为，hermetic 化不引入后端变量）；扁平 per-turn 抽象剧本词汇 `{text|transient|hard|timeout|spawnFail}`，引擎调用序确定性保证两腿同槽消费；run-state 归一化比较器——字段集 + 稳定值 + 值豁免字段仅 presence/type）+ `plugin/dsh/test/backend-parity-matrix.test.mjs`（22 用例：组 1/2 marker 分类与重试预算（含 correction re-prompt 次数 = `onUnknownMaxRetries` 钉死、transient 429 分类/恢复/降级）、组 3/6 run-state 形状 + 产物存在性（含 subflow placeholder、无 running 残留、tool 步真 spawn 产物）、组 4/5 EXIT_MAP 查表（8/10 键端到端，余为 executor 无关路径归 `exit-map.test.js` 互补）与三预算触发）+ `plugin/dsh/test/helpers/fake-agents.mjs` 纯增量矩阵化（`{rejectIdle}` 剧本项 → whenIdle 拒绝 → native errorTail → 引擎 `stderrTail||errorTail` 等价分类；`state.calls` 全序 trace；1447-2 单测 24/24 零回归）。**分歧台账 3 条全有 owner-doc 背书**：D1 tool timeout 漂移（process 丢弃 `timeout` 60min 默认 vs native ms 消费——场景 `tool-timeout-drift` 显式钉住双腿各自绝对结果，背书 packaging doc §Implementation state and boundaries，1447-2 Decision 3 残险闭环）；D2 sessionId 值语义（R3 §3 断言 3 豁免）；D3 产物/诊断内容形状 + pre-dispatch 失败 promptFile presence 不对称（packaging doc 存在性契约）。CI 接线（Phase 1 Decision 1 备选 A 定稿）：根级 `verify-age.sh` 聚合门禁（L1 `pnpm --prefix tools/mission-driver test` + L2 `npm --prefix plugin/dsh test`，插件 devDeps 按需 `npm ci`，纯 Node 零网络）+ `.github/workflows/age-ci.yml`（push/PR 路径限定，跑 `./verify-age.sh` merge-blocking）。验证：`./verify-age.sh` exit 0（引擎 654/654 + prompt-check、插件 46/46〔24 WI6/WI7 + 22 矩阵〕+ manifest + tsc + bundle 新鲜度 + smoke-import）；矩阵单文件 ×5 连跑零 flake；`web run typecheck`/`build`/`lint:prompts` 绿（web/src 零改动 → dist 还原 HEAD 0 diff）；引擎目录零 diff、引擎测试链零插件 import（边界双向保持）。**verification scope limited: L2 mock/scripted 域——真 spawn 仅 tool 步短命令，driver 真 spawn 与真宿主归 L3（WI9）/L4（WI10）**。

- **BUILD_VERIFY + draft jobId 同毫秒碰撞根治（mission dsh-plugin 验证 step，plan 1447-2 closure-then-commit，full-green verification）**：四命令全绿——`web run typecheck` / `web run build` / `lint:prompts` / 全量 `pnpm --prefix tools/mission-driver test` **654/654**。首跑 653/654 唯一失败为历史 flaky `monitor.test.js` "GET /api/missions/draft lists recent draft jobs (default 9)"——本次根因定位并根治：**真实根因是 `startDraftJob` 的 ms 精度 jobId 同毫秒碰撞**（两个 POST 落同一毫秒 → jobId 相同 → 第二个 jobDir 覆盖第一个 → list 只见 1 条；此前日志归因 "mtime 排序时序" 不准确）。修复：`src/draft-job.mjs` startDraftJob 加 `existsSync` 碰撞守卫循环（随机 4 位 base36 后缀重试至目录空闲，仅非固定 draftJobDir 路径；同步执行无 check-then-mkdir 交错窗口）+ `test/draft-job.test.js` 冻结时钟确定性回归用例（同毫秒双提交 → 两个独立 jobDir + list 2 条 + 首个 state 未被覆盖）。引擎侧最小修，超出 plan 1447-2 范围（其预期引擎零 diff）→ 独立 `fix(mission-driver)` commit。`web/src/` 零改动 → dist 还原 HEAD（0 diff 不提交，同 WI1-WI3 先例）。
- **dsh-plugin M2-WI7 executed（plan 聚合条目，Phase 1-3 全过 + closure）**：NativeExecutor 原生派发后端 + engine-bridge 选择工厂落地。Plan: `docs/plans/dsh-plugin/2026-08-23-1447-2-native-executor-dispatch-factory.md`（Plan Status → `completed`，Closure Gates 9/9）；roadmap M2-WI7 → `done`（M2 里程碑 WI8-WI10 未动）。Phase 1/2 实现与验证细节见下方两条 provisional 条目。
  - Phase 3（文档收口 + 全量验证）：`docs/architecture/dsh-plugin-packaging.md` 状态标头 P2 部分交付范围更新（+M2-WI7）、§Packaging Layout as-built 树刷新（native-executor.ts / engine-bridge.ts 转 LANDED，test/ 补两个测试文件 + helpers/fake-agents.mjs，tsconfig 注 allowJs）、§Execution Backend Seam 选择工厂句转已落地（per-run 构造 + 不静默回退）、§Native Dispatch 后新增 "Implementation state and boundaries (M2-WI7, landed)" 小节（model+parseModel 双 gap / 日志内容形状不等价边界 / executeTool 插件层最小 spawn 决策与漂移残险〔1447-3 钉住〕/ 不静默回退原则 / 调用期回调契约 / stderrTail 恒 null）、§Behavioral differences Model selection 行扩展显式覆盖 `mission.model` 与 `parseModel`、§Phased Delivery P2 行 WI7 交付标注；roadmap WI7 回写 + Last Updated 刷新；baseline 显式核对结论 = **No owner-doc update required**（native 相关行均为引擎侧事实且仍准确，零 standalone 行为变化）。全量验证：引擎 653/653 + `lint:prompts` 绿（一次 652/653 为既有文档化 flaky `monitor.test.js` draft-listing mtime 时序，单文件 84/84 + 全量复跑均绿，零回归）；插件 `npm test` 全链绿（24 用例 + manifest + tsc + 新鲜度 + smoke-import）；`web run typecheck`/`build` 绿且 `web/src/` 零改动 → dist 还原 HEAD 0 diff（同先例）。**verification scope limited: fake-agents 单元域，无真实宿主（L3 归 WI9）/无 native 端到端（demo mission 归 WI10）/无 L4**。
  - Closure audit（independent，fresh-session subagent 冷回放）：**PASS**——交付物与契约保真逐项核验（8 字段结果 shape 对 `step-executor.js:22`、调用期回调解析逐字镜像 `runner.js:204-206`、tool spawn 零诊断——sys-snapshot/active-run-registry 仅存在于否决理由注释、红线三项：引擎零 diff / 引擎 src 零 `@deepseek-ai` / web/dist 零改动）；审计会话独立复跑全绿（插件 24/24、引擎 653/653、driver-whitelist 18/18）。证据录于 plan §Closure Audit Evidence。
- [provisional, dsh-plugin plan 1447-2 Phase 2 / WI7] engine-bridge 选择工厂 + native config 接线：`plugin/dsh/src/engine-bridge.ts` 由接口占位转实现——`resolveExecutor({ driver, ctx, config })`（M1 plan 1 deferred 项收编：`native` → `createNativeExecutor(ctx, config)` per-run 构造（两次调用两个实例，禁跨 run 单例），`ctx.agents` 缺失抛显式 wire error 不静默回退 ProcessExecutor；`opencode|pi|cline` → `new ProcessExecutor(await createRunner(config))` 复用 bundle 内 runner）；`bootstrapNativeConfig(projectRoot, args)`（engine bootstrap〔loadDotenv → resolveConfig〕+ `dir` 兜底 + `driver: "native"` 默认 + `allowNativeDriver: true` 放行 + `embed: true` M1-WI4 门控）；`runNativeMission({ ctx, projectRoot, args })`（bootstrap → 工厂 → bundle `orchestrateRun`〔其内部 `config.onStepUpdate = engine._onAgentStepUpdate` 接线先于首步——与 NativeExecutor 调用期回调解析配套〕→ finally `executor.dispose?.()` run 终态释放（正常/失败均一次））。`tsconfig.json` 加 `allowJs: true`（引擎 bundle JS 以推断类型进入 typecheck，checkJs 关——JS 本体零检查）。新增 `test/engine-bridge.test.mjs` 6 用例全绿：工厂三 driver → ProcessExecutor / native → per-run 双实例 / agents 缺失 wire error（ctx 空与 agents:null 两形态）/ bootstrap config 形状（driver native + embed true + runDir 创建 + flowName 解析 + driver 覆写）/ **orchestrateRun 全链冒烟（native 腿 + fake agents）**——run-state.json `steps[]` 的 PING 记录带 live `logFile/promptFile/sessionId`（`_wfClose` 只在 `_onAgentStepUpdate` 收到实时更新时保留这些字段 → 钉死构造期死通道类回归）、marker `pass` → `completed` → exit 0、followup prompt 带 `[MISSION_DRIVER:smoke-run]` 前缀、单 handle + 终态单次 dispose、run-dir 产物在盘；失败腿（marker fail → exit 1 + failed + 仍 dispose）。验证：插件 `npm test` 全链绿（24 用例 + manifest + tsc + bundle 新鲜度 36 文件 + smoke-import）；引擎全量 `pnpm --prefix tools/mission-driver test` 653/653 绿（首跑 652/653——唯一失败为既有文档化 flaky `monitor.test.js` draft-listing mtime 时序，单文件复跑 84/84 绿 + 全量复跑绿，与 WI6 日志先例一致，零回归）；`driver-whitelist.test.js` 复跑 18/18（CLI native 拒绝行为零变化）；引擎目录 `git diff` 为空 + `grep @deepseek-ai tools/mission-driver/src/` 零命中（红线保持）。

- [provisional, dsh-plugin plan 1447-2 Phase 1 / WI7] NativeExecutor 落地：`plugin/dsh/src/native-executor.ts` 由接口占位转完整实现（`DshNativeExecutor` 三方法 + `createNativeExecutor` 工厂）。dispatch 链 = `agents.create({sessionId: 生成的 childId, meta {cwd, origin:'subagent', delegationDepth, agentPreset}, signal: opts.timeoutMs 换算})` → `followup(createUserMessage(...))`（`[MISSION_DRIVER:<runId>]` 边界前缀）→ `await whenIdle()` → 从 `agent.session.events` 逆序收割最后一条非 interrupted 非空 `assistant/message` text；exit 合成 = 完成回合有收割 → `code 0`，abort/error/空收割 → `code 1 + errorTail`，`stderrTail` 恒 null（Decision 2）。回调契约逐字镜像 runner.js：`opts.onStepUpdate ?? config.onStepUpdate` **调用期解析**（`typeof === "function"` 守卫形式；构造期捕获在 orchestrator.js:644 接线时序下必为 undefined 死通道），双点回调 files-先-session-后（Decision 1）。watchdog：`cancel(cause {kind:'hook'})` → 有限 grace（默认 10s，可注入）→ 不收敛才 `dispose()`，产出 `code 1 + [TIMEOUT] errorTail`。handle 生命周期 = per-run 持有步骤间复用、`dispose()` 双重释放防护、冷 handle 经 `agents.resume({resumeSessionId})` 恢复（create 失败/whenIdle 拒绝均走结构化失败结果，executor 永不 throw——ProcessExecutor 对齐）。`executeTool` = 插件层自有最小 spawn（Decision 3：零诊断——不 sysSnapshot / 不触 active-run registry；复用 executor.js 工具路径被否决，M1 plan 1 deferred 项 2 正式收口见 plan §Deferred）。测试基建 `test/helpers/fake-agents.mjs`（可复用 fake agents service，WI8/WI9 沿用：scripted 回合/never 挂起/cold 开关/onCancel converge|hang 可编程 + 全调用 state 记录）+ `test/native-executor.test.mjs` 14 用例全绿（正常回合产物与回调顺序 / 构造后接线回调仍到达〔钉死死通道〕/ opts 级优先 / 无 marker 透传 / cancel-converge 不 dispose / cancel-hang → dispose / 空收割 / create 失败 / 工厂 wire error / resume 恢复 / handle 生命周期复用+终态单次 dispose+dispose 后拒绝复活 / tool spawn 成功/失败 tail/超时）。`createUserMessage` import 自 `@deepseek-ai/dsh-agent`/`dsh-session` 的 peer `@deepseek-ai/dsh-llm`（同 0.1.1-rc.2 cohort，宿主必然供给，package.json 零新增依赖）。验证：`plugin/dsh npm test` 全链绿（manifest + 14+4 用例 + tsc + bundle 新鲜度 + smoke-import）；引擎全量 `pnpm --prefix tools/mission-driver test` 653/653 零回归（引擎目录零 diff）。**verification scope limited: fake-agents 单元域，无真实宿主（L3 归 WI9）/无 L4**。

- **BUILD_VERIFY（mission dsh-plugin commit-time step，plan 1447-1 closure-then-commit，full-green verification）**：四命令单轮全绿——`web run typecheck` / `web run build` / `lint:prompts` / 全量 `pnpm --prefix tools/mission-driver test` **653/653**（无 flaky 复发）；另复跑 `plugin/dsh` 本地测试链全绿（check-manifest 14 项 + `node --test` 4/4 + `tsc --noEmit` + `build-bundle.mjs --check` 新鲜度 36 文件 + smoke-import 五入口）。`web/src/` 零改动 → build 重生成的 dist hash 漂移还原 HEAD（0 diff 不提交，同 WI1-WI3 先例）；`plugin/dsh/node_modules` 由根 `.gitignore` 覆盖不入库。
- **dsh-plugin M2-WI6 executed（plan 聚合条目，Phase 1-3 全过）**：`plugin/dsh/` 脚手架 + bundle manifest + isolate realm patch + 构建打包闭包门禁全量落地。Plan: `docs/plans/dsh-plugin/2026-08-23-1447-1-plugin-shell-bundle-scaffold.md`；roadmap M2-WI6 → `done`（M2 里程碑 WI7-WI10 未动）。
  - Phase 1（钉版 + manifest/patch）：P2 版本复查 `npm view` 五包 dist-tags 与 R2 表逐行相同（零漂移）→ 按 R2 原建议 exact pin（cordis `4.0.1` + agent/goal/tools/subagent `0.1.1-rc.2`，goal/tools 先钉不装——单 cohort 一致性），记录 `docs/analysis/2026-08-23-0001-p2-version-survey.md`；`plugin/dsh/package.json`（`dsh.bundle.patch` 字段逐字匹配 R1 §5 形状；scripts.build/test 指向本 plan 脚本）；`cordis.patch.yml`（`- insert:` op〔better-sidebar bundle patch 先例〕→ `cordis:group` + `isolate: { missionControl: true }` entry-local realm〔anchored-standard preset 先例〕→ service row 自引用包名走 two-anchor 解析；纯 YAML 无 `!!js` 表达式）；`scripts/check-manifest.mjs` 结构校验 14 项全绿（manifest 逐字段 + patch YAML 解析〔dev-dep `yaml`，插件层 devDeps 不触引擎零依赖不变式〕+ 挂载键结构：group/isolate/service row）。
  - Phase 2（构建打包 + 闭包断言）：`scripts/build-bundle.mjs`（纯 Node 复制式——esbuild/rollup 与 symlink 备选均按 plan Decision 否决）：19 模块允许集显式列出，从五入口（orchestrator/config/engine/runner/step-executor）算静态 import 传递闭包 ⊆ 允许集；scanner 字符串/注释感知（orchestrator.js:424 注释里的 `from "Stage 1..."` 散文不能伪造 import）；禁 npm 包名/禁逃逸 src 根/禁 NOT-bundled 四类（monitor.js|draft-job.mjs|spawner.mjs|main.js）。产物 `assets/src/`（相对 import 原样复制 → `TOOL_ROOT=assets/` 下 config.js agentFile 与 flow-loader flows/prompts 解析语义与引擎逐字节一致）+ `flows/ prompts/ agents/` → 共 36 文件，**入 git 定稿提交**（web/dist 先例；`assets/` 命名不落 `.gitignore` `dist/` 规则）；`--check` 新鲜度门禁（重算 copy plan 与提交树内容级 diff，缺/旧/多文件均红）。**负例自证（一次性手动，四类全红后还原）**：(A) orchestrator.js 尾加 `import "./monitor.js"` → FAIL NOT-bundled；(B) 加 `import "commander"` → FAIL npm package；(C) 允许集删 `run-reconcile.mjs` → FAIL not in allowed list；(D) 篡改 `assets/prompts/execute.md` → --check FAIL outdated，重建后绿。引擎目录全程 `git diff --exit-code` 为空（零引擎改动）。`scripts/smoke-import.mjs` 无宿主冒烟：五入口从 `plugin/dsh/` 位置 import 成功、关键导出键齐全（orchestrator 5 键/config 2/engine 2/runner 1/step-executor 1）、零 npm 解析。
  - Phase 3（骨架 + 收口）：`src/service.ts` 最小骨架（apply(ctx,config) 挂载即一条结构化日志 `mdcontrol` logger；`MdControlRoutes` 空 占位——不注册 `mdcontrol.*`、不触 active-run guard，WI10 范围纪律）；`src/native-executor.ts` 接口占位（三能力方法签名与 step-executor.js JSDoc 逐参数镜像 + R1 §1 派发链注释，实现归 WI7）；`src/engine-bridge.ts` 接口占位（bootstrap/orchestrateRun/orchestrateAnalyze 包裹面 + `allowNativeDriver` 注记）；`tsconfig.json`（NodeNext/strict/noEmit）。**插件本地测试入口建立**（后继 1447-2/-3 沿用）：`npm test` = check-manifest → `node --test test/*.test.mjs`（`test/bundle-scaffold.test.mjs` 4 用例：manifest 逐字形状/patch isolate-realm 结构/assets-src ⊆ 允许集精确相等/TOOL_ROOT 资产目录存在）→ `tsc --noEmit` → `build-bundle.mjs --check` → smoke-import，全链绿。文档收口：packaging doc 状态标头 P1 → P2 PARTIALLY DELIVERED（WI6）、§Packaging Layout 蓝图改 as-built 树 + Build bundling/Version pins 两小节（闭包门禁/新鲜度/钉版结论落档）、§Phased Delivery P2 行标注部分交付边界（WI6 done / WI7-10 未动）；roadmap WI6 回写 + Last Updated 刷新；active-run guard 再裁定已在 plan §Deferred But Adjudicated 记录（M1 plan 3 的 "M2-WI6/WI10" 收窄为 WI10 单一归属）。
  - Verification（plan Closure gate 域）：全量 `pnpm --prefix tools/mission-driver test` 653/653 绿（一次中间复跑 652/653——唯一失败为既有文档化 flaky `monitor.test.js` "GET /api/missions/draft lists recent draft jobs"〔mtime 排序时序，08-23 既有日志 git-stash 基线法证实 HEAD 同样复现；本 plan 引擎零 diff，单文件连跑同样间歇复现〕，复跑即绿，零回归）；`web run typecheck` / `lint:prompts` 绿；`web run build` 绿但产物 hash 随本机依赖漂移且 `web/src/` 零改动 → `web/dist/` 还原 HEAD（0 diff 不提交，同 WI1-WI3 先例）。**verification scope limited: 无真实宿主**——本 plan 验证域 = 结构级校验 + 无宿主 import 冒烟 + 引擎全量零回归；真实 DSH 宿主挂载冒烟归 WI9（L3，本地脚本门禁不阻塞 CI）。
- **BUILD_VERIFY + 预存平台用例修复（mission dsh-plugin 验证 step，full-green verification）**：`web run typecheck` / `web run build` / `lint:prompts` 绿（`web/src/` 零改动，dist hash 本机依赖漂移还原 HEAD 0 diff，同 WI1/WI2/WI3 先例）；全量 `pnpm --prefix tools/mission-driver test` **653/653 全绿**——修复两个自 HEAD（57d315f，git worktree 复现证实）预存失败的 Windows 路径平台用例：(1) `engine.js` 持久化 `logFile`/`promptFile`/`subflowRuns.file` 的取 basename 改 `basenameXp`（`path.win32.basename`，`/` 与 `\` 双分隔符全平台可切；run-state 可移植性是 `engine.js:1810` 区既有注记的本意）——POSIX `basename` 不切 `\`，Windows 路径整串入库；(2) `mission-check-cli.test.js` Case D 首用例改平台分支断言（win32 钉 `file:///C:/...` 精确形，POSIX 钉 `file:///` 三斜杠前缀，同文件第二用例既有惯例）——POSIX 上 `pathToFileURL` 对反斜杠路径 cwd-resolve，原 "every platform" 精确等式不可满足。CLI 冒烟 `node src/main.js demo --step CHECK --dry-run --no-monitor` exit 0 / marker pass。
- **dsh-plugin M1-WI3/WI4/WI5 executed（plan-3 聚合条目，覆盖 Phase 1-3 + P1 门禁）**：driver 白名单 + embed 门控 + P1 文档收口全量落地。Plan: `docs/plans/dsh-plugin/2026-08-23-1300-3-driver-whitelist-embed-gating-p1-doc-sync.md`；roadmap M1-WI3/WI4/WI5 → `done`（M1 五 WI 全 done，里程碑无遗留）。Phase 1/2 代码与验证细节见下方两条 provisional 条目（`config.js` `SUPPORTED_DRIVERS`/`assertSupportedDriver`、`engine.js` `_diag` 缝 + embed 门控、`executor.js` 设计注记、`test/driver-whitelist.test.js` 18 用例、`test/embed-gating.test.js` 4 用例）。
  - Phase 3 文档收口：`mission-driver-baseline.md` §Driver selection 增 native（host-only + CLI 拒绝）+ resolve-time 校验 + `embed` 标志说明（指向 packaging doc），§Public CLI Surface run 行 `--driver` 同步，§Public Exports 新增 `src/config.js` 条目（`SUPPORTED_DRIVERS` 单源约束），§Public Exports vs Test Seams 登记 `delegates.diagnosticHooks` 注入缝（test-only 规则同 `__` 前缀）；`module-boundaries.md` §Test Seams 补三个新测试文件（step-executor / driver-whitelist / embed-gating）+ 用例数刷新 653（引擎核心行复核无需变更——plan 1 已修订到位）；`dsh-plugin-packaging.md` 状态标头 PLANNED → P1 DELIVERED（P2-P4 仍 planned）、§Execution Backend Seam 接口草图加"以三能力方法落地"锚注 + 两处 P1 hardening 段改落地时态、§Scope 边界修订段改已履行时态、:43 链路句按现实标注（secret-resolver dormant、实际链 loadDotenv 先于 resolveConfig；`secret-resolver.mjs` → `.js` 笔误修正）、`runtime-types.ts:87` 行号引用改锚点式（预存 doc-line-refs 失败用例转绿）；roadmap M1-WI3/4/5 回写。
  - P1 门禁复核（packaging doc gate 原文逐字执行）：全量 `pnpm --prefix tools/mission-driver test` 653/651/2——失败集 = 预存集（2 个 Windows 路径平台用例 + `monitor.test.js` draft-listing flaky〔git-stash 基线法证实 HEAD 同样复现〕），doc-line-refs 经本 plan 修复转绿，零回归；`node --test test/exit-map.test.js` 13/13（EXIT_MAP 钉住）；CLI 行为不变冒烟 `node src/main.js demo --step CHECK --dry-run --no-monitor` exit 0 / `single_step_done` / marker pass。真实模型 `run demo` 未跑——**verification scope limited: dry-run 域**（M1 验证域为纯本地引擎套件 + dry-run，同 plan 1/2 先例；native 执行域属 M2 WI8-WI10）。
- [provisional, dsh-plugin plan-3 Phase 2 / WI4] embed 门控落地：`engine.js` `run()` 内构造 `this._diag` 分发表（缺省 = 真实调用形态：`registerActiveRun` 裸函数 / `sysMon` 接 label 的 `_sysMon` 包装 / `warnOrphans` 包装；`delegates.diagnosticHooks` 可整体覆盖——测试可观测缝非公共 API）；startup 诊断分支 `cfg.isSubflow !== true` 再套 `cfg.embed !== true`——embed 时三者（active-run 注册 / START sys-snapshot / orphan reaping）整体跳过，默认关闭行为零变化；`executor.js` 心跳级诊断处补设计注记（ProcessExecutor 路径独有，native 不选中故不门控）。新增 `test/embed-gating.test.js`（4 用例：(a) embed 三 spy 零调用且 flow 照常完成；(b) 默认三 spy 实调用形状 + 真实缺省路径落盘补充证据——sandbox HOME 的 registry 文件 + runDir sys-snapshot.log START 行，HOME 隔离经"先设 env 再动态 import engine"规避 `ACTIVE_RUNS_DIR` import 时常量陷阱；(c) embed+isSubflow 与现状一致均跳过）。全量 test 653/650/3——失败集 = 预存集（2 Windows + `runtime-types.ts:87`；`monitor.test.js` draft-listing flaky 经 git-stash 基线法证实在 HEAD 同样复现，非本变更引入）；typecheck / lint:prompts / web build 绿（dist 还原 HEAD，同先例）。
- [provisional, dsh-plugin plan-3 Phase 1 / WI3] driver 白名单落地：`config.js` 导出 `SUPPORTED_DRIVERS = ["opencode","pi","cline","native"]`，`assertSupportedDriver` 在 resolveConfig 三个返回点（主/draft/analyze）resolvedDriver 定稿后校验——未知值抛英文错误（含合法值清单 + 实际值 + 来源提示 flag/env/mission/base），`native` 无 `args.allowNativeDriver: true` 抛专属中英混合文案；CLI 层零新增校验（抛错直通 stderr + exit 1，与既有 resolve 期错误同路径，实测确认）；`main.js` 两处 `--driver` help 文案与 env help 同步 native 限定。新增 `test/driver-whitelist.test.js`（18 用例：三返回点 × 来源矩阵 / native 门禁 ±allowNativeDriver / 合法值逐字段回归 / CLI 级 exit 1 断言）。全量 test 649/646/3——失败集 = 预存集（2 Windows 平台用例 + `runtime-types.ts:87` 行号引用〔Phase 3 收口〕），零回归；typecheck / lint:prompts / web build 绿（dist hash 本机漂移还原 HEAD，前端零改动，同 WI1/WI2 先例）。
- WI2 BUILD_VERIFY 复验（mission dsh-plugin 独立验证 step，先于 commit）：`web run typecheck` / `web run build` / `lint:prompts` 绿；全量 `pnpm --prefix tools/mission-driver test` 631/627/4——失败集与日志记录的预存集逐条相同（2 个 Windows 路径平台用例 + `dsh-plugin-packaging.md → runtime-types.ts:87` WI5 收口项 + `monitor.test.js` draft-listing flaky，单跑复验该用例绿），零回归。proactive 检查：`test/context-map.test.js` 19/19 绿（EXPECTED_VARS/VAR_PROVENANCE 与 orchestrator.js 同步）。`web run build` 产物 hash 与 worktree 预存漂移版一致（确定性重建）但 `web/src/` 零改动 → `web/dist/` 还原 HEAD（0 diff 不提交，同 WI1/WI2 先例）。
- dsh-plugin M1-WI2 executed: 程序化编排入口 + EXIT_MAP 提升落地。Plan: `docs/plans/dsh-plugin/2026-08-23-1300-2-programmatic-entry-exitmap-hoist.md`；roadmap WI2 → `done`。
- Code (Phase 1): 新增 `tools/mission-driver/src/exit-map.js`（EXIT_MAP 值逐行不变，零依赖纯数据模块）；`main.js` 改 import + 兼容 re-export；`test/exit-map.test.js` 改 import 新家，断言零修改。
- Code (Phase 2): 新增 `tools/mission-driver/src/orchestrator.js`（656 行）——`bootstrap({projectRoot,args})`（loadDotenv → resolveConfig；secret-resolver 链 dormant 不接线）、`orchestrateRun({config,executor})`（flow 创建 + delegates.vars 逐字段迁移含 `(不存在)` 兜底与 selfMemory 排除 + singleStep/entryOverride 处理 + FlowEngine 驱动 + EXIT_MAP 映射；早退路径返回 `{exitCode:1}` 无 status，壳层跳过 banner 保行为）、`cmdDraftMission` 原名原签名整体迁入（内部自解析 config）、`parseDraftArtifact`/`extractBriefGate` 随迁、`validateDraftDesc` 定义自 draft-job.mjs 迁入（draft-job.mjs 改 `export … from "./orchestrator.js"` 维持自身导出，monitor.js 引用链零改动、无环）、`__setRunnerFactoryForTest` 缝随迁、`orchestrateAnalyze({config})`（自建/关闭 runner 包装 runPostmortem）；另导出 CLI 壳共用小助手 `getTopSteps`/`resolveProjectRoot`/`resolveMissionsDir`（非程序化入口面，baseline 已标注）。`main.js` 薄壳化（968 → ~460 行）：run/analyze 命令体 = bootstrap + orchestrate* + process 生命周期 + banner；draft 直调 cmdDraftMission；五个 draft 系导出经 `export … from "./orchestrator.js"` re-export（同模块实例，缝状态单一性保持），EXIT_MAP 保持 from `./exit-map.js`（不进 from-orchestrator 列表，无双源）。VAR_PROVENANCE 漂移门随迁：`src/context-map.mjs` main.js 引用全量清扫改指 orchestrator.js（头注释 / VAR_PROVENANCE docstring / EXPECTED_VARS 行锚 / 提取器更名 `extractVarsKeysFromOrchestrator` + `@param` 契约），`test/context-map.test.js` `MAIN_JS` → `ORCHESTRATOR_JS`。
- Verification (all green, baseline-comparison method):
  - 全量 `pnpm --prefix tools/mission-driver test`：基线（HEAD 941c155）631 tests / 627 pass / 4 fail（预存失败集：2 个 Windows 路径平台用例 + `dsh-plugin-packaging.md → runtime-types.ts:87` 行号引用 + `monitor.test.js` GET /api/missions/draft）；改后 631 / 628 / 3——失败集 ⊆ 基线集，无新增失败 → 零回归。`monitor.test.js` draft-listing 用例经 3 次重复复跑证实为预存 flaky（mtime 排序时序，本变更不触及 monitor/listDraftJobs 代码路径）。五个 main.js 消费测试（exit-map / draft-brief / brief-gate / draft-desc-validate / draft-path-consistency）经兼容 re-export 全绿零改动（缝同一性硬验收过）。
  - 归一化 dry-run 冒烟：`node src/main.js demo --step CHECK --dry-run --no-monitor --run-dir _tmp/o2-before|-after`，plan 1 固化 sed 管线（剥 `[HH:MM:SS]` 前缀 / 剔 `Elapsed:` 行 / 剔 `[monitor]` 行 / `localhost:<PORT>` / `<RUNDIR>` token 归一）处理后 diff 为空。冒烟即时抓到一次真回归：手写迁移的 result banner 分隔线 `═` 少 2 字符（37 vs 39）→ 修正后 diff 空——归一化管线对"逐字节保持"的把关有效性实证。`from-step.test.js` 子进程 spawn 覆盖同步绿。
  - 全流程 dry-run 退出码：`node src/main.js demo --dry-run --no-monitor --run-dir _tmp/o2-full-before|-after`（git-stash 基线法）改前 exit 0 = 改后 exit 0。
  - import 图 Proof：`node -e "import('<abs>/orchestrator.js')"` 成功（11 导出键）；`grep -nE "^(import|export .* from)" src/orchestrator.js` = node:fs/path/url + config.js / runner.js / engine.js / flow-loader.js / expression.mjs / postmortem.mjs / env-loader.js / exit-map.js —— ⊆ 允许集，无 vendor/commander、无 monitor.js、无 draft-job.mjs/spawner.mjs（step-executor.js 未引用，"若引用"条款允许）。传递闭包论证：允许集各成员直接 import 复核全部落在打包集内（config→mission-check、runner→executor/platform、engine→sys-snapshot/reap-orphans/active-run-registry/platform/expression/roadmap-check、flow-loader→plan-check、postmortem→config/expression、env-loader/exit-map 零依赖）⇒ 全图 ⊆ 打包集（R1 核实 + 本次 live 复核），M2 打包清单直接证据成立。
  - `web run typecheck` / `lint:prompts` 绿；`web run build` 绿但产物 hash 随本机依赖版本漂移（同 WI1 日志先例），前端源码零改动 → `web/dist/` 还原保持 HEAD 版本（0 diff 不提交）。
- Docs (Phase 3): `docs/architecture/mission-driver-baseline.md` §Public Exports 改标真实定义处——新增 `src/orchestrator.js` 完整导出面条目 + `src/exit-map.js` EXIT_MAP 新家条目，`src/main.js` 标注为薄 CLI 壳 + 兼容 re-export 层，`src/draft-job.mjs` 条目改注 validateDraftDesc re-export 来源；§Marker Contracts `<BRIEF_GATE>` 段 extractBriefGate 定义处同步改 orchestrator.js；Test seams 段 `__setRunnerFactoryForTest` 定义处迁移并注明 live-binding 约束。`tools/mission-driver/CONTEXT.md` 目录结构段补 `orchestrator.js` / `exit-map.js` / `step-executor.js` 三行 + main.js 行改述 CLI 壳职责。roadmap M1-WI2 `ready → done`。
- Next: WI3–WI5（`2026-08-23-1300-3` driver 白名单 + embed 门控 + P1 文档收口）。
- dsh-plugin M1-WI1 executed: StepExecutor seam + ProcessExecutor landed. Plan: `docs/plans/dsh-plugin/2026-08-23-1300-1-stepexecutor-seam-process-executor.md`；roadmap WI1 → `done`。
- Code: new `tools/mission-driver/src/step-executor.js`（StepExecutor JSDoc 命名接口 + `ProcessExecutor`，三方法对 runner 1:1 位置转发，零行为逻辑）；`engine.js` 三处消费点 + `_runChildSubflow` parent 包装改读 `this.delegates.executor.execute*`（`engine.js:846/:916/:981/:1005`、subflow 包装 `engine.js:1310-1330`）；`main.js` 接线 `executor: new ProcessExecutor(runner)` 单键替代旧三键。`runScript` 第四 delegate 明确留在 seam 外（现状不动）。测试侧 `test/helpers.js` makeMockDelegates 改产 mock executor 并翻译 legacy 三键 override（各测试调用点零/微改）。
- Verification (all green, baseline-comparison method):
  - 全量 `pnpm --prefix tools/mission-driver test`：改前基线 175 ✔ / 6 ✖（预存失败集：2 个 Windows 路径平台用例 + 其父 suite 行、`dsh-plugin-packaging.md → runtime-types.ts:87` 行号引用 2 用例——WI5 收口范围）；改后 ✔ 列表 = 基线 + 新增 `test/step-executor.test.js`（8 用例 3 suite），失败集逐条相同 → 零回归。
  - dry-run 冒烟（Exit Criteria）：`node src/main.js demo --step CHECK --dry-run --no-monitor --run-dir _tmp/diff-before|-after`，固定归一化管线后 diff 为空。管线（sed 固化）：`-e 's/^([[:space:]]*)\[([0-9]{2}:[0-9]{2}:[0-9]{2})\] /\1/'`（剥墙钟前缀，含 Last activity 缩进行）`-e '/Elapsed:/d'` `-e '/^\[monitor\]/d'` + `localhost:<PORT>` 端口归一 + `_tmp/diff-(before|after)` → `<RUNDIR>` token 归一。执行日发现：draft review 未覆盖的端口漂移——`--no-monitor` 因 commander `--no-X` 语义设的是 `opts.monitor=false` 而 main.js 读 `opts.noMonitor`（恒 false，预存 quirk，不属本 plan 修复范围），monitor 总会启动且端口随并行 run 漂移，故管线补 `[monitor]` 行剔除 + 端口归一（已记入本条，后续 WI 裁决是否修 flag）。
  - Proof (c) git-stash 法：`_tmp/seam-capture.mjs`（START agent(timeoutMs)→BUILD tool(timeout)→FINAL agent 夹具，富字段 canned 结果）改前 old-form 三键注入 capture 与改后 new-form executor 注入 capture 逐字节 diff 为空；同夹具期望值钉入 `test/step-executor.test.js` (c)。
  - subflow 路径：`subflow-state-isolation` + `subflow-incremental` 18/18 全绿（包装链等价迁移直接证据，含 onStepUpdate 路由回归用例）。
  - `web run typecheck` 绿；`lint:prompts` 绿。`web run build` 因本机 `web/node_modules` 缺 `unplugin-vue-components`（部分安装的预存环境缺口）失败——本变更零前端改动、`web/dist/` 0 diff，按约束无需重建提交。
  - BUILD_VERIFY 复验（同日独立复跑）：`pnpm --prefix tools/mission-driver/web install` 补全 deps 后 `web run build` 绿（vue-tsc + vite build）；重建产物因依赖版本漂移 hash 不同但源码零改动，`web/dist/` 还原保持 HEAD 版本（0 diff 不提交）；`typecheck` / `lint:prompts` 绿；全量 test 627/631，4 个预存失败在 HEAD（981badc）worktree 复跑证实逐条相同 → 零回归成立。
- Docs: `docs/architecture/module-boundaries.md` 引擎核心行 "in-process: not exported" → "importable as a library behind the injected StepExecutor seam；CLI 仍是默认形态"（step-executor.js 入列）；`docs/architecture/mission-driver-baseline.md` §Public Exports 增补 `src/step-executor.js` — `ProcessExecutor` 条目。
- Next: WI2（`2026-08-23-1300-2` programmatic entry + EXIT_MAP hoist）。
