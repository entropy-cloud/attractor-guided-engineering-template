# Development Log - 2026-08-24

### 2026-08-24

- **age-autonomy 设计基线正式转 supported baseline（human 批准，2026-08-24）**：终审通过 + human 批准后，六份设计文档（00-overview/01-file-ledger/02-rule-law/03-supervisor/04-efficiency/05-usage）status 行统一改为 `> Status: supported baseline（human 批准，2026-08-24）`，00 §1 父段同步 P0–P4 立项期受支持行为以其他 owner doc 为准的措辞；roadmap header/Purpose 同步去掉「候选最终版」措辞；`docs/design/README.md` 路由说明与 `docs/index.md` 入口表同步刷新。age-autonomy 自此脱离候选态，P0–P4 按 roadmap 立项落地。

- **age-autonomy 设计基线终审（独立 subagent 无种子终审）→ conditional-pass，条件全部收口**：终审记录 `docs/audits/dsh-plugin/2026-08-24-age-autonomy-design-final-review.md`。无 P0；5 P1 + 7 P2。终审后当日收口 7 项：① 05 §3.3 增 A3「同 checkout 单执行形态」边界行（P1-1）；② 02 §4.5 改写 CAS 三选一 + Q4 显式未裁决（P1-2，源码事实：fs/edit-intent 单槽被 fs-observation-policy 默认占据）；③ 02 §4.9 增 fixedPrefix 块 schema（P1-3）；④ 01 §4.1 示例 agent "audit-heavy"→"auditor"（P2-6）；⑤ 03 §2 receipt 描述同步 A8（P2-7）；⑥ mission json 增 `autonomyPolicy` 字段（P1-5 数据面）；⑦ roadmap WI13/WI26/WI27/WI32/WI36 增补终审映射（P1-5/P2-1/P2-2/P2-3/P2-4/P2-5）。剩余全部为随里程碑项。**待 human 一句话批准转 supported baseline**（AI 修改文档不得自行转，ai-autonomy-policy）。

- **产品定位裁决（human，讨论记录 A8）**：mission-driver = 长时间 AI 自主运行，中间过程只提供部分查看能力（pull：monitor 9300 + mdcontrol.status），无实时交互、无逐 token/逐里程碑通知；monitor×DSH 集成为后续改造项（沿用 WI15 reopen 触发条件）。处置：**N1 里程碑通知三件套不做**（push 面与定位相斥）；**N5 阶段间人工 gate 明确不做**（反自主；人工边缘只在收尾回执点）；G6 中间态静默成文为有意、终态可查不依赖 push；N2（findings 平台期）/N3（错误码枚举）/N4（journal 纪律）不受影响按原归属落地。0005 §6.1 同步修订。

- 0005 对比评估增补 §6 修订对照（A1/A2/A7/P8 落地后）：四根轴从「架构主张」变为「独有机理」——① 独立性：`requireDistinctModel` + model lineage 机制化突破 inspect「同源同模型」天花板（单模型部署显式降级）；② 执法免疫：P8 + Protected Areas + CI merge-blocking 根除「执法者文件」单点失效（插件族无对应物）；③ 信任模型分层显式化（对比 automation 单一 fail-closed）；④ 宿主面纪律（第二真相通道显式排除，插件族是默认存储形态）；⑤ 具名 agent 契约/部署分离（agent 是部署数据非运行时实体，plan 级 audit 路由无插件提供）。N1–N5 纸面差距不变；诚实限制：架构主张 vs 运行代码、requireDistinctModel 依赖部署多模型、agents 面新增实现负担。

- **具名 agent 设计落地（A7 修订：bindings → agents）**：human 两轮提问驱动（「bindings 名称是否明确」「agent 不只涉及 model，还有提示词与工具配置」）→ 裁定弃用 `bindings`（与审计回执绑定 audit-binding 同名异义 + 概念本体是具名 agent 而非模型绑定），统一为 **`agents:` + `dispatch:`**：policy 内定义具名 agent（mode pooled|fresh、池化参数、fixedPrefix 组合、`model:{provider,model,reasoningEffort}`——措辞对齐宿主 ModelSelection、reasoningEffort 即思考级别、`requireDistinctModel` 独立性下限）；`dispatch:` 把派发类型（plan-review/closure-audit/deep-audit/execute/draft-plans）映射到具名 agent——提示词/trigger 只提派发类型，契约层零模型零配置（双形态可移植）；plan frontmatter 增可选 `agent:` 名引用（有界名单，守夜人路由解析）；04 §5 `sessions:` 并入 `agents:`。落地文件：02-rule-law §3 示例 + §4.1 model lineage + 新 §4.9；01-file-ledger §4.1 `agent` 字段；04-efficiency §5 示例；roadmap WI13（schema+agents/dispatch）、WI14（lineage+requireDistinctModel）、WI26（派发应用：补 native-executor ModelSelection documented gap / CLI 走 model/variant/agentFile）、WI24（真值表增 requireDistinctModel 与名引用用例）；讨论记录 A7 全文修订。G4 从哲学问题变为可测（驳回率按 exec/aud agent 对统计）。

- 新增对比评估 `docs/analysis/2026-08-24-0005-age-autonomy-vs-plugin-survey-comparison.md` —— AGE Autonomy 设计 vs dsh-plugin-survey 全部 18 份报告。结论：**三条本质轴上 AGE 全部超越插件族**（① 状态=意图工件非执行痕迹——turn-rewind「前提关系」论证 + automation/agent-teams store 撞零记忆红线被 Reject 为旁证；② 法律=纯函数+数据、三部署面同实现——插件全部宿主耦合零移植；③ 独立性=结构性第二 agent，插件全部自评）；但判「更优」前必须补五项调研已点名缺口：N1 里程碑通知三件套（background-agents 水印/quiet-wakeup → 缓解 G6）、N2 DEEP_AUDIT 平台期停止信号（ouroboros findings_hash，0000 §9 已点名未携带）、N3 失败错误码词汇表（automation → G10 归因解药）、N4 机器字段 journal/reconcile 纪律（turn-rewind/pipeline-kernel → P2 实测）、N5 阶段间人工 gate + attempt 上限（pipeline-kernel → backlog 候选）。诚实边界：「更本质」只在意图驱动文档循环域内成立；审计「同源同模型」天花板与 inspect 共享（确证 G4 需独立对策）。

- **A1 信任模型 + A2 执法层自护：human 委托 AI 选择并落地**（讨论记录 A6，2026-08-24）：**A1 裁定**= 默认护栏（诚实但会犯错）+ 只保留宿主 pre-execute 边界零成本对抗级（actor/nonce/CAS）+ **law 层唯一无条件对抗级例外** + 重开触发（出现伪造回执现实案例 → 立项受控变更通道）；bash/sed 旁路成文接受（与 plan-status-gate D 系列一致），CI 结构匹配 + git blame 事后兜底。**A2 已实施最小修复**：① `docs/context/ai-autonomy-policy.md` Protected Areas 新增「AGE 执法层」行（ask-first：`plugin/dsh/src/law/**` + `missions/autonomy.policy.yml` + `tools/mission-driver/src/{plan-check,gate-check}.mjs`）；② `02-rule-law.md` §4.7 新增执法层自护 deny 段、§2 新增信任模型段、§4.1 回执强度降级声明；③ `00-overview.md` 原则表新增 P8 执法者自护；④ roadmap WI21 增执法层范围、WI24 真值表增 law 域 deny 用例；⑤ 讨论记录 A6。明确不列入保护：mission-check CLI、plan/roadmap guides（走既有立项流程）。设计文档仍为候选基线，未经 human 终审不转 supported baseline。

- **age-autonomy 独立评审（grill）+ 两处成文约定落地**：新增 `docs/discussions/2026-08-24-age-autonomy-design-independent-grill.md` —— 对抗式评审（用户指定的 grill-me/grill-with-docs 技能确认不存在于 `~/ai/superpowers`，改以同方法自做）。新发现（超出前审 29 项）：G1 执法层自护缺失（law 代码 + `missions/autonomy.policy.yml` 在 AI 可写域，无 deny 规则、Protected Areas 未覆盖）；G2 执行通道旁路未经信任模型（门禁只拦 3 工具面，bash/sed 等价旁路；建议混合信任模型：护栏默认 + 对抗级保护只给审计回执/law 自护/终态冻结三处）；G3 双形态并发（**human 裁定关闭**：同一项目目录不会同时运行两个 mission driver）；G4 结构性独立≠统计独立（同模型族审计无失效度量、无角色互斥）；G5 宿主两未知数源码核实（pre-execute 载荷带 `exec.agent?.id` = SessionId；CAS 存在但 `fs/edit-intent` 单槽被默认挂载的 fs-observation-policy 占据，02 §4.5 需改为三选一）；G6 人侧异常通道静默；G7 claim TTL 无心跳；G8 迁移窗口无引擎 e2e 门；G9 trigger DSL 声明性打折（R1–R4 在代码里）；G10 failures 归因误杀；G11 M4 收益依赖未验证 provider 缓存。**A4 源级对照 DSH goals/plan/todos**（`~/ai/deepseek-harness` 第一方源码）：三者都是会话 JSONL 事件日志域状态、绑单一会话谱系——不能作 AGE 状态权威，仅可作「账本→宿主面」单向投影（todos 进度镜像 / goal run 句柄 / plan-mode 提问 UX），本轮全部降级为 P3 后产品可选项。**human 裁定（A5）**：host goals/plan/todos 非插件契约面，AI 按需自用；例外 = 两行约定。**已落地两行约定**：① `docs/design/dsh-plugin-integration.md` AGE mode 段加 authority-discipline 行（host goals/plan/todos 是会话临时刻写，账本唯一权威，host 状态不作完成判据、不回流）；② `docs/design/age-autonomy/03-supervisor.md` §4 加宿主 goal 注入权互斥行（mission 子代理不设 goal，守夜人不与 goal-round-driver 争同一 agent 注入权）。其余未决：A1 信任模型、A2 law 自护入 Protected Areas（最小处置约 1 小时）。

- **age-autonomy 设计文档审查 + 修订**（audit record `docs/audits/dsh-plugin/2026-08-24-age-autonomy-design-audit.md`）：按 design-doc-audit / state-machine-business-review 对照 0003/0004 与现 plan guide 审查六文档，发现并修复 6 个 P0（R1 静默 completed、Review Hold 状态矛盾、claim/dispatch 登记无存储位置、gate 签名缺 actor、全勾过渡死锁、审计拒绝写回死锁）与 25 项 P1（终态状态集、failures 解锁重置、完成公式补 `status:active`、终态冻结防旧回执复用、nothing 活锁、BUILD_VERIFY 命令来源、Supervisor 确定性、trigger DSL、open finding 机器表示、context-profile 路径冲突 missions scanner、WI13 证据面重建、EXIT_MAP 纪律、append-only 门禁、CAS fallback、步数口径等）。六文档 status 由「最终版」改为「目标设计基线（候选最终版）」，避免未立项目标被当作 supported baseline；`docs/design/README.md` 增 Domain Design Sets 路由。剩余风险：human 批准、宿主 actor/CAS 能力实测、partial/blocked EXIT_MAP 显式立项、trigger DSL schema 测试。审查为同会话冷回放，候选基线仍需 human 或独立 subagent 终审。

- **最终设计文档集落盘** `docs/design/age-autonomy/`（6 文件，自包含、不引用分析文件）：00-overview（总纲/设计原则 P1-P7/Ledger-Law-Supervisor/双形态/执行保证三层/迁移路径 P0-P4/术语）、01-file-ledger（roadmap 极简 frontmatter + plan frontmatter 最小集、状态格、完成派生公式、审计/review 内联、计量一般规则）、02-rule-law（门禁纯函数、声明式 DSL、三硬门 + nothing 兜底 + 认领 + 预算闸 + 路径护栏、BUILD_VERIFY 机械化、pre-execute/CI/git-hooks 部署面、shadow 纪律）、03-supervisor（五职责、三触发沿、连续模式=roadmap 即队列、幂等 occurrenceKey+claim、崩溃恢复、卡死检测、终态规则 R1-R4）、04-efficiency（池化+group+代际令牌+审计禁入池、FRESH/CONTINUE 组装、file embed hash、上下文画像、独立形态降级）、05-usage（DSH 三入口 + 技能路由 + 典型一天 + 介入点；独立 CLI/CI/cron/手动；双形态对照；上手路径）。docs/index.md 增路由项。

- **human 裁定：meter 持久化归属**——跨 run 全局计量（maxAuditRounds 花费）直接放 roadmap frontmatter（`audit-rounds: N`），消解 0004 报告 Q1-2 缺口 A 的「零记忆原则 vs meter 持久化」矛盾。limit 留配置、count 进账本一事一处；语义变为 mission 全局审计预算（修正 M1「按 run 重置」怪点，行为变更须成文）；写者纪律 = 合法写者 + 乐观锁。连带修订：讨论稿 §4.3（roadmap 极简 frontmatter，推翻原「免 frontmatter」倾向）+ §7.5 裁定、0004 §3.2 缺口 A 标注、0003 §5.c meter 行。
- **human 裁定（延续）：per-plan 失败计数**——同样进 plan frontmatter（`failures: N`），与 audit-rounds 同模式；limit=maxFailures 留配置；跨 run 累计；写者=监督者失败归因（executor 错误/测试红/claim 到期无产出）；升级规则 `failures ≥ maxFailures → held + 人工回执`（M4 等价物=M1 onMaxRetries→failed）。消解 0004 Q1-3 缺口。立一般规则：跨 run 需持久化的计量归账本 frontmatter、limit 归配置、run 内临时量归 scratch。讨论稿 §4.2 已更新。

- 新增独立核查报告（红队）：`docs/analysis/2026-08-24-0004-m4-feasibility-and-recovery-verification.md` —— 对 0003 M4「法律门禁」方案做源码级可行性核查（引擎 engine.js/orchestrator.js/config.js/main.js/reap-orphans.mjs/run-reconcile.mjs/flow-loader.js + 插件 mdcontrol-routes.ts/plan-status-gate.ts + 三 flow 全部 prompts + frontmatter 讨论稿 + 0001/0003 报告）。判定：条件可行。关键发现：① M4 钦点原型 WI13 的证据面（F1/F2/F3）全建在 run-state-*.json 子流程文件上（plan-status-gate.ts:321-355），M4 引擎可选后证据面恒空 → no-op，迁移是「证据面重建」非「泛化」；② 「独立性=谁派发」漏了「验证派发发生且回执匹配」的机械绑定层，不补则内联审计+completed 派生化让「自审过关」比今天更容易；③ nothing 声称/全勾过渡/draft→active 三处需机器兜底门禁；④ meter 量（审计轮次等）持久化与 §4.0 零记忆原则冲突需显式裁定；⑤ claim 租约 TTL、无产出守卫、乐观锁、EXIT_MAP 等价物、往返熔断等 7 项现机制在 M4 缺失或失效（§5 表）。三硬门前置：审计回执绑定、draft→active 写者身份门禁、完成派生公式补全。结论全部带 file:line 或方案条款，判断区分为「源码证实/方案推断/我的推理」。

- 0003 报告增补 §9：回应「局部步骤传参」质疑——对三个 flow 全部数据流源码级清点（5 类），关键事实：子流程参数全部来自磁盘扫描（activePlans/draftPlans/openAudits）而非 AI 返回值，引擎只是文件系统搬运工；唯一 AI 返回数据块（DRAFT_PLANS 的 FLOW_VARS.PLAN_FILE）主循环内被扫描版 flowArgs 覆盖、仅剩存在性校验用途，近乎冗余；真正跨步数据（审计发现→重做指令）在审计内联裁定下改为账本直读。定理：AGE file-in/file-out 下返回通道要么冗余要么违规。诚实边界三处：工具输出 agent 自跑自读、临时量入 scratch、并发认领需乐观锁门禁（M1 亦有非 marker 造成）。北极星路径修正：门禁族清单加「认领/寻址原语」。

- 新增架构北极星分析：`docs/analysis/2026-08-24-0003-ai-automation-interaction-essential-design.md` —— 四种 AI↔自动化交互模式（函数调用/对等消息/AI自驱/法律门禁）对照 DSH（原生循环免费、拦截是母语）与 AGE（文件真理/结构独立/不信认知）两轴推演。核心洞察：**AI_STEP_RESULT marker 是第二真相通道，与 Plan Status 行同病**——frontmatter 派生化后 marker 信息归零，flow JSON 是文档态格的影子拷贝。判定：M4 法律门禁模式（Ledger 文件账本 + Law 纯函数门禁族 + Supervisor 守夜人五职责）本质优于现 M1 引擎驱动，且现设计三动向正在向其收敛；建议不重写、立为北极星绞杀式迁移（frontmatter→门禁族→Supervisor seam→引擎降级为实现之一）。

- 完成 dsh-plugin 插件生态调研：18 份独立子代理源码级报告落盘 `docs/analysis/dsh-plugin-survey/`（核心编排 7 + agent 组织/自进化 3 + 先例核对/工件恢复 3 + 门控/闭环 5）+ INDEX.md 索引（含未调研插件理由表）。每份含 Adopt/Adapt/Reject 映射本项目线程。要点：agent-teams attemptId 代际令牌补强池化判据；automation 幂等认领四语义可移植 roadmap-as-queue（store 明确 reject）；relay 检验"不引入总线"裁定成立并列三个翻转场景；turn-rewind 判定快照回滚在有权威工件时冗余；goal-scaffold 无机械看守成 completed 派生化反差例证；spec-loop 前提修正（非 workflow 引擎）但 bash-before-judge 可入 CLOSURE_AUDIT；better-sidebar 三先例源码确证 + WI15 reopen 七项清单。
- 既有分析报告增补：0000 报告 §9（池化判据/停止信号/VM 隔离预案）、0001 报告 §8（队列语义背书/恢复标签法/总线翻转条件/协作中断对照）、frontmatter 讨论稿 §9（scaffold 反差例证/CLOSURE_AUDIT 增强/审计独立性五层/最小集再确认）。

- 新增观念评估报告：`docs/analysis/2026-08-24-0002-age-minimal-memory-and-executor-separation-assessment.md` —— 对照 workflow/source-of-truth/mission-design 等原理文档逐条评估 human 八条观念。总评：六条完全正确有文档直接背书（最小外化记忆/闭环切片/独立监控/分解拓扑/规范过程分离/强化loop），两条需精确化（元结构规定+内容自适应的自进化；「不绑定特定 flow engine」而非「无需引擎」）。中心结论：信息组织、执行机制、机械验证三事正交——第一件归 docs 体系，第二件可替换（StepExecutor 已兑现），第三件不可由 AI 认知承担。

- 设计讨论稿增补（同日 human 裁定）：review/audit 记录内联进 roadmap/plan 文件本身，不再产生外部分析文件——核实该裁定实为扶正既有实践（Draft Review Record 与 Closure 证据本就规定在 plan 内、closure-audit 提示词本就直接编辑 plan、docs/audits/ 真实存量仅 ~6 文件）；独立性=独立 dispatch 非分文件；Source Audits 跨文件生命周期消解（发现→直接成 plan 或 roadmap 注记，关闭=勾 checkbox）；frontmatter 字段表相应删除 source-audits。见 `docs/discussions/2026-08-24-roadmap-plan-frontmatter-reform.md` §8。

- 新增设计讨论稿：`docs/discussions/2026-08-24-roadmap-plan-frontmatter-reform.md` —— roadmap/plan 格式 frontmatter 化提案分析。核心意见：赞同且建议再进一步——**completed 从可写状态改为纯派生**（status: active + grep 无未勾选项），使 "prematurely write completed" 不可伪造，WI13 门禁随之大幅简化；per-Phase `Status:` 行整体删除（guide 规则 11/12 治理的三源状态病根除）；roadmap Work Item 块改纯 checkbox 列表、免 frontmatter；格式硬边界 = 引擎零依赖决定的扁平标量子集（~30 行内置解析器）。待裁定：completed 派生化、存量迁移策略、checkbox 计数域等五项。

- 新增第二份调研报告：`docs/analysis/2026-08-24-0001-mission-run-mode-and-async-control-research.md` —— mission 运行模式精读（loop 归属/启停续/重跑语义）+ 10 个新插件生态对照。**Human 裁定 AGE 本质性设定入档为报告 §4.0 最高约束**：状态只外化于 roadmap/plan 文件、插件零持久记忆、暂停=直接中断（恢复=收敛式重跑）、跨 session checkout 即恢复；据此修订裁定——waiter-gate pause 机器否决（dsh_workflow 模式存档作反例）、stop=暴露既有 watchdog 中断序列、定点续跑降级 CLI 本机便利、队列持久意图=仓库本身（roadmap 即队列）。
- 新克隆 10 个参考插件至 `~/ai/dsh-plugins/`（dsh_workflow、dsh-automation、DSH-pipeline-kernel、dsh-background-agents、dsh-loop-dock、dsh-agent-relay、dsh-spec-loop、dsh-inspect、dsh-agent-teams、dsh-plugin-agent-workflow），关键读物：dsh_workflow `docs/ARCHITECTURE.md`+`lib/engine.js`（waiter-gate pause/snapshot rerun）、dsh-background-agents `ARCHITECTURE.md`（双通道纪律/FIFO inbox）、dsh-automation `docs/DESIGN.zh-CN.md`（occurrenceKey 幂等/crash=interrupted/Schedule≠authorization）。

- 新增调研报告：`docs/analysis/2026-08-24-0000-longrun-autonomy-agent-reuse-prompt-dsl-research.md` —— 针对 dsh 插件长时自主运行形态的六命题分析（文档门禁化 / reviewer 池化 + group 标识 / drafter 跨 run 复用与缓存纪律 / FRESH-CONTINUE 双模式 prompt 组装 / 轨迹驱动必读文件 `<file path hash>` 预注入 / 规则 DSL），并含跨 run 连续自主运行（队列 + 断线续跑）形态草案。
- Key decision（human 已裁定 scope）：仅调研不改 roadmap；跨 run 连续自主在界内；CLOSURE_AUDIT/DEEP_AUDIT 结构性独立审计禁入池化复用。
- 关键结论：三件套抽象（AgentSessionPolicy / PromptAssembler / GateRule）全部可居插件层、零引擎 diff；M3-WI13 的 pre-execute 门禁是 P1/P6 的直接先例；建议切片顺序 S1 组装器 → S2 池化 → S3 profile 挖掘 → S4 docs-gate → S5 continuous 模式。
- Next step：human 审阅报告后决定是否立项（报告 §8 列出五个开放问题，其中 provider 缓存语义与宿主 token-meter 可观测面须实测核验）。

- **新增 mission `age-autonomy-plugin` + 插件形态收窄版 roadmap `age-autonomy-plugin-roadmap.md`**（同日 human 提议直接拟制）：以 `docs/design/age-autonomy/` 候选最终版为设计基线，把 Ledger · Law · Supervisor · Efficiency 落进 `plugin/dsh/` 侧（**零引擎 diff** 硬约束保留）；30 WI 拆为 6 个 milestone（M1 Ledger / M2 Law / M3 Supervisor / M4 机械验证 / M5 效率层 / M6 E2E 自动验证收口），**每个 milestone 末位设 Verification Gate WI**——4–5 条真实命令（`plan-check.mjs --strict` / `pnpm --prefix tools/mission-driver test` / `plugin/dsh/test/<suite>.test.mjs` / `mission-check.mjs` / `verify-age.sh` / `verify:e2e` / `grep -c "^- \[ \]"`），任何一条红 = milestone 未收口 = 不允许写 `done`。mission = `missions/age-autonomy-plugin.json`（`extends: base`，`roadmapPath` / `plansDir` / `auditsDir` 全部就位，`commands` 链含 `plan-check` / `gate-check` / `mission-check` / `verify-age` / `verify-e2e` 五条自动验证命令，`commitFormat` = `<type>(age-autonomy-plugin): ...`）。`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-plugin.json .` 真实 exit 0 通过 mission 校验。**与旧 `age-autonomy-implementation-roadmap.md`（35 WI P0–P4 总览版）关系**：本版是子集（聚焦 plugin 侧可执行切片 + 加严自动验证），不动旧版 P0–P4 立项口径，跨形态共有面（codemod/guide 同步/frontmatter 落地）仍归旧 roadmap 收口。**关键纪律**（已写入 roadmap 顶部「核心纪律」五项）：① 零引擎 diff 为底线 ② 自动验证先行（Verification Gate 是收口硬条件，不由 chat 决定）③ CI merge-blocking 三层（L1 单测 → L2 双后端矩阵 → L3 真宿主 e2e）④ 人类可读性 ≠ 验收标准 ⑤ 审计回执绑定硬门（无手动 close 通道）。下游影响：M1 WI1 落地时将以零行数 `plugin/dsh/src/ledger/frontmatter.mjs` 为首片，与 M3-WI13 的 pre-execute gate 同源以共享真值表。

- **收口修正（human 抓到歧义）**：上述新增的 `age-autonomy-plugin` mission + roadmap 与早上的 `age-autonomy-implementation` 复刻同一份 design、引用同一份 prompts/moduleDir/contextDir/extends，**两份不是子集/超集关系而是同一工作的两个分裂版本**——属「同一事实写在多处」反模式。human 拍板走方案 A「收编到既有」：① 删除 `missions/age-autonomy-plugin.json` + `docs/backlog/age-autonomy-plugin-roadmap.md` + 我新建的空 `docs/{plans,audits}/age-autonomy-plugin/` 目录；② 保留并增强既有 `missions/age-autonomy-implementation.json`：补 `commands` 段（`test` / `build` / `lint` / `typecheck` + `plan-check` / `gates` / `verify-age` / `verify-e2e` 五条自动验证命令，`missionCheck` 真实 exit 0 通过）+ 描述段增「零引擎 diff 为底线 / 每个 milestone 配以机械验证门」；③ 保留并增强既有 `docs/backlog/age-autonomy-implementation-roadmap.md`：在 M1–M5 末位各插入一个 Verification Gate WI（WI11 / WI24 / WI31 / WI36 / WI40），5 个 Gate 共 22 条真实可执行命令（`plan-check.mjs --strict` / `pnpm test` / `plugin/dsh/test/<suite>.test.mjs` / `mission-check.mjs` / `gate-check.mjs` / `verify-age.sh` / `verify:e2e` / `grep -c "^- \[ \]"`），每条红 = 整 milestone 不收口；新增「核心纪律」五项（零引擎 diff / 自动验证先行 / CI merge-blocking 三层 / 审计回执绑定硬门 / 人类可读性 ≠ 验收标准）+「形态聚焦：DSH 插件优先」+「marker 迁移纪律」+「与其他 mission/roadmap 的关系」三段 Dependencies & Notes 增补。终态：roadmap 35→40 WI（35 个工程 WI + 5 个 Verification Gate WI），mission 单一、roadmap 单一、design 单一——**单 mission + 单 roadmap + 单 design 收口**，符合 AGENTS.md「chat 不作 durable memory」+「同一事实不写多处」原则。下次「拟制新 X」请求的自我约束：先核对仓库是否已存在对等 X；若存在，强化它而非另立门户。
