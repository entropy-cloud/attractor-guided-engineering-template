# Development Log - 2026-07-30

### 2026-07-30 (mdr-audit-fix-1 followup: manuals CHECK semantics doc-drift sweep)

- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-30-0936-1-check-semantics-doc-sweep.md` Phase 1（单 Phase，docs-only），全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`。这是上一条 `[P1]` plan（`2026-07-30-0902-1`）在其 Closure out-of-scope note + Follow-up 里显式推迟的 doc-sweep，现关闭。
- 背景: 上一 [P1] plan 修正了手册的 step-table 行 + `commands` 示例，但显式把两个最高可见度 surface 移出 scope——"30 秒版" ASCII 循环图注 + "第一次循环" 散文讲解仍用旧的 "health check: do tests/build pass?" 措辞，与 shipped mdr-fix-3 行为矛盾；一句摘要 stage label（en/zh :11）亦读 `health-check`/`健康检查`。
- Fix（纯文档对齐，零代码 / 零受保护区 / 零依赖 / 零 web-dist 改动）:
  - `user-manual.en.md:39` ASCII 图注 `CHECK (health check: do tests/build pass?)` → `CHECK (deterministic-state gate: commands.check or git-clean check)`，box-drawing + `↓ pass` 对齐保留。
  - `user-manual.zh.md:39` 同义 → `CHECK（确定性状态门：commands.check 或 git 冲突标记检测）`。
  - `user-manual.en.md:341` 散文 `CHECK runs the health check.` → `CHECK runs the deterministic-state gate (commands.check, else git-clean check).`
  - `user-manual.zh.md:547` 同义 → `CHECK 跑确定性状态门（配置了 commands.check 就跑它，否则 git 冲突标记检测）`。
  - `user-manual.en.md:11` + `user-manual.zh.md:11` 一句摘要 stage label：Decision outcome A（对齐）→ `state-check` / `状态检查`（替代 `health-check` / `健康检查`），双手册一致。
- Decision（摘要 label 对齐 vs 保留）: 选 A（对齐）。`health-check`/`健康检查` 虽是口语 stage 名非显式 tests/build 断言，但它是读者对 CHECK 的第一心智模型 surface；对齐为中性 `state-check`/`状态检查` 与已修正的图注/散文完全一致，消除暗示。禁止 en 对齐 / zh 保留的混搭。
- Safety scope: 仅本 plan scope（两本手册）。未触碰 sibling plan 工作（working tree 中另有 3 个 pre-existing modified 文件 `.opencode/skills/mission-driver/SKILL.md`、`references/roadmap-template.md`、`template/docs/backlog/implementation-roadmap.md` 属 sibling work，本 plan 未动）。未触 flow JSON / engine / prompts / `health-check.md` / `web/dist` / `package.json` / `install-age.sh` / `mission-check.mjs`。
- Verification（full green）: `pnpm --prefix tools/mission-driver run lint:prompts` → `prompt-check: OK`；`pnpm --prefix tools/mission-driver/web run typecheck && build` clean 且 `web/dist/` git status 空（字节一致，零 web 影响）；`pnpm --prefix tools/mission-driver test` → **597 pass / 0 fail**。Proof grep（`tests/build|测试/构建|do tests.*build|health check|health-check|健康检查`，rg 不可用用 Grep 等价）仅命中 2 处合法 BUILD_VERIFY 行（en:369 / zh:575），无 CHECK 相关 tests/build 或 health-check/健康检查 残留。
- Closure: 独立 subagent closure audit（task `ses_04f4653b7ffe7ijqBE6u0o2tKo`，fresh session）→ `AUDIT_RESULT: PASS`（5 项全过：6 处 drift 已修且 ASCII 对齐保留；与 step-table 行一致；grep proof 仅 2 处 BUILD_VERIFY；scope discipline 仅两本手册；无受保护区/web-dist 回归）。
- Owner-doc: user-manual en+zh 对齐 IS the owner-doc update。`CONTEXT.md` 已文档化 CHECK 为可配置确定性状态门，无需 edit。
- Roadmap: `docs/backlog/mission-driver-actionable-fixes-roadmap.md` Follow-up Backlog 追加 `[P1-followup] ... RESOLVED 2026-07-30` 条目指向本 plan（镜像 [P1] 的记录方式），Last updated → 2026-07-30。Work Item Status block 4 项仍全 `done`（本 plan 是 follow-up，非编号 work item）。

### 2026-07-30 (mdr-audit-fix-1: surface commands.check + correct CHECK doc semantics)

- 执行 plan `docs/plans/mission-driver-actionable-fixes/2026-07-30-0902-1-surface-commands-check.md` Phase 1（单 Phase，prompt 措辞 + JSON 示例 + 用户手册对齐），全部 Exit Criteria + Closure Gates 达成，Plan Status → `completed`。open-audit [P1] finding 关闭。
- 背景: mdr-fix-3 的 `commands.check` + 可配置 CHECK 引擎特性已 ship 且 tested，但在所有 mission-creation/doc surface 上不可见——`draft` agent 从不生成 `check`，手册 CHECK 行还事实性矛盾 shipped 行为（说 CHECK 跑 tests/build/lint，正是 `health-check.md:16` 禁止的）。`extends` 浅合并会丢掉 base.json 的 `check` 默认值。
- Fix/Add（纯措辞 + JSON 示例 + 手册对齐，零受保护区、零新依赖、零 delegate var、零 flow/engine/mission-check.mjs 代码改动 → context-map drift gate 不触发）:
  - `prompts/mission-draft.md:39-44` — `commands` JSON 示例新增 `"check": "{optional deterministic-state gate command, e.g. mvn clean compile; empty/omitted = git conflict-marker fallback}"`。Notes 段新增 `commands.check` note（含浅合并提醒：自定义任何 commands key 时须显式重设 check，否则 base 默认被丢）。`<AI_STEP_RESULT>created</AI_STEP_RESULT>`/`<MISSION_FILE>` 输出契约未改；无 literal `docs/backlog/`（`draft-path-consistency` Case E 守卫保留）。
  - `user-manual.en.md` — quickstart `commands` 示例（`:128-133`）+ full reference（`:420-425`）各加 `"check"`；CHECK 步骤行（`:301`）由 "Health check: run tests/build/lint... / `pass`/`fail`" 改为确定性状态门语义（配置跑 `commands.check` 否则 git 冲突标记兜底；不跑 `commands.test`）+ `pass`/`needs_fix`/`fail`。base.json 缩写 one-liner（`:447`）用 `...` 省略号，as-is。
  - `user-manual.zh.md` — 枚举（`:123`）加 `check`（留空=git 兜底）；CORE（`:215-219`，配 `mvn compile`）/ quickstart（`:333-337`）/ full reference（`:625-629`）三处 `commands` 示例各加 `"check"`；CHECK 步骤行（`:506`）改为确定性状态门语义 + `pass`/`needs_fix`/`fail`。base.json 缩写 one-liner（`:652`）as-is。
- Decision A（同 slice 对齐两本手册）: mdr-fix-4 先例——当 generation/CLI/config surface 被重述，手册须同 slice 对齐以防 doc drift；且 Minimum Rule 1 要求盘点矛盾 surface，手册是人类手改 mission 的平行创建路径；留着手册 CHECK 描述说 "跑 tests/build/lint" 却在 commands 示例加 `check` 会制造新的内部矛盾。
- Safety scope: 仅本 plan scope。未触碰 sibling plan（未触 flow JSON / engine core / executor / flow-loader / mission-check.mjs 合并逻辑 / web/dist / package.json dependencies / memory/_index.md / install-age.sh）。`prompt-check.mjs` 确认不受影响（无新 `<AI_STEP_RESULT>` tag；`mission-draft.md` 的 `created` marker 不绑定 flow step，marker-membership check 被跳过）。
- Out-of-scope note（未执行）: en 手册 `:39` ASCII 图注 `CHECK (health check: do tests/build pass?)` 同类 drift，但非本 plan target，留待未来 doc-only sweep。
- Owner-doc: user-manual en+zh 对齐 IS the owner-doc update（Decision A）。`CONTEXT.md` 已文档化 CHECK 为可配置门，无需 edit。
- Roadmap: `docs/backlog/mission-driver-actionable-fixes-roadmap.md` Work Item Status block 仍为原 4 项（全 `done`，本 plan 不属其中）。本 [P1] follow-up resolution 记录于该 roadmap 的 Follow-up Backlog（同审计 P2 同区，便于追溯），Last updated → 2026-07-30。
- Closure: solo cold-replay（Reviewer-Availability Fallback，非受保护/非高风险 slice，无第二 reviewer 可用）。
- Verification（full green）: `pnpm --prefix tools/mission-driver/web run typecheck` clean；`web run build` clean 且 `web/dist/` git status 空（字节一致，零 web 影响）；`pnpm --prefix tools/mission-driver run lint:prompts` → `prompt-check: OK`；`pnpm --prefix tools/mission-driver test` → **597 pass / 0 fail**（含 draft-path-consistency Case E + prompt-check）。Content grep（6 处 `commands` 全含 `check` key + `commands.check` note + 双手册 CHECK 行含 `needs_fix`）+ negative grep（mission-draft.md 无 literal `docs/backlog/`）均通过。
