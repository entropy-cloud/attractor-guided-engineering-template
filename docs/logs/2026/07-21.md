# Development Log - 2026-07-21

### 2026-07-21

- mdr-remediate-4 (mission-driver-draft-robustness) closed: 3 个 open-audit findings（H2, H3, H9）收口——non-forEach subflow 增量落盘 + WI1 base.json wire-up 真 integration test + subflow-incremental.test.js Case B 改 deterministic latch（去 wall-clock delay）。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1523-1-non-foreach-subflow-and-test-gaps.md` → `Plan Status: completed`。
  - Phase 1 — H2 non-forEach subflow incremental persistence:
    - **Decision**: 选 Option A——engine 代码修复（不是 doc-only）。非-forEach 分支（`engine.js` `_executeSubflowStep` 末尾的 single-child 路径）镜像 forEach 分支的 `_wfAppendSubflowRun` 模式但更进一步：forEach 是 post-completion append，non-forEach 是 **pre-run placeholder** + 现有 `_wfClose`-based 终态覆盖。这是 WI5 之外的新 pattern（plan iteration-1 review 纠正了原始 Goal 把 forEach 描述成有 pre-run placeholder 的错误）。
    - **H2 (Fix)**: `tools/mission-driver/src/engine.js:1115-1121` 区域（non-forEach 分支）在 `await this._runChildSubflow(flowDef, childArgs)` 之前加一行 `this._wfAppendSubflowRun(stepName, visit, { forEachIndex: 0, forEachItem: null, file: null, status: "running" });`——SIGKILL mid-child 时主 run-state.json 现在反映 `subflowRuns: [{status:"running",...}]`（不再 `[]`）。子流程返回后，run() 调用方（`:1799`）构造 `completedMeta = { type: "subflow", subflowRuns: result.subflowRuns }`，经 `_wfClose`（`:1802 → :394 spread → :401 replace`）覆盖 placeholder——无新 patch helper，无 duplicate，无残留 running entry。forEach 分支（`:1020-1104`）完全不动。
    - **H2 (Add | Proof)**: `test/subflow-incremental.test.js` 新增 2 个 `it()`：(a) Case G `non-forEach single-child subflow writes a pre-run running placeholder`——用 manually-controlled Promise latch（`new Promise((r) => { releaseChild = r; })` + separate `enteredGate` signal），不依赖 setTimeout；mock 在 entry 同步 snapshot 一次（断言 placeholder 已写为 `[{status:"running",forEachIndex:0}]`），await childGate 后再读 final state（断言 `_wfClose` 覆盖为单个 `status:"completed"` 条目，无 duplicate）；(b) Case H `_wfAppendSubflowRun no-op safety also covers the non-forEach call site's running-placeholder shape`——镜像 Case E（forEach 用 `{status:"completed"}` 形状）但用 non-forEach 调用点的 `{status:"running",forEachItem:null,file:null}` 形状，锁住 no-op contract 对两种 record shape 都成立。同步把 Case F grep anchor 从 `>= 3` 升到 `>= 4`（新 call site 加入）。Test count +2。
    - **H2 (Fix)**: `design/draft-robustness-design.md` §4.5 新增 §4.5.5 段 "非 forEach 分支的扩展（mdr-remediate-4）"——一段说明 WI5 初始实现只覆盖 forEach，mdr-remediate-4 扩展到 non-forEach；§2.6 "run-state.json 自包含" 目标至此对所有 subflow 类型都成立；monitor `mergeSubflowChildren` fallback 保留作为孤儿 child 的 defense-in-depth。
    - **H2 (Fix)**: `CONTEXT.md:118` 关键约束段从 "draft-robustness WI5：subflow step 的 `subflowRuns` 在 `_executeSubflowStep` 的每项完成后立即增量追加..." 改为 "draft-robustness WI5（mdr-remediate-4 后扩展到非-forEach 分支）：...无论 forEach 还是单子流程，都在子流程开始前写入 `status: "running"` placeholder..."——避免 "每项完成后" 暗示只 forEach 的过度声明。
  - Phase 2 — H3 base.json integration tests:
    - **H3 (Add | Proof)**: `test/draft-desc-validate.test.js` 新增 `describe("cmdDraftMission — base.json draft.minDescLength wire-up (H3)")` 含 3 个 `it()`：(a) Case D1 distinguishing threshold-8——写 `{draft:{minDescLength:8}}` 到 tmp project 的 `missions/base.json`，调 `cmdDraftMission("add xy")`（len 6，passes default-4 但 fails configured-8）断言 rejection；附 `assert.equal(validateDraftDesc("add xy", 4).ok, true)` guard 让 distinguishing 性质显式可读；(b) Case D2 garbage fallback——`{draft:{minDescLength:"garbage"}}`，"add" (len 3) rejected + "add x" (len 5) accepted（同一 `it()` 内两次调 cmdDraftMission，之间 `process.exitCode = undefined` 手动 reset）；(c) Case D3 null fallback——同 D2 形状但 `{draft:{minDescLength:null}}`。每个 `it()` 用 `makeTmpProject()` + `writeFileSync(join(root,"missions","base.json"), ...)` 创建真实 base.json（之前所有 test 都不写，wire-up 走 catch{} → {} → default 4）。Test count +3。
    - **H3 (Fix)**: `docs/plans/mission-driver-draft-robustness/2026-07-21-0954-2-cli-draft-desc-validate.md:102` 的 ticked exit criterion 末尾追加 parenthetical pointer："（backed by `draft-desc-validate.test.js` Cases D1 / D2 / D3 as of mdr-remediate-4 H3 — full coverage of all 3 sub-cases enumerated above: D1 distinguishing threshold-8 case ... D2 garbage-string fallback ... D3 null fallback ...）"——把 fake closure 转为 honest closure，不重开 WI1 plan 自己的 closure audit。
  - Phase 3 — H9 deterministic latch:
    - **H9 (Fix | Proof)**: `test/subflow-incremental.test.js` Case B 从 `delays = [10, 10, 200]` + `setTimeout` 改为 Promise-park latch——items 0 和 1 分别 `await park0` / `await park1`（manually-controlled Promises），item 2 mock 入口后 `await Promise.resolve()` 让 dispatcher 的两条 `.then()` recordResult chain 都排空再 snapshot。注释里写完整 microtask trace（dispatcher0.then → recordResult(0) → dispatch starts item 2；dispatcher1.then → recordResult(1)；item2.mock yield 后 snapshot）。原 200ms wall-clock 完全去掉，断言 `subflowRuns.length === 2` 现在是 order-based 不是 timing-based。Case B 的 `it()` 标题同步加 "(mdr-remediate-4 H9)" 标记。`delay` helper 保留（Case D 仍用）。
  - 未改 forEach 分支（`engine.js:1020-1104`）；未改任何 flow 定义文件（`flows/mission-driver.json` 的 `DEEP_AUDIT` step 不动，修复在 engine 层不在 flow 层）；未移除 monitor `mergeSubflowChildren` fallback（保留作 orphan child defense-in-depth）；未改 `validateDraftDesc` 纯函数或黑名单；未引入新 test framework / assertion library / fake-timer utility（全部复用既有 `mkdtempSync + config.runDir` + `__setRunnerFactoryForTest + makeFakeRunner` + manually-controlled Promise 模式）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **520 pass / 0 fail**（baseline 515 = mdr-remediate-3 closure；+2 H2 Cases G/H + 3 H3 Cases D1/D2/D3 + 0 H9 rewrite-in-place = 520）；H9 stability gate：3 consecutive runs 都 520 pass / 0 fail，零 flake；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built in ~18s；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback；3 findings 源 audit 评级 H2 MEDIUM-LOW / H3 LOW-MEDIUM / H9 LOW)。无 `> Work Item:` 标签（audit-sourced plan，非 roadmap-sourced），roadmap 不需更新（WI1-WI5 已 done）；无 `> Source Audits:` 标签（front matter 用的是 `> Source:`），source audit 文件不需 close（按指令"omit the step entirely if there is no `> Source Audits:` line"）。

- mdr-remediate-3 (mission-driver-draft-robustness) closed: A1 stuck-running draft-state.json failure mode remediation——WI1 reject 分支现在写 `{status: "failed", phase: "rejected", endedAt, error}` 到 `draft-state.json`，monitor draft-job UI 不再看到永远卡 running 的失败 job。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1005-3-stuck-running-draft-state-remediation.md` → `Plan Status: completed`。
  - Phase 1 — Reject-branch state write + regression test:
    - **A1 (Fix)**: `tools/mission-driver/src/main.js:344-405` 三处耦合改动：(1) 把 `const stateFile = ...` 与 `const writeDraftState = (patch) => {...}` 闭包定义从 reject 分支之后（旧 `:362-372`）提到 `validateDraftDesc` 调用之前（新 `:355-365`），解掉 TDZ `ReferenceError`——否则 reject 分支引用闭包会抛错；(2) 在 reject 分支（`:367-389`）`await runner.close(); return` 之前插入 `if (opts.draftJobDir) writeDraftState({ status: "failed", phase: "rejected", endedAt, error: v.reason });`——startDraftJob 写的 initial `desc` 经 writeDraftState 的 `{...prev, ...patch}` merge 语义自动保留；(3) 既有 7 个 `writeDraftState` 调用点（re-affirm running / brief 失败 / brief_done / gate blocked / Stage 2 入口 / draft 失败 / completion）行为不变，只是闭包定义位置上移。新 phase 值 `"rejected"`：与既有 runtime-failure phases `"brief"` / `"draft"` 语义不同（WI1 输入拒绝是 pre-Stage-1，没有 brief/draft agent 跑过），故新增 terminal phase 而非复用既有 failed-path 约定。
    - **A1 (Add | Proof)**: `tools/mission-driver/test/draft-desc-validate.test.js` 改造 + 扩展：(a) Case B1（direct CLI 路径）原 `it()` 内追加 `readdirSync` 递归 walk 断言——`draftJobDir` 未设时 `writeDraftState` 自守护 `if (!stateFile) return;`，整个 tmpDir 不应出现任何 `draft-state.json`（+0 `it()` blocks）；(b) Case B2（draftJobDir 路径）从旧的 "断言 cmdDraftMission 不写 running re-affirm" 改为新的 "断言写 terminal failed/rejected state"——pre-populate startDraftJob 的 initial running state（`{status:"running", desc:"d", phase:"brief"}`），调 `cmdDraftMission("d", { draftJobDir })`，断言 resulting `draft-state.json` 为 `{status:"failed", phase:"rejected", error: /too short|placeholder/, desc:"d" (preserved by merge), endedAt}` + 目录只含 `draft-state.json`（WI1 file-pollution goal intact）+ `readDraftJob(root, jobId)` 消费方读回的 state 含 `error` 字段（锁住 monitor detail-view read path 能显示失败原因）；(c) Case B2 describe 末尾新增 1 个 `it()`——placeholder rejection (`"test"`) 同样写 failed/rejected terminal state 且 `error` 含 "placeholder"，锁住所有 WI1 rejection reason（不只 "too short"）都触发 terminal 写入。新增 `import { readDraftJob } from "../src/draft-job.mjs";` + `writeFileSync` / `readdirSync` import。Test count +1（B2 是替换不是新增；placeholder 测试是新增）。
    - **A1 (Fix)**: `tools/mission-driver/design/draft-robustness-design.md` §4.1 在 "校验失败时..." 段之后插入 "Reject-branch state write (mdr-remediate-3 A1, implementation vs prior WI1 closure)" callout，记录 reject 分支的 failed/rejected 写入契约 + `phase: "rejected"` 语义 + 直接 CLI 路径不变 + 交叉引用本 plan；§1.4 schema 表的 `phase` 行加上 `"rejected"` 值与新 patch-point 引用 (`:381`, mdr-remediate-3 A1)，`status` / `endedAt` / `error` 行加上 WI1 reject patch-point (`:378-385`)，`desc` 行注明 merge 保留语义；§1.4 schema 表所有其它行号刷新到 main.js 当前位置（旧 `:373-382` → 新 `:397-404` 等，因 Phase 1 move 增加 ~23 行）。
    - Live end-to-end probe: 手写 `_tmp/draft-live-test/draft-state.json` = `{status:"running", desc:"d", phase:"brief"}` → `node tools/mission-driver/src/main.js draft "d" --draft-job-dir _tmp/draft-live-test` → exit 1 + resulting state = `{status:"failed", desc:"d" (preserved), phase:"rejected", endedAt:..., error:"description too short..."}`；目录只含 `draft-state.json`。Direct CLI 路径 `node ... draft "d" --dir _tmp/direct-cli-test` → exit 1 + 整个 tmpDir 不出现 `draft-state.json`。
  - Phase 2 — Deferred successor resolution (Option A):
    - **Decision**: 选 Option A——Phase 1 代码修复已消除 stuck-running 失败模式（state 不再撒谎），UI 渲染 `failed` / `rejected` 是 cosmetic residual（UI 已 textual 渲染 `status` 字段；缺专门样式不是 correctness gap）。备选 Option B（保留 `Successor Required: yes` + 加 backlog row）否决——它会保留一个本已关闭的状态机层面的 successor 标记。
    - **A1 (Fix)**: `docs/plans/mission-driver-draft-robustness/2026-07-21-0954-2-cli-draft-desc-validate.md` Deferred But Adjudicated 段的 `Successor Required: yes` → `Successor Required: no — underlying gap closed by mdr-remediate-3 ...`；`docs/backlog/mission-driver-draft-robustness-roadmap.md` 新增 "Follow-up Backlog (post-mission residuals)" 段，含 1 行（priority `P3` cosmetic；trigger = 用户反馈 `failed` 文本被误读为 `running`；acceptance = RunList/RunDetail 视觉区分 failed/rejected vs running，`statusTagType` 扩展覆盖 `failed`）；roadmap `Last Updated` 头部刷新。Grep 验证：`grep -rn "Successor Required: yes" docs/plans/mission-driver-draft-robustness/ docs/backlog/mission-driver-draft-robustness-roadmap.md` 显示剩余的 `yes` 都是无关项（WI4 `--fix`/`--strict` CLI 能力 / WI2 prompt marker 稳定性），无 draft-job 状态机层面的未解 successor。
  - 未改 `startDraftJob` 的 spawn 契约（`stdio: "ignore"` / `detached: true` / `child.unref()` 保持不变——失败信号属 state file 而非 parent pipe，design §4.1 callout 显式声明）；未扩展 `run-reconcile.mjs` 覆盖 `draft-state.json`（producer-side 修已给即时反馈，reconciler extension 是 defense-in-depth 备选，由未来 orphan 案例触发）；未改 monitor draft-job 前端渲染（UI styling 是 watch-only residual，已 backlog 跟踪）；未改 WI2 gate-blocked 路径（`:475` 已正确写 `status: "blocked"`）；未引入新 `draft-state.json` 字段（`status` / `phase` / `error` / `endedAt` 都在其它 call site 已建立）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **515 pass / 0 fail**（baseline 514 = mdr-remediate-2 closure；+1 placeholder rejection test in Case B2），chained `prompt-check: OK — all prompt result-tag examples are well-formed.`；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- mdr-remediate-2 (mission-driver-draft-robustness) closed: 4 个 open-ended-audit findings（A2, A3, A4, A5）收口——verification pipeline chain + draft-desc 黑名单完整覆盖 + `extractBriefGate` 空 reason 规范化 + 多行 reason regression anchor。Test/code side（与 sibling plan `1005-1` 的 doc side 互补；A5 同时锁住 sibling plan F3 doc fix 的 `/is` regex flag）。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1005-2-verification-and-contract-hardening.md` → `Plan Status: completed`。
  - Phase 1 — verification pipeline + 测试覆盖：
    - **A2 (Fix)**: `tools/mission-driver/package.json:11` `"test"` script 从 `"node --test test/*.test.js"` 改为 `"node --test test/*.test.js && node src/prompt-check.mjs"`——`pnpm --prefix tools/mission-driver test` 现在同时跑单元测试与 prompt 结构性校验，任一失败即整体非零退出。同步更新 `tools/mission-driver/CONTEXT.md` "构建与验证" 段注释（命令本身不变，仅补一句说明 chained 行为）。Live negative control 已跑：临时把 `prompts/health-check.md` 的 `<AI_STEP_RESULT>fail</AI_STEP_RESULT>` 改成 typo'd `<AIE_STEP_RESULT>fail</AI_STEP_RESULT>`，`pnpm test` 退出 1（既有 `prompt-markers.test.js` + 新 chained `prompt-check.mjs` 双重捕获），scratch 改动还原后再跑退出 0。
    - **A3 (Add)**: `test/draft-desc-validate.test.js:64-70` Case A `for`-loop 数组从 `["test", "asdf", "xxx", "TODO", "N/A"]` (5 项) 扩展到 `["test", "asdf", "foo", "bar", "todo", "xxx", "none", "null", "n/a", "TODO", "N/A"]` (11 项 = 9 个 regex alternatives + 2 个 `/i`-flag case anchors)。`it()` 标题同步更新为 "all 9 regex alternatives + case-insensitivity anchors (TODO / N/A)"。扩展既有 `for`-loop，无新 `it()` 块，无 +test-count。
    - **A5 (Add)**: `test/brief-gate.test.js` Case A `describe` 末尾新增 1 个 `it()` 块——multi-line reason 测试：`extractBriefGate("<BRIEF_GATE>blocked</BRIEF_GATE><BRIEF_GATE_REASON>line1\nline2</BRIEF_GATE_REASON>")` → `{gate: "blocked", reason: "line1\nline2"}`，锁住 `main.js:187` 的 `/is` regex flag（compounds sibling plan F3 doc side）。Test count +1。
  - Phase 2 — `extractBriefGate` 空字符串规范化：
    - **A4 (Fix)**: `tools/mission-driver/src/main.js:188` 从 `reason: r ? r[1].trim() : null` 改为 `reason: r ? (r[1].trim() || null) : null`——`<BRIEF_GATE_REASON></BRIEF_GATE_REASON>` 与 `<BRIEF_GATE_REASON>   </BRIEF_GATE_REASON>` 都返回 `reason: null`（whitespace-only 原本返回 `""`，contract drift 修正）。Live probe 验证：`node -e "import('./tools/mission-driver/src/main.js').then(m => console.log(m.extractBriefGate('<BRIEF_GATE>x</BRIEF_GATE><BRIEF_GATE_REASON>   </BRIEF_GATE_REASON>')))"` → `{"gate":null,"reason":null}`。无 call-site 改动：三处既有消费方（`main.js:431` destructure / `:436` `writeDraftState({ briefGateReason: reason })` / `:449` `reason || "(no reason)"`）对 `""` 和 `null` 行为一致（均 falsy）。
    - **A4 (Fix | Proof)**: `test/brief-gate.test.js` Case A `describe` 新增 1 个 `it()` 块——whitespace-only reason 测试：`extractBriefGate("<BRIEF_GATE>pass</BRIEF_GATE><BRIEF_GATE_REASON>   </BRIEF_GATE_REASON>")` → `{gate: "pass", reason: null}`（fix 前为 `""`）。Test count +1。
  - 未改 `validateDraftDesc` 黑名单成员（9-entry regex 不变）；未改 `/is` regex 本身（sibling plan F3 doc-side）；未改其它 prompt / prompt-check 行为（仅在 `package.json` test script 链入既有 `lint:prompts`）；未处理 A1（由 sibling plan `1005-3` 拥有）；未引入新 `verify` script 命名约定（Deferred But Adjudicated 选 chained `test`）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **514 pass / 0 fail**（baseline 512 = 510 at plan-write + 2 unrelated landed since；+1 A5 + 1 A4 = 514；A3 在既有 `for`-loop 内扩展 +0 `it()` blocks），chained `prompt-check: OK — all prompt result-tag examples are well-formed.` 也通过；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。注：plan 原写预期 512 是基于 510 baseline；实际执行时 baseline 已演进到 512，最终 514 与 plan 的相对增量（+1 A5 + 1 A4）一致。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- mdr-remediate-1 (mission-driver-draft-robustness) closed: design doc 与 owner-doc sync 修复 7 个 audit findings（F1, F2, F3, F4, F5, A6, A7）——`draft-robustness-design.md` 与 `mission-design.md` 的元数据 / 行号 / 偏差说明与高层概览与已落地的 WI1–WI5 baseline 对齐。Doc-only sync，无代码 / 测试 / prompt / missions 改动。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1005-1-design-owner-doc-sync.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/design/draft-robustness-design.md`:
    - **F1**: line 5 Status 字段从 `proposal (analysis + recommended solution, no code change yet)` 改为 `implemented — §4.1/§4.2/§4.3/§4.4/§4.5 landed via WI1–WI5; see docs/plans/mission-driver-draft-robustness/`。
    - **F2**: §1.1/§1.2/§1.4/§2.1 stale `main.js:` 行号刷新。策略：function-name anchors 主、line numbers 辅。`cmdDraftMission` 函数体改引 `main.js:317-509`（原 `:244-384`）；Stage 1 brief block 改引 `:397-438`（原 `:298-332`）+ 注明 `extractBriefPath` 在 `:160-164`；Stage 2 draft block 改引 `:458-509`（原 `:334-383`）+ 注明 `parseDraftArtifact` 在 `:236-307`；Stage 2 入口注释行号改 `:458`；§1.2 `{{missionsDir}}` resolveTemplateVars 行号改 `:462`（原 `:340`）；§1.4 writeDraftState 闭包定义改引 `main.js:362-372`（原 `:270-289`）；§2.1 `cmdDraftMission` 入口行号改 `:317`（原 `:244`）；§2.1 Commander `draft` 注册改引 `main.js:844-855`（原 `:675`；plan 写的 `:833-844` 实际错位，live grep 验证后用正确范围）。`resolveProjectRoot` (`:76-78`) / `resolveMissionsDir` (`:80-84`) / `extractBriefPath` (`:160-164`) 等仍是原行号，不动。
    - **A6**: §4.1 在 `validateDraftDesc` code block 之后、"阈值 4 是经验值"段之前插入 "Deviation note (A6, implementation vs design)" callout，记录实现用 `empty → placeholder → length` 顺序（design 块写的是 `empty → length → placeholder`），引 `main.js:199-204` JSDoc 解释（design 的顺序让 3 字符黑名单条目 `xxx` / `foo` / `bar` / `n/a` 永远不可达）。
    - **F3**: §4.2.2 `extractBriefGate` code block 内的 `BRIEF_GATE_REASON` 正则从 `/i` 改为 `/is`（匹配实现 `main.js:187`），并在 code block 后加 "Regex flag (F3)" callout 说明 `s` (dotall) 让 `.` 匹配换行符、支持多行 reason；多行分支测试覆盖交叉引用 `2026-07-21-1005-2-verification-and-contract-hardening.md` A5。
    - **F5**: §1.4 从单段散文扩为 schema 表，枚举 `draft-state.json` 全部 14 字段（`status` / `phase` / `startedAt` / `endedAt` / `desc` / `flowHint` / `targetFile` / `briefPath` / `briefGate` / `briefGateReason` / `missionName` / `roadmapPath` / `missionFile` / `error`），每行附 patch-point 行号引用（`main.js:362-382,417-422,432-437,451-453,495-506`）+ 消费方 `src/monitor.js`。`CONTEXT.md` 不重复此 schema（已交叉引用本设计文档）。
  - Phase 2 — `tools/mission-driver/design/mission-design.md`:
    - **Decision (F4 + A7)**: 选 **Option A**（honored the promise）——编辑 `mission-design.md §9`，不动 roadmap cross-cutting row。理由：Option A 兑现显式承诺、保持高层 doc 对冷读者可用；Option B（"deferred"）会让 §5 Two-Phase Usage 指向的 §9 仍读作单段式。Residual risk：未来 pipeline 演化可能让 §9 再度漂移——由发现本次 finding 的同一 audit prompt 兜底。
    - **F4 + A7 (Fix)**: §9 在原 7 步列表之前新增一段 brief→draft 两段式管线描述：(1) Stage 1 brief + Stage 2 draft 分工，(2) `<BRIEF_GATE>pass|blocked</BRIEF_GATE>` + `<BRIEF_GATE_REASON>` marker 契约 + 引擎 `extractBriefGate` 强制（blocked 时 `draft-state.json` 标 `status: "blocked"`），(3) `{{backlogDir}}` / `{{missionsDir}}` 模板变量统一锚定 projectRoot（消除字面量相对路径与绝对路径 split-brain），(4) `--skip-brief` 退化为单段式向后兼容，(5) 交叉引用 `draft-robustness-design.md` 为 controlling owner doc。Roadmap cross-cutting row (`:226`) 不动——Option A 不编辑 roadmap；承诺已通过 §9 编辑兑现，与 WI2/WI3 plan closures 的矛盾消解。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 512 pass / 0 fail（baseline 不变，无代码改动）；`pnpm --prefix tools/mission-driver run lint:prompts` → OK（无 prompt 改动）。
  - Scope proof: `git diff --stat tools/mission-driver/design/draft-robustness-design.md tools/mission-driver/design/mission-design.md` → 仅这两个 design doc 受本 plan 影响（`+41 / -12`）；其余 working-tree 未提交改动（`src/main.js` / `flows/mission-driver.json` / `test/from-step.test.js` / `design/step-execution-and-audit-count-design.md` / `docs/logs/2026/07-21.md` 自身）属 sibling plans / sessions 的 in-progress 工作，与本 plan 无关。
  - 未改任何代码 / 测试 / prompt / missions 文件（Non-Goal）；未重开 WI1–WI5 契约；未把历史迁移 prose 倒灌进 `docs/architecture/`；未处理 A1 / A2–A5（分别由 sibling plans `1005-3` / `1005-2` 拥有）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI5 (mission-driver-draft-robustness) closed: forEach 子流程每完成一项即增量追加 `subflowRuns` 到主 `run-state.json` 的 placeholder entry，父进程中途被杀时 run-state 仍反映已完成项（设计文档 §2.6 / §4.5 / 方案 E）。monitor 侧渲染（`mergeSubflowChildren` 的 `status === "running"` gate）已在 commit 06749fa 修固，本 WI 让 run-state.json **自包含**——`--analyze` / `git show` 等直读消费方不再依赖 monitor fallback 扫描磁盘 `run-state-<stepName>-<visits>-<i>.json` 文件。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1207-2-subflow-runs-incremental.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/engine.js`:
    - 紧邻 `_onAgentStepUpdate`（`:438-449`）之后新增 `_wfAppendSubflowRun(stepName, visits, run)` 方法（直接采用设计文档 §4.5.1 snippet）。镜像 `_onAgentStepUpdate` 的"找 running 记录 + patch + `_writeWorkflow`"模式，**关键差异**：用 `name + visits + status==="running"` 三元组定位（`_onAgentStepUpdate` 仅 `name + status==="running"` 二元组）——同一 stepName 可能被 re-entry 多次，不同 visit 都 `status: "running"` 时只用 stepName 会写错 entry。从 `workflow.steps` 末尾向前遍历（找最近一个匹配）；`if (!Array.isArray(steps[i].subflowRuns)) steps[i].subflowRuns = [];`（防御）；push 后 `this.workflow.updatedAt = new Date().toISOString(); this._writeWorkflow(); return;`；`if (!this.workflow) return;` 早 return；找不到匹配 entry 静默 return。
    - `_executeSubflowStep` concurrency=1 路径 `subflowRuns.push(...)` 之后（`:1035`）+ sliding-window 路径 `recordResult(r)` 里 `subflowRuns.push(...)` 之后（`:1066`）各加一行 `this._wfAppendSubflowRun(stepName, visit, subflowRuns[subflowRuns.length - 1]);`——用 `subflowRuns[subflowRuns.length - 1]`（刚 push 的同对象引用）而非再构造一份，确保 sort 时局部数组与磁盘引用一致。
  - Phase 2 — 新增 `tools/mission-driver/test/subflow-incremental.test.js`（1 describe / 6 tests，结合 `forEach-concurrency.test.js:1-70` 的 mock `_runChildSubflow` 范式 + `audit-count.test.js:46-57` 的 `mkdtempSync` + `config: { projectRoot: runDir, runDir }` + `readFileSync(join(runDir, "run-state.json"))` 范式；用 `withDir` wrapper 自动清理 tmpdir）：
    - Case A（concurrency=1 父中断反映已完成项）：mock forEach=3，concurrency=1，第 3 项（forEachIndex=2）入口同步读磁盘快照；断言快照 `subflowRuns.length === 2`（前 2 项已增量写入）、step `status === "running"`、记录 `[0,1]` forEachIndex 序；run 结束后最终 `subflowRuns.length === 3` 且 forEachIndex 序（`_wfClose` 覆盖）。
    - Case B（concurrency=2 sliding-window）：mock forEach=3，delays `[10, 10, 200]`，第 3 项 mock **先 `await delay(200)` 再读快照**（同步入口读会撞 microtask 调度边界——item 2 的 mock 入口恰在 item 0 的 `.then` 链里同步调度、item 1 的 `recordResult + _wfAppendSubflowRun` 要等下一个 microtask）；断言快照 `length === 2`、forEachIndex 集合 `{0,1}`；最终 `length === 3` forEachIndex 序。
    - Case C（多 visit 不串味）：flow `START (loop) → SUB visit=1 → START (loop) → SUB visit=2 → START (end) → done`；SUB visit=2 第 1 项入口读快照——visit=1 placeholder `status === "completed"` + 2 条 subflowRuns（已被 `_wfClose` 替换），visit=2 placeholder `status === "running"` + `subflowRuns.length === 0`（增量写不会把 visit=2 记录误写到 visit=1 的 entry）；visit=2 第 2 项入口读快照——visit=2 placeholder `subflowRuns.length === 1`（item 0 已增量追加），visit=1 closed 记录仍 `length === 2`（**visits 匹配的关键性**——防回归到只用 stepName 匹配）。
    - Case D（最终覆盖语义不回归）：mock forEach=3，delays `[100, 10, 10]`（resolve 序 `[1,2,0]`）；断言最终 `subflowRuns` 严格 forEachIndex 序 `[0,1,2]`（`_wfClose` 用 sort 后的局部数组覆盖，不被增量期间的 resolve 序污染）。
    - Case E（`_wfAppendSubflowRun` 找不到匹配 entry 时静默 no-op）：手动构造 `engine.workflow = { steps: [{ name: "OTHER", ... }] }`、直接调 `engine._wfAppendSubflowRun("MISSING", 1, {...})`；`assert.doesNotThrow` + `workflow` 不变。
    - Case F（grep 锚点）：`readFileSync` 读 `src/engine.js`，断言 `match(/_wfAppendSubflowRun/g).length >= 3`（方法定义 1 + concurrency=1 调用 1 + sliding-window 调用 1 = 至少 3 处，防 Phase 1 改动被部分回退）。
  - Phase 3 — `tools/mission-driver/CONTEXT.md` "关键约束" 段末尾新增一条 `draft-robustness WI5`：说明 `subflowRuns` 增量持久化（`_wfAppendSubflowRun` 镜像 `_onAgentStepUpdate` 但加 `visits` 匹配）、`_wfClose` 仍是最终真相、与 step-audit mission 的 WI5（`auditRound` / `maxAuditRounds`，见上方 Monitor Dashboard 段）同名但分属不同 mission。
  - 未改 `_wfClose` 的 meta 字段或调用点（`:1575` / `:1799` 保持不变，最终覆盖语义不变）；未改 `_wfOpen` placeholder 初始字段（`subflowRuns: []` 初始化不变）；未改 monitor.js 的 `mergeSubflowChildren`（已在 commit 06749fa 修固，本 plan 让 run-state.json 自包含后 monitor 仍走 fallback 扫描，不删除兜底）；未改单 subflow 路径（无 forEach，return 时即写主 run-state，不需要增量）；未改 forEach-concurrency / 顺序逻辑（只加磁盘写、不动调度）；未引入 metrics / 计数；未引入新 npm 依赖（`_writeWorkflow` / `basename` 已在 engine.js 顶部 import 或同模块可用），保持引擎核心零依赖约束。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 510 pass / 0 fail（WI2 baseline 504 + Phase 2 新增 6）；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Roadmap: `docs/backlog/mission-driver-draft-robustness-roadmap.md` WI5 → `done`（M4 milestone 现已全部完成，整 mission draft-robustness 五个 WI 全部交付）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI2 (mission-driver-draft-robustness) closed: brief "是否放行"判定从 prompt 文字层升级为结构化 `<BRIEF_GATE>pass|blocked</BRIEF_GATE>` marker，引擎据 marker 决定是否进 Stage 2（设计文档 §4.2 / 方案 B）。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1207-1-brief-gate-marker.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/main.js`:
    - 紧邻 `extractBriefPath`（`:160-164`）新增 `extractBriefGate(resultText)` 纯函数 + JSDoc：正则 `/<BRIEF_GATE>\s*(pass|blocked)\s*<\/BRIEF_GATE>/i` + `/<BRIEF_GATE_REASON>\s*(.+?)\s*<\/BRIEF_GATE_REASON>/is`，返回 `{ gate, reason }`；输入非字符串 → `{ gate: null, reason: null }`；marker 缺失 → `gate: null`（向后兼容）；reason tag 缺失 → `reason: null`（不抛错）；非法 gate 值（如 `unknown`）正则不匹配 → `gate: null`。直接采用设计文档 §4.2.2 snippet。
    - 底部 `export { cmdDraftMission, parseDraftArtifact };` → `export { cmdDraftMission, parseDraftArtifact, extractBriefGate };`，注释说明供 Phase 4 测试直接 import。
  - Phase 2 — `tools/mission-driver/prompts/mission-brief.md`:
    - 在 `## Task` 与 `## Output` 之间新增 `## Brief Gate` 段，写明 pass / blocked 判定规则 + 正向（`"add audit count to dashboard"`）与反向（`"optimize"` 单词）示例；明确 blocked 时引擎不进 Stage 2、用户需读 brief 非目标 / open questions 后重跑 draft。
    - `## Output` 代码块从单行 `<BRIEF_FILE>` 扩展为三行：`<BRIEF_FILE>` + `<BRIEF_GATE>pass|blocked</BRIEF_GATE>` + `<BRIEF_GATE_REASON>一句话说明（blocked 时必填、pass 时可空 tag）</BRIEF_GATE_REASON>`。
    - 既有 7 个 `##` section 列表、`{{backlogDir}}` 模板变量、`<BRIEF_FILE>` marker 不动。
  - Phase 3 — `tools/mission-driver/src/main.js` `cmdDraftMission`:
    - Draft Review iteration 1 修复（外层作用域）：在 `let briefPath = null;` 之后新增 `let gate = null;` + `let reason = null;`（**外层** `let`，不在 `if (!skipBrief)` 块内——否则 gate 分支引用会 `ReferenceError`）。`skipBrief === true` 时 Stage 1 整段跳过、`gate` 保持 null、gate 分支不进、控制流落到 Stage 2（向后兼容单段式，与设计 §5.3 一致）。
    - Stage 1 结束段（`briefPath = extractBriefPath(...)` 之后）改写：用**括号赋值解构** `({ gate, reason } = extractBriefGate(briefResult.text));`（不是 `const` 重新声明——外层已 `let` 声明，赋值给外层）；`writeDraftState` patch 加 `briefGate: gate, briefGateReason: reason` 字段（无论 pass / blocked / null 都写，区分"Stage 1 跑了但 AI 没输出 marker"与"Stage 1 没跑"）。
    - 在 `if (!skipBrief) { ... }` 闭括号之后、`// ── Stage 2: draft` 注释之前插入 gate 分支：`if (gate === "blocked")` → `console.log("[BRIEF GATE] blocked: ...")` + `Brief written to ...`、`writeDraftState({ status: "blocked", endedAt })`、`await runner.close(); return;`。**不调** `runner.runAgent("draft-mission", ...)`、**不写** mission.json / roadmap。`process.exitCode` 不设（默认 0；gate blocked 是正常工作流，与 WI1 `validateDraftDesc` 失败的 `exitCode = 1` 区分）。
  - Phase 4 — 新增 `tools/mission-driver/test/brief-gate.test.js`（6 describe / 12 tests，沿用 `draft-brief.test.js` 的 `__setRunnerFactoryForTest` + `makeFakeRunner` + `makeTmpProject` 范式）：
    - Case A（纯函数层，7 例）：pass / blocked+reason / 大小写+前后空白 / 无 marker / 非字符串（undefined / null / 123）/ blocked 无 reason tag / 非法 gate 值。
    - Case B（集成 gate=blocked）：mock runner 只调 1 次（mission-brief）、`exitCode` 保持 undefined、`draft-state.json` `status === "blocked"` + `phase === "brief_done"` + `briefGate === "blocked"` + `briefGateReason === "desc too vague"` + `endedAt`；不含 `missionName` / `missionFile`；stdout 含 `[BRIEF GATE] blocked`。
    - Case C（集成 gate=pass）：mock runner 调 2 次（brief + draft）；`briefGate === "pass"` + `phase === "completed"` + `status === "completed"`。
    - Case D（集成 gate=null 向后兼容）：mock runner 调 2 次（Stage 2 照跑）；`briefGate === null` + `phase === "completed"` + `status === "completed"`。
    - Case E（`--skip-brief` 不回归）：mock runner 只调 1 次（draft-mission）；`briefGate` 字段不存在或 === null（Stage 1 整段跳过，gate 永远是 null）。
    - Case F（prompt grep 锚点）：`readFileSync` 读 `prompts/mission-brief.md`，断言含 `<BRIEF_GATE>` 与 `<BRIEF_GATE_REASON>` 字面量，防 Phase 2 改动被回退。`PROMPTS_DIR` 用 `fileURLToPath(import.meta.url)` 派生（避免 `process.cwd()` 在 `pnpm --prefix` vs repo-root 直跑两种入口下不同）。
  - 未改 monitor draft-job 前端（新字段对旧 UI 透明，UI 升级非阻塞；设计 §4.2.3 / §5.2 已声明）；未改 `--skip-brief` 单段式路径（设计 §5.3：gate 机制不介入）；未改 brief gate 的 AI 判定逻辑本身（prompt 里给规则）；未在引擎里读 brief 文件内容做 grep（设计 §6.2 已否决：依赖措辞、脆）；未让 `gate === null` 强制阻塞（向后兼容）；未在 Stage 1 之前加 gate（WI1 范围）；未改其它子命令；未引入新 npm 依赖（纯正则 + 字符串处理），保持引擎核心零依赖约束。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 504 pass / 0 fail（WI3 baseline 492 + Phase 4 新增 12）；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。（注：`test/monitor.test.js:339 retries +1 on port conflict (EADDRINUSE)` 在高并发全量套件下偶发 Windows EACCES——动态端口撞上 Windows 临时端口段权限问题，重跑通过，与本 WI 改动无关。）
  - Roadmap: `docs/backlog/mission-driver-draft-robustness-roadmap.md` WI2 → `done`（M1 milestone 现已全部完成）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI3 (mission-driver-draft-robustness) closed: draft/brief 管线路径双轨制消除——`{{missionsDir}}` 走模板变量（projectRoot 锚定），`docs/backlog/` 走字面量（agent cwd 锚定）的双基准散落问题统一为 `{{backlogDir}}` 模板变量；`parseDraftArtifact` 加防御 warn。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-0954-3-draft-path-template-var.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/main.js` Stage 1 brief 渲染（~:355）+ Stage 2 draft 渲染（~:398）的 `resolveTemplateVars` 调用新增 `backlogDir: resolve(resolved.projectRoot, "docs/backlog")` 与现有 `missionsDir / projectRoot / flowHint / ...` 同列；`tools/mission-driver/prompts/mission-brief.md`（`:13` / `:27` / `:30`）三处字面量 + `tools/mission-driver/prompts/mission-draft.md:13` 一行内**三处**字面量（含设计 §4.3.2 遗漏的 `00-roadmap-authoring-guide.md` 引用）全部替换为 `{{backlogDir}}/`；`{{missionsDir}}/{mission-name}.json` 不动。
  - Phase 2 — `tools/mission-driver/src/main.js`:
    - `import { resolve, dirname } from "node:path";` → `import { resolve, dirname, relative, isAbsolute } from "node:path";`（同 import 行补两个 helper，无新增 import 行）。
    - `parseDraftArtifact` `<MISSION_FILE>` 命中分支（`out.missionFile = file;` 之后、`return out;` 之前）加 15 行 warn：`const rel = relative(resolve(missionsDir), resolve(dirname(file)));` + `if (rel.startsWith("..") || isAbsolute(rel))` → stderr `[WARN] mission.json landed outside expected missionsDir: ...`。**不强制失败**，配合 Phase 1 后正常情况下不再触发；**用 `relative + starts-with("..")` 而非 `startsWith`** 避免 `/foo/bar` vs `/foo/barbaz` 前缀诡计与 Windows drive-letter 大小写歧义。
    - 底部 `export { cmdDraftMission };` → `export { cmdDraftMission, parseDraftArtifact };`，注释说明供 Phase 3 测试直接调用。
  - Phase 3 — 新增 `tools/mission-driver/test/draft-path-consistency.test.js`（6 tests，沿用 `draft-brief.test.js` 的 `__setRunnerFactoryForTest` 范式）：
    - Case A（模板变量注入）：mock runner 截获 Stage 1/2 prompt，断言无 `{{backlogDir}}` 残留 + 含 `resolve(root, "docs/backlog")` 绝对路径。
    - Case B（projectRoot ≠ 仓库根 一致性）：`projectRoot=<tmpDir>/sub`，断言 brief + draft prompt 里 backlog 路径都解析到 `<tmpDir>/sub/docs/backlog`、draft 里 missions 路径同根；负向断言不泄漏到 outer-root 的 `docs/backlog`（split-brain guard）。
    - Case C（前缀诡计 warn）：写真实 tmpfile 到 `<tmp>/expected/missions-but-also-extended/x.json`、missionsDir=`<tmp>/expected/missions`；`parseDraftArtifact` 直接调用，断言 stderr 含 `[WARN] mission.json landed outside expected missionsDir`、返回 `missionFile` 为该路径（验证 `relative+startsWith("..")` 严格识别 `/foo/barbaz` vs `/foo/bar` 边界——`startsWith` 会误判为 inside）。
    - Case D（正常路径无 warn）：file 落在 `<missionsDir>/x.json`，断言 stderr 为空字符串、`missionFile` 正确返回。
    - Case E（grep 锚点）：`readFileSync` 读 `prompts/mission-brief.md` + `prompts/mission-draft.md`，断言 `match(/docs\/backlog\//g) === null`。测试锚点用 `fileURLToPath(import.meta.url)` 派生 prompts dir，避免 `process.cwd()` 在 `pnpm --prefix tools/mission-driver test` 与 repo-root 直跑两种入口下不同导致 path-doubling。
  - Owner-doc 同步：`tools/mission-driver/design/draft-robustness-design.md` §4.3.2 补回遗漏的 `00-roadmap-authoring-guide.md` 字面量替换说明（一行内三处）；§4.3.3 warn snippet 从 `startsWith` 改为 `relative + starts-with("..")` 与实施一致。
  - 未改 `run` 命令路径解析（已正确）、未改 `flow-loader.js` 扫描逻辑、未引入 `plansRoot` 模板变量（设计 §4.3.1 旁注列为可选，当前两份 prompt 未引用 `{{plansRoot}}`，预添加属 dead code）、未强制对齐 projectRoot 到仓库根（设计 §4.3.4 已否决）、未引入新 npm 依赖（`relative, isAbsolute` 来自 `node:path`），保持引擎核心零依赖约束。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 492 pass / 0 fail（Phase 2 baseline 486 + Phase 3 新增 6）；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Roadmap: `docs/backlog/mission-driver-draft-robustness-roadmap.md` WI3 → `done`（M2 milestone 完成）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI1 (mission-driver-draft-robustness) closed: `cmdDraftMission` 入口加确定性 desc 校验（空 / 占位 / 过短三段式），挡住 `draft "d"` 这类明显垃圾输入。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-0954-2-cli-draft-desc-validate.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/main.js`:
    - 新增 `export function validateDraftDesc(desc, minLen)`（紧邻 `extractBriefPath` `:160-164` 一带）：empty → placeholder → too-short 三段式 + `Number.isFinite(+minLen) && +minLen > 0 ? +minLen : 4` 默认兜底。
    - **相对设计 §4.1 的可控偏差**：占位检查移到长度检查之前。设计 §4.1 的 `empty→length→placeholder` 顺序让 3 字符占位词（`xxx` / `foo` / `bar` / `n/a`）永远撞 length<4 兜底，黑名单形同虚设；本 plan 的 Case A 测试明确要求这些占位词命中 `placeholder` reason。调换顺序后 `placeholder` 是比 `too short` 更 actionable 的拒绝原因。偏差理由已写进函数 JSDoc。
    - 在 `cmdDraftMission` 入口（`resolveConfig` 之后、`:281` running re-affirm 块之前）加 5 行 + 注释：直接 `JSON.parse(readFileSync(missionsDir/base.json))` 读 `draft.minDescLength`（镜像 `monitor.js handleGetBaseConfig` 既有范式，绕开 `loadMission` 对 base.json 必填字段的拒绝；try/catch 兜底 `{}`）；校验失败时 `console.error("[DRAFT VALIDATION] " + reason)` + hint、`process.exitCode = 1`、`await runner.close()` + `return`——**不**进 Stage 1、**不**写 `draft-state.json` running re-affirm。
  - Phase 2 — 新增 `tools/mission-driver/test/draft-desc-validate.test.js`（10 tests，沿用 `draft-brief.test.js` 的 `__setRunnerFactoryForTest` 范式）：
    - Case A（纯函数层，7 例）：`""` / `"   "` → empty；`"d"` → too short；`"test"` / `"asdf"` / `"xxx"` / `"TODO"` / `"N/A"` → placeholder；`"add audit count"` → ok；`("add", 8)` → too short（阈值覆盖）；`("add audit count", "garbage")` → ok（非有限数回退默认 4）；`("ad", "garbage")` → too short（并行锁住：非有限数回退**默认 4** 而非无限制）。
    - Case B1（集成层，无 draftJobDir）：注入 mock runner，调 `cmdDraftMission("d", { dir: tmpDir })`，断言 `runAgent` 未被调用 + `exitCode === 1` + stderr 含 `[DRAFT VALIDATION]` + reason + hint。
    - Case B2（集成层，draftJobDir）：调 `cmdDraftMission("d", { dir: tmpDir, draftJobDir: "sub/dj" })`，断言 `runAgent` 未调用 + `exitCode === 1` + `draft-state.json` 不含 cmdDraftMission 写的 running re-affirm（校验早于 re-affirm 块 return）。
    - Case C（合法路径不回归）：调 `cmdDraftMission("add audit count", { dir: tmpDir, skipBrief: true })`，断言 mock runner 的 `runAgent` 被调用过（Stage 2 跑了）。
  - 回归调整：`test/draft-brief.test.js:187` 的 `"skipBrief draft failure writes failed state"` 测试用例原本用 `"x"` 作 desc，被 WI1 新校验合法拦下；改为 `"fail draft"`（仍是任意会触发 draft agent throw 的 desc，但长度 ≥ 4）。
  - 未改 brief gate 引擎强制（WI2 范围）、未改路径解析（WI3 范围）、未引入 AI 语义判断（设计 §6.1 已否决）、未改 commander `<description>` argument required 语义、未引入新 npm 依赖（纯正则 + 字符串处理），保持引擎核心零依赖约束。
  - Windows 手动验证：`node tools/mission-driver/src/main.js draft "d"` → exit 1 + "too short"；`draft "test"` → exit 1 + "placeholder"；`draft "xxx"` → exit 1 + "placeholder"。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 486 pass / 0 fail（Phase 1 baseline 476 + Phase 2 新增 10）；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Roadmap: `docs/backlog/mission-driver-draft-robustness-roadmap.md` WI1 → `done`（M1 milestone 进度 1/2）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI4 (mission-driver-draft-robustness) closed: 修固 `mission-check.mjs` 独立 CLI 入口在 Windows 静默 no-op 的平台缺陷。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-0954-1-mission-check-cli-cross-platform.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/mission-check.mjs`: 顶部新增 `import { pathToFileURL } from "node:url";`（:11）；入口判断（:107）从 `import.meta.url === \`file://${process.argv[1]}\`` 改为 `process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href`。`pathToFileURL` 把任意平台路径规范成与 `import.meta.url` 同形的 `file:///` URL（三斜杠前缀），三平台一致；`process.argv[1] &&` 短路防护 REPL / `node -e` / `node -` / Electron main 等无 argv[1] 宿主下 `pathToFileURL(undefined)` 抛 `ERR_INVALID_ARG_TYPE`。
  - Phase 2 — 新增 `tools/mission-driver/test/mission-check-cli.test.js`（6 tests，镜像 `from-step.test.js:24,117` 的 spawnSync + `process.execPath` 范式）：Case A 坏 mission（缺 `name`）→ exit 1 + stderr "missing required field"；Case B `plansDir` 不存在 → exit 1 + stderr "does not exist"；Case C 合法 mission → exit 0 + stdout `"valid": true`；Case D `pathToFileURL` 规范化单元锚点（Windows 路径 → `file:///C:/...` 三平台同形；反例证明旧 `file://C:\...` 两斜杠拼接为假）。执行期发现 plan 原文"POSIX 路径在三平台同形"不准确（Windows 上 `/abs/...` 按当前盘符解析为 `file:///C:/abs/...`），Case D 第二断言改为平台条件 + 三斜杠 invariant 兜底，仍满足 Exit Criteria"D 在任意平台绿"且不弱化回归锚点。
  - Windows 手动验证：`node tools/mission-driver/src/mission-check.mjs`（无参）→ exit 2 + Usage（修复前静默 exit 0）；`node ... <bad.json> <root>` → exit 1 + stderr 报错（修复前静默 exit 0）。CLI 主体现真正执行。
  - 未改 `validateMission` / `loadMission` 校验逻辑，未改 `run`/`list` 函数调用路径，未引入新 npm 依赖（`node:url` 内置）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 476 pass / 0 fail；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。（注：`from-step.test.js` Case 3 在高并发全量套件下偶发 15s 超时——subprocess 启动受 CPU 调度影响，隔离运行 820ms 通过，与本 WI 改动无关。）
  - Roadmap: `docs/backlog/mission-driver-draft-robustness-roadmap.md` WI4 → `done`。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

- WI5 (mission-driver-step-audit) closed: events/日志/monitor/postmortem audit 计数可观测性收尾 + WI2 deferred `_finalizeWorkflow` step-level status 修复。
  - Plan: `docs/plans/mission-driver-step-audit/2026-07-20-1559-2-audit-count-observability.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/src/engine.js` `run()` (~:1411-1432): `_log("[step N] X (visit #M)")` 行**移到 `_wfOpen` 之后**（紧挨 `_emitEvent` 之前）以确保 auditEntry 步骤读到的是已递增的"当前轮次"值；新增 `isMainAuditEntry` 守卫（`currentStep === auditEntry && cfg.isSubflow !== true`），命中时 log 行追加 ` (audit round N/M)` 后缀；`_emitEvent("step_started", ...)` payload 用 spread `...(isMainAuditEntry ? { auditRound, maxAuditRounds } : {})` 追加两字段（子流程内部步骤零侵入，旧 reader 向后兼容）。
  - Phase 2 — `tools/mission-driver/src/monitor.js`: `summarizeRun` (:337-354) 返回对象追加 `auditRound: state.auditRound ?? 0` 与 `maxAuditRounds: state.maxAuditRounds ?? 0`（旧 run-state 兜底）；`synthesizeFromEvents` (:314-335) 由 events 合成的兜底 state 也补默认值 `auditRound: 0, maxAuditRounds: 0`，避免旧 run 无 run-state.json 时前端 undefined。`handleGetRun`（:506-524）无须改动（`state` 是 `readRunState` 原对象，天然含字段）。
  - Phase 3 — 前端：
    - `web/src/types/run.ts` — `Run` (:27-48) 与 `RunSummary` (:51-68) interface 都加 `auditRound?: number` / `maxAuditRounds?: number`（可选修饰符；旧 run 兜底 0）。
    - `web/src/components/run/detail/DefaultRunDetail.vue` (:25-32) — 右侧 content 区顶部（MissionConfig 之前）新增 `<n-tag>` Deep Audit 展示，仅当 `(run?.maxAuditRounds ?? 0) > 0` 时渲染；`auditRound >= maxAuditRounds` → tag type `success`，否则 `info`。`NTag` 已加入 naive-ui import 段。
    - `web/src/views/RunList.vue` `statusTagType` (:201-210) 与 `web/src/components/layout/AppHeader.vue` `statusTagType` computed (:64-73) 同步加 `single_step_done → 'success'` 分支（与 `main.js` exitMap exit code 0 对齐，避免 RunList 与 RunDetail header 显示不一致）。
  - Phase 4 — postmortem skeleton 注入 audit round：
    - `tools/mission-driver/src/config.js` `buildRunSkeleton` (:217-223) — state 段末尾仅当 `state.maxAuditRounds > 0` 时 push `Audit rounds: ${state.auditRound ?? 0}/${maxAuditRounds}`（旧 run `?? 0` 兜底）。
    - `tools/mission-driver/prompts/run-postmortem.md` (:31-35) — `<run_skeleton>` 段附近加 WI5 提示性说明：区分 audit-gate vs `maxAuditRounds` 闸门触发完成的检查路径（看 events.jsonl 的 `via: "audit_gate"` vs `limitType: "max_audit_rounds"`）。
  - Phase 5 — WI2 deferred 项决策与修复：**选择 A**（修正 `_finalizeWorkflow` 把 `single_step_done` step 记录错误标 `failed`）。理由：`single_step_done` 在 `main.js:643` exitMap 已映射到 exit code 0（视同成功），step 级 status 应与之一致；events.jsonl 仍保留 `single_step_done` 顶层 status 字段，故事件层契约不变。
    - `tools/mission-driver/src/engine.js` `_finalizeWorkflow` (:413-425) — 三元改为 `(status === "completed" || status === "single_step_done") ? "completed" : "failed"`；workflow 顶层 status 仍保留原值以区分单步调试 vs 真完成。
    - `tools/mission-driver/test/single-step.test.js` Case 1 (:83-90) — 新增 WI5 断言：`workflow.steps` 中那条单步记录的 `status === "completed"`（而非 failed），且 `workflow.status === "single_step_done"`（顶层 status 保留）。
  - Phase 6 — `tools/mission-driver/CONTEXT.md` Monitor Dashboard 前端 / 关键 UI 交互 段加一条 WI5：说明 `GET /api/runs/:id` 返回 `auditRound` / `maxAuditRounds`、RunDetail 顶部 Deep Audit tag、RunList/AppHeader status tag 同步识别 `single_step_done` 为 success。
  - 测试覆盖：`test/analyze-run.test.js` 已有 WI5 skeleton 三例（emits / omits when 0 / legacy fallback）；`test/single-step.test.js` 已有 WI5 step-level status 断言；`test/audit-count.test.js`（WI1）覆盖 `auditRound` 持久化与 gate 边界，未断言精确字段集合，无需更新。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → 470 pass / 0 fail；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` built；`pnpm --prefix tools/mission-driver run lint:prompts` OK。
  - Roadmap: `docs/backlog/mission-driver-step-audit-roadmap.md` WI5 → `done`（M4 milestone 现已全部完成）。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback)。

### 2026-07-21 — DEEP_AUDIT→DRAFT_PLANS 历史回归修复（`complete` transition 路由错）

- 现象：`mission-driver-draft-robustness` run（`_tmp/2026-07-21-095220-mission-driver`，PID 4396）连续跑了 3 次 DEEP_AUDIT，每次之间 DRAFT_PLANS 都返回 `nothing`，audit 创建的 4 个 `active` remediation plans 从未被 EXEC_PLANS 执行。
- 根因（git 考古 + plan 状态证据）：
  - `tools/mission-driver/flows/mission-driver.json:88` 当前是 `"complete": { "goto": "DRAFT_PLANS" }`。
  - `git blame` + `git show 3447742`（2026-07-01）显示这条 transition **曾经被正确改成 `REVIEW_PLANS`**（commit message 明说 "AUDIT 完成后转 REVIEW_PLANS"）；`git show 0c763f0`（2026-07-14 "migrate evolved engine..."）大迁移时**无意回退成 `DRAFT_PLANS`**，diff 里有这行改动但 commit message 完全没提——典型大重构夹带回归。
  - 空转机制：DEEP_AUDIT 子流程的 `SCAN_NEW_RESULTS` 用 `draft-from-audit.md` 起草 plan 并自行 sub-agent review 推到 `active`（visit 1 log 原文："3 plans promoted to active after independent sub-agent consensus"）。主流程回 DRAFT_PLANS，但 DRAFT_PLANS 用 `draft-from-roadmap.md` 只查 roadmap、看不到 active plans → 返回 `nothing` → `nothing → DEEP_AUDIT` 回审计 → 审计重新发现同样的 14 个未解 issue → 起草新 plan → 回 DRAFT_PLANS → 又 `nothing` → …。唯一兜底是 `maxAuditRounds=3`，audit_gate（`_shouldCompleteOnAuditQuota` 要求 active plans + open audits 同时为 0）因每次都制造新 active plan 而永不触发。
  - plan 状态证据：`docs/plans/mission-driver-draft-robustness/` 下 7 个 plan，`1005-1/2/3`（visit 1 创建）+ `1523-1`（visit 2 创建）4 个是 `active`、从未被执行；`1605-1/2/3`（visit 3 创建）3 个是 `draft`。EXEC_PLANS 上次执行是 visit 3（结束于 06:08:58 UTC），早于所有 DEEP_AUDIT visit。
- 修复：`flows/mission-driver.json:88` 改回 `"complete": { "goto": "REVIEW_PLANS" }`。不动 `SCAN_NEW_RESULTS`——`REVIEW_PLANS` 作为兜底：`forEach = draftPlans()`，若 deep-audit-loop 已把 plan 推到 `active`，`draftPlans()` 为空，engine.js:942-944 返回 `marker: "all_complete"` 直通 `EXEC_PLANS`；若仍有 `draft` plan，正常评审。`step-execution-and-audit-count-design.md:43` 同步更新（原文 "跑完后无条件回到 DRAFT_PLANS" 改为 REVIEW_PLANS + 兜底语义 + 回归说明）。
- Verification (full green): `pnpm --prefix tools/mission-driver test` → 510 pass / 0 fail（与 baseline 一致；测试用合成 flow，不锁 transition）。
- 运行中引擎兼容性：PID 4396 启动时已把 flow 加载进内存，本次改 JSON 文件不影响当前 run（它会在 DEEP_AUDIT visit 3 结束后继续走旧的 `→ DRAFT_PLANS` 路径，第 4 次进 DEEP_AUDIT 时被 `maxAuditRounds=3` gate 短路 → completed）。下次重启 run 才会拾取修复。
  - 三个候选方向（A=EXEC_PLANS、B=REVIEW_PLANS+不动 SCAN_NEW_RESULTS、C=DRAFT_PLANS 增强）按用户决策走 B：REVIEW_PLANS 是兜底，不强制由它改状态，符合"审计起草已完成、主流程只走评审/执行路由"的语义。

### 2026-07-21 — `run` 子命令 option-stripping 修复（commander 15.0.0 + `enablePositionalOptions`）

- 现象：用户跑 `./tools/mission-driver.sh run mission-driver-draft-robustness --step EXEC_PLANS`，引擎从 CHECK 起步、`--step` 被静默丢弃（`_tmp/2026-07-21-171445-mission-driver/run-state.json`：currentStep=REVIEW_PLANS，从 CHECK 起步；`mission-driver-draft-robustness.log:1` 是 `[step 1] CHECK (visit #1)` 而非 `Step: EXEC_PLANS (single-step mode)`）。
- 根因：commander 15.0.0 的 option-stripping quirk——同一个 option 名（`--step`/`--from-step`/`--no-monitor` 等）同时声明在 `program.command("run")`（`main.js:864-888`）和 `program` 主命令（`main.js:892-920`）上时，调用子命令会静默丢失 option。最小复现：`program.command("run").option("--step").action((m,o)=>log(o.step))` + 主命令同名 option → `run X --step Y` 进 action 收到 `step: undefined`。commander 在 npm 上的 latest tag 就是 15.0.0，**没有 16+ 可升**（用户最初要求"升级到 16+"不可达），所以改用 commander 自己的 config 开关解决。
- 修复：`tools/mission-driver/src/main.js:779` 之后加 `program.enablePositionalOptions()`。这是 commander 提供的 parsing config，改变 option-after-subcommand 的解析路由——option 正确送到 subcommand 的 action。实测 4×case 矩阵（baseline / positional / passthrough）：
  - baseline：`run X --step Y` → RUN 收到 `step: undefined` ❌
  - `enablePositionalOptions()`：`run X --step Y` → RUN 收到 `step: Y` ✅；主命令 `X --step Y` 仍正常 ✅
  - `passThroughOptions()`：能修 `run` 子命令但**破坏主命令**（`X --step Y` 报 "too many arguments"），所以不能用
- 测试覆盖：`tools/mission-driver/test/from-step.test.js` 新增 Case 5（2 tests），用 `runCliViaSubcommand`（`main.js --dir <root> run ...`）锁死 mitigation——`run <mission> --step CHECK` 必须打印 `Step: CHECK (single-step mode)`；`run <mission> --from-step DEEP_AUDIT` 必须打印 `From step: DEEP_AUDIT`。若有人删 `enablePositionalOptions()` 那行，这两个 assertion 会立刻 fail（commander 退回 stripping 行为）。原 `runCli`（主命令路径）保留，Case 2/3/4 继续用它。
- Verification (full green): `pnpm --prefix tools/mission-driver test` → **512 pass / 0 fail**（+2 vs 上次 baseline 510）；真实 `_tmp/` 0 leak。手动验证用户原命令：`run mission-driver-draft-robustness --step EXEC_PLANS --dry-run --no-monitor` 现在正确打印 `Step: EXEC_PLANS (single-step mode)` 并从 EXEC_PLANS 起步。
- 旁注：用户最初要求"升级 commander 到 16+"——`pnpm view commander versions` 显示 latest 就是 15.0.0，16+ 不存在；所以改走了 config 开关路径。如果将来 commander 发 16+ 且明确修了这个 quirk，可考虑去掉 `enablePositionalOptions()`（但留着也无害）。
- 历史遗留：`from-step.test.js` 的早期注释 `Uses the MAIN command path (no 'run' keyword) to avoid the commander 15.0.0 option-stripping quirk` 记录了这个 bug，但只绕不开生产 CLI——本次才把生产 CLI 也修了。

### 2026-07-22 — monitor 看不到 forEach 正在运行的子项（`mergeSubflowChildren` 早 return 漏扫盘）

- 现象：用户在 monitor 详情页看 `2026-07-22-075526-mission-driver` run，EXEC_PLANS visit=2 下面只看到 Plan 1/2/3 已完成，看不到正在运行的 Plan 4。等 Plan 4 跑完才出现。
- 根因（`tools/mission-driver/src/monitor.js:249` 的 `mergeSubflowChildren`）：
  ```js
  if (Array.isArray(step.subflowRuns) && step.subflowRuns.length > 0) {
    step.children = step.subflowRuns.map(...);
    continue;   // ← BUG：早 return 跳过下面的磁盘扫描
  }
  // 磁盘扫描 fallback 只在 subflowRuns 为空时跑
  ```
  - engine.js 的 WI5 `_wfAppendSubflowRun` 在每项**完成时**才把记录追加到 `subflowRuns`（不是开始时）。所以 forEach 第 4 项正在跑时，它的 `run-state-EXEC_PLANS-2-3.json` 已经落盘（`status:running, currentStep:BUILD_VERIFY`），但 `subflowRuns` 数组里只有前 3 项。
  - monitor 的磁盘扫描本意就是兜底这种情况（CONTEXT.md 原文："not dependent on monitor fallback scanning disk"），但被 `length > 0 → continue` 错误短路——只要之前任何一项完成了，磁盘扫描就再也不跑。
- 实测证据（`_tmp/2026-07-22-075526-mission-driver`）：run-state.json 的 EXEC_PLANS visit=2 `subflowRuns.length === 3`（索引 [0,1,2] 全 completed），但磁盘上 `run-state-EXEC_PLANS-2-{0,1,2,3}.json` 4 个文件都在，第 4 个 `status:running, currentStep:BUILD_VERIFY`。原 `mergeSubflowChildren` 只产 3 个 children；修后产 4 个，第 4 个正确显示 running + BUILD_VERIFY。
- 修复：把 `mergeSubflowChildren` 改成 **UNION**——总是扫盘，按 forEachIndex 去重；`subflowRuns` 条目优先（保留 `forEachItem` 元数据），磁盘发现的额外索引补充进去。disk file 总是重新读取，所以已完成项的状态也有 live freshness（running → completed 不依赖 engine 重写 subflowRuns）。修后行为：
  - subflowRuns 有 N 项 + 磁盘有 N+M 个文件 → children 是 N+M 个，按 forEachIndex 排序
  - subflowRuns 为空（aborted run）→ 全部从磁盘恢复（保持原 fix `06749fa` 的语义）
  - 非 subflow step → 跳过（保持原行为）
- 测试覆盖：`tools/mission-driver/test/subflow-state-isolation.test.js` 新增 1 个 regression test "shows in-flight forEach child on disk even when subflowRuns is non-empty"，构造 3 completed subflowRuns + 1 磁盘 in-flight 文件，断言修后 `children.length === 4` 且第 4 个 `status:running, currentStep:BUILD_VERIFY, forEachItem:null`。
- Verification (full green): `pnpm --prefix tools/mission-driver test` → **529 pass / 0 fail**（+1 vs 上次 baseline 528）；live 数据实测从 3 children → 4 children。
- 部署：用户需**重启独立 monitor**（PID 28224，端口 9300）才能拾取修复——monitor.js 是启动时加载进内存的。重启后立即生效，不影响运行中的引擎。

### 2026-07-21 — 用户手册（双语）新增

- 新增 `tools/mission-driver/docs/user-manual.zh.md` 与 `tools/mission-driver/docs/user-manual.en.md`（结构对称，便于交叉引用）。
- 内容覆盖：mission 是什么 / 何时用（>1h 任务）何时不用（小改动）/ 核心概念术语表 / 5 阶段典型工作流（需求文档 → draft → run → monitor → analyze）/ 完整 CLI 参考（run/draft/analyze/monitor/list，`--step` vs `--from-step` 对比表，env vars）/ monitor dashboard 用法 / flow 状态机详解（5 step + 状态流转 ASCII 图 + subflow 嵌套 + plan 生命周期 + maxAuditRounds 配额语义）/ mission.json 字段参考 + base.json 继承 + 自定义 flow / 常见配方（bug 修复 / 新功能 / tech debt / dry-run audit / 续跑）/ 排错速查 / 进阶阅读路径 / quick reference card。
- 写作依据：基于本次会话中对 `flows/mission-driver.json`、`engine.js` 的 5 阶段循环、plan 的 draft→active→completed 生命周期、DEEP_AUDIT 子流程结构、`--step`/`--from-step` 语义、monitor 端口行为、`_tmp/` run 目录布局的实际理解。不限于用户提示词，补充了 quick reference card、troubleshooting 速查、进阶阅读路径等培训向内容。
- 与现有文档关系：与 `README.md`（命令速查）/ `EXECUTION-PRINCIPLE.md`（内部原理深读）/ `TROUBLESHOOTING.md`（卡住诊断）/ `CONTEXT.md`（给 AI 自己读的 30 秒上下文）互补——手册是面向**新用户培训**的入口文档，前几节自带概念解释和工作流，结尾再链向更深的现有文档。

### 2026-07-21 — `.opencode/skills/mission-driver/` skill 新增（stack-agnostic）

- 参考 `C:\Work\cardlite-acq\.opencode\skills\mission-driver` 的 cardlite 专用 skill，在 `.opencode/skills/mission-driver/` 下创建**stack-agnostic** 等价物（适用于任何复用本 AGE 模板的项目）。
- 文件：`SKILL.md`（389 行，frontmatter + 4 个工作流 A/B/C/D）/ `references/mission-config-schema.md`（353 行，schema + 4 个 stack 示例 + runtime state + flowName 自定义）/ `references/roadmap-template.md`（323 行，结构总览 + auth-refactor 注解样例 + 粒度判定 + 反模式清单）。
- 与 cardlite 版的关键差异（"更通用"的具体含义）：
  - **不硬编码 stack**：去掉 Maven / `-pl ABO -am` / `[PAYSUITEE1-XXXX]` / ABO/AAS/IMS/CAP 模块名 / `{USER}` slug plansDir 等所有 cardlite 专有假设。改为指向 `docs/context/project-context.md` 的 Verification Commands 表 + `missions/base.json` 继承机制。
  - **多 stack 命令速查表**：Node(pnpm/npm) / Python(pytest+ruff+mypy) / Java(Maven) / Go / Rust 五列对照，用户按项目栈自选。
  - **示例多样化**：4 个 mission.json 示例覆盖 Node auth-refactor、Python api-perf、Java payment-debt、cross-cutting（`moduleDir: "."`）。
  - **roadmap 样例去 cardlite 化**：用合成的 auth-refactor 替换 abo-tech-debt，避免泄露业务模块名。
  - **覆盖新命令**：包含 `draft`（两阶段 brief→draft，cardlite 版未含——它写在 `--draft-mission` 时代之前）、`--step` vs `--from-step` 互斥与差异说明、`--no-monitor` / `--fast` / `--skip-steps`、独立 `monitor` 子命令、`analyze` postmortem 工作流（Workflow D）。
  - **覆盖修后的 flow 语义**：DEEP_AUDIT.complete → REVIEW_PLANS（兜底 passthrough）而非旧的 → DRAFT_PLANS；plan-execution 子流程 4 step、deep-audit-loop 子流程 4 step 都列出。
  - **maxAuditRounds 解释**：审计配额语义、ping-pong 兜底、`audit_gate`（`_shouldCompleteOnAuditQuota`）触发条件——cardlite 版完全没覆盖。
  - **链接到刚写的 user-manual**：SKILL.md 多处链向 `tools/mission-driver/docs/user-manual.{zh,en}.md`，避免重复内容。
- skill 触发词（frontmatter `description`）：roadmap / mission / mission-driver / dev loop / draft-mission / list-missions / AGE loop / 自动开发。
- 设计取向：skill 是**操作向导**（"现在该跑什么命令、字段填什么"），user-manual 是**培训向导**（"为什么这样设计、整体怎么跑"）——两者互补，skill 在 chat 里按关键词激活、提供精确步骤；manual 用于一次性系统性学习。

### 2026-07-21 — 培训用 Obsidian 幻灯片（`training-deck.zh.md`）

- 新增 `tools/mission-driver/docs/training-deck.zh.md`（585 行，**32 张幻灯片**），基于 `user-manual.zh.md` 压缩提炼。
- 格式：标准 markdown + `---`（单独一行、上下各一空行）作分页符。Obsidian Slides 或 Advanced Slides 插件直接演示，PowerPoint/Keynote 也能识别。
- 内容覆盖（32 张）：标题 / 一句话定义 / 解决什么问题 / 何时用不用 / 30 秒工作流图 / 核心概念表 / 5 阶段总览 / Stage A 需求文档 / Stage B draft+mission.json 字段+stack 命令速查 / Stage C 执行+常用 flag / `--step` vs `--from-step` 对比 / Stage D Monitor / Stage E 复盘 / Flow 5 step 表 / 状态流转图 / plan-execution 子流程 / deep-audit-loop 子流程 / plan 生命周期 / maxAuditRounds 配额 / base.json 继承 / 常见配方 / 卡住诊断 / 看 agent 进度 / 常见坑表 / 关键约束 / Quick Reference Card / 关键 Takeaways / 进阶阅读 / 谢谢。
- 与讲义（`user-manual.zh.md`）的关系：幻灯片**只保留核心步骤**——每张最多 1 个概念、3-7 个要点或 1 张表/图；详细解释（比如"为什么 DEEP_AUDIT 回 REVIEW_PLANS 而不是 DRAFT_PLANS"的历史回归原因）在讲义里。幻灯片顶部注明讲义路径供延伸阅读。
- 设计取向：每张幻灯片独立可读（不依赖前一张的滚动位置）；用 ASCII 图代替 mermaid（slides 插件兼容性更广）；用表格代替段落（演示时一眼可读）；用 emoji 极少（避免演示分心）。

### 2026-07-21 — 培训幻灯片按黄金法则重做（32→19 张）

- 用户反馈：标题换行、内容溢出、字体过大、总数太多（>20 张）。
- **参考来源**：装了 `antfu/skills@slidev` skill（11.8K installs，markdown 演示事实标准），读其 references 提取 Slidev 默认画布约束（`canvasWidth: 980`，16:9 = 980×551px）。结合通用演示黄金法则（rule of 6×6 → 改用更严的 4×15；one-idea-per-slide；≤20 张）。
- **应用的黄金法则**：
  | 规则 | 数值 | 解决用户哪个痛点 |
  |------|------|------------------|
  | 标题长度 | ≤6 CN / ≤4 EN 词 | 标题换行 |
  | 每张要点数 | ≤4 | 内容溢出 |
  | 每条要点字数 | ≤15 chars | 字体过大不能缩小 |
  | 表格规模 | ≤3 列 × 4 行 | 表格横向溢出 |
  | ASCII 图宽度 | ≤40 chars | 图溢出 |
  | 总张数 | ≤20 | "没抓重点" |
- **改动**：`tools/mission-driver/docs/training-deck.zh.md` 从 585 行/32 张 → 219 行/19 张（-62% 内容）。所有标题缩短到 ≤9 chars（最长 "C 常用 flag" / "Plan 生命周期"）；最长的内容行 52 chars（slide 11 的表格分隔行，仍能 fit 980px 画布）；每张最多 4 个要点；ASCII 图都压到 ≤36 chars 宽。
- **合并/裁剪决策**：Stage A+B 合并成 3 张（A 需求 / B 生成 / B 字段）；Stage C 拆 3 张（启动 / flag / step-vs-from-step）；Stage D+E 合并；stack 命令速查表移到讲义（不进幻灯片）；maxAuditRounds / base.json 继承 / 子流程细节移到讲义；Quick Reference Card 移到讲义；troubleshooting 速查从 1 张表精简为 1 张要点。
- **方法论沉淀**（可复用）：Obsidian/Slidev markdown 幻灯片的画布约束是 980×551px——这个物理边界决定了内容密度上限。黄金法则不是审美偏好，是画布像素的硬约束反推出来的：title 字号 36-44pt → 一行能放 ≤10 个 CJK 字符；body 字号 24pt → 一屏 ≤4 行 × ≤15 字。下次写幻灯片直接套这个表格。

### 2026-07-21 — `_tmp/` orphan-dir pollution by `from-step.test.js` (test-isolation fix)

- 现象：`mission-driver-draft-robustness` 长跑期间 `_tmp/` 自动堆出几十个空 `<ts>-mission-driver` 目录（成对、间隔 1–3 秒）。用户确认只手动启动过一次 monitor + 一次 run，排除了手动重跑假设。
- 根因（file-in/file-out 调查链）：
  - `tools/mission-driver/src/config.js:571-574` 在 `resolveConfig()` 入口无条件 `mkdirSync(_tmp/<ts>-mission-driver)`，远早于任何 liveness / 端口 / mission-占用 校验。
  - `tools/mission-driver/test/from-step.test.js` 是唯一漏网测试：用真实 `REPO_ROOT = resolve(__dirname, "..", "..", "..")` 而非 OS tmpdir，且 Cases 2/3 `spawnSync` main.js + Case 4 直接 `resolveConfig()`，每次 `pnpm test` 都向真实 `_tmp/` 戳 2 个目录。
  - 主 mission 的 BUILD_VERIFY / EXECUTE agent 每跑一次 `pnpm --prefix tools/mission-driver test` 自检就制造一对孤儿目录，几十次累计 75+ 个。其他测试文件（`skip-steps.test.js` / `transient-error.test.js`）已用 `mkdtempSync(tmpdir(), ...)` + `afterEach` rmSync 隔离；`cli-help.test.js` 只跑 `--help`，`mission-check-cli.test.js` 跑的是 `mission-check.mjs` 不是 `main.js`。
- 修复：`tools/mission-driver/test/from-step.test.js` 改用 `tmpRoot()`（`mkdtempSync(join(tmpdir(), "from-step-"))`）+ `setupMission(root)` 在 tmpdir 里建最小 `missions/{base,mission-driver-step-audit}.json` + 必需目录（mirror `skip-steps.test.js` 模式）；Cases 2/3/4 各自 `beforeEach/afterEach` 设置/清理。Case 1 不依赖 REPO_ROOT（用 `simpleFlow` mock），保持原样。
- 清理：删除 `_tmp/` 下 85 个空 `-mission-driver` 目录；保留 7 个非空真实 run + 3 个 `draft-mission-*`（不同机制，超出本次范围）。
- Verification (full green): `node --test tools/mission-driver/test/from-step.test.js` → 5/5 pass + 真实 `_tmp/` 0 leak；`pnpm --prefix tools/mission-driver test` → 510 pass / 0 fail + 真实 `_tmp/` 0 leak（与 `2026-07-21-1523-1-non-forEach-subflow-and-test-gaps.md` 记录的 510 baseline 一致，无回归）。
- 设计遗留（不在本次范围）：`resolveConfig` 在"纯解析"路径里 `mkdirSync` 是设计气味。彻底修复需把 mkdir 推迟到 `engine.run()` 的 `_initWorkflow` 之前（即引擎确定要写 run-state.json 时），让 `resolveConfig` 真正无副作用——但这是跨 main.js 多个调用点（cmdRunMission / cmdDraftMission / cmdAnalyzeRun / cmdListSteps）的较大重构，且当前主 mission 还在跑（PID 4396），暂不动。
- 范围备注：本次修复与运行中的 `mission-driver-draft-robustness` mission 无冲突——该 mission 的活跃 plan `2026-07-21-1523-1-non-forEach-subflow-and-test-gaps.md`（H2/H3/H9）针对 subflow 持久化 / base.json 集成 / timing 鲁棒性，与本测试隔离修复正交。
