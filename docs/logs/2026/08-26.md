# Development Log - 2026-08-26

### 2026-08-26

- **M2-WI23+WI24（2026-08-25-0950-3）VERIFY_BUILD full-green + 三 Phase 落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-130203 BUILD_VERIFY 步）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck` 绿、`pnpm --prefix tools/mission-driver/web run build` 绿（4225 modules，`web/src/` 零 diff，`web/dist/` 与 committed 世代一致无漂移）、`pnpm --prefix tools/mission-driver run lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **904/904 pass / 0 fail**（prompt-check OK）。Project-specific 面复核：`src/main.js` delegate vars / `flows/*.json` / `web/src/` / `missions/*.json` 零变更（对应专项检查不触发）；`git diff --stat tools/mission-driver/src/` 为空（零引擎 diff）。本 run 三 Phase 工件按 code/docs 两笔提交落库（code = `.githooks/pre-commit` + `verify-age.sh` L2.5 段 + `age-ci.yml` 触发路径扩集 + CONTEXT.md hook/CI 说明；docs = plan 勾选/回执 + roadmap 收口回写 + 本日志）——**full-green verification**。

- **M2-WI24 Phase 3 收口：Verification Gate 四命令真实执行 + M2 milestone 收口回写**（plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md` Phase 3，**M2（P1 法律：门禁族 + 三硬门 + 机械验证机械化）至此收口**）：① **WI24 gate 四命令实测全绿**——㈠ `node tools/mission-driver/src/gate-check.mjs --policy missions/autonomy.policy.yml` exit 0（valid=true；18 gates / 7 triggers / agents={drafter,reviewer,auditor,executor}；roadmap 字面 check-policy 的落地 CLI 前门形态，对齐注记在案）㈡ `node --test plugin/dsh/test/law-truth-table.test.mjs` **113 用例 / 0 fail**（≥30 超额 3.8×；分类计数：三硬门 34 = closure/roadmap-audit-binding 15 + writer-identity 10 + plan-completed 9；配套门 19 = nothing 4 + claim 7 + meter(audit-rounds-overflow) 4 + append-only 2 + verify-keys 2；护栏/结构面 60 = freeze 8 + p8 5 + path-guardrail 3 + roadmap-write-guard 5 + work-item 族 4 + seam(actor 缺省结构子集) 5 + corpus 5 + adapter 8 + runner 3 + policy 3 + boundary/truth-table 12；`--test` runner 形态对齐注记在案）㈢ `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md` exit 0（语料 = 当期最新收口 M2 plan WI22，回执对 + pass 行齐全——**三硬门全 allow**：closure-audit-binding/writer-identity/plan-completed + 其余 6 结构规则 allow + workItem M2-WI22 对账 ok）㈣ `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0（autonomyPolicy 字段经校验）。② **roadmap 收口回写**：WI23/WI24 `[x]` + 证据指针（hook/CI 接线面 + gate 四命令输出摘要）；头部 `> Last Updated` 刷新为 M2 收口态（round-2 P2 持续清偿）；`## Status Values` 表删除 + `Work Item Status` 导语改列 0 勾选框语义（round-1 P2 + 0925-3 Deferred 触发条件命中——按 00-roadmap-authoring-guide 2026-08-25 现行格式归一，`grep -c ready` 仅余 2 处历史注记类命中：头部 changelog 散文 + Follow-up 已清偿条目划线文本）；Follow-up Backlog 两条 P2 标注 absorbed-by 本 plan Phase 3（round-1 退役格式段条目划线清偿 + round-2 Last Updated 条目追加持续清偿注记）；**M2 sanity 对账**：`grep -c "^- \[ \]"`=16 + `grep -c "^- \[x\]"`=28 = **44 全量**（M1=11 + M2=17 含 WI41–44 + M3=7 + M4=5 + M5=4；M2 全勾 17/17，unchecked = M3+M4+M5 = 16，与 plan 预期 28+16=44 逐项一致；全零断言不在本 gate 语义——归 M5/WI40）。③ CONTEXT.md 增 CI 触发路径说明（账本/执法数据/hooks 路径入触发集 + 本地=CI 同构）；roadmap-check 改后复跑 exit 0。④ **收口链全量验证**：`./verify-age.sh` 三段全绿（L1 904/904 + prompt-check OK、L2 223/223、L2.5 policy + 10 份 frontmatter corpus + 真值表 113/113）。

- **M2-WI23 Phase 2 收口：CI 接线——verify-age.sh 增 L2.5 law gates 段 + age-ci.yml 触发路径扩集**（plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md` Phase 2）：① `verify-age.sh` 第三段「L2.5 law gates」按 Decision 落地三面——`gate-check.mjs --policy missions/autonomy.policy.yml`（`"valid": true` 断言）/ `docs/plans/age-autonomy/` 全量 corpus 逐文件 gate-check（frontmatter 形必须 exit 0）/ `node --test plugin/dsh/test/law-truth-table.test.mjs`（独立显式调用，与 L2 段 `npm --prefix plugin/dsh test` 内含同文件双轨——WI24 gate 字面对齐面）；头注 L1/L2/L2.5 描述同步 + gate 出处注记，收尾 echo 改 `L1+L2+L2.5 gate: GREEN`。**执行期注记（corpus 面裁定）**：legacy 形 plan（0635-1/2，无 `---` frontmatter 块）注记跳过——WI22 `legacy-plan-freeze` 在无 actor 结构面按设计 fail-closed（corpus 未注入例外不可评估，live 实测 54 份 legacy 冻结语料全 deny），CI 本身是该冻结面的合法通道（02 §4.7 CI 支）；policy 缺失 fail-open + 注记（模板消费者姿态，本仓库恒有 policy 故实际恒跑）。② `.github/workflows/age-ci.yml`：push/pull_request 两侧对称扩集 `docs/plans/**` + `docs/backlog/**` + `missions/**` + `.githooks/**`（账本与执法数据路径 + hooks 路径入触发集——只改 plan/roadmap/policy 的提交不再绕过 CI）；workflow 头注同步（L2.5 描述 + 触发路径扩集理由）；name 改 `age L1+L2+L2.5 gate`（step 名同步）；单 job 保持（本地=CI 同构契约，无第二入口）。Proof 实测：`./verify-age.sh` **三段全绿 exit 0**（L1 904/904 + prompt-check OK、L2 223/223、L2.5 policy ok + 10 份 frontmatter corpus ok + 真值表 113/113）；拦截证明——合法 frontmatter + body 带 legacy 终态行的 fixture（L1 905/905 容忍通过后）L2.5 段 `DENY`（structural-9 legacy-plan-freeze reason 输出）exit 1，fixture 用后即弃；首版拦截探针（frontmatter 写派生态 `status: completed`）在 L1 段即被 `ledger-corpus.test.js` 语料钉扎拦截（聚合门仍 exit 1——同一合法性属性双层钉住，L1 先触）。YAML 解析核对 + push/pull_request 对称断言；`git diff --stat tools/mission-driver/src/engine.js` 为空；web typecheck + lint:prompts 绿。`.github/` 与 verify-age.sh 不入 install-age.manifest（模板消费者边界维持）。

- **M2-WI23 Phase 1 收口：pre-commit hook 接线（law 结构子集暂存区触发面）**（plan `docs/plans/age-autonomy/2026-08-25-0950-3-m2-wi23-wi24-ci-wiring-m2-verification-gate.md` Phase 1）：新增 `.githooks/pre-commit`（bash，`set -euo pipefail`，三段独立 fail-fast + 段间注记；启用 = `git config core.hooksPath .githooks` 一次性本地配置，文档化进 `tools/mission-driver/CONTEXT.md`「构建与验证」段）。触发面 = `git diff --cached --name-only --diff-filter=ACMR` 中的 `docs/plans/**/*.md`（gate-check 单文件结构面）/`docs/backlog/*.md`（roadmap-check CLI 面，0925-2 交付消费）/`missions/*.json`（mission-check）。**执行期裁定两则**（plan Decision 字面外的必要落地面，均承仓库先例）：① legacy 形 plan（无 `---` frontmatter 块）注记跳过——WI22 `legacy-plan-freeze` 在无 actor 结构面按设计 fail-closed（corpus 未注入时例外不可评估），human git-commit 通道是 02 §2 A1 成文合法面，hook 结构面不重复执法（防 54 份 legacy 冻结语料恒红误杀）；② `missions/base.json` 等 base 配置（无 `roadmapPath` 键）注记跳过——mission-check 对 base 形配置恒 exit 1（缺 name/roadmapPath/plansDir 必填），monitor.js `GET /api/configs` 过滤先例。fail-open 纪律落地：missions/ 目录缺失 → 注记跳过退出 0；node 不可用 → 注记跳过退出 0。Proof 五场景实测（fixture 用后即弃）：干净仓库 exit 0；暂存违规 plan（frontmatter 写派生态 `status: completed`）→ exit 1 且 reason 指向 gate-check plan-structure deny；暂存违规 roadmap（未知字段 + 缺 `audit-rounds`）→ exit 1 且 fieldErrors 输出（roadmap-check CLI 接线回归面）；暂存合法 plan（`status: cancelled` + 已注册 work-item）→ exit 0；临时移走 missions/ 模拟模板消费者 → exit 0 + fail-open 注记（gate-check 无 owning mission 语境仍 grammar-only 放行）。验证：`pnpm --prefix tools/mission-driver test` **904/904**（prompt-check OK）、`git diff --stat tools/mission-driver/src/engine.js` 为空、仓库根无新增 package.json（零 npm 依赖纪律）。`.githooks/` 为 real-project 面，不入 install-age.manifest。

- **M2-WI22（2026-08-25-0950-2）VERIFY_BUILD full-green + 三 Phase 落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-130203 BUILD_VERIFY 步）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（4225 modules，`web/src/` 无 diff，build 未产生 dist 变更，dist 与 committed 世代一致）、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **904/904 pass / 0 fail**（prompt-check OK）。plan `## Verification` 双 pass 行 + `## Closure` dispatch/accepted 回执对已在库（本 run 早前批次写入，basisHash=f5638930…1ef2e 绑定）；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff 退出判据成立）；`missions/*.json` 零变更（mission-check 不适用）。本 run 三 Phase 工件按 code/docs 两笔提交落库（本条日志随 docs 提交）——**full-green verification**。

- **M2-WI22（2026-08-25-0950-2）closure 反馈重派冷放复验 no-op（零改动，全绿确认）**（mission `age-autonomy-implementation`，run 2026-08-26-130203 重派）：closure audit 反馈三项（missing-pass:test / missing-pass:verify-age / no-audit-receipt）经核已由本 run 早前批次全部清偿——`## Verification` 双 pass 行（test / verify-age，basisHash=f5638930…1ef2e 与 plan 当前内容绑定，exit=0）+ `## Closure` dispatch/accepted 回执对（`#audit-2026-08-26-130203-mission-driver-…-evidence-face-rebuild-1-681208e9`）。本步冷放复验全绿：`plan-check --strict` 本 plan passed=true（24/24 勾、零残勾、derivedCompleted=true、fieldsValid=true、EXIT=0）；`pnpm --prefix tools/mission-driver test` **904/904**（prompt-check OK）、`npm --prefix plugin/dsh test` **223/223**（closure 25/26 + freshness 42 文件 content-equal + smoke-import ok）、`./verify-age.sh` **L1+L2 GREEN**、web `typecheck`/`build` 绿（dist 与 committed 世代一致，web 树零 diff）、`lint:prompts` OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff 退出判据成立）；grep 守护复核——`openAudits|AUDIT_STATUS_RE|_scanOpenAuditsList` 于 flow-loader.js + flows/*.json 零命中（engine.js 三处既有 optional-chained 消费面按执行期裁定保留）、`scanSubflowMatches|registerPlanStatusGate` 于 plugin/dsh/src/ 零命中；roadmap WI22 `[x]` 五段证据指针在位。本步除本条日志外零文件改动。

- **M2-WI22 Phase 3 收口：证据面单一性证明 + 文档回写**（plan `docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md` Phase 3，全 plan 三 Phase 收口）：① **run-state 残留消费面清点**：`grep -rn "run-state" plugin/dsh/src/` 命中全部为 mdcontrol 状态透传（`mdcontrol.status` run-state.json passthrough——异步作业契约进度面）、skill 指令文本与退役历史注记——**plan 完成态裁决面零消费**；引擎侧完成态判定（closureScriptCheck/`_scanPlansByStatus`/monitor plans API）账本化收口证据引用 0925-1（closureScriptCheck 回执感知路由）与 0925-2（校验器读 seam 接线），不重复断言；monitor RunDetail run-state 显示面 = 步骤机/监控语义（Non-Goals 类，非证据面）。② **文档同步（改动逐条）**：`tools/mission-driver/CONTEXT.md` 增 WI22 段（退役三面拆解 + 守护测试指针）；`docs/architecture/dsh-plugin-packaging.md`（Phase 1 已落：scripts/test/src 树条目、ledger 消费面、dsh-tools pin 持有者改 host-adapter.ts、§Service Surface Reinforcement gate RETIRED、P3 changelog + 状态头）；`design/mission-driver-flow-design.md` §6 deep-audit 段重写（mermaid 两步拓扑 + RETIRED banner + 历史 rationale 保留为 pre-WI22 记录）+ WI4 审计门三处 openAudits 腿退役注记；`design/step-execution-and-audit-count-design.md` 顶部 M2-WI22 retirement banner（分析章节保留为 pre-retirement 记录）；`design/flow-engine-design.md` forEach 例注记 + prompt 树 draft-from-audit 条目删除注记 + §6.3 子流程步列表更新；`design/mission-design.md` 表达式注册表 openAudits 行划线 RETIRED + when 例注记 + deep-audit mermaid 更新（注册表行为事实已变，防 WI43 型漂移——超出 plan 枚举面的同型小面积注记，在此成文）。③ **回写**：roadmap WI22 `[x]` 五段证据指针（裁定/三面拆解/通道退役+前置清点/duplicate-anchor/文档同步+测试计数）+ Last Updated 头同步；Follow-up Backlog round-1 P2 duplicate-anchor 条目划线清偿注记（指向 `test/ledger-sections.test.js` 钉扎）；本日志三 Phase 条目。④ **全量验证**：`pnpm --prefix tools/mission-driver test` **904/904**（prompt-check OK）、`npm --prefix plugin/dsh test` **223/223**（closure 25/26 + freshness 42 文件 content-equal + smoke-import ok）、`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（dist 与 committed 世代一致，web 树零 diff）、`./verify-age.sh` **L1+L2 GREEN**、`plan-check` 本 plan fieldsValid=true / roadmap-check exit 0；engine.js 零 diff（`git diff --stat` 为空）。

- **M2-WI22 Phase 2 收口：引擎 legacy 审计通道退役 + duplicate-anchor 结构 error**（plan `docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md` Phase 2）：① **退役前置清点复核**（先证后拆）：live `docs/audits/**` 头行 `> Audit Status:` 6 命中 / **1 open**（mission 级 `mission-driver-step-audit/2026-07-21-0919-multi-audit-…md`）/ 5 planned——与 Current Baseline 2026-08-25 评审期计数逐项对账零漂移。② **开放记录机械关闭**：该 open 记录状态行改写 `closed` + Disposition 注记（findings 实际去向诚实成文：从未 dispatch 进 step-audit mission 的 roadmap；F4/F5 随通道退役被取代（被测对象即通道本体）；F1–F3/F6 为 doc-sync 残项由本 plan Phase 3 同步其提及面）。③ **deep-audit-loop 形态裁定落地**：删除 `CHECK_OPEN_AUDITS`/`SCAN_NEW_RESULTS` 两节点，`entry` 直连 `"MULTI_AUDIT"`，`OPEN_AUDIT` 的 otherwise/transitions(clean|issues)/onError **三处**全部改终态 `{done: "completed"}`（iteration-2 评审指出的 otherwise 悬空 goto 缺陷位 :51 精确对应）；等价性钉在已验证 when 语义（缺省 = 条件块跳过、步执行）逐分支对照 + 零开放记录前提。④ **通道移除**：`flow-loader.js` 删 `AUDIT_STATUS_RE`/`AUDIT_TYPE_RE`/`_scanOpenAuditsList`/`_isMissionLevelAudit` + 导出行 + 表达式注册表 `openAudits` 键；`prompts/draft-from-audit.md` 删除（mission prompts 映射无引用，全树零悬空）；`config.js` auditsDir 注释同步（派生逻辑保留——audit 报告写入面仍在用）。⑤ **测试迁移**：`audit-convergence.test.js` 重写为「通道已移除」守护套件 6 例（导出/实现零命中、flow JSON 无 openAudits、prompt 已删、**无悬空引用守护断言**（walk 含 entry 键：全部 goto/retry/done/otherwise/onError/onUnknown/onMaxRetries 目标命中现存 step 或终态——loadFlowFile 无此校验，防「加载绿、运行期 unknown_step」静默断裂）、mission-driver 主流程拓扑联动守护、docs/audits 零 open 语料守护）；`draft-plans-audit-gate.test.js` 删 WI4 Phase 5 describe 块（被测对象退役，非断言漂移，成文注明）保留 A–G 七例（engine.js 零 diff，mock 注入语义仍活）；`context-map.test.js` prompt 库计数 12→11 钉扎 + draft-from-audit 不在库断言。⑥ **duplicate-append-only-anchor 结构 error**（deep-audit round-1 P2 清偿）：`ledger-sections.mjs` `scanPlanLedger`（重复 `## Draft Review Record`/`## Verification`/`## Closure` → `duplicate-anchor` error 进 scan.errors，首锚定语义保留——首块仍派生面，error 行号指向第二块标题行）+ `scanRoadmapLedger`（重复 `## Deep Audit Record` 同型）；deny 面（plan-structure rule push scan.errors）与读面（inspectPlan structuralErrors）均可见；`ledger-sections.test.js` +5 例（正反例 + 全量 corpus 零重复锚点断言——`docs/plans/` 全树 + age-autonomy roadmap 零误伤）。⑦ helpers.js 私有 deep-audit-loop 夹具标注 LEGACY-TOPOLOGY NOTE（真实拓扑已简化，夹具按消费面保留）；assets 重建（43→42 文件）。**engine.js 零 diff 退出判据成立**（`git diff --stat` 为空）；grep 残留 = engine.js :687/:734/:740 三处**既有** optional-chained 消费面（保护域 + plan Prereqs 双重钉死不动；注册表键删除后 null-safe 退化为 `[]`（零开放语义），与「grep src 零命中」判据的字面冲突以保护域裁定优先并在此成文）。验证：`pnpm --prefix tools/mission-driver test` **904/904**（899 基线：audit-convergence 2→6、draft-plans-audit-gate −4、ledger-sections +5、context-map 改写；prompt-check OK）、`npm --prefix plugin/dsh test` **223/223**（freshness 42 文件 content-equal + closure 25/26 + smoke-import ok）、web typecheck 绿。

- **M2-WI22 Phase 1 收口：plan-status-gate 退役 + `legacy-plan-freeze` 保护收编**（plan `docs/plans/age-autonomy/2026-08-25-0950-2-m2-wi22-evidence-face-rebuild.md` Phase 1）：裁定落地 = **退役 + 保护收编**。① `tools/mission-driver/src/law-rules.mjs`（assets 副本同步）新增 `legacy-plan-freeze` 规则（`legacyPlanStatusOf` 复用共享 `PLAN_STATUS_RE` + `normalizeLegacyStatus`——零第二正则实现，代码围栏跳过承 dual-read 读 seam 纪律；终态集 = completed/cancelled/superseded/deferred；deny 面 = proposed 携带终态行 ∨ 磁盘终态行的改写/删除（un-freeze）；例外 = P8 字面三支 human/CI/已批准立项（`activePlanReferencing`）；roots 未注入 fail-open、语料未注入 fail-closed——与 P8 同姿）；`missions/autonomy.policy.yml` 注册第 18 条 gate（enforce）。② 删除 `plugin/dsh/src/plan-status-gate.ts`（457 行）、`plugin/dsh/test/plan-status-gate.test.mjs`（23 例）、`plugin/dsh/scripts/e2e-plan-gate.mjs` + package.json `verify:e2e:gate` 脚本行；`service.ts` 挂载收敛单 law 监听器（`registerLawGate` solo `tools/pre-execute` listener）；`law/host-adapter.ts` 增 `legacyFreezeFaceActive` 终态面预检（终态行写入罕见——corpus 走查仅按需触发）与 freeze corpus 注入。③ `docs/architecture/dsh-plugin-packaging.md` 同步：scripts/test/src 树条目、ledger 消费面（PLAN_STATUS_RE 消费者改 law 内核）、dsh-tools type-only pin 持有者改 host-adapter.ts、§Service Surface Reinforcement gate 条目改 RETIRED、P3 changelog 行 + 状态头历史注记。④ 真值表 `law-truth-table.test.mjs` 新增 §24 freeze 六例（字形矩阵/终态全集/un-freeze/例外通道/域外面/适配层端到端——修复执行中断期三处测试缺陷：`cancelled（disposition）` 注记形按 PLAN_STATUS_RE 字符集限制应 null 非 cancelled（load-bearing 约束，加 `> Status: additive (annotation)` 反例钉住）；approved-project 用例缺 `ctx.projectRoot` 注入致相对路径候选不生成；`roots/plans: undefined` 落入默认参数陷阱改 `null` 哨兵显式省略）。验证：`npm --prefix plugin/dsh test` **223/223**（240 基线 − 23 退役 gate 例 + 6 freeze 例，legacy corpus 用例原地改写；closure 25/26 + freshness content-equal + smoke-import ok）、`pnpm --prefix tools/mission-driver test` **899/899**（`law-policy.test.js` 真 policy gate 快照 17→18 同步）、web typecheck + `lint:prompts` 绿。`grep -rn "scanSubflowMatches\|registerPlanStatusGate" plugin/dsh/src/` 零命中。

- **VERIFY_BUILD full-green + 0950-1（M2-WI21）收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 BUILD_VERIFY 步）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（4225 modules，`web/src/` 无 diff，build 未产生 dist 变更，dist 与 committed 世代一致）、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899/899 pass / 0 fail**。`docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` `## Verification` 执行期复核段 + 双 pass 行（test / verify-age，basisHash=c14c88ea…9350 绑定，exit=0）+ `## Closure` dispatch/accepted 回执对（`#audit-2026-08-26-072439-mission-driver-…-m2-wi21-path-structure-guardrails-p8-1-001637b5`，models={exec:zhipuai/glm-5.2,aud:zhipuai/glm-5.2} = policy single-model 降级诚实记录，承 0925 批次先例）随本步 doc commit 提交。独立收口审计（ses_auditor_2026-08-26-072439 subagent）一轮 approved：27/27 计数域全勾、三 Phase 工件实证（law-core `workItemRegistered` + plan-structure 注册面接线 / law-rules `path-guardrail`/`roadmap-write-guard`/`law-self-protection`（`isLawProtectedPath` 四保护路径族 + corpus 缺省 fail-closed）/ mission-check `checkRoadmapUniqueness` + CLI 接线 / law-policy `{{projectRoot}}` match/resolve 双面 / policy 7 新 gate 条目全 enforce + plan-structure 切 enforce / 真值表 107 例计数一致）、命令复跑 899/0 + verify-age L1+L2 GREEN、`git diff --stat HEAD -- tools/mission-driver/src/engine.js` 为空（零引擎 diff）、回写面（roadmap WI21 `[x]` 全证据指针 + Last Updated 同步、CONTEXT.md WI21 段、08-26 日志三 Phase 条目）、Deferred 诚实性（三项均与 Non-Goals 对应且后继已登记）。plan 满足 01 §5.2 完成派生公式（derivedCompleted）——**0950 批次（0950-1）至此收口**。

- **0950-1（M2-WI21）EXEC_PLANS 冷放复验 no-op（零改动，全绿确认）**（mission `age-autonomy-implementation`，run 2026-08-26-072439 EXEC_PLANS 步重派）：plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` 三 Phase 计数域 27/27 全勾零残勾（本 run 早前批次执行完毕），roadmap WI21 已 `[x]` 带证据指针 + Last Updated 头部同步（Phase 3 回写项），工作树洁净零 diff（`git diff --stat tools/mission-driver/src/engine.js` 为空——零引擎 diff 退出判据成立）。冷放复验命令全绿：`pnpm --prefix tools/mission-driver test` **899/899**（prompt-check OK）、`./verify-age.sh` **L1+L2 GREEN**（plugin 240/0 + closure 25/26 + freshness 43 文件 content-equal + smoke-import ok）；`plan-check` 本 plan fieldsValid=true / 27 勾 / exit 0，`derivedCompleted=false` 仅缺 `## Verification` pass 行与 `## Closure` 回执——按 00-guide 规则 11 与本 plan Phase 3 注记，二者由父 mission BUILD_VERIFY/CLOSURE_AUDIT 步写入（`completed` 派生态禁写，frontmatter `status` 维持 `active`），本步不越权代写。本步仅落本条复验记录。

- **VERIFY_BUILD full-green + 0925-3（M2-WI43）收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 BUILD_VERIFY 步）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（4225 modules，`web/src/` 无 diff，build 未产生 dist 变更，dist 与 committed 世代一致）、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899/899 pass / 0 fail**。`docs/plans/age-autonomy/2026-08-25-0925-3-m2-wi43-arch-ownerdoc-contract-sync.md` `## Verification` pass 行（basisHash=67c276dd…8ad9 绑定，exit=0）+ `## Closure` dispatch/accepted 回执对（`#audit-2026-08-26-072439-mission-driver-…-m2-wi43-arch-ownerdoc-contract-sync-1-ca37f07e`，models={exec:zhipuai/glm-5.2,aud:zhipuai/glm-5.2} = policy single-model 降级诚实记录，承 0635-3/0925-1/0925-2 先例）随本步 doc commit 提交。独立冷放审计（ses_auditor_2026-08-26-072439-n3 subagent）一轮 approved：21/21 计数域全勾零残勾、Phase 1/2 工件实证（baseline §Public Exports 三模块逐名登记 `rg -c` 逐模块 =2 ≥1；packaging `rg "19 (allowed|files)"` 零命中、PLAN_STATUS_RE 源 :212/:279 → `assets/src/ledger-dualread.mjs`、import-graph ledger 精确边落地、计数 = live **26** = `ALLOWED_MODULES`/`assets/src` 实测逐名一致——判据主句优先于快照值 22，裁定已记 roadmap 证据行）、Phase 3 回写实证（roadmap :63 WI43 `[x]` + 证据指针 + Last Updated :7 同步、WI24 未勾 in-scope 口径）、命令复跑 899/899 + prompt-check OK + `mission-check` exit 0、日志在册（:51-55 三 Phase 条目）、Deferred 诚实性（唯一 deferred 与 Non-Goals 逐字对应）。**0925 批次三 plan（0925-1/0925-2/0925-3）至此全部收口**。

- **VERIFY_BUILD 复验 full-green（零 diff，同 run 二次复跑）**（mission `age-autonomy-implementation`，run 2026-08-26-072439 BUILD_VERIFY 步）：工作树验证前即洁净（HEAD `b947268`），零代码改动。四命令全绿：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（4225 modules，dist 产物与 committed 世代一致——`web/src/` 无 diff，build 后 `git status --short` 空）、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899/899 pass / 0 fail**。本步仅落本条复验记录。

- **0925-2（M2-WI42+WI44）收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439）：`docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` 落 `## Verification` pass 行 + `## Closure` dispatch/accepted 回执对（`#audit-2026-08-26-072439-mission-driver-…-m2-wi42-wi44-validator-wiring-verify-vacuity-1-86ac5983`，` models={exec:zhipuai/glm-5.2,aud:zhipuai/glm-5.2}` = policy single-model 降级诚实记录，承 0635-3/0925-1 先例），plan 由 `deriveCompleted` 派生 **completed**（31/31 计数域全勾 + pass 行 basisHash=e7ea7bb2…bdd9 绑定 + 回执对同 id 配对；frontmatter `status` 维持 active——completed 禁写）。pass 行由 `gate-check <plan> --verify`（verify-runner，M2-WI19 交付面）机械产出：真实 `commands.test` exit 0 + verify-keys gate allow。独立冷放审计（ses_auditor_2026-08-26-072439-n2 subagent）一轮 approved 七面全 PASS：计数域 31/31、工件实证（`ledger-dualread.mjs:49-52` 读 seam + `plan-check.mjs:206-208/:268` exit 1 + `flow-loader.js:96-100` warn/`:104` 成员资格不变 + `monitor.js:838-853` API + `roadmap-check.mjs:146-147/:187-189` + `ledger-frontmatter.mjs:187-196` verify:[] 拒绝（`:104` 解析面分层钉住）+ `ledger-sections.mjs:448-464` no-verify-keys fail-closed precedence）、命令复跑 **899/899 pass / 0 fail** + prompt-check OK、live 探针（temp dir 零 repo 触碰：未知字段/负 audit-rounds/verify:[]+注入默认键三 fixture 全拦 + `rg "^verify: \[\]\s*$"` 语料零命中）、回写面（roadmap :62/:64 WI42/WI44 `[x]` + Last Updated :7、01 §4.1 :69 裁定句、CONTEXT.md:72、本日志 :55-68 三 Phase 条目）、Deferred 诚实性、anti-forgery（dispatch 行 id/session 精确匹配且零 accepted 残留）。收口后核验：`plan-check` 本 plan derivedCompleted=true / reasons 空 / fieldsValid=true、谓词族 `activePlans` 离队 + `closedPlans` 入队。收口链复跑：`pnpm --prefix tools/mission-driver test` **899/0**、`./verify-age.sh` **L1+L2 GREEN**（closure 25/26 + freshness 43 文件 content-equal + smoke-import ok）、`mission-check` exit 0。roadmap WI42/WI44 执行期已 `[x]` 带证据指针（Phase 3 回写项），本步无 roadmap 改动需求；0925 批次余 N=3（0925-3/WI43）执行期已收口。

- **VERIFY_BUILD 复验 full-green（零 diff）**（mission `age-autonomy-implementation`，run 2026-08-26-072439 BUILD_VERIFY 复跑）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899/0**；web/dist 复核与 committed 世代一致（`web/src/` 无 diff，build 未产生 dist 变更）。工作树在验证前即洁净——本步零代码改动，仅落本条复验记录。

- **0925-1（M2-WI41）收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439）：`docs/plans/age-autonomy/2026-08-25-0925-1-m2-wi41-closure-routing-deadlock.md` 落 `## Verification` pass 行 + `## Closure` dispatch/accepted 回执对（`#audit-2026-08-26-072439-mission-driver-…-m2-wi41-closure-routing-deadlock-1-8bc8bf3e`，` models={exec:zhipuai/glm-5.2,aud:zhipuai/glm-5.2}` = policy single-model 降级诚实记录），plan 由 `deriveCompleted` 派生 **completed**（27/27 计数域全勾 + pass 行 basisHash=15599b35…c644 绑定 + 回执对同 id 配对；frontmatter `status` 维持 active——completed 禁写）。pass 行由 `gate-check <plan> --verify`（verify-runner，M2-WI19 交付面）机械产出：真实 `commands.test` exit 0。独立冷放审计（ses_auditor_2026-08-26-072439 subagent）一轮 approved 六面全 PASS：工件实证（closureScriptCheck 第三 fail 条件 :260-273 + defaultVerifyKeys 三面注入 + `missionDefaultVerifyKeys` 单一实现 + inspectPlan 四增量字段；`closure-routing.test.js` 单文件复跑 13/0）、全量复跑 **899/0** + prompt-check OK、`engine.js`/`flows/plan-execution.json` 零 diff、回写面（bug doc D2 fixed / roadmap WI41 `[x]` + Follow-up P2 ×2 清偿 / CONTEXT.md / 08-25 日志三 Phase 条目）、Deferred 诚实性。非阻塞注记 1 项：Verification 首段「18 例」算术误写（live = 13 例，耐久回写均为 13）已按 append-only 纪律追加更正行。收口后核验：`plan-check` 本 plan derivedCompleted=true / reasons 空、`gate-check` 本 plan exit 0（completion formula satisfied）——**D2 死锁受害者 0635-3 已于本 run 早前恢复收口，WI41 修复的路由面在后续 run 不再产生新受害者**。roadmap WI41 执行期已 `[x]` 带证据指针（Phase 3 回写项），本步无 roadmap 改动需求。

- **VERIFY_BUILD full-green + 0815-3 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 BUILD_VERIFY 步）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899/0**、`npm --prefix plugin/dsh test` **240/0**（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc 干净）；web/dist 与 committed 世代一致（本批 `web/src/` 无 diff，build 未产生 dist 变更）。0815-3 收口回执对 + 语料钉扎同步（§10/§16 completed 语料类扩为 0635-3/0815-1/0815-2/0815-3）随本步 doc commit 提交（承 a6fac25 单提交先例——corpus 钉扎读 live plan 派生态，与回执对不可拆分）。

- **0815-3 收口回执落库 + 语料钉扎同步**（mission `age-autonomy-implementation`，run 2026-08-26-072439，M2-WI17+WI18+WI19+WI20 全批三 plan 收口完毕）：`docs/plans/age-autonomy/2026-08-25-0815-3-m2-supporting-gates.md` `## Closure` 落 dispatch/accepted 回执对（`#audit-2026-08-26-072439-mission-driver-…-0815-3-m2-supporting-gates-1-f7417fa6`，` models={exec:glm-5.2,aud:glm-5.2}` lineage 后缀同 0815-2 先例 = policy single-model 降级诚实记录），plan 由 `deriveCompleted` 派生 completed（27/27 计数域全勾 + pass 行 basisHash=483f44e2…0429 绑定 + 回执对同 id 配对；frontmatter `status` 维持 active——completed 禁写）。独立冷放审计一轮 approved（六面全 PASS：五规则可达性 + policy 六 gate 条目、真实命令复跑、engine.js 零 diff + 零 npm 运行时依赖、文档同步、Deferred 诚实性、append-only 纪律；非阻塞注记 = live 计数 899/240/107 较 plan 执行期记录 863/221/88 增长系同批后继 plan 增补非回退）。语料钉扎随 live 派生态同步（承 0815-2 收口纪律、修正 8d3c92b 同类滞后教训）：truth table §10/§16 completed 语料类扩为 0635-3/0815-1/0815-2/0815-3 循环。收口后全量验证绿：truth table **107/0**、`pnpm --prefix tools/mission-driver test` **899/0**（prompt-check OK）、`npm --prefix plugin/dsh test` **240/0**（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc 干净）；审计期命令证据：`gate-check 0635-3 --verify` 端到端 exit 0（真实 commands.test exit 0、合法 passLine）、`./verify-age.sh` **L1+L2 GREEN**、`mission-check` exit 0、`gate-check --policy` exit 0（17 gates 含配套门禁六条 enforce）、`git diff --stat engine.js` 为空（零引擎 diff）。roadmap WI17/WI18/WI19/WI20 执行期已 `[x]` 带证据指针与 M3/WI25/WI26/WI27 残项注记（Phase 3 回写项），本步无 roadmap 改动需求。

- **VERIFY_BUILD full-green + 0815-2 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 CLOSURE_AUDIT 步）：`docs/plans/age-autonomy/2026-08-25-0815-2-m2-three-hard-gates.md` `## Closure` 落 dispatch/accepted 回执对（**dispatch 行 ` models=` lineage 后缀首个生产写入者**——exec/aud 同模型对 = policy 已声明 single-model 降级的诚实记录），plan 由 `deriveCompleted` 派生 completed（25/25 计数域全勾 + pass 行 basisHash 绑定 + 回执对同 id 配对；frontmatter `status` 维持 active——completed 禁写）。独立冷放审计两轮：round 1 发现 0815-1 收口回执（8d3c92b）遗留的语料钉扎滞后 2 例（truth table §10/§16 断言 0815-1 awaitingClosure——本 plan 交付的 corpus 用例，106/108 红）→ 按 0635-3 先例修正：completed 语料类扩为 0635-3/0815-1/0815-2 循环、awaitingClosure corpus 钉扎退役（语义由构造 gate3 fixture 钉住——live awaitingClosure 态转瞬即逝，任何 run 的 BUILD_VERIFY/CLOSURE_AUDIT 步可合法闭环之，不可作钉扎语料）、§16 旗标翻转；round 2 复核 approved。修正后全量验证绿：truth table **107/0**、`pnpm --prefix tools/mission-driver test` **899/0**（prompt-check OK）、`npm --prefix plugin/dsh test` **240/0**（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc 干净）、`./verify-age.sh` **L1+L2 GREEN**、`gate-check --policy` exit 0（17 gates）+ 0815-2/0635-3 corpus exit 0、web typecheck/build 绿（web/src 无 diff，dist 与 committed 世代一致）、`lint:prompts` OK、`git diff --stat engine.js` 为空（零引擎 diff）。

- **VERIFY_BUILD full-green + 0815-1 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 后续验证步，本步零代码 diff）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899 pass/0 fail**；web/dist 复核与 committed 世代一致（本批 `web/src/` 无 diff，build 未产生 dist 变更）。`docs/plans/age-autonomy/2026-08-25-0815-1-m2-law-seam-policy-schema.md` `## Closure` 落 CLOSURE_AUDIT dispatch/accepted 回执对（独立冷放审计通过：31/31 计数域全勾、899 复跑全绿、engine.js 零 diff、roadmap/CONTEXT.md/02-rule-law/changelog 同步实证、Deferred 诚实性核验），plan 由 `deriveCompleted` 派生 completed——随本步 doc commit 提交。

- **VERIFY_BUILD full-green + 0635-3 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` **899/0**、`npm --prefix plugin/dsh test` **241/0**、`./verify-age.sh` **L1+L2 GREEN**（web/src 无 diff，dist 与 committed 世代一致）。**语料状态追踪修复**：0635-3 由本 run CLOSURE_AUDIT 落下 dispatch/accepted 回执对后 `deriveCompleted` 翻为 completed——真值表两例 awaitingClosure 语料钉住（law-truth-table.test.mjs §10/§16）随之更新（0815-1 单独钉 awaitingClosure、0635-3 新增 completed 语料用例断言「completion formula satisfied」allow 面，241 例）。WI21 实现批次 + 0635-3 收口回执随本步分三笔提交（feat 代码 / 0950-1 docs / 0635-3 closure receipts）。

- **age-autonomy M2-WI21 Phase 3 收口：P8 执法层自护、姿态裁定与回写**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 3，全 plan 三 Phase 收口）：
  - **`law-self-protection` 规则**（`law-rules.mjs`，P8 唯一无条件对抗级）：保护路径四族（`plugin/dsh/src/law/**` 递归前缀 + `missions/autonomy.policy.yml` + `tools/mission-driver/src/{plan-check,gate-check}.mjs` 精确匹配）按 `ctx.projectRoot` 解析（`isLawProtectedPath` 导出供适配层/CLI 复用）；例外集 = 02 §4.7 字面三支——human（actor role）/ CI（部署面承载，写入不经 pre-execute，边界对称覆盖 human git-commit——02 §2 A1 成文漏杀面）/ 已批准立项（`activePlanReferencing`：active plan 正文含目标路径字符串（绝对或 projectRoot 相对形），reason 记命中 plan 文件与行）；**`engine` 不入例外集**（反向真值表用例钉住——字面三支无 engine，全部合法写入面由已批准立项支覆盖）；plan corpus 未注入 = fail-closed deny（对抗面不在不可观测事实上开门）；身份依赖例外（human）结构面退化 unverified-writer 注记、actor 无关条件（保护路径 ∧ 无 active-plan 引用）结构面可判。
  - **policy 注册**（`missions/autonomy.policy.yml` 17 gates）：plan-structure 切 **enforce**（0815-1 注释预留的收紧开关由本 plan 兑现）+ work-item 注册增项随种子规则生效；新 gate 条目 path-guardrail（`action:write` 面）/ roadmap-write-guard（`{{roadmapPath}}`）/ law-self-protection ×4（`{{projectRoot}}` 前缀——law-policy schema 扩展第三占位符，match/resolve 双面）全 enforce（姿态裁定成文于 policy 注释：窄域可判定事实同 0815-3 论证；P8 observe 爬坡 = 被执法者可改执法者的漏洞窗口，直接 enforce）。
  - **消费面接线**：DSH 适配层 ctx 四增注（`projectRoot`/`plansRoots`（`discoverPlansRoots` 祖先走查缓存）/`roadmapText`（每次新鲜读——enforce 后防 stale 注册表误判）/`plans`（保护路径时读 governing mission plansDir 语料 `readPlanRecordsUnder`））；gate-check CLI 同 ctx + 保护路径语料注入 + `workItem` 对账明细输出；`loadLawContextAt` 增 one-mission-one-roadmap 冲突检查（Phase 2）；`lawGateMountSummary` 更新。
  - **Proof（真值表 +5 例至 107，WI24 ≥30 超额推进）**：P8 四路径 × executor/drafter/engine 三角色全 deny（reason 列例外通道 + engine 显式「not in the exception set」反向句）+ `isLawProtectedPath` 边界（嵌套 law 路径命中/非保护路径/域外路径不命中）；human allow（例外①）/ active-plan fixture allow（例外③，记文件+行）/ draft plan 不携带例外 / corpus 缺省 fail-closed / id-only deny 含 unverified-writer 注记；域外面（非保护路径 + projectRoot 缺省）注记不 deny；**自指一致性**（真实语料 12 份 plan 记录下评估 policy 文件写入 → allow 经 active plan 引用；narrowed 到本 plan 单记录同样 allow——规则的第一个合法消费者 = 它自己的宿主 plan）；**真实 17-gate enforce policy 全 ctx corpus 无误杀**（10 份 frontmatter plan + 00-guide 全 allow，path-guardrail 逐文件「inside registered plans root」断言，guide 无 frontmatter 域外）。
  - **收口链（全绿）**：`node --test plugin/dsh/test/law-truth-table.test.mjs` 107/0；`pnpm --prefix tools/mission-driver test` 899/0（prompt-check OK；law-policy.test 真实实例断言同步 17 gates）；`npm --prefix plugin/dsh test` 240/0（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc --noEmit 干净）；`./verify-age.sh` **L1+L2 GREEN**；web typecheck/build 绿（web/src 无 diff，dist 与 committed 世代一致）；`lint:prompts` OK；gate-check CLI corpus 13 文件（10 frontmatter + 2 legacy + 00-guide）全 exit 0 allow；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）。
  - **文档同步与回写**：`tools/mission-driver/CONTEXT.md` 增「路径与结构护栏 + 执法层自护 P8」段；roadmap WI21 `[x]` + 证据指针（含残项注记：「已批准立项」判据结构近似归 M3 精确化、02 §4.7 保护清单 vs 0815-1 引擎侧放置的字面错位归 M5 WI39、path-guardrail 并集域弱化 watch-only）+ `> Last Updated` 头部同步（WI24 不勾——M2 收口门归 N=3）。注记：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写，完成由全勾 + `## Verification` pass 行 + `## Closure` 回执派生（父 mission BUILD_VERIFY/CLOSURE_AUDIT 步为唯一写者，同 0925 批次先例）。

- **age-autonomy M2-WI21 Phase 2：路径护栏、one-mission-one-roadmap 与 roadmap 写回护栏**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 2）：
  - **plan 形文件判定面（Decision 成文）**：path-guardrail 拦截目标 = `.md` 写入 ∧ proposedContent frontmatter 三键齐备（`status`+`mission`+`work-item`，宽松于全字段——非 plan 文档不会巧合三键齐备）；备选「拦一切含 frontmatter 的 .md」否决（00-guide/00-roadmap-guide 会被误杀）；legacy `> Plan Status:` 形域外写入不在本规则面（归 N=2/WI22）。**域弱化残险成文**：并集域（全 mission plansDir ∪ 各祖先默认 docs/plans）非 per-mission 强读法，跨 mission 错放由 CI 结构面兜底（Deferred watch-only）。
  - **`path-guardrail` 规则**（law-rules.mjs，structural 注册）：域内 allow / 域外 deny + reason 列出注册根集与合法路径；新建与改写同拦（proposedContent 判定等价）；`ctx.plansRoots` 缺省 fail-open 注记（02 §6）。域计算 = 调用面被动扫描（承袭 plan-status-gate `knownPlansRootsAt` 先例，malformed mission 零根）：DSH 适配层 `discoverPlansRoots`（祖先走查 + per-ancestor 缓存）+ gate-check CLI `knownPlansRoots` 同源实现。
  - **one-mission-one-roadmap 边界**（01-file-ledger :30 边界条）：`mission-check.mjs` 新增 `checkRoadmapUniqueness(missionsDir)`（light-parse 扫描 roadmapPath 声明、重复 = 结构化冲突项含冲突 mission 名；malformed/无 roadmapPath 零贡献）+ CLI 接线（loadMission 通过后跑 sibling 检查，冲突 exit 1 fail-fast）；DSH 适配层 `loadLawContextAt` 重构为全量收集 + 冲突检查（冲突 → 该祖先贡献零 law 上下文，mission 集歧义不静默执法）。本仓库 live 合规（各 mission roadmapPath 互异），冲突 fixture 双面钉住。
  - **roadmap 写回护栏语义（字面 vs 实践裁定成文）**：允许 = 已注册 WI 行勾选翻转（仅 `[ ]→[x]`）+ 行内尾部证据注记追加（M1 以来回写实践 = roadmap guide :66 的既成解释）；deny = WI 行新增/删除/换序、WI id 改写、milestone 标题增删改——例外 = actor role ∈ {engine, supervisor}（deep-audit findings 落 WI 的引擎路径；id-only 面 role 例外退化为 deny reason 内 unverified-writer 注记不冒充）∨ 已批准立项（active plan 正文含 roadmap 路径字符串，`activePlanReferencing` 结构近似 + reason 记命中 plan 文件与行）。残险成文：WI 行内非注记内容改写不在此拦（CI 结构校验 + git 归因兜底，02 §2 A1）。
  - **`roadmap-write-guard` 规则**（structural 注册，self-domain = ctx.roadmapPath）：current/proposed `scanRoadmapLedger` WI 结构比对（milestone 块序/号/题 + 逐行 id 序保持 + 勾选单向 + rest 前缀保持=尾部追加）；无 currentFileState = 不可观测注记；legacy 不可比域外。**真实 roadmap 语料抽验**：WI21 勾选翻转与证据追加两形态构造评估均 allow（未实际改 roadmap）。
  - **Proof（真值表 +10 例至 102）**：path-guardrail 3 例（域内新建/改写、域外 deny 列根集、非 plan 形/两键缺/legacy/非 .md/根缺省域外或注记）；边界 2 例（合规 pass + 冲突 fail-fast 命名双方 + live 仓库合规断言；适配层面冲突 = 惰性放行零记录）；rwg 5 例（翻转/追加/组合 allow；删行/改 id/改行文/反勾/换序/改题/增 milestone deny；engine allow / drafter deny / id-only deny 含注记 / approved-project allow 含 draft 反例；惰性面；真实语料 smoke）。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 102/0；`pnpm --prefix tools/mission-driver test` 899/0；`npm --prefix plugin/dsh test` 235/0（freshness 43 文件 content-equal）；mission-check CLI 冲突 fixture exit 1（结构化报错指明冲突 mission 名）；live 仓库 mission-check exit 0。

- **age-autonomy M2-WI21 Phase 1：work-item 注册面（复合标签裁定 + 注册谓词）**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 1）：
  - **复合标签语法钉住**（0815-3 Non-Goal 移交项兑现）：`law-core.mjs` `expandWorkItemLabel`——合法 = `M<n>-WI<a>` 单项或 `M<n>-WI<a>+WI<b>+…`（首 token 携里程碑前缀、后续裸 `WI<m>` 继承同里程碑；后续显式重复 `M<n>-` 前缀 = 等价展开接受，跨里程碑显式形态每 token 按自带里程碑对账）；畸形分隔符（`WI12+`/`+WI12`/连续 `+`）、空标签、裸 token 首位 → deny reason 指向合法形态。
  - **注册谓词** `workItemRegistered(label, roadmapScan)`（law 内核纯函数）：(milestone, WI id) 二元组对 `scanRoadmapLedger` 注册表逐 token 对账；未命中 reason 含未命中 token + 注册表合法集提示（02 §2 结构化 deny）；注册表空/缺里程碑 fail（不空过）。接进 `plan-structure` 校验链：语法面（label 单独可判定）无条件 deny；注册面需 `ctx.roadmapText` 注入——缺省 = 事实不可观测 → allow + note（02 §2 纪律，不冒充）。
  - **CLI 面**：`gate-check.mjs <plan.md>` 增 owning-mission 走查（`discoverOwningMission`）+ roadmap 读取注入 + 输出 `workItem` 对账明细（label/expanded/registered/missing/roadmap）；同函数预置 `projectRoot`/`plansRoots` ctx（Phase 2/3 消费）。
  - **Proof（真值表 +4 例至 92）**：语法矩阵（单项/复合/显式前缀/跨里程碑/六种畸形）；注册表反例（空注册表/无里程碑扫描/prose-only roadmap = fail）；**live 语料对账**（10 份 frontmatter plan 对真实 roadmap 全 pass——M1-WI4+WI7+WI8+WI9+WI10+WI11 等复合标签逐 token 命中；legacy 0635-1/2 无 frontmatter 域外确认）；plan-structure 面四列（注入 roadmap 合法 allow / 未注册 deny 指向注册表 / 无 roadmap 语法 deny / 无 roadmap 注册面 note）。
  - **CLI corpus 抽验**：12 份 plan 文件 gate-check exit 0（10 份 frontmatter `workItem.ok=true`、2 份 legacy `applicable=false`）；构造反例（`M2-WI21+WI99` 临时文件于 plansDir 域内）CLI exit 1 + deny reason 列出 M2 注册集（写面与 CLI 面双 deny）。
  - **policy schema 预扩展**：`{{projectRoot}}` 第三占位符进 `POLICY_PLACEHOLDERS`/`matchPatternError`/`resolvePolicyPlaceholders`（Phase 3 P8 门 match 面所需，law-policy.test 既有断言 `/match must start with/` 兼容）。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 92/0；`pnpm --prefix tools/mission-driver test` 899/0（prompt-check OK）；`npm --prefix plugin/dsh test` 225/0（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc 干净）。

- **VERIFY_BUILD full-green**（mission `age-autonomy-implementation`，HEAD `f590572` 后复核，本步零代码 diff）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899 pass/0 fail**；web/dist 复核与 committed 世代一致（本批 `web/src/` 无 diff，build 未产生 dist 变更）。WI43 纯文档批次（baseline §Public Exports 登记 + packaging 四漂移修复 + roadmap/plan 勾选回写）随本步 doc commit 提交。

- **age-autonomy M2-WI43 架构 owner-doc 契约同步：全 plan 三 Phase 收口**（plan `docs/plans/age-autonomy/2026-08-25-0925-3-m2-wi43-arch-ownerdoc-contract-sync.md`，纯文档批次）：
  - **Phase 1（baseline 登记）**：`docs/architecture/mission-driver-baseline.md` §Public Exports 登记 M1 ledger 三模块各一条（`ledger-frontmatter.mjs` 解析器+双校验器+状态词表常量，契约 01 §2/§3.1/§4.1/§5.1；`ledger-sections.mjs` 计数域扫描+内联审计区校验+basisHash/deriveCompleted+扫描谓词族，01 §2(5)/§3.2/§3.3/§4.2/§4.4/§5.2；`ledger-dualread.mjs` 双读 resolver+`MISSION_DRIVER_LEDGER` 断路器+`PLAN_STATUS_RE` 单实现，01 §5.2）；各条注明消费面（四引擎读面 + plugin assets 复制通道 + 两 guide machine face 声明）与零 import（frontmatter）/零 npm 约束；M1 交付进度句（0635 批次指针）按 Update Rule 字面形态（同 change 登记，该文件无 changelog/状态段形态）落地；`rg "ledger|dualread|frontmatter"` 全文核对——修前 0 命中、修后全部为新登记行，零漂移残留。
  - **Phase 2（packaging 四漂移修复）**：`docs/architecture/dsh-plugin-packaging.md` ①计数 :108/:121 19→**26**；②PLAN_STATUS_RE 源 :212/:279 `assets/src/plan-check.mjs` → `assets/src/ledger-dualread.mjs`（修前恰两处命中，修后 `rg` 零命中，「零 engine diff」语境保留）；③import-graph 补 ledger 链精确边（plan-check → dualread+sections；roadmap-check → dualread+sections+frontmatter〔frontmatter 边为 WI42 接线后新增，live 穷尽核对补入〕；flow-loader → dualread 直连；库内链 dualread → frontmatter+sections、sections → frontmatter；monitor 边按 NOT-bundled 排除，未入图）；④`rg "ledger|allowed modules|ALLOWED|\b19\b"` 全文核对（divergence ledger/six-call ledger 等无关命中排除，零漂移残留）。
  - **执行时核定（计数 22→26）**：plan 撰写时 live 计数 22（19+ledger 3）；0815 law 批次（4 模块）在 plan 定稿与执行之间落地，执行时 live = ALLOWED_MODULES 26 = `assets/src` 实测 26（逐名一致，closure 25/26 = verify-runner unreachable-allowed，`build-bundle --check` 钉住）。按 Phase 2 退出判据「文档模块计数与 ALLOWED_MODULES 及 assets/src 实测清单一致」的主句（一致性）优先于括注快照值（22），取 26 并写明组成——写 22 将制造本 plan 要修复的同族新漂移。裁定记入 roadmap WI43 证据行。
  - **Phase 3（回写与收口）**：roadmap WI43 `[x]` + 证据指针 + `> Last Updated` 头部同步（WI24 不勾——M2 收口门归后续批次）；收口断言链全绿：`rg -c "ledger-<name>"` baseline 逐模块 =2（≥1×3）；`rg "19 (allowed|files)"` packaging 零命中；`rg "assets/src/plan-check.mjs"` packaging 零命中；`pnpm --prefix tools/mission-driver test` **899/899 全绿（prompt-check OK）**；`mission-check` exit 0。
  - **注记①（范围外发现，未执行）**：law 层登记债无主——0815 批次各 plan 内无 architecture 文档同步项（live 核实），其遗留三处待登记面：baseline §Public Exports law 四模块（law-core/law-policy/law-rules/verify-runner）零登记、packaging import-graph law 边（config.js → law-policy 链）未枚举、packaging Config path bullet「imports only node builtins + mission-check.mjs」已失实（config.js:5 现导入 law-policy.mjs）。本 plan Non-Goals 明示不收编；已按 dispatch 指令仅报告不动手，待 human/下轮 audit 立项归属。
  - **注记②**：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写（01 §5.1），`## Verification`/`## Closure` 由父 mission BUILD_VERIFY/CLOSURE_AUDIT 步写入（同 0925-2 批次先例）。

- **age-autonomy M2-WI42+WI44 Phase 3 收口：文档同步与回写**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 3，全 plan 三 Phase 收口）：
  - roadmap 回写：WI42/WI44 `[x]` + 证据指针（读 seam/四消费面/双层封堵/测试文件/899-0）；`> Last Updated` → 2026-08-26；WI24 不勾（M2 收口门归后续批次）。
  - `docs/design/age-autonomy/01-file-ledger.md` §4.1 `verify` 行补空数组语义裁定句（显式 `[]` = 校验器拒绝；派生面 no-verify-keys fail-closed 不回落默认——「未定义空数组语义」成文为契约）。
  - `tools/mission-driver/CONTEXT.md` frontmatter 账本库段增接线 + 空真封堵语义句（M2-WI42/WI44）。
  - **收口链（全绿）**：`mission-check` exit 0；`./verify-age.sh` L1 899/0（prompt-check OK）+ L2 221/0（freshness 43 文件 content-equal、smoke-import ok）GREEN；web typecheck/build 绿、`lint:prompts` OK；语料断言 `rg "^verify: \[\]\s*$" docs/plans docs/backlog missions` 零命中（收口复跑钉住）。
  - 注记：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写（本 plan Phase 1 执法的核心禁令），完成由全勾 + `## Verification` pass 行 + `## Closure` 回执派生（父 mission CLOSURE_AUDIT/BUILD_VERIFY 步为唯一写者）。

- **age-autonomy M2-WI44 Phase 2：`verify: []` 空真通道双层封堵**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 2）：
  - **校验器层**：`ledger-frontmatter.mjs` `validatePlanFrontmatter` 对 `verify: []` 报 error（字面含合法路径指路：非空 command-key 数组或省略字段回落 mission 默认）；`parseFrontmatter` 解析面不变（`verify: []` 仍合法格式子集，既有解析用例零修改——解析/校验两层分离）。`law-core.mjs` `plan-structure` 规则消费同一校验器（`law-core.mjs:232`），拒绝自动随写时 observe 面生效，law-rules 零改动（`verify-keys` 注记「空真归 WI44」维持成立）。
  - **派生层防御**：`ledger-sections.mjs` `deriveCompleted` 对显式空数组按 `no-verify-keys` 处理（`mechanicalVerification=false`）且 **fail-closed 优先于** `opts.defaultVerifyKeys`（显式空=拒绝语义，非缺失语义——与 0925-1 Phase 2 注入的组合行为钉住）；覆盖不经校验器的输入路径（外部写入/旧文件/旁路调用方）。
  - **Proof**：`ledger-frontmatter.test.js` 新增拒绝用例（含 omission 合法对照）；`ledger-derivation.test.js` 新 describe 四例（①双层面钉住 ②旁路防御 ③precedence fail-closed ④`verify: [test]` 绿路径无伤）；语料断言 `rg "^verify: \[\]\s*$" docs/plans docs/backlog missions` 零命中（收口复跑钉住）。
  - **验证**：`pnpm --prefix tools/mission-driver test` 899/0（+5；prompt-check OK）；plugin assets 重建（freshness 43 文件 content-equal）；`npm --prefix plugin/dsh test` 221/0（truth table + smoke-import 绿）。

- **age-autonomy M2-WI42 Phase 1：校验器生产读面接线**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 1）：
  - **读 seam**：`ledger-dualread.mjs` `fmReadResult`（`readPlanStatus` frontmatter 分支）调 `validatePlanFrontmatter(split.fm)`，返回值增 `fieldErrors[]`/`fieldsValid`，随 `planLedgerState` 透传——plan-check/flow-loader/monitor 三引擎读面一次接线全覆盖（01 §5.2 单实现纪律）；非 frontmatter 格式不附键（legacy/guide 无字段集可校验）。
  - **后果矩阵落地**：① plan-check `analyzeFrontmatter` 把 fieldErrors 并入 `field:` 前缀 details → exit 1（不分 strict 档）；② flow-loader `_scanPlansByStatus` 对 field-invalid plan `console.warn` 一次/文件/扫描（`[flow-loader] plan field validation failed: <relpath> — <summary>`；队列成员资格仍按可解析 status——kill silence 非 kill queue）；③ monitor plans API 条目透传 `fieldErrors`（`handleGetRoadmap` 经 `parseRoadmapMarkdown` spread 同获覆盖）；roadmap-check `parseRoadmapMarkdown` `hasFm` 点跑 `validateRoadmapFrontmatter` + 新 CLI 面（exit 0/1/2，镜像 plan-check CLI 约定）。
  - **Proof**：新 `test/field-wiring.test.js`（deep-audit R1 三探针反向钉住——手写 `status: completed` exit 1、`verfy` typo exit 1、roadmap 缺/负 `audit-rounds` 非 0；flow-loader warn console-spy 断言 + 成员资格不变；legal corpus 5 文件 CLI exit 0）；corpus 断言 `fieldsValid === true` + roadmap `fieldErrors []`（零误杀）；monitor plans API fieldErrors 端到端。
  - **验证**：`pnpm --prefix tools/mission-driver test` 894/0（813 基线 + WI41 批 +13 → 881 后本 Phase +13；prompt-check OK）；plugin assets 重建（build-bundle 43 文件，4 模块 diff）；web typecheck/build 绿（types 增 `fieldErrors?`）；`lint:prompts` OK。
