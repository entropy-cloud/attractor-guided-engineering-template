# Development Log - 2026-08-26

### 2026-08-26

- **VERIFY_BUILD full-green + 0815-1 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439 后续验证步，本步零代码 diff）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899 pass/0 fail**；web/dist 复核与 committed 世代一致（本批 `web/src/` 无 diff，build 未产生 dist 变更）。`docs/plans/age-autonomy/2026-08-25-0815-1-m2-law-seam-policy-schema.md` `## Closure` 落 CLOSURE_AUDIT dispatch/accepted 回执对（独立冷放审计通过：31/31 计数域全勾、899 复跑全绿、engine.js 零 diff、roadmap/CONTEXT.md/02-rule-law/changelog 同步实证、Deferred 诚实性核验），plan 由 `deriveCompleted` 派生 completed——随本步 doc commit 提交。

- **VERIFY_BUILD full-green + 0635-3 收口回执落库提交**（mission `age-autonomy-implementation`，run 2026-08-26-072439）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` **899/0**、`npm --prefix plugin/dsh test` **241/0**、`./verify-age.sh` **L1+L2 GREEN**（web/src 无 diff，dist 与 committed 世代一致）。**语料状态追踪修复**：0635-3 由本 run CLOSURE_AUDIT 落下 dispatch/accepted 回执对后 `deriveCompleted` 翻为 completed——真值表两例 awaitingClosure 语料钉住（law-truth-table.test.mjs §10/§16）随之更新（0815-1 单独钉 awaitingClosure、0635-3 新增 completed 语料用例断言「completion formula satisfied」allow 面，241 例）。WI21 实现批次 + 0635-3 收口回执随本步分三笔提交（feat 代码 / 0950-1 docs / 0635-3 closure receipts）。

- **age-autonomy M2-WI21 Phase 3 收口：P8 执法层自护、姿态裁定与回写**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 3，全 plan 三 Phase 收口）：
  - **`law-self-protection` 规则**（`law-rules.mjs`，P8 唯一无条件对抗级）：保护路径四族（`plugin/dsh/src/law/**` 递归前缀 + `missions/autonomy.policy.yml` + `tools/mission-driver/src/{plan-check,gate-check}.mjs` 精确匹配）按 `ctx.projectRoot` 解析（`isLawProtectedPath` 导出供适配层/CLI 复用）；例外集 = 02 §4.7 字面三支——human（actor role）/ CI（部署面承载，写入不经 pre-execute，边界对称覆盖 human git-commit——02 §2 A1 成文漏杀面）/ 已批准立项（`activePlanReferencing`：active plan 正文含目标路径字符串（绝对或 projectRoot 相对形），reason 记命中 plan 文件与行）；**`engine` 不入例外集**（反向真值表用例钉住——字面三支无 engine，全部合法写入面由已批准立项支覆盖）；plan corpus 未注入 = fail-closed deny（对抗面不在不可观测事实上开门）；身份依赖例外（human）结构面退化 unverified-writer 注记、actor 无关条件（保护路径 ∧ 无 active-plan 引用）结构面可判。
  - **policy 注册**（`missions/autonomy.policy.yml` 17 gates）：plan-structure 切 **enforce**（0815-1 注释预留的收紧开关由本 plan 兑现）+ work-item 注册增项随种子规则生效；新 gate 条目 path-guardrail（`action:write` 面）/ roadmap-write-guard（`{{roadmapPath}}`）/ law-self-protection ×4（`{{projectRoot}}` 前缀——law-policy schema 扩展第三占位符，match/resolve 双面）全 enforce（姿态裁定成文于 policy 注释：窄域可判定事实同 0815-3 论证；P8 observe 爬坡 = 被执法者可改执法者的漏洞窗口，直接 enforce）。
  - **消费面接线**：DSH 适配层 ctx 四增注（`projectRoot`/`plansRoots`（`discoverPlansRoots` 祖先走查缓存）/`roadmapText`（每次新鲜读——enforce 后防 stale 注册表误判）/`plans`（保护路径时读 governing mission plansDir 语料 `readPlanRecordsUnder`））；gate-check CLI 同 ctx + 保护路径语料注入 + `workItem` 对账明细输出；`loadLawContextAt` 增 one-mission-one-roadmap 冲突检查（Phase 2）；`lawGateMountSummary` 更新。
  - **Proof（真值表 +5 例至 107，WI24 ≥30 超额推进）**：P8 四路径 × executor/drafter/engine 三角色全 deny（reason 列例外通道 + engine 显式「not in the exception set」反向句）+ `isLawProtectedPath` 边界（嵌套 law 路径命中/非保护路径/域外路径不命中）；human allow（例外①）/ active-plan fixture allow（例外③，记文件+行）/ draft plan 不携带例外 / corpus 缺省 fail-closed / id-only deny 含 unverified-writer 注记；域外面（非保护路径 + projectRoot 缺省）注记不 deny；**自指一致性**（真实语料 12 份 plan 记录下评估 policy 文件写入 → allow 经 active plan 引用；narrowed 到本 plan 单记录同样 allow——规则的第一个合法消费者 = 它自己的宿主 plan）；**真实 17-gate enforce policy 全 ctx corpus 无误杀**（10 份 frontmatter plan + 00-guide 全 allow，path-guardrail 逐文件「inside registered plans root」断言，guide 无 frontmatter 域外）。
  - **收口链（全绿）**：`node --test plugin/dsh/test/law-truth-table.test.mjs` 107/0；`pnpm --prefix tools/mission-driver test` 899/0（prompt-check OK；law-policy.test 真实实例断言同步 17 gates）；`npm --prefix plugin/dsh test` 240/0（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc --noEmit 干净）；`./verify-age.sh` **L1+L2 GREEN**；web typecheck/build 绿（web/src 无 diff，dist 与 committed 世代一致）；`lint:prompts` OK；gate-check CLI corpus 13 文件（10 frontmatter + 2 legacy + 00-guide）全 exit 0 allow；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）。
  - **文档同步与回写**：`tools/mission-driver/CONTEXT.md` 增「路径与结构护栏 + 执法层自护 P8」段；roadmap WI21 `[x]` + 证据指针（含残项注记：「已批准立项」判据结构近似归 M3 精确化、02 §4.7 保护清单 vs 0815-1 引擎侧放置的字面错位归 M5 WI39、path-guardrail 并集域弱化 watch-only）+ `> Last Updated` 头部同步（WI24 不勾——M2 收口门归 N=3）。注记：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写，完成由全勾 + `## Verification` pass 行 + `## Closure` 回执派生（父 mission BUILD_VERIFY/CLOSURE_AUDIT 步为唯一写者，同 0925 批次先例）。

- **age-autonomy M2-WI21 Phase 2：路径护栏、one-mission-one-roadmap 与 roadmap 写回护栏**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 2）：
  - **plan 形文件判定面（Decision 成文）**：path-guardrail 拦截目标 = `.md` 写入 ∧ proposedContent frontmatter 三键齐备（`status`+`mission`+`work-item`，宽松于全字段——非 plan 文档不会巧合三键齐备）；备选「拦一切含 frontmatter 的 .md」否决（00-guide/00-roadmap-guide 会被误杀）；legacy `> Plan Status:` 形域外写入不在本规则面（归 N=2/WI22）。**域弱化残险成文**：并集域（全 mission plansDir ∪ 各祖先默认 docs/plans）非 per-mission 强读法，跨 mission 错放由 CI 结构面兜底（Deferred watch-only）。
  - **`path-guardrail` 规则**（law-rules.mjs，structural 注册）：域内 allow / 域外 deny + reason 列出注册根集与合法路径；新建与改写同拦（proposedContent 判定等价）；`ctx.plansRoots` 缺省 fail-open 注记（02 §6）。域计算 = 调用面被动扫描（承袭 plan-status-gate `knownPlansRootsAt` 先例，malformed mission 零根）：DSH 适配层 `discoverPlansRoots`（祖先走查 + per-ancestor 缓存）+ gate-check CLI `knownPlansRoots` 同源实现。
  - **one-mission-one-roadmap 边界**（01-file-ledger :30 边界条）：`mission-check.mjs` 新增 `checkRoadmapUniqueness(missionsDir)`（light-parse 扫描 roadmapPath 声明、重复 = 结构化冲突项含冲突 mission 名；malformed/无 roadmapPath 零贡献）+ CLI 接线（loadMission 通过后跑 sibling 检查，冲突 exit 1 fail-fast）；DSH 适配层 `loadLawContextAt` 重构为全量收集 + 冲突检查（冲突 → 该祖先贡献零 law 上下文，mission 集歧义不静默执法）。本仓库 live 合规（各 mission roadmapPath 互异），冲突 fixture 双面钉住。
  - **roadmap 写回护栏语义（字面 vs 实践裁定成文）**：允许 = 已注册 WI 行勾选翻转（仅 `[ ]→[x]`）+ 行内尾部证据注记追加（M1 以来回写实践 = roadmap guide :66 的既成解释）；deny = WI 行新增/删除/换序、WI id 改写、milestone 标题增删改——例外 = actor role ∈ {engine, supervisor}（deep-audit findings 落 WI 的引擎路径；id-only 面 role 例外退化为 deny reason 内 unverified-writer 注记不冒充）∨ 已批准立项（active plan 正文含 roadmap 路径字符串，`activePlanReferencing` 结构近似 + reason 记命中 plan 文件与行）。残险成文：WI 行内非注记内容改写不在此拦（CI 结构校验 + git 归因兜底，02 §2 A1）。
  - **`roadmap-write-guard` 规则**（structural 注册，self-domain = ctx.roadmapPath）：current/proposed `scanRoadmapLedger` WI 结构比对（milestone 块序/号/题 + 逐行 id 序保持 + 勾选单向 + rest 前缀保持=尾部追加）；无 currentFileState = 不可观测注记；legacy 不可比域外。**真实 roadmap 语料抽验**：WI21 勾选翻转与证据追加两形态构造评估均 allow（未实际改 roadmap）。
  - **Proof（真值表 +10 例至 102）**：path-guardrail 3 例（域内新建/改写、域外 deny 列根集、非 plan 形/两键缺/legacy/非 .md/根缺省域外或注记）；边界 2 例（合规 pass + 冲突 fail-fast 命名双方 + live 仓库合规断言；适配层面冲突 = 惰性放行零记录）；rwg 5 例（翻转/追加/组合 allow；删行/改 id/改行文/反勾/换序/改题/增 milestone deny；engine allow / drafter deny / id-only deny 含注记 / approved-project allow 含 draft 反例；惰性面；真实语料 smoke）。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 102/0；`pnpm --prefix tools/mission-driver test` 899/0；`npm --prefix plugin/dsh test` 235/0（freshness 43 文件 content-equal）；mission-check CLI 冲突 fixture exit 1（结构化报错指明冲突 mission 名）；live 仓库 mission-check exit 0。

- **age-autonomy M2-WI21 Phase 1：work-item 注册面（复合标签裁定 + 注册谓词）**（plan `docs/plans/age-autonomy/2026-08-25-0950-1-m2-wi21-path-structure-guardrails-p8.md` Phase 1）：
  - **复合标签语法钉住**（0815-3 Non-Goal 移交项兑现）：`law-core.mjs` `expandWorkItemLabel`——合法 = `M<n>-WI<a>` 单项或 `M<n>-WI<a>+WI<b>+…`（首 token 携里程碑前缀、后续裸 `WI<m>` 继承同里程碑；后续显式重复 `M<n>-` 前缀 = 等价展开接受，跨里程碑显式形态每 token 按自带里程碑对账）；畸形分隔符（`WI12+`/`+WI12`/连续 `+`）、空标签、裸 token 首位 → deny reason 指向合法形态。
  - **注册谓词** `workItemRegistered(label, roadmapScan)`（law 内核纯函数）：(milestone, WI id) 二元组对 `scanRoadmapLedger` 注册表逐 token 对账；未命中 reason 含未命中 token + 注册表合法集提示（02 §2 结构化 deny）；注册表空/缺里程碑 fail（不空过）。接进 `plan-structure` 校验链：语法面（label 单独可判定）无条件 deny；注册面需 `ctx.roadmapText` 注入——缺省 = 事实不可观测 → allow + note（02 §2 纪律，不冒充）。
  - **CLI 面**：`gate-check.mjs <plan.md>` 增 owning-mission 走查（`discoverOwningMission`）+ roadmap 读取注入 + 输出 `workItem` 对账明细（label/expanded/registered/missing/roadmap）；同函数预置 `projectRoot`/`plansRoots` ctx（Phase 2/3 消费）。
  - **Proof（真值表 +4 例至 92）**：语法矩阵（单项/复合/显式前缀/跨里程碑/六种畸形）；注册表反例（空注册表/无里程碑扫描/prose-only roadmap = fail）；**live 语料对账**（10 份 frontmatter plan 对真实 roadmap 全 pass——M1-WI4+WI7+WI8+WI9+WI10+WI11 等复合标签逐 token 命中；legacy 0635-1/2 无 frontmatter 域外确认）；plan-structure 面四列（注入 roadmap 合法 allow / 未注册 deny 指向注册表 / 无 roadmap 语法 deny / 无 roadmap 注册面 note）。
  - **CLI corpus 抽验**：12 份 plan 文件 gate-check exit 0（10 份 frontmatter `workItem.ok=true`、2 份 legacy `applicable=false`）；构造反例（`M2-WI21+WI99` 临时文件于 plansDir 域内）CLI exit 1 + deny reason 列出 M2 注册集（写面与 CLI 面双 deny）。
  - **policy schema 预扩展**：`{{projectRoot}}` 第三占位符进 `POLICY_PLACEHOLDERS`/`matchPatternError`/`resolvePolicyPlaceholders`（Phase 3 P8 门 match 面所需，law-policy.test 既有断言 `/match must start with/` 兼容）。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 92/0；`pnpm --prefix tools/mission-driver test` 899/0（prompt-check OK）；`npm --prefix plugin/dsh test` 225/0（closure 25/26 + freshness 43 文件 content-equal + smoke-import + tsc 干净）。

- **VERIFY_BUILD full-green**（mission `age-autonomy-implementation`，HEAD `f590572` 后复核，本步零代码 diff）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（prompt-check 全部 result-tag 例良构）、`pnpm --prefix tools/mission-driver test` **899 pass/0 fail**；web/dist 复核与 committed 世代一致（本批 `web/src/` 无 diff，build 未产生 dist 变更）。WI43 纯文档批次（baseline §Public Exports 登记 + packaging 四漂移修复 + roadmap/plan 勾选回写）随本步 doc commit 提交。

- **age-autonomy M2-WI43 架构 owner-doc 契约同步：全 plan 三 Phase 收口**（plan `docs/plans/age-autonomy/2026-08-25-0925-3-m2-wi43-arch-ownerdoc-contract-sync.md`，纯文档批次）：
  - **Phase 1（baseline 登记）**：`docs/architecture/mission-driver-baseline.md` §Public Exports 登记 M1 ledger 三模块各一条（`ledger-frontmatter.mjs` 解析器+双校验器+状态词表常量，契约 01 §2/§3.1/§4.1/§5.1；`ledger-sections.mjs` 计数域扫描+内联审计区校验+basisHash/deriveCompleted+扫描谓词族，01 §2(5)/§3.2/§3.3/§4.2/§4.4/§5.2；`ledger-dualread.mjs` 双读 resolver+`MISSION_DRIVER_LEDGER` 断路器+`PLAN_STATUS_RE` 单实现，01 §5.2）；各条注明消费面（四引擎读面 + plugin assets 复制通道 + 两 guide machine face 声明）与零 import（frontmatter）/零 npm 约束；M1 交付进度句（0635 批次指针）按 Update Rule 字面形态（同 change 登记，该文件无 changelog/状态段形态）落地；`rg "ledger|dualread|frontmatter"` 全文核对——修前 0 命中、修后全部为新登记行，零漂移残留。
  - **Phase 2（packaging 四漂移修复）**：`docs/architecture/dsh-plugin-packaging.md` ①计数 :108/:121 19→**26**；②PLAN_STATUS_RE 源 :212/:279 `assets/src/plan-check.mjs` → `assets/src/ledger-dualread.mjs`（修前恰两处命中，修后 `rg` 零命中，「零 engine diff」语境保留）；③import-graph 补 ledger 链精确边（plan-check → dualread+sections；roadmap-check → dualread+sections+frontmatter〔frontmatter 边为 WI42 接线后新增，live 穷尽核对补入〕；flow-loader → dualread 直连；库内链 dualread → frontmatter+sections、sections → frontmatter；monitor 边按 NOT-bundled 排除，未入图）；④`rg "ledger|allowed modules|ALLOWED|\b19\b"` 全文核对（divergence ledger/six-call ledger 等无关命中排除，零漂移残留）。
  - **执行时核定（计数 22→26）**：plan 撰写时 live 计数 22（19+ledger 3）；0815 law 批次（4 模块）在 plan 定稿与执行之间落地，执行时 live = ALLOWED_MODULES 26 = `assets/src` 实测 26（逐名一致，closure 25/26 = verify-runner unreachable-allowed，`build-bundle --check` 钉住）。按 Phase 2 退出判据「文档模块计数与 ALLOWED_MODULES 及 assets/src 实测清单一致」的主句（一致性）优先于括注快照值（22），取 26 并写明组成——写 22 将制造本 plan 要修复的同族新漂移。裁定记入 roadmap WI43 证据行。
  - **Phase 3（回写与收口）**：roadmap WI43 `[x]` + 证据指针 + `> Last Updated` 头部同步（WI24 不勾——M2 收口门归后续批次）；收口断言链全绿：`rg -c "ledger-<name>"` baseline 逐模块 =2（≥1×3）；`rg "19 (allowed|files)"` packaging 零命中；`rg "assets/src/plan-check.mjs"` packaging 零命中；`pnpm --prefix tools/mission-driver test` **899/899 全绿（prompt-check OK）**；`mission-check` exit 0。
  - **注记①（范围外发现，未执行）**：law 层登记债无主——0815 批次各 plan 内无 architecture 文档同步项（live 核实），其遗留三处待登记面：baseline §Public Exports law 四模块（law-core/law-policy/law-rules/verify-runner）零登记、packaging import-graph law 边（config.js → law-policy 链）未枚举、packaging Config path bullet「imports only node builtins + mission-check.mjs」已失实（config.js:5 现导入 law-policy.mjs）。本 plan Non-Goals 明示不收编；已按 dispatch 指令仅报告不动手，待 human/下轮 audit 立项归属。
  - **注记②**：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写（01 §5.1），`## Verification`/`## Closure` 由父 mission BUILD_VERIFY/CLOSURE_AUDIT 步写入（同 0925-2 批次先例）。

- **age-autonomy M2-WI42+WI44 Phase 3 收口：文档同步与回写**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 3，全 plan 三 Phase 收口）：
  - roadmap 回写：WI42/WI44 `[x]` + 证据指针（读 seam/四消费面/双层封堵/测试文件/899-0）；`> Last Updated` → 2026-08-26；WI24 不勾（M2 收口门归后续批次）。
  - `docs/design/age-autonomy/01-file-ledger.md` §4.1 `verify` 行补空数组语义裁定句（显式 `[]` = 校验器拒绝；派生面 no-verify-keys fail-closed 不回落默认——「未定义空数组语义」成文为契约）。
  - `tools/mission-driver/CONTEXT.md` frontmatter 账本库段增接线 + 空真封堵语义句（M2-WI42/WI44）。
  - **收口链（全绿）**：`mission-check` exit 0；`./verify-age.sh` L1 899/0（prompt-check OK）+ L2 221/0（freshness 43 文件 content-equal、smoke-import ok）GREEN；web typecheck/build 绿、`lint:prompts` OK；语料断言 `rg "^verify: \[\]\s*$" docs/plans docs/backlog missions` 零命中（收口复跑钉住）。
  - 注记：plan frontmatter `status` 维持 `active`——`completed` 为派生态禁写（本 plan Phase 1 执法的核心禁令），完成由全勾 + `## Verification` pass 行 + `## Closure` 回执派生（父 mission CLOSURE_AUDIT/BUILD_VERIFY 步为唯一写者）。

- **age-autonomy M2-WI44 Phase 2：`verify: []` 空真通道双层封堵**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 2）：
  - **校验器层**：`ledger-frontmatter.mjs` `validatePlanFrontmatter` 对 `verify: []` 报 error（字面含合法路径指路：非空 command-key 数组或省略字段回落 mission 默认）；`parseFrontmatter` 解析面不变（`verify: []` 仍合法格式子集，既有解析用例零修改——解析/校验两层分离）。`law-core.mjs` `plan-structure` 规则消费同一校验器（`law-core.mjs:232`），拒绝自动随写时 observe 面生效，law-rules 零改动（`verify-keys` 注记「空真归 WI44」维持成立）。
  - **派生层防御**：`ledger-sections.mjs` `deriveCompleted` 对显式空数组按 `no-verify-keys` 处理（`mechanicalVerification=false`）且 **fail-closed 优先于** `opts.defaultVerifyKeys`（显式空=拒绝语义，非缺失语义——与 0925-1 Phase 2 注入的组合行为钉住）；覆盖不经校验器的输入路径（外部写入/旧文件/旁路调用方）。
  - **Proof**：`ledger-frontmatter.test.js` 新增拒绝用例（含 omission 合法对照）；`ledger-derivation.test.js` 新 describe 四例（①双层面钉住 ②旁路防御 ③precedence fail-closed ④`verify: [test]` 绿路径无伤）；语料断言 `rg "^verify: \[\]\s*$" docs/plans docs/backlog missions` 零命中（收口复跑钉住）。
  - **验证**：`pnpm --prefix tools/mission-driver test` 899/0（+5；prompt-check OK）；plugin assets 重建（freshness 43 文件 content-equal）；`npm --prefix plugin/dsh test` 221/0（truth table + smoke-import 绿）。

- **age-autonomy M2-WI42 Phase 1：校验器生产读面接线**（plan `docs/plans/age-autonomy/2026-08-25-0925-2-m2-wi42-wi44-validator-wiring-verify-vacuity.md` Phase 1）：
  - **读 seam**：`ledger-dualread.mjs` `fmReadResult`（`readPlanStatus` frontmatter 分支）调 `validatePlanFrontmatter(split.fm)`，返回值增 `fieldErrors[]`/`fieldsValid`，随 `planLedgerState` 透传——plan-check/flow-loader/monitor 三引擎读面一次接线全覆盖（01 §5.2 单实现纪律）；非 frontmatter 格式不附键（legacy/guide 无字段集可校验）。
  - **后果矩阵落地**：① plan-check `analyzeFrontmatter` 把 fieldErrors 并入 `field:` 前缀 details → exit 1（不分 strict 档）；② flow-loader `_scanPlansByStatus` 对 field-invalid plan `console.warn` 一次/文件/扫描（`[flow-loader] plan field validation failed: <relpath> — <summary>`；队列成员资格仍按可解析 status——kill silence 非 kill queue）；③ monitor plans API 条目透传 `fieldErrors`（`handleGetRoadmap` 经 `parseRoadmapMarkdown` spread 同获覆盖）；roadmap-check `parseRoadmapMarkdown` `hasFm` 点跑 `validateRoadmapFrontmatter` + 新 CLI 面（exit 0/1/2，镜像 plan-check CLI 约定）。
  - **Proof**：新 `test/field-wiring.test.js`（deep-audit R1 三探针反向钉住——手写 `status: completed` exit 1、`verfy` typo exit 1、roadmap 缺/负 `audit-rounds` 非 0；flow-loader warn console-spy 断言 + 成员资格不变；legal corpus 5 文件 CLI exit 0）；corpus 断言 `fieldsValid === true` + roadmap `fieldErrors []`（零误杀）；monitor plans API fieldErrors 端到端。
  - **验证**：`pnpm --prefix tools/mission-driver test` 894/0（813 基线 + WI41 批 +13 → 881 后本 Phase +13；prompt-check OK）；plugin assets 重建（build-bundle 43 文件，4 模块 diff）；web typecheck/build 绿（types 增 `fieldErrors?`）；`lint:prompts` OK。
