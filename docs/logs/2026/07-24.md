# Development Log - 2026-07-24

### 2026-07-24

- mission-driver 实现 **clone-and-run**：提交预构建 `web/dist/` + CI 新鲜度校验（plan `docs/plans/2026-07-24-1030-mission-driver-web-onboarding-committed-dist-plan.md`）。
  - 背景：引擎已零 install（vendor commander，见下条）；前端是最后一块——提交 dist 后消费者 clone 即用、**零 install 零 build**。
  - 改动：根 `.gitignore` 加 negation `!tools/mission-driver/web/dist/`（根 `dist/` 全局忽略需例外）；`web/.gitignore` 移除 `dist`；提交 `web/dist/`（17 文件，C 瘦身后产物）；`web/package.json` 加 `check:dist`；新增 `.github/workflows/web-dist-check.yml`（path-scoped `web/**`，pin Node20/pnpm10，`--frozen-lockfile` + `pnpm build` + `git diff --exit-code -- dist`，业界标准 committed-artifact 守卫）。
  - 文档：`web/README.md` + `CONTEXT.md`（依赖行/关键约束）改“dist 已提交、clone 即跑”；重写 user-manual §1.3 为“零安装零构建 + 前端开发者 opt-in（改前端须重建并提交 dist，CI 拦截遗漏）”，§1.4 前提行同步。
  - 决策：新鲜度校验选 GitHub Actions rebuild+diff（用户授权“按业界流行做法”；仓库在 GitHub）；不引入 workspace（与 committed dist 冲突）；pre-commit hook 列为 Deferred（避免给非前端提交者装 husky）。ask-first 闸门由用户明确批准满足。
  - 验证：monitor `GET /` 返回完整 App HTML 引用构建 chunk（非占位）；`check:dist` 正反用例（一致 exit0 / 改源码 exit1，检出 entry hash 变化）；typecheck+build 全绿。

- mission-driver CLI 依赖 vendor 化：`commander@15` 内联进仓库，实现「clone 即跑，无需 `pnpm install`、无构建步骤」。
  - 动机：引擎核心本已零依赖，唯一 npm 依赖是 CLI 层 `commander`。为让协作者 clone 后直接 `node src/main.js`，调研业界实践（vendor / util.parseArgs / bundle / commit node_modules / Yarn PnP），选定 **vendor（供应商内联）**——commander 纯 ESM、零传递依赖、无原生二进制，是 vendor 的理想对象，行为 100% 不变、回归风险最低。
  - 改动：新增 `tools/mission-driver/vendor/commander/`（`index.js` + `lib/` + `LICENSE` + `package.json`，源自 15.0.0）与 `vendor/README.md`（记录来源版本 + revendor 升级步骤）；`src/main.js:5` import 改为 `../vendor/commander/index.js`；`package.json` 移除 `dependencies.commander`；删除 `pnpm-lock.yaml`。因 mission-driver 自身 `"type":"module"`，vendored `.js` 自动按 ESM 解析。
  - `util.parseArgs` 彻底去依赖（方案 B）留作后续独立技术债——CLI 用了 6 个子命令 + positional options + commander 15 quirk workaround，重写有回归风险，不与本次 zero-install 目标捆绑。
  - 验证（全绿）：临时移走 `node_modules` 模拟纯净 clone → `node src/main.js list/run/--help` 均正常解析；`npm test` **556 tests pass / 0 fail** + `prompt-check: OK`。`git status` 确认 `vendor/` 未被 `.gitignore` 忽略、可入库。

- mission-driver web 首屏减包：naive-ui 全量注册 → 按需导入（plan `docs/plans/2026-07-24-1030-mission-driver-web-bundle-slimming-plan.md`）。
  - 根因：`web/src/main.ts` `import naive from 'naive-ui'` + `app.use(naive)` 全量注册，把 ~90 个组件（含从未用的 Calendar/DatePicker/TimePicker/Transfer/Cascader + date-fns 本地化）打进单一入口 chunk（1476KB/gz 409KB）。
  - 改造：`vite.config.ts` 加 `unplugin-vue-components` + `NaiveUiResolver`（`dts:true` 生成 `web/components.d.ts`），删除 `main.ts` 全局 `app.use(naive)`。业务组件早已显式 `import { N* }`；App.vue 的 provider/layout（`<n-config-provider>/<n-message-provider>/<n-layout>/<n-grid>/<n-tabs>/<n-select>…`）改由 resolver 自动补齐。composable（useMessage 等）本就显式 import，未引入 unplugin-auto-import。
  - 效果：首屏 JS gzip **408.74 → ~198 KB（约 −51%）**（入口 117KB + 静态共享 81KB）；未用组件近乎清零（Calendar 87→2、Transfer 29→6 等，残余为零星字符串）。RunDetail-only 组件下沉到懒加载 RunDetail chunk。
  - 验证（全绿）：`npm run typecheck` + `npm run build` 通过；Playwright headless 加载 `vite preview` 首屏 → `appMounted=true`/`naiveRendered=true`/**0 errors**，RunList 完整渲染（header/表格/tag/暗色主题），证明删全局注册后 provider 未失效。
  - 决策记录：分发/onboarding 的 workspace 方案（原 A）经用户反馈与 committed dist 冲突，已重构为“默认引擎-only install + web 开发者 opt-in + 提交 dist”，见 `docs/plans/2026-07-24-1030-mission-driver-web-onboarding-committed-dist-plan.md`（draft, ask-first，待人工确认机制）。

- mission-driver monitor 前端移除 ECharts，资源监控由折线图改为「最近快照表格」。
  - 动机：`echarts` 是前端唯一重量级可视化依赖——node_modules 约 65MB（echarts 60MB + zrender 5MB），构建产物独立懒加载 chunk 约 539KB。ResourceChart 是其唯一消费点。
  - `web/src/components/chart/ResourceChart.vue`：删除 `<VChart>` + `echarts` 的 `option`/series computed；改用 `n-data-table` 展示最近 8 条 sysmon 快照（Time / Free Mem GB / Opencode RSS GB / Opencode / Node / Pressure，newest-first）。保留原有 Active Processes 表（本就用 naive-ui，不依赖 echarts）。
  - 删除 `web/src/components/chart/echarts-setup.ts`。
  - `web/src/stores/sysmon.ts`：移除仅为图表服务的 `freeSeries/rssSeries/ocCountSeries` 派生数据，`append`/`fetch` 简化为只维护 `snapshots`。
  - `web/package.json`：移除 `echarts`、`vue-echarts` 依赖（`pnpm install` 连带移除 zrender，共 -4 包）。
  - `web/vite.config.ts`：`manualChunks` 移除 echarts/zrender 规则，仅保留 xterm。
  - 同步注释/文档：`web/src/main.ts`、`web/src/router/index.ts`、`web/README.md`、`web/src/components/run/detail/DefaultRunDetail.vue`（卡片标题 Resource Chart→Resource Monitor）、`tools/mission-driver/CONTEXT.md`、`tools/mission-driver/docs/user-manual.zh.md`。
- 影响面：仅丢失资源随时间的折线趋势可视化；进程表、实时快照数据、SSE 心跳更新均保留。首屏本就不加载 echarts（已懒加载隔离），故首屏无变化；收益为 node_modules -65MB、构建去掉 539KB chunk、RunDetail chunk 40KB→31KB。
- 验证（全绿）：`npm run typecheck`（vue-tsc --noEmit）通过；`npm run build` 通过，产物中已无 `echarts-*.js` chunk。

- mission-driver 启用 GitHub Releases 分发（tag push → 自动建 release + auto changelog）。
  - 新增 `.github/workflows/release.yml`：`on: push tags v*`，softprops/action-gh-release@v2 + `generate_release_notes: true`；含 `-` 的 tag 自动判为 prerelease。下载物为 GitHub 自动附的 Source code (zip/tar.gz)（源码即跨平台 Node.js，无 per-OS 二进制）。
  - `tools/mission-driver/README.md` 加「Download & Run」段：指向 releases 页、前置条件（Node ≥18 + opencode CLI + Win 用 Git Bash）、零 install 零 build 说明、版本钉定方式。
  - 决策（对照 HBase/lazygit/fnm）：Releases+Assets 是"下载即跑"的标准机制；Packages 需 npm publish，与 clone-and-run 矛盾已排除；SEA/pkg 原生二进制对本工具不划算（产物巨大、仍需 opencode、方向矛盾）。

- 新增 `install-age.sh` —— AGE 脚手架一键安装脚本。
  - 交互/参数两种模式：输入目标项目路径 + 项目名，自动算 MISSION_DRIVER_HOME 相对路径。
  - 7 步：①拷 docs 脚手架（排除 articles/ + template-design-decisions.md）②拷 AGENTS.md（替换 `<project-name>`）③建 tools/mission-driver.sh shim ④建 .env/.env.example ⑤建 missions/base.json（commands 占位待填）⑥建 docs/logs/{year}/ ⑦补 .gitignore。
  - 验证：临时目录跑通——shim 经相对路径正确解析到引擎（`mission-driver.sh list` 列出 missions）、排除项生效、占位符已替换。

- `install-age.sh` 改为 manifest 驱动：只拷显式列出的脚手架文件，排除 template-internal 产物。
  - 问题：旧版用 `cp -rn` 按目录全量拷，混入了 mission-driver 自身的 plans/audits/roadmaps/requirements/logs（121 文件中 44 个无用）。
  - 修复：新增 `install-age.manifest`（77 行，每行一个脚手架文件路径，含注释解释排除项）；脚本逐行读取、`[ -f ]` 判断已存在则跳过、末尾打印 COPIED/SKIPPED 清单。
  - 验证（code-generator 实跑）：77 文件全拷（含 1 AGENTS.md + 76 docs），0 个 template-internal 泄漏（articles/mission-driver-*/template-design-decisions/dated-logs 全 absent）；shim list 跑通；重跑 = `copied 0, skipped 77`（跳过生效）。

- `install-age.sh` 新增 demo.json + demo-roadmap.md 生成；修复 monitor 端口冲突报告 bug。
  - demo.json：extends base，commands 全用 `echo demo-ok`（无论项目技术栈都能跑通），moduleDir="."，roadmapPath 指向生成的 demo-roadmap.md。
  - demo-roadmap.md：项目无关的极简 roadmap（3 个 work item），用于验证 Dashboard 渲染。
  - monitor 端口 bug：`server.listen(port, callback)` 每次重试都新增一个 'listening' listener，EADDRINUSE 后旧 callback 仍注册 → 重试成功时旧 callback 先 resolve，报告了旧端口（如 :9400 而非 :9401）。改为 `server.removeAllListeners("listening")` + `server.once("listening", ...)` + 无 callback 的 `server.listen(port)`。
  - 同时将重试上限从 5 提到 20（:9300→:9319），加 `[monitor] :N in use, trying :N+1...` 日志。
  - 验证（code-generator）：list 列出 base+demo；双 monitor 同端口 → mon1 :9400 / mon2 :9401，报告正确；npm test 556 pass/0 fail。

- `install-age.sh` 修复 3 个问题（用户反馈）。
  - ① 参数解析：`./install-age.sh /c/Work/code-generator`（仅路径）不再提示输入目录；项目名缺省时从目录名自动推导。
  - ② demo plansDir：`docs/plans` → `docs/plans/demo`（plans 不再散落在 docs/plans 根，而是集中在 demo 子目录）；安装时自动建该目录。
  - ③ DRAFT_PLANS 报错：demo-roadmap 的 WI3 从 ready 改为 done → 全部 done → DRAFT_PLANS 无剩余项 → 返回 nothing → mission 快速完成，不再让 AI 空转。
  - 验证（code-generator）：list 显示 base+demo；dry-run 全流程 completed；monitor :9300 HTTP 200。
