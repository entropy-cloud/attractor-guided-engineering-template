# Development Log - 2026-08-31

### 2026-08-31

- **Plan landed**: `docs/plans/multi-plugin-dsh/2026-08-31-1511-nop-route-cooldown-state-machine.md` — M5-WI2+WI3 (nop-route 熔断 + 分层路由 + wait-check).
- **Design doc rewritten**: `docs/design/dsh-routing-with-failover.md` — removed all code skeletons and implementation pseudocode; technical detail single-source-of-truth = code. Added §15 Changelog entry.
- **Design doc scoping clarified** (user feedback): routing state is account-level (`~/.nop/dsh/`, NOT `~/.age/`). Three layers: account-level (circuit), project-level (stats per `projectRoot × model`), mission-level (in-memory). Added §11.2–§11.7 + D9/D16–D19.
- **New modules** in `plugin/nop-route/src/`:
  - `circuit-breaker.ts` — three-state machine (closed/open/half-open) with exponential cooldown `min(base × 2^(n-1), max)`. Per-errorClass base/max: transient=60s/1800s, auth=1800s, budget=18000s. `permanent:invalid-input` / `partial:marker` do not enter circuit.
  - `tier-selector.ts` — layered selection with escalation policy. Last tier all-unavailable → wait-check (never gives up unless user STOP). Equality at threshold boundary → wait (not escalate).
  - `atomic-write.ts` — 12-line tmp + rename vendor from `plugin/nop-age/src/efficiency/context-profile.ts:91-99`. Zero external deps (not `@deepseek-ai/dsh-atomic-write`).
  - `state-persistence.ts` — `~/.nop/dsh/routing-state.json` circuit state round-trip with graceful fallback on missing/corrupt files.
  - `project-stats.ts` — per-project, per-model stats persisted to `~/.nop/dsh/routing-stats/<sha256(projectRoot).slice(0,16)>.json`. `__global__` bucket for missing projectRoot.
  - `home.ts` — `$NOP_HOME` env var → `~/.nop/dsh/` resolver.
- **error-classifier modification** — D12: `permanent:budget` rule match + retry-after hint → promoted to `transient:rate-limit`. Bare retry-after unchanged.
- **`noproute-routes.ts` extended** — new wire methods: `noproute.circuit-state`, `noproute.project-stats`, `noproute.pause`, `noproute.resume`. `noproute.route` returns `{ decision: "paused" }` while pause flag set.
- **`service.ts` integrated** — mount-load (try/catch graceful), debounce 60s flush, teardown flush. `circuitBreaker` / `missionCallStats` exposed on `NopRouteService` face. Added `nopHome` + `io` config overrides for testability.
- **Tests**: 169/169 pass, tsc --noEmit clean, engine + nop-age zero diff.
- **Independent closure audit** (subagent review): ACCEPTED-WITH-FOLLOW-UPS. Phase 3f (routing-core 整合 circuit-breaker + tier-selector) recorded as Deferred But Adjudicated.
- **Deferred** (see plan §Deferred But Adjudicated): Phase 3f routing-core integration, monitor dashboard UI components (Phase 4), persistent ledger JSONL, health-score sliding window upgrade, DSH plugin-API adaptation.
- Next: monitor dashboard UI panel is consumer-side work; can be activated independently when needed.