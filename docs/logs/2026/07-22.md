# Development Log - 2026-07-22

### 2026-07-22 — mdc-1: 审计收敛（P0/P1/P2 拟制闸门 + 干净短路 + 行号锚点护栏）(plan `docs/plans/mission-driver-convergence/2026-07-22-1708-1`)

- Source: `docs/requirements/mission-driver-convergence-and-cost-optimization.md`（R1/R2/R3）。根因来自 run `_tmp/2026-07-22-075526-mission-driver`：roadmap 全 `done` 后进入纯审计模式，3 轮 DEEP_AUDIT 每轮都发 `issues`+写 open 文件+起草计划，最终审计自评 "no blocking issue / 533 green" 仅剩 2 个 LOW/doc-only，却仍付全量验证；总耗时 5h38m。
- **Phase 1（R1）P0/P1/P2 拟制闸门**：
  - `prompts/multi-audit.md` / `prompts/open-audit.md`：报告头 `> Audit Status: open` 与结尾 marker `issues`/`clean` **保持原状**，新增每项发现须前缀 `[P0]`/`[P1]`/`[P2]` + 判定说明。
  - `prompts/draft-from-audit.md`：改为**只就 P0+P1 起草**计划（`Fix`，非降级）；`P2` 入 follow-up backlog；含 P0/P1 的审计 `open→planned`，只含 P2 的审计 `open→triaged`（终态、非 open，不被 `openAudits()` 计入）。
  - `src/flow-loader.js` `_scanOpenAuditsList` 保持只认 `open`（`triaged`/`planned` 天然不计入）——由新测试 `test/audit-convergence.test.js` 固定该行为。
- **Phase 2（R2）干净短路**：`src/engine.js` `_shouldCompleteOnAuditQuota` 判据由 `round >= maxAuditRounds` 改为 `auditRound >= 1`（DEEP_AUDIT 至少跑过一次）+ `activePlans()`/`openAudits()` 均空 → 提前 `completed`。`maxAuditRounds` 仍作为 `run()` 入口闸门的安全上限。等价恢复旧版第 1 轮干净退出，但保持引擎强制。更新 `draft-plans-audit-gate.test.js` Case B（原断言 `auditRound===2`，现 `===1` 早退）。
- **Phase 3（R3）行号锚点护栏**：`tools/check-doc-references.mjs` 新增 `file.ext:NNN` 行号引用检测（导出 `findLineRefs` + CLI guard，逃逸 `AGE_ALLOW_LINE_REFS=1`）；清理 `docs/architecture/mission-driver-baseline.md` 残余 `mission-check.mjs:13/14/24-50` 行号引用为函数名锚点；新测试 `test/doc-line-refs.test.js` 固定检测器 + 断言 `docs/architecture/` 零行号引用。
- **Owner doc**：`design/mission-driver-flow-design.md` 更新 changelog + audit-gate 语义（clean short-circuit）+ §6 deep-audit-loop 的 P0/P1/P2 拟制闸门 + `triaged` 说明。
- **收敛效果**：仅 P2 的运行在 ≤1 轮确认审计内收口、零修复计划（P2-only 审计自标 triaged → `openAudits()==0` → 短路）；含 P0/P1 正常起草直到清空或撞 `maxAuditRounds` 兜底。无法消除的最小成本 = 修完 P0/P1 后一轮免修复的确认审计。
- **验证（全绿，known-good baseline）**：`npm --prefix tools/mission-driver test` → **556 pass / 0 fail**（533 + 新增 23，含 audit-convergence 3 例 + doc-line-refs 3 例 + Case B 改写；duration ~11s），末行 `prompt-check: OK`。`node tools/check-doc-references.mjs` 的行号检测段零命中（架构 doc 已清）。
- **范围外（记录）**：`check-doc-references.mjs` 的 `[backtick]` 路径存在性告警（`mission-driver-baseline.md` 用 `src/x` 简写、`document-naming-and-timeliness.md` 的示例 bug 文件名）是**既存**缺陷、与本次收敛无关，未纳入本计划；R3 的验证以 `test/doc-line-refs.test.js`（已链入 npm test）为准，而非整仓 `check-doc-references` 硬门。
- **Closure**：solo cold-replay（用户直接指示实现，无第二 reviewer）；本计划为 tooling 改动、非 protected、有全套测试兜底，按 AGENTS.md Reviewer-Availability Fallback 记录 solo 限制。R4/R5/R6 列为后继项（见计划 Deferred But Adjudicated）。


### 2026-07-22 — B1 + B2 fix: Run-State section doc anchors (anchor-first convention extended from Public Exports) (plan `2026-07-22-1223-3`)

- Closes 0755 open-audit **B1** + multi-audit **B1** (same finding, surfaced independently) + 0755 open-audit **B2** + multi-audit **B2** (same finding, surfaced independently) — both [LOW — doc-only navigational rot]. Source: `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-open-audit-mission-driver-draft-robustness.md` (findings B1, B2) AND `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-multi-audit-mission-driver-draft-robustness.md` (findings B1, B2). The two audits converged on the same two findings at the same 07:55 timestamp — high-confidence evidence the defects are real, not a single audit's blind spot.
- **Phase 1 — anchor-first convention extended into the Run-State section** (`docs/architecture/mission-driver-baseline.md:61-81`): the prior `217af6d` doc-sync pass anchor-ized the Public-Exports section but skipped the Run-State section, so it re-rotted. Three concrete fixes, all extending the function-name-anchor-first strategy the Public-Exports section already adopted (the `export`/`function` keyword is findable and does not rot; numeric line spans do):
  - **B1 (`:63`)** — stale `_writeWorkflow` line citation: was `(_writeWorkflow, engine.js:427-436)` (lines `:427-440` are actually `_finalizeWorkflow`, NOT `_writeWorkflow`; `_writeWorkflow` is at `:442-451`). Reworded to `(_writeWorkflow in src/engine.js; the function name is the durable anchor — line numbers rot as the engine grows)` — function-name primary, stale numeric span dropped. The closure log `07-22.md:18` had recorded that the `217af6d` pass *re-verified* `:427-436` as "accurate" — that re-verification was wrong (the strengthening the auditor flagged); the durable fix is the anchor-first form that cannot re-rot when the engine grows.
  - **B1 neighbour (`:65`)** — `_wfOpen`/`_wfClose` line spans: was `(opened by _wfOpen engine.js:331-368, closed by _wfClose engine.js:370-411)`. The `_wfClose` span undershot (body runs to `:426`, not `:411`). Reworded to `(opened by _wfOpen in src/engine.js, closed by _wfClose in src/engine.js)` — both spans dropped, function names kept as anchors.
  - **B2 (`:79`)** — `subflowRuns` sort mis-attributed to `_wfClose`: was `sorted by forEachIndex on close (inside _wfClose in engine.js)`. Actual: the sort is at `engine.js:1134` (`subflowRuns.sort((a, b) => a.forEachIndex - b.forEachIndex)`) inside **`_executeSubflowStep`** (the forEach drain, after `:1129-1131`), with the owning code comment at `:1132-1133`; `_wfClose` does NOT sort — it only persists the already-sorted record. Reworded to `sorted by forEachIndex before close (inside _executeSubflowStep in src/engine.js; monitor.js's mergeSubflowChildren re-sorts defensively on the consumer side)`. The **invariant holds** (pinned by `subflow-incremental.test.js` Case D — resolve order `[1,2,0]` yields `forEachIndex [0,1,2]`); the defect was purely the wrong enforcement-site attribution.
- **Sweep result (no additional drift found)**: while in the Run-State section, re-verified every other function-name-and-line citation in `:61-81` against live `engine.js` / `monitor.js`:
  - `_subflowId` convention at `:79` — doc says "set on child flow vars inside `_executeSubflowStep` in `engine.js`" (no line span). Live: `_subflowId` set at `engine.js:1062` / `:1089` / `:1146` inside `_executeSubflowStep`. **Anchor-first, accurate — no change.**
  - `_wfAppendSubflowRun` at `:81` — doc says "(in `engine.js`)" (no line span). Live: defined at `engine.js:491`. **Anchor-first, accurate — no change.**
  - `mergeSubflowChildren` at `:81` — doc says "(in `monitor.js`)" (no line span). Live: defensive re-sort at `monitor.js:350` inside `mergeSubflowChildren`. **Anchor-first, accurate — no change.**
  - `_executeSubflowStep` at `:79`/`:81` — doc names the function (no line span). **Anchor-first, accurate — no change.**
  - Net sweep: the only rotted citations were B1 (`:63`) + B1 neighbour (`:65`) + B2 (`:79`); the rest of the Run-State section had already adopted the anchor-first form (the `_subflowId` / `_wfAppendSubflowRun` / `mergeSubflowChildren` citations were written anchor-first when the section was last touched). The sweep confirms B1+B2 were not cherry-picked — every citation in `:61-81` is now anchor-first.
- **Cold-replay closure proof (Phase 2 item 1)**: fresh re-read of `mission-driver-baseline.md:61-81` (the edited section), then fresh re-read of live `engine.js` (`:331-368` `_wfOpen`, `:370-426` `_wfClose`, `:442-451` `_writeWorkflow`, `:491` `_wfAppendSubflowRun`, `:1062`/`:1089`/`:1146` `_subflowId`, `:1129-1134` `_executeSubflowStep` sort) + `monitor.js` (`:349-350` `mergeSubflowChildren` defensive re-sort), then compare. Confirmed: (a) B1 citation corrected — no `engine.js:427-436` remains at `:63`; (b) B2 attribution corrected — `:79` names `_executeSubflowStep` (not `_wfClose`); (c) sweep found zero additional drift. This cold-replay IS the closure proof for a doc-only plan — no test suite can assert doc-vs-code alignment, so the basis is recorded here explicitly for future debugging.
- **Narrative-flow check**: re-read the edited section end-to-end. The anchor-first form preserves the surrounding sentences — the `_writeWorkflow` / `_wfOpen` / `_wfClose` / `_executeSubflowStep` function names flow naturally in prose; no broken grammar, no dangling line-span fragments.
- Verification (full green, doc-only so suite unchanged): `pnpm --prefix tools/mission-driver test` → **551 pass / 0 fail** (matches the `9bd3cbf` O7 baseline; doc-only change introduced no regressions; duration 17.65s), chained final line `prompt-check: OK — all prompt result-tag examples are well-formed.` This plan is doc-only — no `pnpm test` gate applies per its own Closure Gate (the closure proof is the cold-replay above); the test run is a known-good-baseline re-confirm checkpoint, not a closure requirement.
- Scope proof: 触及文件 = `docs/architecture/mission-driver-baseline.md` (Phase 1 — three citation rewordings at `:63`/`:65`/`:79`) + `docs/logs/2026/07-22.md` (this entry) + 本 plan 文件 (items ticked + Status: completed + Closure section filled). No `src/` / `web/` / `test/` / `prompts/` / `missions/` / `flows/` change (Non-Goal honored — the invariants hold in code; this plan only makes the doc name them correctly).
- Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback — source audits rated B1 + B2 LOW doc-only nits; no code / contract / API / auth / data / integration / deployment change whatsoever; this is the lowest-risk plan class under the Plan Decision Table). Independent draft review completed earlier (1 iteration, recorded in plan Draft Review Record — reviewer independently verified every citation). 无 `> Work Item:` 标签（audit-sourced plan — post-WI audit cleanup of doc-anchor rot, not a roadmap work item），roadmap work-item 状态不需更新；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line" — audit lifecycle manages its own status via mission-driver closure step). Same convention as sibling plans 1223-2 / 1223-1 / 1106-2 / 0814-2 / 0814-3.

### 2026-07-22 — O7 fix: main.js exitMap covers flow-error statuses + ping_pong Decision + exit-map.test.js regression suite (plan `2026-07-22-1223-2`)

- Closes 0755 open-audit **O7** ([MEDIUM-LOW — NET-NEW] `main.js:749` `exitMap` omitted `unknown_step` / `unknown_type` / `no_transition` / `invalid_transition` (documented exit 1 per `EXECUTION-PRINCIPLE.md §11`) and `ping_pong` (documented `—` ambiguous) → all five silently fell through to `undefined` → Node's default exit 0 (success); zero test coverage on the exit-code mapping). Source: `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-open-audit-mission-driver-draft-robustness.md` (finding O7).
- **Phase 1 — O7 code fix + ping_pong Decision** (`tools/mission-driver/src/main.js`): replaced the inline literal at `cmdRunMission` (`:749`) with a top-level named `export const EXIT_MAP` (`:45-51`) consumed by `cmdRunMission` (`:767`) — additive refactor that makes the map directly importable by the regression test. The map now covers every documented terminal status: `completed`/`single_step_done` → 0, `failed` → 1, the four flow-definition errors (`unknown_step`/`unknown_type`/`no_transition`/`invalid_transition`) → 1, the three loop-guard caps (`max_cycles`/`max_total_steps`/`max_retries`) → 2, and `ping_pong` → 2. A 10-line comment block (`:35-44`) cites `EXECUTION-PRINCIPLE.md §11` as the controlling contract and the audit's `Deferred But Adjudicated` carve-out (`skipped` + dynamic `done` values intentionally unmapped → exit 0 by Node default). **`ping_pong` Decision (adjudicated)**: chose **alternative (1) `ping_pong: 2`** over alternatives (2) `1` (loses loop-guard signal) and (3) `0` (contradicts "death-loop is not success"). Rationale: a death-loop IS a loop-guard trip, so the 0/1/2 contract stays internally consistent — scripted callers can distinguish "definition error" (1) from "loop guard" (2). The doc's `—` was ambiguous; the chosen `2` carries the same "死循环保护" meaning as the other `2` codes. Residual risk: scripted callers relying on today's de-facto exit 0 will now see exit 2 — but the doc's `—` was ambiguous, so any caller relying on undocumented behavior was already broken-by-design.
- **Phase 2 — test-placement Decision + regression suite** (`tools/mission-driver/test/exit-map.test.js`, new): **Decision**: chose **alternative (1) new focused `exit-map.test.js`** over alternative (2) extend `core.test.js` — a focused test makes the contract drift the audit flagged structurally impossible to reintroduce, and importing the exported `EXIT_MAP` directly avoids spawning a full `cmdRunMission` process. Two assertion blocks: (a) **documented-status mapping** — 11 cases asserting `EXIT_MAP[status]` equals the documented exit code for every status in `EXECUTION-PRINCIPLE.md §11` (`completed`/`failed`/`max_cycles`/`max_total_steps`/`max_retries`/`ping_pong`/`unknown_step`/`unknown_type`/`no_transition`/`invalid_transition`) + `single_step_done` (documented in `single-step.test.js:6` comment); (b) **no-documented-status-maps-to-undefined sweep** — 2 cases asserting (i) every documented status has an explicit `EXIT_MAP` entry (scoped explicitly to the 11-status documented set, with `skipped` + dynamic-`done` carve-out cross-referenced to the plan's `Deferred But Adjudicated`) and (ii) the `DOCUMENTED` table guards against accidental drift (exactly 11 statuses). Single-source-of-truth `DOCUMENTED` table consumed by both blocks so a §11 row change is a one-place update.
- **Regression-detection proof (mandatory, live verification)**: simulated the pre-O7 broken map `{ completed: 0, single_step_done: 0, failed: 1, max_cycles: 2, max_total_steps: 2, max_retries: 2 }` and ran the sweep logic against it → 5 documented statuses would be unmapped (`unknown_step`/`unknown_type`/`no_transition`/`invalid_transition`/`ping_pong` → silently exit 0). The live `EXIT_MAP` returns `ping_pong: 2` and `unknown_step: 1` as required. Without this step the test would be theater.
- **Phase 3 — doc sync** (`EXECUTION-PRINCIPLE.md §11` + `mission-config-schema.md` + `docs/architecture/mission-driver-baseline.md`): `EXECUTION-PRINCIPLE.md §11` table's `ping_pong` row changed from `| — |` to `| 2 |` (closing the `—` ambiguity), and the `unknown_step 等 | 1` row's `等` "etc." expanded to name all four statuses explicitly (`unknown_step` / `unknown_type` / `no_transition` / `invalid_transition`) so the doc and the code agree row-by-row without an ambiguous "etc.". `.opencode/skills/mission-driver/references/mission-config-schema.md:269` synced — added `no_transition` to the flow-definition-error group (the skill reference doc already had the correct `ping_pong → 2` but was missing `no_transition`, so it now lists all four statuses matching §11). `docs/architecture/mission-driver-baseline.md` Public Exports section's `src/main.js` row extended to name `EXIT_MAP` (named `export const` near the top, the `§11` contract table, consumed by `cmdRunMission` + pinned by `test/exit-map.test.js`). Other grep hits re-verified accurate and intentionally untouched: `CONTEXT.md:87` (passing `single_step_done → 0` reference, still correct), `step-execution-and-audit-count-design.md:385` (backward-compat note, still correct), `mission-driver-step-audit-roadmap.md:135` (passing mention, still correct), `draft-robustness-design.md` `process.exitCode = 1` (WI1 validation reject — different code path, already correct). Audit evidence quotes in `2026-07-22-0755-open-audit-*.md` (lines 70-72, 80-81) reproduce the OLD broken map — intentionally left as historical audit evidence (must not be edited).
- Verification (full green, recorded as known-good baseline): `pnpm --prefix tools/mission-driver test` → **551 pass / 0 fail** (538 baseline + 13 new cases: 11 documented-status mapping + 2 sweep guard; duration 13.10s), chained final line `prompt-check: OK — all prompt result-tag examples are well-formed.`; `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (14.30s, pre-existing chunk-size warning only — no new warnings); `pnpm --prefix tools/mission-driver run lint:prompts` OK.
- Scope proof: 触及文件 = `tools/mission-driver/src/main.js` (Phase 1 — top-level `EXIT_MAP` export + `cmdRunMission` consumer + comment block) + `tools/mission-driver/test/exit-map.test.js` (Phase 2 — new, 13 cases) + `tools/mission-driver/EXECUTION-PRINCIPLE.md` (Phase 3 — §11 `ping_pong` row + `等` expansion) + `.opencode/skills/mission-driver/references/mission-config-schema.md` (Phase 3 — `no_transition` added) + `docs/architecture/mission-driver-baseline.md` (Phase 3 — Public Exports `EXIT_MAP` entry) + `docs/logs/2026/07-22.md` (this entry) + 本 plan 文件 (items ticked + Status: completed + Closure section filled).
- Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback — source audit rated O7 MEDIUM-LOW latent code-vs-doc contract drift; the exit-code change closes the drift in the direction the doc already promised, so callers scripting against the doc's exit-1 promise GAIN correctness; no contract/API/auth/data change beyond closing the doc-vs-code drift). Independent draft review completed earlier (2 iterations, recorded in plan Draft Review Record). 无 `> Work Item:` 标签（audit-sourced plan — WI1-WI5 已 done，本 plan 是 post-WI audit cleanup），roadmap work-item 状态不需更新；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line" — audit lifecycle manages its own status via mission-driver closure step). Same convention as plans 1223-1 / 1106-2 / 0814-2 / 0814-3.

### 2026-07-22 — verify-then-commit docs batch: 0755 audit refresh + plans 1223-2/1223-3 drafted + training-deck golden-rules redo (full-green baseline re-confirm)

- Follow-up docs commit after `1ec8d63` (plan `2026-07-22-1223-1` O6 code fix) landed. This batch commits the EXECUTE-step output that was left uncommitted: 0755 audit refresh, plan 1223-1 commit-hash backfill, two new sibling plans (1223-2 / 1223-3), and the training-deck golden-rules redo. No code change in this batch.
- **Full-green verification re-confirmed** (this VERIFY step, recorded as known-good baseline): `pnpm --prefix tools/mission-driver test` → **538 pass / 0 fail** (matches the `1ec8d63` baseline); `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (17.09s, pre-existing chunk-size warning only — no new warnings); `pnpm --prefix tools/mission-driver run lint:prompts` OK. No code change in this batch — the green state is the post-`1ec8d63` baseline, re-verified as a checkpoint before committing the docs.
- **0755 audit refresh**: `2026-07-22-0755-multi-audit-*.md` and `2026-07-22-0755-open-audit-*.md` rewritten to reflect post-`217af6d` reality — prior `planned`-status drafts targeted the older `c2a1ea9` tree (their O4/O5/A1 were all about the then-uncommitted `mergeSubflowChildren` batch, since committed green in `caf741e`/`919f3aa`/`217af6d`). Refreshed audits re-verify prior findings closed and surface net-new findings: open-audit O6 (closed by `1ec8d63`), open-audit O7 (exit-code map contract drift in `main.js:749`), multi-audit B1+B2 (Run-State section doc-anchor rot in `mission-driver-baseline.md`). Both audits now route to plans 1223-1/2/3.
- **Plan `2026-07-22-1223-1` closure backfill**: `Closure Audit Evidence` block now cites commit hash `1ec8d63` (was placeholder "see `git log --oneline -1`").
- **New plans drafted** (`Plan Status: active`, `Audit: required`): `2026-07-22-1223-2-exitcode-map-flow-error-statuses.md` (O7 — `main.js` `exitMap` missing `ping_pong`/`unknown_step`/`no_transition`/etc. → documented exit 1 statuses silently exit 0; doc-vs-code contract drift + zero test coverage on the exit-code mapping), `2026-07-22-1223-3-run-state-section-doc-anchors.md` (B1+B2 — apply the anchor-first convention `217af6d` adopted for the Public-Exports section to the Run-State section: `_writeWorkflow` line-citation rot at `:63` + `subflowRuns` sort mis-attributed to `_wfClose` at `:79`).
- **Training-deck golden-rules redo** (`tools/mission-driver/docs/training-deck.zh.md`): 585 lines / 32 slides → 219 lines / 19 slides (−62%) per Slidev 980×551px canvas constraints (title ≤6 CN / ≤4 EN words; ≤4 bullets per slide; ≤15 chars per bullet; ≤3×4 tables; ≤40-char ASCII diagrams; ≤20 slides total). Companion `docs/logs/2026/07-21.md` entry records the methodology (canvas pixels → font size → content density).

### 2026-07-22 — O6 fix: plan-check.mjs Windows CLI guard + plan-check-cli.test.js regression suite (plan `2026-07-22-1223-1`)

- Closes 0755 open-audit **O6** ([MEDIUM-LOW] `plan-check.mjs:142` carried the exact Windows false-positive CLI guard that WI4 fixed for `mission-check.mjs` — the sibling tool was missed). Source: `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-open-audit-mission-driver-draft-robustness.md`.
- **Phase 1 — O6 code fix** (`tools/mission-driver/src/plan-check.mjs`): added `import { pathToFileURL } from "node:url";` (`:28`) and replaced the broken guard `if (import.meta.url === \`file://${process.argv[1]}\`)` (`:142`) with the WI4-correct form `if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href)` (`:147`), mirroring `mission-check.mjs:107` verbatim (including the `process.argv[1] &&` short-circuit for REPL / `node -e` / `node -` hosts). Added a 4-line comment block (`:143-146`) citing WI4 / design §2.5 "缺陷 4" (Rule 12 rare-comment exception — the broken form looks innocent and was already reverted-to once across the sibling tools). Live repro flipped: `node plan-check.mjs /nonexistent/missing-plan.md` → was **silent exit 0**, now **exit 1 + ENOENT**; valid-plan anchor still exits 0 with JSON stdout.
- **Phase 2 — Rule 15 helper-extraction Decision** (`Decision`, adjudicated): chose **alternative (2) "Defer with trigger"** over alternative (1) "Extract now". Rationale: after Phase 1 the *defect class* (broken template-string) is extinct (0 surviving) — the only residual is cosmetic *idiom drift* (2 tools use `pathToFileURL(...).href`; `prompt-check.mjs` uses the distinct-but-correct `resolve() === fileURLToPath()` form), which is not a defect; consolidating `prompt-check.mjs` is scope creep beyond O6's closure surface; Phase 3 Case E (source-inspection anchor) structurally prevents the broken form from returning on any platform, mitigating the residual risk the helper would otherwise address. Trigger condition recorded in plan `Deferred But Adjudicated`: extract `isMainModule` into `src/main-module.mjs` when a fourth check tool lands OR one of the three idiom sites regresses. Consistent with the codebase's borderline-Rule-15 pattern (`1106-1` mergeSubflowChildren, `1605-3` N3 — both deferred-with-trigger at recurrence count 2).
- **Phase 3 — regression suite** (`tools/mission-driver/test/plan-check-cli.test.js`, new): mirrors `mission-check-cli.test.js` (WI4) with 5 cases — A: nonexistent plan path → exit 1 + ENOENT/stderr (Windows-only regression catch); B: missing positional arg → exit 2 + "Usage:"; C: valid completed plan → exit 0 + `"passed": true`; D: failing plan (unchecked items) → exit 1 + `"passed": false`; E: **cross-platform regression anchor** — source-inspection asserting `import.meta.url === pathToFileURL(` is present and the broken `` `file://${` `` template form is absent (fires identically on Windows / macOS / Linux because it inspects source text, not the runtime comparison; this is the case that catches a guard revert on POSIX CI where Case A's Windows-only catch hides).
- **Regression-detection proof (mandatory, deliberately-revert verification)**: reverted `plan-check.mjs:147` back to the broken template-string form, re-ran `plan-check-cli.test.js` → **all 5 cases fail on Windows** (A/B/C/D: guard never fires so CLI body never executes → exit 0 with no output; E: source-inspection regex catches the broken form). On POSIX CI: Cases A/B/C/D would pass (guard happens to match for absolute script paths) but **Case E still fails** — this is the cross-platform anchor the plan's Draft Review iteration 1 demanded. Restored the fix → all 5 cases green. Platform-specific failure modes recorded here per Phase 3 Exit Criteria — without this step the test would be theater.
- Verification (full green): `pnpm --prefix tools/mission-driver test` → **538 pass / 0 fail** (533 baseline + 5 new cases; duration 16.98s), chained final line `prompt-check: OK — all prompt result-tag examples are well-formed.`; `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (25.95s, pre-existing chunk-size warning only — no new warnings); `pnpm --prefix tools/mission-driver run lint:prompts` OK.
- Scope proof: 触及文件 = `tools/mission-driver/src/plan-check.mjs` (Phase 1 guard fix + import + comment) + `tools/mission-driver/test/plan-check-cli.test.js` (Phase 3 new, 5 cases) + `docs/logs/2026/07-22.md` (this entry) + 本 plan 文件 (items ticked + Status: completed + Closure section filled). No owner-doc edit needed (`design/draft-robustness-design.md` §2.5 already documents this defect class; `CONTEXT.md` "构建与验证" already lists the standalone CLIs — both confirmed accurate during plan authoring).
- Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback — source audit rated O6 MEDIUM-LOW latent diagnostic-tool defect; no contract/API/auth/data change beyond a standalone diagnostic CLI the audit explicitly notes is "not in the npm test chain" and "the production loop is unaffected"). 无 `> Work Item:` 标签（audit-sourced plan — WI4 already `done` in roadmap; this plan is post-WI audit cleanup of the sibling tool WI4 missed），roadmap work-item 状态不需更新；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line" — audit lifecycle manages its own status via mission-driver closure step).

### 2026-07-22 — subflow 步骤的 session/log 按钮在执行期间不出现（onStepUpdate 路由错 + _wfClose 覆盖）

- 现象：用户在 monitor 详情页看 `_tmp/2026-07-22-075526-mission-driver`，正在运行的 BUILD_VERIFY 步骤没有 session 按钮（要等步骤跑完才出现）。
- 根因 ①（`tools/mission-driver/src/engine.js:1162` `_runChildSubflow`）：runner 的 `onStepUpdate` 回调（`onSpawn` 时发 logFile/sessionId）绑在 **父** 引擎的 `config.onStepUpdate`（main.js:752 `config.onStepUpdate = (payload) => engine._onAgentStepUpdate(payload)`，这里的 `engine` 是父引擎）。子引擎跑 agent 步骤时，runner 读 `config.onStepUpdate` → 调父引擎的 `_onAgentStepUpdate` → 在父的 `workflow.steps` 里按 stepName 找——子流程的 stepName（EXECUTE / BUILD_VERIFY / MULTI_AUDIT）根本不在父的 workflow 里 → 静默丢失。实测：`run-state-EXEC_PLANS-3-1.json` 的 EXECUTE 步骤 `sessionId=(none) logFile=(none)`。
- 根因 ②（`tools/mission-driver/src/engine.js:370` `_wfClose`）：即使 ① 修了让 `_onAgentStepUpdate` 路由到子引擎、执行期间写入了 sessionId，`_wfClose` 构建全新 record 替换 running placeholder 时用的是 `sessionId: sessionId || null`（来自 `result.sessionId`）。如果 runner 的 `extractSessionId(text)` 没从输出文本提取到 session（但 `onSpawn` 轮询早已找到），`_wfClose` 会用 null **覆盖** `_onAgentStepUpdate` 写的值。
- 修复 ①（`engine.js _runChildSubflow`）：子 delegates 的 `runAgent` 包装一层——`parentRunAgent.bind(parentDelegates)` 保留 `this`（测试 mock 用 `this.callLog`），调用时注入 `opts.onStepUpdate = (payload) => childEngine._onAgentStepUpdate(payload)`。`runner.js:127` 改为优先读 `opts.onStepUpdate`，回退 `config.onStepUpdate`。race-free：每个 child 的 `runAgent` 调用独立注入 opts，不修改共享的 `config`。
- 修复 ②（`engine.js _wfClose`）：先找到 running placeholder entry，捕获其 `sessionId/logFile/promptFile`（`_onAgentStepUpdate` 执行期间写的 live 值），构建 record 时 `sessionId: sessionId || live.sessionId || null`（close 参数优先，回退 live 值，最后 null）。logFile/promptFile 同理从 live entry 保留。生产场景下 `result.sessionId` 和 live 值相同（都是同一个 opencode session），所以这个回退不影响正常路径——只修 `extractSessionId` 失败但 `onSpawn` 已成功的边缘情况。
- 测试覆盖（`tools/mission-driver/test/subflow-state-isolation.test.js` 新增 1 describe / 1 test）：mock `runAgent` 在返回前调 `opts.onStepUpdate({ sessionId: "ses_live_WORK" })` 且返回 `sessionId: null`——断言子 state 文件里 WORK 步骤的 `sessionId === "ses_live_WORK"`。修前 ①：onStepUpdate 路由到父引擎，子步骤 sessionId=null。修前 ①+② 未修：onStepUpdate 到子引擎但 _wfClose 用 null 覆盖。修后 ①+②：live 值保留。
- Verification (full green): `pnpm --prefix tools/mission-driver test` → **533 pass / 0 fail**（+1 vs 上次 baseline 532）。
- 部署：用户需**重启 engine**（PID 31980 或后续 run 的 PID）才能拾取修复——`engine.js` + `runner.js` 是启动时加载进内存的。monitor 不用重启（这次没改 monitor.js，前端的 session 按钮渲染逻辑 `v-if="step.sessionId"` 本身没问题，只是之前 sessionId 永远到不了子步骤）。

### 2026-07-22 — architecture baseline doc sync: A1 facets a + b + c closed (plan `2026-07-22-1106-2`)

- Doc-only refresh of `docs/architecture/mission-driver-baseline.md` to honor its own Update Rule (`:125`) after the N1/N3/H2/O4 code edits. Closes finding A1 (all 3 facets) carried forward across both 0755 audits. Depends on sibling plan 1's commit `caf741e` (`mergeSubflowChildren` verify-then-commit) having landed — facet (c) describes that shipped behavior, not in-flight refactor.
- **Facet (a) — Public Exports list** (`:97-101`): added `validateDraftDesc` to the `src/main.js` row (it was missing — defined `draft-job.mjs:61`, re-exported `main.js:922`, imported+called by `monitor.js` as the N2 gate); added a new `src/draft-job.mjs` row (`startDraftJob`/`readDraftJob`/`listDraftJobs`/`validateDraftDesc`, all `export function`, consumed cross-module by `monitor.js`); corrected `parseRoadmapMarkdown`'s owner to `src/roadmap-check.mjs` (defined `:41`, only re-exported by `monitor.js`); added `mergeSubflowChildren`/`handleStartDraft`/`startMonitor` to the `src/monitor.js` row.
- **Facet (b) — stale line-number citations → function-name-anchor primary** (same strategy as the F5 design-doc fix): converted the rotted citations at `:30` (CLI registration, was `853-922`/`926-954` which overran EOF), `:79` (`_subflowId` was `1032,1059,1116` → actual `1047,1074,1131`; sort-on-close was `1103-1104` → `1119`), `:81` (`_wfAppendSubflowRun` was `461-473` → `476`), `:87` (`extractBriefGate` was `184-189` → `169-176`) to function-name-anchored citations (line numbers dropped — the `export`/`function` keyword is findable and does not rot). Leave-alone citations re-verified accurate: `mission-check.mjs:13/14/24-50`, `_writeWorkflow engine.js:427-436`, `_wfOpen engine.js:331-368`, `_wfClose engine.js:370-411`.
- **Facet (c) — `mergeSubflowChildren` behavioral rewrite** (`:81`): replaced "retained as defense-in-depth fallback for orphan children" with "the **primary live-state reader** for subflow children: the on-disk child run-state file is the source of truth for `status`/`steps`/`currentStep`, while `subflowRuns` seeds the `forEachItem` metadata the disk file does not carry" — matching the now-shipped `caf741e` behavior. The detailed seed-vs-disk-merge rationale is cited (lives in the `mergeSubflowChildren` comment block at `monitor.js:249+`), not duplicated.
- Proof: re-read the refreshed doc top-to-bottom; every Public-Exports entry names a real `export` statement in the cited file; every retained line number is within file bounds; no remaining "fallback"/"defense-in-depth" wording mischaracterizes the now-primary `mergeSubflowChildren`.
- Verification (full green, doc-only so suite unchanged): `pnpm --prefix tools/mission-driver test` → **532 pass / 0 fail**, chained `prompt-check: OK — all prompt result-tag examples are well-formed.`
- Closure: plan `2026-07-22-1106-2` → `Plan Status: completed`. No `> Work Item:` label (audit-sourced post-WI doc cleanup, not a roadmap work item) → roadmap work-item flip N/A; front matter uses `> Source:` not `> Source Audits:` → source-audit close omitted per the literal step-4c rule (audit lifecycle manages its own status). Same convention as mdr-remediate-7 closure.

### 2026-07-22 — verify-then-commit docs batch: 0755 audit refresh + plan 1106-2 (full-green baseline re-confirm)

- Follow-up commit batch after `caf741e` (plan `2026-07-22-1106-1`) landed the `mergeSubflowChildren` + `StepTimeline.vue` code. That commit's scope proof explicitly left the two 0755 audit files + sibling plan 1106-2 out of scope ("sibling plan 2 owns the architecture-doc `:81` rewrite after this commit lands"); this batch commits them.
- **Full-green verification re-confirmed** (this VERIFY step, recorded as known-good baseline): `pnpm --prefix tools/mission-driver test` → **532 pass / 0 fail** (matches the 1106-1 baseline below); `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (15.79s, pre-existing chunk-size warning only — no new warnings); `pnpm --prefix tools/mission-driver run lint:prompts` OK. No code change in this batch — the green state is the post-1106-1 baseline, re-verified as a checkpoint before committing the docs.
- **0755 audit refresh**: `2026-07-22-0755-multi-audit-*.md` and `2026-07-22-0755-open-audit-*.md` rewritten to reflect post-1106-1 reality — O4 (the in-flux `mergeSubflowChildren` batch) + O5 (`visits`-absent prefix fragility) are now **closed** (committed green in `caf741e`); A1 (`mission-driver-baseline.md` contract drift) carried forward with a **net-new facet (c)**: the O4 refactor changed `mergeSubflowChildren` from a gated fallback into the primary live-state reader, so the doc's "defense-in-depth fallback" description is now stale. All 23 prior findings across both audit generations remain genuinely closed.
- **New plan `2026-07-22-1106-2`** (architecture baseline doc sync, `Plan Status: active`, `Audit: required`): doc-only plan to restore `docs/architecture/mission-driver-baseline.md` against its own Update Rule — complete the Public-Exports list (add `validateDraftDesc` + a `draft-job.mjs` row + correct `parseRoadmapMarkdown`'s owner to `roadmap-check.mjs`), anchor-ize the rotted `main.js`/`engine.js`/`monitor.js` line citations (function-name-anchor-primary strategy, same as the F5 fix), and rewrite the `:81` description to "primary live-state reader". Closes A1 facets a + b + c.
- **Separate deliverable committed alongside** as its own `docs(mission-driver)` commit (general tool doc, not draft-robustness-scoped — mirrors the earlier `d187d5a` user-manual commit): `tools/mission-driver/docs/training-deck.zh.md` (32-slide Obsidian/Advanced-Slides deck distilled from `user-manual.zh.md`) + its `docs/logs/2026/07-21.md` entry.

### 2026-07-22 — O4 verify-then-commit + O5 `mergeSubflowChildren` visits-absent hardening (plan `2026-07-22-1106-1`)

- Closes 0755 open-audit O4 (process + latent defect) + O5 (defensive hardening) as one verify-then-committed unit. Source: `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-open-audit-mission-driver-draft-robustness.md`.
- **O4 process lesson** (the headline): the uncommitted `mergeSubflowChildren` + `StepTimeline.vue` batch (today's earlier "monitor 看不到 DEEP_AUDIT" entry) was momentarily broken mid-audit — the refactor's narrow new test ("fills live state from disk when subflowRuns placeholder has file=null", `test/subflow-state-isolation.test.js:267`) **passed** while the pre-existing broad regression test `:99` ("merges child state files into step.children tree") **failed** (`0 !== 2`). Root cause: the `:99` synthetic `SUB` step omits `visits`, the refactor had removed the seed-loop direct `r.file` read, and the disk-scan prefix `${step.name}-${step.visits}-` became `"SUB-undefined-"` → no match → empty steps. Stash-confirmed causality (clean HEAD 7/7, intermediate working tree 6/7). Recovered to 531 green only via a concurrent editor restoring `r.file ? readSubflowState(runDir, r.file) : null` in the seed loop. **Lesson pinned here for future refactors of this method**: verify against the **full** `pnpm --prefix tools/mission-driver test` suite, not the refactor's own new test — the new test exercises only the path the refactor's author was thinking about; broad regressions cover the contracts the refactor forgot. This is the third time `mergeSubflowChildren` has been refactored (status-gate removal → fallback → merge), and each refactor re-tripped the `:99` contract.
- **O5 hardening** (`src/monitor.js:295-307`): the disk-scan prefix construction `const prefix = \`${step.name}-${step.visits}-\`` silently no-op'd when `step.visits` was absent (`undefined` → `"SUB-undefined-"` → `matching: []`). Production always writes `visits` (`engine.js` `_wfOpen` increments on entry), so this is latent — but it is exactly the coupling each refactor trips over. Fix: fall back to a broader stepName-only prefix when `step.visits == null` — `const prefix = (step.visits == null) ? \`${step.name}-\` : \`${step.name}-${step.visits}-\``. The idx (trailing `-N` segment parsed at `:318`) still disambiguates forEach items; the seed loop's direct `r.file` read remains the authoritative path when a real file pointer exists. Comment block added at `:291-306` explaining the visits requirement + the fallback rationale. Chose option (b) (broader prefix) over option (a) (skip scan) — option (a) would still drop the `file:null` placeholder case to empty steps when visits is also absent, which is the exact silent drop O5 names.
- **Regression test** (`test/subflow-state-isolation.test.js`, after the `:267` case): "falls back to broad stepName prefix when step.visits is absent (O5: visits-absent + file:null no silent empty steps)". Constructs a `subflow` step with `visits` deliberately omitted (mirrors the `:99` step shape) AND a `subflowRuns` placeholder with `file: null` (the non-forEach start case) AND a real `run-state-SUB-1-0.json` on disk. Asserts the merge locates the file via broad prefix, merges live state (`currentStep="WORK"`, `steps.length=2`, `file` back-filled), and does not silently drop to empty steps. This pins the exact coupling (visits-absent + file:null) that O4's mid-audit break tripped.
- **Batch committed as one verify-then-committed unit** (per Phase 2 Exit Criteria): `monitor.js` (O4 refactor + O5 guard) + `StepTimeline.vue` (`v-if="group.forEachItem"` gate, suppresses misleading "📋 Plan N" label for non-forEach subflows like `DEEP_AUDIT`) + `subflow-state-isolation.test.js` (the `:267` file:null test + the new O5 test) + `user-manual.{en,zh}.md` (drop obsolete `_tmp/` empty-dir bug section) + `docs/logs/2026/07-22.md` (this entry, ships in same commit per AGENTS.md Docs Maintenance) + this plan file. Audit files (`2026-07-22-0755-*.md`) and sibling plan 2's doc edits are intentionally out of scope (sibling plan 2 owns the architecture-doc `:81` rewrite after this commit lands).
- **Rule-15 lesson-promotion adjudication** (Phase 2 `Decision` item): the "each `mergeSubflowChildren` refactor re-breaks the `:99` contract; narrow new test passes while broad regression fails" pattern is at recurrence count 2 (status-gate removal → merge). AGENTS.md Rule 15 says promote "when the pattern is recurring enough to justify reuse" — two refactors is borderline. **Decision: defer with trigger** — promote into `tools/mission-driver/memory/lessons.md` + add a pre-commit checklist item ("run full suite, not the new test, for any `mergeSubflowChildren` change") when a **third** refactor of this method re-breaks the `:99` contract. The durable record is this log entry + the plan's Deferred-But-Adjudicated section. Residual risk if the trigger fires before promotion: the next refactor may repeat the cycle (mitigated by the O5 test now pinning the visits-absent coupling).
- Verification (full green, recorded as known-good baseline): `pnpm --prefix tools/mission-driver test` → **532 pass / 0 fail** (baseline 531 + 1 O5 test = 532; duration 10.76s), chained final line `prompt-check: OK — all prompt result-tag examples are well-formed.`; `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (15.55s, pre-existing chunk-size warning only — no new warnings); `pnpm --prefix tools/mission-driver run lint:prompts` OK.

### 2026-07-22 — monitor 看不到 DEEP_AUDIT 正在运行的子步骤 + 非 forEach subflow 多余 "Plan 1" 标签

- 现象（参考 `_tmp/2026-07-22-075526-mission-driver`）：① DEEP_AUDIT 步骤展开后是空的，看不到正在跑的 OPEN_AUDIT；② DEEP_AUDIT 下面有个多余的 "📋 Plan 1" 标签（DEEP_AUDIT 不是 forEach subflow，没有"Plan"概念）。
- 根因 ①（`tools/mission-driver/src/monitor.js:249` `mergeSubflowChildren`）：上一轮修 forEach in-flight 时引入的 disk-scan 逻辑有 `if (childrenByIndex.has(idx)) continue` 早跳过——对 subflowRuns 已经有的索引不再读磁盘。但 non-forEach subflow（DEEP_AUDIT）启动时 engine 写的 placeholder 是 `{forEachIndex:0, status:"running", file:null}`——`file` 是 null！seed 阶段 `readSubflowState(runDir, null)` 返回 null → child 拿到空 steps + null currentStep。而真正的状态在 `run-state-DEEP_AUDIT-2-0.json`（`status:running, currentStep:OPEN_AUDIT, steps:[CHECK_OPEN_AUDITS skipped, MULTI_AUDIT completed, OPEN_AUDIT running]`），但 disk-scan 看到 idx=0 已在 map 里就 continue 了，永远不读。实测证据：磁盘文件有 3 个 steps，monitor 输出 0 个。
- 根因 ②（`tools/mission-driver/web/src/components/run/StepTimeline.vue:96-107`）：subflow children 的标签硬编码为 `📋 Plan {{ forEachIndex + 1 }}`——对 forEach subflow（EXEC_PLANS over activePlans）正确，对 non-forEach subflow（DEEP_AUDIT 单 child）误导：它不是 plan 迭代。
- 修复 ①（`monitor.js`）：重写 `mergeSubflowChildren` 为 MERGE 语义而非 SKIP 语义——
  - seed 阶段：`r.file` 非 null 时直接 `readSubflowState(runDir, r.file)` 拿 live state（正常 completed item 的主路径，不依赖 `step.visits` 推算前缀）
  - disk-scan 阶段：对每个磁盘发现的索引，若已在 map 里则 **MERGE**（disk state 覆盖 live 字段 status/steps/currentStep，保留 seed 的 forEachItem 元数据），若不在则 ADD（catches in-flight item）。修后 `file=null` 的 placeholder 也能从磁盘补全 live state。
- 修复 ②（`StepTimeline.vue`）：`<span v-if="group.forEachItem">` 仅当 forEachItem 存在时才渲染 "📋 Plan N: name" 标签；forEachItem 为 null（non-forEach subflow）时只显示 status tag，不再误导。判断可靠：engine 对 non-forEach subflow 的 subflowRuns 永远写 `forEachItem: null`（`engine.js:1120`），forEach subflow 的 forEachItem 永远是 plan 路径（非 null）。
- 测试覆盖（`tools/mission-driver/test/subflow-state-isolation.test.js`）：新增 1 个 regression test "fills live state from disk when subflowRuns placeholder has file=null"——构造 DEEP_AUDIT 场景（`subflowRuns:[{file:null, status:"running"}]` + 磁盘 `run-state-DEEP_AUDIT-2-0.json` 有 3 steps），断言修后 child 的 `currentStep="OPEN_AUDIT"` + `steps.length=3`（修前是 null + 0）。
- Verification (full green): `pnpm --prefix tools/mission-driver test` → **531 pass / 0 fail**（+1 vs 上次 baseline 530）；`pnpm --prefix tools/mission-driver/web run build` → `vue-tsc --noEmit && vite build` clean。live 数据实测 DEEP_AUDIT v=2 从 0 steps → 3 steps（CHECK_OPEN_AUDITS/MULTI_AUDIT/OPEN_AUDIT），EXEC_PLANS v=2 仍正确显示 6 个带 forEachItem 的 plan。
- 部署：重启 monitor 加载新 `web/dist` + 新 `monitor.js`。

### 2026-07-22 — 移除 RunDetail 顶部 "Deep Audit: N / M" 指示器

- 现象：用户反馈 RunDetail 右上角的 `Deep Audit: 1 / 3` 指示器无意义——Deep Audit 默认只应跑一次，配额进度展示是噪音。
- 该指示器来源：mission-driver-step-audit WI5（`design/step-execution-and-audit-count-design.md §4.4`），`DefaultRunDetail.vue:28-32` 在 `maxAuditRounds > 0` 时渲染 `<n-tag>Deep Audit: {{auditRound}} / {{maxAuditRounds}}</n-tag>`。`maxAuditRounds: 3` 来自 `flows/mission-driver.json:7`。
- 背景：上次已把 `DEEP_AUDIT.complete` transition 从 `→ DRAFT_PLANS`（无限循环 bug）修成 `→ REVIEW_PLANS`，循环不再空转——多轮配额（`maxAuditRounds: 3`）本就没有实际意义，指示器更是冗余。
- 修复：`tools/mission-driver/web/src/components/run/detail/DefaultRunDetail.vue` 删掉 `audit-round-row` 整段 `<div>` + 关联 `.audit-round-row` CSS 规则 + 不再使用的 `NTag` naive-ui import。`run.ts` 的 `auditRound?` / `maxAuditRounds?` interface 字段保留（API 仍返回，未来若有需要可重新挂回 UI；删字段会破坏后向兼容，得不偿失）。
- Verification (full green): `pnpm --prefix tools/mission-driver/web run build` → `vue-tsc --noEmit && vite build` clean（typecheck pass，build 成功，无 unused import / unused CSS 警告）。
- 部署：用户需**重启 monitor**（独立 monitor PID 28224，或运行中引擎的内置 monitor）才能加载新构建的 `web/dist`。

### 2026-07-22

- **NF1 adjudication + cross-mission commit hygiene — `DEEP_AUDIT complete → REVIEW_PLANS` flow edge** (plan `2026-07-22-0814-3` Phase 1, adjudicating open-audit O2 + multi-audit NF1). The 0755 multi-audit originally rated this edge **MEDIUM BLOCKING — NET-NEW** ("undocumented / unplanned / accidental regression or deliberate fix — either way undocumented"). The 0755 open-audit O2 re-checked the authoritative owner doc and found it is a **partial false positive**: the edge is (a) **deliberate** — reverts the 2026-07-14 commit `0c763f0` regression that caused `DRAFT_PLANS` (which only reads the roadmap) to ignore active plans the audit created → `nothing` → `DEEP_AUDIT` spin; (b) **documented** with rationale + commit history in `tools/mission-driver/design/step-execution-and-audit-count-design.md:43` (the authoritative design doc for step execution + the audit counter); (c) **semantically justified** — `REVIEW_PLANS.forEach = draftPlans()` funnels audit-created active plans to `EXEC_PLANS`, or no-ops to `EXEC` when `SCAN_NEW_RESULTS` already promoted them. NF1's scoping checked `mission-design.md §6` + `mission-driver-flow-design.md` but never opened `step-execution-and-audit-count-design.md`. **Adjudication**: NF1 downgraded from BLOCKING to LOW in `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-multi-audit-*.md` (heading + body + Recommendation + Residual Risks table); the genuine residuals — (1) transition test gap and (2) cross-mission commit hygiene — are closed/recorded here + by `test/draft-plans-audit-gate.test.js` Case G. **Do NOT revert** — reverting reintroduces the documented spin bug.
  - **Cross-mission commit hygiene (residual #2)**: the `complete → REVIEW_PLANS` flow change in `tools/mission-driver/flows/mission-driver.json:88` was committed in `ab16984` (`feat(mission-driver-draft-robustness): plan-2026-07-21-1605 draft-pipeline robustness closure`, bullet "deep-audit-loop complete transition retargeted to REVIEW_PLANS"). That commit cites the **draft-robustness** plan (`2026-07-21-1605-2`), but the change logically belongs to the **`mission-driver-step-audit`** mission — its authoritative rationale + regression history live in `design/step-execution-and-audit-count-design.md` (owned by step-audit per `missions/mission-driver-step-audit.json`). The commit already rode the draft-robustness batch (cannot retroactively re-attribute); this log entry is the durable attribution record so a future reader tracing the edge finds the correct owner doc + mission. **Forward note**: future commits touching step-execution / audit-count flow edges (`DEEP_AUDIT` transitions, `auditEntry`, `maxAuditRounds`) must cite the `mission-driver-step-audit` mission's design doc + owning plan, not the draft-robustness commit. This is a commit-message / attribution concern, not a behavior concern — the code is correct.
  - **Transition test (residual #1)**: `test/draft-plans-audit-gate.test.js` Case G added — structural assertion against the real flow via `createMissionDriverFlow({ flowName: "mission-driver" })` pinning all three `DEEP_AUDIT` edges (`complete → REVIEW_PLANS`, `failed → DRAFT_PLANS`, `onError → DRAFT_PLANS`) so no silent regression in any direction can recur. Uses the REAL flow, NOT the `gateFlow()` helper (which deliberately routes `complete → DRAFT_PLANS` as a simplified fixture — mirroring it would assert the wrong destination).

- mdr-remediate-7 (mission-driver-draft-robustness) closed: 0755 multi-audit NF1 (flow-edge adjudication) + F4/N1-arch (architecture-doc decision cross-ref) 收口。Plan adjudicates the same `DEEP_AUDIT complete → REVIEW_PLANS` edge that mdr-remediate-6 synced the operator-doc mermaid to (Plan 2/3 complementary per plan `> Related:`). Net code change: +1 transition test (Case G); rest is audit-severity edits + decision cross-refs + log.
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-22-0814-3-flow-edge-adjudication-and-architecture-debt-decision.md` → `Plan Status: completed`.
  - Phase 1 — NF1 (Decision|Fix|Add|Add): see the NF1 adjudication + commit-hygiene bullet above (decision confirmed + audit downgraded + commit-hygiene note recorded). **Fix**: NF1 heading in `2026-07-22-0755-multi-audit-*.md` changed from `[MEDIUM, BLOCKING — NET-NEW]` to `[LOW — cross-mission commit hygiene + transition test gap]`; body rewritten to state edge is deliberate + documented + semantically justified (cross-ref `step-execution-and-audit-count-design.md:43` + open-audit O2); Recommendation moved NF1 from Primary blockers to secondary bundle; Residual Risks table rows updated. **Add**: Case G transition test (structural assertion against real flow via `createMissionDriverFlow`, pinning all 3 DEEP_AUDIT edges — see bullet above). **Add**: commit-hygiene note (see bullet above — flow change committed in `ab16984` under draft-robustness, logically belongs to step-audit; forward note for future commits).
  - Phase 2 — F4/N1-arch (Decision|Add): **Strategy adapted to changed reality.** Plan draft preferred Option C (defer with note), but the decision was already made + executed by sibling `2026-07-21-1605-1` Phase 2 (closed before this plan executed) — chose **Option A** (mission-scoped `docs/architecture/mission-driver-baseline.md`). The sibling plan's rationale governs (landed first + completed); this plan's Option C preference is moot. Residual scope for this plan = audit cross-reference only. **Add**: both 0755 audit files' F4/N1-arch sections now carry "Decision recorded" note pointing at `docs/architecture/mission-driver-baseline.md` + the sibling plan. Multi-audit Prior-Finding Status table row updated from "STILL OPEN" to "DECISION RECORDED". No new artifact created (sibling plan already created it).
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **530 pass / 0 fail** (baseline 529 + 1 Case G = 530), chained `prompt-check: OK`; `pnpm --prefix tools/mission-driver/web run typecheck` clean; `pnpm --prefix tools/mission-driver/web run build` success (18.11s); `pnpm --prefix tools/mission-driver run lint:prompts` OK. test-count delta +1 (Case G).
  - Scope proof: 触及文件 = `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-multi-audit-*.md` (Phase 1 NF1 downgrade + Recommendation + Residual Risks + Prior-Finding table; Phase 2 F4/N1-arch cross-ref) + `docs/audits/mission-driver-draft-robustness/2026-07-22-0755-open-audit-*.md` (Phase 2 F4/N1-arch cross-ref) + `tools/mission-driver/test/draft-plans-audit-gate.test.js` (Phase 1 Case G) + `docs/logs/2026/07-22.md` (Phase 1 NF1 adjudication + commit-hygiene bullet + 本 closure bullet) + 本 plan 文件 (items ticked + Status: completed + Closure section filled)。No src/ / web/ / prompt / missions / flow change (Non-Goal honored — the flow edge already ships; this plan only pins + documents it).
  - Closure: executor cold-replay pass + independent draft review (2 iterations, recorded in plan) — the plan resolves a source-of-truth conflict (NF1 BLOCKING vs O2 false-positive), so AGENTS.md Reviewer-Availability Fallback requires independent review; the independent draft review (which verified `step-execution-and-audit-count-design.md:43` evidence live in both iterations) satisfies that independence bar. Plan advances to `completed` per mission-driver EXECUTE flow. 无 `> Work Item:` 标签（audit-sourced plan — WI1-WI5 已 done，本 plan 是 post-WI audit cleanup），roadmap work-item 状态不需更新；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line"）。

- **NF2 attribution — `enablePositionalOptions()` was a user-visible CLI change that should have routed through its own small plan** (process-routing remediation, no code change). The fix at `tools/mission-driver/src/main.js:771` (`program.enablePositionalOptions();` after `const program = new Command();`) works around the **commander 15.0.0 option-stripping quirk**: when the same option name (e.g. `--step`) is declared on BOTH a subcommand (`program.command("run").option("--step")`) AND the main command (`program.option("--step")`), invoking the subcommand silently drops the option (`run X --step Y` → sub action sees `step=undefined`). `enablePositionalOptions()` makes options after the subcommand name route to the subcommand — verified empirically to fix `run <mission> --step <S>` / `--from-step <S>` / `--no-monitor` without breaking the bare main-command path. The change is pinned by `test/from-step.test.js` (rewritten with isolated `tmpdir` roots + new CLI assertions covering `run <mission> --step` / `--from-step` / `--no-monitor`). **Routing gap:** this landed inside the draft-robustness remediation batch (code comment at `src/main.js:762-770`) but is unrelated to any draft-robustness finding. Per AGENTS.md Planning Triggers it crossed both "changes user-visible behavior" and "public contract behavior" (CLI surface) and should have routed through its own small plan — but a back-dated plan cannot drive the routing decision it should have driven, so the residual gap is closed by this attributed log entry + this plan's reference (`docs/plans/mission-driver-draft-robustness/2026-07-22-0814-2-owner-doc-and-plan-traceability-sync.md`, Phase 2) instead of fake plan theater. **Forward note:** future CLI-surface changes route through a plan first (the `enablePositionalOptions()` code comment is the durable code-level record; this log entry is the durable process-level record).

- mdr-remediate-6 (mission-driver-draft-robustness) closed: 0755 open-audit O1 + multi-audit NF2/NF3/NF4 + carry-forward N4 doc-sync 收口。Doc-only / traceability reconciliation — no code / test / prompt / missions change.
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-22-0814-2-owner-doc-and-plan-traceability-sync.md` → `Plan Status: completed`.
  - Phase 1 — O1 (Fix|Proof): `tools/mission-driver/EXECUTION-PRINCIPLE.md §3` state-machine mermaid 2 drifts fixed. **Drift A** (`:101`): `DEEP_AUDIT --> DRAFT_PLANS: complete / failed` 拆成两条边 `DEEP_AUDIT --> REVIEW_PLANS: complete` + `DEEP_AUDIT --> DRAFT_PLANS: failed`，matching `flows/mission-driver.json:88-89`。**Drift B + whole-doc sweep**: 全文 cycle-cap `30 → 8` sweep at `:117-118` (`visitCount > 8`) / `:169` (`maxCycleVisits(8)?`) / `:479` (`超 8 次`) / `:579` (`maxCycleVisits(8)`)，matching `flows/mission-driver.json:4` `"maxCycleVisits": 8` + `README.md §"Cycle limits"`。Proof: re-read mermaid，其余 8 条 edges 全 match live flow；`grep -n "30" EXECUTION-PRINCIPLE.md` 剩 3 hits 全非 cycle-cap（`:321`/`:532` line-number ranges `engine.js:430-512` / `:130-141`，`:431` exit code 130）— intentionally untouched。**N4** already closed upstream by sibling `mdr-remediate-1` (`1605-1` `Plan Status: completed`)：`design/draft-robustness-design.md §4.3.1` snippet 无 `plansRoot` (option a dropped) + `:322` callout `> **plansRoot omitted (N4 fix, mdr-remediate-1)**` citing `docs/logs/2026/07-21.md` WI3 entry。`grep -n "plansRoot" draft-robustness-design.md` → 1 hit (the deferred-annotation callout)。
  - Phase 2 — NF2 (Decision|Add): 见上方独立 NF2 attribution bullet。Decision: 选 attributed log entry over back-dated plan — code 已 landed + tested + commented (`src/main.js:762-771`)，residual gap 纯粹是 attribution，back-dated plan 无法驱动它本应驱动的 routing decision。NF2 bullet 作为 `### 2026-07-22` section 的 first bullet 加入（file 已存在，未创建 duplicate section header）。
  - Phase 3 — NF3 + NF4 (Decision|Fix): **Strategy adapted to changed reality.** 本 plan 的 baseline (2026-07-22) 假设 `1605-1/2/3` 仍是 empty drafts (NF3 核心问题)，但执行时发现三 plan 已被独立 narrowed + executed + closed — 全 `Plan Status: completed` (`grep -c "^- \[ \]" 1605-{1,2,3}.md` → 0/0/0)。Option D (mark `superseded`) 的 premise 不再成立：`completed` (executed) 是比 `superseded` (retired-without-execution) 更强更准的 terminal state，按 AGENTS.md Rule 13 + guide Plan Status Flow 不应降级。Reconciliation 收敛为 Option C-style header updates：两 0952 audit headers (`multi-audit:4` + `open-audit:4`) 的 `Remediation Plans:` line 重写为完整 execution chain — `1005/1523` (original F1–F14) → `1605-1/2/3` (narrowed residual, completed) → `2026-07-22-0814-1/2/3` (0755 re-audit residual owners: O1/O3/N4-carryforward/NF1–NF4)，header 末尾注明 "1605 set was executed as narrowed plans, not retired as superseded drafts"。NF4 closed: design doc (`draft-robustness-design.md:248,424,428`) 对 `1005-3` / `mdr-remediate-3 A1` / `mdr-remediate-4` 的 citation 现与 reconciled audit header (cite 同一 `1005/1523` set) 一致，无 contradiction。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **529 pass / 0 fail**（baseline preserved — 无代码改动；plan 的 "520/0" Exit Criteria 来自更早 baseline，test count 后续因 N1/N1-D 增长至 529，behavior 无变化）。Plan 是 doc-only / traceability edits — 无 code/prompt/missions 改动，typecheck/build/lint:prompts 不受影响（未触及 web/ 或 prompts/）。注：`test/subflow-incremental.test.js:204` Case C 是 timing-sensitive Promise-park latch test，本次执行中曾 1 次 flaky fail（visit-2 item-0 incremental append race），但重跑稳定 529/0；pre-existing flakiness 与本 plan 无关（doc-only），未在 scope 内修复。
  - Scope proof: 触及文件 = `tools/mission-driver/EXECUTION-PRINCIPLE.md` (Phase 1 Drift A/B + sweep) + `docs/audits/mission-driver-draft-robustness/2026-07-21-0952-multi-audit-*.md` (Phase 3 header) + `docs/audits/mission-driver-draft-robustness/2026-07-21-0952-open-audit-*.md` (Phase 3 header) + `docs/logs/2026/07-22.md` (Phase 2 NF2 bullet + 本 closure bullet)。`design/draft-robustness-design.md` 与 `1605-1/2/3` plan 文件 NOT edited (already in correct final state)。working-tree 其它未提交改动属 sibling plans 与既有 sessions 的 in-progress 工作，与本 plan 无关。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk doc-only / traceability plan per AGENTS.md Reviewer-Availability Fallback；源 0755 audit 评级 O1 MEDIUM doc-only / NF2 LOW process / NF3+NF4 LOW-MEDIUM traceability)。无 `> Work Item:` 标签（audit-sourced plan，非 roadmap-sourced — WI1-WI5 已 done，本 plan 是 post-WI audit cleanup），roadmap work-item 状态不需更新；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line"）。

- mdr-remediate-1 (mission-driver-draft-robustness) closed: design + architecture owner-doc sync 收口最后 2 个 open findings（N4 stale code snippet, F4/N1-arch architecture coverage gap）。Doc-only sync，无代码 / 测试 / prompt / missions 改动。原始 draft 针对 8 个 findings，其中 6 个已由 completed `2026-07-21-1005-1` 收口（见 plan 的 Pre-Resolved By Sibling Plan 表）；本 plan 只处理剩余 N4 + F4/N1-arch。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1605-1-design-and-architecture-doc-sync.md` → `Plan Status: completed`。
  - Phase 1 — `tools/mission-driver/design/draft-robustness-design.md` §4.3.1 (N4):
    - **N4 (Fix)**: §4.3.1 `resolveTemplateVars` code block 内的 `plansRoot: resolve(resolved.projectRoot, "docs/plans"), // 新增（供 mission-draft 引用 planGuide 相对位置）` 行**删除**（选 preferred "drop" option — snippet 应描述 what shipped，not what was drafted-then-deferred）。code block 后新增 callout：`> **plansRoot omitted (N4 fix, mdr-remediate-1)**`，说明 WI3 deliberate omission（no prompt references `{{plansRoot}}` → 预添加属 dead code）+ 引 `docs/logs/2026/07-21.md` WI3 entry 的 "未引入 `plansRoot` 模板变量 … 预添加属 dead code" 原文 + 确认剩余 vars match `src/main.js:423-429` (Stage 1) / `:487-493` (Stage 2)。grep 验证：`plansRoot` 在整个 `tools/mission-driver/` 仅此一处出现（无 prompt / src 引用），删除安全。
  - Phase 2 — Architecture coverage decision (F4 / N1-arch):
    - **Decision**: 选 **Option A**（mission-scoped）。理由：AGENTS.md Documentation Ownership 声明 `docs/architecture/` owns "cross-cutting technical and module-boundary truth"；mission-driver 是 stable cross-cutting tool（`mission-design.md` §10: "operationalizes the AGE loop"）；其 public contracts 应在 architecture level 有文档（cite 而非 duplicate detailed design docs）。Option B（defer with note）会让 `docs/architecture/README.md` 继续声称一个不成立的状态；Option C（minimum）too minimal。Residual risk：broader template debt（`system-baseline.md` / `module-boundaries.md` / `project-vision.md` 仍是 "Fill In" stubs）仍未解——三个 option 都会加 backlog row 跟踪（见下）。
    - **F4 / N1-arch (Fix)**: 新建 `docs/architecture/mission-driver-baseline.md`（不 duplicate 详细设计，enumerate stable contracts + cite owner docs）：Public CLI Surface（`run` / `draft` / `list` / `list-steps` / `analyze` / `monitor` 六命令 + key options + commander registration anchors `main.js:853-922` / `:926-954`）；Mission Config Schema（`REQUIRED_FIELDS` / `REQUIRED_COMMANDS` / `extends` shallow merge chain + cite `mission-design.md` §8）；Draft-State Schema（不重复 14-field 表，cite `draft-robustness-design.md` §1.4，记录 key invariants：status enum / phase `"rejected"` 语义 / merge 保留 `desc`）；Run-State Shape（`run-state.json` top-level + per-step fields + subflow `subflowRuns[]` ordering invariant + `_subflowId` convention + WI5/mdr-remediate-4 incremental persistence + monitor fallback defense-in-depth）；Marker Contracts（`<BRIEF_GATE>` + `<AI_STEP_RESULT>` cross-cutting + step-transition markers cite `mission-design.md`）；Public Exports vs Test Seams（`cmdDraftMission` / `parseDraftArtifact` / `extractBriefGate` / `validateMission` / `loadMission` / `parseRoadmapMarkdown` / `snapshot` 为 public；`__setRunnerFactoryForTest` / `__setSpawnerForTest` / `_scanOpenAuditsList` 等 `__`/`_`-prefixed 为 test seams）；Detailed Owner Docs 表（7 个 cross-ref）。
    - **F4 / N1-arch (Fix)**: `docs/architecture/README.md` 同步更新——Suggested Reading Order 加 `4. mission-driver-baseline.md`（带一行说明）；Initial Owner Docs 列表加 `mission-driver-baseline.md`。grep 验证 `grep -rn "mission-driver\|draft-state\|subflowRuns\|BRIEF_GATE" docs/architecture/` 现有大量 hits（之前 zero hits）。
    - **Template-debt backlog row**: `docs/backlog/mission-driver-draft-robustness-roadmap.md` Follow-up Backlog 段新增 1 行——"Project-wide architecture template-debt paydown (`system-baseline.md` + `module-boundaries.md` + `project-vision.md` 仍是 Fill In stubs)"，priority `P3 (template debt)`，trigger = "second cross-cutting tool lands OR copied project needs the baseline"，acceptance = 三 stub 填实；roadmap `Last Updated` 头部刷新到 2026-07-22。
  - 未改任何代码 / 测试 / prompt / missions 文件（Non-Goal）；未重开 WI1–WI5 契约；未把历史迁移 prose 倒灌进 `docs/architecture/`（AGENTS.md Rule 6 — `mission-driver-baseline.md` 只记 current supported baseline）；未 duplicate `draft-state.json` schema 表（cite `draft-robustness-design.md` §1.4 — 同 CONTEXT.md 的交叉引用约定）；未处理 code-fix findings（F2 / N2 / F6 / F11-test / F12 / F13 在 `2026-07-21-1605-2`；N1 / F3 / N3 / F7 / F14 在 `2026-07-21-1605-3`）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **520 pass / 0 fail**（baseline preserved — 无代码改动），chained `prompt-check: OK`；`pnpm --prefix tools/mission-driver run lint:prompts` OK。Phase 2 doc-only（新建 architecture doc + README + backlog row），无需 typecheck/build（前端代码未动）。
  - Scope proof: 本 plan 触及的文件 = `tools/mission-driver/design/draft-robustness-design.md` (Phase 1) + `docs/architecture/mission-driver-baseline.md` (new) + `docs/architecture/README.md` + `docs/backlog/mission-driver-draft-robustness-roadmap.md` (Phase 2)。working-tree 其它未提交改动（`src/main.js` / `src/engine.js` / `test/*.js` / `flows/mission-driver.json` / `tools/mission-driver/CONTEXT.md` / `docs/logs/2026/07-21.md` 等）属 sibling plans `1605-2` / `1605-3` 与既有 sessions 的 in-progress 工作，与本 plan 无关。
  - Closure: solo cold-replay pass (no second reviewer available; non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback；源 audit 评级 N4 LOW / F4+N1-arch MEDIUM-LOW doc-only)。无 `> Work Item:` 标签（audit-sourced plan，非 roadmap-sourced），roadmap work-item 状态不需更新（WI1-WI5 已 done，本 plan 是 post-WI audit cleanup）；无 `> Source Audits:` 标签（front matter 用的是 `> Source:`），source audit 文件不需 close（按指令"omit the step entirely if there is no `> Source Audits:` line"）。

- mdr-remediate-2 (mission-driver-draft-robustness) closed: N2 monitor-side pre-validation 收口 — `handleStartDraft` 在 `startDraftJob` 调用前加 `validateDraftDesc` upstream gate，镜像 `cmdDraftMission` 的 WI1 契约（design §4.1），使 dashboard 的 `POST /api/draft` 路径不再能以 placeholder/over-short `desc` 触发 doomed child spawn。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1605-2-draft-pipeline-robustness-closure.md` → `Plan Status: completed`。
  - Phase 1 — N2 (Fix): `src/monitor.js` 新增 `import { validateDraftDesc } from "./main.js"`（line 34）；`handleStartDraft` 在 byte-size check 之后、flowHint/targetFile 解析之前，读 `missions/base.json`（同 `src/main.js:344-348` 模式 `catch { cfg = {} }`），调 `validateDraftDesc(desc, cfg?.draft?.minDescLength)`，`!v.ok` → `return { error: v.reason, status: 400 }`。`handleStartDraft` 改为 `export function`（供 N2 测试直接调用）。Closes "defense in depth on both ends" pair: F2 downstream terminal-state write（已 shipped）+ N2 upstream monitor gate。
  - Phase 1 — N2 (Add|Proof): `test/monitor.test.js` 新增 4 个 N2 测试（mirror `__setSpawnerForTest` setup at `:2205`，spawn counter 而非 lastSpawn）：N2-A too-short `"d"` → 400 `/too short/i` + 0 spawns + no jobDir；N2-B placeholder `"test"` → 400 `/placeholder/i` + 0 spawns；N2-C base.json `{draft:{minDescLength:8}}` + `"add xy"`（len 6 ≥ default 4 but < 8）→ 400 `/too short/i` + 0 spawns（证明 monitor 侧读同一 override）；N2-D valid `"add audit count to dashboard"` → jobId/pid + 1 spawn + jobDir created（no-regression）。
  - Regression fix: N2 gate 改变了 validation 顺序（validateDraftDesc 在 flowHint/targetFile 之前），4 个现有 mdo-2/mdo-4 P2 测试用了短 `desc`（`"x"` / `"a"` / `"b"`，len < 4）被新 gate 拦截而非到达它们原本测试的 code path。更新为 valid desc（`"make mission"` / `"make mission one"` / `"make mission two"` / `"build feature"`）使它们到达 targetFile/flowHint/listing 分支。测试意图不变。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **524 pass / 0 fail**（baseline 520 + 4 N2 = 524）；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` success（19.26s）；`pnpm --prefix tools/mission-driver run lint:prompts` OK。test-count delta +4 记录在 Closure Gates。
  - Circular import 安全：`main.js` imports `startMonitor` from `./monitor.js`，`monitor.js` 现在 imports `validateDraftDesc` from `./main.js`。ESM function declaration 的 live binding 在 instantiation phase 已初始化，`handleStartDraft` 仅在 request time 调用 `validateDraftDesc`（非 module eval time），circular import 无 TDZ 风险。
  - Closure: solo cold-replay pass (non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback；源 audit 评级 N2 LOW-MEDIUM)。无 `> Work Item:` 标签（audit-sourced plan）；无 `> Source Audits:` 标签（`> Source:` 非 `> Source Audits:`），source audit 不 close（audit 覆盖 N1-N4，N1/N3 归 `1605-3`，N4 归已 closed 的 `1605-1`）。

- mdr-remediate-3 (mission-driver-draft-robustness) — engine extraction + verification invariants (N1, F3, N3, F7, F14) 执行中。Plan: `docs/plans/mission-driver-draft-robustness/2026-07-21-1605-3-engine-extraction-and-verification-invariants.md`。Draft review 已确认 F3/F7/F14 已在 baseline 落地（verify-only）；净新工作只有 N1（extraction-site ANSI strip）+ N3（`_onAgentStepUpdate` visits guard Decision）。
  - Phase 1 — N1 (Fix|Add|Proof): `src/main.js` import `stripAnsiControl` from `./engine.js`（已有 `FlowEngine` import 行加一项）；三个 extraction call site 在 regex match 前各加 `const clean = stripAnsiControl(resultText);` 并改 `resultText.match` → `clean.match`：`extractBriefPath` (`:160-168`)、`extractBriefGate` (`:188-195`)、`parseDraftArtifact` (`:244-248`)。镜像 engine-layer strip-at-extraction 纪律（memory L009；`engine.js:705-712` 已对所有 marker 提取这样做）。新增 3 个回归测试：N1-A (`brief-gate.test.js`) ANSI-wrapped gate+reason `\x1b[32m<BRIEF_GATE>pass...\x1b[0m` 仍解析；N1-B ANSI 夹在 tag 名字符内 `<BRIEF\x1b[0m_GATE>` strip 后仍匹配；N1-C (`draft-path-consistency.test.js`) ANSI 夹在 value capture（path 与 `</MISSION_FILE>` 之间 `\x1b[0m`）仍正确 resolve missionName/roadmapPath/missionFile（cross-ref `ansi-and-mixedcase-tag.test.js:108` 的 value-pollution failure mode）。
  - Verification (Phase 1): `pnpm --prefix tools/mission-driver test` → **527 pass / 0 fail**（baseline 524 + 3 N1 = 527），chained `prompt-check: OK`；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` success（25.12s）；`pnpm --prefix tools/mission-driver run lint:prompts` OK。test-count delta +3。
  - Phase 2 — F3 verify + N3 Decision (Option B doc-only): F3 已在 baseline 落地（`engine.js:1124` 在 `await _runChildSubflow` `:1125` 前调 `_wfAppendSubflowRun` 写 running placeholder，H2 comment block `:1117-1123`；Case F/G/H 在 `subflow-incremental.test.js:353,362,434` 全绿）。N3 选 **Option B（doc-only）**：`_onAgentStepUpdate` (`engine.js:438`) 加注释说明为何无 visits guard — run loop 严格顺序执行，`_wfOpen` (`:332`) 在 push 下一个 running entry 前先 close 前一个 `_wfCurrent`，故任一时刻同一 stepName 至多一条 "running" 记录，`name+status==="running"` 永远唯一解析；`_wfAppendSubflowRun` 保留 visits guard 因 forEach re-entry + H2 pre-run placeholder 更接近其失败面。若未来引入 re-entrant/concurrent agent step，从 `_wfAppendSubflowRun` retrofit。Option A（retrofit guard）被否决的理由：sequential lifecycle 下 guard 是冗余的，无法构造真实 flow 复现 bug（agent step 在 re-entry 前已 close），加 guard + 不可复现的测试属 over-engineering（违反 AGENTS.md "Prefer small complete slices over broad placeholder coverage"）。Option B 准确记录 assumption 而非假装修一个不会发生的 bug。
  - Phase 3 — F7 + F14 verify-already-landed (Proof only，无代码/测试改动): F7 已落地 — `package.json:11` `"test": "node --test test/*.test.js && node src/prompt-check.mjs"` 链 `prompt-check.mjs`，`CONTEXT.md` "构建与验证" 段已文档化（"同时跑 prompt-check.mjs 结构性校验，任一失败即整体失败"）；本次 `pnpm test` 输出同时含 `527 pass` + `prompt-check: OK` 证明链生效。F14 已落地 — `subflow-incremental.test.js:119-202` Case B 用 deterministic Promise-park latch（`park0`/`park1` + item 2 `await Promise.resolve()` 单 microtask yield，comment cites `mdr-remediate-4 H9`），Case B 内无 `setTimeout`-based waits；共享 `delay()` helper (`:29`) 仅 Case D (`:317-318` resolve-order coverage) 使用。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **527 pass / 0 fail**，chained `prompt-check: OK`；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` success；`pnpm --prefix tools/mission-driver run lint:prompts` OK。test-count delta +3（N1-A/B/C）。触及文件 = `src/main.js` (N1 import + 3 call sites) + `src/engine.js` (N3 Option B comment) + `test/brief-gate.test.js` (N1-A/B) + `test/draft-path-consistency.test.js` (N1-C)。
  - Closure: solo cold-replay pass (non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback；源 audit 评级 N1/N3 LOW)。无 `> Work Item:` 标签（audit-sourced plan，非 roadmap-sourced — WI1-WI5 已 done，本 plan 是 post-WI audit cleanup）；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（共享 audit 覆盖 N1-N4，N4 归已 closed `1605-1`、N2 归已 closed `1605-2`、N1/N3 本 plan 收口 — 全部 4 个 open-audit finding 现归三个 completed plan；audit 文件的 `> Audit Status: planned` 由 mission 级 closure 处理，非单 plan 职责）。

- mdr-remediate-5 (mission-driver-draft-robustness) closed: 0755 open-audit 的 4 个 findings (N1, N2, N3, O3) 收口。本 plan 的 baseline 是 0755 audit 时的快照（"0 hits in main.js" / "0 hits in monitor.js"）；执行时发现 mdr-remediate-2 (N2) 和 mdr-remediate-3 (N1+N3) 已 commit 收口了大部分 code/test scope — 本 plan 的净值工作变成: Phase 1 补一个 missing test (BRIEF_FILE ANSI)、Phase 2 把 mdr-remediate-2 留下的 monitor→main 循环依赖 refactor 掉、Phase 3 删 dead code。
  - Plan: `docs/plans/mission-driver-draft-robustness/2026-07-22-0814-1-draft-pipeline-invariants-adoption.md` → `Plan Status: completed`。
  - Phase 1 — N1 (Fix|Proof|Decision): N1 的 code (3 call sites + import) 与 3 个回归测试 (N1-A gate+reason ANSI / N1-B intermixed-tag ANSI / N1-C `<MISSION_FILE>` value-capture ANSI) 已由 `mdr-remediate-3` ship。Plan 的 Proof item line 70 明确要求 "an ANSI-wrapped `<BRIEF_FILE>` case and an ANSI-wrapped `<MISSION_FILE>` case" — 后者在 N1-C 已 cover，前者缺失。新增 `test/draft-path-consistency.test.js` Case N1-D：用 fake runner 返回 ANSI-wrapped `<BRIEF_FILE>` marker (`\x1b[32m<BRIEF_FILE>path</BRIEF_FILE>\x1b[0m`)，跑 `cmdDraftMission`，断言 `draft-state.json` 的 `briefPath` 等于 clean resolved path（证明 extractBriefPath 在 regex match 前 strip 了 ANSI）。`extractBriefPath` 不是 exported（不在 public surface），故走 integration test 而非 unit test。Decision: call-site option (Option 1) chosen over structural `src/ansi.mjs` (Option 2) 与 runner-boundary pre-clean (Option 3)；Rule-15 reopen condition = 第三个 `engine.js`/`main.js` 之外的 caller fails to adopt `stripAnsiControl`（目前仍 0 个外部 caller，未达 promotion threshold）。
  - Phase 2 — N2 (Fix|Proof|Decision): mdr-remediate-2 ship 了 `handleStartDraft` 的 validateDraftDesc gate + 4 个 N2 测试 (N2-A too-short / N2-B placeholder / N2-C base.json override / N2-D valid)，但用 `import { validateDraftDesc } from "./main.js"` 进 monitor.js — 因 main.js:12 statically imports startMonitor from monitor.js，形成 cycle `monitor.js → main.js → monitor.js`。ESM live bindings 让它 runtime-safe（validateDraftDesc 只在 handleStartDraft request-time 调用，非 module eval time），但留下 graph cycle 让未来 maintainer 加 top-level reference 时 hit TDZ。本 Phase refactor: 把 `validateDraftDesc` (+ JSDoc) 从 `main.js:197-226` move 到 `draft-job.mjs:49-83`（leaf module — draft-job.mjs 只 import `node:fs`/`node:path`/`node:url`/`./spawner.mjs`，无 cycle）；main.js 加 `import { validateDraftDesc } from "./draft-job.mjs";`（cmdDraftMission 仍 internal 调用）+ 加入 `export { ..., validateDraftDesc }` re-export（保持 `test/draft-desc-validate.test.js` 的 `import { validateDraftDesc } from "../src/main.js"` 不破）；monitor.js 改 `import { ..., validateDraftDesc } from "./draft-job.mjs"`（与现有 startDraftJob/readDraftJob/listDraftJobs 同源）。Verify: `grep -n "from \"./main.js\"" tools/mission-driver/src/*.js` → 0 hits in source (仅 main.js 内部 comment 引用)；`draft-job.mjs` imports 只有 `node:*` + `./spawner.mjs`。
  - Phase 3 — N3 + O3 (Fix|Decision): N3 comment 已由 mdr-remediate-3 ship 在 `engine.js:438-452` — 覆盖 plan 要求的全部点（`name+status==="running"` only / `_wfAppendSubflowRun` 额外 match visits / sequential lifecycle 假设 / retrofit trigger）。Verify-by-read-back 确认 sufficient，无改动。N3 Decision: doc-only Option B confirmed sufficient — re-entry race 是 latent（无 production flow self-loops an agent step），structural refactor (`_findRunningStep` helper) 是 durable Rule-15 answer 但 defer 到 trigger fire (`transitions: { retry: <agentStep> }` or `goto: <agentStep>`)。O3: `readKnowledgeDir` (main.js:49-70 with JSDoc) 删除 — repo-wide `*.js` grep 仅 1 hit (定义本身)，not in export list，no test reference，no caller。O3 Decision: D5 (deterministic-regression-executor) 不在任何 backlog（`grep -rn "D5\|deterministic-regression" docs/backlog/` → 0 hits），不 in near-term roadmap → 删除（AGENTS.md Rule 12 no-dead-code）。
  - Verification (full green): `pnpm --prefix tools/mission-driver test` → **528 pass / 0 fail** (baseline 527 from mdr-remediate-3 + 1 new N1-D = 528)，chained `prompt-check: OK`；`pnpm --prefix tools/mission-driver/web run typecheck` clean；`pnpm --prefix tools/mission-driver/web run build` success (12.60s)；`pnpm --prefix tools/mission-driver run lint:prompts` OK；`node tools/mission-driver/src/mission-check.mjs missions/mission-driver-draft-robustness.json .` → `"valid": true`。test-count delta +1 (N1-D)。
  - Grep evidence (Closure Gates): `grep -n "stripAnsiControl" tools/mission-driver/src/main.js` → 4 hits (line 8 import + lines 146/172/197 call sites in extractBriefPath/extractBriefGate/parseDraftArtifact)；`grep -n "validateDraftDesc" tools/mission-driver/src/{draft-job,main,monitor}.js` → definition in draft-job.mjs:61, import+re-export in main.js:18+922, call in monitor.js:1174；`grep -rn "readKnowledgeDir" tools/mission-driver/src` → 0 hits；`grep -n "^import" tools/mission-driver/src/draft-job.mjs` → 仅 `node:fs`/`node:path`/`node:url`/`./spawner.mjs` (no cycle)。
  - Scope proof: 触及文件 = `src/draft-job.mjs` (+ validateDraftDesc def + JSDoc) + `src/main.js` (- validateDraftDesc def, + import + re-export, - readKnowledgeDir) + `src/monitor.js` (import 改自 ./main.js → ./draft-job.mjs) + `test/draft-path-consistency.test.js` (+ Case N1-D)。无 owner-doc 改动（design §4.1/§4.2.2/§4.3 已描述 plain-text + HTTP 400 契约；stripping 是 upstream cleaning detail，validation pattern extension 非 new contract）。
  - Closure: solo cold-replay pass (non-protected / non-high-risk per AGENTS.md Reviewer-Availability Fallback；源 audit 评级 N1 MEDIUM-LOW / N2 MEDIUM-LOW / N3 LOW / O3 LOW)。无 `> Work Item:` 标签（audit-sourced plan — WI1-WI5 已 done，本 plan 是 post-WI audit cleanup）；无 `> Source Audits:` 标签（front matter 用 `> Source:` 非 `> Source Audits:`），source audit 文件不 close（按 instruction "omit the step entirely if there is no `> Source Audits:` line"）。
