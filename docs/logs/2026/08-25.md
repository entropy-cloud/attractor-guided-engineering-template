# Development Log - 2026-08-25

### 2026-08-25

- **age-autonomy M2-WI41 Phase 1：closureScriptCheck 回执感知路由（D2 死锁修复）**（plan `docs/plans/age-autonomy/2026-08-25-0925-1-m2-wi41-closure-routing-deadlock.md`，bug `docs/bugs/2026-08-25-ledger-plan-closure-deadlock.md` D2）：
  - **落点裁定（引擎侧）**：修复落 `flow-loader.js` script step 判定 + `plan-check.mjs` 派生视图透传——收口路由是引擎自有流程语义，插件 pre-execute 面拦截不到引擎内部 script step 路由；「零引擎 diff 底线」约束门禁/守夜人新增逻辑沉淀位置，不豁免引擎自身缺陷修复。硬约束兑现：`git diff --stat tools/mission-driver/src/engine.js` 与 `flows/plan-execution.json` 均为空（fail→CLOSURE_AUDIT 既有路由即设计意图，只修判定条件）。
  - **fail 条件**：`format === "frontmatter" ∧ 计数域全勾 ∧ deriveCompleted 不成立` → fail（路由 CLOSURE_AUDIT）；derived.reasons 逐条（`no-audit-receipt` / `missing-pass:<key>` / `basis-hash-mismatch:<key>`）进 fail text 与 `SCRIPT_CHECK_DETAILS`。三态归一复用 `planLedgerState`/`deriveCompleted` 单一实现（01 §5.2 纪律），无第二套判定；legacy 与 format:none 逐字节不变（既有测试零修改通过）。
  - **派生视图透传**：`inspectPlan`/`analyzePlan` frontmatter 分支增 `derivedCompleted`/`completionReasons`/`verifyKeys`/`verifyKeysSource`（显式 frontmatter / mission 默认注入）纯增量字段；CLI JSON 既有字段语义不变。
  - 回归测试 `test/closure-routing.test.js`：①全勾无回执无 pass 行 fail（双 reason）②补回执仍 fail（missing-pass:test）③补 basisHash 匹配 pass 行 pass ④Closure Findings 返工重勾 → stale basisHash fail ⑤legacy 双 fixture 行为不变 ⑥0635-3 live 语料断言（路由决策镜像派生态——收口前后双向钉住，引擎恢复后不飘绿）。
- **age-autonomy M2-WI41 Phase 2：defaultVerifyKeys 引擎读面注入（Follow-up P2 清偿）**：
  - **默认键裁定 = `["test"]`**（`plan-check.mjs missionDefaultVerifyKeys` 单一实现）：`commands.test` 是 mission-check REQUIRED_COMMANDS 唯一强制命令键、全 mission 普适且语义即机械验证；全部非空 commands 键会把 BUILD_VERIFY 变全命令矩阵（否决）；mission 增显式 `defaultVerify` 配置面无真实需求背书（否决，0815-3 verify-keys 门禁后按需重开）。gate-check `--verify` 面保留自己的 DEFAULT_VERIFY_KEY_ORDER（不同消费面，另案裁定）。
  - **注入面**：flow-loader `_scanPlansByStatus`（activePlans/draftPlans 谓词）+ `closureScriptCheck` + plan-check CLI（`discoverOwningMission` 祖先走查自 gate-check.mjs 上移 mission-check.mjs 共享——owning mission 的 commands.test 注入，无 owning mission / 无 commands.test 退化为不注入，行为同现状）。效果：省略 `verify` 的全勾 + 回执齐全 plan 派生 completed、离开 activePlans（verify-省略版死锁消灭）。
  - 注入语义测试：省略 verify + 全勾 + 回执 + 匹配 pass 行 → 注入后 pass/derivedCompleted:true；无 commands.test → `no-verify-keys` 钉住；谓词面 activePlans/closedPlans 归属断言；CLI mission-default 注入端到端。
  - **验证（Phase 1+2 全链）**：`pnpm --prefix tools/mission-driver test` 876/0（基线 863 → +13 新测试，prompt-check OK）；`npm --prefix plugin/dsh test` 221/0（freshness 43 文件 content-equal——assets 经 build-bundle 重建）；`./verify-age.sh` L1+L2 GREEN；web typecheck/build 绿（web/src 无 diff，dist 漂移还原 committed 世代）；`lint:prompts` OK；engine.js/plan-execution.json 零 diff。

- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0815-3 M2-WI17+WI18+WI19+WI20 落地后复核）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 863 pass/0 fail（prompt-check OK）；`npm --prefix plugin/dsh test` 221 pass/0 fail（truth table 88；closure 25/25、freshness 43 文件 content-equal、smoke-import ok、tsc --noEmit 干净）；`gate-check --policy missions/autonomy.policy.yml` exit 0（11 gates：plan-structure observe + 三硬门四条 + 配套门禁六条 enforce）、`mission-check` exit 0。`--verify` 端到端（0815-3 plan，runId=2026-08-25-205251-mission-driver）：verify-keys gate allow、真实执行 commands.test exit 0、basisHash=483f44e2…08429 当次计算、CLI exit 0。web/src 无 diff——本地 build dist chunk 哈希漂移还原 committed 世代（承袭既定裁定）。`## Verification` pass 行已按过渡期写者裁定补写（0815-3）。

- **age-autonomy M2-WI19+WI20 Phase 3 收口：机械验证门禁、append-only 与真值表收口**（plan `docs/plans/age-autonomy/2026-08-25-0815-3-m2-supporting-gates.md` Phase 3，全 plan 三 Phase 收口）：
  - `law-rules.mjs` 新增 `verify-keys`（02 §5 命令来源纪律）：plan `verify` ⊆ mission `commands.*` 非空 key（未知 key / 空命令映射 deny + reason 列已知非空 key 集 +「Proof 文本永非命令源」）；ctx.commands 缺失 = fail-open 注记；verify 缺省 = 派生面默认解析注记；`verify: []` 空真通道归 WI44。DSH 适配层 ctx 增注 `commands` + `maxAuditRounds`（预算闸/枚举闸执法输入面；maxAuditRounds 经 `resolveMaxAuditRounds` policy 权威 / mission flow 回退解析；flows JSON 回退通道留引擎侧 M3）。
  - **commands runner** `src/verify-runner.mjs`（零引擎 diff 新模块）：`resolveVerifyPlan`（verify 缺省 → mission 默认 test/build/lint/typecheck 交集）+ `runVerifyCommands`（仅 spawn commands.*——Proof 文本结构性非命令源；cwd=projectRoot；每命令 10min 墙钟超时 SIGTERM→5s 宽限→SIGKILL、超时 exitCode=null 如实记录；输出截尾 4000 字符）产出每 key `{exitCode, passLine}`（01 §4.2 语法，basisHash 经 `computeBasisHash` 与完成公式合取同源绑定——返工重勾旧 pass 自然失效）。**执行面** `gate-check.mjs <plan.md> --verify`：owning mission 祖先走查（missions/*.json 上溯 + plansDir 包含判属 + `loadMission` 全 extends 解析）+ verify-keys gate + runner + 结构化 JSON 输出（不写 plan 文件——写盘归守夜人/BUILD_VERIFY，M3/WI26 接管）；BUILD_VERIFY prompt 步不动（Non-Goal）。build-bundle ALLOWED_MODULES 预登记 verify-runner（M3 守夜人消费 assets 副本，unreachable-allowed 直至接线）+ copy plan 改为复制全部 allowed 模块（维持 bundle-scaffold 镜像恒等，freshness 43 文件 content-equal）。
  - `law-rules.mjs` 新增 `record-append-only`（02 §4.8，直接 enforce——§6 例外授权同 0815-2 引用）：plan `## Draft Review Record`/`## Verification`/`## Closure` + roadmap `## Deep Audit Record` 四锚区 current/proposed 整区前缀保持（**prose 亦不可删改**——容忍策略是「未知行不参与语法匹配」非「可删」）；删行/改行/换序/整区删除 deny + reason 指出首个违例行；行尾空白与尾部空行 run 清理容忍（编辑器卫生）；proposed 非 frontmatter 账本 = 双读域外（先检查 proposed 格式再检查 current——legacy 写入直接域外）、无 currentFileState = 不可观测注记；双 policy gate 条目（plans + roadmap 域）。
  - **真值表收口 88 例**（64 基线 + 24：Phase 1 +8 / Phase 2 +7 / Phase 3 +9——verify-keys 2 / append-only 2 / runner 3（resolveVerifyPlan 矩阵、passLine 语法经 M1 扫描器回读、runVerifyCommands 真实 spawn 矩阵 exit 0/3/超时/空映射）/ corpus 2（新格式 0635-3+0815-1+0815-2 真实 11-gate policy 全 allow 无误杀 + legacy 全域外））。WI24 ≥30 基线超额（收口门验收归下批）。
  - **端到端**：`node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-25-0635-3-m1-corpus-migration-dual-read-guides-ci.md --verify` → owning mission = age-autonomy-implementation、verify=[test]、gate allow、真实执行 commands.test（863/0，21.2s）exit 0、合法 passLine（basisHash 当次计算）、CLI exit 0、plan 文件零写入。
  - **回写**：roadmap WI17/WI18/WI19/WI20 `[x]` + 证据指针（各 WI 残项注记：WI17 R1/R3 与 trigger 派发归 M3；WI18 过渡期 claim 写者供给残项给 WI25 追踪——prompt 写者可绕与现状持平，M3 守夜人唯一机器字段写者后消除；WI19 守夜人接管执行/自动写盘归 M3/WI26，runner M3 消费面已预登记；WI20 prose 保护口径 + corpus 回归）；`CONTEXT.md`「配套门禁 + 机械验证 runner」段。
  - **验证（全链）**：`pnpm --prefix tools/mission-driver test` 863/0（prompt-check OK）；`npm --prefix plugin/dsh test` 221/0（closure 25/25 + freshness 43 文件 content-equal + smoke-import + tsc --noEmit 干净）；`./verify-age.sh` L1+L2 GREEN；`gate-check --policy` exit 0（11 gates：1 observe + 10 enforce）；`mission-check` exit 0；web typecheck/build 绿（web/src 无 diff，dist 环境漂移还原 committed 世代）；`lint:prompts` OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff 不变量）。

- **age-autonomy M2-WI18 Phase 2：claim 合法性门禁**（plan `docs/plans/age-autonomy/2026-08-25-0815-3-m2-supporting-gates.md` Phase 2）：
  - **过渡期 claim 写者供给裁定成文**（Phase 2 Decision，承 0635-3 过渡期写者模式）：M2 过渡期 claim 由引擎流程派发面供给（EXECUTE prompt 指令写 claim，写者 = flow 派发会话）；执法信任基与现状同级——成文残险：M2 内 prompt 写者可绕（与今日现状持平，M2 内 claim 字段无生产写入者），M3 守夜人接管唯一机器字段写者后消除；规则按终态语义实现（engine/supervisor 角色白名单），M3 只换写者不改规则。**依赖注记落地**：0815-1 Explore 结论 = DSH 面 actor={id}、role 不可推断 → 写者 deny 面只在 role-bearing 调用面生效（engine/守夜人/测试），DSH id-only 面 = unverified-writer 注记不 deny。
  - `law-rules.mjs` 新增 `claim-validity`（02 §4.5）五面：① 写者面——claim/claim-expires 引入/变更 actor.role ∈ {engine, supervisor}（executor/drafter/reviewer/auditor deny + 指路；id-only/无 actor 注记）+ 写入 TTL 合法形状（非 ISO-8601 / 写入即过期 deny）；② 持有者面——勾选写入（计数域 checked 增加）× 活 claim：`actor.id == holderSessionId（尾部锚定）∧ 未过期`才 allow，结构面退化「claim 存在 ∧ 未过期」注记不冒充；③ 单活——claim 型 action（payload = attempt token / `{claim}`）对活异 claim deny（「一次写入产生双活」转移面；parse 面 = frontmatter 重复键拒绝，两层边界真值表注明）+ 畸形 payload deny；④ 清除转移——全勾无回执（入 awaitingClosure）携 claim deny（0815-2 plan-completed ② 同约束的另侧声明）；⑤ active 外携 claim deny（transition 面；静态形状面 = M1 格校验经 plan-structure observe）；claim 清除 = holder ∨ dispatcher，第三 session 带 id 即 deny（session 比对 DSH 面可判定）。
  - 真实 policy 注册 `claim-taken` gate（match `{{plansDir}}/**/*.md`，enforce）；引擎侧真实实例断言同步；assets 重建。真值表 +7 例至 **79**（写者六角色矩阵 / TTL 面 / holder 匹配-错位-结构退化 / 过期边界 < = > 时钟注入三点 / claim action 单活五列 / ④⑤双 deny 面 + draft 落 claim / 清除四例 + 惰性 + legacy 域外；twoItemTickPair fixture 保证单勾 ≠ 全勾过渡，避免 ② 与 ④ 面互扰）。执行校准两则：claim token 字符集补 `_`（session id 合法字符）；id-only 第三者清 claim = deny（session 比对可判定）而非注记——注记面收敛到「无 actor」姿态。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 79/0；`pnpm --prefix tools/mission-driver test` 863/0（prompt-check OK）；`npm --prefix plugin/dsh test` 212 pass/0（closure 25/25 + freshness 42 文件 content-equal + smoke-import + tsc --noEmit 干净）。

- **age-autonomy M2-WI17 Phase 1：nothing-claim 兜底 + audit-rounds 预算闸**（plan `docs/plans/age-autonomy/2026-08-25-0815-3-m2-supporting-gates.md` Phase 1）：
  - **四一般门禁姿态裁定成文**（Phase 1 Decision）：nothing-claim-guard / audit-rounds-overflow / claim-validity / verify-keys 四规则注册即 `mode: enforce`——deny 面均为窄域可判定事实（无 matcher 模糊性，02 §6 observe 爬坡针对的误杀风险不构成）；observe 爬坡的日志消费回路 M3 才有；存量语料兼容性由 corpus 按文件类钉住回归网兜底。备选（严格 observe 至 M3）否决：observe 期间公式输入不可信会侵蚀 0815-2 三硬门 allow 判定根基。残险（手写 terminal-claim.json / 手改 verify 误杀）mitigation = deny reason 指向合法路径 + corpus 回归；真实误杀出现时按 02 §6 纪律回调该规则为 observe 并记录。裁定与回滚触发同步成文于 `missions/autonomy.policy.yml` gates 注释。
  - `law-rules.mjs` 新增 `nothing-claim-guard`（02 §4.4 动作面规则）：拦截目标 = `action:terminal-claim`（`_tmp/<runDir>/terminal-claim.json` 动作记录，非账本文件）；内容 `kind: nothing-to-draft` 时经注入谓词判定——`draftPlans()>0 ∨ activePlans()>0` → deny + reason 列出可见未完成 plan（指合法路径）；否则 allow 且规则结果携带 **Deep Audit 触发信号**（`trigger: {dispatch: deep-audit, when: terminal-claim=nothing-to-draft ∧ draftPlans()==0 ∧ activePlans()==0}`，M3/WI26 守夜人消费，数据形状真值表钉住）；kind 词法面（非 nothing-to-draft）域外放行；非 JSON 动作记录 = 可判定 malformed → deny；ctx.plans 未注入（非守夜人面）= 谓词不可观测 → allow + 注记不冒充（02 §2）。marker 通道不动（诊断/日志面，M5 评估物理删除）。
  - `law-rules.mjs` 新增 `audit-rounds-overflow`（02 §4.6 预算闸）：拦截目标 = roadmap `## Deep Audit Record` **新增 dispatch audit 行**的写入（current/proposed 扫描差集）；roadmap frontmatter `audit-rounds ≥ maxAuditRounds` → deny 新审计轮次（R1 终态收口归 M3/WI27，本门禁只 deny）；预算读 current 态 frontmatter（派发写同写自增，01 §3.1）；既有行不动 = 预算面惰性；无 currentFileState = 新增不可观测 → allow + 注记；legacy roadmap 双读域外。**maxAuditRounds 双源解析**：`law-policy.mjs` 新增 `resolveMaxAuditRounds(policy, missionConfig)`（policy limits 权威 / mission `flow.maxAuditRounds` 回退 / 双缺 = 0——0815-1 裁定的消费面切换在本 plan 兑现；规则侧只读 `ctx.maxAuditRounds`，规则不 import law-policy（import 环边界））。max=0（无审计概念）语义：0 ≥ 0 → 任何新 dispatch 行 deny（镜像引擎 max=0 不进审计轮的现状语义），成文于测试。
  - 真实 policy 注册两 gate（enforce）：`nothing-claim`（match `action:terminal-claim`）+ `meter-guard`（match `{{roadmapPath}}`）；引擎侧 `law-policy.test.js` 真实实例断言同步（per-phase 增量注册注释）。assets 重建（law-rules/law-policy 副本物化，freshness content-equal）。
  - 真值表 +8 例（至 **72**）：terminal-claim ×（draftPlans/activePlans 0-非0，含 only-draft / only-active 列）× kind 词法 ×（非 JSON / records 缺失）；**真实 plansDir fixture**（tmp 目录落盘 draft-one / active-one / done-one 三文件读入 records——谓词注入面经磁盘文件验证非 mock，done-one 断言派生 completed 退出 activePlans）；触发信号数据形状（getRule 直连规则层断言）；audit-rounds 边界（< max 放行 / = max deny / 未配置 0≥0 deny）×（新增行 / 既有行 / accepted 落地 / 无 currentFileState / 无 DAR / legacy）；`resolveMaxAuditRounds` 优先级矩阵（policy > mission > 0，非法形状不取胜）。
  - 验证：`node --test plugin/dsh/test/law-truth-table.test.mjs` 72/0；`pnpm --prefix tools/mission-driver test` 863/0（prompt-check OK）；`npm --prefix plugin/dsh test` 全绿（closure 25/25 + freshness 42 文件 content-equal + smoke-import + tsc --noEmit 干净）；web typecheck/build 绿（web/src 无 diff，dist 环境漂移还原 committed 世代）；`lint:prompts` OK。

- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0815-2 M2-WI14+WI15+WI16 落地后复核）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 863 pass/0 fail（prompt-check OK）；`node --test plugin/dsh/test/*.test.mjs` 197 pass/0 fail；`gate-check --policy missions/autonomy.policy.yml` exit 0（5 gates：plan-structure observe + 三硬门四条 enforce）、`mission-check` exit 0；plugin assets 经 `build-bundle.mjs` 复核 fresh（无再生成 diff）。web/src 无 diff——本地 build dist chunk 哈希漂移还原 committed 世代（承袭既定裁定）。`## Verification` pass 行已按过渡期写者裁定补写（basisHash=091bf73e… 当次计算）。

- **age-autonomy M2-WI16 Phase 3 + 三硬门收口：plan-completed 规则、enforce 切换与回写**（plan `docs/plans/age-autonomy/2026-08-25-0815-2-m2-three-hard-gates.md` Phase 3，全 plan 三 Phase 收口）：
  - `law-rules.mjs` 新增 `plan-completed`（02 §4.3，整文件 proposed content 粒度）：**全勾过渡三岔**——① 回执齐（Closure 配对 > 0）→ `deriveCompleted` 五合取公式校验（stale basisHash → deny 指向重验证，返工重勾的旧 pass 复用被 basisHash 绑定天然拦截，评审遗留非阻塞项 1 的 stale basisHash 用例已钉）；② 无回执 → 仅有效 claim 持有者（actor.id == holderSessionId ∧ 未过期，时钟 ctx.now 可注入）放行入派生 `awaitingClosure`，且该放行写入必须同写清除 claim（01 §4.4「claim 在 awaitingClosure 前必清」，无/过期/错主/残留四种 deny 面各有指路 reason）；已在 awaitingClosure（current 已全勾无回执）的维护写（pass 行/dispatch 行落地）放行——不造 WI41 式死锁；③ 审计拒绝 = `## Closure Findings` 追加未勾项自然脱离全勾（无状态位）。**终态冻结**：current 派生 completed 或 status ∈ 终态 → basis 域（frontmatter + Phase + Closure Findings = 全部 status/checkbox/机器字段）不可变（`computeBasisHash` 比对），append-only 区追加放行，防「已 accepted 旧回执被新未勾项复用」（WI21 后继只消费）。
  - **enforce 切换**（Phase 3 Decision 成文）：三硬门四 gate 条目（closure-audit-binding / roadmap-audit-binding / writer-identity / plan-completed）注册 `mode: enforce`，授权 = 02 §6 例外条款（P0/M1 收口：WI1–WI11 全 tick + 807 tests）——不走 observe 爬坡；plan-structure 维持 observe（收紧开关归 WI21）；gate 顺序 = binding 先于 formula（deny reason 优先级，评审遗留非阻塞项 3）。**prompt 最小增量**：closure-audit/multi-audit/open-audit 三 prompt 的 dispatch 行指令加 ` models={exec:…,aud:…}` lineage 后缀写入（本 plan 自身收口回执 = 仓库首个生产写入者）；`lint:prompts` 绿。
  - **corpus 按文件类钉住**：`gate-check 0635-3` exit 0 且 plan-completed observation 断言 `awaitingClosure (legal derived middle state, 01 §5.2)`；legacy 0635-1/2 五 gate 全部域外跳过 exit 0（无误杀）；0815-1 同为 awaitingClosure 语料入测试；真实 enforce policy 端到端：构造全勾越权写入（无回执无 claim）被 deny。真值表累计 **64 例**（WI24 ≥30 门的本批推进量；WI24 收口门本身不勾，归下批）。
  - **回写**：roadmap WI14/WI15/WI16 `[x]` + 证据指针（WI14 显式残项注记：requireDistinctModel 运行时派发强制归 M3/WI26 守夜人派发点，接口 = dispatch 行 models= 数据 + 检查函数复用；WI16 终态冻结 WI21 消费注记）；`tools/mission-driver/CONTEXT.md` 三硬门段。
  - **验证（全链）**：`pnpm --prefix tools/mission-driver test` 863/0、`npm --prefix plugin/dsh test` 197/0（closure 25/25 + freshness 42 文件 content-equal + smoke-import + tsc --noEmit 干净）、`./verify-age.sh` L1+L2 GREEN、`gate-check --policy missions/autonomy.policy.yml` exit 0（5 gates enforce 注册校验过）、`mission-check` exit 0、web typecheck/build 绿（web/src 无 diff，dist 环境漂移还原 committed 世代）、`lint:prompts` OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff 不变量成立）。

- **age-autonomy M2-WI15 Phase 2：状态转移写者身份规则**（plan `docs/plans/age-autonomy/2026-08-25-0815-2-m2-three-hard-gates.md` Phase 2）：
  - `law-rules.mjs` 新增 `writer-identity` 规则（01 §5.1 逐边表为契约，`LEGAL_TRANSITIONS` 集合钉住合法边——非法边/终态复活一律 deny + 指路 reason）：① `draft→active` = 写者 actor.id 必须命中 DRR 内配对回执（dispatch review + 同 id 结论行）的 reviewerSessionId，无配对回执 deny（防 drafter 自升 active）；② `held→active` = 同一次写入必须重置 `failures: 0` ∧ 移除 `hold`（缺失 → deny malformed-transition）+ 身份面（新评审 reviewer 命中可证；守夜人 unlock 无回执语法 → unverified-writer 注记不冒充，`mdcontrol.unlock` 路由归 M3）；③ 终态 disposition（cancelled/superseded/deferred）= 已登记 reviewer/supervisor 可证，其余身份依赖 → 注记不 deny；④ 执行者永禁写 `status`（role=executor 直接 deny + actor.id 命中 claim holderSessionId（尾部锚定提取，连字符安全）= 注册执行者 deny，无例外边）。
  - **评审租约**：current DRR 存在未闭环 dispatch review 且本次写入未落同 id 结论 → 除该 reviewerSessionId / supervisor / engine 外 deny（第三者带 actor.id 即 deny——session id 比对在 DSH 面可判定）；结论行同写落地 = 租约结束放行（reviewer 翻 active 的正常路径）。
  - **结构面退化纪律**（02 §2）：无 currentFileState（单文件/CI 姿态）→ 转移与租约不可观测 → allow + 注记不冒充；无 actor → 租约第三者不可排除 → 注记。transitionPlan fixture 矩阵 11 例全绿（转移边 × actor 角色 × 证据在 current/proposed 三维）。
  - 验证：`plugin/dsh/test/law-truth-table.test.mjs` 52 例全绿（+11）；`pnpm --prefix tools/mission-driver test` 863/0、`npm --prefix plugin/dsh test` 全绿（closure 25/25 + freshness 42 文件 content-equal）。

- **age-autonomy M2-WI14 Phase 1：审计回执绑定双面规则 + model lineage**（plan `docs/plans/age-autonomy/2026-08-25-0815-2-m2-three-hard-gates.md` Phase 1）：
  - `ledger-sections.mjs` dispatch 行 `models=` 后缀显式解析（02 §4.1 G4 机制化）：` models={exec:<名>,aud:<名>}` 尾部可选字段——无后缀行为不变；合法后缀解析进 dispatch 记录（`models: {exec, aud}`，供绑定校验与驳回率统计消费）；` models=` 起头但不完整解析（半配对/无花括号/空槽）= malformed-dispatch 结构 error（严格语法行不容忍为 prose）。引擎测试 +3（合法/畸形/半配对）。
  - 新增 `tools/mission-driver/src/law-rules.mjs`（0815-1 放置裁定沿伸：引擎侧零 npm 规则模块，经 law-policy.mjs side-effect import 注册进内核 registry——config.js fail-fast / gate-check / DSH host adapter 三消费面同源可达，避免 law-core↔law-rules 循环 import）：`closure-audit-binding`（plan `## Closure` 面）+ `roadmap-audit-binding`（roadmap `## Deep Audit Record` 面，同构 + findings=none|items required）——拦截目标 = 触碰回执区或全勾过渡；结构面（全部署面）= 区内行语法（经 M1 扫描器 error 按行号过滤进区）∧ 同 id 配对（accepted 无同 id dispatch = 伪造回执 deny；dispatch 无 accepted = 派生中间态永不 deny）∧ plan 面 findings 词法禁用；写者面（actor.id 可得）= 新增 accepted 行写者 == dispatch 行 auditorSessionId（错位 deny）+ 新增 dispatch 行写者角色 ∈ {engine, supervisor}（role 可得时；id-only = 过渡期 flow 派发步注记不 deny）；无 actor = 结构子集 allow + 「不声称写者验证」reason；legacy 无 frontmatter 文件域外放行（WI13 误杀教训）。build-bundle ALLOWED_MODULES + bundle-scaffold 镜像双侧登记（closure 25/25），assets 重建。
  - `requireDistinctModel` 静态可满足性检查（02 §4.9，check-policy 面）：`law-policy.mjs` 新增 `checkDistinctModelSatisfiability`（dispatch 映射 execute→executor vs closure-audit/deep-audit→auditor 的 `{provider, model}` 对相等 → 校验错误，指明两条合法路径）+ agent def 新字段 `downgrade: single-model`（显式单模型部署降级通道，不静默；无 requireDistinctModel: true 时声明 = 校验错误）+ 接入 `validatePolicy`（gate-check --policy 与 config.js fail-fast 同面生效）。运行时派发绑定 = M3/WI26 接口注记成文（接口 = dispatch 行 models= 数据 + 本函数复用）。真实 policy `missions/autonomy.policy.yml`：auditor 显式声明 `downgrade: single-model`（本仓库单模型部署诚实口径）+ 注册两 binding gate（observe——enforce 切换授权归 Phase 3 Decision）。
  - 验证：`plugin/dsh/test/law-truth-table.test.mjs` 41 例全绿（25 基线 + 16 新：gate1 双面 10 + roadmap 面 4 + requireDistinctModel 1 + 引擎侧 law-policy +3 / ledger-sections +2）；`pnpm --prefix tools/mission-driver test` 全绿、`npm --prefix plugin/dsh test` 全绿（closure 25/25 + freshness 42 文件 content-equal + smoke-import + tsc）；`gate-check --policy missions/autonomy.policy.yml` exit 0；web typecheck/build 绿（web/src 无 diff，dist 环境漂移还原 committed 世代）；`lint:prompts` OK。

- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0815-1 M2-WI12+WI13 落地后复核）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 858 pass/0 fail（prompt-check OK）；`npm --prefix plugin/dsh test` 158 pass/0 fail（closure 24/24 + freshness 41 文件 content-equal + smoke-import ok + tsc --noEmit 干净）；`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0（autonomyPolicy 引用 live 复核有效）。web/src 无 diff——本地 build 产生的 dist chunk 哈希漂移已还原 committed 世代（承袭 worktree 环境性漂移既定裁定）。`## Verification` pass 行已按过渡期写者裁定补写（basisHash 当次计算）。

- **age-autonomy M2-WI12+WI13 law seam 落地**（plan `docs/plans/age-autonomy/2026-08-25-0815-1-m2-law-seam-policy-schema.md` 三 Phase 全执行）：
  - **Phase 1 law 内核 seam**：新增 `tools/mission-driver/src/law-core.mjs`——proposedAction 契约（02 §2 七 type 枚举；非法输入 deny malformed 不 crash；baseHash CAS 尽力比对 + `stale-write`/`cas-unverified` 注记，deny 路由留 Q4/M3；actor 缺省或 id-only（DSH 面 Explore 结论：`exec.agent.id: SessionId` 可得、role 不可推断）= 结构子集 `unverified-writer` 姿态）+ 规则注册表 + `evaluateGates`（policy gates 按 match 域分派——`action:<type>` / `{{plansDir}}`/`{{roadmapPath}}` glob（`**/` 匹配零或多层）；observe/enforce 姿态（缺省 observe，02 §6 机械化）；per-rule fail-open（`rule-error` observation 不阻断其他 gate））+ 种子规则 `plan-structure`（scanPlanLedger + validatePlanFrontmatter(agentNames)——「写入后仍是合法 plan 账本」；**执行校准**：无 frontmatter 块 = 域外放行（legacy 双读过渡 + WI13 误杀教训），有 frontmatter 块但语法坏 = deny）；`ledger-frontmatter.mjs` `validatePlanFrontmatter` 增第二参 opts.agentNames（additive，M1 行为不变）。
  - **Phase 2 policy schema + mission 注入**：新增 `tools/mission-driver/src/law-policy.mjs`——受限 YAML 子集解析（镜像 ledger-frontmatter 硬边界：块映射/块序列/单行 flow（含 flow-in-flow 深度 3）；锚点/别名/块标量/多行标量/tab/块深 >4 全拒并指路）+ schema 校验器（version/limits（policy 权威、mission config 回退，maxAuditRounds=3 与 flows/mission-driver.json 现值一致）/gates（id 唯一、match 形态、rule 命中注册表、mode 枚举）/triggers（when 受限谓词语法：14 谓词 atom/cmp/call 三形态 + and/or/not/括号，出口 dispatch|action|terminal 三选一值域钉住）/agents（mode pooled|fresh、pooled 必 poolKey、fixedPrefix `{kind: text|file|dir, ref, maxFileBytes?}` dir 必带上限、model `{provider, model, reasoningEffort?}`、requireDistinctModel）/dispatch（值必命中 agents——agents 段缺失时同样拒绝）+ `parseTriggerWhen`/`resolvePolicyPlaceholders`（单 braces `{projectRoot}` 不动）；真实实例 `missions/autonomy.policy.yml`（plan-structure observe + 02 §3 全部 7 trigger 机器语法化 + 4 agents + 6 dispatch）；mission-check `autonomyPolicy` 存在性家族；config.js `autonomyPolicyPath`/`autonomyPolicy`（eager load+validate fail-fast + `_agentNames`）——`commands.gates` 与 autonomyPolicy 两处悬空引用消除。
  - **Phase 3 CLI + 宿主适配 + 真值表奠基**：新增 `src/gate-check.mjs`（`--policy` 结构化校验 exit 0/1；`<plan.md>` 单文件结构面（结构规则直连，actor 缺省）；bare → usage exit 1）；新增 `plugin/dsh/src/law/host-adapter.ts`（祖先走查发现 mission policy + per-ancestor 缓存、write/edit/str_replace_editor 提取、actor=exec.agent.id、内核 evaluate、观察面 = `<projectRoot>/_tmp/law-observations.jsonl` JSONL + logger 结构行；enforce-deny 返回路径在位；整面 fail-open try/catch）+ service.ts 挂载（独立监听器/disposer，与 plan-status-gate 并存）；`plugin/dsh/test/law-truth-table.test.mjs` 25 例（经 assets 副本 import 内核 = 通道即证明；**宿主面证据 = 真实 cordis `Context.waterfall('tools/pre-execute')`** + 真实磁盘 policy + 观察 JSONL 落盘，非 ctx mock；fail-open 两层各自钉住）。
  - **接线与不变量**：build-bundle ALLOWED_MODULES += law-core/law-policy（可达链 config.js→law-policy→law-core，closure 24/24、unreachable-allowed 清零、assets 副本物化 41 文件；gate-check.mjs 不入 ALLOWED——CLI 属 main.js 家族）+ bundle-scaffold 镜像同步；`git diff --stat tools/mission-driver/src/engine.js` 为空；零 npm 依赖不变量成立。文档：CONTEXT.md law 内核段、02-rule-law §6 部署面表 gate-check.mjs/DSH 适配层实名 + Changelog 首条、roadmap WI12/WI13 `[x]` + 证据指针。
  - **验证**：`pnpm --prefix tools/mission-driver test` 857/0（816 基线 + 41 新例）；`npm --prefix plugin/dsh test` 158/0（133 基线 + 25）；`./verify-age.sh` L1+L2 GREEN；mission-check / gate-check --policy / gate-check 存量 plan 三命令 exit 0；web typecheck/build 绿（web/src 无 diff，worktree dist 环境漂移已还原 committed 世代）。roadmap 计数时刻：grep `^- \[ \]` = 31（44 total − 13 checked，`scanRoadmapLedger` 0 error——M1 11 + WI12/WI13 本批）。

- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，worktree 迁入后首验）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 816 pass/0 fail（prompt-check OK）。无代码变更，仅复核记录。备注：worktree 本地 node_modules 重建 dist 产物 chunk 哈希与 committed 世代不一致（web/src 无 diff 的纯环境差异，非源码漂移），已还原 committed dist 保持净树；dist 权威重建以主树/CI 钉住环境为准。
- **共享引擎 worktree 隔离裁定 + age-autonomy 迁入 worktree**（human 发起，起因 = D1 陈旧引擎死锁暴露活树共享无版本隔离的结构性风险；裁定全文 `docs/discussions/2026-08-25-2030-shared-engine-worktree-isolation.md`；**模型经 human 复核修正一轮**——初版曾新建 `feature/age-autonomy` 分支并把主树钉在 dsh，最终裁定：**主工作树钉 `main`**（消费者 `MISSION_DRIVER_HOME` 零改动即获稳定版），age-autonomy **复用现有 `feature/dsh-plugin` 分支**不另开新支（dsh roadmap 已全量收口，该分支即事实开发主干），临时分支已删除、两个工件 commit（`78ac8cb` M2 九份 active plan + D1/D2 bug note + roadmap 回写、`b17e2e1` 裁定文档 + log）快进并入 dsh；worktree 落位 `../age-worktrees/age-autonomy`，引擎 816/816 + 插件 133/133 复验绿。操作纪律成文：EXEC_PLANS 触碰引擎代码后下一轮前重启引擎。Follow-up 两项 P2 立案（消费侧版本钉住、谓词绕缓存直读磁盘）；main 落后开发分支 46 提交为已知接受态，合并时点 human 另行裁定）。
- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0635-3 收口 + M1 milestone 关闭后复核）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（web/dist 无 diff）、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 807 pass/0 fail（prompt-check OK）。无代码变更，仅复核记录。
- **age-autonomy M1 收口（M1-WI10+WI11 Verification Gate 全绿 + roadmap 回写）**（plan `docs/plans/age-autonomy/2026-08-25-0635-3-m1-corpus-migration-dual-read-guides-ci.md` 六 Phase 全执行完毕）：
  - **WI10 CI 前置**：全语料双读结构冒烟常驻 L1 链（`test/ledger-corpus.test.js`：docs/plans/**（55+ 文件）+ docs/backlog/*roadmap*（8 文件）逐文件双读 + 结构校验 0 error——含两个已切换 guide）；`./verify-age.sh` L1+L2 GREEN（prompt 改写后 assets 重建，closure 22/22、freshness 39 文件 content-equal、smoke-import ok）；`mission-check` exit 0。
  - **WI11 四条 gate 真实绿**（输出已记录于 plan `## Closure`）：① `plan-check --strict` 00-guide → exit 0（format none / 0 unchecked）；② `pnpm --prefix tools/mission-driver test` → 807/0（prompt-check OK）；③ ledger 测试族聚合 `node --test test/ledger-*.test.js` → **147 例**/0 fail ≥12（六面：解析器/字段集=frontmatter 22、状态格/完成派生=derivation 21、append-only 结构/计数域=sections 15、双读切换=dualread 23、语料冒烟=corpus 66；路径裁定=按意图钉住为族聚合命令）；④ roadmap grep gate 时刻 **35**（= 40 − 5 已收口，与 Decision 5 钉住值一致）→ M1 回写后稳态 **29**（M2–M5），`^- \[x\]` = 11——双时刻期望值全中，无计数域污染。
  - **roadmap 回写**：M1 WI4/WI7/WI8/WI9/WI10/WI11 六项 `[ ]` → `[x]`——M1 全 11 项 done，与三份 plan（0635-1：WI1/WI2；0635-2：WI3/WI5/WI6；0635-3：WI4/WI7–WI11）closure 状态一致；roadmap「阶段依赖」M1-WI11 绿 → M2 enforce 前置解锁。
  - **收口状态**：0635-3 计数域 43/43 全勾、结构 0 error，`deriveCompleted` = false，reasons = `missing-pass:test` + `no-audit-receipt`——按 Phase 1 Decision 2 过渡期写者裁定，`## Verification` pass 行（BUILD_VERIFY）与 `## Closure` dispatch/accepted 回执（CLOSURE_AUDIT）由后续引擎步骤补写后派生 completed；frontmatter `status: active` 保持（completed 永不可写）。回滚点 = 「ledger corpus migration + dual-read wiring」commit（git revert + `MISSION_DRIVER_LEDGER=legacy`）。
- **age-autonomy M1-WI8+WI9 guides 收口 + AGENTS.md 职责行收窄**（plan 0635-3 Phase 5）：
  - `docs/plans/00-plan-authoring-and-execution-guide.md` 全量切换 ledger 格式（changelog 事件成文）：模板替换为 frontmatter + `## Phase <n>` 骨架（Verification/Closure 空区起步、禁 Closure Gates/per-Phase Status）；rules 11（文本一致性）/12（status-vs-checkbox 一致性）退役、替换为完成派生公式引用（新 rule 11）；原 rule 13 重述为回执强制的独立评审/收口（新 rule 12——dispatch + 同 id 结论行，自收口结构性不可能）；淘汰项映射表（Plan Status/Review Hold/Mission/Work Item/Last Reviewed/Audit/per-Phase Status/Closure Gates → 各自去向）成文；0635-1/2 两个 additive 小节转正合并为正文格式段。live 验证：`plan-check --strict` exit 0；字段表与 `PLAN_FRONTMATTER_FIELDS` 常量逐字段同序一致（node 对照 true）；模板/示例围栏经 `scanPlanLedger` 0 error（body 示例 2 phases、Template 1 phase 6 项）。
  - `docs/backlog/00-roadmap-authoring-guide.md` 全量切换：frontmatter `audit-rounds` + `## Deep Audit Record`（findings=none|items）转正为正文格式段；纯 checkbox Work Item 纪律（唯一 grep 通道）；Status Values 表退役成文（done=勾选、ready 语义去向 plan 侧 frontmatter status、❌/✅ 图标禁令入 Anti-Patterns）；changelog 事件。Deep Audit Record 围栏示例经 `scanRoadmapLedger` 0 error（dispatch/accepted 配对 1）。
  - AGENTS.md `docs/audits/` 职责三站点收窄（diff 仅 3 行：Operating Rule 10 不再把 audits 作为晋升落点、Documentation Ownership 段收窄为方法论指南 + 罕见人工跨 mission 记录 + 内联指向（`## Draft Review Record`/`## Closure`/`## Deep Audit Record`）、Optional Workflow Layers 段同步）；外部存量审计文件保留为归档不迁移；`> Source Audits`/`> Audit:` 头部线删除映射已含于 codemod（Phase 2）；生产面内联化（Phase 4 multi/open-audit）+ 消费通道 legacy-only 裁定（Phase 1 D6-4）= WI8 全链。
  - `mission-check` valid（exit 0）；`node --test ledger-corpus` 66/0（两 guide 仍零污染）。
- **age-autonomy M1-WI7 prompt 状态指令对齐**（plan 0635-3 Phase 4，8 个 prompt 按 Phase 1 Decision 6 五条裁定改写）：① 状态操作双模式——execute.md（格式探测引导 + 勾选即完成/ledger 禁写 completed + 4a 双模式 + roadmap tick 双模式 + 4c Source-Audits 步整体删除）、plan-review.md（ledger 提升写 `status: active` + `## Draft Review Record` dispatch/日期迭代结论回执两行同 id——过渡期评审回执写者；hold 双模式 held+hold / Review Hold）、closure-audit.md（FAIL 修复路径与 PASS 语义路径均双模式；五点一致性条目替换为完成派生引用；approved 时写 `## Closure` dispatch/accepted 回执两行同 id——过渡期审计回执写者）、draft-from-roadmap.md / draft-from-audit.md（一律产出新格式骨架：frontmatter status/mission/work-item/group/`verify: [test]` + `## Phase <n>` 区块，禁 `> Plan Status:`/per-Phase Status/Closure Gates；draft-from-audit 双通道——legacy 归档文件照旧消费闭合 + 内联深审发现走 roadmap 未勾 WI）；② build-verify.md 增过渡期 `## Verification` pass 行写入职责（basisHash 经共享库 `computeBasisHash` 的 node 单行命令成文、pass 行最后写不入 hash 域、仅写实际跑绿的 verify key、禁触 status 与 Closure）+ commit 模板 ✅ 图标去除；③ multi-audit.md / open-audit.md 审计生产面内联化——结论写 roadmap `## Deep Audit Record`（dispatch/accepted 同 id + `findings=none|items`）+ 自增 frontmatter `audit-rounds` + P0/P1 发现落为 milestone 块内未勾 WI / P2 落 Follow-up Backlog，不再新造外部 `docs/audits/` 文件（legacy 归档保留可读）；④ `lint:prompts` 绿（rg 扫残留命中全为显式 legacy 回退/双模式语境）；`pnpm --prefix tools/mission-driver test` 807/0、`npm --prefix plugin/dsh test` 133/0。
- **age-autonomy M1-WI4+WI7 语料迁移落盘 + 双读接线**（plan 0635-3 Phase 2+3，同一 commit 落盘满足「迁移后无旧解析器失明窗口」约束）：
  - codemod `plugin/dsh/scripts/migrate-ledger.mjs`（`--dry-run` / `--scope plans|roadmaps` / `MIGRATE_LEDGER_ROOT` 沙箱根覆盖；幂等复跑零 diff 已证）：plan 面迁移非终态 plan——今日仅 0635-3 自身（`> Plan Status: active` → frontmatter `status: active` + mission/work-item/group/`verify: [test]`；Last Reviewed/Audit 行删除；Source/Related 保留为 prose；`## Execution Plan` 包装移除；`### Phase N - <name>` → `## Phase N — <name>`；per-Phase Status 行删除；Closure Gates 消解——9 项中 8 项派生类删除、「relevant docs are aligned」可执行项并入 Phase 6 尾部）；roadmap 面迁移全部 6 份（missions/*.json roadmapPath 发现口径）：frontmatter `audit-rounds: 0` + 纯 checkbox 归一（age-autonomy checkbox+尾缀→剥尾缀；dsh-plugin/actionable-fixes/draft-robustness/step-audit bullet+尾缀→转 checkbox（done⇒[x]，todo/ready/planned⇒[ ]，`: status` 剥除、尾注/plan 引用保留）；demo 表格行→按 M 前缀分组转 `### M<n> — <title>` checkbox 块（owner/deps/reuse 转尾注括号）；actionable-fixes 无 M 块→合成 `### M1 — Work Items` 包裹）。52 份存量 completed plan + 0635-1/2 保持旧格式（terminal 跳过），由 legacy 通道永远识别为 closed。迁移产物经 0635-2 结构校验器全绿（age-autonomy 40/5/35 与 grep 通道逐项一致；0635-3 计数域 43 项 0 error）。
  - 双读接线：新增 `tools/mission-driver/src/ledger-dualread.mjs`（`PLAN_STATUS_RE` 单一定义点——charset 收窄版（并集 plan-check 字符集 + flow-loader bold/case 容错），prose `> Status: additive (…)` 不再误判；`ledgerReadMode()` 读 `MISSION_DRIVER_LEDGER=auto|legacy|frontmatter`（非法值 fail-loud）；`readPlanStatus`/`planLedgerState`（frontmatter 优先 / 旧状态行回退 / guide 类 none；legacy completed ⇒ closed 恒真））。四消费面切换：plan-check.mjs（新格式走计数域扫描、legacy 维持全文计数、guide `--strict` exit 0 = WI11 gate 1 提前达成）、flow-loader.js `_scanPlansByStatus`（共享谓词；自持 PLAN_STATUS_RE 删除；AUDIT_STATUS_RE 按 Phase 1 D6-4 裁定保留为 legacy-only 通道并注释）、roadmap-check.mjs（checkbox 优先 / 尾缀+表格回退双读；`roadmapAllDone` 对新旧格式均语义正确）、monitor.js `handleListPlans`（第四面，切 `planLedgerState`）。plugin/dsh `plan-status-gate.ts` 的 `PLAN_STATUS_RE` import 改自 assets 内 `ledger-dualread.mjs`（引擎 src 中 rg `PLAN_STATUS_RE|AUDIT_STATUS_RE` 仅共享库 + flow-loader 裁定例外命中）；build-bundle ALLOWED_MODULES + bundle-scaffold 测试镜像登记 `ledger-dualread.mjs`，三 ledger 模块经 plan-check/flow-loader/roadmap-check importer 进入闭包（22 modules reachable，unreachable-allowed 清零），assets 重建 39 文件 content-equal。
  - 新测试：`test/ledger-dualread.test.js`（23 例：auto 三分支 frontmatter/legacy/none、legacy 强制回退、frontmatter 收紧拒绝旧格式、legacy completed⇒closed、env 非法值 fail-loud、roadmap checkbox 优先/表格回退/双端 roadmapAllDone）+ `test/ledger-corpus.test.js`（66 例：docs/plans/** + docs/backlog/*roadmap* 全语料双读 + 结构校验 0 error 常驻回归网 = WI10 CI 前置面）。
  - 验证：`pnpm --prefix tools/mission-driver test` 807 pass/0 fail（基线 718 + 23 + 66）；`npm --prefix plugin/dsh test` 133 pass/0 fail；`node tools/mission-driver/src/plan-check.mjs docs/plans/00-plan-authoring-and-execution-guide.md --strict` → exit 0；live 对账：0635-1/2 → "completed"（closed）、0635-3 → "active"、6 roadmap scanRoadmapLedger 全 0 error。回滚点 = 本 commit（git revert + `MISSION_DRIVER_LEDGER=legacy`）。
- **age-autonomy M1-WI4+WI7–WI11 执行 Phase 1 决策确认**（`docs/plans/age-autonomy/2026-08-25-0635-3-m1-corpus-migration-dual-read-guides-ci.md`，七项 Decision 全文已记录于该 plan Phase 1 条目，本轮执行复核 + 两项执行校准）：① Decision 5 grep 期望值 live 核实——`grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` = 35（40 总 WI − WI1/WI2/WI3/WI5/WI6 已勾 5），与 gate 执行时刻钉住值一致；② **执行校准（Decision 4 baseline 修正）**：6 份 roadmap 实况为三形态——age-autonomy（checkbox + `: ready` 尾缀）、dsh-plugin / actionable-fixes / draft-robustness / step-audit（bullet + `: done` 尾缀，无 checkbox）、demo（Work Item Status 表格形态），plan Baseline「均为 checkbox + 尾缀混合形态」仅对 age-autonomy 成立；codemod roadmap 面按三形态归一到「frontmatter + 纯 checkbox Work Item 块」（checkbox+尾缀→剥尾缀；bullet+尾缀→转 checkbox（done⇒[x]，todo/ready/planned⇒[ ]）；表格行→按 M 前缀分组转 checkbox 块），语义零丢失；③ **执行校准（Decision 2/3 补充）**：codemod 对迁移的非终态 plan 统一写 `verify: [test]`——完成公式第三合取项的 key 集在 M1 无 mission 默认注入通道（0635-2 谓词契约：缺省且未注入 ⇒ `no-verify-keys` 永不完成），无 verify 字段的迁移 plan 将陷入「全勾 + 回执齐但派生不出 completed → activePlans 永久重喂」（正是 Decision 2 要解的自指陷阱的机械验证半边）；`[test]` = WI11 gate 第 2 条 L1 链命令，BUILD_VERIFY 过渡期写 pass 行的 key 与之对齐。
- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0635-2 收口后）：`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿（web/dist 无 diff）、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 718 pass/0 fail（prompt-check OK）。
- **age-autonomy M1-WI3+WI5+WI6 plan 执行完成**（`docs/plans/age-autonomy/2026-08-25-0635-2-m1-ledger-sections-derivation.md`，三 Phase 全绿）：
  - 新增 `tools/mission-driver/src/ledger-sections.mjs`（import 仅 `node:crypto` + `ledger-frontmatter.mjs`，零 npm）——`scanPlanLedger`/`scanRoadmapLedger`（计数域扫描：`## Phase <n>`（可带尾名）+ Closure Findings + `### M<n>` Work Item 块，仅列 0 checkbox，缩进/代码围栏跳过；计数域外列 0 checkbox = 结构 error；append-only 区块已知前缀行严格校验、未知行容忍 prose、未配对 dispatch = 派生态事实）+ `parseLedgerId`（尾部锚定：nonce8←iter/round←合并前缀，连字符 stem 安全）+ 三种结论行分钉（plan accepted 无 findings / roadmap accepted 带 `findings=none|items` / review 日期迭代行）+ `computeBasisHash`（fm + Phase + CF 区块规范化文本 sha256）+ `deriveCompleted`（五合取公式逐项 reasons：`status-not-active`/`unchecked-items`/`no-verify-keys`/`missing-pass`/`basis-hash-mismatch`/`no-audit-receipt`/`invalid-dispatch-register`）+ 谓词族 `draftPlans/activePlans/heldPlans/closedPlans/openPlans/awaitingClosure`（可注入 `defaultVerifyKeys`，无 frontmatter 文件不在谓词域）。
  - 新增 `tools/mission-driver/test/ledger-sections.test.js`（15 例）+ `test/ledger-derivation.test.js`（21 例），合计 36 例进 L1 链；完成公式五合取项各有独立击穿用例（真值表）。live 对账：`scanRoadmapLedger` 对现行 roadmap 计数 M1 2/11、M2 0/13、M3 0/7、M4 0/5、M5 0/4（40 total/2 checked），与 grep `^- \[ \]`=38 + `^- \[x\]`=2 一致。
  - `plugin/dsh/scripts/build-bundle.mjs` ALLOWED_MODULES 登记 `ledger-sections.mjs`（closure 19 ⊆ 21，两 ledger 模块 unreachable-allowed 属预期——0635-3/WI7 接线后 assets 副本物化）；assets 重建后 content-equal 无 diff。
  - 文档：00-guide 增补 `## Plan Body Sections (M1 Additive Format)`（canonical 区块示例——concrete 形态经 `scanPlanLedger` live 验证 0 error、2 phases、closure/drr 配对各 1、pass 1；三种结论行分钉表；计数域/append-only/内联有界护栏规则）+ Changelog 第 2 条；00-roadmap-guide 增补 `## Roadmap Frontmatter And Audit Record (M1 Additive Format)`（audit-rounds 语义 + Deep Audit Record 示例（live 验证 findings=none 配对绿）+ Work Item 纯 checkbox 纪律）+ Changelog 首条；两 guide 围栏外列 0 checkbox 保持 0 新增；CONTEXT.md 补账本区块/派生库一段；roadmap WI3/WI5/WI6 `ready → done`（证据指针已附；per-WI gate 面 = 新测试族常驻 L1 链，WI11 为 milestone backstop，解释同 0635-1）。
  - 验证：`node --test` 两新文件 36/0；`pnpm --prefix tools/mission-driver test` 718 pass/0 fail（prompt-check OK）；`npm --prefix plugin/dsh test` 133 pass/0 fail（closure + freshness + smoke 全绿）；`pnpm --prefix tools/mission-driver/web run typecheck`/`build` 绿、`lint:prompts` OK（web/dist 无 diff）。
- **age-autonomy M1-WI3+WI5+WI6 执行 Phase 1 决策钉住**（`docs/plans/age-autonomy/2026-08-25-0635-2-m1-ledger-sections-derivation.md`，决策全文记录于该 plan Phase 1 四个 Decision 条目）：① 区块语法 = `## Phase <n>`（h2，可带 `—`/`-` 尾名，边界至下一 h2）+ 固定锚点（Closure Findings / Draft Review Record / Verification / Closure / roadmap Deep Audit Record / `### M<n> —` WI 块）；计数域 = 列 0 checkbox（缩进与代码围栏内均不计）；计数域外列 0 checkbox = 结构 error；未知 h2 容忍但不计数不进 hash。② basisHash = sha256(frontmatter 原文 + 全部 Phase + Closure Findings 区块，文档序拼接，逐行去尾空白 + CRLF→LF，其余逐字)——raw-bytes 备选否决（lint/空白编辑会无谓击穿旧 pass 行）。③ id 词法尾部锚定解析（nonce8←iter/round←合并前缀，不做贪心切分，连字符 stem 安全）；三种结论行分钉（plan accepted 无 findings / roadmap accepted 带 findings=none|items / review 日期迭代行）；append-only 区块 = 已知前缀行严格、未知行容忍 prose、未配对 dispatch = 派生态事实非 error（放行 0635-3 迁移语料）。④ 谓词输入 = 文件记录 + 可注入 `defaultVerifyKeys`（缺省解析不内嵌谓词，未注入 → `no-verify-keys` 显式失败）；`activePlans = active ∧ ¬completed`；`awaitingClosure = active ∧ 全勾 ∧ 无有效审计回执`；无 frontmatter 文件归 0635-3 双读接线。
- **BUILD_VERIFY full-green**（mission `age-autonomy-implementation`，0635-1 收口后）：web typecheck/build 绿、`lint:prompts` OK、`pnpm --prefix tools/mission-driver test` 682/0（prompt-check OK）、mission-check valid、`npm --prefix plugin/dsh test` 133/0（closure 19⊆20 + freshness + smoke 全绿）。web/dist 随 build 重建并提交——存量 dist 落后于 web/src（echarts 残留 + 重复 hash 世代），本次一并同步。
- **age-autonomy M1-WI1+WI2 plan 执行完成**（`docs/plans/age-autonomy/2026-08-25-0635-1-m1-frontmatter-ledger-core.md`，三 Phase 全绿）：
  - 新增 `tools/mission-driver/src/ledger-frontmatter.mjs`（零 import：`rg -n "^import"` 空）——`parseFrontmatter`（扁平标量 + 单层流式数组子集，块标量/嵌套/锚点/别名/重复键/空值/裸多词值全部拒绝并带行列 error，无容错降级；无 frontmatter → `fm:{}`）+ `validatePlanFrontmatter`（status 词汇表、`completed` 派生不可写、hold⇔held、claim/claim-expires 仅 active 且成对、failures 非负整数、verify 单层 command-key 数组、未知键 error）+ `validateRoadmapFrontmatter`（仅 `audit-rounds` 非负整数）+ 常量 `WRITABLE_PLAN_STATUSES`/`TERMINAL_PLAN_STATUSES`/`DERIVED_PLAN_STATUS`/`PLAN_FRONTMATTER_FIELDS`。
  - 新增 `tools/mission-driver/test/ledger-frontmatter.test.js`（22 例 ≥12，进 L1 链）；`plugin/dsh/scripts/build-bundle.mjs` ALLOWED_MODULES 预登记 `ledger-frontmatter.mjs`（build-bundle.mjs:55，闭包 19/20，unreachable-allowed 属预期——importer 在 0635-3/WI7 接线后 assets 副本才物化）。
  - 验证：`node --test tools/mission-driver/test/ledger-frontmatter.test.js` 22/0；`pnpm --prefix tools/mission-driver test` 682 pass/0 fail（prompt-check OK）；`npm --prefix plugin/dsh test` 133 pass/0 fail（closure + freshness + smoke 全绿）；`pnpm --prefix tools/mission-driver/web run typecheck` 绿。
  - 文档：00-guide 增补 `## Plan Frontmatter Field Table (M1 Additive Format)`（字段表与 `PLAN_FRONTMATTER_FIELDS` 常量逐字段同序一致，经 node 对照脚本核实）+ `## Changelog` 首条事件（01 §7）；guide 计数面无新增污染（全文 `- [ ]` 仍 15，`plan-check --strict` 行为不变，修复归 0635-3）；`tools/mission-driver/CONTEXT.md` Mission 配置系统段补 frontmatter 库一行；roadmap WI1/WI2 `ready → done`（证据指针已附，按 plan Phase 3 gate 面解释：per-WI done 不等 WI11 milestone backstop）。
- **age-autonomy M1-WI1+WI2 执行 Phase 1 决策钉住**（`docs/plans/age-autonomy/2026-08-25-0635-1-m1-frontmatter-ledger-core.md`）：① 共享库落点 = 引擎侧 `tools/mission-driver/src/ledger-frontmatter.mjs` 零依赖纯函数模块（否决插件 TS 反查与双份实现；残险 = 零 importer 时 build-bundle import 闭包不可达，assets 副本待 0635-3/WI7 接线后物化，本 plan 只做 ALLOWED_MODULES 预登记）；② 接口契约 = `parseFrontmatter`/`validatePlanFrontmatter`/`validateRoadmapFrontmatter` + 常量 `WRITABLE_PLAN_STATUSES`/`TERMINAL_PLAN_STATUSES`/`DERIVED_PLAN_STATUS`/`PLAN_FRONTMATTER_FIELDS`（0635-2/0635-3 消费面）；③ `agent` 字段按设计 §4.1 纳入格式面校验（跨文件名单存在性随 M2/WI13）。
- **age-autonomy mission 首批 3 份 plan 起草 + 双轮独立 draft review 通过转 active**（DRAFT_PLANS step，work item 覆盖 M1 全部 WI1–WI11，执行顺序 N=1→2→3）：
  - `docs/plans/age-autonomy/2026-08-25-0635-1-m1-frontmatter-ledger-core.md`（WI1+WI2 frontmatter 解析器 + 字段集；共享库落点裁定 = 引擎侧零依赖 .mjs，插件经 build-bundle assets 通道在 0635-3 接线后物化——reachability 事实经 review 修正：复制面是 ENTRY_MODULES import 闭包）
  - `2026-08-25-0635-2-m1-ledger-sections-derivation.md`（WI3+WI5+WI6 计数域扫描 + 三种结论行语法分钉 + basisHash + 完成派生五合取公式 + 扫描谓词族 + 可注入 defaultVerifyKeys）
  - `2026-08-25-0635-3-m1-corpus-migration-dual-read-guides-ci.md`（WI4+WI7–WI11 codemod + 四消费面双读（review 抓出 monitor.js 第四面）+ 8 prompt 对齐 + guides/AGENTS.md 收口 + WI11 gate 执行）
  - 关键裁定（均记录于各 plan Phase 1）：52 份存量 completed plan 保持旧格式由 legacy 通道识别为 closed；过渡期回执写者 = 引擎流程步骤经 prompt 驱动（自指陷阱裁定）；`MISSION_DRIVER_LEDGER=auto|frontmatter|legacy` 断点开关；WI11 grep 期望值双时刻钉住（gate 执行 35 / 稳态 29）。
  - 评审证据：iter1 三份均 needs-revision（task `ses_fca0e893…`/`ses_fca0e6ad…`/`ses_fca0e2e0…`），iter2 三份均 accept（task `ses_fca03be2…`/`ses_fca03a37…`/`ses_fca037d4…`），记录于各 plan Draft Review Record。
- **roadmap 回写**：`docs/backlog/age-autonomy-implementation-roadmap.md` M1 WI1–WI11 `todo → ready`（独立 draft review 通过，按 roadmap 状态块纪律）。
