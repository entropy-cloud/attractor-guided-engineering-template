# Development Log - 2026-08-27

### 2026-08-27

- **M5-WI48（2122-2）Phase 2 roadmap 回写与证明**（plan `docs/plans/age-autonomy/2026-08-27-2122-2-m5-wi48-engine-silent-completed-terminal.md`）：roadmap **WI48 行 `[x]`** + 行内尾部证据注记（裁定 ②+③ 并取 + law 判据扩未勾三分支 + 03 §3/§8 引擎形态豁免成文 + 06 行 9 G8 登记 + 指针本 plan + 验证计数）；`> Last Updated` 头同步「M5-WI48 收口」批事实（含「03 §4.4」落位注记）。Proof 实测：勾选前 `grep -c "^- \[ \]"` = **2**（WI48/WI49 两行，2122-1 已勾 WI47）、勾选后 → **1**（恰 −1，余项 = WI49 归同组 plan 2122-3 持有、非本 plan 结果面不代勾）+ `grep "^- \[ \]" … | grep -c "WI48"` → **0**（WI48 退出未勾计数域）+ `node tools/mission-driver/src/roadmap-check.mjs docs/backlog/age-autonomy-implementation-roadmap.md` **exit 0**。

- **M5-WI48（2122-2）Phase 1 引擎形态 silent-completed 终态通道封堵：nothing-claim 判据扩 roadmap 未勾 + 引擎形态豁免成文 + G8 登记**（plan `docs/plans/age-autonomy/2026-08-27-2122-2-m5-wi48-engine-silent-completed-terminal.md`）：① **修法三选一裁定 = ② law `nothing-claim-guard` 判据扩 roadmap 未勾 + ③ 03/06 引擎形态豁免成文，②③ 并取**——law 动作面是唯一零引擎 diff 前提下可收紧终态真值的执法点（`ctx.roadmapText` 既有注入面 + `scanRoadmapLedger` 既有 import，扩面零新依赖零引擎改动），守夜人参照实现（`roadmap.unchecked` 谓词 + R1 未勾→partial）已证语义可行；route ① 引擎短路/轮门终态前增读未勾否决（破零引擎 diff 底线 + 引擎读全局账本归 G 缺口机制管辖面，G1/G7 同族）；纯 ③ 否决（law 面对未勾视而不见 = 与 nothing→deep-audit trigger 语义契约缝隙，守夜人派发缺未勾维度数据）。② 代码面：`tools/mission-driver/src/law-rules.mjs` `nothingClaimGuardRule` allow 分支扩未勾维度——`ctx.roadmapText` 注入时经 `scanRoadmapLedger` 计数：未勾 >0 → 触发信号携带 `roadmapUnchecked: N`（additive 字段，守夜人消费端零新接线）+ reason 成文强制沿句（deep-audit dispatch is the mandatory path; a nothing claim is never clean-close evidence while unchecked items exist）；==0 → 现行 allow/trigger/reason 逐字节不变；缺席 → 不可观测不冒充（02 §2），现行路径不变；deny 面与 kind 判别面零改动。真值表 `plugin/dsh/test/law-truth-table.test.mjs` 116→**119 例**（三分支钉住：未勾>0 强化信号 / ==0 逐字段不变 / 缺席现行输出逐字节回归 + deny 面不受 roadmapText 影响反向例）+ `terminalClaim` helper 扩 roadmapText 注入；tools↔assets 双副本经 build-bundle 同步（closure 25/26 ⊆ 26 + freshness 42 文件 in-sync）。③ 成文面：03-supervisor §3 trigger 同源段增「nothing→deep-audit 为强制沿」句（判据扩面 + 与 R1 partial 收口互指）+ §8 R1 增引擎形态 as-built 豁免注记（引擎完成判据 = plans-only 清洁，完整 R1 语义由守夜人形态承载，06 G8 指针）+ changelog 行；02 §4.4 增 roadmap 未勾维度判据句 + changelog 行；06 退役清单行 9 增 **G8**（引擎形态终态真值不读全局未勾——retirement-gated，G1/G2/G7 同族）+ 判定改部分覆盖 + 行 8 注记补 law 面未勾维度已扩（2122-2 交付注记）+ G 表 G8 条目。落位注记：立项文与 roadmap WI48 行所称「03 §4.4」——03 无 §4.4 小节，nothing→deep-audit trigger 语义的 03 承载面即 §3 trigger 同源段（判据契约本体 = 02 §4.4），扩面句按此落位并成文于 03 changelog。④ Proof 实测全绿：`pnpm --prefix tools/mission-driver test` **973/973 / 0 失败**（969 基线只增不减）+ `node --test plugin/dsh/test/law-truth-table.test.mjs` **119/119 / 0 失败** + `gate-check <本 plan> --law` **exit 0**（decision allow、9 enforce gate 全 allow 0 deny、workItem M5-WI48 registered/ok）+ `git diff --stat tools/mission-driver/src/engine.js` **为空**（零引擎 diff）+ `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 973 / 插件 423 / 真值表 119）+ web typecheck/build 绿（dist 零漂移）+ lint:prompts OK + 零新增 npm 依赖。

- **M5-WI47（2122-1）Phase 2 roadmap / Follow-up 回写与证明**（plan `docs/plans/age-autonomy/2026-08-27-2122-1-m5-wi47-audit-budget-dual-face-alignment.md`）：roadmap **WI47 行 `[x]`** + 行内尾部证据注记（裁定 ②+③ 并取 + policy 3→8 全局域 / flows 3 per-run 域分离 + 06 行 3 部分覆盖/G7 登记 + 指针本 plan + 验证计数）；Follow-up Backlog「CONTEXT.md M3-WI27 段『maxAuditRounds :7 先例』事实错误」条目清偿（`[x]` + 删除线原断言 + 已清偿注记指针本 plan Phase 1 Fix 第 5 项——缩进 2 格不在计数域，清偿编辑合法）；`> Last Updated` 头同步「M5-WI47 收口」批事实。Proof 实测：勾选后 `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → **2**（余 WI48/WI49 两行，归同组 plan 2122-2/2122-3 勾选）+ `node tools/mission-driver/src/roadmap-check.mjs docs/backlog/age-autonomy-implementation-roadmap.md` **exit 0**。

- **M5-WI47（2122-1）审计预算双域语义对齐 Phase 1：裁定落地 + policy 校正 + 06 行 3 改判 + Follow-up 事实修正**（plan `docs/plans/age-autonomy/2026-08-27-2122-1-m5-wi47-audit-budget-dual-face-alignment.md`）：① **修法三选一裁定 = ② policy 预算校正 + ③ 双域语义/引擎形态执法域成文，②③ 并取**——审计循环连续四轮 P1 产出（R3→WI45/R4→WI46/R5→WI47/R6→WI48）实证生产性，law deny 消息自带 raise-the-budget 合法出口；route ① 引擎轮门播种/校验全局 audit-rounds 否决（破零引擎 diff 底线 + per-run 瞬态防护独立价值 + 06 G 缺口机制管辖面）；纯 ③ 否决（预算 3 < 已耗 6 = law 面恒 deny-worthy 而引擎形态照发的永久不诚实态）。**双域数值分工**：policy `limits.maxAuditRounds` 3→**8**（全局跨 run 预算权威 = 已耗 6 + 两轮生产性余量；law 面 R7/R8 恢复合法判定通道，轮尽诚实 deny 按 R1 语义收口）；flows `maxAuditRounds` **维持 3**（per-run 瞬态防护域，`engine.js:1668` 轮门供值源零改动）——两域刻意不同值，替代旧头注「matches (3) so the two channels agree」。② 五处 Fix：`missions/autonomy.policy.yml`（值 3→8 + 头注双域语义句 + P8 例外自指注记，WI21 先例沿袭）；06 退役清单行 3 判定 已覆盖→**部分覆盖** + 缺口 无→**G7**（引擎形态无全局 audit-rounds 执法点，retirement-gated，G1–G6 同机制）+ G 表增补 G7 条目 + §4 总判定结论收窄（审计预算 → 部分覆盖 + G7 指针）；02 §4.6 预算闸双域语义句 + changelog 行（M5-WI47）；CONTEXT.md M3-WI27 段「:7 先例」事实错误清偿（→「maxAuditRounds: 3 通道」+ 全史恒 3 / `git log -S '"maxAuditRounds": 7'` 零命中注记随句，R5 P2 Follow-up）；`test/law-policy.test.js` live 断言 `maxAuditRounds: 3`→`8`（fixture 面零触碰——grep 复核全库仅此一处读 live 值）。③ Proof 实测全绿：`pnpm --prefix tools/mission-driver test` **973/973 / 0 失败**（969 基线只增不减）+ `gate-check --policy missions/autonomy.policy.yml` **exit 0**（limits maxAuditRounds: 8，schema 校验绿）+ `mission-check missions/age-autonomy-implementation.json .` **exit 0** + `git diff --stat tools/mission-driver/src/engine.js tools/mission-driver/flows/` **为空**（零引擎 diff）+ `gate-check <本 plan>` 自指 **exit 0**（decision allow、workItem M5-WI47 registered/ok、0 enforce deny）+ web typecheck/build 绿（dist 零漂移）+ lint:prompts OK + 零新增 npm 依赖。

- **多 plugin DSH 重构立项（mission `multi-plugin-dsh` 创建立项 + roadmap + 设计 doc 三件套落档）**：用户提出将项目从单 DSH plugin 形态（`plugin/dsh/` 一棵子树挂 `dsh-mission-control`）重构为多 plugin 家族（`plugin/nop-*/` 顶层并列）。已落档：① `missions/multi-plugin-dsh.json` 任务定义（继承 base，plansDir=`docs/plans/multi-plugin-dsh/`）；② `docs/design/multi-plugin-dsh-architecture.md` 设计 doc（DRAFT 状态，立 nop-* 命名约定、isolate realm 协议、`plugin/plugin-manifest.yml` schema:1 形态、`plugin/load-plugins.sh` 七 flag POSIX sh 脚本形态、nop-age 继承自 plugin/dsh/ 的迁移语义、nop-route 新 plugin 的 4 路由 + 4 纯函数模块 + 零调用契约）；③ `docs/backlog/multi-plugin-dsh-roadmap.md` 18 个 WI 分 5 个里程碑（M1 设计 / M2 nop-age 迁移 / M3 manifest + 脚本 / M4 nop-route 实现 / M5 联合挂载验证）；④ `docs/plans/multi-plugin-dsh/.gitkeep` 空目录占位（plan 在执行 WI 时动态创建）。**关键决策记录**：① 目录布局采 `plugin/nop-xxx/` 顶层并列（原 `plugin/dsh/` 壳不保留）；② nop-route 仅做智能路由/重试/模型选择（鉴权归宿主，不做 API gateway）；③ 启动编排采 sh + manifest 配置形态（无需状态检测/重试简化启动时参数）；④ roadmap WI 拆分遵循"每个 WI 对应一个 plan"原则，但**WI 列表为初始猜测，实际执行时根据 live baseline 调研动态决定拆分/合并/新增**——这是对本批次初稿"过度工程化"的修正（初稿错误地预先创建了 7 份占位 plan 文件，因未做 live baseline 调研而内容凭空；按"plan 是 execution 时机动态产生的"纪律已删除占位、保留空目录）。

- **M1-WI45+WI46（1503-1）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **969/969 / 0 fail** exit 0（prompt-check OK；含新守卫 `prompt-shadow-guard.test.js` 五断言面）+ project-specific 复核：`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` exit 0（valid，键级修正后）· `roadmap-check` exit 0 · roadmap `grep -c "^- \[ \]"` = 0。工作树变更 = WI45/WI46 代码面（删除 `missions/prompts/` 三共享覆盖 + `missions/age-autonomy-implementation.json` prompts map 9→2 键 + 新增 `tools/mission-driver/test/prompt-shadow-guard.test.js`——见上方 1503-1 条目）+ docs 回写面（1503-1 plan 全文 + roadmap WI45/WI46 勾选与 Follow-up 清偿 + CONTEXT.md 增量句 + 本日志），零引擎源码 diff（四文件 diff 空）、零新增依赖、monitor web 源码零触碰。

- **M1-WI45+WI46（1503-1）closure 反馈重执行冷验证（零代码改动）**：closureScriptCheck 前轮报 `missing-pass:test` + `no-audit-receipt`，经 closure audit 修复（`## Verification` pass 行 basisHash 经 computeBasisHash 现算回填 + `## Closure` dispatch/accepted 同 id 配对 `#audit-…-a27b8433`）后本轮全量复验绿——`deriveCompleted` completed=true 零 reasons（basisHash `19e320d…` 与 plan 现算一致，1 pair 0 unpaired）+ `plan-check --strict` exit 0（16/16）+ `pnpm --prefix tools/mission-driver test` **969/969 / 0 失败** exit 0 复跑 + mission-check / lint:prompts / roadmap-check / gate-check（自指，9 enforce gate 全 allow）exit 0 + repo 现场复核（`missions/prompts/` 不存在、prompts map 恰 `{multiAudit, openAudit}` 值未动、引擎四文件 diff 空、零新依赖、roadmap `grep -c "^- \[ \]"` = 0）。

- **M1-WI45+WI46（1503-1）roadmap 回写与计数域收口**（plan `docs/plans/age-autonomy/2026-08-27-1503-1-m1-wi45-wi46-shared-prompt-override-fix.md` Phase 2）：Follow-up Backlog「`prompts` map 字段零消费面」条目清偿（`[x]` + 删除线原断言 + R4 更正注记保留为历史 + 已清偿注记指针本 plan——结论 = 键级修正（7 dead 删 / 2 live 留）非「整删 map」）；WI45/WI46 行 `[x]` + 行内尾部证据注记（修法裁定 / 删除三覆盖 / 守卫断言指针）；`> Last Updated` 头同步本批事实。Proof：勾选后 `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → **0 实测**（Follow-up 缩进行不在计数域——R4 余四条 P2 仍在册）+ `node tools/mission-driver/src/roadmap-check.mjs docs/backlog/age-autonomy-implementation-roadmap.md` **exit 0**。
- **M1-WI45+WI46（1503-1）共享 prompt 覆盖清偿 + prompts map 键级修正——修法落地与验证**（plan `docs/plans/age-autonomy/2026-08-27-1503-1-m1-wi45-wi46-shared-prompt-override-fix.md` Phase 1）：① WI45 修法三选一裁定 = **删除三共享覆盖回落内置双模式**（否决②逐字节对齐 = 永久双维护负债；否决③逐 mission `promptsDir` = 配置面扩大且共享陷阱留存；链代码兜底分支在库零代码改动）——`git rm missions/prompts/{execute,closure-audit,build-verify}.md`（目录随空消失），五个无 `promptsDir` mission（age-autonomy-implementation/demo/dsh-plugin/mission-driver-draft-robustness/mission-driver-step-audit）的 plan-execution 子流程（EXECUTE/CLOSURE_AUDIT/BUILD_VERIFY）即时回落 `TOOL_ROOT/prompts/` 内置双模式版；自指闭环 = 同 run 的 CLOSURE_AUDIT/BUILD_VERIFY（含本 plan 收口）即解析内置版。② WI46 键级修正 = `missions/age-autonomy-implementation.json` `prompts` map 9 键 → 恰 `{multiAudit, openAudit}`（两键值逐字不动——live 消费面（orchestrator.js → vars → deep-audit-loop `when` 门）；7 dead 键删除，键域与三姊妹 mission 先例同构；整删 map 即 DEEP_AUDIT 静默空转——R4 立案误删面排除）。③ 守卫测试 `tools/mission-driver/test/prompt-shadow-guard.test.js` **+8 用例**五断言面：① plan-execution promptPath basename 集 ∩ `missions/prompts/` 现存文件 = ∅（再遮蔽即红）② `missions/*.json`（排除 base*）prompts map 键域 ⊆ {multiAudit, openAudit}（dead 键回流即红）③ 两 live 键引用路径在 projectRoot 下存在（dangling 即红）④ deep-audit-loop 存在 `multiAuditPrompt != ''`/`openAuditPrompt != ''` when 门步（删 map/删门两面回溯可见）⑤ 内置三件双模式标记在库（execute「Do NOT write `completed` anywhere」/ closure-audit「NEVER write `completed`」+ dispatch/accepted 回执协议 / build-verify「Ledger Verification pass lines」+ computeBasisHash 命令）。④ CONTEXT.md Mission 配置系统段增量句（覆盖已删 + 键域 live 事实 + 守卫指针）。验证：`pnpm --prefix tools/mission-driver test` **969/969 / 0 失败**（961 基线 + 8 只增不减，prompt-check OK）+ `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` **exit 0**（键级修正后 valid）+ `lint:prompts` OK + web typecheck/build 绿（dist 零漂移）+ `gate-check <本 plan>` 自指 **exit 0**（decision allow，workItem M1-WI45+WI46 展开-注册对账 ok）+ `git diff --stat` 四引擎源码文件为空 + 零新增 npm 依赖。
- **M5-WI40（1203-1）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **961/961 / 0 fail** exit 0（prompt-check OK）。工作树变更 = WI40 代码面（`tools/mission-driver/src/monitor.js` extends 合并修复 + `monitor.test.js` +7 用例 + `plugin/dsh/test/law-truth-table.test.mjs` corpus smoke 终态适配——见上方 WI40 条目）+ docs 回写面（1203-1 plan 全文 + roadmap WI40 勾选与 Follow-up 清偿 + CONTEXT.md 增量句 + 本日志），零引擎源码 diff、零新依赖、monitor web 源码零触碰。
- **M5-WI40（1203-1）执行期修正 1 处（Phase 3 收口门依赖，非 Non-Goal 违反——留红即 fail，WI39 同型先例）**：`plugin/dsh/test/law-truth-table.test.mjs` rwg REAL roadmap corpus smoke 的前提「live roadmap 存在未勾 WI 行」随 WI40 勾选永久失效（roadmap 全 done = 终态，`assert.ok(wiLine)` 实测 fail 1 复现）——改为从真实已勾行构造 unchecked current（`- [x]`→`- [ ]` 仅在构造文本，零文件写），前向翻转 + 证据追加两 allow 面仍对真实 corpus 形状与注册表实测（roadmap 文本本体受 roadmap-write-guard 保护不可改，测试侧适配是唯一合法修复位；116/116 复绿）。
- **M5-WI40（1203-1）Phase 3 最终收口审计 + roadmap 回写——roadmap 全 done**（plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md`）：① **独立 subagent 收口审计 findings=none**（dispatch/accepted 同 id 回执落 plan `## Closure`）：M1–M4 全部 Verification Gate 复跑绿——M1（plan-check --strict exit 0 / 引擎 **961/961 / 0 失败** / ledger-frontmatter 23/0 实际路径 `tools/mission-driver/test/ledger-frontmatter.test.js`（WI11 门字面 `plugin/dsh/test/` 路径为 M1 期笔误注记，N1 informational）/ 勾选前 grep=1 仅 WI40 行）· M2（gate-check --policy exit 0 / 真值表 **116/116** / WI22 语料 gate-check exit 0 / mission-check exit 0）· M3（supervisor-trigger **43/0** / supervisor-recovery **18/0** / verify:e2e:continuous 缺 `DSH_E2E_CONTINUOUS` env **fail-fast exit 1 = 文档化括号语义**（WI31 opt-in 门姿势如实留档）/ WI31 plan `--law` exit 0 九 enforce gate 全 allow）· M4（pool-lifecycle **13/0** / prompt-assembly **16/0** / context-profile **13/0**）+ 三硬门 allow face（WI36 语料 gate-check exit 0）+ spot-check（Phase 1/2 全勾、engine.js 零 diff、零新依赖、Follow-up 行清偿在册、日志条目在册）——**roadmap WI40 允许勾选（PASS）**。② **回执落点 as-built 裁定**（plan Phase 3 Decision）：WI40 门字面 `docs/audits/age-autonomy/<final-closure>.md` 为 M2-WI22 证据面重建（2026-08-25）前旧指针——外部审计生命周期通道已退役，plan 级收口审计唯一面 = plan `## Closure` dispatch/accepted 回执（同 id 配对）、mission 级 = roadmap `## Deep Audit Record` inline；不新建 `docs/audits/age-autonomy/` 旧形文件。③ **roadmap 回写**：WI40 `[ ]`→`[x]` + 行内证据注记（三 Phase 交付 + 六门实测结果 + 收口审计回执指针 + 落点裁定）+ `> Last Updated` 头「M5 最终收口」句；勾选后 `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → **0 实测**（`[x]` 44 行，M1–M5 全里程碑收口）。Follow-up 条件项未触发（findings=none）。plan `## Verification` pass 行（test / verify-age，basisHash 绑定全勾基）+ `## Closure` 回执由本 Phase 写入——completed 为派生态禁写（01 §5.2），plan 终态由五合取公式派生（`plan-check` derived completed 实测 + `gate-check` 自指 exit 0）。
- **M5-WI40（1203-1）Phase 2 门命令实测：全绿**（plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md`）：① `./verify-age.sh` → **L1+L2+L2.5 GREEN exit 0**（引擎 961/0 · 插件 420/0 · 真值表 116/0，含 Phase 1 新用例计数只增不减）② `pnpm --prefix tools/mission-driver test` → **961/961 / 0 失败**（HEAD 基线 954 + 7）③ `node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` → **exit 0**（valid=true + one-mission-one-roadmap 无冲突）④ `pnpm --prefix plugin/dsh run verify:e2e` → **真实宿主 e2e PASS exit 0**（env 在场实跑绿——demo 双后端 shape identity + correction-retry 恢复 + onboarding 双形式 parity + monitor render（oc-/native- stepLogs/logs/node-detail）+ WI12 三技能（skills.list / analyze tag-parse / draft 异步两段 terminal completed）；两分支取「env 在场真实跑绿」如实记录，非 fail-fast 括号分支）⑤ 三硬门 allow face 复核——`node tools/mission-driver/src/gate-check.mjs <本 plan 文件>` 自指 **exit 0**（workItem M5-WI40 registered/ok）+ 已收口 M4 语料 `gate-check docs/plans/age-autonomy/2026-08-27-0558-3-m4-wi36-verification-gate.md` **exit 0**（closure-audit-binding / writer-identity / plan-completed 全 allow）。⑥ 勾选前 `grep -c "^- \[ \]" docs/backlog/age-autonomy-implementation-roadmap.md` → **1**（仅 WI40 一行，Phase 3 前置确认）。
- **M5-WI40（1203-1）Phase 1 Follow-up P2 清偿：monitor extends 合并修复**（plan `docs/plans/age-autonomy/2026-08-27-1203-1-m5-wi40-final-gate-roadmap-closeout.md`）：monitor 三个配置读面统一改经 `mission-check.mjs` 导出面 `loadMission`（单一 extends 合并实现 base → base.local → mission，零第二合并）——`readMissionConfig`（display 面不传 projectRoot，跳过路径存在性校验保持 dangling-path 容忍；throw → null 优雅退化，:531 flowName 兜底 / :594 run 详情 config / :823 plans 列表 / :1160 POST /api/runs 白名单门四消费面自动受益）+ `handleListConfigs`（throw → skip；`!roadmapPath` 过滤防线保留）+ `handleGetRoadmap`（失败 → 既有空响应形状）。**校验语义收窄如实记录**（plan Phase 1 Decision 裁定：校验语义即既有 `/api/configs` 过滤意图）：缺必填字段的 partial mission 从 configs 列表/config 面 null 退化——`monitor.test.js` 既有 fixture 补全为合法 mission 形状（legacy-mission / no-flow-mission / c1–c5 / alpha·beta·gamma / mission-fix2 系 / writeRunnableMission；断言语义不变）。新增 extends 用例 **+7**（`monitor.test.js` M5-WI40 describe：① `/api/configs` 列出 extends mission 且合并链字段可见 + run 详情 config 继承面 ② 继承 roadmapPath 解析 ③ 继承 plansDir 读 plans ④ base.local 覆盖优先级（local > base ∧ mission-own > 双方）⑤ dangling extends target 三面 graceful skip（不 500/不进列表/空形状）⑥ base.json/base.local 过滤回归钉 ⑦ 无 extends mission 响应形状钉（configs 行 7 键 + config 白名单 11 键 deepEqual））。回写：CONTEXT.md Mission 配置系统段增量句（monitor 配置读面走 extends 合并）+ roadmap Follow-up Backlog 行 `[x]` + 删除线 + 已清偿注记（本 plan 指针）。验证：`pnpm --prefix tools/mission-driver test` **961/961 / 0 失败**（HEAD 基线 954 实测 +7 只增不减）+ web typecheck 绿 + `web run build` 绿（dist 零漂移）+ `lint:prompts` OK + `git diff --stat tools/mission-driver/src/engine.js` 为空 + 零新增 npm 依赖。
- **M5-WI39（1023-3）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **953/953 / 0 fail** exit 0（prompt-check OK）。工作树变更 = WI39 执行期唯一代码面修正（`plugin/dsh/test/law-truth-table.test.mjs` rwg corpus smoke 正则放宽接受粗体标记 WI 行——见下方 WI39 条目「执行期修正」段）+ docs 回写面（design/architecture owner-doc 六处事实性增补 + roadmap WI39 勾选 + 1023-3 plan Verification/Closure 回执 + CONTEXT.md + 本日志），零引擎源码 diff、零新依赖、monitor web 源码零触碰。
- **M5-WI39（1023-3）owner-doc 一致性收口（Phase 2 处置 + Phase 3 回写收口）**（plan `docs/plans/age-autonomy/2026-08-27-1023-3-m5-wi39-ownerdoc-consistency-closure.md` 三 Phase 全勾，零代码纯 doc 面——Phase 1 矩阵见下方前条）：① **Phase 2 处置落地**（全为事实性增补，diff 自查零契约语义变更）——02-rule-law §4.7 as-built 放置与保护姿态注记 + changelog 行（引擎侧 law 内核三模块 0815-1 放置裁定 + P8 字面集不含的理由 + 替代保护面：import 闭包消费 / build-bundle 复制通道 freshness / CI + 已批准立项纪律——**收口 0950-1 WI21 Deferred「law 内核 P8 覆盖缺口」**）；05-usage §2.2 unlock 行「（目标能力）」→ M3-WI28 已落地指针（unlock/dispose 双处置）+ 「（continuous）」行 → `mdcontrol.continuous` route 名 + 落地注记 + 头部句精确化（**复核发现：`mdcontrol.stop` 未落地**——wire 路由七条 run/status/list/draft/analyze/continuous/unlock 无 stop，头句改为「continuous 已落地、stop 仍目标形态」）+ §4 池化对照行「归 M5-WI37」→ D1 维持不交付裁定指针；00-overview §7 P0–P4 落地状态注记（P0 M1 0635 / P1 M2 0815+0950 / P2 M3 1411+1954+0433-1 / P3 M4 0433+0558 / P4 = WI37 总判定 + G1–G6 + WI38 G3 收口）；06-checklist 矩阵行 #9 + 缺口 G3「增补中/进行中」→「已收口/已闭合」刷新（WI38 audit 范围外注记指认的措辞过时清偿）；04-efficiency §6 「归 **M5-WI37**」→ D1 裁定指针（rg 粗体标记漏命中行，人工清点补入）；mission-driver-baseline §Public Exports `src/exit-map.js` 补 M5-WI38 增量登记（EXIT_MAP 11→13 键 supported behavior 变更的 same-change 登记补偿——本 plan Related「复核 WI38 同步完整性」的命中发现 + M3/M4 supervisor/efficiency 面判定 = plugin 侧归 packaging doc 无需 baseline 登记成文于同句）；packaging doc（native-panel T1–T3 / run-guard / draft-split / preset-residual / 状态头）全条目复核 as-built 准确**零编辑**。② **Phase 3 Proof 实测**——`./verify-age.sh` **L1+L2+L2.5 GREEN exit 0**（引擎 953 · 插件 420 · 真值表 116，doc 变更零行为回归）+ `node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-27-1023-3-….md` **自指 exit 0**（decision allow、workItem M5-WI39 registered/ok、9 观察 0 deny——本 plan 是 02 §4.7 所载门禁的合法消费者）+ `pnpm --prefix tools/mission-driver test` **953/953 / 0 失败**。③ **回写**——roadmap **WI39 `[x]`** + 行内证据注记（矩阵 42 行 + 三态分布 + 四 Fix 项指针）+ `> Last Updated` 头「M5 第三片」句；CONTEXT.md「owner-doc 一致性收口（M5-WI39）」段；本日志条目。范围外发现（不处置，已在矩阵注记归属）：`mdcontrol.stop` 未落地（05 §2.2 目标形态行如实保留）；monitor extends P2 附挂 deferral 归后继代码修复 slice。**执行期修正 1 处（Phase 3 Proof 依赖，非 Non-Goal 违反——留红即 fail）**：`plugin/dsh/test/law-truth-table.test.mjs` rwg REAL roadmap corpus smoke 的 find 正则 `/^- \[ \] WI\d+\b/` 不接受粗体标记形 WI 行（WI40 `- [ ] **WI40 …**` 自 roadmap 创建即在库；WI39 勾选后 WI40 成唯一未勾行 → `assert.ok(wiLine)` fail，实测 fail 1 复现）——放宽为 `/^- \[ \] \**WI\d+\b/`（guard 本体对粗体行 allow 语义不变，116/116 复绿；改 WI40 行文本本身被 roadmap-write-guard「rewrite line text」deny 面 prohibited——测试正则窄化是唯一合法修复位）；修复后全链复跑绿。plan `## Verification`/`## Closure` 由后续 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）。
- **M5-WI39（1023-3）Phase 1 全量清点矩阵**（plan `docs/plans/age-autonomy/2026-08-27-1023-3-m5-wi39-ownerdoc-consistency-closure.md`——design+architecture owner-doc 一致性收口，零代码纯 doc 面）：清点命令留痕 `rg -n "目标能力|[Dd]eferred|归 M5|reopen|Reopen" docs/design/age-autonomy/ docs/architecture/mission-driver-baseline.md docs/architecture/dsh-plugin-packaging.md` 全量命中 **33 行**（dsh-plugin-packaging.md 9 / 05-usage.md 2 / 01-file-ledger.md 7 / 02-rule-law.md 1 / 03-supervisor.md 3 / 06-engine-retirement-checklist.md 10 / 00-overview.md 1；mission-driver-baseline.md 零命中）+ Current Baseline 已知项与人工清点补充 8 行 + Non-Goals 归属注记 1 行 = 矩阵 **42 行**，逐行三态判定（closed-by-evidence / still-deferred / superseded；「已核无需动作」= 三态判定完成且处置动作为空的记法）。矩阵（条目 × as-built 现状 × 三态 × 处置）：

  | # | 条目（文档:行） | as-built 现状 | 三态判定 | 处置动作 |
  | --- | --- | --- | --- | --- |
  | 1 | 05-usage:33 §2.2 `mdcontrol.unlock` 行「（目标能力）」 | M3-WI28 已落地（第七路由 + writer `unlockPlan`/`disposePlan` 经守夜人 role=supervisor） | closed-by-evidence | 落地注记（M3-WI28 指针） |
  | 2 | 05-usage:83 §4 对照行「跨步未交付归 M5-WI37」 | WI37 D1 已裁定维持不交付 | closed-by-evidence | 指针刷新（06 §3 D1） |
  | 3 | 05-usage:34 §2.2「（continuous）」行（补充复核——Fix 项「含 continuous 行复核」） | M3-WI28 已落地 `mdcontrol.continuous`（opt-in 默认 off） | closed-by-evidence | 行名落 route 名 + 落地注记 |
  | 4 | 05-usage:4 头部句「带 `continuous`/`stop` 的能力行是目标形态」（补充） | continuous 已落地；`mdcontrol.stop` **未落地**（wire 路由七条无 stop——run/status/list/draft/analyze/continuous/unlock） | closed-by-evidence（continuous）∧ still-deferred（stop） | 头部句精确化（stop 仍目标形态） |
  | 5 | 01-file-ledger:64/79/135/145/159/185 状态词枚举 ×6（`cancelled\|superseded\|deferred` 契约文本） | 与 `ledger-frontmatter.mjs` 常量及 law 执法面 as-built 一致 | 已核无需动作 | 无 |
  | 6 | 01-file-ledger:109 legacy 迁移映射史录 | 历史记录（codemod 已执行） | 已核无需动作 | 无 |
  | 7 | 02-rule-law:126 终态冻结（§4.3 内） | 契约文本，`plan-completed` 终态冻结腿已执法 | 已核无需动作 | 无 |
  | 8 | 02-rule-law:167 §4.7 P8 保护清单 vs 0815-1 放置裁定（Current Baseline 已知项，非 rg 命中） | law 内核三模块（`tools/mission-driver/src/{law-core,law-policy,law-rules}.mjs`）引擎侧放置；P8 字面集（`isLawProtectedPath` 四族）只含 `plugin/dsh/src/law/**`（实指 host-adapter）+ policy + 两 CLI——字面错位未成文 | closed-by-evidence | §4.7 as-built 放置 + 保护姿态注记（收口 0950-1/WI21 Deferred「law 内核 P8 覆盖缺口」） |
  | 9 | 03-supervisor:93 §10 P4 判定面在库指针 | WI37 已同步在册 | 已核无需动作 | 无 |
  | 10 | 03-supervisor:100 WI37 changelog 行 | 历史回执 | 已核无需动作 | 无 |
  | 11 | 03-supervisor:105 WI28 changelog「直至 M4-WI33 评估——plan Deferred 登记在案」 | 后继裁定已落：06 §3 D4 本轮不接线（前置 = 独立立项 + 真宿主长跑证据） | superseded（后继指针 = 06 D4 在册） | 无（changelog 是历史记录不回写；06 D4 即裁定本体） |
  | 12 | 06-checklist:13/43/45/47/53/59/65/71/77/83 D1–D7 裁定记录本体 ×10 | 判定工件本体（三要素齐备，rg 断言在册） | 已核无需动作 | 无 |
  | 13 | 06-checklist:36 矩阵行 #9「EXIT_MAP 现行 11 键 / 增补中 / plan 在库 active」（补充——08-27 WI38 收口审计范围外注记指认的措辞过时） | WI38 已收口：EXIT_MAP 13 键 + `exit-map.test.js` 19 例 + EXECUTION-PRINCIPLE §11 同步 | closed-by-evidence | 状态刷新（增补中 → 已收口） |
  | 14 | 06-checklist:99 缺口 G3「M5-WI38 独立立项（进行中）」 | WI38 已收口（G3 缺口已闭合） | closed-by-evidence | 状态刷新（进行中 → 已闭合） |
  | 15 | 00-overview:105 §8 文档地图 06 行 | WI37 已双登记 | 已核无需动作 | 无 |
  | 16 | 00-overview:84-92 §7 迁移路径表 P0–P4 无落地状态注记（Current Baseline 已知项） | P0–P3 已落地（M1/M2/M3/M4）；P4 已评估（WI37 总判定引擎留任主后端（条件退役）） | closed-by-evidence | §7 落地状态注记（事实性增补；P4 行指向 06 清单 + WI37 总判定） |
  | 17 | 04-efficiency:107 §6「二选一裁定归 **M5-WI37**」（补充——rg 因粗体标记 `归 **M5-WI37**` 漏命中，人工清点补入） | WI37 D1 已裁定：维持 as-built 不交付 | closed-by-evidence | 指针刷新（06 §3 D1 verdict + 重开触发） |
  | 18 | mission-driver-baseline.md（rg 零命中；Current Baseline 已知项 = Update Rule 增量判定）M3/M4 交付面（supervisor/efficiency Public Exports 或状态面） | 判定：supervisor/efficiency 模块本体在 `plugin/dsh/src/`（packaging doc 领地，增量已登记至 M4-WI34）——baseline（scope = `tools/mission-driver/` 引擎面）**无需登记**。清点另发现注册面漂移：§Public Exports `src/exit-map.js` 条目缺 M5-WI38 增补登记（EXIT_MAP 11→13 键 = supported behavior 变更，Update Rule 要求同 change 更新未发生——本 plan Related「复核 WI38 同步完整性」的命中发现） | closed-by-evidence（补登记） | EXIT_MAP 增量句补登记（M5-WI38 指针）；law 内核三模块未注册面复核 = 契约 owner 是 02-rule-law + packaging §Packaging Layout（copy 通道）+ CONTEXT.md，非 baseline 注册面漂移——已核无需动作 |
  | 19 | dsh-plugin-packaging:3 状态头（increments 至 M4-WI34 + P3/P4 deferral 裁定史录） | M5-WI37 零代码；M5-WI38 = 引擎数据模块 + assets 副本内容刷新（exit-map.js 原已在 ALLOWED_MODULES 26 内，无 StepExecutor/dispatch chain/packaging layout 变更——Update Rule 不触发） | 已核无需动作 | 无 |
  | 20 | dsh-plugin-packaging:262 dispose disposition 树注 | WI28 as-built 一致 | 已核无需动作 | 无 |
  | 21 | dsh-plugin-packaging:436 supervisor bullet（LANDED…CONTEXT PROFILE，至 M4-WI34）+ executor pooled 休眠注记 | as-built 一致（execute 休眠经 06 D4 终审维持） | 已核无需动作 | 无 |
  | 22 | dsh-plugin-packaging:438 run-guard adjudicated 条目 | ActiveRunGuard per-root 单活动 + draft 共槽 as-built 未变（continuous 模式不启引擎 run——D4 不接线） | still-deferred（adjudicated 复核准确） | 无 |
  | 23 | dsh-plugin-packaging:440 draft jobs as-built split「moving it in-process remains deferred」 | 未变（mdcontrol.draft 仍经 cmdDraftMission seam） | still-deferred（准确） | 无 |
  | 24 | dsh-plugin-packaging:442 native client panel deferral（reopen triggers T1/T2/T3） | T1 未发生（client-plugin 面仍 developer-preview/undocumented）、T2 无在册需求、T3 monitor 仍在用（05 §6 零改动继续使用） | still-deferred（触发条件复核不变） | 无 |
  | 25 | dsh-plugin-packaging:455 preset true-model 自然语言质量 deferred watch-only residual | 无触发（06 D1–D7 未覆盖此项 = 独立 residual，条件不满足） | still-deferred（准确） | 无 |
  | 26 | dsh-plugin-packaging:486 RPC-direct panel question adjudicated deferred | 同 #24 族（reopen-time 实现约束在册） | still-deferred（准确） | 无 |
  | 27 | dsh-plugin-packaging:505 P4 delivered 表行（deferral 史录） | 历史记录 | 已核无需动作 | 无 |
  | 28 | monitor extends 合并 P2 + 附挂 deferral（display-only verify 派生态显示，0925-1/0950-2 登记；06 G6 / roadmap Follow-up Backlog 未勾项） | 代码修复独立结果面 | Non-Goal 归属注记 | 仅注记归属（后继代码修复 slice 收口，本 plan 不动） |

  对账：rg 33 命中行 → 矩阵行 1、2（2）+ 5、6（7）+ 7（1）+ 9、10、11（3）+ 12（10）+ 15（1）+ 19–27（9）= 33，零遗漏；补充行 3、4、8、13、14、16、17、18 = Current Baseline 已知项（02 §4.7 / 05 §2.2 / 00 §7 / baseline Update Rule）+ 人工清点发现（06 #9/G3 措辞过时——WI38 收口审计范围外注记指认、04 §6 粗体标记漏命中行）。三态分布：closed-by-evidence 11 行（处置 8 项：02 §4.7 注记 / 05 §2.2 unlock+continuous+头句 / 05 §4 行 / 00 §7 注记 / 04 §6 行 / 06 #9+G3 刷新 / baseline EXIT_MAP 补登记）、still-deferred 6 行（触发条件复核均不变，零处置）、superseded 1 行（后继指针在册）、已核无需动作 24 行。Phase 2 落地处置，Phase 3 回写收口。

- **M5-WI38（1023-2）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **953/953 / 0 fail** exit 0（prompt-check OK）。工作树变更 = WI38 代码面（`src/exit-map.js` + plugin assets 副本 + `test/exit-map.test.js` + `EXECUTION-PRINCIPLE.md` §11）+ docs 回写面（roadmap WI38 勾选 + 1023-2 plan Verification/Closure 回执 + 03-supervisor changelog + CONTEXT.md + 本日志），零 `engine.js` diff、零新依赖、monitor web 源码零触碰。
- **M5-WI38（1023-2）EXIT_MAP 显式增补 partial/blocked → exit 3 收口**（plan `docs/plans/age-autonomy/2026-08-27-1023-2-m5-wi38-exit-map-partial-blocked.md` 两 Phase 全勾——03 §8 终态映射纪律的独立立项变更，冻结公共契约 EXIT_MAP / EXECUTION-PRINCIPLE §11 的显式数据行增补）：① **Phase 1 契约变更**——`src/exit-map.js` EXIT_MAP 11→13 键：`partial: 3, blocked: 3`（exit 3 = 新退出码类「终态非完成、需人工处置」，补救 = unlock/dispose；Phase 1 Decision 备选否决四项——复用 2（保护族语义/补救路径不同）/ 分离 3·4（零消费面区分）/ 映射 1（CI 误报）/ 维持 fall-through exit 0（违反 03 §8 不得静默纪律），重开触发 = 出现按退出码分支的真实消费者）；头注更新（supervisor 终态词入表理由 = 前瞻防护独立形态暴露面；`skipped`/动态 done 词 fall-through 语义不动且 M5-WI38 增补不改变该 carve-out）；`EXECUTION-PRINCIPLE.md` §11 表增两行（触发 = 守夜人终态词经独立形态暴露（R1/R3 / R3∧held>0·R4）+ 表尾语义句（词源守夜人 03 §8 R1–R4、引擎存续期 `_result` 不发射、exit 3 与 2/1 分离理由）；plugin assets 副本经 build-bundle 同步（closure ok 25/26 + 写 42 文件，仅 `plugin/dsh/assets/src/exit-map.js` 内容变更）。② **Phase 2 测试钉住**——`test/exit-map.test.js` 13→**19 例**：DOCUMENTED 表翻 13 键（partial/blocked → 3）+ 「exactly 13」drift guard 翻新 + M5-WI38 专项 describe 4 例（partial→3（非 0——未完成 roadmap 不得静默读作 completed）/ blocked→3（非 2——补救是人工处置非重跑）/ 既有 11 键逐行回归 deepEqual（single_step_done→0 + unknown 四键 1 码 + max*·ping_pong 四键 2 码族）/ skipped·未映射动态词 fall-through exit 0 + 头注一致性断言（读 exit-map.js 源文本 pin carve-out 注记与 supervisor 词源注记双向在册））；执行期修正 1 处（头注一致性 regex `fall\s*through` 匹配不了注释续行 ` * ` 星号 → `fall[\s\S]*?through`）。③ **零行为变更实证**——`git diff --stat tools/mission-driver/src/engine.js` 为空；引擎 `_result` 不发射两词（DSH 回执 + mdcontrol.status 面不变）；零新增 npm 依赖（双 package.json 零 diff）；monitor web 零触碰（typecheck/build 绿、dist 零漂移）。④ **回写**——03-supervisor §8 Changelog 独立立项兑现条（exit 3 选型 + 一句理由 + 零行为变更声明）；CONTEXT.md 关键约束增量句（EXIT_MAP 现行 13 键冻结契约）；roadmap **WI38 `[x]`** + 行内证据注记 + `> Last Updated` 头「M5 第二片」句；本日志条目。⑤ **Proof 双验证实测绿**——`pnpm --prefix tools/mission-driver test` **953/953 / 0 失败**（947 基线 + 6 新例只增不减，prompt-check OK）+ `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 953 · 插件 420 · 真值表 116）；`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移）· `lint:prompts` OK。plan `## Verification`/`## Closure` 由后续 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）。**范围外注记**：`docs/design/age-autonomy/06-engine-retirement-checklist.md` 覆盖矩阵行 #9 与缺口 G3 的「增补中/进行中」措辞随 WI38 收口已过时（该文件归 WI37 plan 所有，非本 plan Targets——留给后续 doc-sync/深审计自然刷新）。
- **M5-WI37（1023-1）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`，零代码纯验证面）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **947/947 / 0 fail** exit 0（prompt-check OK）。工作树变更全部为 docs（roadmap WI37 勾选 + 1023-1 plan 收口回执 + 06-engine-retirement-checklist 清单工件 + 00/03 design 增量 + 本日志 + CONTEXT.md + 1023-2/1023-3 新 plan 草案），无代码/前端/dist 面改动。
- **M5-WI37（1023-1）引擎退役判定清单收口**（plan `docs/plans/age-autonomy/2026-08-27-1023-1-m5-wi37-engine-retirement-decision-checklist.md` 两 Phase 全勾，纯评估与文档工件——零代码变更/零引擎 diff/零插件行为变更/零新依赖）：① **Phase 1 清单工件 + 覆盖矩阵**——新建 `docs/design/age-autonomy/06-engine-retirement-checklist.md`（Status 头 = living decision-gate artifact 非 supported baseline 契约；落点 Decision：`docs/analysis/` 一次性件与 mission 内嵌双备选否决）：覆盖矩阵 **12 行**（transient 分类退避/循环防护 ping-pong·max*/审计预算轮门/失败预算与熔断 maxRetries vs 三桶/reconcile·孤儿回收/子流程编排 plan-execution 四步链/draft 管线/主流程编排与 execute 腿/终态与退出码面（EXIT_MAP 11 键冻结——WI38/1023-2 在库 active 按「增补中」如实登记不阻塞）/monitor·run-state 面/marker 协议/L2 parity 证据），每行 = 引擎职责 → 门禁+守夜人覆盖面 → 证据指针（模块+测试+plan 三类）→ 缺口 → 判定三态（已覆盖 ∨ 缺口阻塞 ∨ 引擎留任面）；「裁定记录」节 D1–D7 空位表 + 总判定位；00-overview **§7 P4 行指针 + §8 文档地图双登记**（rg ≥2 实测）。Proof 断言实测：`rg -c "判定"` = **22**（≥13）/ `rg -c "重开触发"` = **11**（≥7）。② **Phase 2 D1–D7 裁定 + 总判定**——**D1** 跨步 `--session` 续用二选一：**维持 as-built 不交付**（live 证据：execute 腿休眠现状 + 零引擎 diff 底线否决 engine threading + `PoolAgentsFace` DSH 独有否决 CLI 会话池；重开 = 退役执行期 ∨ token 观测主因）；**D2** reasoningEffort 载体：**维持无载体注记**（config.js 增字段 = 引擎 diff；随退役路径一并裁定）；**D3** BUILD_VERIFY 物理退役：**保留至退役执行期**（双驱动幂等已落地冗余无害；删除 = Flow JSON 契约保护面变更需独立立项）；**D4** 守夜人 initial-execute 派发：**本轮不接线**（PromptAssembler 前置已备但接线 = 行为变化需独立立项 + 真宿主长跑证据；引擎 run 执行腿 as-built 成熟；R3/R4 兜底诚实失败面）；**D5** TTL 未到期死会话 claim 提前回收：**不立项**（停滞指纹+活动信号兜底已就位，收益 ≈5 分钟 vs 误杀活执行成本倒挂；重开 = 误杀/滞留实战案例）；**D6** cron 声明面：**文档 seam 完备**（05 §3.1 三通道成文；install-age.sh 样例归模板产品面不在本 mission）；**D7** marker 物理删除：**存续期保留诊断/日志面，删除 = 退役执行期动作**（prompt 模板/prompt-check/engine 解析/monitor 四点协同）；**总判定 = 引擎留任主后端（条件退役）**——账本域职责已覆盖，run 内执行语义面/execute 腿/run-state·monitor/EXIT_MAP 冻结契约/marker 存续期 = 引擎留任面；**缺口前置清单 G1–G6**（execute 接线/独立形态后端替代/EXIT_MAP 增补归 WI38/跨步续用/marker 删除/monitor extends P2）。③ **指针与回写**——03-supervisor §10 判定清单指针 + Changelog 行（事实性增补非契约变更）；CONTEXT.md「引擎退役判定清单（M5-WI37）」段；roadmap **WI37 `[x]`** + 行内证据注记（矩阵行数/7 裁定/总判定/验证计数）+ `> Last Updated` 头「M5 首片」句；本日志条目。④ **Proof 双验证实测绿**——`pnpm --prefix tools/mission-driver test` **947/947 / 0 失败**（prompt-check OK；944 基线 + 1023 组新 plan 文件 corpus 面 +3，计数只增不减）+ `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 947 · 插件 420 · 真值表 116，零代码变更回归钉住）。plan `## Verification`/`## Closure` 由本 run 后续 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）。
- **M4-WI36（0558-3）BUILD_VERIFY full-green verification**（mission `age-autonomy-implementation`，零代码纯验证面）：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移——git status 无 `web/dist/` 变更）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **944/944 / 0 fail** exit 0（prompt-check OK）。工作树变更全部为 docs（roadmap WI36 勾选 + 0558-3 plan 收口回执 + 本日志 + CONTEXT.md），无代码/前端/dist 面改动。

- **M4-WI36（0558-3）M4 Verification Gate 三门实跑 + M4 收口（里程碑 5/5）**（plan `docs/plans/age-autonomy/2026-08-27-0558-3-m4-wi36-verification-gate.md` 两 Phase 全勾，零代码纯验证面）：① **prereq 核验**——0558-1/0558-2 `node tools/mission-driver/src/gate-check.mjs <plan> --law` 均 exit 0 且 derivedCompletion.completed=true（五合取 statusActive/allChecked/mechanicalVerification/auditReceipt/dispatchRegister 全真 + verification basisHash 双 fresh 匹配）——前置满足不代偿纪律遵守。② **三门真实复跑全绿**——`node plugin/dsh/test/pool-lifecycle.test.mjs` **13 tests / 13 pass / 0 fail / exit 0**（门限 ≥10 超额，在库 13 例基线一致）；`node plugin/dsh/test/prompt-assembly.test.mjs` **16 tests / 16 pass / 0 fail / exit 0**（≥12 超额，16 例基线一致）；`node plugin/dsh/test/context-profile.test.mjs` **13 tests / 13 pass / 0 fail / exit 0**（≥8 超额，0558-1 交付面实测 13 例）。exit code 以独立复跑捕获（首轮流经 tail 管道的 PIPESTATUS 捕获在 zsh 失效，重跑裸命令钉住 exit 0——非测试红）。③ **观测项（终审 P2-4，不阻断）缺 env 分支如实留档**——`pnpm --prefix plugin/dsh run verify:e2e:continuous` **fail-fast exit 1**（`DSH_E2E_CONTINUOUS` 缺失，opt-in gate 拒跑不静默跳过，WI31 门命令 3 括号语义先例同型）；另经源核对 `e2e-continuous.mjs` 为 fixture 驱动 scratch 面（mock 模型 usage 固定 `prompt_tokens:3`），在库无 FRESH vs CONTINUE token 差采集面——roadmap 原句「机制落地即收口，收益待观测」记录，Deferred 归档 watch-only residual（Successor Required: no；重开条件 = 真实宿主连续模式长跑随运营观测自然采集）。④ **回写**——roadmap **WI36 `[x]`** + 行内证据注记（三门实测输出 + 观测项分支）+ `> Last Updated` 头「M4 收口」句（**M4 区块 5/5 全勾**：WI32–WI36）；CONTEXT.md 增「效率层四件套完成态（M4 收口）」一段；本日志条目。⑤ **收口复核全绿**——`node tools/mission-driver/src/roadmap-check.mjs docs/backlog/age-autonomy-implementation-roadmap.md` exit 0；`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 **944/944** · 插件 **420/420** · 真值表 **116/116**，exit 0）；`pnpm --prefix tools/mission-driver test` **944/944** 0 失败（prompt-check OK）。零 `engine.js` diff、零新增 npm 依赖、零前端面改动（无需重建 dist）。plan `## Verification`/`## Closure` 由后续 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）。

- **M4-WI35（0558-2）独立形态降级统一裁定 + 前缀纪律钉住收口**（plan `docs/plans/age-autonomy/2026-08-27-0558-2-m4-wi35-independent-form-degradation.md` 两 Phase 全勾，04 §6 as-built 裁定）：① **Phase 1 承载裁定（Decision）**——独立形态效率承载 as-built = ㈠步内 `--session` 续用（correction retry 与 parse 内层 run 共会话，`runner.js` `DEFAULT_DRIVER_ARGS` `{session}` token + `runParseAgent` 转发）㈡prompt 前缀纪律（本 plan 审计 + 钉住）；**跨步续用不在库且本 plan 不交付**（`engine.js` 三 `_executeAgentStep` 调用点 :1119/:1356/:1790 传 `null`、`lastSessionId` 唯一消费面 = `_runCorrectionAgent` :949——每 agent step 起新会话）。备选 A（CLI 侧 opencode session 池）否决：continuable 子代理 face 是 DSH 宿主独有（`PoolAgentsFace`）+ 跨 run 会话复用缺 attemptId 代际判据（04 §2.3 陈旧上下文风险）+ 引擎 dispatch 面重构违反零引擎 diff 底线；备选 B（`engine.js` 最小 threading）否决：违反 roadmap 核心纪律 1 + 会话跨步累积改变 run 语义（超出效率层「不改契约」边界）→ 归 M5-WI37（守夜人接管独立形态派发 ∨ 引擎 threading 二选一裁定）。残险 = 独立形态跨步 KV 缓存损失（接受——04 §6「部分存在」as-built 如实收窄为「步内存在、跨步未交付」）。② **步内续用覆盖核对（引用一致，零缺面）**——`runner-routing.test.js` :34-49（`--session` 转发）/ :69/:169-181（无 session 剥离 + pure 面）、`prompt-cmdline-limit.test.js` :60/:90-91（cmdline `--session` 保留/缺省）、`step-executor.test.js` :165（`lastSessionId` = ses_contract_1）、`transitions.test.js` :431（correction retry 会话传递）、`parse-fallback.test.js` :164（parse 共会话）逐项核对与 plan 基线清单一致，已钉住面零重复造测。③ **prompt 前缀纪律审计（11/11 模板，零重排）**——全部 `{{var}}` 分类：唯一大易变负载 = `{{runSkeleton}}`（run-postmortem.md :28，`buildRunSkeleton` JSON 骨架），位于 27 行固定 ground-rules 指令前导**之后**非前置 → 无需重排；其余全部为小引用（路径/命令/名字 + 小易变诊断：`PLAN_FILE`/`forEachItem`/`SCRIPT_CHECK_RESULT`/`SCRIPT_CHECK_DETAILS`（单行 join 诊断）/`targetRunDir`）。④ **钉住测试**——新 `tools/mission-driver/test/prompt-prefix-discipline.test.js` **15 例 0 失败**：经 **live 渲染路径**（`expression.mjs resolveTemplateVars`，`_buildPrompt` 同一函数）双渲染断言共享固定前缀字节 = 首个易变变量偏移（11 模板逐件）；PAYLOAD/VOLATILE/STABLE 三类注册表 drift gate（未分类新变量 = fail，镜像 context-map EXPECTED_VARS 先例）+ 无死条目；除 run-postmortem 外 10 件模板零易变负载注入（加强为全模板零 payload）；run-postmortem payload 后置于 `</ground_rules>` 前导之后双断言（偏移 + LCP ≥ 前导全长）。引擎测试 **929→944**（+15，计数只增不减）；`lint:prompts` OK（零 prompt 编辑）；`git diff --stat` engine.js/runner.js/executor.js 为空（零引擎 diff 底线保持）。⑤ **Phase 2 如实成文 + 回写**——04 §6 as-built 注记（步内已钉住/前缀纪律审计+钉住指针/policy fixedPrefix·assembly 为 DSH 形态面/跨步未交付 + M5-WI37 后继指针）+ 05 §4 对照行 ◐ 注记同步 + CONTEXT.md 独立形态降级段；`independentChannelOf` seam 头注复核：`dispatch-resolve.ts` :222「reasoningEffort … no config.js carrier (documented residual, M5 independent-form gate)」与 roadmap M5-WI37（引擎退役判定门）文字核对一致（编号未写入 seam 注记 = 以文字核对为准），零 seam 编辑零行为改动。验证（full-green）：`pnpm --prefix tools/mission-driver test` **944/944** exit 0 · `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 944/0 · 插件 420/0 · 真值表 116/0 只增不减）· web typecheck/build 绿（dist 零漂移）+ lint:prompts OK · 零引擎 diff + 零新增 npm 依赖；roadmap **WI35 `[x]`** + 行内证据注记 + Last Updated 头同步。Deferred 在册：跨步 `--session` 续用（M5-WI37）、reasoningEffort 独立形态载体（M5-WI37 一并裁定 config.js 增字段与否）。

- **M4-WI34（0558-1）上下文画像 context-profile 收口**（plan `docs/plans/age-autonomy/2026-08-27-0558-1-m4-wi34-context-profile.md` 三 Phase 全勾，04 §4/§5 落地）：① **Phase 1 画像模块 + schema + 种子化**——新 `plugin/dsh/src/efficiency/context-profile.ts`（效率层第三件；可注入 io/clock、零运行时 import 与 prompt-assembler 无环）：工件 schema v1 `{version, seededFrom, updatedAt, entries:[{path,reads,lastSeenAt}]}`——`loadProfile`（缺失 = 未初始化→种子化；未知 version = 显式注记 + 保守重建不静默沿用）+ `saveProfile` 确定性序列化（entries 按 path 排序、字段序固定、单尾换行，同输入字节恒等）+ tmp+rename 原子写；种子化 `seedFromReadFirst` 解析 AGENTS.md「Read This First」（仅直接路径行入表，「listed in …」间接行跳过——间接面经该文件自身嵌入覆盖）；`mergeReads` 读频归并（reads 累加、lastSeenAt 刷新、projectRoot 前缀剥离）；**工件不进 `missions/`**（mission scanner 全量 `*.json` 扫描污染面——`listMissionsString` 反向用例钉住），落 `docs/references/context-profile.json` 并入库首启种子工件；`law-policy.mjs`（tools+assets build-bundle 同步）fixedPrefix 增第四 kind `profile`（`{kind, ref, topN?}`，topN 正整数且仅 profile kind 合法，缺省 5 DSL 可覆盖）+ 引擎测试 +1 + live policy drafter 追加 profile 块（P8 例外 = 本 plan active 先例沿袭）+ `gate-check --policy` exit 0。② **Phase 2 挖掘 + 消费接线**——数据源三分层各有消费面：池成员 `sessionEvents` 句柄透出 + `memberSessionEvents()` 池面（零池语义改动）归集 `tool/call` 事件 × read/grep/glob 类文件参数（schema 以 live 宿主 SessionEventMap 钉住）+ `_tmp/<runDir>/run-state.json` steps[].promptFile 文本挖路径（存在性过滤）+ Reflexion `memory/runs.md` 同法，缺面 fail-soft 注记；headless（无 agents face）= 显式降级注记保留种子表不挖掘（独立形态完整面归 0558-2/WI35）；watchdog `emitTerminalEvent` 链尾 run-terminal 事件触发一次挖掘（单飞守卫复用；fail-soft——采集异常只产回执不影响终态回执与停派；`profileMining` 挂载选项注入 io/sessionEvents）；防抖 = 空 tally 不写盘 ∨ top-N 集（排序后**集**等价判定）不变不写盘（集内 rank 波动不触发、进出振荡天然抑制）；消费 = `resolveAssemblyBlocks` 钉 `profileRoot` + `filesOfBlock` 按 reads 降序/path 升序取 top-N 逐文件展开（复用 embedStamp/maxFileBytes/hash 台账/CONTINUE dedup 全部 file 语义；工件缺失/未知 version/无 projectRoot = 显式 note 不崩溃）；角色显式 DSL 声明优先，未声明部署薄指针逐字节不变（向后兼容钉）。③ **Phase 3 Proof + 回写**——`plugin/dsh/test/context-profile.test.mjs` **13 例 0 失败**（WI36 门 ≥8 下限：种子化直接/间接行 + 首启 / schema 往返 + 字节确定性 + 未知版本保守重建 / tool-call tally / missions 零污染反向例 / mergeReads+topN 序 / 防抖双腿 + rank 漂移不写 / 三层源归并 / headless 降级 / watchdog 终态沿 e2e + fail-soft / top-N 展开与角色覆盖向后兼容钉）；执行期修正两处 = `fsProfileIo` 首启写盘前父目录 mkdir（e2e 探针发现）+ 防抖集比较从有序串改排序集等价（Phase 2 Decision 语义对齐）。验证（full-green）：`pnpm --prefix tools/mission-driver test` **929/929**（prompt-check OK）exit 0 · `pnpm --prefix plugin/dsh test` **420/420**（manifest/tsc/bundle --check/smoke 全链）exit 0 · `./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 925→929 · 插件 407→420 · 真值表 116 只增不减）· web typecheck/build 绿（dist 零漂移）+ lint:prompts OK · `git diff --stat tools/mission-driver/src/engine.js` 为空 + 双 package.json 零依赖新增。回写：roadmap **WI34 `[x]`** + 行内证据指针 + Last Updated「M4 第三片」句；CONTEXT.md「效率层上下文画像」段；packaging doc 四面（状态头 CONTEXT PROFILE DELIVERED 增量句 + src 树 efficiency/ context-profile 条 + test 树 context-profile 条 + Service Surface CONTEXT PROFILE 面与 Proof 串 13）。Deferred 在册：独立形态挖掘采集面（headless，归 0558-2/WI35）、native-executor run 子代理默认启用画像取材（重开条件：观测显示重复读取成为成本主因时经 DSL 显式启用）。
- **M4 0558 组三 plan 起草 + 独立评审共识翻 active（DRAFT_PLANS 步产物）**：roadmap M4 剩余三项成组交付，执行序 = 文件名 N 前缀（group `2026-08-27-0558`）——① `docs/plans/age-autonomy/2026-08-27-0558-1-m4-wi34-context-profile.md`（M4-WI34 上下文画像：`plugin/dsh/src/efficiency/context-profile.ts` schema v1 + AGENTS.md「Read This First」种子化 + run 终态读频挖掘（watchdog 终态沿，fail-soft）+ 防抖（top-N 集等价不写盘）+ fixedPrefix 第四 kind `profile` top-N 消费（law-policy schema 扩展 + build-bundle assets 同步）；`context-profile.test.mjs` ≥8 例对齐 WI36 门）② `2026-08-27-0558-2-m4-wi35-independent-form-degradation.md`（M4-WI35 独立形态降级统一裁定：**跨步 `--session` 续用不在库且本 plan 不交付**——评审 blocking 修正后重基线（`_executeAgentStep` 三调用点 :1119/:1356/:1790 传 null、`lastSessionId` 唯一消费面 correction :949），CLI 池 / engine threading 双备选否决，Deferred 后继 M5-WI37；交付面 = 步内续用既有钉住测试核对引用（runner-routing/step-executor/transitions/parse-fallback）+ 引擎 prompt 前缀纪律逐件审计与钉住测试（预期多数/全部模板无需重排）+ 04 §6/05 §4/CONTEXT.md as-built 如实成文）③ `2026-08-27-0558-3-m4-wi36-verification-gate.md`（M4-WI36 收口门：三门命令真实复跑（pool-lifecycle 13 ≥10 / prompt-assembly 16 ≥12 / context-profile ≥8 随 WI34）+ 观测项括号语义如实记录（WI31 先例）+ prereq = 0558-1/2 derived completed 等待不代偿）。评审：三 plan 各经独立 sub-agent 冷评审——WI34 iteration 1 acceptable-with-changes（PoolAgentsFace 事件面表述 + 「可选」模糊词两修订）→ iteration 2 共识；WI35 iteration 1 **blocking-issues**（跨步续用基线事实错误）→ 重基线 → iteration 2 共识；WI36 iteration 1 acceptable-as-is（零阻塞）；DRR 回执对齐在案，status 全部翻 active。结构校验：三文件 plan-check fieldsValid=true / gate-check exit 0（9 structural gate 全 allow）。roadmap 未动（勾选归各 plan 执行期回写）。

- **M4-WI33（0433-3）mission VERIFY 步四命令复跑（full-green verification）**：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `pnpm --prefix tools/mission-driver/web run build` 绿（dist 零漂移）· `pnpm --prefix tools/mission-driver run lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **925/925**（prompt-check OK）exit 0 · 追加 `pnpm --prefix plugin/dsh test` **407/407**（manifest + tsc --noEmit + bundle --check + smoke-import 全链）exit 0 · `gate-check --policy missions/autonomy.policy.yml` exit 0（assembly 段过 schema）· `./verify-age.sh` L1+L2+L2.5 GREEN（引擎 925/0 · 插件 407/0 · 真值表 116/0）。工作区切分两提交：code（`src/efficiency/prompt-assembler.ts` + 池/exec-arm/native-executor 接线 + law-policy 双副本 assembly schema + policy `assembly`/`fixedPrefix` 块 + `prompt-assembly.test.mjs` 16 例 + 引擎 law-policy 测试 +2）+ docs（plan 0433-3 + packaging/roadmap/CONTEXT.md + 本日志）。

- **M4-WI33（0433-3）PromptAssembler 双模式组装收口**（plan `docs/plans/age-autonomy/2026-08-27-0433-3-m4-wi33-prompt-assembler.md` 三 Phase 全勾，04 §3/§5 落地）：① **Phase 1 assembler 纯函数 + assembly schema**——新 `plugin/dsh/src/efficiency/prompt-assembler.ts`：`assemble(mode, spec, dynamicCtx, ledger, io)`（FRESH = fixedPrefix 块聚合 + 动态块 / CONTINUE = hash 台账比对 deltaEmbed：未变跳过、变更全量重发；可注入 AssemblerIo、确定性、fail-soft 缺文件显式 note）；`hash8Of` = sha256 前 8 hex（`computeBasisHash` 同算法源）；`renderEmbedStamp` 默认 `<file path="{path}" hash="{hash8}">{content}</file>` 模板可覆写；目录全文嵌入（顶层文件排序展开、子目录不递归、maxFileBytes 超限显式 `[NOT EMBEDDED: …]` 注记不静默截断）；前缀纪律 = 固定块 → 动态块 → volatile 尾缀（两次 FRESH 字节恒等）；hash 三用（dedup / 不符重发 / grep 可审计）；compaction 对抗 = 台账 `__sends` 计数 + 每 8 次 CONTINUE 全 charter 周期性重发；台账协议读写分离（`commitToLedger` 单一写面、sentHashes 无 sentinel 污染、`charterHashesDiffer` sentinel 免疫）；`tools/mission-driver/src/law-policy.mjs` 增 `assembly:` 段 schema（`ASSEMBLY_FIELDS` + embedStamp 三渲染槽校验 + continueDelta 布尔 + `DEFAULT_EMBED_STAMP` 同源导出）+ 引擎测试 +2（923→925）+ assets 副本经 build-bundle 同步；live policy `assembly` 块 + drafter `fixedPrefix`（AGENTS.md text persona + project-context.md file 嵌入 maxFileBytes 60000，P8 例外 = 本 plan active 自指先例）。② **Phase 2 消费接线**——`agent-pool.ts` 池成员增 `sentHashes` per-member 台账（随成员生灭、crash 替补首发 FRESH 保守向裁定成文）+ `acquire` 增 `currentFileHashes` charter-hash 轮换腿（**M4-WI32 Deferred 收编**：04 §2.2 第二触发腿，判据单一实现、池层零 fs IO）+ 出口携台账；`exec-arm.ts` `dispatchPromptFor`（fixedPrefix ∧ assembly 材料在场才组装——薄指针派发 prompt 为动态后缀；`continueDelta: false` 钉 FRESH；否则逐字节透传）统一四 followup 位（dispatchAgentExit / dispatchDraftPlans / reclaimClaim / `recovery.ts` redispatch，后两者经 lawCtx 占位 + io 注入）；`native-executor.ts` `assemblyPrefix` config 面（镜像 `nativeModelSelection` config-consumed 先例）：run 子代理首步 FRESH / 后续步 CONTINUE（per-run 内存台账），engine prompt（promptsDir 覆盖链已解析）原样为动态后缀——policy 叠加不取代（04 §7）。③ **Phase 3 Proof + 回写**——`plugin/dsh/test/prompt-assembly.test.mjs` **16 例 0 失败**（WI36 门 ≥12 下限：字节序+确定性 / 前缀纪律 / dedup / 变更重发 / 目录全文 / 超限注记 / 台账语义 / 模板覆写 / compaction 第 8 次重发 / 轮换判据四腿 / fail-soft / 占位解析 / 池双模式 e2e / 向后兼容逐字节钉 / continueDelta 钉 / native 双步 + legacy 透传）；执行期修正 = 测试 rig 三处（charter 变更须经 `dispatchPromptFor` commit 才入台账——补全真实派发流；假 agents `get` 须报活否则池误判崩溃换新；池 idle timer 须假定时器防吊住 node --test 事件循环）+ 实现修正一处（`charterHashesDiffer` 须排除 `__sends` sentinel 否则 commit 后恒判变）。验证：`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 923→**925**/0 · 插件 391→**407**/0 · 真值表 **116**/0 只增不减 · policy face ok + corpus 19 文件全 ok）；`gate-check --policy missions/autonomy.policy.yml` exit 0；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零依赖新增。回写：roadmap **WI33 `[x]`** + 行内证据指针 + Last Updated「M4 第二片」句；CONTEXT.md「效率层 PromptAssembler」段；packaging doc 四面（状态头 PROMPT ASSEMBLY DELIVERED 增量句 + src 树 efficiency/ 增 prompt-assembler.ts 条 + test 树 prompt-assembly 条 + Service Surface supervisor bullet PROMPT ASSEMBLY 面与 Proof 串 16）。Deferred 在册：画像 top-N 取材（WI34 后继）、独立形态 CONTINUE CLI（WI35）、FRESH vs CONTINUE token 收益观测（WI36 观测项不阻断）。

- **M4-WI32（0433-2）mission VERIFY 步四命令复跑（full-green verification）**：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `pnpm --prefix tools/mission-driver/web run build` 绿（dist 零漂移）· `pnpm --prefix tools/mission-driver run lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **923/923**（prompt-check OK）exit 0 · 追加 `pnpm --prefix plugin/dsh test` **391/391**（manifest + tsc --noEmit + bundle --check + smoke-import 全链）exit 0。工作区切分两提交：code（`src/efficiency/agent-pool.ts` + supervisor 三件接线 + policy reviewer `mode: pooled` + `pool-lifecycle.test.mjs` 13 例）+ docs（plan 0433-2 + packaging/roadmap/CONTEXT.md + 本日志）。

- **M4-WI32（2026-08-27-0433-2）效率层 agent 池 + 角色互斥收口**（plan `docs/plans/age-autonomy/2026-08-27-0433-2-m4-wi32-agent-pool.md` 三 Phase 全勾，04 §2 落地）：① **Phase 1 池模块**——新目录 `plugin/dsh/src/efficiency/agent-pool.ts`（效率层首件）：角色池 `drafter:{projectRoot}` 长期复用 + `reviewer:{groupId}` 同批复用（`groupScopeOf` = frontmatter `group:` → 文件名时间戳前缀回退（01 §2）→ 裸 stem 兜底）；create-on-first-use + followup 续用 + 空闲 TTL dispose（可注入 timers + dispose 幂等）+ rotateEvery 计数轮换 + 崩溃凭持久 session 换新（liveness 三态）；attemptId 代际令牌（`attemptStale` 单一判据面）；live policy `reviewer` 翻 `mode: pooled`（poolKey `reviewer:{groupId}`/TTL 30/rotate 8，镜像 drafter 先例；anchoring 残险由轮换 + 跨组新启兜底，Phase 1 Decision 成文）。② **Phase 2 红线 + 接线**——P7 审计禁入（closure-audit/deep-audit/multi-audit 结构性永不入池，无视 agent `mode` 配置硬拒 + 诚实注记）；角色互斥登记表（drafter ∩ reviewer/auditor = ∅，冲突派发拒绝；同 run auditor ≠ 任何执行者——执行者集 = plans claim holders frontmatter 派生（runId 锚定剥离）∪ 池 executor 标签，审计派发候选校验，watchdog 每周期刷新 `runExecutorSessions` 透传）；池挂点在 `exec-arm.ts` `createDispatchAgent` 内部（返回型改 `{created|refused}` union；recovery redispatch 直调自动经池无绕过孔）；execute/closure-audit/deep-audit 保持 fresh 路径（executor pooled 声明**休眠**——WI28 裁定沿袭，注记指向 M4-WI33/M5-WI37，executor 标签仍登记）；`dispatch-resolve.ts` 零改动；恢复互操作 = `recovery.ts` live ∧ `attemptStale` → 跨代重派 + 显式 revoke（活会话陈旧 attempt 不 resume），headless 降级路径保持；watchdog 持 mount 级池 + stop() 幂等 teardown（ctx.effect 生命周期链）。③ **Phase 3 Proof**——`plugin/dsh/test/pool-lifecycle.test.mjs` **13 例 0 失败**（WI36 门 ≥10 下限：drafter/reviewer 生命周期 / 组作用域三腿 / TTL dispose / rotateEvery + 代际（旧 stale·新 current·崩溃换新）/ P7 禁入池层 + 派发链反向钉（auditor 声明 pooled 也恒 fresh）/ executor 休眠注记 + 标签 / 角色互斥登记表 + 派发链 / auditor ≠ executor（候选校验 + `executorSessionsOf` 推导）/ 恢复同代 resume e2e + 跨代重派 e2e + 死成员换新 / 池复用 × 评审租约（同周期双派同 session + writer-identity per-plan 最新行租约 allow·deny）/ headless 降级）；执行期修正 = 测试 rig 假定时器（真 30min idle timer 会吊住 node --test 事件循环——全部 direct-pool 构造切 fakeTimers）。验证：`./verify-age.sh` **L1+L2+L2.5 GREEN**（引擎 923/0 · 插件 378→**391**/0 · 真值表 116/0，基线只增不减）；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零依赖新增。回写：roadmap **WI32 `[x]`** + 行内证据指针 + Last Updated「M4 首片」句；CONTEXT.md「效率层 agent 池」段；packaging doc 四面（状态头增量句 + src 树 `efficiency/` 条目（ALLOWED_MODULES 计数 26 不动注记）+ test 树 pool-lifecycle 条目 + Service Surface AGENT POOL 面与 Proof 串 13）。Deferred 在册「上游文档 hash 变化触发的组内轮换」（M4-WI33 hash 台账后接线）。

- **M3-WI31（0433-1）mission VERIFY 步四命令复跑（full-green verification）**：`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `pnpm --prefix tools/mission-driver/web run build` 绿（dist 零漂移）· `pnpm --prefix tools/mission-driver run lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **923/923**（prompt-check OK）exit 0。工作区切分两提交：code（`src/gate-check.mjs` `--law` 执法面 + `test/gate-check-law.test.js` 10 例）+ docs（plan 0433 组三件 + roadmap WI31 勾选 + CONTEXT.md + 本日志）。

- **M3-WI31（2026-08-27-0433-1）closure 反馈重派冷放复验 no-op（零改动，全绿确认）**（mission `age-autonomy-implementation`，run 2026-08-26-130203 重派）：closure audit 反馈三项（missing-pass:test / missing-pass:verify-age / no-audit-receipt）经核已由本 run 早前批次全部清偿——`## Verification` 双 pass 行（test / verify-age，basisHash=c96f7574…d7ce0 与 plan 当前内容 `computeBasisHash` 实测同值绑定，exit=0）+ `## Closure` dispatch/accepted 同 id 回执对（`#audit-2026-08-26-130203-mission-driver-…-m3-wi31-verification-gate-law-cli-1-137db353`）。本步冷放复验全绿：`plan-check --strict` 本 plan passed=true（16/16 勾、derivedCompleted=true、completionReasons 空、fieldsValid=true、EXIT=0）；`pnpm --prefix tools/mission-driver test` **923/923**（prompt-check OK）exit 0；`./verify-age.sh` **L1+L2+L2.5 GREEN**（真值表 116/116）exit 0；四门命令独立复跑——supervisor-trigger **43/43** exit 0 · supervisor-recovery **18/18** exit 0 · `verify:e2e:continuous` 无宿主 env **fail-fast exit 1**（括号语义分支如实复现非虚报）· `gate-check <本 plan> --law` **exit 0**（9 enforce gate 全 allow + workItem M3-WI31 对账 ok）；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空 + 引擎 package.json 零 diff（零新增 npm 依赖）；roadmap WI31 `[x]` + 行内四门证据注记 + Last Updated「M3 收口」句 + CONTEXT.md `--law` 注记在位。frontmatter `status` 维持 active（completed 派生禁写，01 §5.2）。本步除本条日志外零文件改动。

- **M3-WI31 0433-1 收口：gate-check `--law` 执法面 + M3 Verification Gate 四命令实跑（M3 里程碑收口）**（plan `docs/plans/age-autonomy/2026-08-27-0433-1-m3-wi31-verification-gate-law-cli.md` 两 Phase 全勾）：① **Phase 1 `--law` 第四 CLI 模式**（roadmap 门命令 4 此前字面不可执行——WI28 DRR iteration 2 裁定「补模式 ∨ 修门文本」取补模式）：`tools/mission-driver/src/gate-check.mjs` `<plan.md> --law` = `discoverOwningMission` 祖先走查 → mission `autonomyPolicy`（回退 `missions/autonomy.policy.yml` 发现，双缺结构化 deny）→ `loadPolicyFile` 真实 policy → `evaluateGates` 全量 gates（ctx = plansRoots/roadmapPath/roadmapText/projectRoot/commands/agentNames/maxAuditRounds，默认 verify 键复用 `plan-check.mjs missionDefaultVerifyKeys` 单一实现；P8 保护面语料仅当求值目标命中保护路径时注入——镜像单文件面条件读）→ stdout JSON：per-gate verdict + `deriveCompleted`/completionReasons + workItem 对账 + plan 队列谓词（draftPlans/activePlans/heldPlans/closedPlans/openPlans/awaitingClosure，mission plansDir 语料、排序确定性输出）；exit 0 iff 全部 enforce gate allow；与单文件结构面的差异成文于头注与 usage；trigger→closure 链路覆盖拆分 Decision 成文（trigger 派发腿归门命令 1 e2e 承载，gate-check 不复刻 plugin 侧求值器——TS 引擎侧不可达 + assets 单向）。② 新引擎测试 `tools/mission-driver/test/gate-check-law.test.js` **10 例 0 失败**（usage 面 / allow 面 + workItem 对账 + 派生视图 / 队列谓词钉住（forge 留 active 队列语义） / 默认 verify 键注入派生 / 伪造回执 enforce deny（closure-audit-binding unbound conclusion）/ P8 条件语料（corpus 注入分支非 fail-closed 分支，宽 plansDir fixture）/ 无 owning mission / autonomyPolicy 回退发现 / 双缺 deny / legacy 域外放行）。③ **Phase 2 四门实测**：`node plugin/dsh/test/supervisor-trigger.test.mjs` **43/0** exit 0（≥20 超额）；`node plugin/dsh/test/supervisor-recovery.test.mjs` **18/0** exit 0（≥8 超额）；`pnpm --prefix plugin/dsh run verify:e2e:continuous` 本机无 `DSH_E2E_CONTINUOUS` → **fail-fast exit 1**（roadmap 括号语义分支如实记录，WI28 plan `2026-08-26-1954-1` 真实宿主三连跑 PASS 为 standing evidence）；`node tools/mission-driver/src/gate-check.mjs docs/plans/age-autonomy/2026-08-27-0433-1-…-law-cli.md --law` **exit 0**（9 enforce gate 全 allow + workItem 对账 ok + 派生视图 + 队列谓词——自指先例）。④ 回写：roadmap **WI31 `[x]`** + 行内证据注记 + 头部 Last Updated「M3 收口」句（M3 区块 7/7 全勾）；CONTEXT.md gate-check 面增 `--verify`/`--law` 模式注记；本日志条目。⑤ 验证：`pnpm --prefix tools/mission-driver test` **923/923**（基线 913 + 新 10，prompt-check OK）；web typecheck + build 绿（dist 零漂移）；`roadmap-check` passed exit 0；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 依赖。plan `## Verification`/`## Closure` 由引擎 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）。

- **M3-WI30 1954-3 Phase 2 收口 + 全 plan 收口：恢复语境补强 + 文档同步与回写**（plan `docs/plans/age-autonomy/2026-08-26-1954-3-m3-wi30-stagnation-pingpong-detection.md` Phase 2，两 Phase 全勾）：① **supervisor-recovery.test.mjs 补齐至 WI31 门**——文件 **18 例**（1954-2 份额 9 + 本 plan 份额 9）超额覆盖门清单（过期 claim 回收 / resume-or-redispatch（1954-2）+ 停滞指纹 N-1/N 边界 / 往返检测 / partial/blocked 显式区分恢复语境变体（R3∧停滞 → blocked 叠加 / held==0 R3 → partial order-shadow 沿 1411-3 区分矩阵）+ 重启 ring buffer 清零重积不误熔断 + 双入口交叉零单周期分歧）；plan 文本「1954-2 份额 ≥5」顺手对齐 ≥9（评审 iteration 2 cosmetic 项）。② **文档同步**——03-supervisor.md changelog M3-WI30 条（§7 执行面落地注记：指纹/活动/振荡三判定 + N/K 配置裁定 + scratch 纪律引用 + 双入口同注入，非契约变更）；CONTEXT.md 增「守夜人停滞指纹 + 往返检测」段；packaging doc 四面（状态头 M3-WI30 增量句 + src 树 stagnation.ts 条目 + test 树 supervisor-recovery 18 例增量注记 + Service Surface Supervisor bullet 增 STAGNATION/PING-PONG DETECTION 面 + Proof 串 9→18）；roadmap WI30 `[x]` 全证据指针（检测器纯函数/双入口/N-K 配置/Proof 9 例清单/验证计数 910·378·116 只增不减/零引擎 diff 零新依赖/Deferred 承 1954-2 watch-only 注记）+ 头部 Last Updated 刷新「M3 第六片」。③ **收口面全绿**：`node --test plugin/dsh/test/supervisor-recovery.test.mjs` **18/18**；`pnpm --prefix tools/mission-driver test` **910/910**；`./verify-age.sh` **L1+L2+L2.5 GREEN**（插件 **378/378** / 引擎 910 / 真值表 **116/116** / policy face ok（`stagnationRounds` 新键经 gate-check --policy schema 校验）/ corpus 全 ok 含本 plan）；`node tools/mission-driver/src/mission-check.mjs missions/age-autonomy-implementation.json .` valid exit 0；`node tools/mission-driver/src/roadmap-check.mjs` passed exit 0；web typecheck + build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 双 package.json 零 diff（零新增 npm 依赖）。plan `## Verification`/`## Closure` 由本 run 后续 BUILD_VERIFY/CLOSURE_AUDIT 步写入（completed 派生禁写，01 §5.2）；Deferred「TTL 未到期死会话 claim 提前回收」保持 watch-only residual（停滞指纹兜底已就位，Successor M5/WI37 评估）。

- **M3-WI30 1954-3 Phase 1 收口：停滞指纹 + 往返检测 + N/K 阈值策略配置**（plan `docs/plans/age-autonomy/2026-08-26-1954-3-m3-wi30-stagnation-pingpong-detection.md` Phase 1，四 Decision + Add + Proof 全勾）：① 新增 `plugin/dsh/src/supervisor/stagnation.ts` 纯函数检测器——`computeLedgerFingerprint`（排序后 (planPath → basisHash) 行 + roadmap 全文 hash 聚合 sha256；**指纹域 = basisHash 域**（frontmatter + Phase + Closure Findings）——DRR/Verification/Closure 追加在域外、dispatch/pass 行不重置指纹，测试语义钉住）+ `observeStagnation(state, input)` 纯转移（停滞腿：指纹不变 ∧ 活动窗口（默认 = 一个心跳周期）内 noteActivity 零命中 → 轮数 +1，**活动信号必须参与**——有活动轮清零重积，长任务未落盘不误判 03 §7 字面；往返腿：per-plan 状态采样连续交替翻转计数，两态间 ≥ 2K 翻转（K = max(2, floor(N/5)) 完整往返，同键派生无第二配置键）无终态进展 → **饱和注入** `{rounds: threshold, threshold}` → 同一 R4 出口（03 §7「停滞检测收口」单出口纪律）；第三态破对重计 / 终态 disposition 重置 / 离场 plan 清行；threshold < 1 = 检测器关闭）；承载 = watchdog 持有内存 scratch 态（03 §6 归零成文接受——重启重积保守向）。② **双入口同注入（1411-3 契约保持 + 评审阻塞项修复）**：检测器输出为 watchdog 持有单点 `stagnationFact`——周期末端求值（既有入口）与 exec-arm `forwardTerminalDecision` 声明面入口（经新 `ExecArmOptions.stagnationFact` 回调透传）同读同一状态，非仅周期入口；跨入口时序差 ≤ 一周期由幂等重评收敛，交叉用例钉住零单周期分歧。③ **N 阈值策略配置**：`tools/mission-driver/src/law-policy.mjs` `LIMITS_FIELDS` 增 `stagnationRounds`（非负整数校验免费获得）+ `resolveStagnationRounds(policy, missionConfig)` 双源（policy 权威 / mission flow 回退 / 双缺默认 10——30s 心跳 × 10 ≈ 5 分钟墙钟换算成文；0 = 检测器 off）；`missions/autonomy.policy.yml` limits 行 + 头注墙钟换算句；`flows/mission-driver.json` 顶层回退键 `stagnationRounds: 10`（镜像 maxFailures 先例，引擎惰性数据键）；host-adapter `MissionLawContext.stagnationRounds` 透传；tools+assets 双副本经 build-bundle 同步（freshness content-equal）。④ 接线面：`watchdog.ts` 周期后置面（熔断 → 新扫描 → **检测器求值 → R1–R4 注入**——fact 首次非空落 `stagnation-detected` 观察回执（fingerprint/ping-pong 双措辞），终态沿既有 setTerminal 回执链零终态面改动）；exec-arm 声明面注入同源。⑤ Proof：`plugin/dsh/test/supervisor-recovery.test.mjs` 扩至 **18 例 0 失败**（WI30 份额 +9：指纹聚合确定性 + 域钉住（DRR/Verification/Closure 追加不动 / status 翻转·勾选·Closure Findings·roadmap 变更动）/ 停滞 N-1/N 边界 e2e（R4 blocked + stagnation-detected + run-terminal 回执）/ 活动信号参与正例（推进时钟 + noteActivity 清零重积）/ 往返检测 unit 边界（K 派生矩阵 + <K/=K/第三态/终态重置/threshold 0 inert）+ e2e oscillation（auditRounds=0 隔离 R1–R3——纯 hash 盲腿）/ 重启清零（fresh mount 不误熔断、满 N 重积）/ 双入口交叉（声明面与周期末端同词 blocked + 无 fact → 声明 deferred）/ partial/blocked 区分恢复语境变体（R3∧停滞 → blocked 叠加 / held==0 R3 → partial order-shadow，沿 1411-3 区分矩阵经声明面）/ `resolveStagnationRounds` 六态矩阵 + schema 面（负数/非整数 deny、未知键仍 deny））。执行期修正两处：test rig threshold 硬编码（threshold 0 用例误用 10）；冻结时钟下 noteActivity 恒在窗（活动用例改推进时钟——活动老化出窗语义保真）。验证：`node --test plugin/dsh/test/supervisor-recovery.test.mjs` **18/18**；`npm --prefix plugin/dsh test` **378/378**（369 基线 + 9 新；tsc 干净 + freshness 42 文件 + smoke ok）；`pnpm --prefix tools/mission-driver test` **910/910**（law-policy 真实例 limits 断言随新键更新 {maxAuditRounds:3, maxFailures:3, stagnationRounds:10}）；web typecheck + build 绿（dist 零漂移）+ lint:prompts OK；`git diff --stat tools/mission-driver/src/engine.js` 为空（零引擎 diff）+ 零新增 npm 依赖。
