# 2026-07-28

### 新增 consumer 维护指南 AGE-FILES-GUIDE.md（install 88 文件总览 + A-F 维护分类）

- 背景: 用户反馈 87 个 manifest 文件混排 template/ 与 docs/ 两源、无 consumer 视角的维护指引；需让 consumer 一看就懂"拿到什么、怎么维护"
- 改动:
  - 新增 `template/install/AGE-FILES-GUIDE.md`（~135 行英文）：6 类维护分类（A 引擎基建 7 / B 一次性配置 2 / C onboarding 填 12 / D 需求设计填 9 / E 即装即用 58 / F 运行时产物），每类列文件 + 维护动作 + 时机；含 Quick Start 4 步、At-a-Glance 表、engine 路由机制说明、template 升级 sync 指引
  - `install-age.manifest` install 区加条目 `template/install/AGE-FILES-GUIDE.md > docs/references/age-files-guide.md`（install-only artifact，落 consumer 的 references 区，符合 docs ownership）
- 自我引用修正: 指南自身是第 88 个文件 → 全文数字统一为 88（5 处：开头/What You Received/Methodology guides count 10→11/E 总数 57→58/flow 路由段），Category E 列表加 age-files-guide
- 验证: `readManifest` 88 entries ✓；实跑 install age-test4 88 copied / 0 skipped ✓；`grep` 复制出的文件 5 处数字一致（88/88/11/58/88）✓
- 设计依据: 指南放 template/install/（install-only，mission-driver 自身不需要）→ docs/references/（consumer 技术参考区）；与 demo-roadmap 同模式（install-only artifact install 到 docs/ 子目录）

### project-context.md 加 Companion Context Files 路由段（治本 consumer 断链）

- 问题: flow prompt 全局只硬编码读 `{{contextDir}}/project-context.md` 一个 context 文件（`draft-from-roadmap.md` / `plan-review.md` / `health-check.md` 共 3 处）；`ai-autonomy-policy.md` / `codebase-map.md` 在 flow prompt 里**完全无引用**。template 靠 AGENTS.md 的 "Read This First" 段间接路由——但 consumer 自定义 AGENTS.md 后该段常被去掉（cardlite-acq-feature-test 实例验证：定制 §0-§12 门控合约，无 Read This First，导致这两个文件 flow 完全读不到）
- 诊断: 引擎无 contextFiles/extraContext 注入机制（grep 零命中）；flow 只读 project-context.md 是硬编码；project-context.md 是 flow + consumer AGENTS.md 双重必读的咽喉 → 挂载点应在此
- 改动（low-risk no-plan path，1 文件 / +9 行）: `template/docs/context/project-context.md` Purpose 段后、Project Identity 前加 `## Companion Context Files` 段，显式列出 ai-autonomy-policy.md / codebase-map.md / source-of-truth-and-precedence.md + "When to read" 列。原文件第 21/64/72 行的零散规则引用保留（不冲突，新段升级为显式读取指针）
- 同时在 cardlite-acq-feature-test 目标项目 `docs/context/project-context.md` 加了对应中文「AI 上下文关联文件」段（consumer 侧临时修复，已填项目信息；template 侧本次改动让未来 consumer 自动获得）
- 验证: 实跑 `node tools/install-age.mjs %TEMP%/age-test2 TestProject2` 87 copied / 0 skipped ✓；`grep Companion` 复制出的 project-context.md 含完整段 ✓
- 设计依据: 把 template AGENTS.md "Read This First" 的挂载点平移到 project-context.md（flow 唯一硬编码必读的 context 文件），契合 consumer "project-context.md 作为唯一入口"的常见定制哲学

### install manifest 补 mission-driver operator skill

- 问题: `.opencode/skills/mission-driver/`（operator skill，驱动 engine dev loop 的 SKILL.md + 两份 references）此前不在 `install-age.manifest` 中，consumer 项目装出来缺这个 skill
- 改动（low-risk no-plan path，1 文件 / 4 行）: `install-age.manifest:37-40` 加新 section `# --- .opencode/skills/mission-driver (operator skill for the engine, ALL) ---` + 3 个条目（SKILL.md / references/mission-config-schema.md / references/roadmap-template.md）
- installer 无需改动 —— 现有 `install-age.mjs` 已支持任意相对路径（src=dst）、自动 `mkdirSync(dirname(dst), recursive)`、skip-existing；SKILL.md 无 `__REL_MDH__`/`<project-name>` 占位符 → 原样复制即可
- 验证: `readManifest` 解析 3 条目 flags=[] src=dst ✓；实跑 `node tools/install-age.mjs %TEMP%/age-test TestProject` 三文件全部 `+` copied，`diff -q` 与源字节一致（18962 / 13081 / 12551 B）

### 修复 runner.js 5 个既有测试 fail（556/556 full green）

- `tools/mission-driver/src/runner.js` 两处 contract drift（mdr-3 Phase 2/3 设计与实现不一致）:
  1. **`DEFAULT_DRIVER_ARGS` 缺 `{session}` token**（`:22`）→ `sessionId.replace("{session}", ...)` 是 no-op，`--session` 从未注入。`indexOf("--session")` 返回 -1，`args[0]="run"` 被误当 session 值。修: 模板末尾加 `{session}`。
  2. **`promptMode` 默认 `"arg"` 而非 `"stdin"`**（`:26`）→ prompt 作为 positional CLI arg 传递（撞 Windows 32K CreateProcess 上限）。mdr-3 Phase 2 设计要求默认走 stdin。修: 默认改 `"stdin"`。
- 验证: `pnpm md:test` exit=0 —— `node --test` 556/556 pass / 0 fail + `prompt-check: OK`
- 此前基线 07-27: 549 pass / 7 fail → 07-28 Phase 1 后: 551/5 → 本次修复后: **556/0 full green**

### Phase 2+3 落地: heredoc 物化 + manifest 扩展 + 安装器 Node 化

- Plan: `docs/plans/2026-07-28-0900-template-internal-workspace-and-node-installer-plan.md`（Phase 2+3，`active`→待 closure）
- **Phase 2** (heredoc 物化 + manifest 扩展):
  - 新增 `template/install/` 下 6 个真文件（shim / .env.example / missions/{base,demo,onboarding}.json / demo-roadmap.md）+ README.md 说明 consumer 版 vs dogfood 版差异
  - `install-age.manifest` 加格式说明 + 6 个 `> dst` / `:: flags` 条目
  - `install-age.sh` manifest 解析扩展支持 `> dst` + `:: flags`（exec/fill/rel-mdh）；删 §2/§3/§4/§4b heredoc；422→259 行
  - 验证: `check-install-age.sh` 10/10 PASS + 手动验证 shim `-rwxr-xr-x` + `.env.example` rel-mdh 插值正确
- **Phase 3** (安装器 Node 化):
  - 新增 `tools/install-age.mjs`（零依赖 Node ESM，仅 `node:*` 内置模块；~240 行；JSDoc 类型；导出 parseManifestLine/applyFlag/ensureGitignoreEntry/installAge 供测试）
  - `install-age.sh` 退为 thin shim（功能 2 行 + 注释共 16 行）
  - 新增 `install-age.cmd`（Windows 原生入口，无需 Git Bash）
  - 新增 `tools/install-age.test.mjs`（15 用例 / 3 suite: manifest 解析 + flags 应用 + gitignore）
  - `tools/package.json` 加 `install-age:test`
  - 验证: `pnpm install-age:test` 15/15 pass + `check-install-age.sh` 通过 shim→.mjs 链 10/10 PASS + 直接 `node tools/install-age.mjs` copied 84 files 成功
- 跨平台收益: Windows 用户不再需要 Git Bash（`install-age.cmd` 或 `node tools/install-age.mjs` 直接可跑）；REL_MDH 计算从 `node -e` 子 fork 改为 `path.relative` 直调
- 后续: closure audit（独立 subagent）→ Plan Status → completed

### Phase 1 落地: tools/ 升为 pnpm workspace

- Plan: `docs/plans/2026-07-28-0900-template-internal-workspace-and-node-installer-plan.md`（Phase 1，`active`）
- 改动:
  - 新增 `tools/pnpm-workspace.yaml`（成员: `mission-driver`, `mission-driver/web`）
  - `tools/package.json`: pin 升 `pnpm@10.0.0` → `pnpm@10.27.0`；加编排 scripts `md:test` / `md:web:build` / `md:web:check:dist`
  - `tools/mission-driver/web/package.json`: 删 `packageManager` pin（继承根）
  - 删 `tools/mission-driver/web/pnpm-lock.yaml`，合并为单一 `tools/pnpm-lock.yaml`
  - `.github/workflows/web-dist-check.yml`（B2 同步）: `working-directory` web/ → tools/；`cache-dependency-path` → `tools/pnpm-lock.yaml`；`pnpm build` → `pnpm --filter mission-driver-monitor-web build`；`git diff -- dist` → `git diff -- mission-driver/web/dist`
  - `tools/README.md`: 加 "Workspace Layout" 小节 + Common Commands 补 `md:*`
  - `web/dist` 刷新（workspace hoist 改变模块解析 → vite chunk hash 变化，功能等价；已 stage）
- 验证（green baseline）:
  - `cd tools && pnpm install`: 单条装齐，单一 lockfile 生成 ✓
  - `pnpm md:test`: 551 pass / 5 fail（**既有** opencode sessionId `ses_main` vs `run` 环境问题，基线 07-27 是 549 pass / 7 fail → 无新回归，反而好转）+ `prompt-check: OK`
  - `pnpm md:web:check:dist`: exit=0 ✓（dist 已刷新匹配新构建拓扑）
  - hash 确定性: `index-Bswk0w89.js` 两次构建一致 ✓
- Decision（.nvmrc）: 保留 `tools/mission-driver/web/.nvmrc` 原位（CI `setup-node` 硬编码 `node-version: 20`，.nvmrc 仅本地提示；最小改动）
- 推翻了 `2026-07-24-1030 Decision 2`（"彻底不引入 workspace"）—— 保留其"不引入根 prepare 自动 build"内核（committed dist 守卫前置不变，workflow 已同步更新）
- 后续: Phase 2（heredoc 物化 + manifest 扩展）、Phase 3（install-age.sh → tools/install-age.mjs）待执行
