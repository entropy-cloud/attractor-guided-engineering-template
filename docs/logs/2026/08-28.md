# Development Log - 2026-08-28

### 2026-08-28

- **multi-plugin-dsh M4-WI16 nop-route e2e 收口：in-process cordis runtime boot + 真四路由调用 + 真实错误样本 + 决策回放（plan `docs/plans/multi-plugin-dsh/2026-08-28-1312-3` Phase 1 全勾，EXEC_PLANS）**：① **fixture 最小面 Decision**——`plugin/nop-route/test/fixtures/e2e-noproute.cordis.yml` 单行组合（nop-age `test/fixtures/e2e.cordis.yml` 先例同裁定缩小行集）：service row `name: ../../src/service.ts` 相对解析指真 service 模块（Node ≥23.6 type-stripping，本机 v25.3.0）、**非 isolate root realm**〔fixture 归驱动进程所有，`ctx.get('noproute')` 必须可解析；真宿主 isolate 走 cordis.patch.yml，1312-2 四腿 dump 证据不受影响〕、**无 webServer 行**〔headless 降级即被 e2e 断言的面，不是挂载前提〕、**无模型行**〔零模型调用零凭据→e2e 环境无关，无 env gate 即可跑；nop-age e2e 的 stub SSE 模型端点整条省略〕、boot() 零基础行需求〔nop-age 15 行组合是 mdcontrol 依赖面（llm/bash/agent-spine/sessions…），noproute 零强制 inject——webServer reactive inject 缺席即休眠〕；备选（含 webServer 行兼证 HTTP 面）否决——`/noproute/api` 注册形状已由 noproute-routes.test.mjs stub 面钉死，重复增面无新信息且 fixture 更脆（watch-only residual 留档 plan §Deferred，重开触发 = 真实 `/noproute/api/*` 消费方出现）。② **驱动脚本**——`scripts/e2e-noproute.mjs`（`npm run verify:e2e` 接线）：`@deepseek-ai/dsh-app-boot` `boot("noproute-e2e", fixture, [], prepare)` 拉起真 cordis runtime（prepare 里挂 cordis logger exporter 捕获 `noproute` 名下 log 行）→ `ctx.get('noproute')` 解析断言〔service + 四路由函数 + resetHistogram 暴露〕→ 真四路由调用：**classify** 8 真实形状样本全 ErrorClass 覆盖〔`ECONNRESET` code/errno/syscall、`ETIMEDOUT`、429+`Retry-After` 头行、401 invalid_api_key（消息内凭据 = `sk-REDACTED` 占位，零真凭据）、400 invalid_request_error、402 insufficient_quota、partial 含未闭合 `<AI_STEP_RESULT>`、无任何已知标记 generic→`unknown` 兜底〕；**route** 10 调用〔样本全集 attempt 0 + 两参数腿：历史 taint→Fallback〔fromModel default→fallback[0]〕/ 预算耗尽全 taint→Give-up(max-retries)@attempt 3〕——四类 RoutingDecision 各 ≥1 命中〔retry/fallback/transform/give-up〕+ retry/fallback 决策 nextModel 非空逐条断言 + 判别细节钉死〔裸 ECONNRESET→retry/backoff/1000ms/nextAttempt 1、429→retry-after 7000ms 优先于曲线、partial→transform extractedMarker="pass"、401→give-up non-retryable〕；**pick-model** 三面〔默认 `{}`→default/null effort/8192 预算、default taint→fallback[0] source "fallback" fallbackIndex 0、历史感知双面：链尾全 taint→链头+historyExhausted true、最近 outcome 恢复〔failure 后 success→untaint〕〕；**health**〔version=package.json version 对账、fallbackChain=[default,+fallbacks]、maxRetries、errorHistogram 与本 run classify/route 调用计数**精确对账**（replay 双倍计入，程序化 tally 非硬编码）+ `resetHistogram()` 复位前快照 deep-equal/归零 `{}`/复位后再累加 1〕；**决策回放**——classify+route 全部 payload 序列化落盘（scratch mkdtemp JSON）→ `readFileSync` 重载 → 二次调用，**18/18 决策 bit-identical**（`JSON.stringify` 逐条比对；确定性合同从纯函数真值表面升级到真 runtime 面证据）；**headless 降级**——exporter 捕获 `nop-route mounted` mount log 行〔package `nop-route`/realm `nopRoute`/service `noproute` 字段对账〕+ `webServer service absent` 降级行，服务全程可解析（结束前复测）；结束 `ctx.fiber.dispose()` 干净 shutdown（失败腿保 scratch 供调试，成功腿清理）。③ **`verify:e2e` 不入 verify-age.sh L2 Decision**——理由：ⓐ roadmap WI18 收口定义本身不含 e2e（L1+L2+L2.5 = 引擎 + 插件测试 + 真值表）；ⓑ 与 nop-age `verify:e2e` 本地脚本 gate posture 家族对称；ⓒ 真 cordis runtime boot 是重腿（进程内组合拉起），CI 面保持纯确定性；备选（环境无关故入 L2）否决——runner 时长与 flake 面换不来新断言（真值表已覆盖确定性）；残险 = e2e 回归不会被 CI 拦截，以 roadmap WI16 行内证据指针 + 本 log 记录补偿，**M5-WI18 收口时显式复跑**。④ **依赖增补**——package.json devDep `@deepseek-ai/dsh-app-boot: 0.1.1-rc.2`（对齐 nop-age pin；npm 自动装齐其 peers：cordis-plugin-loader/group/include、dsh-home-paths/invariants/launch-environment/system-prompt）+ `verify:e2e` script + lockfile 更新（bundle-scaffold 五结构断言零冲突——test 链形状未动）。**Proof**：`npm --prefix plugin/nop-route run verify:e2e` **exit 0**〔逐面计数：classify 8 / route 10〔4 类全命中〕/ pick-model 4 / replay 18 bit-identical / health 3 / headless 2 / clean shutdown；执行期一修：equal 助手改 canonical（递归排序键）JSON——histogram snapshot 按键排序，样本序期望对象键序不同，纯比较器问题非断言面变化〕；`./verify-age.sh` **L1+L2+L2.5 全门 GREEN**〔引擎 990/990 + nop-age 423/423 + nop-route 97/97 + launcher 18/18 + 真值表 119/119；新 devDep 不扰既有链〕；`git diff --stat tools/mission-driver/ plugin/nop-age/` **空**〔引擎与 nop-age 零 diff 自证〕；web `typecheck` + `build` 绿〔dist 零漂移〕+ `lint:prompts` OK；dev guide §nop-route e2e 用法行在案；roadmap WI16 `[x]` + 行内证据 + `> Last Updated` 头同步，`roadmap-check` exit 0〔余 `- [ ]` 恰 2 = WI17/WI18〕。

- **multi-plugin-dsh M4-WI13+WI14+WI15 nop-route 编排 + 路由面 + service 挂载 + manifest 增补（plan `docs/plans/multi-plugin-dsh/2026-08-28-1312-2` 三 Phase 全勾，EXEC_PLANS）**：① **Phase 1 WI13 routing-core**——`src/routing-core.ts` pure `decide(request, config, now=0): RoutingDecision`（导出 `RoutingDecision`/`RouteRequest`/`RoutingConfig`/`TransformErrorObject`）：编排只做策略合成与优先级，判别零重复〔classify / retryDecision / pickModel 三纯模块各自被独立调用〕——partial:marker 最先 → **Transform**〔提取未闭合 `<AI_STEP_RESULT>` payload 成结构化 transform 对象 `{code:"partial-marker", extractedMarker, message, partial:true}`〕→ retryDecision 重试预算 → pickModel 历史感知选模：**Retry**〔同模型退避，retry-after 优先于曲线；全链 taint 时 retry 链头 + `historyExhausted`〕/ **Fallback**〔failed 模型被历史 taint（最近 outcome=failure）→ 下一未 taint 链内模型；或预算耗尽但有链内替代 → delayMs 0 即时换模〕/ **Transform** / **Give-up**〔permanent:*/unknown 原样、预算耗尽且无替代（reason non-retryable|max-retries 透传）〕；history 语义 = failed call **之前**的历史（含本次会塌缩 Retry→Fallback——测试前提修正 3 处：空历史 maxRetries 无替代即 give-up、裸 retry-after 按 classifier 规则= rate-limit 非 unknown、exhausted 链头=preferred 锚即 failed 模型自身→Retry）；零状态/零墙钟〔时间只走 `now` 入参默认 0〕/零随机；`test/routing-core.test.mjs` **16 例**全绿〔4 决策类各 ≥2 + 边界：maxRetries 走向 / Transform-Give-up 分界 / transient × 链在库-耗尽 / 双跑 bit-identical〕；**确定性 grep**〔`Date.now|Math.random|new Date|process.env|readFileSync|writeFileSync`〕于 src/routing-core.ts **零命中**。② **Phase 2 WI14 路由面**——`src/noproute-routes.ts`：`createNopRouteRoutes({config, histogram?, logger?})` 返回 wire-method 全名 record 四 handler **全 sync**〔`noproute.route`=decide / `noproute.classify`=classify / `noproute.pick-model`=pickModel / `noproute.health`=version 0.1.0 + fallback 链（default+fallbacks 去重保序）+ 直方图快照〕+ `registerNopRouteHttpDispatcher(ctx, routes, logger?)`〔`ctx.get('webServer')` 缺席=降级 log 行非失败返 null；在场=`/noproute/api` prefix 注册——mdcontrol-routes 同形：POST-only 405 / 未知方法 404 / wire error+坏 JSON 400 / 内部错 500、`{ok:true,value}`|`{ok:false,error:{code,message}}` envelope、1MiB body 上限〕；参数校验结构化 `NopRouteError`〔缺 `error` key / `attempt` 非负整数 / `history` 条目 `{model,outcome}` 形状 / `reasoningEffort` 枚举 / `expectedTokens` 正数〕；`createErrorHistogram()`〔record/snapshot 键排序稳定/reset 返回复位前快照〕= 注入的 service 层状态（pick-model 不计数）；config 无 defaultModel → 工厂 fail-fast；`test/noproute-routes.test.mjs` **17 例**全绿〔四方法正例（sync 断言）/ deny 形状 / dispatcher 注册形状+envelope 全面 / 缺席降级 / 直方图读写注入面〕；Proof：`npm --prefix plugin/nop-route test` 88/88 + `./verify-age.sh` **L1+L2+L2.5 GREEN**〔990+423+88+18+119〕+ `git diff --stat tools/mission-driver/` 空〔引擎零 diff 自证〕；执行修正：tsc 腿三处 RetryAction 联合类型窄化〔`nextAttempt ?? attempt+1` / source·reason 运行期窄化表达式，零行为变化〕。③ **Phase 3 WI15 挂载 + 两 Deferred 收编**——`src/service.ts` `apply(ctx, config)`：cordis `Service` 子类发布 **`noproute`**〔注册名去 `nop-` 前缀 camelCase，设计 §Naming Convention；`nop-age`→`mdcontrol` 为稳定性例外〕挂 wire record + 直方图〔route/classify 累加、health 读取、`resetHistogram()` 复位语义随 service〕+ `ctx.inject(['webServer'], …)` 可选 HTTP 面〔挂点 webCtx 注册 + disposer 经 `webCtx.effect`；缺席 = headless 降级 mount-log 行非挂载失败，cordis 服务照常发布〕+ mount log 落 `package:"nop-route"`/`realm:"nopRoute"`；**不 inject `agents`**（零 dispatch 纪律）；`test/service.test.mjs` **9 例**全绿〔stub ctx 直调 apply：发布形状（reflect.provide 单条 name=noproute + 四路由 + 直方图）/ mount log / headless 降级 + inject 挂点（webServer 在场→prefix 注册 + effect）/ 直方图累加-读取-复位三面 / 零 agents inject〕。**M3 Deferred 收编**：`plugin/plugin-manifest.yml` 增 nop-route 条目〔path `./nop-route`、realm `nopRoute`、config 镜像 bundle patch 三元组 `defaultModel/maxRetries/fallbackModels`〕+ 分期注记更新（声明全部在库 bundle）+ 头注 M4-WI15 溯源。**真宿主四腿**（M3-WI8 同姿势，scratch profile `nop-route-audit`，dump 面用 M3 as-built 合法 CLI 形 `dsh --profile <p> --dump-config`——plan 字面 `dsh web --no-open --profile` 为已裁定非法形）：① `--dry-run`（PROJECT_ROOT export）双 add 计划零执行〔`plan: dsh plugin --profile web add "link:…/nop-age"` + `…/nop-route`，`dry-run: 2 plugin(s) planned, 0 executed`〕；② `--no-start` 挂载 mounted:2 → dump 338 行 L314-338 **双 realm 并存**〔`# == nop-age` + `isolate: { nopAge: true }` + `# == nop-route` + `isolate: { nopRoute: true }` + `id: nop-route-service` + config 三元组逐字段零漂移〕；③ 二次执行全 already-present（mounted:0 / already-present:2）；④ `--unmount-all`（removed:2）→ 重挂（mounted:2）→ 两次 dump `diff` **为空**（端态一致）；验毕清理：unmount-all + `dsh plugin --profile nop-route-audit list` 空 + `~/.dsh/profiles/nop-route-audit` 目录删除，`web` profile 未触碰。**M2 Deferred 半边收编**：`docs/architecture/dsh-plugin-packaging.md` / `docs/design/dsh-plugin-integration.md` / `docs/process/dsh-plugin-development-guide.md` 各增 §nop-route Plugin/Bundle as-built 段〔目录形状 / realm `nopRoute` / 服务名 `noproute` / 四 sync 路由 / 零宿主调用纪律 / headless 降级 / 直方图状态边界 / 挂载与验证面；完整两 bundle 结构性改写注记归 M5-WI18〕+ dev guide §Unified Launcher「nop-age only」句翻 landed + 三 doc 前向引用段同步。**收口 Proof**：`npm --prefix plugin/nop-route test` **97/97**〔scaffold 5 + error-classifier 20 + retry-policy 16 + model-selector 14 + routing-core 16 + noproute-routes 17 + service 9〕+ `./verify-age.sh` **全门 GREEN**〔引擎 990/990 + nop-age 423/423 + nop-route 97/97 + launcher 18/18 + law 真值表 119/119 + policy/corpus 30 plan ok〕+ web typecheck/build 绿 + lint:prompts OK + `git diff --stat tools/mission-driver/ plugin/nop-age/` 空〔引擎零 diff + nop-age 零触碰〕；roadmap WI13/WI14/WI15 `[x]` + 行内证据 + `> Last Updated` 头同步 + `roadmap-check` exit 0〔unchecked 6→3：WI16/WI17/WI18〕。**后继指针**：WI16 e2e（in-process boot + 真四路由调用）归 `2026-08-28-1312-3`；M5-WI17 联合挂载语义断言（AGE preset 零服务行、技能三 ID、mdcontrol 唯一）+ M5-WI18 全门收口 + owner docs 完整结构性改写。

- **multi-plugin-dsh M4-WI9+WI10+WI11+WI12 nop-route 脚手架 + 纯决策核心三模块（plan `docs/plans/multi-plugin-dsh/2026-08-28-1312-1` 四 Phase 全勾，EXEC_PLANS）**：① **Phase 1 WI9 脚手架 + L2/CI 接线**——`plugin/nop-route/` 六件套落档：package.json〔`private` / `type: module` / engines `^22.19 || >=24` / `dsh.bundle.patch: ./cordis.patch.yml`；**依赖最小化 Decision**：deps 仅 `@deepseek-ai/cordis@4.0.1`，devDeps 仅 typescript/@types/node/yaml（零宿主调用纪律，否决照抄 nop-age 全量 dsh-* deps）；无 `main`/`exports`——boot import 缺口 = 跨两 bundle 独立后继项，本 plan 不单方引入〕、package-lock.json〔`npm install` 生成 + `rm -rf node_modules` 后 `npm ci` 干净重装实测 exit 0〕、cordis.patch.yml〔`- insert:` → `cordis:group` + `group: true` + `isolate: { nopRoute: true }`〔§Isolate Realm Convention〕→ service row `id: nop-route-service` / `name: nop-route` + config `defaultModel: zhipuai-coding-plan/glm-5.2` / `maxRetries: 3` / `fallbackModels: [zhipuai-coding-plan/glm-4.6]`（对齐设计 §Plugin Manifest 例）〕、tsconfig.json〔nop-age 逐字段对齐：strict / noEmit / allowImportingTsExtensions / allowJs / include `src/**/*.ts`〕、scripts/check-manifest.mjs〔三段结构校验原样适配：isolate 期待 truthy `nopRoute`、service row 期待 `nop-route-service`/`nop-route`、+ routing 配置三键校验〕、test/bundle-scaffold.test.mjs〔5 例：manifest 形状 / 依赖最小面 + 无 main/exports / patch 形状 + 配置值 / tsconfig 在库 / scripts 链完整且无 build-bundle/smoke-import 腿〕；test 链 = `check-manifest → node --test test/*.test.mjs → tsc --noEmit`（无 assets 两腿）；**执行注记**：tsc 腿在 src 空时 TS18003——按 nop-age WI6 skeleton 先例以 `export {}` 占位种子过 Phase 1 门，Phase 2 即被真模块覆写（终态零残留）；`verify-age.sh` L2 增腿〔on-demand `npm ci --prefix plugin/nop-route` + `npm --prefix plugin/nop-route test`，镜像 nop-age 段〕+ `.github/workflows/age-ci.yml` push/PR 两处 paths 增 `'plugin/nop-route/**'`。② **Phase 2 WI10**——`src/error-classifier.ts` pure `classify(error): ErrorClass`：7 分类 + `unknown` 兜底，值集逐字对齐设计 §nop-route 路由表〔**口径对齐注记**：roadmap WI10 行文「7 种 ErrorClass」= 7 分类 + unknown 兜底 = 设计表 8 值，本 plan 按设计口径执行，回写 roadmap 行已注记〕；判别规则模块内成文：partial:marker 最先〔`partial === true` 或文本字段含未闭合 `<AI_STEP_RESULT>`——闭合成对 = 完整输出非 partial〕→ 六类固定优先级 walk〔rate-limit→timeout→network→auth→budget→invalid-input；类内 status > code > name > message 形状；code 归一化 lowercase + `-`/空格→`_`〕、裸 retry-after〔无他信号〕兜底 rate-limit、非对象/空对象/未知 code 落 `unknown`；`test/error-classifier.test.mjs` **20 例**全绿〔7 分类 + unknown 各有正例、空/非对象/未知 code 边界、三组同形冲突优先级〔429+ECONNRESET→rate-limit / 401+invalid_request→auth / 408+ECONNREFUSED→timeout〕、code 归一化、Headers-like get 面、Error 实例 message 面、双跑 bit-identical〕。③ **Phase 3 WI11**——`src/retry-policy.ts` pure `retryDecision(error, attempt, config, now=0): RetryAction`（导出 `RetryAction`/`RetryConfig`）：不可重试类短路〔permanent×3 / partial:marker / unknown，含带 retry-after 时仍短路〕→ maxRetries 边界〔0-based attempt，attempt ≥ maxRetries 即 give-up；maxRetries 0 → 立即〕→ retry-after 数值秒〔`retryAfter` 字段或 headers 行；HTTP-date 形出确定性合同域〕优先于曲线且不被 maxDelayMs 封顶〔server 指令胜〕→ 曲线 `min(baseDelayMs · backoffFactor^attempt, maxDelayMs)` 默认 1000/2/30000ms；时间只走 `now` 入参（默认 0 不读墙钟，fake clock 测试侧注入）；`test/retry-policy.test.mjs` **16 例**全绿〔maxRetries 0/中位/达到、曲线采样 1000→2000→4000→8000 + attempt5/9 封顶 30000、自定义 base/factor 500·3²=4500、retry-after 120s/Headers "45"/不封顶/非数值回曲线、非重试短路六输入、attempt 单调 + retryAt 严格递增、give-up 形状、双跑 bit-identical〕。④ **Phase 4 WI12**——`src/model-selector.ts` pure `pickModel(request, history, config): ModelSelection`（导出 `ModelSelection`/`ModelRequest`/`ModelHistoryEntry`/`ModelSelectionConfig`/`ReasoningEffort`；面 = model id + reasoningEffort override + expected token budget，对齐设计路由表）：候选链 `[preferredModel?] → defaultModel → fallbackModels[]` 去重保序、历史感知 = 每模型**最近一次** outcome 为 failure 才 taint（早败后胜不污点；off-chain 模型忽略）、链尾全 taint 回链头 + `historyExhausted: true`〔不发明链外模型〕、reasoningEffort 请求透传（缺省 null）、expectedTokenBudget = request.expectedTokens → baseTokenBudget → 8192 + maxTokenBudget 钳制；`test/model-selector.test.mjs` **14 例**全绿。**确定性合同（三模块自证）**：grep 清单 `Date.now | Math.random | readFile | writeFile | fetch( | require( | process.env | process.argv` 于 `plugin/nop-route/src/` **零命中**；三真值表各含同输入双跑 bit-identical 断言。**Proof**：`npm --prefix plugin/nop-route test` exit 0〔**55/55** = scaffold 5 + error-classifier 20 + retry-policy 16 + model-selector 14，三真值表各 ≥10 实测〕；收口 `./verify-age.sh` **L1+L2+L2.5 GREEN**〔引擎 **990/990** + nop-age 423/423 + nop-route 55/55 + launcher 18/18 + law 真值表 119/119〕；web typecheck/build 绿 + lint:prompts OK；`git diff --stat tools/mission-driver/ plugin/nop-age/` 为空（引擎零 diff + nop-age 零触碰）；roadmap WI9–WI12 `[x]` 回写 + `> Last Updated` 同步 + `roadmap-check` exit 0〔unchecked 16→6〕。Owner docs 结构性改写归 M4-WI15/M5-WI18（前序 plan Deferred 裁定），本批次 No owner-doc update required；`plugin/plugin-manifest.yml` 未动（nop-route 条目增补归 M4-WI15 挂载面 plan `2026-08-28-1312-2`）。

- **multi-plugin-dsh M3-WI6+WI7+WI8 manifest + load-plugins.sh 统一启动器 + 双层验证（plan `docs/plans/multi-plugin-dsh/2026-08-28-0149-3` 四 Phase 全勾，EXEC_PLANS）**：① **Phase 1 manifest 落档**——`plugin/plugin-manifest.yml`：顶层键白名单 `schema`/`profile`/`plugins`（未知键 fail-fast 预检执法）、恰 `nop-age` 一条〔path `./nop-age`、realm `nopAge`、config 镜像 live bundle patch：`assetsDir: ./assets` + `supervisor.projectRoot: ${PROJECT_ROOT}` + `continuous: false`〕、`${VAR}` 语义 = 已定义替换/未定义预检报错退出（静默空串否决——防 projectRoot 静默错位）、M1 分期注记（nop-route 条目随 M4-WI15 增补）；双通道语法验证：python3/PyYAML `safe_load` exit 0 + node 通道〔`(cd plugin/nop-age && node -e 'require("yaml")…')`〕解析通过——**双通道均绿**。② **Phase 2 脚本 + 确定性测试**——`plugin/load-plugins.sh`：`#!/bin/sh` + `set -u`、7 flag 全量〔--profile/--manifest/--no-start/--dry-run/--strict/--skip 可重复/--unmount-all〕、预检四断言〔YAML 双降级：python3+PyYAML 探针优先、缺席转 node 经 nop-age pinned devDep `yaml`（cwd 在 bundle 内令 require 可解析）/ 未知顶层键 deny / `${VAR}` 全文扫描定义性（注释行剔除）+ 值替换 / 逐条目 path 存在且含 cordis.patch.yml〕、幂等〔`dsh plugin --profile P list` grep 名字查重后 `add "link:$ABS"`〕、摘要表〔mounted/already-present/failed/skipped 四类计数+清单；unmount 变体 removed/already-absent〕、退出码全成功 0/任一失败非零；**bashism 清单**（Proof 逐项）：无 `[[ ]]`、无 `local`、无 `function` 关键字、无数组、无 bash 专用参数展开——实际用集 = 内建 printf/test/while/case/for/read/cd/command-v/eval/exit + 外部 sed/grep/cat + python3|node|dsh 按通道；`sh -n` 零错；**shellcheck 0.11.0 零告警**（修幅：SC2034 未用变量 YAML_CHANNEL 移除、SC2154 eval 隐式赋值前显式置空、SC2046/SC2016 有意分词/字面 `${` 模式 disable 注记）；`plugin/test/load-plugins.test.mjs` **18/18 全绿**（plan 要 ≥12：12 面全落 + 超额 6——cordis.patch.yml 缺失 deny、node 通道失败传播、malformed YAML deny、--no-start 与两式启动形状；PATH 注入 stub dsh/python3/node，分支测试用 pruned PATH〔macOS sed/grep 在 /usr/bin〕+ PyYAML 缺席 stub 强制 node 通道）；`verify-age.sh` L2 增腿 `node --test plugin/test/load-plugins.test.mjs` 后 **L1+L2+L2.5 GREEN**（引擎 987/987 + 插件 423/423 + launcher 18/18 + policy/corpus/真值表）；web typecheck/build 绿 + lint:prompts OK。③ **Phase 3 真宿主六面**（scratch profile `nop-load-audit`，`web` profile 未触碰）——shellcheck 干净（版本 0.11.0，`brew install`）；真宿主 dry-run 输出 `plan: dsh plugin --profile web add "link:…/plugin/nop-age"` 且零 dsh 调用〔PROJECT_ROOT 未定义实测 deny：`undefined variable ${PROJECT_ROOT}`〕；真挂载 mounted:1 → `dsh --profile nop-load-audit --dump-config` L314-323 命中 `# == nop-age` / `- id: nop-age` group / **`isolate: { nopAge: true }`** / `id: nop-age-service`，二次执行全 already-present（幂等）；unmount-all（removed:1）→ 重挂 → 两次 dump `diff` **为空**（端态一致，判据 5 单插件版）；strict fail-fast：mktemp 双不存在 path 条目 manifest → 仅报首条 `nop-a`、exit 1、profile 零变更（零 add），临时文件用后删除；**起宿主腿如实留档**：设计字面 `dsh web --no-open --profile <p>` 实测为非法 CLI 形（`web` 子命令 = `--profile web` 别名、拒父级 `--profile` → unknown option），as-built 启动形 `dsh --profile nop-load-audit` 则命中 **M2-WI4 已留档的 bundle import 缺口**（`plugin/nop-age/package.json` 无 `main`/`exports`，宿主 `Cannot find package …/nop-age/index.js`）——本 plan Non-Goal 禁改 bundle，按 M2 裁定「需独立立项补包入口」列为后继项，挂载正确性以 dump 面为证；scratch profile 清理（unmount-all + list 空 + 目录删除）。④ **Phase 4 回写**——dev guide 增 §Unified Launcher: load-plugins.sh（用法/flag 表/PROJECT_ROOT 前置/manual add 保留为 fallback）+ 前向引用段翻 landed；设计文档 §Load Script 增 as-built 注记块（启动命令 CLI 形 / dry-run 摘要形状 / 双通道实现 / 测试挂点 + L2 接线 / shellcheck / 真宿主证据 + import 缺口）+ §User Experience 增 PROJECT_ROOT export + Status 头落地态刷新（M1-M3 landed）+ Changelog M3 条；roadmap WI6/WI7/WI8 `[x]` + 行内证据 + Last Updated 头；`grep -c "^- \[ \]"` = **10** 实测 + roadmap-check exit 0。**残险/后继**：bundle 包入口（main/exports）缺失致真宿主 boot import 失败——M2 同状留档、本 plan 零侵入命中，需独立 work item 补（M4/M5 真宿主联合验证前）；age-ci.yml 触发 path 不含 `plugin/load-plugins.sh`/`plugin/test/**`（本 plan Targets 外，裁定不动——verify-age.sh 变更已触发 CI，纯 launcher 改动的 CI 触发面留 M4 增补判）。

- **multi-plugin-dsh M2-WI3+WI4+WI5 nop-age 迁移 + 零回归验证 + 引用清查收口（plan `docs/plans/multi-plugin-dsh/2026-08-28-0149-2` 三 Phase 全勾，EXEC_PLANS）**：① **Phase 1 迁移执行**——`git mv plugin/dsh plugin/nop-age`（rename 谱系入 index：105×R100 纯改名 + `src/service.ts` RM〔改名+token 编辑〕，`git log --follow` 提交后可查；node_modules 未跟踪面随物理移动）；token map 全量落地：`dsh-mission-control`→`nop-age`〔package.json/package-lock.json name ×2、cordis.patch.yml service row name、age-preset.test D3 断言、agent.cordis.yml 注释〕、`missionControl`→`nopAge`〔isolate 键 + service.ts 头注〕、`mdcontrol-service`→`nop-age-service`〔config row id〕、`mission-control`→`nop-age` **仅 cordis.patch.yml insert row id 单点**、`plugin/dsh`→`plugin/nop-age`〔bundle 内 scripts/test/fixtures/src 自引用 + preset.yml 增包名 tag 注释行（display name 不动——产品名非 token，D2 allowed-keys 门保持）〕；bundle 外 8 处功能引用全更新（law-rules.mjs:1367/:1391 prefix、law-core.mjs:8、autonomy.policy.yml:124 P8 gate match〔approved-project leg：本 plan active 正文具名〕、law-policy.test.js:86、verify-age.sh ×6 行、age-ci.yml:26/:36、age-autonomy-implementation.json:4/:25、dsh-plugin.json:4、pre-commit:28）；**执行期基线修正**：plan 基线记 integration doc 零字面命中，实测 2 处（M1 前向引用段 :7 + 安装示例 :75）——按该段自载「as-built faces are rewritten at M2 close」契约一并同步（路径 + 「Until the refactor lands」句翻 landed 态），四 owner docs（CONTEXT.md 14 处 / dev guide / packaging / integration）全部零悬挂 + 各状态头一行迁移增量句；carve-out 逐条保持：`mdcontrol` 服务名 / `/mdcontrol/api` 前缀 / `mission-control-run/draft/analyze` 技能三 ID / preset 零服务行原样在库（残留 `mission-control` 命中逐条对照 = 技能 ID 及其注记，允许集内）；`missions/multi-plugin-dsh.json:4` description 的 `plugin/dsh` 为本 mission 自身范围陈述（「从单 plugin 重构为 nop-*」——与 roadmap Purpose/M2 标题同类任务域语句，非悬挂功能引用，裁定不动）。② **Phase 2 零回归**——迁移前基线实测：引擎 987/0、插件 423/0；迁移后：插件套件 **423/423 / 0 fail**（check-manifest + unit + tsc --noEmit + freshness + smoke-import）、引擎套件 **987/987 / 0 fail**（含更新后 law-policy 断言，计数对照基线只增不减·纯迁移等值）、`./verify-age.sh` **L1+L2+L2.5 GREEN**（L2 走新路径，真值表 119/0）、mission-check 全 8 个 mission 配置 exit 0（`base.json` = extends defaults 片段无 name/roadmapPath/plansDir——standalone fail 为迁移前既有状，该文件零 diff 佐证非回归）；**真宿主冒烟**（scratch profile `nop-age-mig-audit`）：`dsh plugin add link:` 挂载 → `dsh --profile nop-age-mig-audit --dump-config`（324 行）——`grep -i nop-age` 命中 service row（`id: nop-age` group 行 + `isolate: { nopAge: true }` + `id: nop-age-service` / `name: nop-age`）、`grep -i mission-control` **零命中**（dump 面无技能行——技能面经 e2e 证）；**e2e 真宿主腿 PASS**（`npm --prefix plugin/nop-age run verify:e2e`，keyless 本地 SSE stub）：demo 双腿 shape identity EMPTY diff + markers 全解析 + correction-retry 一次恢复 + onboarding 双形式 parity + `ctx.skills.list()` 携技能三 ID（carve-out 真宿主证据）+ analyze explicit/latest + draft 两段终态 completed + monitor render（oc-/native- 双前缀）；**引擎零 diff 钉住**：`git diff --stat src/engine.js flows/` 为空、`tools/mission-driver/` diff 恰 law 三模块路径字面（law-rules/law-core/law-policy.test）+ CONTEXT.md（owner doc 面，M1 裁定 face (c) 对象非引擎码）；assets 经 `npm run build` 再生 **content-equal**（42 文件 freshness ok，law-*.mjs 副本随引擎源同步）+ `npm ci` 验 lockfile 完整（95 包 1s 装毕，lockfile diff 仅 name ×2 行）；web typecheck/build 绿（dist 零漂移）+ lint:prompts OK；**残险如实留档**：真宿主 boot import 面（bundle patch 包名行加载）因 package.json 无 `main`/`exports` 不可导入——**迁移前同状**（HEAD 旧名版本同样无 main，name-independent；既有全部 runtime 证据走 in-process fixture 相对路径 `../../src/service.ts`），非本迁移回归；M3-WI8 真挂载验证面将正面命中，届时需独立立项补包入口（不在本 plan 机械 token map 内）。③ **Phase 3 回写**——roadmap WI3/WI4/WI5 `[x]` + 行内证据（token map 指针 / 套件计数对照 / grep 收口 / dump 摘录 / install-age 零命中）+ `> Last Updated` 头同步；**WI5 清查证据**：`grep -rn "plugin/dsh" install-age.sh install-age.manifest` **零命中**（live 基线即零，双文件零改动）；grep 收口 live 面（plugin/nop-age + bundle 外引用面 + owner docs + 根级 sh/yml/json）`plugin/dsh`/`dsh-mission-control`/`mdcontrol-service`/`missionControl` 全零命中；历史工件（docs/plans/、docs/analysis/、docs/discussions/、docs/logs/、docs/audits/）零改写（`git diff --stat` 空，本日新增 log 行除外）；`grep -c "^- \[ \]"` = **13** 实测 + roadmap-check exit 0。scratch profile 已清理（remove + 目录删除，`web` profile 未触碰）。
- **multi-plugin-dsh BUILD_VERIFY 批量验证 + 落档提交（plan `2026-08-28-0149-1` / M1-WI1+WI2，纯文档批次）**（mission `multi-plugin-dsh`，full-green verification）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿 · `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **987/987 / 0 失败** + prompt-check OK exit 0；0149-1 `## Verification` pass 行（runId `2026-08-28-104553-mission-driver`，basisHash `ba4610…e4df`）现算 basis 复核一致，无重复追加；分两笔提交——fix（mission plansDir per-mission 纠偏：`missions/multi-plugin-dsh.json` 误携 `docs/plans/age-autonomy`、`missions/demo.json` 宽根 `docs/plans` 收窄 + `docs/plans/demo/.gitkeep` + 0205 事件记录）/ docs（0149-1/2/3 三 plan + 设计文档 AUDITED 五偏差处置 + 三 owner docs 前向引用段与 C1-C3 修正 + roadmap WI1/WI2 回写 + 本日志）。0149-2（M2）/0149-3（M3）保持 draft 待评审派发。
- **multi-plugin-dsh M1-WI2 既有文档同源一致性 + 引用面增补 + 回写（plan `2026-08-28-0149-1` Phase 2，EXEC_PLANS）**：① 三份 live owner docs 各增补 §Multi-Plugin Forward Reference——nop-age 继承声明（单 bundle 文档族 = nop-age 的 as-built 文档；`plugin/dsh/`→`plugin/nop-age/`、`dsh-mission-control`→`nop-age`、`missionControl`→`nopAge` 映射指针 → 设计 doc Concept Mapping；`mdcontrol` / `mission-control-*` 技能 ID / `/mdcontrol/api` 前缀 verbatim 保持 = token-map carve-out）+ nop-route 前向指针（完整 §nop-route 随 M4/M5 落档）。② `docs/backlog/dsh-plugin-roadmap.md` 处置裁定 = **零触碰**（头部/导语无 live-baseline 目录形态声明，均为已收口 mission 史实；6 处 `plugin/dsh` 字面全在 per-WI 史实证据行内；裁定记录在 plan Phase 2 Decision 行下缩进注记）。③ **同源一致性核对矩阵（六维）**：

  | 维度 | 单插件文档族（as-built 面） | 多插件设计文档（target 面） | 结论 |
  | --- | --- | --- | --- |
  | isolate realm 键 | `missionControl`（packaging doc §Packaging Layout / dev guide / service.ts:8，live 真） | nop-age → `nopAge`（目标态） | 一致（as-built vs target 正交） |
  | 服务注册名 `mdcontrol` | bundle patch 唯一挂载一次（packaging §Service Surface / §AGE Preset） | `mdcontrol` 不变（Naming Convention + carve-out） | 一致 |
  | preset 零服务行 | packaging §AGE Preset「ZERO service rows」+ dev guide 注记 + age-preset 结构门 | §Interaction「remains a zero-service-row preset」 | 一致 |
  | 六宿主调用面 | packaging §Dependency「exactly six host calls」（+M4-WI14 roster face 消费注记） | Non-Goals 引用同一枚举；nop-route 零宿主调用 | 一致 |
  | 目录形态 / 支持基线 | `plugin/dsh/` 为 live 挂载路径 | 「single-plugin form remains the supported baseline until this work lands」 | **发现矛盾 → 已修正（C1-C3）** |
  | `mission-control-*` 技能名 | 三技能注册于 ctx.skills（packaging §Service Surface as-built M3-WI12 / integration capability 2 / dev guide） | carve-out：技能 ID 迁移后 verbatim | 一致 |

  矛盾修正清单（原则：单插件文档 as-built 面描述 live 现状保持真，目标态断言只存在于多插件设计文档）：**C1** `dsh-plugin-integration.md` 头部「PLANNED — not yet implemented / Nothing in the Plugin form … supported today」与设计文档「single-plugin form remains the supported baseline」及 live（P1–P4 已交付 2026-08-23）直接矛盾 → 头部改写为 SUPPORTED BASELINE (delivered 2026-08-23)；**C2** 同 doc 双形式表「DSH plugin | Planned」、Concept Mapping 列「(planned)」、「Installing (planned)」+ github 占位安装命令 → 全部翻转为 as-built（link: 本地挂载，与 dev guide §Setup 一致）；**C3** dev guide 头部「forward-looking guide for implementing the plugin」→「operational guide」（已交付）。④ roadmap 回写：WI1/WI2 `[x]` + 行内证据注记（doc-audit 五偏差处置指针 + 一致性矩阵指针）+ `> Last Updated` 头同步。**Proof**：`grep -c "^- \[ \]" docs/backlog/multi-plugin-dsh-roadmap.md` = **16** 实测；`node tools/mission-driver/src/roadmap-check.mjs docs/backlog/multi-plugin-dsh-roadmap.md` exit **0**；`git diff` 历史工件零改写（docs/plans/dsh-plugin/、docs/analysis/、docs/discussions/、docs/logs/ 既有行无变更，本 plan 新增 log 行除外）实测；引擎套件 **987/987 / 0 失败** 复跑绿。
- **multi-plugin-dsh M1-WI1 设计文档 doc-audit 收口（plan `2026-08-28-0149-1` Phase 1，EXEC_PLANS）**：`docs/design/multi-plugin-dsh-architecture.md` 五处 live 偏差全部处置，Status DRAFT → AUDITED——① 迁移面清单入档：§nop-age 新增 **Migration Surface** 三面清单（bundle 内 token 面〔package.json/lockfile、cordis.patch.yml 五 token 含 insert row id、service.ts 注释+mount log、preset/age 注释与提示词、scripts 注释与报错串、test fixtures 路径、assets/ 经 build-bundle 从引擎 law 模块再生〕+ bundle 外功能引用面 8 处〔law-rules.mjs:1391/:1367、law-core.mjs:8、autonomy.policy.yml:124、law-policy.test.js:86、verify-age.sh ×6 行、age-ci.yml:26/:36、age-autonomy-implementation.json:25（+description 提及）、pre-commit:28〕+ owner docs 路径同步面〔CONTEXT.md 14 / packaging doc 17 / dev guide 7 命中，M2 收口时改写〕）+ **技能名 carve-out 成文**（`mission-control-run/draft/analyze` 技能 ID、`/mdcontrol/api` 前缀、cordis 服务名 `mdcontrol` 非 token，裸 `mission-control` 替换会破坏技能 ID）；目录树 scripts/src/test/assets 注释同步修正（"unchanged content" 与 face (a) 矛盾）。② 「引擎零 diff」裁定 = **行为零 diff**，law 三模块路径字面（law-rules/law-core/law-policy.test）随迁移更新，其余 `tools/mission-driver/` 零触碰（备选①字面零 diff 否决：P8 保护集空转 + 套件红自相矛盾；备选②双前缀过渡否决：死条目无消费者）；裁定句写入 Non-Goals + Behavioral Differences + Migration Surface 三处，残险 = M2 plan 须 grep 钉死 diff 边界。③ manifest 分期裁定：M3 仅声明 nop-age，示例标注 M4 后终态，预检存在性语义与分期一致。④ 测试面统一：四张模块名真值表（error-classifier/retry-policy/model-selector 各 ≥10 + routing-core 编排，命名钉死 = 模块名，随 nop-age 包内测试先例）+ e2e 入口（scripts/e2e-noproute.mjs + verify:e2e），目录树/§Scope/成功判据 3/设计不变量段四处同步。⑤ install-age 零字面引用注记已核实（2026-08-28 grep 零命中，M2-WI5 复测）。**Proof**：偏差 1 grep 清单重跑与文档清单零漂移（16 命中逐处对账；CONTEXT/dev-guide/packaging 计数 14/7/17 一致；install-age 零命中）；WI1 六设计面（nop-* 命名/isolate realm 协议/manifest 格式/load-plugins.sh 形态/nop-route 范围/4 路由 + 4 纯函数模块）逐节复核全部有落点且互相一致（realm 键 nopAge/nopRoute 三站点一致、服务名 mdcontrol/noproute 与路由表一致）。**验证**：`pnpm --prefix tools/mission-driver test` **987/987 / 0 失败** + prompt-check OK exit 0（纯文档批次零回归）。
- **multi-plugin-dsh mission plansDir 纠偏 + M1-M3 三 plan 起草（DRAFT_PLANS step，runId `2026-08-27-220026-mission-driver`）**：① mission config 缺陷修正——`missions/multi-plugin-dsh.json` 落地时误携 `plansDir: docs/plans/age-autonomy`（复用已收口 mission 的 plan 目录），与 `prompts/mission-draft.md` per-mission 子目录铁律、设计文档 §Status「plans under `docs/plans/multi-plugin-dsh/`」、`dsh-plugin` 先例三方相悖；实证危害 = `discoverOwningMission`（mission-check.mjs:121-146 首命中即返）把该目录下 plan 对账到 **age-autonomy-implementation roadmap**，M2-WI3+WI4+WI5 / M3-WI6+WI7+WI8 复合标签 work-item 双 deny（M1-WI1+WI2 对旧 roadmap 恰同名 = 假阳性）。②连带修正 `missions/demo.json` `plansDir: docs/plans` → `docs/plans/demo`（宽根吞掉全部 per-mission 子目录的发现序；模板自带 `template/install/missions/demo.json` 本就是 scoped 形态——real-project 副本漂移）+ 建 `docs/plans/demo/.gitkeep`（mission-check 存在性校验）。③三 plan 落 `docs/plans/multi-plugin-dsh/2026-08-28-0149-{1,2,3}-*`（group 同批，执行序 1→2→3）：M1-WI1+WI2 设计文档 doc-audit（起草期已核出五处设计↔live 偏差：迁移面低估 / 「引擎零 diff」语义未裁定 / manifest 分期矛盾 / 测试面三表 vs 四表 / 技能名 carve-out 缺失）+ 既有插件文档同源一致性；M2-WI3+WI4+WI5 迁移 + 零回归 + install-age 清查（bundle 外引用面已全量清点入 baseline）；M3-WI6+WI7+WI8 manifest（分期仅 nop-age）+ load-plugins.sh + 双层验证（stub 确定性 ≥12 例 + 真宿主六面；shellcheck 缺席已列前置项）。**验证**：三 plan `gate-check` 结构面全 `decision: allow` 且 workItem 对账 `multi-plugin-dsh-roadmap.md` 全 ok；`mission-check` demo/multi-plugin-dsh 双绿；`roadmap-check` 绿；引擎套件复跑 **984/984 / 0 失败** + prompt-check OK exit 0（mission config 变更零回归）。三 plan 保持 `status: draft` 待独立评审派发；本 run 引擎已缓存旧 plansDir，REVIEW_PLANS 若空转由下一 run（新配置）接管派发。
- **multi-plugin-dsh BUILD_VERIFY 批量验证 + 落档提交（plan-2026-08-27-2122-3 / M5-WI49）**（mission `multi-plugin-dsh`，full-green verification）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿 · `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **984/984 / 0 失败** + prompt-check OK exit 0；分三笔提交——feat 引擎包（monitor 健壮性 + CLI 管道截断 + prompt 措辞退役 + mission-check extends 合并读 + missions json audits map 删除，tools/mission-driver + missions）/ feat 插件包（assets 同步 8 文件 + engine-bridge/law-truth-table 测试同步，plugin/dsh）/ docs（architecture ×3 + roadmap 回写 + 本日志 + 2122-3 plan）；2122-3 `## Verification` pass 行（runId `2026-08-27-220026-mission-driver`，basisHash `a00a18aa…a126bda8`）现算 basis 复核一致，随 docs 批入库。
- **M5-WI49 收口：R4/R5/R6 P2 Follow-up 一站式清偿（14 项，plan `docs/plans/age-autonomy/2026-08-27-2122-3-m5-wi49-r5-r6-p2-followup-cleanup.md` 六 Phase 全勾）**：① **Phase 1 monitor 健壮性**——`handleStartRun` targets 校验前置 `mkdirSync`（400 零 ghost runDir 残留）+ `sliceFile`/`serveStaticFile`/`serveIndex` 三处裸 `readFileSync` 补 try/catch → 404（TOCTOU 并发删不崩 monitor）+ `config.js` resolveConfig mission 分支 `mkdirSync(runDir)` 移除、创建延迟至 **orchestrateRun 共享 run 入口**（落点注记：plan 原文写「cmdRunMission(main.js 调用点)」，实际落点选 orchestrator.js orchestrateRun——它是 main.js cmdRunMission 与 plugin engine-bridge（runNativeMission/beginNativeMission）共享的 run 调用点，双路径同保 runDir 创建且 list-steps/analyze/draft 只读面零残留；字面落 main.js 会使插件 native-run 路径丢失 run-state.json 写入目录），monitor.test.js +4 例（WI49-1 ghost / WI49-2 EISDIR 404 / WI49-3 chmod-EACCES 404（root skip）/ WI49-4 list-steps 零 _tmp），插件 engine-bridge.test.js 一处旧 mkdir pin 翻转为新契约 pin（resolveConfig 不建目录）；② **Phase 2 Decision = 备选 B**（执行指令未显式选 A，plan 默认生效——零引擎 diff 底线优先）：engine.js `_shouldCompleteOnAuditQuota`/`when` 求值 try 守卫 + `runner.runTool` timeoutMs 转发两子项零代码落地，转 watch-only residual（06 G 缺口机制 retirement-gated；roadmap 两行注记含指针 plan `## Deferred But Adjudicated`；`git diff --stat engine.js runner.js` 为空实测）；③ **Phase 3 CLI 管道截断**——roadmap-check/plan-check/gate-check stdout JSON 面 `process.exit` → `process.exitCode` 赋值 + return（gate-check 同型全位点 14 处一并收口含 --verify/--law 成功出口；mission-check 成功出口复核本就 console.log 后自然排空零改动；usage/console.error stderr 面按 plan Deferred 不动），新 `test/cli-pipe-truncation.test.js` 5 例（>64KB payload 四校验器管道完整投递 + exit code 正反钉），本 roadmap 管道实测 **97,819B 完整投递 = 重定向逐字节一致 + JSON.parse OK + exit 0**（旧缺陷：恰 65,536B 截断）；④ **Phase 4 prompt 措辞退役**——multi/open-audit.md:23「still readable by the engine's legacy channel」→「prose-only history since the legacy audit channel was retired in M2-WI22; no engine consumer」+ draft-from-roadmap.md:40「consumed by the DRAFT_FROM_AUDIT path」→「no longer consumed by any path」，lint:prompts OK + prompts grep 零命中 + assets 副本经 build-bundle 同步（42 文件 freshness ok）；⑤ **Phase 5 文档/mission config 残面五处**——baseline.md:120 test-seams 仅列 `SCRIPT_REGISTRY`（owner-doc grep 零命中；全 docs/ 残命中均为 logs/plans/audits/requirements/backlog 历史史料 append-only 不可清洗）+ packaging.md:348 law 边登记债改「ALLOWED_MODULES + closure 门机械守护，免除批内枚举」+ module-boundaries.md:10 六模块真实扩展名（.js×3/.mjs×3 混居注记）+ `checkRoadmapUniqueness` 改经 `loadMission` extends 合并读（extends 继承 roadmapPath 的 mission 现入 claims map；不传 projectRoot——存在性校验非本面职责）+ mission `audits` map 三 dead 键整段删除（live `prompts` 两键存活钉），mission-check-cli.test.js +2 例 + 插件 truth-table boundary 用例 fixture 补 commands.test 随 merged 读写法同步；⑥ **Phase 6 回写**——roadmap Follow-up 14 项 `[x]` + 指针注记 + WI49 行勾选 + `> Last Updated` 头同步；勾选后实测列 0 `grep -c "^- \[ ]"` → **0** + 缩进域 `rg -c '^  - \[ \]'` → **0** + roadmap-check exit 0。**验证**：引擎 973→**984/984**（+4 monitor / +5 pipe / +2 mission-check）+ 插件 **423/423** + `git diff --stat engine.js runner.js` 全程为空 + 零新增 npm 依赖。
- **multi-plugin-dsh BUILD_VERIFY 批量验证 + 落档提交（plan-2026-08-27-2122 批）**（mission `multi-plugin-dsh`）：四命令全绿——`pnpm --prefix tools/mission-driver/web run typecheck` 绿 · `web run build` 绿（dist 零漂移）· `lint:prompts` OK · `pnpm --prefix tools/mission-driver test` **973/973 / 0 失败** + prompt-check OK exit 0；2122-1 `## Verification` 追加本 run pass 行（runId `2026-08-27-220026-mission-driver`，basisHash `b4b1eaeb…c12d3ec` 现算一致）；分三笔提交——feat（policy `limits.maxAuditRounds` 3→8 双域头注 + law-policy live 断言同步 + CONTEXT.md「:7 先例」事实修正）/ docs（2122-1/2/3 三 plan + 02 §4.6 / 06 行 3·G7 设计文档 + age-autonomy roadmap WI47 勾选回写与 R5/R6 回执 + 08-27/08-28 日志）/ feat（multi-plugin-dsh mission 三件套：`missions/multi-plugin-dsh.json` + `docs/backlog/multi-plugin-dsh-roadmap.md` + `docs/design/multi-plugin-dsh-architecture.md`）。2122-2（WI48）/2122-3（WI49）仍为 drafted active，归后续执行轮。
