# Development Log - 2026-07-01

### 2026-07-01

- mission-driver CLI 重构为 k8s 风格子命令 `run <mission>` / `draft <desc>` / `list [missions|steps]`。
  - `src/main.js` `parseArgs`：原 `--list-missions`/`--list-steps`/`--draft-mission` 是命令伪装成 flag（`config.js` 的 `--list-missions` 在配置解析阶段直接 `process.exit(0)`），改为显式动词分发；`--step`/`--dry-run`/`--agent`/`--model`/`--max-*` 仍为 `run` 的修饰符。裸定位参数不再隐式等于 run（破坏性变更），须 `run <mission>`，否则报错并提示。
  - `src/config.js`：移除 `resolveConfig` 内 `process.exit(0)` 副作用与 draft 分支，拆出纯函数 `resolveProjectRoot`/`resolveMissionsDir`/`resolveCommon`，新增 `resolveDraftConfig`；resolver 仅 throw，输出与分发归 main.js；新增 `-h/--help` 与未知命令友好提示。
  - 同步：`mission-driver.sh.example`、`package.json`（scripts 加 `run` 前缀）、`design/mission-design.md`（Phase 1/2 示例 + §9/§11）、`tools/README.md` 示例改新 CLI。
- **help 体系 k8s 化（registry 驱动）**：`src/main.js` 引入 `COMMANDS` registry 作单一真相源（每条 = `args/summary/description/options/handler`），命令清单由 `Object.keys(COMMANDS)` 派生。新增真子命令 `help`：`help`→顶层、`help <cmd>`→专属；`<cmd> --help` 按命令上下文路由到专属帮助（不再全局短路）；`printUsage`/`printCommandHelp` 从 registry 渲染（加命令只改一处=自动发现）。顺带修掉 `help` 被误判未知命令、提示 "use run help" 的 bug。
- 验证：`npm test` = 77 pass/0 fail；`main.js` CLI 自检（`--help`/`list steps`/未知命令 exit 2/裸调用 exit 2）符合预期。下游 `nop-app-erp` 经其 `tools/mission-driver.sh` launcher 端到端验证跑通新 CLI。
- 下游影响：`nop-app-erp` 删除其本地 fork（与本仓已分叉），改为纯 launcher 引用本仓。
