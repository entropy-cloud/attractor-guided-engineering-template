# Development Log - 2026-07-27

### 2026-07-27 (afternoon) — onboarding mission

- **Plan executed: onboarding mission** (`docs/plans/2026-07-27-0100-onboarding-mission-plan.md`, Plan Status: completed). Replaced meaningless `echo demo-ok` only mission with a real onboarding mission that drives AI to read consumer codebase and fill copied template docs.
  - Phase 1: Created `tools/check-install-age.sh` (persisted closure-gate test, backfills previous plan's missed deliverable — `tools/check-install-age.mjs` was attempted first but hit Windows node-spawn-bash path issues; refactored to `.sh`). Created `template/docs/backlog/onboarding-roadmap.md` (8 WIs: project-scan, project-context, ai-autonomy-policy, codebase-map, index.md, architecture/*, app-workflow + backlog/README, known-good-baselines + logs/{year}/). Edited `install-age.sh`: added `TEMPLATE_COPIED` array tracking fill-in files (manifest entries with `template/` prefix), expanded sed-replace to iterate TEMPLATE_COPIED instead of just AGENTS.md (so all fill-in files get `<project-name>` → project-name substitution, but shared methodology guides are NOT touched — protects literal `<project-name>` references in those guides). Added `missions/onboarding.json` heredoc + `mkdir docs/plans/onboarding`. Updated NEXT STEPS to 6 steps (smoke demo → personalize onboarding → manual fallback).
  - Phase 2: `pnpm check:install` 10/10 assertions PASS; `mission-check.mjs` validates onboarding.json (`{"valid": true}`); `./tools/mission-driver.sh list` from real-target install `/c/Work/...` shows `base + demo + onboarding` (3 missions). `/tmp`-target shim resolution fails due to Git Bash mount quirk (relative path traversal across mount boundary); real-target paths work — acceptable scope limit.

- **Implementation lesson**: node's `execFileSync('bash', ...)` on Windows has multiple interacting issues — PATH entries without drive letters, backslash path mangling in spawn args, Git Bash mount-point relative-path quirks. For closure-gate tests that need to spawn bash from node on Windows, prefer writing the test as `.sh` (native bash) over `.mjs` (node spawns bash) to avoid the spawn-loop. Recorded as future-consideration for `tools/check-*` family design.

### 2026-07-27 (morning) — template-realproject split

- **Plan executed: template-realproject split** (`docs/plans/2026-07-27-0000-template-realproject-split-plan.md`, Plan Status: active → closure pending). All 4 phases landed:
  - Phase 0: Phase 0 grep returned exactly 16 fill-in files (incl `START-HERE-after-copy.md`); scanned `.opencode/` confirmed no `opencode.json` (D6 resolved: Option 4 not available, Option 3 proceeds); decisions D1/D5/D7/D8 recorded in plan.
  - Phase 1 + Phase 2 (atomic — no intermediate commit): `git mv` 16 fill-in files + 2 dual-audience (README.md, README.zh.md) + START-HERE-after-copy.md to `template/` (18 total); added deprecation header to `template/START-HERE-after-copy.md` pointing at `install-age.sh`; deleted 3 D8 files at root (`docs/backlog/implementation-roadmap.md`, `docs/design/{domain-design-guidelines,flow-overview}.md`); wrote new root `AGENTS.md` (real-project contract, 190 lines, opens with `mission-driver (self-hosting AGE workspace)`, new `## Dual-Audience Repo` + `## In-Tree Tool` sections), new root `README.md` + `README.zh.md` (dual-audience), new root `DEVELOPMENT.md` (this repo's own dev process).
  - Phase 3: `install-age.manifest` 15 entries gained `template/` prefix; `install-age.sh` got the 5-line B4 fix patch (strip `template/` prefix at target via `dst_line="${line#template/}"`; push `$dst_line` to `COPIED`/`SKIPPED` arrays instead of `$line`); sed-replace block at `:121-127` unchanged. **Closure-gate test all 5 assertions PASS**: `./install-age.sh /tmp/age-test-XXXX TestProject` → AGENTS.md personalized (`<project-name>` count = 0), TestProject present, shared methodology copied from root, fill-in files copied from `template/`.
  - Phase 4: filled 11 root docs (project-context, ai-autonomy-policy, codebase-map, 4×architecture, process/application-development-workflow, backlog/README, index, testing/known-good-baselines) + new `docs/architecture/template-vs-realproject-boundary.md` (D4 — architecture truth, prevents re-litigation).

- **Verification status** (per Plan Authoring Guide "scoped verification" rule):
  - ✅ Phase 3 closure-gate test: 5/5 PASS
  - ✅ `pnpm --prefix tools check:docs-garbled`: 121 files scanned, 0 garbled
  - ✅ `pnpm --prefix tools check:doc-references`: 2 NEW issues introduced by my changes (index.md:40 dangling ref to deleted `domain-design-guidelines.md`; system-baseline.md line-number refs to `package.json:5`/`:6`) — **both fixed**.
  - ⚠️ `pnpm --prefix tools check:doc-references` residual: 10 PRE-EXISTING errors (`docs/architecture/mission-driver-baseline.md` 8 backtick refs to `src/*.js` without `tools/mission-driver/` prefix; `docs/references/document-naming-and-timeliness.md` 2 refs to nonexistent bug examples) — **not regressions**, predate this plan.
  - ⚠️ `pnpm --prefix tools/mission-driver test`: 549 pass / 7 fail in `test/runner-routing.test.js` (assertion `'run' !== 'ses_main'` about opencode session ID extraction) — **PRE-EXISTING**, confirmed via `git stash` + retest on HEAD before my changes. Untouched by this plan (no engine/test code modified).
  - ⚠️ `pnpm --prefix tools check:oversized-code-files`: 8 files >700 lines (engine.js 2015, monitor.js 1866, main.js 946, vendored commander files, test files) — **PRE-EXISTING** and expected per `docs/architecture/project-vision.md` "Small core, deep tests — engine.js is allowed to be large because the state machine is irreducibly complex". No new oversized files introduced.

- **Key decision recorded**: `docs/architecture/template-vs-realproject-boundary.md` now owns the architecture truth for why this repo has a `template/` subdirectory. Editing rules documented: methodology changes → edit root + template/ in lockstep; real-project-only changes → root only; template-only changes → template/ only. Verification commands included.

- **Plan audit trail**: proposal v4 (`docs/analysis/2026-07-27-0000-template-realproject-split-proposal.md`) survived 4 rounds of independent audit. Plan draft survived 1 round of independent draft review (2 blocking + 5 non-blocking, all fixed). Next: independent closure audit.

- Next step: independent closure audit by subagent; if `closure accepted`, plan promotes to `Plan Status: completed`; ask user about commit strategy (atomic Phase 1+2 commit + separate Phase 3 + Phase 4 commits, per plan B1 fix).
